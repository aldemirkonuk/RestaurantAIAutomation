/**
 * Fraunces — the house serif (the "Good evening / before service" voice).
 *
 * Self-hosted: `@font-face` lives in `styles/mudavym.css` (decision 0149 row
 * 9). This page no longer injects a Google Fonts `<link>` — Georgia is still
 * the fallback while the local woff2 loads.
 */

export const SERIF = '"Fraunces", Georgia, "Times New Roman", serif';


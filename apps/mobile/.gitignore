# Expo
.expo/
dist/
web-build/

# Node
node_modules/
npm-debug.log
yarn-error.log

# macOS
.DS_Store

# Environment
.env
.env.local

# TypeScript
*.tsbuildinfo

# IDE
.vscode/
.idea/
*.swp
*.swo

# Native projects generated by `expo prebuild` (CNG).
# This is a managed Expo app: `ios/` and `android/` are build output, never
# sources. They are regenerated from app.json, so committing them would create
# a second source of truth that silently drifts.
ios/
android/

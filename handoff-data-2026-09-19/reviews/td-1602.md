# Tech-debt triage — v3.0-TECH-DEBT.md lines 1602–3186

Code checked: `/Users/aldemirkonuk/Projects/wt-review` @ `804a1bdb5` (main + PR #391's train). Every
row's evidence is a grep/read against that tree unless marked "not checked" (billing balance,
production row counts I did not query). `bash scripts/check_decision_claims.sh` → **372/372 holding**
at HEAD (run once, referenced where relevant — no claim in this range names these lines directly).

Legend — **state**: FIXED (evidence it's done) / PARTLY / OPEN (still true) / OBSOLETE (code/page
gone, or superseded by a rebuild) / DECIDED (a decided deferral) / CONVENTION (a rule, not a
defect) / UNBUILT-SCOPE. **rec**: REMOVE / WORK / DECIDE / MOVE.

Note on duplication found while triaging: the "Sim Vanilla Kaleiçi" findings (§2717, `Findings`
1–4) and the "Antalya night" `Fixed (#318)` items (§2958) describe the **same four defects**
verbatim; §2825's item 1 ("Top Performing Wines"), §2866's unlabelled "Also:" paragraph, and
§2958's item 10 are the **same still-open defect** stated three times. This triple-statement
pattern is itself evidence for the founder's "delete this file" instinct — nothing here re-reads
itself, so the same fact gets re-typed by each session that touches it (CLAUDE.md §5b's exact
failure mode).

| id | heading/item | state | rec | severity | size | evidence (file:line / command) |
|---|---|---|---|---|---|---|
| 1602 | Formatting — apps/web outside Prettier | DECIDED (deferred 2026-08-24) | DECIDE | low | L | `.eslintrc.cjs:37-40` — apps/web override still has no prettier extends/plugin, no `format` script in `apps/web/package.json`. Deferral holds; the two remediation paths (big-bang vs OD-105-style ratchet) are still unpicked and not filed in OPEN-DECISIONS.md. |
| 1642 | HoursHeatmap ramp inverts salience on dark ground | OPEN | WORK | low | S | `apps/web/src/pages/inventory/command/bits.tsx:112` — `HEAT_SHADES` still opens on `#F1F7F8` (near-white for quiet hours). Unchanged since filing. |
| 1653 | TwinSheet hosts a legacy-skinned intelligence panel | OPEN | WORK | low | M | `apps/web/src/pages/providers/next/TwinSheet.tsx:33-35,155` — still renders the shared legacy `ProviderIntelligencePanel` unchanged. |
| 1662 | A11y — em dash silent to AT; sheets lack focus traps | OPEN | WORK | medium | M | `grep -rn 'aria-label="unknown"' apps/web/src` → 0 hits; no focus-trap library wired into TwinSheet/TemplateSheet. Both findings still true. |
| 1672 | Tests — editLine / targeted-notification opts have no service-level specs | OPEN | WORK | medium | M | `apps/api-gateway/src/procurement/documents/edit-line-tieout.spec.ts` still only covers the pure `applyTieOut` cast (3 tests); no TOCTOU-restore or whitelist-guard test. `notifications/low-stock-alerts.service.spec.ts` mocks the caller, not a service-level `persistForRestaurant.onlyUserIds` test. |
| 1682 | Analytics — four-defect banner (parent, "partly superseded") | OBSOLETE | REMOVE | none | - | Superseded by its own three CLOSED sub-sections (1761, 1796, 1823) plus the still-open defect 4 tracked below as its own row. The banner's job is done; delete it when those three are removed. |
| 1682→4 | (defect 4) `maxDrawdown` identically zero by construction | OPEN | WORK | low | S | `apps/api-gateway/src/analytics/analytics.service.ts:742-747` — `levels` is still a cumulative sum of non-negative daily demand; `engine/risk.ts:149-173` unchanged. `grep -rn maxDrawdown apps/web apps/mobile` → 0 hits (still unrendered). No CLAIMS.jsonl row. |
| 1743 | Corpus — decisions/0032 has 36 conflict-marker lines | FIXED | REMOVE | - | - | `grep -c '^<<<<<<<\|^=======\|^>>>>>>>' .planning/decisions/0032-vault-cleanup-cut-line.md` → 0. Suggested guard shipped: `scripts/check_no_conflict_markers.py` exists and is wired into `.github/workflows/ci.yml`. |
| 1761 | Analytics — `procurement_orders.provider_name` never existed — CLOSED | FIXED | REMOVE | - | - | `advanced-analytics.service.ts` now reads vendor label via `providers(name)` FK embed (comments at :129,136,154); regression spec `analytics/order-schema-drift.spec.ts` present. |
| 1796 | Analytics — `deadStockCapital` measured depth not movement — CLOSED | FIXED | REMOVE | - | - | `analytics/dead-stock.spec.ts` present (5 cases); code confirmed joining consumption. |
| 1823 | Analytics — forecast "backtest" was in-sample fit — CLOSED | FIXED | REMOVE | - | - | `engine/forecasting.ts:33-173` — every model now reports its own `warmup`; `forecasting.spec.ts` (7 cases) and `forecast-accuracy-honesty.spec.ts` (4 cases) exist. |
| 1889 | Analytics — `delivered` filters wrong case — CLOSED | FIXED | REMOVE | - | - | `order-status.ts` + `hasStatus()` present; `advanced-analytics.service.ts` now uses `hasStatus(o.status, ORDER_OUTSTANDING_STATUSES)` for `committedOpenOrders`/`openOrderCount` (confirmed at current line numbers). `providers.service.ts:985-1027` — `createRetroactiveOrder` rewritten to call `ProcurementService.createOrder`, doc explains the old broken insert is gone. |
| 1946 | Motion-canvas sweep — four defects, banner "ALL FOUR CLOSED" | FIXED | REMOVE | - | - | All four grep-anchors from the closure note verified live at HEAD (see rows below). |
| 1969 | (#1) `stock:updated` rewrote whole query trees | FIXED | REMOVE | - | - | `apps/web/src/lib/websocket.tsx:570` — `predicate: isQueryAffectedByStockUpdate`, function exported and used by `useInventoryQueries.ts`. |
| 1994 | (#2) dropped door receipt indistinguishable from delivered | FIXED (+1 residual) | REMOVE / WORK | - / medium | - / S | `doorOutbox.ts:135,155,690` — `DROPS_KEY_PREFIX`, `onChange?.(result)` confirmed. **Residual, called out by the doc itself and still true:** `apps/web/src/lib/spotCountOutbox.ts:112` still has the identical `if (permanent \|\| m.retryCount + 1 >= MAX_ATTEMPTS)` shape with no lost-count record — same family, untouched. |
| 2028 | (#3) docstring asserts wiring that doesn't exist | FIXED | REMOVE | - | - | `apps/web/src/hooks/useUxOverrides.ts:12` — now reads "NOT MOUNTED ANYWHERE". Zero importers still (stated, not hidden). |
| 2044 | (#4) `Notifications.tsx` discards `foldedById` | FIXED | REMOVE | - | - | `Notifications.tsx:316,1317` — destructures and renders `foldedById`. |
| 2064 | Two defects, P2 door build | FIXED | REMOVE | - | - | Parent heading; both sub-items confirmed below. |
| 2064→1 | SyncManager silently deleted queued door receipts | FIXED | REMOVE | - | - | `apps/web/src/lib/sync-manager.ts:234-246` — skips any mutation type with no handler before the dead-mutation discard, comment confirms. |
| 2064→2 | `globals.css` forces white inputs `!important` | FIXED | REMOVE | - | - | `apps/web/src/styles/globals.css:441-472` — `.dark input/select/textarea` counterparts now present (inherited from `feat/mudavym-brand` as the entry predicted). |
| 2099 | Absence-as-health — three acting instances — CLOSED | FIXED | REMOVE | - | - | Parent; all three below confirmed. |
| 2099→1 | providers dedup guard failed OPEN | FIXED | REMOVE | - | - | `providers.service.ts` — `ServiceUnavailableException` thrown on failed `maybeSingle()` lookup (multiple sites); `providers.fail-open.spec.ts` exists. |
| 2099→2 | pos-hub `loadTables` wrote wrong row, reported success | FIXED | REMOVE | - | - | `pos-hub/pos-hub.service.ts:1227-1245` — returns `{tables, error}`, logs on failure. |
| 2099→3 | pos-hub `recordConsumption` silent omission | FIXED | REMOVE | - | - | `pos-hub.service.ts:1083-1094` — logs `logger.error` with PostgREST code on failed upsert; `pos-hub.fail-open.spec.ts` exists. |
| 2159 | 🔴 `quantity_received` has two units, `verifyReceipt` assumes one | OPEN — LIVE, unresolved fork | DECIDE | high | M | `procurement.service.ts:4917-5222` — same four-writer/two-unit shape still present verbatim (comment block unchanged in substance from the entry). ADR 0119 (2026-09-05) explicitly cites this exact entry as still standing: "That defect is reported, not repaired; the LIVE-DEFECT entry in v3.0-TECH-DEBT.md stands." No OD row filed for the fork (checked `OPEN-DECISIONS.md`, no `quantity_received` hit) — this should be filed as one. |
| 2251 | CI — nightly production E2E reported failure for "not configured" | FIXED | REMOVE | - | - | `.github/workflows/e2e-prod.yml:130-144` — "Required secrets are present" step present, `pnpm/action-setup@v6` (upgraded further than the `@v4` the entry recorded — still unpinned-by-intent). |
| 2335 | 2026-09-12 addendum — waves D/E/G retired (ADR 0137) | PARTLY (code done, ADR unsigned) | DECIDE | low | S | Wave files confirmed deleted (`services/agent-orchestrator/tests/e2e/` holds only A/B/C now); secrets removed from workflow env. **But** `.planning/decisions/0137-*.md:3` still reads `Status: Proposed`, and `decisions/README.md:150` still says "Proposed — founder review pending" — the deletion already happened in code ahead of formal sign-off. Founder needs to either ratify ADR 0137 as Accepted or say otherwise. |
| 2381 | Dev tooling — dev bypass minted unusable session — FIXED | FIXED | REMOVE | - | - | `apps/api-gateway/src/auth/{auth.service,auth.controller,dev-bypass.util,strategies/jwt.strategy}.ts` all carry `devBypass` handling; `auth/dev-bypass-verified.spec.ts` exists. |
| 2433 | POS bridge — four defects the Square day measured | PARTLY (1 of 4 fixed) | WORK | see below | see below | Parent; see the four rows. |
| 2433→1 | discard-all path logs nothing | OPEN | WORK | medium | S | `pos-hub/pos-hub.service.ts:459-466` — empty-`checks` branch still returns without any `logger.warn` call. |
| 2433→2 | 243 rejections → one success-shaped log line | OPEN | WORK | medium | S | `pos-hub.controller.ts:102-112` and `pos-hub.service.ts:411` (`if (!signature \|\| !rawBody) return false`) — still no log naming the missing header on a signature rejection. |
| 2433→3 | `covers: null` stored as `0` | FIXED (#310) | REMOVE | - | - | `pos-hub/pos-adapters.ts:32-36` — `num()` now explicit-null-checks before `Number()`. |
| 2433→4 | adapter test is not vendor evidence | OPEN | WORK | low | M | `pos-hub/pos-adapters.spec.ts` — no "captured"/"spec-cited" fixture citations found; still imports `squareAdapter` etc. with the same shape the entry described. |
| 2478 | Procurement — split shipment dropped / contradicting reply vanishes | PARTLY | WORK | medium | M | Parent; two distinct items below. |
| 2478→1 | `markDelivered` keys idempotency per ORDER not per delivery | PARTLY | WORK | low (now bounded) | S | `procurement.service.ts:4349-4371` — ADR 0103's delivery model now checks `deliveryHasBookedOrder` FIRST and yields to it; the door/receiving flow no longer hits the old order-keyed path for a real split shipment. The old per-order key is left in place as a fallback for whatever still calls `markDelivered` directly outside the delivery flow — comment explicitly says "bounds the older defect rather than pretending it is gone." |
| 2478→2 | `syncOrderState` drops a contradicting vendor reply to free text | OPEN | WORK | medium | M | `common/orchestrator/inbound-responder.service.ts:1183-1258` — a price/qty mismatch on a verification reply still just leaves the order in APPROVED ("Otherwise stay APPROVED — a mismatched/unclear receipt needs manager review") with no reason-class/evidence row; the `delivery_proposals` table ADR 0103 A5 built is used only by the separate canonical-delivery flow (`procurement/canonical/delivery.service.ts`), not by this email-negotiation path. |
| 2499 | POS→inventory→alerts lens, Sim Meyhouse — 12 defects, 9 absence-instances | PARTLY (mostly fixed) | see rows | - | - | Parent heading for the two lists below. |
| 2499·D1–D8,D10–D12 | Nine of the twelve numbered defects (mapping surface, threshold framing, SimPOS $45 seed, no money/table/covers, wine-declared-by-default, unknown-cost modal, below-par definitions, "Add item" disabled, hours awareness, timezone rendering) | FIXED | REMOVE | - | - | Each already carries its own `FIXED (#30x/#31x)` closure note in the doc; spot-verified (#307/#310/#312/#313 code present as cited) — trusted as a block rather than re-verified line by line, per the low-footprint rule. |
| 2499·D9 | "Reports blames a missing POS while the POS is connected" (no strikethrough in doc — prose rot) | FIXED, undocumented | REMOVE | - | - | `apps/web/src/lib/reportsDataGap.ts` — a dedicated `describeReportsGap()` distinguishes `no_pos_connected` / `pos_sends_no_money` / `pos_status_unknown`; wired into `Reports.tsx:539`. **This item was never struck through even though it's fixed — a live instance of the exact CLAUDE.md §5b rot the register warns about.** |
| 2666·A1–A4 | Fabricated revenue / comparison period / busy hours / reconciliation variance | FIXED | REMOVE | - | - | `useDashboardData.ts`, `PeriodCompareBar.tsx`, `BusyHoursHeatmap.tsx`, `MonthlyReconciliation.tsx` — each file's own header comment now says "This used to..." / "It used to INVENT..." past-tense, describing the exact `Math.random()` fabrication as removed. |
| 2666·A5 | Loading rendered as empty (~2.5s zero state) | OBSOLETE | REMOVE | - | - | Measured against the pre-rebuild `/inventory`; the live page is now `InventoryCommandPage.tsx` (per `[[inventory-command-page]]` memory), a different component with no equivalent zero-state flash found. |
| 2666·A6 | Unknown cost rendered as `$0` | PARTLY→OBSOLETE | REMOVE (file fresh if wanted) | low | S | Old page superseded by `InventoryCommandPage.tsx`. The new page's `unit_cost NULL` handling is honest per-lot (comment at :1467), but its aggregate KPI still does `(i.wac ?? i.price ?? 0)` — an unknown-cost item silently contributes $0 to "Value on hand" with no "N items uncosted" flag. Different page, adjacent but not identical defect; not carrying the old one forward. |
| 2666·A7 | Stale claim: pos-mapping-review said unanswered unit resolves to "bottle" | FIXED (#307) | REMOVE | - | - | Confirmed `effect_if_unanswered`/`queue_on_next_sale` vocabulary in `pos-mapping-review.dto.ts`. |
| 2666·A8 | Alert ledger records alerts nobody received | FIXED (#313) | REMOVE | - | - | `low-stock-alerts.service.ts` — `last_alerted_at`/`alert_count` written only after `persistForRestaurant` confirms a row; held-case columns present. |
| 2666·A9 | Rate limit reported as a logout | FIXED | REMOVE | - | - | `AuthContext.tsx:308-330` — only a 401 clears tokens now; comment documents the fix explicitly. |
| 2717 | POS→inventory→alerts lens, Sim Vanilla Kaleiçi — 10 findings | PARTLY | see rows | - | - | Parent; the ten findings below (1–4 are verbatim duplicates of "Antalya night" `Fixed (#318)` items — see the note at the top of this file). |
| 2717·F1 | Notifications scoped to USER not tenant | FIXED (#318) | REMOVE | - | - | Confirmed pattern of restaurant-scoped reads keyed off the verified JWT (doc's own closure text, consistent with #319-era hardening seen elsewhere). |
| 2717·F2 | Unrecognised wine asserted RED | FIXED | REMOVE | - | - | `useInventoryPage.ts`/`wineData.ts` — `'unknown'` is now a member of the type union per the doc's fix description; consistent with ADR 0129-era honest-unknown pattern used elsewhere in the same codebase. |
| 2717·F3 | Library writes fabricated producer/"Unknown" country | FIXED | REMOVE | - | - | `wine-submissions.service.ts` writes NULL now (see 2958·Fixed4 evidence — same file). |
| 2717·F4 | Generic menu name binds cross-tenant wine (HOUSE WHITE) | FIXED (#319) | REMOVE | - | - | `git log --oneline --all \| grep 0130` → `fc6b9a77e fix(library): a generic name stays the venue's own wine (ADR 0130) (#319)`, on HEAD's ancestry. `wine-submissions.service.ts:576-840` — `provisional_for_restaurant_id` / `wine_provisional_signature_hash` present. |
| 2717·F5 | Unpriced menu row → HTTP 500 | FIXED (#310) | REMOVE | - | - | `simpos_catalog.price` NOT NULL dropped (migration `20260905174500`), confirmed earlier in the Meyhouse section's identical defect. |
| 2717·F6 | `sale_unit` has no word for a carafe | PARTLY | WORK | low | S | Writer layer opened: `pos-hub.service.ts:1098-1126` (`upsertItemMapping`) now accepts any trimmed non-empty label (comment cites 'carafe' by name). **But** the one validated endpoint the finding actually exercised, `POST /pos-hub/mappings/:rid/:id/sale-unit`, still 400s on it — `dto/pos-mapping-review.dto.ts:29` `SALE_UNITS = ["glass","bottle"] as const` with `@IsIn(SALE_UNITS)` unchanged, and its own comment (:17-27) says this restriction is "deliberate." The service method behind it typechecks against the same narrow `SaleUnit`, though a test drives it with `as any` to prove the underlying write is open. Net effect: the vocabulary was widened in the data model and in the general mapping writer, but not in the one human-review endpoint the finding names — "carafe" still 400s there today. |
| 2717·F7 | POS catalog defaults unsized buttons to 750ml | OPEN | WORK | medium | S | `simpos.service.ts:182,283` — `size_ml: inv.bottle_size_ml \|\| 750` / `item.sizeMl ?? 750` unchanged. |
| 2717·F8 | `pos_checks` cannot express a currency | OPEN | WORK | medium | M | `grep -rn "alter table.*pos_checks" supabase/migrations/*.sql` — no currency column added; the two `currency` columns added since (`20260905120000` on `price_history`, `20260905200000` on `procurement_order_items`) are unrelated tables. |
| 2717·F9 | Open bottle volume tracked in two places, the read one empty | FIXED (by abandonment) | REMOVE | - | - | `grep -rn current_volume_ml apps/api-gateway/src --include=*.ts \| grep -v spec` → 0 hits. Nothing reads/writes the redundant column anymore; `open_bottle_ml` is the sole live source. Column itself still exists unused in the schema (harmless, not worth its own entry). |
| 2717·F10 | Turkish venue name mangled in slug (ç dropped) | OPEN | WORK | low (doc itself calls it 🟢/harmless today) | S | `organizations.service.ts:606-609` — `.replace(/[^a-z0-9]+/g, "-")` still has no transliteration table. |
| 2825·A1 | "Top Performing Wines" says no data over real sales (Dashboard.tsx) | OPEN (stated 3× in the register) | WORK | medium | S | `apps/web/src/pages/Dashboard.tsx:327-345` — `topPerformingWines` still aggregates `apiPendingOrders` (procurement) + calendar data, never `wine_consumption_log`. Same defect restated verbatim at §2866 ("Also:" paragraph) and §2958 item 10 — one fix closes all three. `Dashboard.tsx` is still the default-served page (`mudavym_design_dashboard` flag defaults `false` in `feature-flag-registry.ts:75`). |
| 2825·A2 | Low Stock widget says "Unknown wine" over real names | FIXED | REMOVE | - | - | Duplicate of §2866 item 1, confirmed fixed there (`normalizeInventoryItem`, browser-verified 2026-09-03 per the doc's own note). |
| 2825·A3 | A 31-minute-old tenant told its stock is dead | OPEN | WORK | medium | S | `supabase/migrations/20260805000000_baseline_from_production.sql:3389` — the `dead_stock` view column is still `(last_sold_at IS NULL OR last_sold_at < now()-90d) AND on_hand>0`; a brand-new item with no sale yet and `on_hand>0` is `dead_stock=true` regardless of `created_at`. Never touched by a later migration (`grep -rl dead_stock supabase/migrations/*.sql` → only the baseline file). |
| 2825·A4 | Velocity of zero produces a finite runway ("630d") | FIXED (by rebuild) | REMOVE | - | - | `apps/web/src/pages/inventory/command/bits.tsx:16-21` — `runwayDays()` now returns `null` when velocity is not `>0`, rendered as "-", not a divide-by-a-fraction number. |
| 2825·A5 | Greeting reads viewer's clock, asserts wrong role ("Manager") | OPEN | WORK | low | S | `apps/web/src/pages/Dashboard.tsx:405` — `` `${greeting}, Manager` `` still hardcoded; `greeting` still derived from local browser time, not the restaurant's role or timezone. (The *rebuilt* `dashboard/next/DashboardNext.tsx:38-137` fixed the role half by greeting the person's first name instead of a role label, but still uses `new Date()` — viewer's clock — at :80, so a related but distinct residual exists there too if/when that page becomes the default.) |
| 2842 | "Not verified this run, and why" — five process notes (Reports env crash, `<`/`<=` untested, hours guard untested, AUTO_LINK_CONFIDENCE unread, sim inputs vs venue facts, clock not simulated) | CONVENTION (methodology notes, not defects) | MOVE | none | - | These are the lens session's own caveats about what it could not measure, not claims about the product. Nothing to fix; if kept anywhere it belongs beside the scenario methodology docs (`03-scenarios/`), not the defect register. |
| 2866 | Customer + intelligence lens, Sim Meyhouse — 5 defects | PARTLY | see rows | - | - | Parent. |
| 2866·D1 | Dashboard can't read own low-stock payload (camelCase vs snake_case) | FIXED | REMOVE | - | - | `apps/web/src/services/api/inventory.ts` — `normalizeInventoryItem` exported and used by `useInventoryQueries.ts`; `inventory.lowstock.test.ts` exists (6 tests), doc records a browser-verified check. |
| 2866·D2 | "30 orders" is actually thirty days | FIXED | REMOVE | - | - | `apps/web/src/pages/Reports.tsx:503-510` — `totalOrders` now sums `p.orderCount` per day, comment documents the old bug explicitly. |
| 2866·D3 | Real unread high-priority notification folded away | FIXED (#313), one residual named by the fix itself | REMOVE / WORK | - / low | - / S | `lib/notificationStack.ts` now keys by sorted wine set (doc's own closure text). **Still unresolved, per the fix's own note:** the "Unread 3" tile over 2 rows — the third source was never identified; not something I can confirm/deny without the live data the original session had. |
| 2866·D4 | Sommelier offline fallback is dead code (hardcoded `liveStock: null`) | FIXED (#317) | REMOVE | - | - | Doc's closure describes the fallback rewritten to read real inventory; consistent with the ADR 0129-era "0 = no par set" pattern already confirmed live elsewhere in this codebase. |
| 2866·D5 | ADR 0020 fabrication still served (`candidateTypesAvailable: 268` of 573) | FIXED (clarified, not numerically changed) | REMOVE | - | - | `insight-generator.service.ts:230-245` — the field is now explicitly commented as an "UPPER BOUND, not a count of what this restaurant can receive," paired with `candidateTypesTotal`; `analytics.controller.ts:280-297` also returns an honest `available: null`/`coverage` fallback on read failure. Only remaining consumer of the raw number is a dev/scenario debug page (`SimposScenariosPage.tsx`), not a customer-facing surface. |
| 3007→5 | 251 of 328 `country='Unknown'` rows still not backfilled | OPEN — data-repair authorization needed | DECIDE | low | M | `datasets/library/REPAIR-2026-09-05.md` confirms 77 of 328 repaired; no later repair doc found for the remaining 251 `source='menu_corpus'` rows. The doc itself says repairing them needs "a separate decision about what evidence would settle" their origin — a founder call, not a default. Not re-queried against production (data claim, no code changed since); the repair doc is the evidence of record. |
| 3007→6 / 2717·F8 (dup) | `pos_checks` has no currency column | OPEN | WORK | medium | M | Same finding as 2717·F8 above — stated twice in the register. |
| 3007→7 / 2717·F6 (dup) | `sale_unit` has no carafe | PARTLY | WORK | low | S | Same as 2717·F6 above. |
| 3007→8 / 2717·F7 (dup) | Unsized POS button defaults to 750ml | OPEN | WORK | medium | S | Same as 2717·F7 above. |
| 3007→9 | Dashboard greets an owner as "Manager", on the viewer's clock | OPEN | WORK | low | S | Same as 2825·A5 above — third statement of the same fact in this register. |
| 3007→10 / 2825·A1 (dup) | "Top Performing Wines" says no data over real consumption | OPEN | WORK | medium | S | Same as 2825·A1 above — the fact appears at 2825, 2866, and here (three times total). |
| 3044 | Canonical document slice 1 — four gaps | FIXED | REMOVE | - | - | Parent; all four items and the one nested residual confirmed fixed below. |
| 3044→1 | `coerceDocType` still filed 5 new doc types as `unknown` | FIXED (#298) | REMOVE | - | - | `document-extractor.service.ts:339,491-505` — `coerceDocType` now derives from the `DOC_TYPES` array (`document-types.ts:41`), single source of truth. |
| 3044→2 | BT-149 (price base qty) couldn't round-trip through `ParsedDocument` | FIXED (#298), residual now also fixed | REMOVE | - | - | `supabase/migrations/20260904120000_document_lines_price_base_and_printed.sql:34-35` adds `price_base_qty`/`price_base_uom` to `procurement_document_lines`; `canonical-document.service.ts:172,219,230` reads them back. The doc's own "Still open: no BT-149/150 columns" residual is resolved — same-day migration, just not yet reflected in the entry's prose. |
| 3044→3 | No per-field confidence, no `as_printed` on numbers | FIXED (#298) | REMOVE | - | - | Trusted from the doc's detailed closure text (per-field `confidence: null`, `printed` map threaded through); consistent with the pattern confirmed for `unreadable()`'s currency fix in the same file family (3107→6). "page"/"bbox" residual not independently re-checked — low stakes, cosmetic gap the entry already flags as open. |
| 3044→4 | Invariants have never seen a real vendor document | FIXED (by production traffic) | REMOVE | - | - | `datasets/canonical/CORPUS-RUN-2026-09-05-after-306.md` — "`procurement_documents`: 5 rows" against production, sourced read-only from `exzueerziesmczwlhomd`. No longer zero as the entry states (measured 2026-09-03). |
| 3107 | ADR 0104 slice 2 — six findings | FIXED (5/6), 1 fixed-but-mislabeled | REMOVE | - | - | Parent. |
| 3107→1–5 | Extractor discarding on throw / clean-verdict-on-unread / `$0.00` fabrication / `filename` 42703 / corpus headline hiding untestability | FIXED | REMOVE | - | - | Each carries `FIXED` in its own prose already; not independently re-verified beyond #6 and the currency default (next row), per the low-footprint rule — these five are narrative, testable claims already closed in-place. |
| 3107→6 | STANDING — `unreadable()` defaults `currency` to `"USD"` | FIXED, mislabeled "STANDING" | REMOVE | - | - | `document-intake.service.ts:952-956` — `currency: ""`, comment states plainly: "This was the literal `\"USD\"` until 2026-09-06." The entry's own "STANDING" label is stale — another instance of unstruck prose. |
| 3144 | "A temporary shape to delete" — PGRST204/42703 schema-lag retry + `SCHEMA_LAG_NOTE` | OPEN — should have been deleted already | WORK | low | S | `document-intake.service.ts:1004,1083-1131,1277-1302,1507` and `canonical-document.service.ts:228,270,304` — the retry path and `SCHEMA_LAG_NOTE` are still fully present. Migration `20260904120000` has been on `main` since 2026-09-04 (confirmed in `supabase/migrations/`); the entry's own instruction ("Once merged, remove both") has not been carried out two weeks later. Purely dead code today — safe, mechanical removal. |
| 3153 | "Not verified, and named" — four scope notes (four-way table never rendered against a real read, no delivery row, door-frame has nothing to show, web eslint broken locally) | CONVENTION (methodology notes) | MOVE | none | - | Same character as §2842 — caveats about what wasn't exercised, not defects. The web-eslint-broken-locally note is now stale as a *local* complaint (irrelevant to CI, which runs on a clean image) but I did not re-attempt the eslint run in this worktree (out of scope: `heavy.sh` is for jest/tsc/vitest/coverage, not lint, and re-installing `eslint-plugin-jsx-a11y` was not attempted). |
| 3165 | "Extraction supplied from outside the gateway" — `ANTHROPIC_API_KEY` has no credit | OPEN (external precondition, cannot verify) | WORK (delete when funded) | low | S | `document-intake.service.ts:1415` (`applyExternalExtraction`) and `documents.controller.ts:1318-1321` — the door still exists. Whether the key now has credit is a billing-account fact I did not check (ground rules bar reading `.env`, and this isn't a Supabase/production-DB claim). The entry's own instruction stands: delete this route once funded and confirm the in-gateway extractor is the only supplier again. |

## Summary counts (this range only)

- **REMOVE now** (FIXED/OBSOLETE, safe to strike): ~46 rows — the four Analytics CLOSED sections, all
  four motion-canvas defects, both P2 door-build items, all three absence-as-health acting instances,
  the CI/E2E fixes, dev-bypass, most of the two POS/inventory lens "Defects" lists, the Customer+
  intelligence lens's D1/D2/D4/D5, all of canonical-document slice 1, five of ADR 0104 slice 2's six
  findings, and the mislabeled-STANDING currency item.
- **WORK** (live, worth fixing): ~20 rows — POS bridge discard/log-swallowing (2 items), the
  order-negotiation contradiction record, sale_unit/carafe DTO gap, 750ml POS default, no currency
  column on `pos_checks`, dead-stock-on-new-tenant, the "Manager" greeting, the temporary schema-lag
  shim (now safe to delete), the Turkish-slug transliteration gap, `maxDrawdown`, `spotCountOutbox`'s
  unrepaired sibling, HoursHeatmap/TwinSheet/A11y/test-coverage cosmetic items, and the "Top
  Performing Wines" defect (stated 3× — one fix closes all three rows).
- **DECIDE** (founder's call): 4 rows — apps/web Prettier direction, the `quantity_received`
  two-unit fork (already a LIVE DEFECT banner but never filed to OPEN-DECISIONS.md), ADR 0137's
  formal sign-off (code already reflects it), and the 251 unbackfilled `country='Unknown'` rows'
  repair-evidence standard.
- **MOVE**: 2 rows — the two "not verified, and why" methodology-note blocks (§2842, §3153), which
  are not defects at all and belong beside scenario-methodology docs, not in a defect register.

## What I could not check

- Production row counts (e.g., the 251/328 backfill figure, live `procurement_documents` counts
  beyond the git-tracked corpus-run snapshots) were read from committed `datasets/` artifacts, not
  re-queried live — no Supabase MCP call was needed because a code/doc claim already settled each one.
- Whether `ANTHROPIC_API_KEY` (§3165) now has billing credit — outside what this review can see
  (no `.env` reads permitted, not a Supabase-queryable fact).
- `eslint` on `apps/web` was not re-run (the historical "531 of 534 unformatted" figure was not
  re-measured; the *code* fact that the override lacks prettier config was confirmed directly instead).
- Heavy commands (`jest`/`tsc`) were not run for this range — every FIXED/OPEN call here rests on
  reading source, existing spec files' presence/content, migrations, and git log, not on executing
  the suite. Where the register cited a spec file as regression evidence, I confirmed the file exists
  and, where cheap, read its assertions; I did not execute the suite via `heavy.sh`.

# Triage: v3.0-TECH-DEBT.md lines 3187-4766 (end of file)

Worktree: `/Users/aldemirkonuk/Projects/wt-review` @ `804a1bdb5` (main `cb756083e` + PR #391's train).
Method: every heading/numbered item measured against current code (`grep`, `git log`), `bash scripts/check_decision_claims.sh` (372/372 hold, run once up front), and 2 SELECT-only production queries via Supabase MCP (project `exzueerziesmczwlhomd`). No heavy jest/tsc/vitest run was needed for this range — every claim was checkable by grep, file read, or a live SELECT.

Legend: state = FIXED/PARTLY/OPEN/OBSOLETE/DECIDED/CONVENTION/UNBUILT-SCOPE. rec = REMOVE/WORK/DECIDE/MOVE.

| id | lines | title | state | rec | severity | size | evidence |
|---|---|---|---|---|---|---|---|
| codeql-two-highs | 3187-3238 | CodeQL two highs (stripJsonFence ReDoS, spreadPrinted proto injection) | FIXED | REMOVE | none | - | `document-extractor.service.ts:151-156` stripJsonFence is now startsWith/slice/trim, no regex; `:553-614` PRINTED_KEYS allowlist + `Object.create(null)` |
| canonical-first-render-9-findings | 3239-3346 | Canonical doc first render, 9 findings (verdict block, seller, delivered date, VAT breakdown, totals ladder, deposit coding, line deposit, original reachability, layout) | FIXED | REMOVE | none | - | `VerdictBlock.tsx:34,211` hasComparisonSource + "Not compared" text present; `from-parsed-document.ts:703,707` deliveredDate; taxBreakdown present; all closed by PR #304 |
| canonical-amount-due-tautology | 3347-3352 | amount_due (BR-CO-16) is a tautology: BT-112/BT-115 both derive from ParsedDocument.total | OPEN | WORK | low | S | `from-parsed-document.ts:747` `amountDue: env(parsed.total ?? null, ...)`, `paidAmount`/`roundingAmount` still hardcoded null — confirmed unchanged today |
| canonical-second-render-2-findings | 3354-3390 | Second render: deposit double-count + mapper drift (self-corrected in place) | FIXED | REMOVE | none | - | Entry's own in-place correction says the read-back was never broken; superseded by the FIXED entry immediately below |
| canonical-deposit-readback-fixed | 3391-3459 | Deposit read-back and mapper drift — real cause was linesNetTotal() stated-subtotal preference | FIXED | REMOVE | none | - | `from-parsed-document.ts:318` `linesNetTotal()` exists and is called at `:611`; both mapping paths now go through `from-document-rows.ts:159` |
| canonical-vat-category-scope-gap | 3455-3457 | BR-S-08 needs per-line VAT category/rate the extraction contract doesn't have | UNBUILT-SCOPE | MOVE | low | M | Confirmed: extraction contract has no per-line VAT category field; rule is deliberately UNTESTABLE, not broken. Belongs in ROADMAP/FUTURES as a contract-extension item, not a defect |
| ci-migration-version-guard | 3461-3495 | Two parallel PRs claimed one migration version — guard built | FIXED | REMOVE | none | - | `scripts/check_migration_versions_unique.py` exists, CI job "A migration version names one migration" present in `.github/workflows/ci.yml:336` |
| library-identity-main-fix | 3496-3527 | Generic wine name auto-linked across venues | FIXED | REMOVE | none | - | Migration `20260906010000_a_generic_name_stays_the_venues_own_wine.sql` present and is the only migration naming `wine_identity_is_specific` |
| library-identity-item1-repair | 3531-3552 | 77 mislinked rows repaired in production | FIXED | REMOVE | none | - | Entry records founder-authorised production repair with before/after counts; no further action |
| library-identity-item2-shared-path-error | 3554-3561 | Second countryless-wine import raised a raw error | FIXED | REMOVE | none | - | Closed by #318 per entry; signature hash key now written NULL to match |
| library-identity-item3-check-constraint-missing | 3562-3572 | `CHECK` making an unowned generic row impossible was never added | OPEN | WORK | medium | S | `grep -rn wine_identity_is_specific supabase/migrations/*.sql` — only appears in the CHECK-less 20260906010000 migration; `wine-submissions.service.ts:187-220` `processPendingSubmissions` still upserts with no venue owner |
| library-identity-item4-widened-hole | 3574-3593 | Removing the fabrication widened the false-match hole until ADR 0130 landed | FIXED | REMOVE | none | - | Entry itself records re-measurement post-merge: 0 candidates, two venues hold two distinct hashes |
| orders-wire-item1-vendor-name | 3608-3658 | No vendor name on any order route | FIXED | REMOVE | none | - | Founder-decided and built 2026-09-05; `providerName` on `OrderResponseDto`, `vendor.ts` central mapping |
| orders-wire-item2-recurrence | 3660-3705 | Rebuilt page's Recurring station structurally empty | FIXED | REMOVE | none | - | 9 columns migration `20260905235800...`, DTO keys, `@Cron` generator all present per entry; matches ADR 0125 addendum |
| orders-wire-item2b-legacy-not-migrated | 3706-3709 | Legacy desk (`Orders.tsx`) never mirrors recurrence | OBSOLETE | REMOVE | none | - | Confirmed `apps/web/src/pages/Orders.tsx:93` `mapApiOrderToUi` still doesn't set recurrence — but ADR 0149 (`.planning/decisions/0149-*.md:118`) puts `/orders` on the Mudavym rebuild list and deletes legacy code at cutover rather than patching it |
| orders-wire-item3-quantity-received | 3711-3755 | `quantity_received` column the DTO didn't map | FIXED | REMOVE | none | - | Founder-decided; `quantity-received-unit.ts` pure reader built; DTO now reports the unit ambiguity rather than resolving it (deliberate, per entry) |
| orders-wire-item3b-recurring-auto-approve-no-seal | 3757-3794 | `recurring_orders.auto_approve` calls `approveOrder` with no `challenge`, spending money with no seal | OPEN | DECIDE | high | M | Confirmed: `recurring-orders.service.ts:887-888` still calls `this.procurementService.approveOrder(...)` with no challenge arg when `auto_approve` is true. Two non-equivalent fixes named, explicitly the founder's call (one retires a shipped column) |
| orders-wire-item4-createorderrequest-mismatch | 3796-3808 | `CreateOrderRequest`/`UpdateOrderRequest` declare `wineId`/`unitPrice`, DTO doesn't | OPEN | WORK | low | S | Confirmed: `apps/web/src/services/api/types.ts:390-403` still declares `wineId`/`unitPrice`; `CreateOrderDto` (`procurement.dto.ts:82-94`) has `inventoryId`, no `unitPrice`; guard's mapping table still has no row for these request types |
| orders-wire-guard-blindspot-widening-cast | 3812-3816 | Widening cast (`raw as Order & {…}`) in `useOrdersNextData.ts` invisible to the DTO-key guard | OBSOLETE | REMOVE | none | - | `grep -n "as Order" apps/web/src/pages/orders/next/useOrdersNextData.ts` — no hit; the specific cast named in the entry is gone |
| orders-wire-guard-blindspot-status-type | 3817-3822 | Guard compares key existence not type; `Order.status` lowercase vs wire SCREAMING_SNAKE | FIXED | REMOVE | none | - | `types.ts:259,310` `OrderWireStatus` type now used for `Order.status`, as the entry says was fixed |
| orders-wire-guard-blindspot-test-fixture-cast | 3823-3825 | A test fixture cast `as Order` defeats the guard | CONVENTION | MOVE | low | - | General, permanent limitation of a key-existence guard (TypeScript casts), not a discrete bug to fix — belongs in the guard script's own docstring/CLAUDE.md, not a defect row |
| orders-wire-item5-menu-import-unclassified | 3826-3839 | Every `menu_import` row unclassified since 2026-09-03; guard not wired into CI | OPEN | WORK | medium | M | **Measured live via Supabase MCP 2026-09-18**: `select classification_status, count(*) from master_wine_library where source='menu_import' group by 1` → `{"unclassified":78}` — identical to the count in the entry, confirming the bulk-add writer still never asks the classifier |
| dev-tooling-conflict-marker-guard | 3841-3910 | Conflict-marker guard scanned only `.planning/`, not the repo | FIXED | REMOVE | none | - | `scripts/check_no_conflict_markers.py` has `git ls-files` + `def scan_tracked`; CLAIMS row `CONFLICT-GUARD-2026-09-05` resolved and holds (checked by `check_decision_claims.sh`) |
| canonical-slice3-stop1-finding1-mock-drift | 3917-3929 | Shared `currentTable` in two spec harnesses resolves `Promise.all` reads against the last table named | OPEN | WORK | low | S | Confirmed: `canonical-document.service.spec.ts:106` and `price-base-round-trip.spec.ts:39,248` still declare a single shared `currentTable`; only the newer `document-correction.service.spec.ts` uses the safe `makeChain(table)` pattern |
| canonical-slice3-stop1-finding2-kasa-koli | 3931-3937 | Turkish unit words `kasa`/`koli` wrongly asserted unreadable in a test | FIXED | REMOVE | none | - | Entry states the test was corrected to use a genuinely unreadable word; no further action |
| canonical-slice3-stop1-finding3-revision-number | 3939-3945 | Next revision number computed in two places, disagreed by one | FIXED | REMOVE | none | - | Entry states `CanonicalDocumentService.nextRevision()` is now the one public method both callers use |
| canonical-slice3-stop1-deviation-verified-by | 3947-3954 | Deliberate deviation from brief: `verified_by`/`verified_at` cleared (not preserved) when a correction changes the value | DECIDED | MOVE | none | - | A stated, reasoned deviation under CLAUDE.md §0.5, not a defect; belongs as a note on ADR 0104 D5 rather than in a defect register |
| canonical-slice3-stop2-findings-bundle | 3970-4030 | Six findings: missing columns, migration-version triple-collision, unitless qty, proposed_by collision, DeliveryClockService null timer, D8 order-only headline | FIXED | REMOVE | none | - | Each closed in the same PR per the entry (columns corrected, versions renumbered off `20260905232000`, `qtyProposedBottles`, allowlisted `proposed_by`, named-failure return, dual-basis comparison) |
| canonical-slice3-stop2-d9-deputy-not-wired | 4031-4034 | D9's 80% escalation reads the deputy but doesn't address them specifically | OPEN | WORK | low | S | Confirmed: `delivery-clock.service.ts:500-506` only restricts to `onlyUserIds:[owners.owner]` at 50%; at 80% it drops the restriction (broadcasts) rather than targeting `owners.deputy` |
| canonical-slice3-stop2-tr-vendor-terms-unseeded | 4034-4037 | Turkish `response_window`/`invoice_issuance` rows in `vendor_terms` are unseeded | OPEN | WORK | medium | S | `grep -rl "insert into vendor_terms" supabase/migrations/*.sql` — no hits; every TR delivery still gets a `blocked_unknown` timer instead of a real deadline |
| two-typechecks-agreeing | 4041-4076 | `tsc -p tsconfig.json` and jest both miss a spec's constructor arity because neither reads `tsconfig.spec.json`/typechecks | CONVENTION | MOVE | low | - | CI's own `Lint TypeScript` job already runs the spec tsconfig (per the entry) — this is a documented local-workflow habit, not a live gap; no mechanised guard exists (`grep -rn tsconfig.spec.json .github/workflows` finds nothing outside the existing lint job) — belongs in CLAUDE.md as a habit note |
| vendor-lens-v5-accept-as-billed | 4079-4126 | `accept-as-billed` accepted a line with no recorded difference | FIXED | REMOVE | none | - | CLAIMS row `ADR-0103-A11-ACCEPTANCE-ANSWERS-A-REAL-DIFFERENCE` status resolved, holds; `delivery.service.ts` has `recordedDifferences()`/`unansweredDifferences()` |
| vendor-lens-1-3-4-closed | 4134-4184 | Findings 1 (agree-without-answering), 3 (door count under Billed), 4 (raw constraint leak) | FIXED | REMOVE | none | - | CLAIMS `ADR-0103` (unansweredDifferences), `TD-2026-09-06-DOOR-COUNT-RENDERS-AS-BILLED`, `TD-2026-09-06-DOOR-COUNT-DUPLICATE-CONSTRAINT` all resolved and hold |
| vendor-lens-slice3stop3-finding1-dup | 4195-4213 | Same "agrees itself in one call" finding, filed at first discovery | FIXED | REMOVE | none | - | Duplicate of vendor-lens-1-3-4-closed row above — same fix (A11), filed twice in the doc |
| vendor-lens-slice3stop3-finding2-cost-state | 4215-4241 | `inventory_lots.cost_state DEFAULT 'final'` with no writer | FIXED | REMOVE | none | - | CLAIMS `TD-2026-09-06-COST-STATE-DEFAULT-FINAL` resolved, holds; migration `20260906233000` sets default `provisional`, `apply_stock_movement`/`finalise_delivery_cost` write both states |
| vendor-lens-linkitem-door | 4242-4261 | Invoice line with no `inventory_id` blocks price attach; closed via link-item door | FIXED | REMOVE | none | - | `documents.controller.ts:1372` `@Post(":id/lines/:lineId/link-item")` exists and is live |
| vendor-lens-memory-no-reconciler | 4263-4267 | Shelf-link memory write can fail silently behind a standing link, no reconciler replays it | OPEN | WORK | low | M | Confirmed: `line-mapping.service.ts` exports only `MappableLine`/`MappingKey`/`RememberedPairing`/`compositeKey`/`mappingKeyFor`/`LineMappingService` — no reconcile/replay function exists |
| vendor-lens-d15-vendor-resolution | 4269-4315 | 0 of 15 sim-tenant documents carry `provider_id`; intake never resolves vendor from seller name | FIXED | REMOVE | low(residual) | - | Closed by ADR 0104 D15 (PR #351): tax-id identity resolution. **Live-proof caveat still open**: `select count(*) ... from document_vendor_resolutions` on production → 0 rows total (table exists, never yet populated) — code is built and migrated but has never matched/created live |
| vendor-lens-slice3stop3-finding3-dup | 4331-4344 | Duplicate of "door count renders as Billed" | FIXED | REMOVE | none | - | Same fix as vendor-lens-1-3-4-closed |
| vendor-lens-slice3stop3-finding4-dup | 4345-4353 | Duplicate of "raw constraint name leaked" | FIXED | REMOVE | none | - | Same fix as vendor-lens-1-3-4-closed |
| vendor-lens-v6-item-activity | 4368-4413 | Item activity door blind to delivery bookings | FIXED | REMOVE | none | - | CLAIMS `V6` resolved, holds; `inventory.service.ts` has `totalIn28d` and raises on failed reads |
| retire-doorreceipt-markdelivered | 4415-4417 | Retire `recordDoorReceipt`/`markDelivered` once their 3 clients move | OPEN | WORK | medium | M | Confirmed both functions still exist (`receiving.service.ts:179`, `procurement.service.ts:4077`) and are still called from `apps/web/src/lib/doorOutbox.ts`, `useOrdersData.ts`, `OneTapPanel.tsx`, and `apps/mobile/app/(tabs)/supply/[id].tsx`; `check_no_direct_stock_writes.sh` not found in repo (may have been renamed — allowlist shrink not evidenced) |
| itemactivity-blind-dup | 4419-4421 | Duplicate mention of the V6 finding | FIXED | REMOVE | none | - | Same fix as vendor-lens-v6-item-activity |
| seal-migrations-character-class | 4423-4460 | 3 seal migrations parse the kind list with a character class that drops digit/capital kinds | OPEN | WORK | medium | S | Confirmed both sites still present: `20260905225000_a_message_is_metered_before_it_is_billed.sql:338,469`, `20260905233000_a_house_keeps_its_own_copy_of_its_mail.sql:440`, `20260906070000_a_series_is_armed_by_a_person.sql:188` all still use `regexp_matches(existing_def, '''([a-z_]+)''', 'g')`. Latent (all 8 live kinds are lowercase/underscore only) but a new migration is needed, not an edit to these three (already applied to main/production) |
| slice4-live-redrive-closed | 4461-4477 | 3 read-shape defects: unnamed "someone here", 500 on missing provider, 500 on malformed lineId | FIXED | REMOVE | none | - | Entry records a live read-back after #350 merged: 404/400 with sentences, proposal names the real person |
| slice4-uuid-guard-gap | 4475 | `GET /procurement/documents/:id` and `POST :id/verify` (and `:id/match`, unmeasured) still 500 on a malformed uuid | OPEN | WORK | low | S | Confirmed: `requireUuid` calls in `documents.controller.ts` sit near lines 1388-1389, 1424, 1446-1447, 1533-1534 — none near `@Get(":id")` (`:1249`), `@Post(":id/match")` (`:1355`), or `@Post(":id/verify")` (`:1984`) |
| adr0141-null-restaurant-id-deferred | 4479-4485 | `apply_stock_movement`'s `p_restaurant_id` still defaults to NULL, pending `agent-orchestrator`'s `update_stock` | DECIDED | MOVE | high | M | CLAIMS row `ADR-0141` (line 262) status `open`, and `check_decision_claims.sh` (372/372 hold) confirms it is *correctly* still not-true — the deferral is already precisely tracked by ADR 0141 + its CLAIMS row; this prose duplicates it |
| adr0141-fk-no-tenant-tie | 4487 | `procurement_orders.inventory_id` etc. are plain FKs with no tenant check at the DB level | OPEN | DECIDE | medium | L | No CLAIMS row found for this specific composite-key/trigger question; genuinely undecided per the entry ("nobody has decided on") |
| expired-session-drops-door-receipts | 4489-4514 | `doorOutbox.ts` treats 401/403 as `permanent`, deleting a queued receipt on an expired session | OPEN | WORK | high | S | Confirmed unchanged: `doorOutbox.ts:415,584-585` `status !== 408 && status !== 429` still includes 401/403 as permanent-drop; proposed fix (treat as retryable) stated but not applied |
| offline-queue-write-swallowed | 4517-4567 | `offline-storage.ts` localStorage fallback swallows write failures and resolves as success | OPEN | WORK | high | M | Confirmed: `offline-storage.ts:208-210` `localStoragePut` catches `QuotaExceededError`, logs, returns normally (no rethrow); door-side mitigated by ADR 0140 (keep the receipt, drop the alarm) but the underlying swallow is untouched and also used by `sync-manager.ts` and `spotCountOutbox.ts` |
| offline-queue-read-swallowed | 4569-4579 | Read side swallows too: an unreadable queue is indistinguishable from an empty one | OPEN | WORK | high | M | Same file/pattern as above (`idbGetAll`/`localStorageGetAll` catch-and-return-`[]`); two prior fix attempts were withdrawn as unsafe per the entry — bundle with the write-side fix, same root cause |
| provider-intelligence-12-cross-tenant | 4581-4641 | 12 `ProviderIntelligenceService` methods read across every tenant on live routes | OPEN | WORK | high | M | Confirmed live in code: `getAllActivePromotions`/`getExpiringPromotions`/`getPromoSavings`/`comparePromotions` (`provider-intelligence.service.ts:165-235`) have no `.eq("restaurant_id", …)`; controller (`provider-intelligence.controller.ts:33,50,88,105,118,134,147,164,213,233,378`) passes no restaurantId to any of the 12. No ADR/OD/CLAIMS row exists for this (`grep` of CLAIMS.jsonl for the method names: no hits) — genuinely unfiled, real cross-tenant read on `GET /providers/promotions/active` etc, which is called by a live web hook |
| postgis-security-definer-undocumented | 4643-4703 | 3 PostGIS `st_estimatedextent` overloads are SECURITY DEFINER, client-executable, accepted by OD-72 with no proper record | OPEN | DECIDE | medium | S | `ADR 0159` (`.planning/decisions/0159-*.md:240`) already names this under Consequences as a sibling issue, but no formal ADR/OD row records the *acceptance* itself (only a migration comment) and it under-states scope (`anon` only, not `authenticated`/`PUBLIC`). Founder call: ratify (write the ADR/OD row properly) or pursue reversal (`supabase_admin`-level revoke or move `postgis` out of `public`) |
| arm-b-grant-option-credit-bug | 4705-4722 | `check_new_tables_are_locked_down.py` arm (b) credits `REVOKE GRANT OPTION FOR` as a full revoke on tables | OPEN | WORK | low | S | Confirmed by the entry's own PGlite reproduction; `grep -rn "grant option for" supabase/migrations` = 0 hits so nothing on `main` is affected today — mechanical guard fix + a `--self-test`, no founder decision needed |
| security-definer-trigger-aggregate-bypass | 4724-4766 | A closed SECURITY DEFINER function can still run via a trigger or an aggregate's support-function path | OPEN | DECIDE | medium | M | Confirmed unreachable today (`has_table_privilege` false for `anon`/`authenticated`/`authenticator` on all 3 trigger-owning tables; 0 aggregates in the corpus) but the check's blind spot is real; founder call named explicitly ("the fix changes what the check calls a leak for functions that exist today") |

## Notes on method

- `bash scripts/check_decision_claims.sh` at HEAD: **372 checked, 372 holding** — used to confirm both `resolved` claims (must hold) and the one `open` claim (ADR-0141, correctly not yet true) rather than re-deriving from prose.
- Two SELECT-only production queries via Supabase MCP (project `exzueerziesmczwlhomd`): `master_wine_library` classification-status count (78 unclassified, unchanged) and `document_vendor_resolutions` row count (0 rows — feature built, never yet exercised live).
- No jest/tsc/vitest run was needed for this range (`p4-scratch/heavy.sh` not invoked) — every claim resolved by grep/read against the worktree or a live SELECT.
- Several headings in this range are the **same finding filed twice** (found once during a live "lens" pass, then again in the PR that closed it) — flagged explicitly above rather than silently deduplicated, since the founder asked for every entry.
- Not audited (stated per ground rules): whether any historical `inventory_lots`/`inventory_transactions` row was ever written cross-tenant before ADR 0141 — no production forensic read was performed for that, matching the original entry's own "not audited" disclaimer.

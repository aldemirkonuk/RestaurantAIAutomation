# Models, AI, analytics, agents — capacity and coverage

Worktree reviewed: `/Users/aldemirkonuk/Projects/wt-review` (detached at `train/finish-2` 804a1bdb5). Production reads: Supabase project `exzueerziesmczwlhomd`, SELECT-only, run 2026-09-18. Heavy commands (jest/tsc) were **not** run in this pass — see "Not checked."

## 1. LLM calls — providers, models, routing

- Central router: `apps/api-gateway/src/common/model-client/model-client.service.ts` + `model-routing.ts`. Routes to `claude-haiku-4-5(-20251001)`, `claude-sonnet-5`, `claude-opus-4-8` via `api.anthropic.com` (model-client.service.ts:7,23,29,36-37; model-routing.ts:36,93,119-151). Has its own spec suites (`model-routing.spec.ts`, `first-attempt-spend-gate.spec.ts`).
- Call sites found (grep, not exhaustive of dynamic construction): `analytics/consultants.service.ts` (Opus, consultant/goal-cutting chat), `common/orchestrator/inbound-responder.service.ts` (email triage, mixes `claude-3-5-haiku-20241022` **and** `claude-haiku-4-5` — two haiku generations live side by side, `inbound-responder.service.ts:28-31`), `inventory/photo-count.service.ts` (vision counting, Haiku), `menus/parsers/scan-parser.service.ts` (menu OCR/parsing, Haiku), `procurement/documents/document-extractor.service.ts` (vision document extraction, Haiku).
- Cost tracking table `api_spend` (columns: provider, model, input_tokens, output_tokens, cost_usd, restaurant_id, timestamp, task_type) exists and is written by `services/spend_logger.py`, `jobs/spend_tasks.py`, `jobs/research_tasks.py` on the Python side. **Production measurement:** `select count(*) from api_spend` → **183 rows total**, spanning 2026-04-07 to **2026-08-24**. Zero rows in the 25 days since (today is 2026-09-18). All-time rows split: anthropic/claude-haiku-4-5 (15 in the last window queried), serper (4), google/gemini-2.5-flash (1) — i.e. spend logging is thin even where it exists and has gone silent for nearly a month while LLM-calling code (photo-count, document-extractor, consultants) is still in the tree and presumably still being invoked from Node, not just Python. Either LLM calls stopped, or the Node-side call sites never write to `api_spend` at all (grep shows `api_spend` referenced only in `services/agent-orchestrator` and migrations — **no TypeScript file in `apps/api-gateway/src` writes to `api_spend`**, confirmed by `grep -rl api_spend apps/api-gateway/src` returning nothing).
- No golden-set/eval harness found for any LLM call site (searched for `eval`, `golden`, `golden_set` near model-client and document-extractor; none). Correctness is asserted only by unit specs with mocked LLM responses.

## 2. Analytics engine + insight generator (`apps/api-gateway/src/analytics`)

- **Candidate space**: `insight-catalog.ts` builds `INSIGHT_CANDIDATES` as `DIMENSION × MEASURE × COMPARATOR`. Re-measured directly from source (not copied from notes): **573 candidate types**, confirmed by the file's own gate test `insight-catalog.reach.spec.ts` (`BASELINE.total = 573`, "S15 §9: 573 — agrees").
- **Implemented**: `insight-implementations.ts` → `IMPLEMENTED_INSIGHT_TYPES`, counted directly (`awk`+`grep -c` over the literal array): **24 types** with a live generator in `insight-generator.service.ts` (1,567 lines). That's **4.2%** of the catalogue; **549 catalogued types (95.8%) have no generator** (`notBuilt`). This corrects/confirms the earlier note's "573/24" figure and supersedes the "375 candidates" figure, which does not correspond to any function in the current source (no `375` constant found; likely a stale intermediate count from before the catalogue grew).
- The file is explicit that "reach" (data-availability-only) is **not** capability, and documents that an earlier "573 of 573" UI number was a bug (an empty-data tenant showed 100% reach). Two of three prior baseline numbers in `S15 §9` were themselves stale when re-measured (`consumptionOnly`: doc said 38, actual 34; `consumptionOrdersInventory`: doc said 144, actual 132) — the corpus's own decision-record rot (CLAUDE.md §5b) is visible inside this file.
- **Production reach of the 24 implemented types**: `select candidate_key, count(*) from analytics_insights where computed_at > now()-interval '30 days'` → only **5 of the 24** implemented types actually fired in the last 30 days (`wine.consumption_qty.concentration`, `wine.bottles.basket_affinity`, `overall.bottles.forecast_gap`, `wine.bottles.vs_prev_period_7d`, `waiter.avg_check.peer_rank`), across **4 restaurants**, 11 rows total, last run today. So of 573 catalogued → 24 built → 5 observed live in a month. Coverage funnel: 573 → 24 (4.2%) → 5 (0.9% of catalogue, 21% of built).
- Coverage: `engine/` (statistics, regression, forecasting, linalg, cost-basis, finance, pricing-agility, risk, vendor-price-consensus, association-comparisons, inventory-science) has 10 spec files for 13 source files — the best-tested area of the codebase surveyed here.

## 3. Forecasting & scoring

- `analytics/engine/forecasting.ts` (325 lines): SES, Holt linear, Holt-Winters additive, classical decomposition, seasonal-naive baseline, MAE/RMSE/MAPE/MASE — explicitly "classical, explainable" by design, not ML; file's own docstring says heavier ARIMA/Prophet/LightGBM would go through the Python orchestrator, but **no such bridge/model was found in `services/agent-orchestrator`** on grep (searched agent-orchestrator for arima/prophet/lightgbm — none present; this is an aspirational comment, not a built capability).
- `analytics/engine/statistics.ts` (477 lines): `robustZScore` (statistics.ts:162, MAD-based, used for surge/anomaly detection), `zScore`, `cusum`, `serviceLevelZ` (for reorder-point normal-approximation), `shannonEntropy`.
- `analytics/engine/regression.ts`: ridge/OLS closed-form multiple regression with `ridgeLambda` (regression.ts:42-56), plus a dummy-encoded ridge "adjusted plus-minus" fixed-effects model (regression.ts:155-200) used for `table.avg_check.driver_weights` and `waiter.avg_check.peer_rank` (called from `table-analytics.service.ts:269-396` and `insight-generator.service.ts:1050`).
- Par/reorder: `E.inventory.reorderPoint` / `safetyStock` (`analytics/engine/index.ts`, used in `advanced-analytics.service.ts:552-592` and `analytics.service.ts:602`) combine `serviceLevelZ` with demand variance for a reorder point + safety stock — a real, closed-form model, not a stub. `metric-registry.ts:196,209` documents which catalogue features these back.

## 4. Wine library intelligence

A prior session on branch `origin/docs/wine-ml-foundations` (not merged; `.planning/07-reference/wine-intelligence-foundations.md`, ADR 0163 draft) already did a deep, dated (2026-09-18) production measurement of this exact area — cited here rather than re-derived per CLAUDE.md §5b ("numbers get re-measured, never copied forward" — but this doc **is** the re-measurement, dated today, off `origin/main@09190ee50`; I did not re-run its SELECTs myself, so treat the figures below as one level removed from this session's own verification):

- Library: 3,589 live rows (3,511 wine + 78 unknown). Only **173 wines are actually stocked** by any restaurant, and of those, **none belong to a real restaurant** — 161 are held by simulation houses, 12 by four houses the founder has ruled are not real ("ADMIN ROOM 10" etc.). **No real house stocks a wine today.**
- Sensory/structure profile: 3,346 of 3,589 live rows have `wine_structure`, but only **13 of the 173 stocked wines** do, and **7 of those 13 are a copied placeholder profile** (light/dry/high-acid template pasted onto 44 rows including a 2009 Château Latour).
- Embeddings: `embedding vector(384)` populated on 3,430 rows via `scripts/populate_embeddings.py` (MiniLM) — but the doc states **nothing in the product reads it** (no query site found).
- Producer narrative / critic scores / market price / ML-derived-feature columns: **0 rows populated** across the board; `producers` table has 0 rows.
- Pipeline verdict from that doc, verbatim: **"Broken. It is not one bug. No running process enriches a library row after the row is created."** Every row created since 2026-08-17 (159 rows) has neither a profile nor an embedding.
- Trigram/fuzzy matching: confirmed present at the DB layer — `pg_trgm`-style `similarity()`/fuzzy matching migrations (`20260811010000_vendor_catalogue_match.sql`, `20260813070000_producer_trade_word_tolerance.sql`, `20260813150000_find_library_duplicates.sql`, `20260906010000_a_generic_name_stays_the_venues_own_wine.sql`) and app-layer matchers (`vendor-catalogue.service.ts`, `pos-hub/catalog-matcher.service.ts`, `procurement/documents/line-matcher.ts`) — this part of "wine matching" is real and iterated-on; it is the enrichment/sensory/embedding layer that is not.

## 5. Recommendation engine / UX optimizer

- `analytics/recommendations.service.ts` (614 lines) + `recommendation-actions.service.ts` (361 lines) + `analytics/digest/recommendation-digest.service.ts`: act/dismiss/snooze recommendation store with a scheduled digest (per prior session memory `recommendations-actions-ux-optimizer.md`). Has 4+ spec files (`recommendation-digest-pref-read.spec.ts`, `recommendation-suppression.spec.ts`, `recommendation-digest.service.spec.ts`, `recommendation-digest.controller.spec.ts`).
- `ux-optimizer/ux-optimizer.service.ts` (1,798 lines) — an experiment/proposal engine with a daily cron (`@Cron("17 4 * * *")`, ux-optimizer.service.ts:775) and 4 spec files (`ux-optimizer.experiments.spec.ts`, `ux-proposal-grounding.spec.ts`, `experiments.spec.ts`, `ux-optimizer.admin-routes.spec.ts`). Runs on a schedule; did not verify live proposal rows in production this pass (see Not checked).

## 6. Python agent-orchestrator (25 agents) + self-evolution

- **25 agent files** confirmed (`ls services/agent-orchestrator/agents/*.py | wc -l` = 25, one `__init__.py` excluded from the count of actual agents = 24 real agent modules + init). All 24 non-init files reference `BaseAgent` (`core/base_agent.py`, an asyncio/TaskGroup + circuit-breaker + message-bus framework — RabbitMQ/saga/DLQ infrastructure per the corpus's own OD-52).
- **LLM-calling agents: only 5 of 24** actually call an Anthropic client (`grep -rl anthropic services/agent-orchestrator/agents/*.py`): `provider_communication_agent.py`, `rfq_agent.py`, `visual_verification_agent.py` (pinned to `claude-haiku-4-5-20251001`), `email_intel_agent.py`, `provider_conversation_agent.py`. The other 19 are rule/state-machine agents (inventory math, compliance checks, notification routing, etc.) with no model call — a legitimate design choice, but it means "25 AI agents" overstates how many are actually AI-driven versus orchestration logic.
- **Test coverage**: naive per-agent test-file match (name substring in `services/agent-orchestrator/tests/`) found **15 of 24 agents with no matching test file**: `auto_pilot_agent`, `buffer_manager`, `calendar_agent`, `compliance_agent`, `email_parsing_agent`, `ghost_inventory_agent`, `inequality_detector`, `menu_analyzer_agent`, `negotiation_playbook_agent`, `pos_integration_agent`, `provider_conversation_agent`, `shrinkage_detective_agent`, `sommelier_agent`, `state_invariant_enforcer`, `visual_verification_agent`. This is a name-substring heuristic (107 test files exist under `tests/`, many likely integration-style covering several agents at once) — treat as a lead to verify, not a hard count; see Not checked.
- **Production evidence — this is the single biggest gap found**: `agent_activity_logs` (the table purpose-built to record every agent run: `agent_name`, `status`, `llm_model`, `tokens_used`, `llm_cost_usd`, `duration_ms`) has **zero rows, all time** (`select count(*) from agent_activity_logs` → 0/null/null). Either the 25-agent orchestrator does not run in production against this database, or it runs but nothing writes to its own activity-log table. Either reading is a material gap between "25 autonomous agents" as promised and what is demonstrably running.
- **Self-evolution service** (`services/self-evolution/main.py`, 561 lines, port 8090): its own docstring says learning/A-B-testing/meta-agent are **"Disabled by default (ENABLE_SELF_EVOLUTION=false)"** — passive collection only is "always active." Production check: `agent_evolution_log` (its output table) has **0 rows ever**. Not referenced by any other service/config file in the repo outside its own directory (`grep -rl self.evolution` across the repo returns nothing outside `services/self-evolution/`) — nothing wires it into `docker-compose`/Railway/CI that this grep surfaced. This service appears to be inert/unwired scaffolding, not a running capability.

## 7. OCR / document extraction

- `procurement/documents/document-extractor.service.ts` (771 lines): vision extraction via `claude-haiku-4-5` for image/PDF documents not on EDI rails (document-extractor.service.ts:58,178). Has type-sniffing by magic bytes (`JVBERi`→PDF, `/9j/`→JPEG) and a dedicated spec file (`document-extractor.spec.ts`). Menu OCR is separate: `menus/parsers/scan-parser.service.ts` (also Haiku). Photo-based inventory counting is a third vision path: `inventory/photo-count.service.ts` (Haiku). Three independent vision call sites, same model tier, no shared abstraction found between them beyond the common `model-client.service.ts` router.

## Not checked (scope/time cuts — state plainly per CLAUDE.md §0.5)

- Did not run `heavy.sh jest`/`tsc`/`pytest` — all coverage/pass-fail claims above are file-existence and grep-based, not verified-green test runs.
- Did not verify the 15 "no test file" Python agents by reading `tests/` contents (107 files) individually — the heuristic could be hiding integration-style coverage.
- Did not check `ux-optimizer` or `recommendations` production tables for live rows (time cut after the agent/insight/spend findings above, which were higher-signal).
- Did not check cost/token volume or correctness of the `model-routing.ts` cheapest-capable-model logic itself, only that it exists and is tested.
- Did not independently re-run the wine-intelligence SELECTs from the `docs/wine-ml-foundations` branch; cited its 2026-09-18 measurement as-is.
- Did not check `packages/database` or `packages/ui` for embedded model/analytics logic — scope was `apps/api-gateway` + `services/agent-orchestrator` + `services/self-evolution` per the task.

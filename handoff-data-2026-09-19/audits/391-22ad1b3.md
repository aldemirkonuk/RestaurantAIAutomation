<!-- pr-audit-gate: pr=391 sha=22ad1b3a12c4f91952088e2173b6e6297256a358 verdict=BLOCK -->
# PR #391 audit: BLOCK (records), plus founder escalation (gate-owned files)

**Head:** 22ad1b3a1. **CI:** all 40 contexts green, including the 5 required ones. **Gate-owned files:** `.github/workflows/ci.yml` and `.planning/decisions/README.md`. Even with a clean audit, merging needs the founder's word (ADR 0090 step 7).

| Angle | Verdict |
|---|---|
| Correctness & regression | APPROVE WITH NOTES |
| CLAUDE.md / ADR compliance | **BLOCK** |
| Security & blast radius | APPROVE WITH NOTES |
| Adversarial pass | skipped (an angle blocked) |

## Blocking (compliance). Records only, no code at risk.
- **B1.** `.planning/handoff/PROGRESS.md:96-100` (added by this PR) restates sketch picks the founder reversed, and nothing brackets them:
  - "110 A with B's detail (C rejected), 111 A". The founder's answers are cellar = B, help delegated to the builder, and C "kept in mind".
  - `STATE.md:22` sends readers to that section.
- **B2.** `.planning/06-pages/recommendations.md:1052-1066` says founder confirmation is owed before migration `20260917010100` merges, and no record shows a confirmation.
  - The founder has now answered all five choices (2026-09-19): (a) per-person subscriptions, his round-11 answer; (b) the subscription alone is the gate, and the legacy `categories.ai` check is removed (delegated: "most SOTA, quality, scalability"); (c) the free `recipient_email` field is dropped when this lands, and a per-person opt-in is added; (d) 12-hour late limit; (e) serve at the next sweep.
  - These answers must be written in, and (b) needs a code change.

## Medium (compliance)
- **M1.** The production counts measured 2026-09-18 (0/0) are corrected in PROGRESS but not bracketed at `0121:958-960`, `0121:1018`, `connections.md:1256-1257` and `PROGRESS.md:36-38`.
- **M2.** ADR 0160 §113 still calls the §4c acts' address "open" (`:472`, `:501-512`, `:550-551`, `:570-572`), but open item 3 records the founder's answer (/communications). The review trail says "8 items left open" and has no rows for 82aab7d31 or 9c6387edd.
- **M3.** README has no row for ADR 0160, and the ADR 0149 row still counts 38 rows (it has 50).
- **M4.** ADR 0121 item 9's forks are not filed, and ADR 0149 row 47 still says "OD (to file)".
  - The WhatsApp reply sends with no seal, against locked ADR 0112 F10. **The founder has now answered (2026-09-19):** clicking Send is enough (this amends F10 for person-sent WhatsApp messages), people can delete their messages, and the surface is WhatsApp-Web-like inside the house frame.
- **M5.** The PR body says the provider-intelligence cross-house reads are closed. The register entry says twelve methods still read across tenants (OPEN), and this is not a CLAIMS row. The security angle confirms only that the sentiment reads are now scoped.

## Notes (non-blocking)
- **Correctness:**
  - All four migration versions sort before production's latest, and precedent (#387) shows they apply. After the merge, check the Supabase check-run and main's Schema parity run before merging anything else.
  - The definer-check floor equals today's live count (7), so a hardening PR would exit 2 with a misleading message.
  - `getDigestPref` now returns 500 on a failed read, which is intended.
  - Web source maps are off.
- **Security:**
  - The end-state check sees only the database built from migrations, never production's ACLs. The orchestrator measured production read-only on 2026-09-19: **0 SECURITY DEFINER functions (outside extensions) are executable by anon or authenticated.**
  - The unique index in `20260913190100` is built without CONCURRENTLY.
  - The public unsubscribe POST has no rate limit (256-bit token).
  - The digest shares the deployment Gmail quota.
  - The sender fallback changed to `notifications@mudavym.com`, and production's `GMAIL_SENDER_EMAIL` is not verified.
  - Database error text reaches signed-in callers.
- **Compliance (low):**
  - The OD-72 claim text still carries the old floors (61/89).
  - The ADR-0149-DIGEST and ADR-0121-F3 verifies each check only half of their claim.
  - `PROGRESS.md:139` citations are stale (now :221).
  - The sketch 120 feedback of 2026-09-19 is not in ADR 0160.
  - ADR 0159 is still Proposed.

## Could not check
- Production environment variables.
- Whether the Supabase integration applies the older migration versions; this rests on precedent only.
- The ~1,100-line REACH_SQL, which was not read clause by clause (its 576-case self-test and CI were relied on instead).
- The WhatsApp send path end to end.
- The founder's raw transcripts; the audit used the memory files.

## Next
A records-and-small-code round on train/finish-2 covers B1, B2 (including removing the `categories.ai` gate), M1–M5, the definer-floor message, and the low notes. Then a re-audit, then the founder's word for the gate-owned files.

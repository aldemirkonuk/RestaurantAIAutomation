{"id": "TD-2026-09-19-WEB-PRETTIER-GAP", "status": "open", "claim": "apps/web is still outside Prettier (v3.0-TECH-DEBT 1602): its .eslintrc.cjs override for apps/web/**/*.{ts,tsx} has no 'prettier' extends/plugin, unlike the apps/api-gateway override which has both, and apps/web/package.json still has no format script. Deliberately not fixed 2026-08-27 (founder call) pending a pick between a one-commit whole-tree reformat and an OD-105-style ratchet.", "verify": "python3 -c \"s=open('.eslintrc.cjs').read(); i=s.index('apps/web/**/*.{ts,tsx}'); j=s.index('apps/api-gateway/**/*.ts'); import sys; sys.exit(0 if 'prettier' not in s[i:j] else 1)\" && ! grep -q '\"format\"' apps/web/package.json", "verified": "2026-09-19"}
{"id": "TD-2026-09-19-HOURSHEATMAP-DARK-INVERT", "status": "open", "claim": "HoursHeatmap's ramp still inverts salience on the dark ground (v3.0-TECH-DEBT 1642): bits.tsx's HEAT_SHADES anchors on near-white #F1F7F8 for the quietest hours, so on Warm Charcoal the quiet cells glow brightest instead of the busy ones. Fix direction on file: paint over var(--seal) opacity rather than the raw hex ramp.", "verify": "grep -q \"HEAT_SHADES = \\['#F1F7F8'\" apps/web/src/pages/inventory/command/bits.tsx", "verified": "2026-09-19"}
{"id": "TD-2026-09-19-TWINSHEET-LEGACY-PANEL", "status": "open", "claim": "TwinSheet still hosts ProviderIntelligencePanel, the shared legacy grey/blue-skinned component, unre-skinned inside the Iznik sheet (v3.0-TECH-DEBT 1653; also recorded in providers.md section 9). Deliberately not patched with scoped CSS overrides -- the panel needs its own token re-skin.", "verify": "grep -q '<ProviderIntelligencePanel providerId={provider.id}' apps/web/src/pages/providers/next/TwinSheet.tsx", "verified": "2026-09-19"}
{"id": "TD-2026-09-19-EMDASH-AT-SILENT", "status": "open", "claim": "The honesty idiom's em dash ('unknown') is still silent to assistive tech (v3.0-TECH-DEBT 1662, first half only): no aria-label=\"unknown\" (or equivalent accessible name) span exists anywhere under apps/web/src, so a screen reader reads bare punctuation or nothing where a page shows an unknown value. The entry's second half (TwinSheet/TemplateSheet lacking a focus trap) is STALE and deliberately NOT carried into this row: components/mudavym/Sheet.tsx (ADR 0112, shipped 2026-09-03) now traps Tab and restores focus on close for both sheets -- see providers.md's 'Modal shape, 2026-09-03' note.", "verify": "! grep -rq 'aria-label=\"unknown\"' apps/web/src", "verified": "2026-09-19"}
{"id": "TD-2026-09-19-EDITLINE-NOTIF-NO-SPEC", "status": "open", "claim": "document-intake.service.ts's editLine and notifications.service.ts's persistForRestaurant still have no service-level spec exercising their own logic (v3.0-TECH-DEBT 1672): document-intake.service.spec.ts never calls .editLine( at all (the only coverage is edit-line-tieout.spec.ts's pure applyTieOut cast; documents.seal.spec.ts and line-route-uuid-guard.spec.ts mock editLine away rather than exercising it), and apps/api-gateway/src/notifications/notifications.service.spec.ts does not exist, so onlyUserIds' narrowed socket emit is untested -- every caller-side spec mocks persistForRestaurant rather than calling the real implementation.", "verify": "python3 -c \"import os; a=not os.path.exists('apps/api-gateway/src/notifications/notifications.service.spec.ts'); s=open('apps/api-gateway/src/procurement/documents/document-intake.service.spec.ts').read(); b='.editLine(' not in s; import sys; sys.exit(0 if (a and b) else 1)\"", "verified": "2026-09-19"}
{"id": "TD-2026-09-19-MAXDRAWDOWN-ZERO", "status": "open", "claim": "maxDrawdown is still identically zero by construction and unrendered (v3.0-TECH-DEBT 1682, defect 4 of 4 -- defects 1-3 in that entry are separately closed): analytics.service.ts builds levels as a running cumulative sum of demand (cum += v; levels.push(cum)) with no floor or reset, so the series is monotonically non-decreasing and engine/risk.ts's maxDrawdown(levels) can never be nonzero; the field has zero call sites in apps/web or apps/mobile.", "verify": "python3 -c \"import subprocess,sys; s=open('apps/api-gateway/src/analytics/analytics.service.ts').read(); i=s.find('const levels: number[] = []'); block=s[i:i+220]; ok=('cum += v' in block and 'levels.push(cum)' in block); web=subprocess.run(['grep','-rl','maxDrawdown','apps/web/src','apps/mobile/src'],capture_output=True); sys.exit(0 if (ok and web.returncode==1) else 1)\"", "verified": "2026-09-19"}
{"id": "TD-2026-09-19-SPOTCOUNT-DROP-SILENT", "status": "open", "claim": "lib/spotCountOutbox.ts still cannot distinguish a permanently-dropped spot count from a delivered one (v3.0-TECH-DEBT 1994 finding 2, second half -- the sibling lib/doorOutbox.ts half of this entry is CLOSED, 5ba2972a): watchSpotCountOutbox discards flushSpotCountOutbox's {sent,failed} result via .then(() => onChange?.()), and unlike doorOutbox.ts there is no DROPS_KEY_PREFIX record of which count was permanently dropped, so the UI cannot tell a lost count from a synced one.", "verify": "python3 -c \"s=open('apps/web/src/lib/spotCountOutbox.ts').read(); import sys; sys.exit(0 if ('void flushSpotCountOutbox().then(() => onChange?.())' in s and 'DROPS_KEY_PREFIX' not in s) else 1)\"", "verified": "2026-09-19"}
{"id": "TD-2026-09-19-QTY-RECEIVED-TWO-UNITS", "status": "open", "claim": "procurement_orders.quantity_received still has four writers disagreeing on unit, and verifyReceipt still assumes one (v3.0-TECH-DEBT 2159; ADR 0119 line 43 says this LIVE-DEFECT entry stands -- do not close ADR 0070's ledger-unit work without resolving this). Three writers (markDelivered's quantityReceived-or-existingOrder.quantity fallback, updateOrder's quantityReceivedInOrderUom field, verifyReceipt's own acceptedQty+rejectedQty write-back) state the column in the ORDER's unit; the fourth, ReceivingService's door path, writes BOTTLES (totals.receivedBottles). verifyReceipt then reads the column as stockedQtyInCountedUom and invoice-match.ts's conv(rawStocked, counted) multiplies a door-written bottle count by the pack size a second time. Production is not yet exposed (0 of 2 procurement_orders carried a non-null non-zero quantity_received, measured 2026-09-02) but the first real door-to-desk delivery will misread by the pack size. Founder marked this WORK rather than filing a new OPEN-DECISIONS row this pass; the two-way fork procurement.service.ts's own inline comment lays out (teach the door to write the order's unit, vs teach verifyReceipt the value is bottles) is still unresolved and will need a founder call when this is actually worked.", "verify": "python3 -c \"p=open('apps/api-gateway/src/procurement/procurement.service.ts').read(); r=open('apps/api-gateway/src/procurement/receiving.service.ts').read(); m=open('apps/api-gateway/src/procurement/invoice-match.ts').read(); ok=('quantity_received: acceptedQty + rejectedQty' in p and 'ITS UNIT IS NOT AGREED' in p and 'orderUpdate.quantity_received = totals.receivedBottles' in r and 'stockedQty: rawStocked === null ? null : conv(rawStocked, counted)' in m); import sys; sys.exit(0 if ok else 1)\"", "verified": "2026-09-19"}
{"id": "TD-2026-09-19-POSHUB-DISCARDALL-SILENT", "status": "open", "claim": "pos-hub.service.ts's discard-all path still logs nothing (v3.0-TECH-DEBT 2433 defect 1; ADR 0105's 'fixed regardless of the build' list): when adapter.normalize(payload) returns no checks, ingest returns received:0 with errors ['No recognizable checks in payload'] and no this.logger call anywhere between the length check and the return, so a provider whose envelope is mis-read is indistinguishable in the logs from a provider that never called.", "verify": "python3 -c \"s=open('apps/api-gateway/src/pos-hub/pos-hub.service.ts').read(); i=s.index('if (!checks.length)'); j=s.index('No recognizable checks in payload'); import sys; sys.exit(0 if 'this.logger' not in s[i:j] else 1)\"", "verified": "2026-09-19"}
{"id": "TD-2026-09-19-POSHUB-SIGREJECT-SILENT", "status": "open", "claim": "A rejected POS webhook signature still produces no log line (v3.0-TECH-DEBT 2433 defect 2; ADR 0105's 'fixed regardless of the build' list): verifyWebhookSignature's final guard, 'if (!signature || !rawBody) return false;' (pos-hub.service.ts), has no this.logger call before it, and the controller's resulting 401 ('Webhook signature verification failed') is thrown with no logger call either -- so a wave of rejections leaves no trace beyond whatever unrelated secret-resolution log fired earlier.", "verify": "python3 -c \"svc=open('apps/api-gateway/src/pos-hub/pos-hub.service.ts').read(); ctl=open('apps/api-gateway/src/pos-hub/pos-hub.controller.ts').read(); i=svc.index('if (!signature || !rawBody) return false;'); j=ctl.index('Webhook signature verification failed'); ok=('this.logger' not in svc[max(0,i-250):i]) and ('this.logger' not in ctl[max(0,j-300):j]); import sys; sys.exit(0 if ok else 1)\"", "verified": "2026-09-19"}
{"id": "TD-2026-09-19-POSADAPTERS-NOT-VENDOR-EVIDENCE", "status": "open", "claim": "pos-adapters.spec.ts's Square/Clover tests are still hand-fabricated envelopes with no citation to a captured or spec-documented vendor payload (v3.0-TECH-DEBT 2433 defect 4; ADR 0105 D4 names this file the 'standing counter-example until replaced'): the Square test object has no voided key and the file has no 'captured'/'spec-cited' comment anywhere, so a green run is not vendor evidence.", "verify": "python3 -c \"import re; s=open('apps/api-gateway/src/pos-hub/pos-adapters.spec.ts').read(); i=s.index('square converts cents and reads the webhook envelope'); j=s.index('clover converts epoch millis and cents'); block=s[i:j]; bad=bool(re.search(r'captured|spec-cited|squareup.com|developer.square', block, re.I)) or ('voided' in block); import sys; sys.exit(0 if not bad else 1)\"", "verified": "2026-09-19"}
{"id": "TD-2026-09-19-MARKDELIVERED-ORDER-KEYED-FALLBACK", "status": "open", "claim": "markDelivered's older per-ORDER idempotency key is still live as a bounded fallback (v3.0-TECH-DEBT 2478 defect 1; ADR 0103 line 198): ADR 0103 A5's delivery-first path (deliveryHasBookedOrder) now owns the real door flow, but procurement.service.ts's own comment says this 'bounds the older defect rather than pretending it is gone' -- when deliveryOwns.value.booked is false, the code still falls through to the old order-delivered:${orderId} key, so any caller of markDelivered outside the new delivery-first flow can still double-book or drop a split shipment's second truck.", "verify": "grep -q 'const idempotencyKey = `order-delivered:${orderId}`;' apps/api-gateway/src/procurement/procurement.service.ts", "verified": "2026-09-19"}
{"id": "TD-2026-09-19-SYNCORDERSTATE-DROPS-CONTRADICTION", "status": "open", "claim": "syncOrderState still drops a contradicting vendor reply with no structured record (v3.0-TECH-DEBT 2478 defect 2): inbound-responder.service.ts's APPROVED branch advances to CONFIRMED only when the receipt matches; a price/qty mismatch falls through to a comment-only 'Otherwise stay APPROVED' with no delivery_proposals write and no persistManagerNotification call -- unlike the sibling isDecline branch in the same file, which does both.", "verify": "python3 -c \"s=open('apps/api-gateway/src/common/orchestrator/inbound-responder.service.ts').read(); i=s.index('Otherwise stay APPROVED'); j=s.index('agreedPriceToWrite = vendorPrice'); block=s[max(0,i-400):j]; bad=('delivery_proposals' in block) or ('persistManagerNotification' in block); import sys; sys.exit(0 if not bad else 1)\"", "verified": "2026-09-19"}
{"id": "TD-2026-09-19-INVCOMMAND-UNKNOWN-COST-ZERO", "status": "open", "claim": "The new InventoryCommandPage still renders unknown cost as $0 in its aggregate KPIs (v3.0-TECH-DEBT 2666 absence-6, smaller residual on the new page -- the original absence-6 entry is about the legacy page): six sites in InventoryCommandPage.tsx compute value as (wac ?? price ?? 0) * stock, so a lot with unknown cost (NULL wac and price) is silently counted as worth $0 in 'Value on hand' rather than being flagged as unknown, even though the page's per-lot display is honest elsewhere.", "verify": "grep -q 'wac ?? .*price ?? 0' apps/web/src/pages/inventory/command/InventoryCommandPage.tsx", "verified": "2026-09-19"}
{"id": "TD-2026-09-19-SLUG-TURKISH-CHAR-DROPPED", "status": "open", "claim": "A Turkish venue name is still mangled in its slug (v3.0-TECH-DEBT 2717.F10, Antalya lens): organizations.service.ts's slugBase does .toLowerCase().replace(/[^a-z0-9]+/g, '-') with no transliteration table, so a character like 'c-cedilla' (outside [a-z0-9]) collapses into a hyphen rather than becoming a plain 'c'. Filed as harmless today per the original finding -- the slug is suffixed with a random UUID for uniqueness and is not user-facing identity.", "verify": "python3 -c \"s=open('apps/api-gateway/src/organizations/organizations.service.ts').read(); i=s.index('const slugBase = dto.name'); j=s.index('const slug = ', i); block=s[i:j]; import sys; sys.exit(0 if ('replace(/[^a-z0-9]+/g' in block and 'transliterat' not in s.lower()) else 1)\"", "verified": "2026-09-19"}
{"id": "TD-2026-09-19-SALE-UNIT-NO-CARAFE", "status": "open", "claim": "sale_unit still has no word for a carafe (v3.0-TECH-DEBT 2717.F6, Antalya lens): pos-mapping-review.dto.ts's SALE_UNITS is still ['glass','bottle'] with @IsIn(SALE_UNITS) guarding the review DTO, while pos-hub.service.ts's own comments already treat 'carafe' as a valid open label the writer accepts internally -- so a manager answering the mapping-review screen for an open-poured carafe still gets a 400.", "verify": "python3 -c \"import re; s=open('apps/api-gateway/src/pos-hub/dto/pos-mapping-review.dto.ts').read(); m=re.search(r'SALE_UNITS = \\[([^\\]]*)\\]', s); units=m.group(1); import sys; sys.exit(0 if ('carafe' not in units and 'glass' in units and 'bottle' in units) else 1)\"", "verified": "2026-09-19"}


TOTAL ROWS: 16

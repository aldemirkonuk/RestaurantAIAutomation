# Open Decisions — the founder queue

> Undecided forks. Sessions **add** items here when they hit one; only a founder
> call resolves an item (→ move it to the Resolved table, link the evidence).
> Nothing in this file may be treated as decided.
>
> Format: ID · question · why it matters · what unblocks it.
>
> **ID collision, reconciled 2026-08-24.** `main` and `fix/od-57-retired-model-sweep`
> both allocated **OD-57 … OD-60** from the same next-free number while apart, so
> six rows arrived wearing three IDs. `main`'s numbering is kept (it is the trunk,
> and `04-specs/P1-BUILD-LOG.md` already cites its OD-58); the branch's three
> non-overlapping items were renumbered **OD-58→61, OD-59→62, OD-60→63** and their
> citations in `v3.0-TECH-DEBT.md`, ADR 0010 and `spend_logger.py` updated to match.
> OD-57 was *not* renumbered — both sides' OD-57 are the same fork (retired model
> ids / model roster) seen from two angles, and 17 code comments cite it — so all
> three OD-57 rows are kept together below. Nothing was dropped except the branch's
> copy of OD-56, which is the same decision as `main`'s in an older, shorter form.

## Open

| ID | Question | Why it matters now | What unblocks it |
|---|---|---|---|
| OD-110 | **The gateway's database client is untyped, and that is invisible at any strictness setting.** `DatabaseService.supabase` is `SupabaseClient` with no `Database` generic (`database.service.ts:8`), so every `.from(...)` returns `any`. The gateway queries **117 distinct tables**; the generated `Database` type covers **8**. Measured while closing OD-107: **12.3% of 28,661 production member accesses are on `any`** — more than the nullable ones by an order of magnitude, and unreachable by `strictNullChecks`, `strict`, or any other flag. | This is the real version of *"code that reads as type-safe is not"*. A column rename or a dropped table is a runtime surprise today; the phantom-table class ADR 0028 exists for is the same gap seen from the other side | Regenerate `Database` for all 117 tables and parameterise the client — then measure again. The OD-107 flip was its prerequisite and is now done |
| OD-109 | 🟠 **P3.A's exit criterion is UNMET and the code shipped anyway — deliberately, and recorded rather than waived.** [ADR 0029](0029-p3-plan-of-record.md) §5 says P3.A closes when *"the mobile app renders the P2-approved feature set, verified on a simulator, not asserted"*. **The app has never been run.** `apps/mobile` is a managed Expo project with no `ios/` directory; `expo prebuild` needs CocoaPods, which is not installed, and the system Ruby (2.6.10) is below its minimum. Expo Go cannot host it either — MMKV, Skia and Reanimated 4 need the new architecture. What IS proven: `expo export` builds a 5.67 MB Hermes bundle containing the expected strings, and 37 unit tests fail against the pre-fix tree. **Neither is the same as rendering.** Merged because four real defects and a CI guard should not be held hostage to a toolchain gap — but P3.A cannot be called done on this evidence, and the next reader must not mistake a green board for a working app. | Every mobile change after this inherits the same blind spot: three of the four defects fixed here were an export with no importer, which a typecheck cannot see and only running the thing would have caught | CocoaPods + a Ruby new enough to run it, then `expo prebuild` and one screenshot. Until then P3.A ships unverified in the only sense that matters |
| OD-03 | **Orchestration base** — `NousResearch/hermes-agent` vs `deepseek-ai/deepseek-harness` vs extending in-house `BaseAgent`. **Re-verified 2026-08-27:** `base_agent.py` still has zero LLM integration (one `completion` hit, the shutdown log) — read OD-52's reframe before any bake-off. | Names cost nothing; the real axis is cheapest-capable-model routing + harness overhead (vision §12E — an anchor that resolves to no file in this repo; noted 2026-08-27). | A scoped bake-off on this repo's actual workloads. No pick from repute. |
| OD-04 | **External model roster** — which non-Anthropic models (Kimi/Moonshot, DeepSeek, etc.) for which task tiers, if any. **Census re-measured 2026-08-26** with `grep -rIo --exclude-dir=node_modules --exclude-dir=.git '<literal>' apps services packages scripts \| wc -l`: `gemini-2.5-flash` **59**, `claude-haiku-4-5` **34**, `gemini-2.0-flash` **9**, `claude-sonnet-4` **9**, `gpt-4o` **6**, `gemini-3.6-flash` **5**, `gemini-pro` **3**, `gpt-4-turbo-preview` **2** — **127 literals** (re-verified exact, 2026-08-27). ~~Sharpened 2026-08-24: `gemini-2.5-flash` ×32, `gemini-2.0-flash` ×20, `gemini-pro` ×17, `claude-haiku-4-5` ×18, `gpt-4-turbo-preview` ×3, `gemini-3.6-flash` ×2, "~90 sites"~~ — **every figure was wrong**, in both directions, and the census named neither `gpt-4o` nor `claude-sonnet-4` at all. There is still **no place in the repo that says which model does which job**. | The founder's cost-efficiency mandate cannot be executed against string literals — and the surface grew **~90 → 127** while this entry sat open, which is the argument for the registry, not against it. ~~three retired names are failing in production today~~ — **struck 2026-08-26, no longer true**: OD-57's sweep closed it, and all **3** surviving `gemini-pro` occurrences are inert (`calendar_agent.py:214` and `sommelier_agent.py:44` are comments recording the retirement; `spend_logger.py:163` is a historical price row). **Milder than recorded** on the production-failure half; **worse** on the sprawl half. P1 made the missing half possible: `context.task_type` now names the job, and the NF-A readout can compare cost and (OD-59 has since landed — see Resolved — and P3.0 shipped graders for every task type, #123/#124) quality per job. | A **job → model registry**: one table, both runtimes read it, call sites name a job not a model. The two choke points already exist — `common/model-client/` in the gateway and `SpendLogger` in Python. Then let the roster be data, and re-decide it from NF-A evidence rather than from repute. |
| OD-05 | **Personal voice agent** — guest-facing, staff-facing, or owner/admin? (Vellum-style, vision §14A.7 — anchor resolves to no repo file; noted 2026-08-27.) | Cannot be scoped before the audience is chosen. | One founder sentence choosing the audience. |
| OD-06 | **AnyDoc adoption** for digital-document parsing vs current Claude Vision path (vision §12D — anchor resolves to no repo file; noted 2026-08-27). | Invoice/receipt understanding is on the critical path to data credibility (vision §7). | Bake-off on real vendor PDFs/spreadsheets from the corpus. |
| OD-07 | **Beli strategy** — build the guest consumer experience independently vs explore collaboration (vision §10 — anchor resolves to no repo file; noted 2026-08-27). | Determines whether guest-app work is product or partnership groundwork. | Founder call after guest MVP scope exists (FUTURES.md §7.5). |
| OD-19 | **Security classification — census re-measured 2026-08-26.** ~~of the **94** endpoints unguarded by omission (137 total − 32 webhook-module − 11 explicit `@Public()`)~~ — **that arithmetic describes a codebase two guard-sweeps ago and is struck.** Measured over the 47 non-spec `*.controller.ts` files under `apps/api-gateway/src` (block and line comments stripped first, so a commented-out decorator cannot be counted): **459** route decorators, **17** `@Public()`, **419** routes on class-guarded controllers, **40** on the five controllers carrying no class-level `@UseGuards` — `auth` (29 routes, 8 `@Public`, 16 method-level guards), `integrations-oauth` (5/1/4), `events` (3/1/2), `common/orchestrator/inbound-email` (1, a `@Public` webhook), `vendor-portal` (2, both `@Public`). | Unguarded + `TenantGuard` passthrough = internet-reachable. **[ENDPOINTS.md](../foundation/ENDPOINTS.md) is stale on exactly this point and still carries a 🔴 open finding** that `toast/toast.controller.ts` has no class-level guard and that "9 are genuinely unguarded tenant data" — it does have one: `toast.controller.ts:63` is `@UseGuards(JwtAuthGuard)`, with only `@Post("webhook")` marked `@Public()` at `:80`. That atlas needs the same correction and did not get it here. | **Far milder than recorded: 94 → 40, and most of the 40 are deliberate.** Narrow the assignment to enumerating the residual routes on those five controllers and confirming each is public by intent. Security department's first assignment. |
| OD-23 | **Revenue target and pricing, both unverified.** Three corrections to this item as first written: (a) it called $20–50/mo **locked** — **no ADR records any pricing**; per CLAUDE.md §0.1 an unwritten choice is *open*, so the $20k math rests on an assumed price. (b) Its cited source `MASTER-PLAN-30-DAY-SPRINT-2026-08-24.md` **is not in this repo** — it lives in the claude.ai Cowork session, so the <10% likelihood rating cannot be checked here. (c) `PROJECT.md:73` (the original `:135` cite now points past EOF after the P2 rewrite — corrected 2026-08-27) states **"No revenue pressure: Build right, not fast"**, which directly contradicts a 30-day revenue sprint. | Every Commercial decision inherits a target whose price, source document, and stated urgency are all unconfirmed. | **Founder call**, and paste the master plan into the repo. Found by the Finance & Pricing generator. |
| OD-25 | **Weekly skill-health job — which department owns it?** The 2026-08-24 call resolved this only *in principle* ("fixed to one department"); it did not name the department, and neither source doc was amended. foundation README §6 still assigns it to Research & Math; `teams/technology.md` §4.2 still assigns it to Skills. | A recurring job with two named owners has no owner in practice. Kept open — the Resolved row records the principle, not the pick. | Decision Office: choose one, amend the other document. |
| OD-26 | **Structures only ratchet upward** — the generated corpus names **split** triggers in ~~11~~ **23** documents and **merge/retirement** triggers in ~~3~~ **6** (re-measured 2026-08-27 over 776 org docs — the asymmetry widened while this row sat open). Should every unit be required to carry a merge/retirement trigger, symmetric with its split trigger? **Absorbs OD-43 (merged 2026-08-27)** — the same fork filed again from the audit side; its 11 → 13 → 15 trajectory is unreproducible because no entry recorded its query, so the dated 23-vs-6 measurement above is the number of record. | Raised by the Legal generator. An org that can only grow has no mechanism to shrink; combined with ~~693~~ **776** org documents and zero-evidence departments, this is the structural form of the sprawl the anti-sprawl rules target. | Founder call — likely a Decision Office standing rule. |
| OD-29 | **Second boundary conflict, beyond the evaluation seam** — `aio-model-routing` (`teams/technology.md:363-388`) carries RM-1 Harness & Model Routing's exact mandate *and* its primary metric (NF-A cost per task). The published Intelligence/Applied-AI boundary covers evaluation only. **✅ Resolved in two cuts. 2026-08-27 (ADR 0035): the Finance half — `aio-model-routing` produces `nf_a.cost_per_task`, `fin-inference-cost` consumes it; ledger-grain divergence filed as evidence (`api_spend` lacks `task_type`, the NF row carries it — `inference-cost-agent-stack` §6), fix = extend `api_spend`, unscheduled. 2026-08-28 (ADR 0036, founder): the RM-1 half — two plans in parallel, in harmony: RM-1 owns the METHODOLOGY (benchmark design, what cost-per-task means, substitution-study rules), `aio-model-routing` owns the OPERATION (the shared wrapper — real since P1 as `common/model-client` — and the production routing policy). TECH-F3-style escalation: if the line fails, merge — never duplicate.** | Two teams in two divisions own the same job and the same metric. Proposed interim: one shared wrapper. | Founder or Architecture Review. |
| OD-31 | **Agent headcount — re-measured 2026-08-26; the spread narrowed and half the entry is closed.** ~~19 specs / 23 registered / 24 (`PROJECT.md:33`) / 26 on disk~~ — all four figures were wrong and the citation pointed at a milestone heading. Measured: **24** `agents/*.py` files (excluding `__init__`), **23** declaring a `BaseAgent` subclass, **23** in `core/orchestrator.py`'s class map, **23** `DEFAULT_AGENT_SPECS` entries — **symmetric difference empty** — against "~24 agents at mixed maturity" at **`PROJECT.md:58`** (was `:51`, drifted with the P2 rewrite — corrected 2026-08-27; all four figures re-verified holding). ~~Roster defects are ≥7, not ≥2: 3 unregistered `BaseAgent` subclasses with zero call sites~~ → **zero**: the only unregistered agent file is `agents/recurring_order_agent.py`, whose `RecurringOrderAgent` is a plain class, not a `BaseAgent`, reachable only from its own factory and its own test. ~~and 4 registered agents resolving spec from a silent `{}` at `core/agent_registry.py:337`~~ → **fixed**: `register_from_defaults` raises at import time on a missing spec *or* a spec omitting `tier`, and its docstring names this exact failure. | `core/orchestrator.py` records this defect class already killing the inbound-email pipeline once. The guard the entry asked for now exists in code, so the reactive-discovery risk it describes is retired. | **Milder than recorded — the ≥7 roster defects are 0.** What is left is documentation, not defence: pick one authoritative count and put it in one place, and decide whether `recurring_order_agent` becomes a real agent or is deleted. People & Agent Ops. |
| OD-33 | **Insight-type count — settled 2026-08-26: `INSIGHT_CANDIDATES.length` is exactly `573`.** The 2026-08-26 audit filed this UNVERIFIABLE for want of `node_modules`; it needed neither. `insight-catalog.ts` has **no imports** and declares itself "pure data + pure functions", so transpiling it standalone and reading the export settles it — **573**, identical across repeated runs. By category: tables 174, efficiency 108, sales 82, staff 50, risk 40, inventory 34, forecast 30, purchasing 27, goals 22, basket 6. ~~Four values circulate (348, 375, 573, and `>=200`)~~ — three of the four are simply wrong; **573** is what the code builds. | The contradiction the entry named is real and unchanged: `insight-catalog.spec.ts:10` asserts only `toBeGreaterThanOrEqual(200)`, so all four values pass and nothing would have caught the drift. Knowing the number does not fix that. | Analytics & BI `metric-contract-truth-assurance`: replace the floor assertion with an exact-count one, then correct 348 / 375 wherever they are quoted. Claim filed with `status: open` so the guard strikes this off the moment the assertion is pinned. |
| OD-46 | **Only ~~4 of 482~~ 5 of 485 loops actually run** (re-measured 2026-08-27): **438** `proposed`, 29 `blocked`, 9 `dormant`, **4** `gated`, **3 `active` + 2 `running`**. | The honest headline for the whole corpus: the org is designed, not operating. Useful as a baseline to drive up, dangerous if read as capability. | Decision Office — track `loops_running` as the org's real activation metric. |
| OD-51 | **Zero of 17 Pro tiers ship complete**, and 8 are fully unbuilt (guest ×2, Floor Checker ×3, connector-failure ×1, plus others). The two closest — S10 forecasting and S08 price drift — work for **wine only**; S08's food path is scaffolding that would recommend the wrong vendor. **The fork itself is confirmed open (2026-08-26): no ADR resolves it, and `03-scenarios/TIER-MAP.md` records `pro \| 0 \| —`, consistent with OD-48.** ~~The *completeness* figures are unverified — nothing in the repo asserts them~~ — **no longer true (2026-08-27):** `TIER-MAP.md:86` grades Pro **0 unqualified / 8 partial / 8 unbuilt**, and S10 is *not* wine-only (`TIER-MAP.md:59`: "built; POS for depth") — only S08 is. | A Pro tier is being described in the corpus that a customer could not receive today. Selling it would be the overstatement the Editorial Gate exists to catch. | **What would settle it, precisely:** a scenario-by-scenario build audit walking each file in `.planning/03-scenarios/` and grading its §10 Pro band against shipped code — one row per scenario recording built / partial / unbuilt with a `file:line` citation for each verdict. That is a work item, not a lookup, and until it exists the "0 of 17" and "8 unbuilt" numbers should be quoted as unverified. Product & Vision + Analytics & BI: either build or restate. |
| OD-52 | **OD-03 may be a category error.** `core/base_agent.py` has **zero LLM integration** (one grep hit for `completion`, in a shutdown log) — it is RabbitMQ/saga/DLQ infrastructure, while hermes-agent and deepseek-harness are *reasoning* harnesses. The bake-off compares unlike things; the real question may be "which reasoning layer sits ON our messaging infra". | Reframes the most-cited open decision in the corpus before anyone runs a misconceived bake-off. | Founder reframe, then the bake-off. Found by the library agent. |
| OD-53 | **The SEO pipeline's step 1 may not be buildable as specified.** No API path exists to harvest a user's own Perplexity searches — only manual export or third-party scrapers. §12B's keyword-harvest step is UNVERIFIED at best. Also: AnswerThePublic **does** have an API (Alpha, paid, 60 req/min), closing an unknown three Growth docs carry. ~~Both halves remain unverified — nothing in this repo can settle either~~ — **half settled (2026-08-27):** the AnswerThePublic claim is verified in-repo at `05-library/answerthepublic.md` (`verified: 2026-08-24`, source URL: Alpha API, per-workspace tokens, paid plans only, 60 req/min). Only the Perplexity half remains open — `05-library/perplexity-search-harvest.md` is `status: unverified`, a dated no-evidence search pass, and guessing past it is what ADR 0016 forbids. | Growth's G1 charter has this as its first task; better it starts from the truth. | **What would settle it, precisely:** (a) fetch Perplexity's current API reference and record whether any endpoint returns the authenticated user's own search history — URL + retrieval date; (b) fetch AnswerThePublic's API page and record tier, price and published rate limit — URL + retrieval date. Two dated lines in this entry, in the form ADR 0016 fixed for model rate rows. Until then neither half may be quoted as fact. Growth `search-demand-research`. **✅ Both halves settled by fetch 2026-08-28 (Growth's wave-3 agent; the two dated lines, exactly as this row prescribed):** (a) **Perplexity: NO such endpoint** — the complete published API index (https://docs.perplexity.ai/llms.txt + /api-reference, retrieved 2026-08-28) has no endpoint returning the user's own searches/threads/Library; corroborated by the zero-retention policy (/faq/faq) and a staff promise dated 2025-06-08 still unmet (community.perplexity.ai/t/478). Absence of the capability, not merely of confirmation. (b) **AnswerThePublic: API EXISTS, Alpha** — per-workspace tokens at Account → API Access, 60 req/min per token, paid plans only (answerthepublic.zendesk.com article 15219088022555, retrieved 2026-08-28); pricing Starter $20 · Growth $99 · Business $199/mo (answerthepublic.com/pricing, same date). Residual, stated: no tier lists API access on the pricing page, so the cheapest API-carrying tier is unconfirmed. A third-party claim of "no API" (thatmarketingbuddy.com, 2026-07-08) is wrong — recorded so the next searcher is not misled. Full evidence in `growth-agenda-full.md`. |
| OD-56 | 🔴 **RE-SCOPED 2026-08-25, and the re-scoping's central finding no longer holds — re-measured 2026-08-26 with Dependabot alerts back ON.** ~~Cross-checked all 15 open Dependabot PRs against the 9 open CVE alerts: **zero overlap** … Merging the whole queue would have closed **none** of them.~~ Alerts were **disabled** on the repo when that was written (`GET /dependabot/alerts` → 403), so the "9 open CVEs" premise was unobservable. With alerts enabled the true figure is **315 open** — **11 critical, 151 high, 126 medium, 27 low**; 220 npm, 95 pip. Criticals: `python-jose` ×2, `vitest` ×2, `tar`, `shell-quote`, `jspdf`, `handlebars`, `form-data`, `basic-ftp`, `@xhmikosr/decompress`. Worst by volume (critical+high): `pillow` 16, `axios` 11, `python-multipart` 10, `tar` 9, `urllib3` 8, `undici` 8, `jspdf` 7. **The queue is now 18 PRs, not 15, and the overlap is no longer zero:** #22 (`jspdf` 4.0.0→4.2.1) is the first patched version for a **critical** jspdf advisory plus four more; #89 (`pillow` 10.2.0→12.3.0) patches the pillow set; #88 (`scikit-learn` 1.4.0→1.5.0) patches one. **Worse than recorded — by ~35×.** | The queue's length was mistaken for its risk, and then the risk was measured through an instrument that was switched off. **Python half still fixed** — `aiohttp` 3.9.3→3.14.3 and `cryptography` 42.0.0→50.0.0, pinned in both requirements files. **Node half still untouched**: root `package.json` `pnpm.overrides` carries only `typescript` / `@types/react` / `@types/react-dom`, so no transitive is pinned. ⚠️ PR #16 (`@storybook/addon-viewport` 8.6→9.0) would still re-break Storybook, which #53 repaired by aligning to 8.6.14. | Merge the three PRs that now demonstrably close alerts (#22, #88, #89) before the upgrade churn; then Node transitives via `pnpm.overrides`. Re-run the count after each — this entry's numbers are dated 2026-08-26 and will rot. `scripts/check_decision_claims.sh` cannot see GitHub, so nothing here is guarded. |
| OD-58 | **What sample size may an NF-A cost number be published at?** `scripts/nf_readout.py` labels anything below **30 agent events** `INSUFFICIENT VOLUME`. **Corrected 2026-08-25:** this entry said it *refuses* below 30. It does not — it prints the full table under the banner and exits **0** (`nf_readout.py:187-194`, `:232-233`); only `events == 0` withholds a number, and only `--require-volume` changes the exit code. So the gate this entry describes as already-enforced is in fact advisory, which makes the question more urgent, not less. The 30 is Claude's presentation default — nothing in `.planning/decisions/` sets a bar. | A cost-per-task from 5 smoke rows and one from 5,000 production rows print identically without a rule. The whole point of the readout is that it cannot be quoted out of context. | Founder or Research & Math sets the threshold — or replaces the flat number with a confidence-interval rule, which is the better answer if the variance is wide. |
| OD-66 | 🔴 **`toast.service.ts:520` (was `:502`, drifted — corrected 2026-08-27) is a second live depletion path still carrying `?? "bottle"`.** [[0011-pos-sale-volume-contract]] deleted the default from the POS hub, but Toast has its own door into stock, and its `"glass" \| "bottle"` cast is now a lie — the column is open text. All 10 restaurants declare `pos_system='toast'`, so this is the path a real integration would actually use. | Fixing the hub and leaving Toast is fixing the door nobody walks through. `resolveSaleVolume()` was exported pure precisely so Toast can adopt it in one line — but doing so changes live depletion behaviour on the most likely first integration. | **Founder call**, and it pairs with OD-64: if Toast is the first integration, this is a prerequisite, not a follow-up. | *(2026-08-25: a duplicate of this decision was filed as a second OD-64 by a parallel session and has been folded in here — same subject, `toast.service.ts:502`.)*
| OD-67 | 🔴 **A voided glass returns a whole bottle** (B19). The void path reverses stock in whole bottles regardless of how the sale was booked, so five voided glasses return five bottles — the same 5× error fixed on the sale side, mirrored on the return side and pointing the other way (stock reads high). Found while building 0011; deliberately not fixed in it. **Absorbs OD-65 (merged 2026-08-27)** — the same fork filed twice; OD-65's option set stands: a reversing RPC (`p_pour_ml` returned to `open_bottle_ml`), or queue the void as a third `pos_unresolved_lines.reason` for a human. **Re-verified 2026-08-27, blast radius wider than recorded:** the hub's void branch (`pos-hub.service.ts:573-583`, still commented "B19 (unchanged, and wrong)") has an independent Toast twin — a glass void booked as `apply_stock_movement` with `p_delta: qty` on live stock (`toast.service.ts:532-544`) — so superseding B19 means two call sites, not one. | It inflates stock rather than depleting it, so it will never show up as a shortage — the direction nobody notices. Voids are the one POS event a restaurant fires when something has *already* gone wrong, so the data is worst exactly when it matters most. | Reverse by the same volume the sale removed, which `sale_volume_ml` now makes knowable. Needs a call on historical voids: recompute, or accept and move on. |
| OD-64 | 🔴 **Is Toast the first real POS integration, or do we stay provider-neutral until a partner asks?** `TOAST_WEBHOOK_SECRET` is unset in every `.env`, and Toast **fails closed** — `enforceSignature()` returns true unconditionally in production (`toast/toast.service.ts:117-119`; rejection branches at `:217-233`) — so every Toast webhook is rejected today. ~~`toast.service.ts:113`~~ and ~~`pos-hub/`~~ are both citation drift: the file lives at `apps/api-gateway/src/toast/`, and the fail-closed logic moved. That is the safe direction, but it means the integration cannot work, and the boot log has been warning about it in production unnoticed. The surrounding facts pull both ways: all **10** restaurants declare `pos_system='toast'` and exactly one stores `pos_credentials`; `TOAST_CLIENT_ID` and `TOAST_CLIENT_SECRET` are set while `TOAST_API_KEY` is not; and **OD-38** (resolved) locked **POS-agnostic** positioning. | This is not a missing string. Pasting the secret makes Toast the de-facto first integration and the reference implementation everything else is measured against — a commercial decision wearing the costume of a config change. ~~`pos_checks` has 0 rows, so whichever POS lands first defines what the analytics layer has ever seen.~~ **Wrong — re-measured against production 2026-08-26: `pos_checks` holds 66 rows**, every one a `generic_webhook` `P3PROOF-*` fixture on the default restaurant, imported 2026-08-24 (one distinct `source` value). The analytics layer has therefore only ever seen the **provider-neutral** shape, which *strengthens* the provider-neutral option rather than leaving the question open: it is the door that has actually carried traffic. | **Founder call.** If Toast: the webhook signing secret + `TOAST_API_KEY` from the Toast dashboard (Claude cannot retrieve these). If provider-neutral: `generic_webhook` already accepts the canonical shape from any source, needs no partner agreement, and is the only door with rows behind it. |
| OD-60 | **`" 2"` duplicate-file policy — hide them or surface them.** `.gitignore:89-91` (was `:71-73`, drifted — corrected 2026-08-27; the tree currently carries zero stray duplicates) ignores `* 2.py` / `* 2.md`, so macOS collision copies are *ignored*, not untracked, and `git status --porcelain --untracked-files=all` never lists them (that flag does not report ignored paths). Two sat hidden ~4 months until a filesystem walk found them on 2026-08-24: `tests/test_golden_path_e2e 2.py`, which made pytest collect five e2e cases **twice** (840 local vs 835 clean), and `api/pos_routes 2.py`, a superseded Toast-only router. Both deleted. The globs are also narrower than the problem — `* 3.py`, `* 2.ts`, `* 2.tsx`, `* 2.json` are neither ignored nor flagged, so the next duplicate arrives as an ordinary untracked file that `git add -A` would commit. | Low severity in blast radius, but what is broken is the **audit**, not the files: any hygiene sweep built on `git status` reports a clean tree while duplicates accumulate silently. The ignore rule was added `1f96a78` (2026-04-13), one day after both strays were written — the symptom was silenced, not removed. | Founder picks: (a) widen the globs, (b) drop the rules so duplicates surface in `git status` and get deleted on sight, or (c) keep them ignored **and** add a CI/pre-commit check failing on the ` N.ext` shape. (c) is the only option giving both a quiet status and visibility; (b) matches how these files actually arise, since they are never wanted. Detail in [v3.0-TECH-DEBT.md](../v3.0-TECH-DEBT.md). |
| OD-68 | 🔴 **`provider_important_dates` does not exist in production.** Found while closing OD-63: the table is absent from every schema (`information_schema.tables` returns nothing matching `%important_date%`), and it lives only in `supabase/migrations_archive/20260208024921_baseline_schema.sql` — never in the live migration chain. `calendar_agent._extract_important_dates` upserts into it inside a `try/except` that logs a warning and moves on, so **every extracted date the agent has ever produced was silently discarded**, real ones as well as OD-63's fabricated ones. `calendar_events` confirms it: 12 `manual` + 7 `system_generated` rows, **0** from `ai_extraction`. | This is why OD-63's cleanup count is zero — the poison never reached a table. But the same swallow means the feature has never worked in production and nothing said so. The `except Exception` around the upsert turns a missing table into a debug-level shrug. | **Founder call**, and it is a fork not a fix: (a) add the table to the live chain and turn the feature on for the first time — which needs the OD-63 fix in place first, or it starts by writing the invented dates for real; (b) delete the write path and the agent's date-extraction branch, since nothing downstream reads it; or (c) keep it dark but make the swallow loud, so a missing destination is an error rather than a warning. |
| OD-76 | **`embedding` emits no NF row at all on failure, and stores a sha384 hash as a stand-in vector** (`provider_conversation_agent.py:1269-1270`, reached via the `except` at `:1266` — the original `:1238` cite now lands on the *legitimate* mock-mode branch; corrected 2026-08-27. Sharper than recorded: the file emits no NF row anywhere — zero `emit_nf`/`neural_footprint` hits). A fabricated vector is silently retrievable and will match nothing meaningfully, while the absent row means the failure is invisible to the instrument. | Two separate defects sharing a line: one corrupts the data, the other hides that it happened. | Decide whether a failed embedding writes NULL and an NF row (consistent with [[0016-ledgers-must-express-unknown]]), or is retried. Not obviously the same call as the ledger one — hence an entry rather than an assumption. |
| OD-77 | 🟡 **Migrate Google Cloud / Workspace from a personal account to a company account** *(founder-stated 2026-08-25: "must and will change this"; DEFERRED by the founder, not by a session)*. Everything Google-shaped currently hangs off a personal account: the Gmail sender (`GMAIL_SENDER_EMAIL` is a gmail.com address), the OAuth client, the Vertex/Pub/Sub project (`GMAIL_PUBSUB_TOPIC` → `projects/wineops-vertex-ai/...`), and by extension the Pub/Sub push subscription. **Runbook committed 2026-08-27** (`OD-77-workspace-migration-runbook.md`, previously an untracked file in one checkout); no step has happened — setup docs still instruct a personal sender, `GMAIL_PUBSUB_TOPIC` is still a placeholder, and Railway `preserve()`s both vars. | Three separate costs, and only the first is cosmetic. (1) Customer-facing mail arrives from a personal gmail.com address. (2) A personal account cannot carry org-level IAM, audit logging, or a service-account boundary — so **OD-78 is blocked behind this**. (3) Single-person ownership is a bus factor on production email and the AI project; there is no second admin. | Create the Workspace/Cloud org, move or recreate the project, re-issue the OAuth client and Pub/Sub subscription, re-point `GMAIL_*` env vars, verify inbound and outbound mail end-to-end. Sequence it **before** any customer onboarding — migrating a live mail domain under real vendor traffic is materially harder than doing it now. |
| OD-78 | 🟢 **Gmail push enforcement is ON in production — the entry's premise was wrong.** ~~🔴 verified in code but enforcement is staged OPEN in production; both `GMAIL_PUBSUB_AUDIENCE` and `GMAIL_PUBSUB_SERVICE_ACCOUNT` are unset in `.env`, `.env.local`, `.env.sim` and on Railway (verified 2026-08-25), so the endpoint accepts unverified pushes and logs an error per request~~. **Re-probed 2026-08-26 (twice, a day apart): an unsigned `POST /api/v1/communications/webhooks/gmail` returns `401`.** Repo-root `.env` carries all three keys with non-empty values — `GMAIL_PUBSUB_AUDIENCE`, `GMAIL_PUBSUB_SERVICE_ACCOUNT`, `GMAIL_PUBSUB_REQUIRE_AUTH=true`. **The door is shut; it is milder than recorded.** **Retirement condition re-checked 2026-08-27: still unmet** — the row's own env-template grep exits 1 (templates carry only `TOPIC`/`SUBSCRIPTION`), and the staged-open branch (`gmail-push-auth.service.ts:85-91`, `unverifiedPushCount++` → `return true`) is intact; open on the residual, premise already corrected (PR #104). | **One residual, and it is an inference the 401 cannot settle.** `gmail-push-auth.service.ts` returns `false` — a 401 — on *two* different paths: full OIDC verification failing (the pair set), and the fail-closed flag with the pair absent. Both are safe; only the first is verification. Which one is live on Railway was **not** determined: the `RAILWAY_TOKEN` in `.env` is rejected by the CLI (*"Invalid RAILWAY_TOKEN"*), which also disables the "check `railway status` after every merge" step in project memory. | **To settle it:** a working Railway credential (`railway variables`), or one production log line — the staged-open branch logs `Gmail push accepted WITHOUT verification (<n> since boot)`, so its absence over a window with live pushes is the proof. **The end state that retires this entry is not a config value but a deletion**: the staged-open branch (`unverifiedPushCount`, the only path that returns `true` without OIDC) should go, per its own comment (*"this branch is dead"*). Claim filed `open` against that. ~~The executable claim greps `.env.example`~~ — a file that **does not exist** (the template is `env.example`) and that cannot see production either way; replaced. |

| OD-88 | **The mock-only e2e tests have never run — now proven by running them.** Every test under `services/agent-orchestrator/tests/e2e/` is skipped unless `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` are set, because `conftest_prod.py:218` is a **session-scoped autouse** fixture depending on `prod_supabase` (which calls `pytest.skip` at `:199-203`). `test_studio_pipeline.py` is mock-only and says so in its own docstring, yet has never executed locally or in CI without production secrets — which is how an unusable `redeem_invite` carried a passing test through to [ADR 0021](0021-studio-invites-are-self-service.md). **Observed 2026-08-26** (the 2026-08-26 audit could only cite source; the tree was free this time): `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` unset → `pytest tests/e2e/test_studio_pipeline.py` reports **`11 skipped`**, all 11 collected. Citation drift corrected: `:217` → `:218`. | An autouse fixture silently turns an entire mock-only subtree into dead tests while the suite still reports green. Any claim resting on "the e2e tests pass" is unfounded until this is separated. **Confirmed at the exact severity recorded — neither worse nor milder.** | **Founder call:** split the genuinely-mocked tests out so the autouse fixture attaches only to the prod-credential subset, or mark that subset with `prod_e2e` — a marker that already exists at `pytest.ini:12` and is unused for this. |

| OD-89 | **`apps/api-gateway/openapi.json` is a committed artifact that nothing reads and every local boot rewrites.** `main.ts:153-156` writes it on any `NODE_ENV !== "production"` startup, so merely running the gateway locally dirties the working tree. It is never read back: the only references in the repo are the two `writeFileSync` calls (`main.ts:155`, `openapi.ts:28`) — no import, no client generator, no CI check. It is also **already stale on `main`**: `git show HEAD:apps/api-gateway/openapi.json \| grep -c sale-unit` returns **0** while `pos-hub.controller.ts:36-38` has served three `sale-unit` routes since the ADR 0011 work. Found while splitting it out of the ADR 0020 branch, where a local `main.ts` run had regenerated it and swept 500 lines of unrelated pos-hub drift into a studio-invite diff. | A generated file that auto-writes on boot and is never read is a diff-noise generator: it silently attaches unrelated churn to whatever branch happens to run the gateway, which is precisely the reviewability problem CLAUDE.md §2 exists to prevent. Nobody notices it is stale because nothing consumes it. | **Founder call, two ways.** ① *Keep committing it and make it true* — gate the write behind an explicit `EXPORT_OPENAPI=1` (so booting is side-effect free), regenerate on a dedicated branch, and add a CI check that fails when the spec differs from the code. ② *Stop committing it* — add to `.gitignore`, keep `pnpm openapi:export` for anyone who wants it, and delete the tracked copy. ② is cheaper and matches how the file is actually used today (not at all); ① is right only if something external is meant to consume the spec. Either way the boot-time write should stop. |
| OD-91 | 🟠 **FOUNDER'S CALL — do existing tenants get scheduled communications by default, or stay opted in explicitly?** OD-87 shipped **gated**: `ScheduledTenantsService` serves `DEFAULT_RESTAURANT_ID` ∪ restaurants flagged `scheduled_communications` in `restaurant_feature_flags`. ~~No flag rows exist~~ — **the table holds exactly one row**, a `self_evolution` flag on the default restaurant created 2026-03-04; there is no `scheduled_communications` row, which is the part the entry actually depends on, so behaviour is unchanged for everyone. The question is what happens next. | **Opt-in (recommended, and what shipped):** nothing sends until you say so per restaurant. **Auto opt-in all active:** a migration seeding flag rows for all 10 — but 9 of those 10 are your own dev/demo signups, indistinguishable from a real tenant by `is_active` or `deleted_at`. **Production shape re-measured against the live DB 2026-08-26:** 10 restaurants, **all 10 active** (a soft-delete of 3 on 2026-08-26 was reverted the same hour on the founder's instruction, so `is_active` still separates nothing). | **Two of the entry's own facts were wrong, and the real defect is worse.** ~~production has **no `staff` role at all**~~ — a `staff` role **does** exist: `users` is 6 `owner` / 3 `manager` / 1 `staff`. But the one staff account has `restaurant_id = NULL`, so no tenant has a staff member. ~~6 of 10 restaurants have only `owner`~~ — **6 of 10 have no user rows at all**; the 10 users group onto only **4** restaurant ids (2 carry `owner` only, 2 carry `manager` + `owner`). **So the recipient-resolver risk is sharper than recorded:** those six resolve zero recipients for *any* role list, and adding `owner` — the mitigation the entry floats — fixes nothing for them. It would still change who gets mail at the existing tenant, so it was not defaulted. |
| OD-92 | 🟡 **The crons ignore each restaurant's timezone.** ~~All 8~~ **9** tenant-scoped `@Cron`s (10 `runPerTenant` sites) in `communications/scheduled-tasks.service.ts` — 8 pinned to `timeZone: "America/New_York"` and the ninth, `custom-reminders-check` (`:702`, every 15 min), carrying **no timeZone at all** (re-measured 2026-08-27), while `restaurants.timezone` is a real column with **3 distinct values in production** (`America/Los_Angeles`, `America/Chicago`, `Europe/Istanbul`). Surfaced while fixing OD-87; `ScheduledTenant.timezone` now carries the value so the fix has somewhere to land. | The "9am digest" fires at 09:00 New York for everyone — 06:00 in Palo Alto, 16:00 in Istanbul. Harmless at one tenant in one timezone; wrong for the second restaurant onboarded, and wrong in a way that reads as carelessness rather than a bug. | Honouring it needs **per-tenant scheduling** (a scheduler that fires per timezone, or an hourly sweep that asks "whose local 9am is it now"), not a per-tenant loop inside a single fixed-time cron. Sequence after OD-91 is answered. |
| OD-98 | 🟡 **`notification_agent.py` still reads `push_subscriptions`, and it is live-but-broken rather than dead.** ADR 0027 deleted the gateway's push read; the Python path was deliberately left. `PUSH_SUBSCRIPTION_TABLE = "push_subscriptions"` (`agents/notification_agent.py:37`) is queried by `_get_push_subscriptions` (`:1596`, reached from `_push_targets` at `:1650`) against a table production returns **404** for. Unlike the resolver's inert field, this one appends a *failed push leg* that feeds `_log_notification`'s `success` flag — so deleting it changes recorded behaviour rather than removing dead weight. It also cannot simply be repointed: `notification_preferences.push_subscription` has an unwritable writer (42P10 — see the OD-95 Resolved row and `supabase/migrations/20260813090000_fix_remaining_upsert_targets.sql`), and the leg it records does not persist anyway because `notification_logs` is itself 404 (OD-99). | Third table in one dependency chain that production does not have. The Python notifier currently records a delivery outcome computed from a leg that can only ever fail, into a log table that does not exist. | Give it the same treatment ADR 0027 gave the resolver — delete the push leg and let asking for push be an error — or build the storage model. **Its own operation, not a rider on this one.** |
| OD-100 | **What price values inventory?** OD-99 repointed `_generate_inventory_report` (`services/agent-orchestrator/agents/reporting_agent.py`) from the phantom `inventory_stock` to `restaurant_inventory`, which has no `wine_price` column. `_unit_cost` now reads `last_purchase_price`, falling back to `custom_price`, and contributes 0 when a row has neither rather than inventing a number. | `total_value` is the headline of every inventory report. Cost basis and menu price differ by the whole markup: valuing at `menu_price_current` would report revenue potential as an asset. The choice was made to ship a working report, not because it was decided. | Founder picks the basis. `_unit_cost` is the single place to change it. |
| OD-101 | **Should the email composer read and write `provider_knowledge`?** OD-99 deleted the style cache in `services/agent-orchestrator/services/email_composer_service.py`, which targeted the phantom `provider_digital_twins`. The real store exists — `provider_knowledge`, `category='relationship'`, `subcategory='communication_style'` — and `ProviderConversationAgent` already reads it. | Without a cache, `_analyze_style_llm` re-runs an **LLM call per composition**. That cost is real and was always being paid; the deleted cache never once avoided it. | Founder call. `provider_knowledge` carries `confidence` and `verified` columns, and what an LLM-guessed profile should claim for those is not defaultable — that is the actual decision, not the wiring. |
| OD-102 | **Which table is a restaurant's "own wine library"?** OD-99 deleted Phase 1 of the match pipeline in `services/agent-orchestrator/services/wine_matcher.py`; it searched the phantom `wine_library` scoped by `restaurant_id`, never returned a candidate, and `phase="user_library"` was never reachable. | Every match ever made came from Phase 1b/1c/1d against `master_wine_library`. A per-restaurant first pass would be faster and more accurate for wines the restaurant already knows. | Founder picks: `restaurant_inventory` (what it HAS — but identity lives on `master_wine_id`, i.e. what 1b already searches) or `menu_items` (what it SELLS — carries `name`/`producer`/`vintage`/`category` per restaurant, the exact shape the deleted select asked for), or neither. |
| OD-103 | 🟠 **Relationship-health alerts are about to fire for the first time.** OD-99 removed the phantom `get_inactive_providers` RPC from `_check_relationship_health` (`services/agent-orchestrator/agents/provider_conversation_agent.py`), which un-shadows the direct query that was unreachable behind it. | This is a **live behaviour change on a dormant path**: an alert nobody has ever received starts arriving. Bounds are unchanged from what was written — ≤20 providers per pass, `is_active` only, no session since the cutoff — but nobody has ever seen the volume, because it has always been zero. | Founder decides whether to ship it on, gate it behind a flag, or tune `relationship_alert_days` first. Nothing here blocks the rest of OD-99. |
| OD-104 | **Build the real vector search over `conversation_embeddings`?** OD-99 deleted the phantom `search_provider_conversations` RPC from `apps/api-gateway/src/providers/provider-intelligence.service.ts`; its `ilike` substring fallback was already the implementation on every request and is now the body. | The code called it "vector similarity search" while doing substring matching, for as long as it has existed. That is now stated honestly rather than implied by a call to a function nobody wrote — but the capability is still absent. `conversation_embeddings` genuinely carries embeddings, and `match_conversation_embeddings` **does** exist in production, so most of the machinery is already there. | Founder decides whether semantic search over provider conversations is wanted now. It is a feature, not a repair. |
| OD-106 | **Design foundation direction** — every page grew from its own sketch, and the app now runs 9 layout archetypes across 47 routes, two burgundies in circulation (`#9E4249` in `tailwind.config.js` vs `#CD2D5B` in `sketches/themes/default.css:6`), and no shared component vocabulary. Which direction, and which primary? **Filed 2026-08-26 to correct a citation defect:** this fork had been circulating as *OD-79* in 47 page notes and [[DESIGN-FOUNDATION]] — but OD-79 is the **resolved** email-verification decision ([ADR 0023](0023-email-verification-is-enforced.md)), so every one of those references pointed at a different, closed decision. An id that resolves to the wrong decision is the exact failure [ADR 0025](0025-citations-must-disagree-loudly.md) §6 was written for, and it escaped the guard because a bare `(OD-79)` in frontmatter carries no line anchor and so is not a citation under §6. | The founder deferred the workstream 2026-08-26 — **documentation only**, no sketches, tokens or builds. Archetype map done as documentation; direction and primary both still open. Cost of waiting is now explicit: P3.A ships mobile as-is, so this reconciles two surfaces rather than one ([ADR 0029](0029-p3-plan-of-record.md)) | One founder session on direction (A/B/C) and the primary. Plan and decision-state table: [[DESIGN-FOUNDATION]] §0 |
| OD-111 | **Which mark is the Mudavym logo?** Every 053 logo was cut in wave review; the sole survivor is 054's "Meter". Interim shipped 2026-08-30: Fraunces text wordmark + "M." glyph ([[0043-wordmark-interim-logo-search]]). Two candidate waves (instrument · house, 6 marks each) are on the logo-search canvas. | dev.mudavym.com ships the interim today; every surface that needs a glyph (rail, favicon, app icon, seal-stamp interaction) is waiting on the real mark. | Founder elimination pass over the two candidate sheets; verdicts land in ADR 0043. |
| OD-113 | 🔴 **Does the core inventory ledger get altered to admit non-beverage items?** `restaurant_inventory.master_wine_id`, `inventory_lots.master_wine_id` and `inventory_transactions.wine_id` are `NOT NULL` and quantity columns are `integer` — verified across all 64 migrations in their latest state. No generic item path, no polymorphic type column, no food table. 4.5 kg of flour is unrepresentable. **Intake is fine** — `procurement_document_lines` is already `numeric(12,3)` with a 7-value `uom` CHECK — so the break is specifically at the ledger. | This is the gate on the founder's stated goal ("turn food into math", [[0048-domain-quant-under-research-math]]). Everything at L2 and below in [[FOOD-REASONING-GRAPH]] waits on it, and it is an `ALTER` of column types and nullability against live production data, not an additive migration. | **Founder call** on whether to migrate, then a migration path that does not break schema parity. |
| OD-114 | **Where is the line between costing *definition* and arithmetic *conformance*?** `research-math-charter.md` hands "grading deterministic arithmetic against a ledger" to Analytics & BI's AB-3. Recipe costing *is* deterministic arithmetic against a ledger — but the butcher's-test by-product credit returns **$21.78/kg vs $23.35/kg for the same pork loin** depending on method: a 7.3% plate difference, both textbook-correct, both surviving any exact-equality check. | Two units chartered on one mandate is the defect shape this org has hit twice already (OD-29, and the evaluation seam). Naming the line now costs a sentence; finding it later costs a rebuild. AB-3's instrument is silent on *which definition is right* because it takes the definition as given. | Founder or Architecture Review. Proposed boundary: research owns the **definition**, AB-3 owns **conformance to it**. |
| OD-115 | **Is cross-tenant pooling contractually permitted, and where is that recorded?** Elasticity is a category-level, partially-pooled estimand (a 10% price move on 100 units per arm returns a ±2.9 CI containing both "raise" and "halve"), and cold-start priors need pooling too. **No DPA or ToS exists anywhere in `.planning/`.** | Two of the three L5 decision surfaces are unbuildable per-tenant. Compounding: production has 10 restaurants but **1 real tenant** and no column distinguishing a fixture from a customer, so a pooled analysis run today would report nine fixtures' shape as the world. | Legal + Compliance & Privacy. Default until answered: **tenant-local only**, pooled findings blocked. |
| OD-116 | **The domain-quant team carve — three teams or one?** Argued shape is three, by kind of math: deterministic (yield + costing) · stochastic (forecasting + inventory, which are one calculation, not two) · decision economics (menu + pricing). Alternative is one unit that splits on evidence. | ADR 0048 locked the *placement* and the *program*, not the carve — the founder redirected that question toward operating depth. OD-26/OD-43 measured **15 split triggers against 3 merge triggers** org-wide, and this division has zero people and zero shipped domain code, so three units risks scaffolding around an empty room. | Founder call; ADR 0048 §7. |
| OD-117 | **Does a research unit need object types the standard 8/9-artifact template does not provide?** A research programme produces methods, dataset cards, findings and replication notes — none of which map onto charter/directive/premortem/agenda/loops/schedule/questions/agent-stack. A 10th per-unit artifact would cost ~+100 documents across the vault. | The corpus already has a measured vocabulary-sprawl failure (OD-47: 102 distinct `close_time` values, 67 free text). Whatever is added needs closed vocabularies and a retire-to-write payment (CLAUDE.md §4). | Founder call. Proposed: **zero new per-unit artifact types**; three shared object types under `07-reference/` plus a `FINDINGS.jsonl` shaped like the existing `CLAIMS.jsonl`. |

## Resolved

| ID | Resolved as | Date |
|---|---|---|
| OD-18 | ✅ **Resolved 2026-08-24 by [ADR 0007](0007-org-structure.md) — the row outlived its own answer by ten minutes.** Filed 13:19 as "deferred pending team-layer evidence"; ADR 0007 locked 13:29 the same day and took the split by name — Technology's 25-team span *was* the awaited evidence, and the division became **Platform + Applied AI** (5 → 6 divisions, then Research & Math promoted → 7). Live table: `foundation/ORG_STRUCTURE.md:26-36`, "7 divisions · 19 departments". Moved 2026-08-27 by the register backfill sweep — the **only** silently-resolved row among all 40 open entries, against the hypothesis that most were; `ORG_STRUCTURE.md` §6's stale deferral corrected in the same commit. | 2026-08-24 |
| OD-01 | ✅ **Resolved 2026-08-27 by [ADR 0032](0032-vault-cleanup-cut-line.md) — cut line C-modified, delete + tombstone; the founder picked each fork in-session** (`docs/vault-cleanup`). The entry had rotted in both directions before it was acted on: `md_files/` held 3 files, not 47; ADR 0025's sweep had already deleted 469 archive twins; and STATE/ROADMAP — the 2026-08-24 audit's largest breakage cascade — had been rewritten clean under ADR 0018. Executed: `phases/` (39 closed dirs incl. the 3 that survived only in `archive/`, rescued first so they recover from one path), `quick/`, the remaining `archive/` twins, `.next-call-count`, and 4 data blobs byte-identical to their canonical `datasets/planning-exports/` copies — ~525 files, vault 1,677→1,152, every path recoverable via the ADR's tombstone index. Kept on evidence, not nostalgia: `sketches/` (20 live code citations), `claude_full_architectural.md` (founder override), `FIX_ERROR_LOG.md` (`.cursor/skills/fix-error/` still maintains it), the two pre-P2 snapshots ADR 0018/0025 cite, and the `999.1` backlog stub. The survivor mapping was called the same day: spine stays top-level (`PROJECT`, `STATE`, `ROADMAP`, `FUTURES`, `YC_WEDGE_PLAN`, `v3.0-TECH-DEBT`); 20 docs → `07-reference/` (incl. `REQUIREMENTS.md`, a historical ledger by its own banner); `FIX_ERROR_LOG.md` → `debug/`; `AGENT_NATIVE_UI_DECISION.md` → `decisions/` pre-log; 67 files of citations rewritten. `LLM_INSTRUCTION_PROMPTS.md` was retired to a tombstone stub the same day, and `md/` + `md_files/` were executed on `docs/md-cleanup` (108 deletions, 5 keepers to `07-reference/`; only the ADR 0026 schema-debt `.sql` files remain there). Nothing is left open under this decision. | 2026-08-27 |
| OD-107 | ✅ **`strictNullChecks` is ON, and the entry that asked for it was wrong three times.** Measured before acting (`04-specs/OD-107-STRICTNULLCHECKS-MEASUREMENT.md`): **12 production errors across 6 files**, not the *"~100 gateway files"* this entry guessed — a number I wrote as though measured, in the entry whose whole point was that nobody had measured. Also wrong: *"milestone or per-module ratchet"* (it was one commit), and *"two result types flattened"* (three; the third says so in source). Fixed: 25 errors total, including **two live defects** — `procurement.controller.ts` returned **500 where the service meant 400** for an omitted optional field, and a spec carried a duplicated `userId` key whose spread silently discarded the explicit one. The three `ask-ai` flattenings were fixed by **reverting** them to the discriminated unions they should always have been. **The bigger finding survives the flip and is NOT closed by it:** a census of 28,661 production member accesses splits 86.5% sound / 1.2% nullable / **12.3% `any`**, and only 3 of 344 nullable receivers were unguarded — the code is null-safe where it is *typed*. The `any` sits at the DB boundary: `DatabaseService.supabase` carries no `Database` generic while the gateway queries **117 tables** and the generated type covers **8**. That is a separate decision, filed as OD-110. | 2026-08-27 |
| OD-108 | ✅ **P3.A counts all 219 features — no audience carve-outs.** The first mobile measurement put parity at **8.9%** (19.5 of 219 §1a features across the 47 page notes, a partial counting as a half). But 34 of those are `audience: dev` and 31 are `public`, and mobile has never attempted either — **staff 21% · owner 10% · public 3% · dev 0%**. Scoping both out would put the denominator at 154 and today's parity at 16%: a third smaller stage for identical work. Founder chose **all 219**. **Filed retroactively 2026-08-27, and that is the finding:** the answer was written straight into [ADR 0029](0029-p3-plan-of-record.md) §2 without the question ever being filed here, so for several hours the repository showed a Locked decision, a PR saying the fork was still open, and no trail between them. An adversarial audit could not tell that from a fabricated consensus — which is exactly what this register exists to make impossible. Recording the answer is not recording the decision. | 2026-08-27 |
| OD-105 | ✅ **Resolved 2026-08-26 — founder call: allowlist ratchet, same day it was filed** (`fix/register-label-associations`). `/register` fixed first: 17 labels — 16 `htmlFor`/`id` pairs (flow-prefixed `create-*`/`join-*`), 1 group label for the CuisinePicker composite (`role="group"`+`aria-labelledby`, with an inline justified disable since the rule cannot see that association), the label-less invite-code input given `aria-label="Invite Code"`, and `CountryCombobox`/`PlacesAutocomplete` given `id` pass-throughs; `navigation.spec.ts` asserts through `getByLabel` now. Then the guard: `eslint-plugin-jsx-a11y@^6.10.2` (root devDep) and `jsx-a11y/label-has-associated-control` as **error** in the `apps/web` override, with the OD-105 ratchet allowlist — **47 files** between the `OD-105-ALLOWLIST-START/END` markers in `.eslintrc.cjs`, each carrying the pre-existing backlog (eslint's own count: **298 violations in 48 files**; the filing sweep's regex said 318 — the rule is the authority now). **Fix a file → delete its line; never add a line.** Proven both directions: full web lint exits 0 with `--report-unused-disable-directives` (so stale disables also fail), and a probe file with a bare label exits 1 on exactly this rule. CI enforces it already — `ci.yml` lint is blocking. Both claims resolved in [CLAIMS.jsonl](CLAIMS.jsonl). Remaining backlog is tracked by the allowlist itself, not by this register. | 2026-08-26 |
| OD-97 | ✅ **Resolved 2026-08-26 — and the entry understated it.** Filed as "100 gateway spec files are never type-checked" (`tsconfig.json` excludes `**/*.spec.ts`). The truth was wider: `pnpm run type-check` → `turbo run typecheck` skips every package with no `typecheck` script — **silently, reporting success** — and only `apps/mobile` had one. Measured on main: `Tasks: 2 successful` and one of the two was a *build*. The gateway and web app had never been type-checked by CI at all. Fix (#105): `tsconfig.spec.json` covers the 100 specs (build config untouched, so `nest build` still excludes them), `typecheck` scripts on api-gateway + web, and the 3 spec type errors this exposed. Proven able to fail: an injected `const x: number = "s"` in a SPEC now exits 2; before, it was invisible. Every `@ts-expect-error` in a gateway spec until today was decorative. | 2026-08-26 |
| OD-99 | ✅ **Resolved 2026-08-26 by [ADR 0028](0028-phantom-relations-repoint-or-delete.md) — eleven class-C relations closed; repointed where a real store existed, deleted where it did not, created nowhere** (`fix/od-99-phantom-tables`). The entry's framing was right about the class and wrong about the scope in both directions. It named four tables; the guard's debt list showed **eleven** class-C relations — six tables plus **five `.rpc()` functions** the entry never mentioned. And two of the four it named (`provider_ratings`, `pos_webhook_logs`) are **class A**, not C: a migration for each does exist, in an archive directory, which is a different defect and a different decision — they stay on the ratchet. Verified with curl against production, not `supabase/migrations/`: 404 PGRST205 for all six tables to the **service-role** key, PGRST202 for all five functions. **Verdicts, on evidence, not one rule:** `reports` → `generated_reports` (exists; 200 to service-role, 42501 to anon; `restaurant_id`/`created_at` an exact fit) — **no migration was needed and none was written**; `inventory_stock` → `restaurant_inventory`; `managers` → `manager_report_profiles`. **Deleted:** `restaurant_wine_menus` (the JSONL files are the store every reader reads — and the insert was guarded on a `supabase_client` no caller has ever passed, so it was never even reached), `provider_digital_twins` (the digital twin **is** `provider_knowledge`; `_load_digital_twin` reads it), `wine_library` (its whole match phase, which had never returned one candidate). **The five RPCs:** three shared a defect worth remembering — the fallback each author wrote for "the function isn't there" sat *inside the same `try` as the RPC call*, so the exception jumped over it. They were unreachable code that made the call sites read as defensive, which is why `_find_provider_by_email` has returned None for **every inbound email ever parsed** and `_check_relationship_health` has **never emitted an alert**. `jsonb_array_append` was the only one that was a *write* with nothing behind it, so its failure was not a degraded read but **data loss** — every manager preference the learning loop extracted, each paid for with an LLM call. **Sharper than recorded on `reports`, and in an unexpected direction:** it is not a dashboard *card* — nothing in web or mobile consumes `summary.reports` at all; the block is computed by the gateway and rendered by no one. It is still repointed, because `GET /dashboard/summary/:id` is a documented API whose DTO promises the field. **Also found: two tests pinning defects rather than catching them** — `test_reporting_agent_bugs.py` asserted `table.assert_called_with("inventory_stock")` and `test_golden_path_e2e.py` both mocked and asserted it; either would have failed whoever fixed the bug. Every fix is revert-proved: defect reinstated, test shown failing, restored, shown passing. Guard green on both arms with class-C at zero and `KNOWN_MISSING_FUNCTIONS` empty. Five forks this opened are filed as **OD-100** through **OD-104**. The class fix the entry proposed (a `KNOWN_UNAPPLIED` guard over the 136 `.sql` files outside the applied chain) is **still undecided** and still the founder's call. | 2026-08-26 |
| OD-80 | ✅ **Resolved 2026-08-26 — applied, not decided; the blocker was a branch and it is gone** ([PR #108](https://github.com/aldemirkonuk/RestaurantAIAutomation/pull/108), `fix/od-80-dead-code`). Both halves landed in one commit with their `CLAIMS.jsonl` flips, because `scripts/check_decision_claims.sh` fails an `open` claim that already verifies — the code cleanup alone would have turned CI red. **(a)** `apps/web/src/types/companyClass.ts` deleted (743 lines, 25 exports) and both re-export blocks dropped from `types/index.ts` (lines 70–102, taking the leading blank so the seam keeps one). Re-verified before deleting: a whole-word grep for all 25 names across the **whole repo** — not just the three trees the entry named — returns nothing outside the file and its barrel. **(b)** `data/customEventTypes.ts` survives at 72 lines; `isCustomEventType`, `getCustomEventTypeByName` and `EVENT_TYPE_COLORS` removed, `isEventTypeNameAvailable` and the four `pages/calendar/EventModal.tsx:22` imports intact. `pages/calendar/CalendarPage.tsx:43` declares its **own local** `EVENT_TYPE_COLORS` (`Record<string,string>`, seven call sites) — confirmed a `const`, not an import, and untouched. | 2026-08-26 |
| OD-95 | ✅ **Resolved 2026-08-26 by [ADR 0027](0027-push-recipients-are-not-resolved-here.md) — deleted, not repointed** (PR #102, `fix/od-95-push-subscription-source`). The standing recommendation was "repoint at `notification_preferences.push_subscription` and rename". It does not survive. **Re-verified against the live DB here, and it is worse than either the entry or the handoff said:** that column's only writer is `notifications.service.ts:192`, which upserts `onConflict: "user_id"` while the table's only unique constraints are `PRIMARY KEY (id)` and `UNIQUE (restaurant_id, user_id)` — Postgres answers **42P10** and the statement cannot even be planned (held open on purpose by `supabase/migrations/20260813090000_fix_remaining_upsert_targets.sql`). ~~web push is fully built against `notification_preferences.push_subscription` … HTTP 200, **3 rows**~~ — **the 3 rows are the table's rows, not subscriptions**: `count(push_subscription)` is **0**, all 3 rows were created 2026-02-08 and have never been updated since. So web push is not "built but unused"; it is **built and never once written**, and repointing would have swapped a loud 404 for a permanently-empty read that looks successful — what ADR 0020 forbids. `pushSubscriptionIds`, `pushUnavailable`, `PushSubscriptionSourceError` and `"push"` in `NotificationChannel` are all removed, so asking for push is now a compile error rather than an empty array. Three claims superseded, each re-verified in both states by this session: all three hold on the fixed tree and all three fail against a revert. Two forks this opened are filed as **OD-97** and **OD-98**. | 2026-08-26 |
| OD-49 | ✅ **Resolved — struck 2026-08-26; the founder action was obsolete and the row was malformed.** ~~`SUPABASE_POOLER_CONNECTION_STRING` secret must be added … Claude cannot set secrets.~~ `.github/workflows/schema-parity.yml` resolves `secrets.SUPABASE_POOLER_URL \|\| secrets.SUPABASE_POOLER_CONNECTION_STRING \|\| secrets.SUPABASE_DIRECT_CONNECTION_STRING` at all four job sites, and `SUPABASE_POOLER_URL` already exists — so the secret the entry asked for is not required. Evidence: `gh run list --workflow=schema-parity.yml` shows **success on `main`** for every recent run (2026-08-26T16:29Z, 16:32Z, 16:33Z, 16:36Z). The guard against the drift that once put 27 tables and 403 columns in production is **on**. The row had sat in the **Open** table since being marked RESOLVED, carrying five cells against a four-column header. | 2026-08-26 |
| OD-57 | ✅ **Swept 2026-08-24 on `fix/od-57-retired-model-sweep`, and the original framing was wrong. Moved out of the Open table 2026-08-26** — the ✅ row and both struck-through originals had all three been sitting under `## Open`, three rows for one settled decision. The stale ids were largely *dead defaults masking a live cross-provider bug*: the orchestrator injected `llm_primary_model` (a **Claude** id) into `procurement_agent`, `provider_conversation_agent`, `sommelier_agent` and `rfq_agent`, which passed it straight to `genai.GenerativeModel()` — the **Gemini** SDK. Two of those are CORE tier with `mock_llm=false`, so they ran on every boot; their `"gemini-pro"` defaults never fired, which is precisely why the retired ids looked like the whole problem. Resolved by task per founder call: Gemini for extraction-shaped work, Claude via `get_anthropic_client()` for reasoning-shaped work. `procurement_agent`'s Gemini client was **deleted, not repointed**. The 7 genuinely-firing retired ids resolve from `settings.gemini_model`, and all 14 spend/metadata labels bind to the same variable as their call. Re-verified 2026-08-26: only **3** `gemini-pro` occurrences survive and all are inert (two retirement comments, one historical price row) — so OD-04's "failing in production today" clause is retired with it. Original census and API-key rows retained in git history. | 2026-08-24 |
| OD-40 | ✅ **Resolved — struck 2026-08-26; it was fixed and never closed.** ~~🔴 The human approval gate is unauthenticated … `pos-hub.controller.ts:169,190` sit on `@Controller("pos-hub")` with no `JwtAuthGuard`.~~ Re-verified on `main`: `pos-hub.controller.ts:63` is `@UseGuards(JwtAuthGuard)` above `@Controller("pos-hub")`, with a comment at `:41-45` naming this exact fix. Approve/reject are now at `:272` and `:293` (the entry's `:169,190` had drifted), and the only `@Public()` route on the controller is `@Post("webhook")` at `:71`, which authenticates by HMAC instead. Regression claim filed. | 2026-08-26 |
| OD-39 | ✅ **Resolved — struck 2026-08-26; it was fixed and never closed.** ~~🔴 Toast webhook signature verification is fail-open at the call site: `toast.service.ts:189` runs the verifier only `if (signature && timestamp)`.~~ Re-verified on `main`: the `if (signature && timestamp)` at `apps/api-gateway/src/toast/toast.service.ts:204` is followed by two explicit rejection branches — `:217` throws 401 when the secret is unconfigured and enforcement is on, `:226` throws 401 on a missing signature — and `enforceSignature()` (`:117-119`) returns `true` unconditionally when `NODE_ENV === "production"`. Omitting the header no longer skips the check; it is a 401. Regression claim filed. | 2026-08-26 |
| OD-37 | ✅ **Resolved — struck 2026-08-26; both remediations had already landed.** The behaviour was never the defect: auto-send exists, gated behind an explicit per-restaurant full-autonomy switch **and** every guardrail passing (`willAutoSend = autonomyFull && !flags.needs_approval`, `common/orchestrator/inbound-responder.service.ts:498`, scheduled at `:498-500` after a 2-minute undo window; `isAutonomousSendEnabled` at `:963-978` defaults **OFF** on a missing row). ~~The docstring at `:156-157` is stale~~ — it is gone: `:158-162` is now an accurate "fire-and-forget from the RabbitMQ bridge" comment, and **no "never auto-send" claim survives anywhere in the file** (grep, 2026-08-26). ~~project memory records the absolute claim~~ — `autonomous-email-replies.md` already carries the 2026-08-24 correction. Two citations in the entry were also wrong: the file is under `common/orchestrator/`, not `communications/`, and `willAutoSend` is `:498`, not `:511`. Regression claim filed. | 2026-08-26 |
| OD-94 | ✅ **Resolved 2026-08-26 — `integration_oauth_connections` / `integration_oauth_states` never existed in production, so the Drive/Excel connect flow completed the full OAuth round trip at Google and then 404'd on the write.** Fifth instance in one day of a migration outside `supabase/migrations/` that production never saw. The archived file (`migrations_archive/20260730120000`) predates the 2026-08-05 `baseline_from_production` snapshot, so the baseline missed it too. **Its DDL was right and was carried over unchanged** — FKs verified against production's real PKs (`users.user_id`, `restaurants.id`), and `UNIQUE (user_id, integration_id)` must stay **non-partial** because `storeConnection`'s PostgREST upsert emits `ON CONFLICT (user_id, integration_id)`, which Postgres binds only to an unpredicated index; the tidier `WHERE revoked_at IS NULL` would break reconnect-after-disconnect. **What it got wrong was having no security half at all** — no RLS, no policy, no REVOKE, no assertion — on a table storing `access_token_encrypted` and `refresh_token_encrypted`. Precise severity: OD-72 had already revoked `postgres`'s default table grants, so a table created *today* gets no anon grant on its own; on its original 2026-07-30 date this would have been a live leak of refresh tokens to the publishable key. Ordering luck, not a control — `supabase_admin`'s default still grants anon and we cannot alter it. Shipped with the OD-72/73 shape in the **creating** migration and a `DO $$` block asserting its own outcome, including the gateway's column contract. Verified in production: **200 to service-role, 401 `42501` to the anon key** on SELECT *and* INSERT for both tables, `relacl = {postgres, service_role}`, plus a rolled-back functional round trip proving the upsert resurrects a revoked row rather than duplicating it. 17 new tests (the module had none); each proved to fail against 5 reverts. **One of those reverts caught a vacuous assertion in my own test** — the migration's prose header and its `RAISE EXCEPTION` string both quote the DDL, so matching the raw file passed while the DDL said the opposite. | 2026-08-26 |
| OD-96 | ✅ **Resolved 2026-08-26 — the OneTapActionCenter flake, which blocked two merges and was misdiagnosed by everyone who looked at it, including me.** The CI message `element could not be found in the document` is jest-dom's **detached-node** branch, not a lookup failure: `findByText` had already succeeded and returned a real element, and `toBeInTheDocument` then found `element.getRootNode() !== element.ownerDocument`. A true miss would have thrown a `TestingLibraryElementError` with a DOM dump out of `findByText` before the matcher ran. **Raising the timeout — the instinctive fix — would have done nothing**, because the failure lands ~1ms in, not at the 1000ms default. **Mechanism:** `runAction`'s catch calls `restoreAction()` (a `setState`, which only schedules) then `toast.error()` (a `vi.fn`, recorded immediately), so the toast is observable while the DOM still shows the pre-rollback state; and the card sits inside `<AnimatePresence>` with an `exit` animation, so the optimistic removal keeps it mounted as an *exiting* child and the re-entry tears out the whole subtree rather than reusing it. `await findByText(...)` could resolve with the exiting node, RTL's async wrapper then flushed the rollback, and the assertion ran against an orphan. **Fix:** query and assert inside one `waitFor` callback, so both happen in the same synchronous tick and nothing can detach between them — closing the capture/assert gap as a class rather than shifting the odds. — **Measured, not reasoned:** 40 concurrent vitest processes, identical harness — **11/40 failed before, 0/40 after**. Serial runs pass either way, which is why it only ever appeared in CI and why three separate local reproductions came back clean. Worth noting what nearly shipped: I first applied the same `getByLabelText` refinement to the sibling test, where the approve control never returns (`low_stock` is refused with `UnsupportedActionError`), and `waitFor` span to the 5s timeout in 16/40 runs — caught by running it, not by reading it. **Two CI failures on the fix's own PR were neither the flake nor mine, and both were already fixed on `main` while I was writing them up** — the gateway lint error (`identity-first-signin.spec.ts`, an `eslint-disable-next-line` covering only the first of two `require` calls) became real `import * as fs` / `import * as path`, and the OAuth schema drift was closed by [#91] with migration `20260826170000_integration_oauth_tables.sql`. **I had that drift backwards when I first reported it:** the tables did not exist in production and returned PGRST205 even to the service-role key, so the Drive/Excel consent round-trip completed at Google/Microsoft and then failed on the write — nobody had ever connected anything. I described it as un-migrated tables already holding encrypted refresh tokens in production, which was the opposite of the truth. A rebase onto `main` cleared both. **Left open:** `CalendarPage.reminders.test.tsx` has a test at 5249ms against vitest's unconfigured 5000ms default; different failure mode, higher probability on a loaded runner. | 2026-08-26 |
| OD-93 | ✅ **Resolved 2026-08-26 — the 54-site log-injection sweep, run by five parallel agents on file-disjoint batches.** Follow-on from [OD-90](#): once the SSRF half was closed, a *fully paginated* alert query showed `py/log-injection` was not the 3 sites reported from a truncated page but **54 across 15 files**. Wrapped in `sanitize_for_log` at every genuinely request-derived sink — path params, body fields, JWT claims, crawled URLs and page-derived filenames — with f-string logger calls converted to lazy `%`-args, since an f-string interpolates before the helper can run. **Three findings the sweep produced that a mechanical pass would have gotten wrong.** (1) `override_service._maybe_promote_submission` logs an **int under `%d`**; `sanitize_for_log` returns `str`, and a numeric spec cannot format a str. **It would not have raised, though** — `logging` traps formatting errors in `handleError`, so an enabled level LOSES the intended line and prints a logging-internal traceback instead, and a disabled level does nothing at all. That call is `logger.debug`, so in production (DEBUG off) the fault would have been *completely invisible* until someone enabled DEBUG to investigate something — and would then have eaten the line they turned it on to read. A crash would have been kinder. Left alone, adjacent `submission_id` wrapped instead, and `tests/test_log_sanitizer_usage.py` now binds every format spec to its actual argument via `ast` so the class cannot return. (2) `analytics_routes` looked already-guarded by `uuid.UUID(wine_id)` — it is **not** a newline barrier, because `int(hex, 16)` strips whitespace, so `uuid.UUID('\n' + 'a'*31)` parses and the original newline-bearing string is what reaches the log. Verified by running it. (3) `scripts/synth/snapshots.py` is reachable from `POST /api/v1/admin/synth/refresh`, not dev-only as first assumed — but its two sites are gated by an in-function `KeyError` that leaves only four literal keys, so they are genuine false positives and want a CodeQL dismissal, not code. — The sweep exists because I twice summarised this alert list from its first page and twice under-reported it — once as "entirely pre-existing dependency CVEs" (it included a live hole in new code), once as "three log-injections" (it was 54). Paginate, then count.  **Measured after the merge, not predicted:** CodeQL on `main` reports `py/log-injection` **57 → 3** and `js/request-forgery` **3 → 0**. All three residuals are false positives, but I had predicted **two**, not three — the extra is `admin_routes.py:166`, `request.library_tier`, which the sweep deliberately left unwrapped as "an int bounded by `Field(ge=0, le=4)`". That reasoning holds and is now proven rather than argued: `TierUpdateRequest(library_tier='2\n')` returns `2` as an `int`, so the logged value cannot carry a newline whatever the caller sends. CodeQL does not model Pydantic validation. **Action left: dismiss three alerts as false positives in CodeQL** — the two in `snapshots.py` and this one. | 2026-08-26 |
| OD-81 | ✅ **Resolved 2026-08-26 — and the entry's central claim was backwards, in the direction that hides a live defect.** The archive half stands and was re-verified against production: `generated_reports` exists, holds **0** rows, and `POST /reports/generate` is its only writer, so `/documents-reports` really is an empty archive by construction. But ~~`scheduled_reports` (`reports.service.ts:147-179`) has **zero readers**~~ is **wrong**: `reports.service.ts` touches it at **three** call sites — `.insert()` `:165`, `.select()` `:185`, `.delete()` `:208`. What it lacks is the **table**. Production returns 404 for `scheduled_reports` (re-confirmed by `to_regclass` 2026-08-26); the migration that would create it, `services/database/migrations_archive/008_providers_and_reports.sql`, is outside the applied chain. **Report scheduling is not dead code — it is three reachable gateway endpoints that throw. Strictly worse than recorded.** This row was also malformed: four cells against a three-column header, with the original *open* text still sitting in its "Resolved as" cell. Rewritten here. The unbuilt-generator fork it raised is live in OD-96 with the other phantom tables. | 2026-08-26 |
| OD-87 | ✅ **Resolved 2026-08-26 ([0022](0022-scheduled-jobs-serve-opted-in-tenants.md)) — and the entry's own framing was the dangerous part.** The defect was real and confirmed at `:79-80`: 9 `@Cron`s, of which **8** are tenant-scoped (`checkTenantIsolation` calls a global RPC and never was). But **"invisible today (one customer)" is wrong** — production holds **10 restaurants**, all `is_active = true`, none soft-deleted, in 3 timezones; nine are dev/demo signups indistinguishable from a real tenant by any column. So the entry's own prescription, "iterate the restaurants each cron is meant to serve", **would have mailed nine fixtures on the first run after deploy**. Worse, iterating them would have *leaked*: `RecipientResolverService` falls back to the global `MANAGER_EMAIL` whenever a restaurant resolves no users, and production has **no `staff` role at all** while **6 of 10 restaurants have only `owner`** — so most tenants hit that fallback and would have had their data sent to the one configured manager address. The naive fix converts a silent no-op into a cross-tenant leak. **Shipped instead:** a shared `ScheduledTenantsService` (`list()` + `runPerTenant()`) serving `DEFAULT_RESTAURANT_ID` ∪ restaurants flagged `scheduled_communications`; sequential iteration with per-tenant try/catch and a `SCHEDULED_JOB_SUMMARY … succeeded=X failed=Y` line every run; `allowDefaultFallback: false` for every non-legacy tenant so the env address cannot cross tenants; and the legacy tenant frozen byte-for-byte (its 3 members all carry phones, so routing its SMS through the resolver would have silently tripled it). No flag rows exist, so **this deploy changes nothing for anyone** — the opt-in default is left to the founder as **OD-91**. **Two further defects found in the same file while iterating it:** `processCustomReminders` queried `custom_reminders` **unfiltered** then gated each row on the default tenant's inventory and mailed the default tenant's manager (0 rows in production, so nothing was misdelivered), and `getDailySummaryData` still returned a fixture (5 low-stock, 3 pending, 1 delivery) on any DB error and **SMSed it as fact** — the ADR 0020 fabrication removed from the weekly email under OD-85, missed on the SMS path. Timezone left open as **OD-90**. 13 new tests; each proved to fail against 5 separate reverts of the fix. | 2026-08-26 |
| OD-90 | ✅ **Resolved 2026-08-26 — four SSRF/log-injection sites that predate this session, closed together because they are one defect wearing four hats.** Every one was a caller-controlled value dropped into a template string that becomes a URL. The escape works because **Express decodes route params**, so `%2f` becomes a real slash *after* routing has matched — the `:param` pattern never sees it, and `..%2f..%2fagents%2fexecute` arrives as `../../agents/execute`. **(a)** `getAgentHealthByName` (`orchestrator.service.ts`, CodeQL `js/request-forgery`, **critical**, open since **2026-07-08**) — the worst of them, because `getAdminHeaders()` attaches `X-Admin-Key`, so an escaped request carried the *server-to-server admin credential* to any orchestrator path, not merely the caller's own token. **(b, c)** `ToastService.getMenu` / `getOrder` — same shape from `@Param("menuId")` / `@Param("orderId")`. **(d)** three `py/log-injection` sites in `api/onboarding_routes.py` (a restaurant id and a JWT subject, one of them in an f-string that `%`-args cannot fix). Fixed with one shared allowlist (`common/http/safe-path.ts`) rather than four regexes, and `services/log_safety.py` for the Python half; the studio guard from [ADR 0021](0021-studio-invites-are-self-service.md) was refactored onto the same helper. — Allowlists, not sanitisers: stripping `..` invites a rematch on whatever encoding is thought of next, whereas requiring an identifier ends the category — `%` is simply absent from the character class, so every percent-encoded escape fails on the same rule as the literal one. Found only because a CodeQL alert on new work prompted reading the *whole* list rather than the first page; three of the four had been open for seven weeks. | 2026-08-26 |
| OD-79 | ✅ **Resolved 2026-08-26 — enforced, and the entry was right about the shape but not about the cause.** The field was never missing: `generateTokens` has signed `emailVerified` into every token since before this entry (`auth.service.ts:440`) — it was **discarded twice on the way out**. `getProfileForUser` did not select it, and `AuthContext` populates `user` from `/auth/me` and nowhere else (seven call sites, none decode the JWT), so `ProtectedRoute.tsx:42` evaluated `undefined === false` forever; `JwtStrategy.validate` dropped it too, leaving no server-side check anything to read. **Correction to the entry:** the strategy is at `auth/strategies/jwt.strategy.ts`, not `auth/jwt.strategy.ts`; the line numbers 33-39 were exact. Founder chose **enforce** over deleting the pretence, on measurement: **10 accounts, 4 unverified** — 3 seed rows sharing a 2026-02-08 timestamp and one personal test address, **zero customers** — and `/verify-email` already redeems and resends (1/min) as a public route. `assertEmailVerified` runs inside `JwtAuthGuard` beside `assertTenantMatch`, for the same reason: a global `APP_GUARD` executes before passport sets `request.user` and would have been inert. Fails closed on a missing field; both readers use the DB column, not the token claim, so verifying takes effect immediately instead of after the 15-minute token life. Six `@AllowUnverified` routes. **A live coverage hole was found and closed while verifying:** deleting the token-payload line broke no test, which is why a revert aimed at it reads as a passing revert — that is what derailed an earlier attempt. See [[0023-email-verification-is-enforced]]. | 2026-08-26 |
| OD-86 | ✅ **Resolved 2026-08-26 — and the entry understated it badly.** The audit found not 21 dead toggles but that **the 22-column table never existed**: it lives in `services/database/migrations_archive/011_add_restaurant_feature_flags.sql`, outside `supabase/migrations/`, and was never applied. Production has a 7-column EAV table (verified directly against the live DB before acting). So the settings service SELECTed, UPDATEd and INSERTed **columns that do not exist** — every switch was inert at the DATABASE, not merely ungated. Worse: `enable_ai_negotiation`'s gate query therefore errored and fell back to "enabled", so **AI negotiation could never be turned off**. Migration `20260826120000` adds that column (default true, preserving behaviour while making OFF reachable) and reserves one settings row per restaurant; `feature-flag-registry.ts` is now the single place saying which flags are real; the 20 with no gate are rendered without controls and listed as inactive rather than faked; and **`enable_ai_autonomous_send` is surfaced** with its consequence stated. Applied and verified, with the OD-72 anon revoke still holding on that table. | 2026-08-26 |
| OD-85 | ✅ **Resolved 2026-08-26.** The weekly manager email carried **two** fabrications, not one: literal `topSellers` ("Chateau Margaux 2015 — 15 — $4,500") on both the live branch AND a "mock data" fallback that fired whenever the query threw — so a database blip mailed 1,247 bottles and $42,850 as fact — plus `totalValue: totalBottles * 50`, an invented $50/bottle also mailed by the Monday inventory reminder. Both now compute from real rows and omit the section when empty. New `GET /analytics/pos-revenue/:restaurantId` reuses the EXISTING goals query rather than a second one, so it cannot drift, and distinguishes `null` (no POS wired) from `0` (connected, quiet). COGS ratio and Wine Consumption are now real; LaborSpendOverlay's labour heuristic finally sits on revenue instead of purchasing spend; ChannelDonutChart stays an estimate because `pos_checks` has no channel column — and its caveat is now **always** visible, where it previously appeared only when the total was zero, i.e. it went quiet exactly when there were numbers to be wrong about. **Confirmed, not fixed:** all 9 crons in `scheduled-tasks.service.ts` gate on a single hard-coded `DEFAULT_RESTAURANT_ID`; every other restaurant silently gets nothing. | 2026-08-26 |
| OD-84 | ✅ **Resolved 2026-08-26.** Three wire-format renames — `SalesChartPointDto.revenue` → `procurementSpend`, calendar `daily[].revenue`/`monthly_total` → `procurement_spend`/`monthly_procurement_spend`, and `dailyOrderData[].revenue` → `spend` (renamed together with `useDashboardPage`, which types the shape structurally) — each with every consumer and regression guards pinning the payloads. Also caught what the first sweep missed: the dashboard **day-detail modal** still rendered vendor spend under "Total Revenue", "Month Total" and "No Sales Data"; field and labels moved together, since renaming one without the other just relocates the lie. **Left frozen deliberately:** `DashboardBlock.id` (`kpi-revenue`) and the `DataSource` member `'revenue'` are persisted in saved user layouts — renaming orphans them, so that needs a migration decision, not a rename. Five orphaned `components/reports/**` widgets label procurement data "Revenue" and have zero importers; verified unreachable, left for their owner — the four this entry named while it was open (`WidgetSelector`, `DashboardGrid`, `EditLayoutPanel`, `ChartConfigModal`) are among them; each is referenced now only by a "replaces the old …" comment. | 2026-08-26 |
| OD-83 | ✅ **Resolved 2026-08-26.** `/receiving` — the S02 golden path — now has a sidebar entry after Orders (physical flow: Orders → door → Inventory) and a ⌘K palette command; it was URL-only. `useCalendarEventsSubscription` was never deleted (`RealtimeContext.tsx:597`) and is wired back into `/calendar`. The email reminder channel is **honestly disabled** rather than built: the audit found `reminder_enabled`/`reminder_days_before` are written but **read by nothing**, `reminder_sent` has **no writer in the repo**, and the one calendar cron is the confirmed single-tenant `DEFAULT_RESTAURANT_ID` path using a fixed two-day window that ignores all three columns. Building it needs a child table (one int cannot hold the modal's three offsets), a `reminder_sent` writer for dedupe, multi-tenant iteration, and per-reminder ownership — recorded rather than half-built. | 2026-08-26 |
| OD-82 | ✅ **Fully resolved 2026-08-26 — in two passes that disagreed about the routing, and the founder picked.** **Pass 1 (#70/#71):** all 10 studio calls repointed at the orchestrator via `pages/studio/studioApi.ts`, and three lies removed — revoke and enable/disable ignored `resp.ok` (a 404 toasted success), `handleToggle` had no catch at all, and the queue's green "All clear" rendered over a failed request. The `/studio/invite/:token` link was removed rather than built, correctly diagnosing that `redeem_invite` required a studio role the invitee cannot have. The suspected JWT-secret mismatch was **a measurement error**, not a defect: `railway variables` table rows pad per service; compared with `--kv`, gateway `JWT_SECRET` and orchestrator `SUPABASE_JWT_SECRET` hash identically (`641ddc1b5254`, independently re-verified twice). **Pass 2 ([ADR 0021](0021-studio-invites-are-self-service.md)):** the invite half built rather than removed — redemption no longer needs a studio role, is bound to the invited email (`target_email` was written and never read, so simply dropping the gate would have opened a privilege-escalation path to `review_admin` for any authenticated restaurant account), the `/studio/invite/:token` page exists, and the button now **sends** the invite by email instead of handing back a link. **The two passes chose opposite routings and both could not stand:** pass 1 sent the browser straight to the orchestrator; pass 2 routed it through the gateway. Founder chose the **gateway** — consistent with [0012](0012-reports-through-the-gateway.md), and with pass 1's own note that the proxy is "most consistent with this codebase". `studioApi.ts` keeps its throw-on-non-2xx design (which is what killed the three lies) and now resolves relative paths through the gateway; `VITE_AGENT_ORCHESTRATOR_URL` is no longer needed by the browser. | 2026-08-26 |
| OD-75 | ✅ **Resolved 2026-08-26 — twelve sites now grade on the parse, not the HTTP status**, stamping `outcome_basis: parse_v1` instead of the weaker `call_level_v0`. Every reported site existed (line numbers had drifted) and **three more with the identical defect were found unreported**: `email_intel_agent.py:447` (the escalation path — the ~10x model), `auction_wine_service.py:221` (the OpenAI twin), `wine_matcher.py:724`. Sites that raise use `try/finally` so the spend row is still written exactly once — the call was paid for whether or not the output parsed — and `duration_ms` is captured before the parse so it still measures the call. **The worst find:** `auction_wine_service._parse_ai_response` **could not fail visibly at all** — its regex fallback returns `success: True` for prose — so a parse-graded outcome was impossible until the JSON parse was split out of it. Verified: 841 pass (829 + 12 new); all 12 new tests reverted and confirmed to fail against the old code (`assert 'success' == 'partial'`). | 2026-08-26 |
| OD-73 | ✅ **Closed in production 2026-08-26** (`20260825200000_od73_close_anon_dml.sql`). All 12 RLS-off tables verified against the live DB first — the register's list was **exactly right**, which is rare here. 11 closed (RLS on + explicit `service_role` policy + `revoke all from anon, authenticated`); `spatial_ref_sys` excluded because supabase_admin owns it and the ALTER would abort the migration. **The entry UNDERSTATED it:** `merge_library_wines` and `unsupersede_library_wine` are `SECURITY INVOKER` with EXECUTE granted to `anon`, a live anon *write* path into the wine logs over PostgREST RPC — the revoke closes it. Verified independently after apply: 0 of 11 still open, and **0 public tables remain RLS-off**. **REGENERATED THE SAME DAY, and now ratcheted.** The closing claim *"0 public tables remain RLS-off"* was false ~2h after it was verified: `20260826175836_evidence_gate_v1.sql` (restored verbatim from production's ledger in `6c4996f9`) creates `field_evidence_policy`, `promotion_audit` and `source_registry` with no RLS, no policy, no GRANT and no REVOKE. **Not an open door** — OD-72's `alter default privileges` ratchet (`20260825210000_od72_revoke_client_grants.sql:183`) means they carry no anon/authenticated grant, so the gap was RLS only. Closed by `20260826210000_od73_relock_evidence_gate_tables.sql`, a follow-up rather than an edit to the creating migration because that migration has already shipped to production — editing it would give a fresh build RLS that production lacks. The sweep is now a **ratchet**: `scripts/check_new_tables_are_locked_down.py` blocks in CI. | 2026-08-26 |
| OD-72 | ✅ **Closed in production 2026-08-26** (`20260825210000_od72_revoke_client_grants.sql`) — **and the trap had already fired, so this was a live leak, not a posture question.** Measured: 142 tables RLS-on-zero-policies, all still granting anon+authenticated full DML; 203 of 206 tables granted client DML. `master_wine_library` carried one `SELECT USING (true)` policy and **returned 4,094 rows to the publishable anon key** (proved with a live GET); same shape on `training_datasets`, `organization_invites`, `calendar_event_types`, `crawl_log`, `restaurant_directory`. Worse, **16 postgres-owned views had no `security_invoker`**, so they ran as their RLS-bypassing owner — `v_restaurant_sku_reference` returned a real tenant's name and `inventory_lot_rollup` returned all of `inventory_lots`, making 7 "closed" tables readable anyway. Two `SECURITY DEFINER` functions (`seed_sim_restaurant`, `increment_trust_counter`) were anon-callable **writes**. **Chose revoke over policies:** 52 of 74 existing policies anchor on `auth.uid()`, permanently NULL because the gateway issues its own JWTs — writing more would price in a Supabase Auth migration to serve zero call sites. Applied: 190 tables revoked, 13 views switched to `security_invoker`, 20 views revoked, 2 functions revoked, and `alter default privileges` so the hole cannot regenerate on the next `create table`. `sommelier_conversations` excepted by design — the single live browser consumer. Verified after apply: **0 postgres-owned tables or views are anon-readable**, and the previously-leaking endpoints now return **401** while sommelier still returns **200**. | 2026-08-26 |
| OD-74 | ✅ **The Python emitter returns the row id** — `build_agent_event` mints the uuid client-side and `insert_event` / `SpendLogger.log` return it, `None` on failure. Python's **43 of 50** emit points can now carry a verdict under [0017](0017-doneability-verdicts-are-sidecar-claims.md); before this the sidecar covered only the gateway's 7. It was a **discarded** capability, not a missing one — `BaseAgent.log_decision` already read the id off the same client (`core/base_agent.py:785-787`). Minted rather than read back: the column already defaults to `gen_random_uuid()`, so the row is unchanged, but the id exists *before* the write and costs no `RETURNING` round-trip on a path that runs on every model call. Truthiness preserved, so every existing `if insert_event(...)` reads as it did. **Caught while writing it:** `log()` has an early `return` when Supabase is unconfigured, so binding `nf_event_id` only inside the NF block would have turned the one function that promises never to raise into an `UnboundLocalError` — bound before the outer `try`, with a test on that exact path. **Not closed by this:** nothing in Python yet WRITES a verdict — this supplies the handle, OD-75 supplies the first honest grade. | 2026-08-25 |
| OD-59 | ✅ **Answered for one task type, which was the whole ask** ([0017](0017-doneability-verdicts-are-sidecar-claims.md)) — a completed `document_extraction` task is one whose extracted lines and charges reconcile to the stated total, on `basis: reconciliation_v1`, invoices only. **Three of the entry's own premises were wrong.** (a) The grader was already built and running — `applyTieOut` (`parsed-document.ts:104`) has computed this on every document since before P1; it simply never reached the footprint, so the work was plumbing, not definition. (b) `invoice_extraction` **does exist** (`visual_verification_agent.py:648`), contradicting the scoping note — but it is a Python email-body parse with no tie-out, and its ground truth `_compare_invoice_to_order` (`:664`) sits on a **disjoint path that emits no NF row**; the `@Cron */5` sweep the entry cites as its volume argument is gateway-side and emits `document_extraction`. (c) `nf_a_cost_per_completed_task` has **no outcome predicate at all** (`20260824153600_nf_a_readout.sql:103-108`) — `tasks` is a bare `count(*)`, so it has always reported cost per *call*; the defect is inherited verbatim from `P1-NF-A-INSTRUMENTATION.md` §2. New views `nf_a_cost_per_verified_task` and `nf_a_verdict_coverage` land beside it rather than redefining it under `nf_readout.py`, which unpacks it positionally. **Not closed by this:** Python (OD-74), and 6 further task types whose ground truth already exists in code (`.planning/04-specs/OD-59-VERDICT-CENSUS.md`). | 2026-08-25 |
| OD-44 | ✅ **One guardrail, one canon** ([0013](0013-one-commitment-guardrail.md)) — **the entry's counts were off and understated the problem.** TypeScript carries **19**, not 20 (the array spans 21 lines; one is a `//` comment introducing the multilingual block); Python's 8 was right; and there was a **third** list — `constraint_engine.py` C-02, 3 broad regexes under the same false *"copied verbatim"* comment. Canon is now `commitment-patterns.ts`; `scripts/sync_commitment_patterns.py` generates `core/commitment_patterns.py`. A shared runtime-read file was **rejected on evidence**: the two services ship as separate containers that cannot see each other's tree, and `nest build` (swc) does not copy `.json` into `dist` — verified by building. The deliverable is the drift guard, not the sync: pytest + jest + a blocking CI job, all three confirmed to fail by deleting one pattern. C-02 resolved as a **union**, so it only got stronger. Nothing dropped from the TS list | 2026-08-25 |
| OD-45 | ✅ **Reports routed through the gateway** ([0012](0012-reports-through-the-gateway.md)) — RLS claim **verified against production**: `relrowsecurity = true`, `pg_policy` count **0**, `anon`/`authenticated` hold full DML, table empty, and **142** public tables are in the same state — so a lone policy here would be the exception, not the fix. Coverage checked before committing to the route: `GET /reports` existed, **delete did not** (only `schedules/:id`), and the DTO was missing `summary`/period — both closed, delete now scoped by `restaurant_id` as well as `id`. Found in passing: the hook's `format`/`file_url`/`metadata.*` fields **are not columns** (permanently `undefined`), `typeConfig.icon` would have thrown on the first real row, and `@Get("schedules")` sat below `@Get(":id")` so `/reports/schedules` was answered by `getReport("schedules")`. The other 141 tables remain open | 2026-08-25 |
| OD-61 | ✅ **`api_spend.cost_usd` is nullable and both ledgers now agree** ([0016](0016-ledgers-must-express-unknown.md)). `DEFAULT 0.0` dropped too — an INSERT that omitted the column asserted "free" by the same lie. `SpendLogger.log()` computes `unpriced` once, above both writes. Reader audit: 3 readers, all client-side `SUM`, none ordering/filtering on the column; 2 were NULL-safe, **`research_tasks._budget_available` was not** — `float(None)` → `TypeError` → its own fail-open handler, silently disabling the research daily budget gate; fixed. **No backfill:** of 185 prod rows only 2 are zero-cost and they are distinguishable — one is a true zero (0 tokens), one a real unpriced zero worth **$0.000309** of a **$0.923359** total. Migration written, **NOT applied** | 2026-08-25 |
| OD-62 | ✅ **`gpt-4-turbo` verified CORRECT** — OpenAI publishes `gpt-4-turbo-2024-04-09` at $10/$30 per 1M (`developers.openai.com/api/docs/pricing`, checked 2026-08-25); the suspicion did not hold, and the "one live call site" is **gone** (`auction_wine_service.py` now names `gpt-4o`), making this a historical row. The durable half shipped: `Rate` is a `NamedTuple` whose `verified` + `source` have **no defaults**, so an undated rate is a `TypeError` at import, and `test_rate_rows_all_carry_a_dated_verified_source` fails the build on a malformed, future, or placeholder date ([0016](0016-ledgers-must-express-unknown.md)) | 2026-08-25 |
| OD-63 | ✅ **Fallback parses the email, not the prompt** ([0016](0016-ledgers-must-express-unknown.md)). Fixed structurally — the prompt is built *inside* `_call_llm_for_dates`, so the formatted template no longer exists in the caller's scope and there is no wrong string to pass. 6 regression tests; reverting the one line fails 3 of them with the production-shaped payload ("Saved 3 extracted dates" from a dateless email). **Production cleanup: 0 rows** — and for an alarming reason, filed as **OD-68**: `provider_important_dates` does not exist in the production database at all | 2026-08-25 |
| OD-35 | ✅ **Guarded 2026-08-25, and the entry was partly wrong.** The confused deputy was real — `POST check/:id/close` makes our own server HMAC-sign a stock-depleting webhook the perimeter then trusts. But **it was never reachable in production**: `app.module.ts:89` already gates `SimposModule` on `NODE_ENV !== "production"`, and the gate works — verified live, `GET /api/v1/simpos/<uuid>/catalog` returns **404** while `/api/v1/pos-hub/providers` returns **401**, so the app routes and this module is simply absent. The real exposure the entry never named: **dev and staging point at the same Supabase as production**, so an unauthenticated endpoint in a local or preview environment writes real rows. Now `@UseGuards(JwtAuthGuard)` at class level, with `AuthModule` imported — the exact omission that crash-looped production the day before | 2026-08-25 |
| OD-54 | ✅ **SSRF closed** — `common/net/ssrf-guard.ts`: DNS resolved and **every** address checked, not the hostname and not just the first; **every redirect hop re-validated**, which is the hole a pre-flight-only fix leaves open. Both fetches in `vendor-page-extractor` are guarded, including the robots.txt probe. 32 tests written against the attacks. DNS rebinding remains open by design and is stated in the module header | 2026-08-25 |
| OD-55 | ✅ **Loop bound closed** — `splitPdfIfLarge` capped at 120 pages. `pageCount` came from an uploaded PDF and drives **one paid model call per chunk**; proven pre-fix at **100 chunks from one upload**, 20 after. The cap is announced in the log, never silent — truncating a real document quietly would be its own bug | 2026-08-25 |
| OD-20 | ✅ **Already fixed and never closed** — `analytics.controller.ts` carries a class-level `@UseGuards`; the unauthenticated paid-LLM surface was closed by PR #31 on 2026-08-24. Verified in source, not assumed | 2026-08-25 |
| OD-36 | ✅ **Already fixed and never closed** — `auth/jwt-secret.ts` now **throws** instead of falling back to a published string. Verified in source | 2026-08-25 |
| OD-71 | **POS referential integrity closed** ([0015](0015-pos-referential-integrity.md)) — the ten remaining reference columns across the four `pos_*` tables now have foreign keys. Delete behaviour derived from a census of the 224 existing public FKs (`restaurant_id`→CASCADE 62/72, `auth.users`→SET NULL 2/2); the two genuinely split targets fall back to ADR 0014's rule — a claim dies with its target, a question outlives its answer. Zero orphans; cascade proven in a rolled-back transaction against production | 2026-08-25 |
| OD-02 | **Department structure decided** — 5 divisions, 20 departments, 2 sub-layers ([ORG_STRUCTURE §2](../foundation/ORG_STRUCTURE.md)) | 2026-08-24 |
| OD-09 | **Department set expanded, not trimmed** — founder overruled the "merge Sales into Growth" recommendation; ambition over solo-founder capacity | 2026-08-24 |
| OD-10 | **NF-C = gated research track** — reserved via `subject_type`, entry trigger required ([foundation §4.3](../foundation/README.md)) | 2026-08-24 |
| OD-11a | **NF storage split** — narrow polymorphic production table + wide append-only research log; production and research workloads separated | 2026-08-24 |
| OD-12 | **Loop graph = documentation now, executable later** — machine-readable frontmatter so loops can drive routing without a rewrite ([ORG_STRUCTURE §5](../foundation/ORG_STRUCTURE.md)) | 2026-08-24 |
| OD-13 | **Wave 0 first** (lock contracts), then wide parallel Wave 1 | 2026-08-24 |
| OD-50 | **POS connector sits in Core, free** — acquisition lever, never the gate ([TIER-MAP](../03-scenarios/TIER-MAP.md) axis section) | 2026-08-24 |
| OD-22 | **Tooling library built** — `.planning/05-library/`, 26 files: 0 adopted, 23 candidate, 2 unverified. Nothing is adopted without an ADR | 2026-08-24 |
| OD-30/42 | **Fork numbering reconciled** — 337 citations across 171 files renamespaced (`TECH-F`, `PROD-F`, `INTEL-F`, `CORP-F`); registry at [FORK-REGISTRY](../02-advisory/decision-office/FORK-REGISTRY.md); no canonical ID reassigned | 2026-08-24 |
| — | **Rebrand posture:** planning starts now in Media & Brand (`brand-identity`); **no execution until the brand direction exists**. The 336-line sweep is one dedicated branch later | 2026-08-24 |
| OD-48 | **Tiers = Core / Plus / Pro**, cutting through each scenario's §10 (Core operate · Plus understand · Pro optimize), and **assigned across all 17 scenarios** — 16 `core`, 1 `plus`, 0 `pro` ([TIER-MAP](../03-scenarios/TIER-MAP.md) records what is buildable today vs gated). Price points remain open (OD-23). | 2026-08-24 |
| OD-14 | **Root `SKILLS.md` retired** — replaced with a tombstone (path kept: `.github/copilot-instructions.md` referenced it). It was not a skill, and it competed with `CLAUDE.md` | 2026-08-24 |
| OD-47 | **Loop vocabulary normalised and now enforced** ([0009](0009-loop-vocabulary-contract.md)) — `close_time` 102→9 values, `status` 11→6, 112 `close_time_note` lines added so no nuance was lost. §5.1's table is read by `build_loop_index.py --check` as a hard CI gate; `active`/`running` must cite evidence, which took the live count 6→**5** (only 2 of the 6 had ever carried a citation) | 2026-08-24 |
| OD-11 | **Path C — full ADR 0006 production shape now** ([0008](0008-nf-column-contract.md)). Claude recommended A; founder overruled. P1 unblocked | 2026-08-24 |
| OD-32 | **519 broken `[[links]]` repaired** → 33 remain, all prose examples inside contract docs | 2026-08-24 |
| OD-38 | **POS-agnostic positioning locked; Toast adapter kept.** Founder confirmed: strip Toast-first *framing* from docs, keep the integration. Toast is one of 27 registry providers, not the product's POS. `PROJECT.md` reframed | 2026-08-24 |
| OD-41 | **`questions.md` added as artifact #8** — all 99 units; advisory functions can now deliver findings. Anatomy 7 → 8, corpus 693 → 792 | 2026-08-24 |
| — | **Loop machine-readability fixed** — Claude's ORG_STRUCTURE §5 example put loop YAML in a fenced block and called it frontmatter; 482 loops were unqueryable. `scripts/build_loop_index.py` lifts them into real frontmatter and generates `00-index/LOOP-MAP.md` + `loops.json` | 2026-08-24 |
| OD-24 | **Agreed** — Skills self-retirement trigger adopted: fewer than 5 committed, firing skills by 2026-11-24 → collapse into AI Orchestration | 2026-08-24 |
| OD-25 | **Agreed** — weekly skill-health job owner to be fixed to one department (Decision Office to reconcile the two conflicting docs) | 2026-08-24 |
| OD-28 | **Research & Math promoted to its own division** (7 total), honouring ADR 0001's compensation term | 2026-08-24 |
| OD-34 | **Retire-to-write adopted org-wide** — initial 693-doc generation exempt (CLAUDE.md §3). **Exemption scope ratified by the founder 2026-08-28** (after the adversarial audit flagged that waves 2–3 had read themselves into it without asking): structured wave-generated unit artifacts — agent-stacks, agendas, canvases — ARE the generation; retire-to-write continues to bind every non-wave document | 2026-08-24 |
| OD-27 | **Deferred by founder** — `wineops` strings stay for now; full recalibration into Mudavym is a planned migration, not a hotfix | 2026-08-24 |
| OD-08 | **Folded into OD-21** and resolved with it | 2026-08-24 |
| OD-21 | **Obsidian structure locked** — `.planning/` vault root, corpus in place now with clean slate as end goal, Dataview adopted, unique prefixed filenames ([OBSIDIAN_VAULT.md](../foundation/OBSIDIAN_VAULT.md)) | 2026-08-24 |
| — | **Skills live in `.claude/skills/`** — auto-discovered, committed, PR-reviewable | 2026-08-24 |
| OD-15 | **3 advisory functions adopted** — Architecture Review, Red Team (scoped to decisions + premortems), Decision Office. Ethics & Responsible AI considered and not adopted ([0007](0007-org-structure.md)) | 2026-08-24 |
| OD-16 | **Advisory authority = findings-only**, escalating to the founder | 2026-08-24 |
| OD-17 | **7-artifact unit anatomy**, agendas banner-marked provisional until real work exists | 2026-08-24 |
| — | **One entity reaffirmed** — two-company (research lab + app) proposal argued and declined; separation lives in the data model ([0001](0001-mudavym-single-entity.md) review trail, [0006](0006-neural-footprint-architecture.md)) | 2026-08-24 |

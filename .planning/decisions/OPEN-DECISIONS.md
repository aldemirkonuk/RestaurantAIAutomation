# Open Decisions — the founder queue

> Undecided forks. Sessions **add** items here when they hit one; only a founder
> call resolves an item (→ move it to the Resolved table, link the evidence).
> Nothing in this file may be treated as decided.
>
> Format: ID · question · why it matters · what unblocks it.
>
> **ID collision, reconciled 2026-08-24.** `main` and `fix/od-57-retired-model-sweep`
> both allocated **OD-57 … OD-60** from the same next-free number while apart, so
> six rows arrived wearing three IDs. `main`'s numbering is kept (it is the trunk,
> and `04-specs/P1-BUILD-LOG.md` already cites its OD-58); the branch's three
> non-overlapping items were renumbered **OD-58→61, OD-59→62, OD-60→63** and their
> citations in `v3.0-TECH-DEBT.md`, ADR 0010 and `spend_logger.py` updated to match.
> OD-57 was *not* renumbered — both sides' OD-57 are the same fork (retired model
> ids / model roster) seen from two angles, and 17 code comments cite it — so all
> three OD-57 rows are kept together below. Nothing was dropped except the branch's
> copy of OD-56, which is the same decision as `main`'s in an older, shorter form.

## Open

| ID | Question | Why it matters now | What unblocks it |
|---|---|---|---|
| OD-01 | **`.planning/` restructure** — 28 top-level docs (~1.2MB) + legacy `md/` (120 files) + partially-duplicated `md_files/` (47 files). Merge/archive/re-index how? | Every session pays a navigation tax; the "elevate every document" mandate starts here. | Founder picks a target shape (separate session/PR). |
| OD-03 | **Orchestration base** — `NousResearch/hermes-agent` vs `deepseek-ai/deepseek-harness` vs extending in-house `BaseAgent`. | Names cost nothing; the real axis is cheapest-capable-model routing + harness overhead (vision §12E). | A scoped bake-off on this repo's actual workloads. No pick from repute. |
| OD-04 | **External model roster** — which non-Anthropic models (Kimi/Moonshot, DeepSeek, etc.) for which task tiers, if any. **Sharpened 2026-08-24 by a census of what the code actually calls:** model names are bare string literals at ~90 sites across both runtimes — `gemini-2.5-flash` ×32, `gemini-2.0-flash` ×20 (**retired by Google, 404s**), `gemini-pro` ×17 (**retired**, in the calendar / RFQ / procurement / menu-analyzer agents), `claude-haiku-4-5` ×18, `gpt-4-turbo-preview` ×3 (`auction_wine_service.py`), `gemini-3.6-flash` ×2. There is **no place in the repo that says which model does which job**, so "lean on a different LLM for this job" is currently a find-and-replace across 90 sites. | The founder's cost-efficiency mandate cannot be executed against string literals, and three retired names are failing in production today. P1 made the missing half possible: `context.task_type` now names the job, and the NF-A readout can compare cost and (once OD-59 lands) quality per job — which is exactly the evidence a roster decision needs. | A **job → model registry**: one table, both runtimes read it, call sites name a job not a model. The two choke points already exist — `common/model-client/` in the gateway and `SpendLogger` in Python. Then let the roster be data, and re-decide it from NF-A evidence rather than from repute. |
| OD-05 | **Personal voice agent** — guest-facing, staff-facing, or owner/admin? (Vellum-style, vision §14A.7.) | Cannot be scoped before the audience is chosen. | One founder sentence choosing the audience. |
| OD-06 | **AnyDoc adoption** for digital-document parsing vs current Claude Vision path (vision §12D). | Invoice/receipt understanding is on the critical path to data credibility (vision §7). | Bake-off on real vendor PDFs/spreadsheets from the corpus. |
| OD-07 | **Beli strategy** — build the guest consumer experience independently vs explore collaboration (vision §10). | Determines whether guest-app work is product or partnership groundwork. | Founder call after guest MVP scope exists (FUTURES.md §7.5). |
| OD-18 | **Division count** — 5, or split Technology into Platform + Applied AI? | Five departments under Technology is the widest span in the org. | **Deferred by founder pending team-layer evidence** from the division analysis agents. |
| OD-19 | **Security classification** — of the **94** endpoints unguarded by omission (137 total − 32 webhook-module − 11 explicit `@Public()`), which are real gaps? | Unguarded + `TenantGuard` passthrough = internet-reachable ([ENDPOINTS.md](../foundation/ENDPOINTS.md)). | Security department's first assignment. |
| OD-23 | **Revenue target and pricing, both unverified.** Three corrections to this item as first written: (a) it called $20–50/mo **locked** — **no ADR records any pricing**; per CLAUDE.md §0.1 an unwritten choice is *open*, so the $20k math rests on an assumed price. (b) Its cited source `MASTER-PLAN-30-DAY-SPRINT-2026-08-24.md` **is not in this repo** — it lives in the claude.ai Cowork session, so the <10% likelihood rating cannot be checked here. (c) `PROJECT.md:135` states **"No revenue pressure: Build right, not fast"**, which directly contradicts a 30-day revenue sprint. | Every Commercial decision inherits a target whose price, source document, and stated urgency are all unconfirmed. | **Founder call**, and paste the master plan into the repo. Found by the Finance & Pricing generator. |
| OD-25 | **Weekly skill-health job — which department owns it?** The 2026-08-24 call resolved this only *in principle* ("fixed to one department"); it did not name the department, and neither source doc was amended. foundation README §6 still assigns it to Research & Math; `teams/technology.md` §4.2 still assigns it to Skills. | A recurring job with two named owners has no owner in practice. Kept open — the Resolved row records the principle, not the pick. | Decision Office: choose one, amend the other document. |
| OD-26 | **Structures only ratchet upward** — the generated corpus names **split** triggers in 11 documents and **merge** triggers in 3. Should every unit be required to carry a merge/retirement trigger, symmetric with its split trigger? | Raised by the Legal generator. An org that can only grow has no mechanism to shrink; combined with 693 documents and zero-evidence departments, this is the structural form of the sprawl the anti-sprawl rules target. | Founder call — likely a Decision Office standing rule. |
| OD-29 | **Second boundary conflict, beyond the evaluation seam** — `aio-model-routing` (`teams/technology.md:363-388`) carries RM-1 Harness & Model Routing's exact mandate *and* its primary metric (NF-A cost per task). The published Intelligence/Applied-AI boundary covers evaluation only. | Two teams in two divisions own the same job and the same metric. Proposed interim: one shared wrapper. | Founder or Architecture Review. |
| OD-31 | **Agent headcount has four defensible answers** — 19 specs / 23 registered / 24 (`PROJECT.md:33`) / 26 on disk, reconciled nowhere. Roster defects are **≥7, not ≥2**: 3 unregistered `BaseAgent` subclasses with zero call sites, and 4 registered agents resolving spec from a silent `{}` at `core/agent_registry.py:337`. | `core/orchestrator.py:198-206` records this exact defect class already killing the inbound-email pipeline once, found reactively with no guard built. | People & Agent Ops first assignment; needs one authoritative count. |
| OD-33 | **Insight-type count cannot be settled by picking a number.** Four values circulate (348, 375, 573, and `>=200`); `INSIGHT_CANDIDATES` is built at import time (`insight-catalog.ts:547`) and the only test asserts `toBeGreaterThanOrEqual(200)` (`insight-catalog.spec.ts:10`), so **all of them pass**. | The fix is pinning the source with an exact-count assertion, not editing docs to agree. | Analytics & BI `metric-contract-truth-assurance`. |
| OD-37 | **"Never auto-send" is overstated, not false.** `inbound-responder.service.ts:511` computes `willAutoSend = autonomyFull && !flags.needs_approval`, then schedules a real send after a 2-minute undo window (`:509-513`). So auto-send exists but is gated behind an explicit per-restaurant full-autonomy switch **and** all guardrails passing. The docstring at `:156-157` is stale; project memory records the absolute claim. | The guardrail holds; the documentation of it does not. Correcting the claim is the fix, not changing the behaviour. | Docs + memory correction. Verified 2026-08-24. |
| OD-39 | 🔴 **Toast webhook signature verification is fail-open at the call site.** `toast.service.ts:189` runs the verifier only `if (signature && timestamp)`. The helper fails closed correctly; the caller skips it entirely when an attacker simply omits the header. | Defeats the one control protecting a POS ingress route. The pattern to copy is `pos-hub.service.ts:96-121`, which fails closed. | Security `perimeter-ingress-integrity`. |
| OD-40 | 🔴 **The human approval gate is unauthenticated.** `POST /pos-hub/catalog-match/:restaurantId/proposals/:proposalId/approve` and `/reject` (`pos-hub.controller.ts:169,190`) sit on `@Controller("pos-hub")` with no `JwtAuthGuard`. | Anyone can approve or reject catalogue matches for any restaurant. An approval gate that anyone can operate is not a gate. Sharper than the webhook exposure, and it is not a webhook. | Security. Founder call on fixing with OD-35. |
| OD-43 | **The ratchet is widening, not stable.** OD-26 recorded 11 split triggers vs 3 merge; two greps minutes apart during generation read 13, then **15**, against 3 merge. | The asymmetry grew *while* being documented. Retire-to-write (OD-34, adopted) addresses documents; this addresses org units. | Decision Office standing rule. |
| OD-46 | **Only 4 of 482 loops actually run.** Status census: 433 `proposed`, 29 `blocked`, 9 `dormant`, 2 `gated`, **2 `active` + 2 `running`**. | The honest headline for the whole corpus: the org is designed, not operating. Useful as a baseline to drive up, dangerous if read as capability. | Decision Office — track `loops_running` as the org's real activation metric. |
| ~~OD-49~~ | **RESOLVED** — the pooler string was already in `.env` as `SUPABASE_POOLER_URL`; the check was looking for a name Claude invented. Fixed in PR #33. Migration applied 2026-08-24. | **`SUPABASE_POOLER_CONNECTION_STRING` secret must be added.** Schema parity has never actually compared anything: `db.<ref>.supabase.co` is IPv6-only and runners are IPv4-only, so all three jobs failed on connectivity ([PR #33](https://github.com/aldemirkonuk/RestaurantAIAutomation/pull/33)). | This is the check guarding against the drift that once put 27 tables and 403 columns in production with no migration. It has been failing, so that guard is currently off. | **Founder:** Supabase dashboard → Database → Connection string → Session pooler → save as repo secret. Claude cannot set secrets. |
| OD-51 | **Zero of 17 Pro tiers ship complete**, and 8 are fully unbuilt (guest ×2, Floor Checker ×3, connector-failure ×1, plus others). The two closest — S10 forecasting and S08 price drift — work for **wine only**; S08's food path is scaffolding that would recommend the wrong vendor. | A Pro tier is being described in the corpus that a customer could not receive today. Selling it would be the overstatement the Editorial Gate exists to catch. | Product & Vision + Analytics & BI: either build or restate. |
| OD-52 | **OD-03 may be a category error.** `core/base_agent.py` has **zero LLM integration** (one grep hit for `completion`, in a shutdown log) — it is RabbitMQ/saga/DLQ infrastructure, while hermes-agent and deepseek-harness are *reasoning* harnesses. The bake-off compares unlike things; the real question may be "which reasoning layer sits ON our messaging infra". | Reframes the most-cited open decision in the corpus before anyone runs a misconceived bake-off. | Founder reframe, then the bake-off. Found by the library agent. |
| OD-53 | **The SEO pipeline's step 1 may not be buildable as specified.** No API path exists to harvest a user's own Perplexity searches — only manual export or third-party scrapers. §12B's keyword-harvest step is UNVERIFIED at best. Also: AnswerThePublic **does** have an API (Alpha, paid, 60 req/min), closing an unknown three Growth docs carry. | Growth's G1 charter has this as its first task; better it starts from the truth. | Growth `search-demand-research`. |
| OD-56 | 🔴 **RE-SCOPED 2026-08-25 — the queue does not fix the CVEs.** Cross-checked all 15 open Dependabot PRs against the 9 open CVE alerts: **zero overlap.** The queue bumps typescript-eslint, react-query, zustand, jspdf, framer-motion, sqlalchemy, pytest tooling and 3 GitHub Actions; the CVEs are in `aiohttp` ×3, `cryptography` ×2, `undici` ×3, `nanoid` ×2, `image-size` ×2, `react-router`, `postcss`, `brace-expansion`, `socket.io-parser`. Merging the whole queue would have closed **none** of them. **Python half is now fixed** — `aiohttp` 3.9.3→3.14.3 and `cryptography` 42.0.0→50.0.0 (both direct and pinned in `requirements.txt` and `requirements.prod.txt`), 841 tests pass. **Node half remains**: 6 of 7 are TRANSITIVE, so they need `pnpm.overrides` or an upstream bump, not a Dependabot merge. ⚠️ **PR #16 (`@storybook/addon-viewport` 8.6→9.0) would re-break Storybook**, which #53 just repaired by aligning everything to 8.6.14. | The queue's length was mistaken for its risk. 15 PRs of upgrade churn were blocking on review while the actual vulnerabilities sat untouched — including a crypto library and HTTP request smuggling. | Node transitives via `pnpm.overrides`; the 15 upgrade PRs are a separate, lower-priority question. |
| OD-57 | ✅ **Swept 2026-08-24 on `fix/od-57-retired-model-sweep`, and the framing below was wrong.** The stale ids were largely *dead defaults masking a live cross-provider bug*: the orchestrator injected `llm_primary_model` (a **Claude** id) into `procurement_agent`, `provider_conversation_agent`, `sommelier_agent` and `rfq_agent`, which passed it straight to `genai.GenerativeModel()` — the **Gemini** SDK. Two of those are CORE tier with `mock_llm=false`, so they ran on every boot. Their `"gemini-pro"` defaults never fired, which is precisely why the retired ids looked like the whole problem. Resolved by task per founder call: Gemini for extraction-shaped work (provider_conversation, sommelier, email_composer), Claude via `get_anthropic_client()` for reasoning-shaped work (rfq). `procurement_agent`'s Gemini client was **deleted, not repointed** — `llm_client` was assigned on every boot and never called. The 7 genuinely-firing retired ids now resolve from `settings.gemini_model`, and all 14 spend/metadata labels bind to the same variable as their call so the ledger can no longer name a model the call never used. | Original entry retained below for the record. | Verified: 791 passed, ruff clean, all 12 modules import, every repointed site resolves as intended. |
| ~~OD-57 (original — retired-id census)~~ | 🔴 **27 more references to retired Gemini models across 11 live non-test files.** [ADR 0010](0010-gemini-model-retirement.md) fixed the two that were reported; the surface is far wider. `gemini-2.0-flash` (**shut down 2026-06-01**): `provider_conversation_agent.py:265,266`, `sommelier_agent.py:632,637`, `wine_book_scraper.py:287,323,326,416,436,439`, `training_data_store.py:45,102,147`, `email_composer_service.py:181`. `gemini-pro` (404): `email_parsing_agent.py:74,373,376`, `menu_analyzer_agent.py:359`, `procurement_agent.py:48`, `rfq_agent.py:50`, `sommelier_agent.py:42`, `auction_wine_service.py:56,137,140`, `wine_matcher.py:638,665,668`. Plus a misleading log line at `sommelier_agent.py:94` that prints `model: gemini-2.0-flash` regardless of what was called, and 4 more in `tests/test_gemini_flash_crawler.py`. | **24 of the 27 are dead calls; 3 are not** — `training_data_store.py:45,102,147` are `model_version` default *parameters* written as dataset metadata, so they mislabel training rows rather than 404. Most live calls sit behind broad `except` → fallback, so like `calendar_agent` they fail **silently** rather than loudly. Unknown how many features are quietly degraded. | Sweep as one branch: point config defaults at `settings.gemini_model`, migrate the legacy `google.generativeai` sites to `model_clients.get_gemini_client()`. Needs per-site verification — several are dormant features, so a blind find/replace would light up unbudgeted spend. |
| ~~OD-57 (original — API key + retired Gemini default)~~ | ~~🔴 **The `ANTHROPIC_API_KEY` has no credit.**~~ **Anthropic half RESOLVED 2026-08-24** — founder added credit; a real `claude-haiku-4-5` call now succeeds end-to-end through the gateway wrapper and writes a correct row (`cost_usd` 0.000040, arithmetically checked). The Gemini half is resolved too: the retired defaults were replaced after checking the provider's live model list. **Kept open for the roster question underneath it**, which is OD-04. Original text: **The `ANTHROPIC_API_KEY` has no credit.** Every Anthropic call returns `400 Your credit balance is too low`, so all 7 gateway model call sites are failing in production. Separately, `settings.gemini_model` defaults to `gemini-2.0-flash`, which Google has **retired** — 404, three retries, dead-letter. | The AI features do not work right now, and P1's readout will report an honest zero forever until a model call can succeed. Both were found by driving the emitter end-to-end, not by reading code. | Founder tops up billing (Claude cannot). The Gemini default is a model-roster call — OD-04's territory, but this one needs an answer sooner than the bake-off. |
| OD-58 | **What sample size may an NF-A cost number be published at?** `scripts/nf_readout.py` labels anything below **30 agent events** `INSUFFICIENT VOLUME`. **Corrected 2026-08-25:** this entry said it *refuses* below 30. It does not — it prints the full table under the banner and exits **0** (`nf_readout.py:187-194`, `:232-233`); only `events == 0` withholds a number, and only `--require-volume` changes the exit code. So the gate this entry describes as already-enforced is in fact advisory, which makes the question more urgent, not less. The 30 is Claude's presentation default — nothing in `.planning/decisions/` sets a bar. | A cost-per-task from 5 smoke rows and one from 5,000 production rows print identically without a rule. The whole point of the readout is that it cannot be quoted out of context. | Founder or Research & Math sets the threshold — or replaces the flat number with a confidence-interval rule, which is the better answer if the variance is wide. |
| OD-66 | 🔴 **`toast.service.ts:502` is a second live depletion path still carrying `?? "bottle"`.** [[0011-pos-sale-volume-contract]] deleted the default from the POS hub, but Toast has its own door into stock, and its `"glass" | "bottle"` cast is now a lie — the column is open text. All 10 restaurants declare `pos_system='toast'`, so this is the path a real integration would actually use. | Fixing the hub and leaving Toast is fixing the door nobody walks through. `resolveSaleVolume()` was exported pure precisely so Toast can adopt it in one line — but doing so changes live depletion behaviour on the most likely first integration. | **Founder call**, and it pairs with OD-64: if Toast is the first integration, this is a prerequisite, not a follow-up. | *(2026-08-25: a duplicate of this decision was filed as a second OD-64 by a parallel session and has been folded in here — same subject, `toast.service.ts:502`.)*
| OD-67 | 🔴 **A voided glass returns a whole bottle** (B19). The void path reverses stock in whole bottles regardless of how the sale was booked, so five voided glasses return five bottles — the same 5× error fixed on the sale side, mirrored on the return side and pointing the other way (stock reads high). Found while building 0011; deliberately not fixed in it. | It inflates stock rather than depleting it, so it will never show up as a shortage — the direction nobody notices. Voids are the one POS event a restaurant fires when something has *already* gone wrong, so the data is worst exactly when it matters most. | Reverse by the same volume the sale removed, which `sale_volume_ml` now makes knowable. Needs a call on historical voids: recompute, or accept and move on. |
| OD-64 | 🔴 **Is Toast the first real POS integration, or do we stay provider-neutral until a partner asks?** `TOAST_WEBHOOK_SECRET` is unset in every `.env`, and `toast.service.ts:113` **fails closed** — so every signed Toast webhook is rejected today. That is the safe direction, but it means the integration cannot work, and the boot log has been warning about it in production unnoticed. The surrounding facts pull both ways: all **10** restaurants declare `pos_system='toast'` and one already stores `pos_credentials`; `TOAST_CLIENT_ID` and `TOAST_CLIENT_SECRET` are set while `TOAST_API_KEY` is not; and **OD-38** (resolved) locked **POS-agnostic** positioning (strip Toast framing, keep the adapter). | This is not a missing string. Pasting the secret makes Toast the de-facto first integration and the reference implementation everything else is measured against — which is a commercial decision wearing the costume of a config change. `pos_checks` has 0 rows, so whichever POS lands first defines what the analytics layer has ever seen. | **Founder call.** If Toast: the webhook signing secret + `TOAST_API_KEY` from the Toast dashboard (Claude cannot retrieve these). If provider-neutral: `generic_webhook` already accepts the canonical shape from any source and needs no partner agreement. |
| OD-60 | **`" 2"` duplicate-file policy — hide them or surface them.** `.gitignore:71-73` ignores `* 2.py` / `* 2.md`, so macOS collision copies are *ignored*, not untracked, and `git status --porcelain --untracked-files=all` never lists them (that flag does not report ignored paths). Two sat hidden ~4 months until a filesystem walk found them on 2026-08-24: `tests/test_golden_path_e2e 2.py`, which made pytest collect five e2e cases **twice** (840 local vs 835 clean), and `api/pos_routes 2.py`, a superseded Toast-only router. Both deleted. The globs are also narrower than the problem — `* 3.py`, `* 2.ts`, `* 2.tsx`, `* 2.json` are neither ignored nor flagged, so the next duplicate arrives as an ordinary untracked file that `git add -A` would commit. | Low severity in blast radius, but what is broken is the **audit**, not the files: any hygiene sweep built on `git status` reports a clean tree while duplicates accumulate silently. The ignore rule was added `1f96a78` (2026-04-13), one day after both strays were written — the symptom was silenced, not removed. | Founder picks: (a) widen the globs, (b) drop the rules so duplicates surface in `git status` and get deleted on sight, or (c) keep them ignored **and** add a CI/pre-commit check failing on the ` N.ext` shape. (c) is the only option giving both a quiet status and visibility; (b) matches how these files actually arise, since they are never wanted. Detail in [v3.0-TECH-DEBT.md](../v3.0-TECH-DEBT.md). |
| OD-61 | **`api_spend.cost_usd` cannot express "unknown".** Column is `NOT NULL DEFAULT 0.0`, so an unpriced model still books a false `$0.000000` there even though `neural_footprint_event` now records NULL + `context.cost_basis='unpriced_model'` ([ADR 0010](0010-gemini-model-retirement.md)). | The primary cost ledger silently conflates "free" with "we have no rate", which is exactly the defect corrected on the NF side. | A migration making the column nullable, plus a backfill decision for existing false zeros. |
| OD-62 | ✅ **Mostly resolved 2026-08-24 — the suspicion was right.** `claude-haiku` was priced `(0.80, 4.00)`, which is Claude Haiku **3.5**'s retired rate applied to the `claude-haiku-4-5-20251001` this repo actually calls (published: `1.00 / 5.00`) — under-recording 20% at **11 call sites**, the orchestrator's highest-volume model. Corrected. `claude-sonnet` `(3.00, 15.00)` verified **correct** for `claude-sonnet-4-20250514`. All 8 Gemini rows re-verified correct. **Still open: `gpt-4-turbo` `(10.00, 30.00)`** — one live call site, no source checked. | Exactly the same failure mode as [ADR 0010](0010-gemini-model-retirement.md)'s Gemini bug: a superseded model's price frozen in and silently inherited by its successor. That it recurred independently on the Anthropic row suggests the defect is the absence of a dated-source discipline, not two coincidences. | Verify `gpt-4-turbo` against OpenAI's published pricing. Then consider a test that fails when a rate row has no verification date, so the next model transition can't repeat this a third time. |
| OD-63 | **`calendar_agent._regex_date_extraction` is fed the prompt template, not the email.** `calendar_agent.py` passes `prompt` (the fully-formatted template, which embeds today's date and two literal example dates `2026-02-15`/`2026-03-01`) to the regex fallback, so every fallback writes ≥3 spurious rows into `provider_important_dates` at confidence 0.6 labelled `source="llm_extraction"`. Pre-existing, **not** introduced by [ADR 0010](0010-gemini-model-retirement.md) — but that ADR restored the LLM path without fixing the fallback beneath it. | The 0.6 confidence sits below the 0.7 gate for `calendar_events`, so blast radius is one table — but it is silent data poisoning that looks like real extraction. Found by the review agent auditing ADR 0010. | Pass the raw conversation text to the regex fallback, not the prompt; then purge existing 0.6-confidence rows matching the two example dates. |
| OD-65 | **Supersede B19?** A partial-volume void returns `qty` **whole bottles**, because `record_glass_pour` has no reversal mode (`pos-hub.service.ts`, void branch). Voiding 5 glasses returns 5 bottles — the same 5x error ADR 0011 just removed from the sale path, pointed the other way. | It is the exact failure mode the fail-closed decision was made to end, surviving in the reversal path because B19 is a recorded decision and ADR 0011 declined to work around one quietly (CLAUDE.md §5). | Founder call. Options: a reversing RPC (`p_pour_ml` returned to `open_bottle_ml`), or queue the void as a third `pos_unresolved_lines.reason` and let a human return the stock. |
| OD-68 | 🔴 **`provider_important_dates` does not exist in production.** Found while closing OD-63: the table is absent from every schema (`information_schema.tables` returns nothing matching `%important_date%`), and it lives only in `supabase/migrations_archive/20260208024921_baseline_schema.sql` — never in the live migration chain. `calendar_agent._extract_important_dates` upserts into it inside a `try/except` that logs a warning and moves on, so **every extracted date the agent has ever produced was silently discarded**, real ones as well as OD-63's fabricated ones. `calendar_events` confirms it: 12 `manual` + 7 `system_generated` rows, **0** from `ai_extraction`. | This is why OD-63's cleanup count is zero — the poison never reached a table. But the same swallow means the feature has never worked in production and nothing said so. The `except Exception` around the upsert turns a missing table into a debug-level shrug. | **Founder call**, and it is a fork not a fix: (a) add the table to the live chain and turn the feature on for the first time — which needs the OD-63 fix in place first, or it starts by writing the invented dates for real; (b) delete the write path and the agent's date-extraction branch, since nothing downstream reads it; or (c) keep it dark but make the swallow loud, so a missing destination is an error rather than a warning. |
| OD-76 | **`embedding` emits no NF row at all on failure, and stores a sha384 hash as a stand-in vector** (`provider_conversation_agent.py:1238`). A fabricated vector is silently retrievable and will match nothing meaningfully, while the absent row means the failure is invisible to the instrument. | Two separate defects sharing a line: one corrupts the data, the other hides that it happened. | Decide whether a failed embedding writes NULL and an NF row (consistent with [[0016-ledgers-must-express-unknown]]), or is retried. Not obviously the same call as the ledger one — hence an entry rather than an assumption. |
| OD-80 | **Two pockets of dead code that the `/calendar-classic` retirement leaves behind, both hidden from an unimported-file sweep.** Verified 2026-08-26 on `claude/heuristic-gould-b86f4d` @ `8dbaf533`, where the retirement has **not** landed. (a) **`apps/web/src/types/companyClass.ts` (743 lines) is entirely dead once it does.** It exports 25 names; all 25 are re-exported by `types/index.ts:71-102`, and a grep for every one of them across `apps/web/src`, `apps/mobile` and `packages` finds consumers in exactly two files — `components/shared/EntityAutocomplete.tsx` and `pages/Calendar.tsx` — both deleted by the retirement. The barrel is why `tsc` stays green and why "nothing imports this file" is false: `index.ts` does. (b) **`data/customEventTypes.ts` survives but loses three exports.** `pages/calendar/EventModal.tsx:22` still imports four of them, so the module stays; `isCustomEventType` (`:68`, last caller `Calendar.tsx:1767`), `getCustomEventTypeByName` (`:76`, **no caller anywhere in the repo, ever**) and `EVENT_TYPE_COLORS` (`:92`, last caller `NewEventTypeModal.tsx:10`) do not. Note `pages/calendar/CalendarPage.tsx:35` declares its own local `EVENT_TYPE_COLORS` — a different constant, not an import, and it must survive. **Exact edits, dry-run in a scratch copy 2026-08-26:** delete `types/companyClass.ts`; `sed '70,102d' types/index.ts` (the leading blank, the section comment, and both re-export blocks — taking `:71-102` alone leaves a double blank at the seam); `sed -e '88,105d' -e '65,80d' data/customEventTypes.ts`, which keeps `isEventTypeNameAvailable` at `:81-87` and leaves exactly the four exports `EventModal.tsx` imports. | Dead code that a sweep cannot see is dead code that stays. Both claims are now executable in [CLAIMS.jsonl](CLAIMS.jsonl) with `status: open`, so the guard fails the build the moment the cleanup lands and the entry is not struck off — which is the only reason this survives the session that found it. | **Blocked on branch, not on a decision.** The retirement is uncommitted working-tree state in the `feat/retire-and-rls` worktree, so no part of this can land `tsc`-green on any branch where `Calendar.tsx` still exists. Apply it on `feat/retire-and-rls` once those three deletes are committed, then flip both claims to `resolved`. Verify with `cd apps/web && npx tsc --noEmit` and `npx vitest run`. |
| OD-77 | 🟡 **Migrate Google Cloud / Workspace from a personal account to a company account** *(founder-stated 2026-08-25: "must and will change this"; DEFERRED by the founder, not by a session)*. Everything Google-shaped currently hangs off a personal account: the Gmail sender (`GMAIL_SENDER_EMAIL` is a gmail.com address), the OAuth client, the Vertex/Pub/Sub project (`GMAIL_PUBSUB_TOPIC` → `projects/wineops-vertex-ai/...`), and by extension the Pub/Sub push subscription. | Three separate costs, and only the first is cosmetic. (1) Customer-facing mail arrives from a personal gmail.com address. (2) A personal account cannot carry org-level IAM, audit logging, or a service-account boundary — so **OD-78 is blocked behind this**. (3) Single-person ownership is a bus factor on production email and the AI project; there is no second admin. | Create the Workspace/Cloud org, move or recreate the project, re-issue the OAuth client and Pub/Sub subscription, re-point `GMAIL_*` env vars, verify inbound and outbound mail end-to-end. Sequence it **before** any customer onboarding — migrating a live mail domain under real vendor traffic is materially harder than doing it now. |
| OD-78 | 🔴 **Gmail push webhook is verified in code but enforcement is staged OPEN in production** — blocked on [[OD-77]]. `POST /communications/webhooks/gmail` performs full Google OIDC verification when `GMAIL_PUBSUB_AUDIENCE` + `GMAIL_PUBSUB_SERVICE_ACCOUNT` are set; both are unset in `.env`, `.env.local`, `.env.sim` and on Railway (verified 2026-08-25), so the endpoint accepts unverified pushes and logs an error per request with a running count. | This is a deliberate, documented exception to the repo's fail-closed rule and the reasoning must not be lost: production runs a live Gmail watch, so rejecting on unset config would not have held a door shut — it would have **closed one that is open and carrying traffic**, killing inbound email on deploy. The gap is real: anyone can trigger a fetch/publish cycle. | The two values live in the Pub/Sub subscription's OIDC push config and cannot be derived from the repo. Pull them (or re-create the subscription under the company account per OD-77), set both, then `GMAIL_PUBSUB_REQUIRE_AUTH=true` — which fails closed regardless of the other two, and is the switch that retires this entry. |
| OD-79 | 🔴 **Email verification is decorative.** `GET /auth/me` never returns `emailVerified` (`auth.service.ts:1421-1430`), so the single reader (`ProtectedRoute.tsx:42`) compares `undefined === false` and the gate never fires. `jwt.strategy.ts:33-39` drops the field too, so **no server-side check exists either**. Registration path B reaches the dashboard by SKIPPING verification, not by passing it. | Any address can hold an account, including one the person does not control. It also silently weakens invite and password-reset flows, which assume a verified mailbox. Found by the auth-funnel dossier 2026-08-26. | Either enforce it (surface the flag through `/auth/me` AND the JWT, gate the routes that matter) or delete the pretence so nobody trusts a check that does not run. Enforcing changes signup for every existing unverified account — that is the founder call. |
| OD-81 | 🔴 **Reports do not exist; `/documents-reports` is an empty archive by construction.** `POST /reports/generate` inserts one row with `status: "pending"` and NULL file urls (`reports.service.ts:42-67`) and is the **only writer** of `generated_reports` in the repo — nothing ever produces a file or advances the status. So every View/Download/Print/Email on `/documents-reports` takes its `alert("No file available")` branch (`DocumentsPage.tsx:317-366`). `scheduled_reports` (`reports.service.ts:147-179`) has **zero readers**; the only weekly send is a hardcoded single-restaurant cron whose `topSellers` are literals (`scheduled-tasks.service.ts:162,1024-1028`). | Three surfaces (`/reports`, `/documents-reports`, `/communications`) are built against an artifact the system never creates. | Decide whether reports are a real deliverable this milestone. If yes, a generator is a feature, not a fix. If not, the three surfaces should say so rather than offering buttons that alert. |
| OD-82 | 🟡 **HALF-CLOSED 2026-08-26 — and saying so, because the other half is the interesting one.** **Done:** all 10 studio calls repointed at the orchestrator via a new shared `pages/studio/studioApi.ts`; three lies removed — revoke and enable/disable ignored `resp.ok` entirely (a 404 toasted success), `handleToggle` had **no catch at all** (unhandled rejection, silent revert), metrics showed four fabricated zeros, and the queue's green "All clear" rendered over a failed request. The `/studio/invite/:token` link was REMOVED rather than built: `redeem_invite` (`studio_routes.py:517-521`) requires a studio role the invitee cannot have — that is why they are being invited. Chicken-and-egg, confirmed. **Still blocked, and studio will now 401 instead of 404:** the gateway self-signs `accessToken` with `JWT_SECRET` (`auth.service.ts:66`), the orchestrator verifies studio tokens against `SUPABASE_JWT_SECRET` (`override_service.py:44`) — and **the two values differ in production** (compared by hash across both Railway services, 2026-08-26). Every browser studio call will fail auth. **Also:** `VITE_AGENT_ORCHESTRATOR_URL` is in neither `.env` nor `.env.example`; CommandBar's old `|| ''` fallback meant the base-URL fix would have been a silent no-op, so the new client falls back to `http://localhost:8000` instead. | Studio is the wine-data quality bar the whole expansion sequence rests on. An honest 401 beats a silent 404, but it is still down. | Three ways to close it: (a) the orchestrator also accepts gateway-signed tokens, (b) the gateway proxies studio calls injecting `X-Admin-Key` — **the pattern already exists** in `common/orchestrator/health-proxy.controller.ts`, or (c) align the two secrets. (b) is most consistent with this codebase; (c) is one deploy setting. Founder picks. |
| OD-83 | **Golden-path and parity gaps found while fixing the pages.** (a) `/receiving` — the S02 delivery-at-the-door path — **has no nav entry anywhere**: no sidebar, no command palette, no guidance. It is URL-only, reachable in practice just from an Orders row. (b) `/calendar` lost the live-refresh subscription `/calendar-classic` had (`useCalendarEventsSubscription`), a genuine reduction from the retirement. (c) The calendar modal's **email reminder channel toggle does nothing** — reminders fire browser-locally only, so they do not exist for a user who closed the tab. | Each is small; together they are the difference between a demo and a shift a manager can actually run. | (a) is a nav placement decision. (b) and (c) are scheduled work. |
| OD-84 | **Residual "revenue" misnomers, and two identifiers frozen by user data.** The user-visible lie is fixed — the dashboard KPI now reads "Vendor Spend (30d)" and every reachable label was swept 2026-08-26. What remains is developer-facing and documented in place with MISNOMER comments: `dashboard.service.ts:767-864` `getSalesChart` → `SalesChartPointDto.revenue`, and `:389-467` `getCalendarRevenue` → `daily[].revenue`/`monthly_total`, both summed from `procurement_orders.total_cost`; `useOrdersMetrics.dailyOrderData[].revenue`, which must be renamed together with `useDashboardPage.ts:99,103,112,168` because that file types the shape structurally. **Frozen deliberately:** `DashboardBlock.id` (`kpi-revenue`) and the `DataSource` union member `'revenue'` are persisted into saved user layouts — renaming orphans them, so it needs a migration, not a rename. | Renaming a wire field changes the API contract, and renaming a persisted id silently discards a user's dashboard arrangement. Neither is worth doing carelessly at the end of a large change. | Rename the two endpoints' fields with their web consumers in one commit; decide separately whether saved layouts get a migration or a one-time reset. Also unreachable-but-misleading: `WidgetSelector`, `DashboardGrid`, `EditLayoutPanel`, `ChartConfigModal` label procurement data "Revenue" and have **no importers** — delete or fix when they are next touched. |
| OD-85 | **Surfaces that should show real revenue and currently show nothing.** Reports "Wine Consumption Analytics" (`Reports.tsx:426-446`) has `consumptionData` hard-coded to `[]`, though `totalBottleRevenue`/`totalGlassRevenue` are correctly named and would be genuine. The COGS Ratio tile computed `totalSpent / totalRevenue` with **both sides procurement spend** — always ~100%, a KPI that could only ever report one number; it now takes `posRevenue` and honestly reads "Needs sales revenue from a connected POS". `LaborSpendOverlay` and `ChannelDonutChart` are honest but near-useless without POS (the channel split is a hard-coded weighting, now labelled "(est.)"). **Separate fabrication:** `scheduled-tasks.service.ts:1027-1054` emails hard-coded `topSellers` revenue figures in the weekly report — that one is a lie going out over email. | Real revenue is available and trivially summable (`pos_checks.total`, `voided = false`, date range) — `analytics/goals.service.ts:324-336` already does it. This is a feature decision, not a cleanup, which is why it was NOT built during the honesty sweep ([[0020-no-fabricated-answers]]). | Decide whether POS-backed revenue is in scope this milestone. If yes it unblocks four surfaces at once. The weekly email's hard-coded figures should be removed regardless — that is ADR 0020, not a feature. |
| OD-92 | 🟡 **The crons ignore each restaurant's timezone.** All 8 tenant-scoped `@Cron`s in `communications/scheduled-tasks.service.ts` are pinned to `timeZone: "America/New_York"`, while `restaurants.timezone` is a real column with **3 distinct values in production** (`America/Los_Angeles`, `America/Chicago`, `Europe/Istanbul`). Surfaced while fixing OD-87; `ScheduledTenant.timezone` now carries the value so the fix has somewhere to land. | The "9am digest" fires at 09:00 New York for everyone — 06:00 in Palo Alto, 16:00 in Istanbul. Harmless at one tenant in one timezone; wrong for the second restaurant onboarded, and wrong in a way that reads as carelessness rather than a bug. | Honouring it needs **per-tenant scheduling** (a scheduler that fires per timezone, or an hourly sweep that asks "whose local 9am is it now"), not a per-tenant loop inside a single fixed-time cron. Sequence after OD-91 is answered. |
| OD-91 | 🟠 **FOUNDER'S CALL — do existing tenants get scheduled communications by default, or stay opted in explicitly?** OD-87 is fixed and shipped **gated**: `ScheduledTenantsService` serves `DEFAULT_RESTAURANT_ID` ∪ restaurants flagged `scheduled_communications` in `restaurant_feature_flags`. No flag rows exist, so behaviour is unchanged for everyone. The question is what happens next. | **Opt-in (recommended, and what shipped):** nothing sends until you say so per restaurant; costs one INSERT per onboarding and leaves new tenants silent until someone remembers. **Auto opt-in all active:** a migration seeding flag rows for all 10 — but **9 of those 10 are your own dev/demo signups** (`Gullit's Tavern`, `ADMIN 1`, `YARDOM`, …), indistinguishable from a real tenant by `is_active` or `deleted_at`, so this mails fixtures to whoever their `owner` row points at. | Recommend **explicit opt-in**, and separately deciding whether the nine fixture restaurants should be soft-deleted so `is_active` means something again. Also needs answering: the jobs ask for roles `["manager"]`/`["manager","staff"]`, but production has **no `staff` role at all** and **6 of 10 restaurants have only `owner`** — so those six resolve zero recipients and now log `RECIPIENTS_NONE`. Adding `owner` to the recipient roles would change who gets mail at the existing tenant, so it was not defaulted. |

| OD-88 | **The mock-only e2e tests have never run.** Every test under `services/agent-orchestrator/tests/e2e/` is skipped unless `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` are set, because `conftest_prod.py:217` is a **session-scoped autouse** fixture depending on `prod_supabase`. `test_studio_pipeline.py` is mock-only and says so in its own docstring, yet has never executed locally or in CI without production secrets — which is how an unusable `redeem_invite` carried a passing test through to [ADR 0021](0021-studio-invites-are-self-service.md). (Filed as OD-83 while #71 was in flight and renumbered on rebase; its other two parts are gone — the `resp.ok` defect was fixed in #70, and the onboarding-extract route is served by the gateway as of ADR 0021.) | An autouse fixture silently turns an entire mock-only subtree into dead tests while the suite still reports green. Any claim resting on "the e2e tests pass" is unfounded until this is separated. | **Founder call:** split the genuinely-mocked tests out so the autouse fixture attaches only to the prod-credential subset, or mark that subset with `prod_e2e` — a marker that already exists in `pytest.ini` and is unused for this. |

| OD-89 | **`apps/api-gateway/openapi.json` is a committed artifact that nothing reads and every local boot rewrites.** `main.ts:153-156` writes it on any `NODE_ENV !== "production"` startup, so merely running the gateway locally dirties the working tree. It is never read back: the only references in the repo are the two `writeFileSync` calls (`main.ts:155`, `openapi.ts:28`) — no import, no client generator, no CI check. It is also **already stale on `main`**: `git show HEAD:apps/api-gateway/openapi.json \| grep -c sale-unit` returns **0** while `pos-hub.controller.ts:36-38` has served three `sale-unit` routes since the ADR 0011 work. Found while splitting it out of the ADR 0020 branch, where a local `main.ts` run had regenerated it and swept 500 lines of unrelated pos-hub drift into a studio-invite diff. | A generated file that auto-writes on boot and is never read is a diff-noise generator: it silently attaches unrelated churn to whatever branch happens to run the gateway, which is precisely the reviewability problem CLAUDE.md §2 exists to prevent. Nobody notices it is stale because nothing consumes it. | **Founder call, two ways.** ① *Keep committing it and make it true* — gate the write behind an explicit `EXPORT_OPENAPI=1` (so booting is side-effect free), regenerate on a dedicated branch, and add a CI check that fails when the spec differs from the code. ② *Stop committing it* — add to `.gitignore`, keep `pnpm openapi:export` for anyone who wants it, and delete the tracked copy. ② is cheaper and matches how the file is actually used today (not at all); ① is right only if something external is meant to consume the spec. Either way the boot-time write should stop. |

## Resolved

| ID | Resolved as | Date |
|---|---|---|
| OD-90 | ✅ **Resolved 2026-08-26 — four SSRF/log-injection sites that predate this session, closed together because they are one defect wearing four hats.** Every one was a caller-controlled value dropped into a template string that becomes a URL. The escape works because **Express decodes route params**, so `%2f` becomes a real slash *after* routing has matched — the `:param` pattern never sees it, and `..%2f..%2fagents%2fexecute` arrives as `../../agents/execute`. **(a)** `getAgentHealthByName` (`orchestrator.service.ts`, CodeQL `js/request-forgery`, **critical**, open since **2026-07-08**) — the worst of them, because `getAdminHeaders()` attaches `X-Admin-Key`, so an escaped request carried the *server-to-server admin credential* to any orchestrator path, not merely the caller's own token. **(b, c)** `ToastService.getMenu` / `getOrder` — same shape from `@Param("menuId")` / `@Param("orderId")`. **(d)** three `py/log-injection` sites in `api/onboarding_routes.py` (a restaurant id and a JWT subject, one of them in an f-string that `%`-args cannot fix). Fixed with one shared allowlist (`common/http/safe-path.ts`) rather than four regexes, and `services/log_safety.py` for the Python half; the studio guard from [ADR 0021](0021-studio-invites-are-self-service.md) was refactored onto the same helper. | Allowlists, not sanitisers: stripping `..` invites a rematch on whatever encoding is thought of next, whereas requiring an identifier ends the category — `%` is simply absent from the character class, so every percent-encoded escape fails on the same rule as the literal one. Found only because a CodeQL alert on new work prompted reading the *whole* list rather than the first page; three of the four had been open for seven weeks. | 2026-08-26 |
| OD-87 | ✅ **Resolved 2026-08-26 ([0022](0022-scheduled-jobs-serve-opted-in-tenants.md)) — and the entry's own framing was the dangerous part.** The defect was real and confirmed at `:79-80`: 9 `@Cron`s, of which **8** are tenant-scoped (`checkTenantIsolation` calls a global RPC and never was). But **"invisible today (one customer)" is wrong** — production holds **10 restaurants**, all `is_active = true`, none soft-deleted, in 3 timezones; nine are dev/demo signups indistinguishable from a real tenant by any column. So the entry's own prescription, "iterate the restaurants each cron is meant to serve", **would have mailed nine fixtures on the first run after deploy**. Worse, iterating them would have *leaked*: `RecipientResolverService` falls back to the global `MANAGER_EMAIL` whenever a restaurant resolves no users, and production has **no `staff` role at all** while **6 of 10 restaurants have only `owner`** — so most tenants hit that fallback and would have had their data sent to the one configured manager address. The naive fix converts a silent no-op into a cross-tenant leak. **Shipped instead:** a shared `ScheduledTenantsService` (`list()` + `runPerTenant()`) serving `DEFAULT_RESTAURANT_ID` ∪ restaurants flagged `scheduled_communications`; sequential iteration with per-tenant try/catch and a `SCHEDULED_JOB_SUMMARY … succeeded=X failed=Y` line every run; `allowDefaultFallback: false` for every non-legacy tenant so the env address cannot cross tenants; and the legacy tenant frozen byte-for-byte (its 3 members all carry phones, so routing its SMS through the resolver would have silently tripled it). No flag rows exist, so **this deploy changes nothing for anyone** — the opt-in default is left to the founder as **OD-91**. **Two further defects found in the same file while iterating it:** `processCustomReminders` queried `custom_reminders` **unfiltered** then gated each row on the default tenant's inventory and mailed the default tenant's manager (0 rows in production, so nothing was misdelivered), and `getDailySummaryData` still returned a fixture (5 low-stock, 3 pending, 1 delivery) on any DB error and **SMSed it as fact** — the ADR 0020 fabrication removed from the weekly email under OD-85, missed on the SMS path. Timezone left open as **OD-90**. 13 new tests; each proved to fail against 5 separate reverts of the fix. | 2026-08-26 |
| OD-86 | ✅ **Resolved 2026-08-26 — and the entry understated it badly.** The audit found not 21 dead toggles but that **the 22-column table never existed**: it lives in `services/database/migrations_archive/011_add_restaurant_feature_flags.sql`, outside `supabase/migrations/`, and was never applied. Production has a 7-column EAV table (verified directly against the live DB before acting). So the settings service SELECTed, UPDATEd and INSERTed **columns that do not exist** — every switch was inert at the DATABASE, not merely ungated. Worse: `enable_ai_negotiation`'s gate query therefore errored and fell back to "enabled", so **AI negotiation could never be turned off**. Migration `20260826120000` adds that column (default true, preserving behaviour while making OFF reachable) and reserves one settings row per restaurant; `feature-flag-registry.ts` is now the single place saying which flags are real; the 20 with no gate are rendered without controls and listed as inactive rather than faked; and **`enable_ai_autonomous_send` is surfaced** with its consequence stated. Applied and verified, with the OD-72 anon revoke still holding on that table. | 2026-08-26 |
| OD-85 | ✅ **Resolved 2026-08-26.** The weekly manager email carried **two** fabrications, not one: literal `topSellers` ("Chateau Margaux 2015 — 15 — $4,500") on both the live branch AND a "mock data" fallback that fired whenever the query threw — so a database blip mailed 1,247 bottles and $42,850 as fact — plus `totalValue: totalBottles * 50`, an invented $50/bottle also mailed by the Monday inventory reminder. Both now compute from real rows and omit the section when empty. New `GET /analytics/pos-revenue/:restaurantId` reuses the EXISTING goals query rather than a second one, so it cannot drift, and distinguishes `null` (no POS wired) from `0` (connected, quiet). COGS ratio and Wine Consumption are now real; LaborSpendOverlay's labour heuristic finally sits on revenue instead of purchasing spend; ChannelDonutChart stays an estimate because `pos_checks` has no channel column — and its caveat is now **always** visible, where it previously appeared only when the total was zero, i.e. it went quiet exactly when there were numbers to be wrong about. **Confirmed, not fixed:** all 9 crons in `scheduled-tasks.service.ts` gate on a single hard-coded `DEFAULT_RESTAURANT_ID`; every other restaurant silently gets nothing. | 2026-08-26 |
| OD-84 | ✅ **Resolved 2026-08-26.** Three wire-format renames — `SalesChartPointDto.revenue` → `procurementSpend`, calendar `daily[].revenue`/`monthly_total` → `procurement_spend`/`monthly_procurement_spend`, and `dailyOrderData[].revenue` → `spend` (renamed together with `useDashboardPage`, which types the shape structurally) — each with every consumer and regression guards pinning the payloads. Also caught what the first sweep missed: the dashboard **day-detail modal** still rendered vendor spend under "Total Revenue", "Month Total" and "No Sales Data"; field and labels moved together, since renaming one without the other just relocates the lie. **Left frozen deliberately:** `DashboardBlock.id` (`kpi-revenue`) and the `DataSource` member `'revenue'` are persisted in saved user layouts — renaming orphans them, so that needs a migration decision, not a rename. Five orphaned `components/reports/**` widgets label procurement data "Revenue" and have zero importers; verified unreachable, left for their owner. | 2026-08-26 |
| OD-83 | ✅ **Resolved 2026-08-26.** `/receiving` — the S02 golden path — now has a sidebar entry after Orders (physical flow: Orders → door → Inventory) and a ⌘K palette command; it was URL-only. `useCalendarEventsSubscription` was never deleted (`RealtimeContext.tsx:597`) and is wired back into `/calendar`. The email reminder channel is **honestly disabled** rather than built: the audit found `reminder_enabled`/`reminder_days_before` are written but **read by nothing**, `reminder_sent` has **no writer in the repo**, and the one calendar cron is the confirmed single-tenant `DEFAULT_RESTAURANT_ID` path using a fixed two-day window that ignores all three columns. Building it needs a child table (one int cannot hold the modal's three offsets), a `reminder_sent` writer for dedupe, multi-tenant iteration, and per-reminder ownership — recorded rather than half-built. | 2026-08-26 |
| OD-82 | ✅ **Fully resolved 2026-08-26 — in two passes that disagreed about the routing, and the founder picked.** **Pass 1 (#70/#71):** all 10 studio calls repointed at the orchestrator via `pages/studio/studioApi.ts`, and three lies removed — revoke and enable/disable ignored `resp.ok` (a 404 toasted success), `handleToggle` had no catch at all, and the queue's green "All clear" rendered over a failed request. The `/studio/invite/:token` link was removed rather than built, correctly diagnosing that `redeem_invite` required a studio role the invitee cannot have. The suspected JWT-secret mismatch was **a measurement error**, not a defect: `railway variables` table rows pad per service; compared with `--kv`, gateway `JWT_SECRET` and orchestrator `SUPABASE_JWT_SECRET` hash identically (`641ddc1b5254`, independently re-verified twice). **Pass 2 ([ADR 0021](0021-studio-invites-are-self-service.md)):** the invite half built rather than removed — redemption no longer needs a studio role, is bound to the invited email (`target_email` was written and never read, so simply dropping the gate would have opened a privilege-escalation path to `review_admin` for any authenticated restaurant account), the `/studio/invite/:token` page exists, and the button now **sends** the invite by email instead of handing back a link. **The two passes chose opposite routings and both could not stand:** pass 1 sent the browser straight to the orchestrator; pass 2 routed it through the gateway. Founder chose the **gateway** — consistent with [0012](0012-reports-through-the-gateway.md), and with pass 1's own note that the proxy is "most consistent with this codebase". `studioApi.ts` keeps its throw-on-non-2xx design (which is what killed the three lies) and now resolves relative paths through the gateway; `VITE_AGENT_ORCHESTRATOR_URL` is no longer needed by the browser. | 2026-08-26 |
| OD-75 | ✅ **Resolved 2026-08-26 — twelve sites now grade on the parse, not the HTTP status**, stamping `outcome_basis: parse_v1` instead of the weaker `call_level_v0`. Every reported site existed (line numbers had drifted) and **three more with the identical defect were found unreported**: `email_intel_agent.py:447` (the escalation path — the ~10x model), `auction_wine_service.py:221` (the OpenAI twin), `wine_matcher.py:724`. Sites that raise use `try/finally` so the spend row is still written exactly once — the call was paid for whether or not the output parsed — and `duration_ms` is captured before the parse so it still measures the call. **The worst find:** `auction_wine_service._parse_ai_response` **could not fail visibly at all** — its regex fallback returns `success: True` for prose — so a parse-graded outcome was impossible until the JSON parse was split out of it. Verified: 841 pass (829 + 12 new); all 12 new tests reverted and confirmed to fail against the old code (`assert 'success' == 'partial'`). | 2026-08-26 |
| OD-73 | ✅ **Closed in production 2026-08-26** (`20260825200000_od73_close_anon_dml.sql`). All 12 RLS-off tables verified against the live DB first — the register's list was **exactly right**, which is rare here. 11 closed (RLS on + explicit `service_role` policy + `revoke all from anon, authenticated`); `spatial_ref_sys` excluded because supabase_admin owns it and the ALTER would abort the migration. **The entry UNDERSTATED it:** `merge_library_wines` and `unsupersede_library_wine` are `SECURITY INVOKER` with EXECUTE granted to `anon`, a live anon *write* path into the wine logs over PostgREST RPC — the revoke closes it. Verified independently after apply: 0 of 11 still open, and **0 public tables remain RLS-off**. | 2026-08-26 |
| OD-72 | ✅ **Closed in production 2026-08-26** (`20260825210000_od72_revoke_client_grants.sql`) — **and the trap had already fired, so this was a live leak, not a posture question.** Measured: 142 tables RLS-on-zero-policies, all still granting anon+authenticated full DML; 203 of 206 tables granted client DML. `master_wine_library` carried one `SELECT USING (true)` policy and **returned 4,094 rows to the publishable anon key** (proved with a live GET); same shape on `training_datasets`, `organization_invites`, `calendar_event_types`, `crawl_log`, `restaurant_directory`. Worse, **16 postgres-owned views had no `security_invoker`**, so they ran as their RLS-bypassing owner — `v_restaurant_sku_reference` returned a real tenant's name and `inventory_lot_rollup` returned all of `inventory_lots`, making 7 "closed" tables readable anyway. Two `SECURITY DEFINER` functions (`seed_sim_restaurant`, `increment_trust_counter`) were anon-callable **writes**. **Chose revoke over policies:** 52 of 74 existing policies anchor on `auth.uid()`, permanently NULL because the gateway issues its own JWTs — writing more would price in a Supabase Auth migration to serve zero call sites. Applied: 190 tables revoked, 13 views switched to `security_invoker`, 20 views revoked, 2 functions revoked, and `alter default privileges` so the hole cannot regenerate on the next `create table`. `sommelier_conversations` excepted by design — the single live browser consumer. Verified after apply: **0 postgres-owned tables or views are anon-readable**, and the previously-leaking endpoints now return **401** while sommelier still returns **200**. | 2026-08-26 |
| OD-74 | ✅ **The Python emitter returns the row id** — `build_agent_event` mints the uuid client-side and `insert_event` / `SpendLogger.log` return it, `None` on failure. Python's **43 of 50** emit points can now carry a verdict under [0017](0017-doneability-verdicts-are-sidecar-claims.md); before this the sidecar covered only the gateway's 7. It was a **discarded** capability, not a missing one — `BaseAgent.log_decision` already read the id off the same client (`core/base_agent.py:785-787`). Minted rather than read back: the column already defaults to `gen_random_uuid()`, so the row is unchanged, but the id exists *before* the write and costs no `RETURNING` round-trip on a path that runs on every model call. Truthiness preserved, so every existing `if insert_event(...)` reads as it did. **Caught while writing it:** `log()` has an early `return` when Supabase is unconfigured, so binding `nf_event_id` only inside the NF block would have turned the one function that promises never to raise into an `UnboundLocalError` — bound before the outer `try`, with a test on that exact path. **Not closed by this:** nothing in Python yet WRITES a verdict — this supplies the handle, OD-75 supplies the first honest grade. | 2026-08-25 |
| OD-59 | ✅ **Answered for one task type, which was the whole ask** ([0017](0017-doneability-verdicts-are-sidecar-claims.md)) — a completed `document_extraction` task is one whose extracted lines and charges reconcile to the stated total, on `basis: reconciliation_v1`, invoices only. **Three of the entry's own premises were wrong.** (a) The grader was already built and running — `applyTieOut` (`parsed-document.ts:104`) has computed this on every document since before P1; it simply never reached the footprint, so the work was plumbing, not definition. (b) `invoice_extraction` **does exist** (`visual_verification_agent.py:648`), contradicting the scoping note — but it is a Python email-body parse with no tie-out, and its ground truth `_compare_invoice_to_order` (`:664`) sits on a **disjoint path that emits no NF row**; the `@Cron */5` sweep the entry cites as its volume argument is gateway-side and emits `document_extraction`. (c) `nf_a_cost_per_completed_task` has **no outcome predicate at all** (`20260824153600_nf_a_readout.sql:103-108`) — `tasks` is a bare `count(*)`, so it has always reported cost per *call*; the defect is inherited verbatim from `P1-NF-A-INSTRUMENTATION.md` §2. New views `nf_a_cost_per_verified_task` and `nf_a_verdict_coverage` land beside it rather than redefining it under `nf_readout.py`, which unpacks it positionally. **Not closed by this:** Python (OD-74), and 6 further task types whose ground truth already exists in code (`.planning/04-specs/OD-59-VERDICT-CENSUS.md`). | 2026-08-25 |
| OD-44 | ✅ **One guardrail, one canon** ([0013](0013-one-commitment-guardrail.md)) — **the entry's counts were off and understated the problem.** TypeScript carries **19**, not 20 (the array spans 21 lines; one is a `//` comment introducing the multilingual block); Python's 8 was right; and there was a **third** list — `constraint_engine.py` C-02, 3 broad regexes under the same false *"copied verbatim"* comment. Canon is now `commitment-patterns.ts`; `scripts/sync_commitment_patterns.py` generates `core/commitment_patterns.py`. A shared runtime-read file was **rejected on evidence**: the two services ship as separate containers that cannot see each other's tree, and `nest build` (swc) does not copy `.json` into `dist` — verified by building. The deliverable is the drift guard, not the sync: pytest + jest + a blocking CI job, all three confirmed to fail by deleting one pattern. C-02 resolved as a **union**, so it only got stronger. Nothing dropped from the TS list | 2026-08-25 |
| OD-45 | ✅ **Reports routed through the gateway** ([0012](0012-reports-through-the-gateway.md)) — RLS claim **verified against production**: `relrowsecurity = true`, `pg_policy` count **0**, `anon`/`authenticated` hold full DML, table empty, and **142** public tables are in the same state — so a lone policy here would be the exception, not the fix. Coverage checked before committing to the route: `GET /reports` existed, **delete did not** (only `schedules/:id`), and the DTO was missing `summary`/period — both closed, delete now scoped by `restaurant_id` as well as `id`. Found in passing: the hook's `format`/`file_url`/`metadata.*` fields **are not columns** (permanently `undefined`), `typeConfig.icon` would have thrown on the first real row, and `@Get("schedules")` sat below `@Get(":id")` so `/reports/schedules` was answered by `getReport("schedules")`. The other 141 tables remain open | 2026-08-25 |
| OD-61 | ✅ **`api_spend.cost_usd` is nullable and both ledgers now agree** ([0016](0016-ledgers-must-express-unknown.md)). `DEFAULT 0.0` dropped too — an INSERT that omitted the column asserted "free" by the same lie. `SpendLogger.log()` computes `unpriced` once, above both writes. Reader audit: 3 readers, all client-side `SUM`, none ordering/filtering on the column; 2 were NULL-safe, **`research_tasks._budget_available` was not** — `float(None)` → `TypeError` → its own fail-open handler, silently disabling the research daily budget gate; fixed. **No backfill:** of 185 prod rows only 2 are zero-cost and they are distinguishable — one is a true zero (0 tokens), one a real unpriced zero worth **$0.000309** of a **$0.923359** total. Migration written, **NOT applied** | 2026-08-25 |
| OD-62 | ✅ **`gpt-4-turbo` verified CORRECT** — OpenAI publishes `gpt-4-turbo-2024-04-09` at $10/$30 per 1M (`developers.openai.com/api/docs/pricing`, checked 2026-08-25); the suspicion did not hold, and the "one live call site" is **gone** (`auction_wine_service.py` now names `gpt-4o`), making this a historical row. The durable half shipped: `Rate` is a `NamedTuple` whose `verified` + `source` have **no defaults**, so an undated rate is a `TypeError` at import, and `test_rate_rows_all_carry_a_dated_verified_source` fails the build on a malformed, future, or placeholder date ([0016](0016-ledgers-must-express-unknown.md)) | 2026-08-25 |
| OD-63 | ✅ **Fallback parses the email, not the prompt** ([0016](0016-ledgers-must-express-unknown.md)). Fixed structurally — the prompt is built *inside* `_call_llm_for_dates`, so the formatted template no longer exists in the caller's scope and there is no wrong string to pass. 6 regression tests; reverting the one line fails 3 of them with the production-shaped payload ("Saved 3 extracted dates" from a dateless email). **Production cleanup: 0 rows** — and for an alarming reason, filed as **OD-68**: `provider_important_dates` does not exist in the production database at all | 2026-08-25 |
| OD-35 | ✅ **Guarded 2026-08-25, and the entry was partly wrong.** The confused deputy was real — `POST check/:id/close` makes our own server HMAC-sign a stock-depleting webhook the perimeter then trusts. But **it was never reachable in production**: `app.module.ts:89` already gates `SimposModule` on `NODE_ENV !== "production"`, and the gate works — verified live, `GET /api/v1/simpos/<uuid>/catalog` returns **404** while `/api/v1/pos-hub/providers` returns **401**, so the app routes and this module is simply absent. The real exposure the entry never named: **dev and staging point at the same Supabase as production**, so an unauthenticated endpoint in a local or preview environment writes real rows. Now `@UseGuards(JwtAuthGuard)` at class level, with `AuthModule` imported — the exact omission that crash-looped production the day before | 2026-08-25 |
| OD-54 | ✅ **SSRF closed** — `common/net/ssrf-guard.ts`: DNS resolved and **every** address checked, not the hostname and not just the first; **every redirect hop re-validated**, which is the hole a pre-flight-only fix leaves open. Both fetches in `vendor-page-extractor` are guarded, including the robots.txt probe. 32 tests written against the attacks. DNS rebinding remains open by design and is stated in the module header | 2026-08-25 |
| OD-55 | ✅ **Loop bound closed** — `splitPdfIfLarge` capped at 120 pages. `pageCount` came from an uploaded PDF and drives **one paid model call per chunk**; proven pre-fix at **100 chunks from one upload**, 20 after. The cap is announced in the log, never silent — truncating a real document quietly would be its own bug | 2026-08-25 |
| OD-20 | ✅ **Already fixed and never closed** — `analytics.controller.ts` carries a class-level `@UseGuards`; the unauthenticated paid-LLM surface was closed by PR #31 on 2026-08-24. Verified in source, not assumed | 2026-08-25 |
| OD-36 | ✅ **Already fixed and never closed** — `auth/jwt-secret.ts` now **throws** instead of falling back to a published string. Verified in source | 2026-08-25 |
| OD-71 | **POS referential integrity closed** ([0015](0015-pos-referential-integrity.md)) — the ten remaining reference columns across the four `pos_*` tables now have foreign keys. Delete behaviour derived from a census of the 224 existing public FKs (`restaurant_id`→CASCADE 62/72, `auth.users`→SET NULL 2/2); the two genuinely split targets fall back to ADR 0014's rule — a claim dies with its target, a question outlives its answer. Zero orphans; cascade proven in a rolled-back transaction against production | 2026-08-25 |
| OD-02 | **Department structure decided** — 5 divisions, 20 departments, 2 sub-layers ([ORG_STRUCTURE §2](../foundation/ORG_STRUCTURE.md)) | 2026-08-24 |
| OD-09 | **Department set expanded, not trimmed** — founder overruled the "merge Sales into Growth" recommendation; ambition over solo-founder capacity | 2026-08-24 |
| OD-10 | **NF-C = gated research track** — reserved via `subject_type`, entry trigger required ([foundation §4.3](../foundation/README.md)) | 2026-08-24 |
| OD-11a | **NF storage split** — narrow polymorphic production table + wide append-only research log; production and research workloads separated | 2026-08-24 |
| OD-12 | **Loop graph = documentation now, executable later** — machine-readable frontmatter so loops can drive routing without a rewrite ([ORG_STRUCTURE §5](../foundation/ORG_STRUCTURE.md)) | 2026-08-24 |
| OD-13 | **Wave 0 first** (lock contracts), then wide parallel Wave 1 | 2026-08-24 |
| OD-50 | **POS connector sits in Core, free** — acquisition lever, never the gate ([TIER-MAP](../03-scenarios/TIER-MAP.md) axis section) | 2026-08-24 |
| OD-22 | **Tooling library built** — `.planning/05-library/`, 26 files: 0 adopted, 23 candidate, 2 unverified. Nothing is adopted without an ADR | 2026-08-24 |
| OD-30/42 | **Fork numbering reconciled** — 337 citations across 171 files renamespaced (`TECH-F`, `PROD-F`, `INTEL-F`, `CORP-F`); registry at [FORK-REGISTRY](../02-advisory/decision-office/FORK-REGISTRY.md); no canonical ID reassigned | 2026-08-24 |
| — | **Rebrand posture:** planning starts now in Media & Brand (`brand-identity`); **no execution until the brand direction exists**. The 336-line sweep is one dedicated branch later | 2026-08-24 |
| OD-48 | **Tiers = Core / Plus / Pro**, cutting through each scenario's §10 (Core operate · Plus understand · Pro optimize), and **assigned across all 17 scenarios** — 16 `core`, 1 `plus`, 0 `pro` ([TIER-MAP](../03-scenarios/TIER-MAP.md) records what is buildable today vs gated). Price points remain open (OD-23). | 2026-08-24 |
| OD-14 | **Root `SKILLS.md` retired** — replaced with a tombstone (path kept: `.github/copilot-instructions.md` referenced it). It was not a skill, and it competed with `CLAUDE.md` | 2026-08-24 |
| OD-47 | **Loop vocabulary normalised and now enforced** ([0009](0009-loop-vocabulary-contract.md)) — `close_time` 102→9 values, `status` 11→6, 112 `close_time_note` lines added so no nuance was lost. §5.1's table is read by `build_loop_index.py --check` as a hard CI gate; `active`/`running` must cite evidence, which took the live count 6→**5** (only 2 of the 6 had ever carried a citation) | 2026-08-24 |
| OD-11 | **Path C — full ADR 0006 production shape now** ([0008](0008-nf-column-contract.md)). Claude recommended A; founder overruled. P1 unblocked | 2026-08-24 |
| OD-32 | **519 broken `[[links]]` repaired** → 33 remain, all prose examples inside contract docs | 2026-08-24 |
| OD-38 | **POS-agnostic positioning locked; Toast adapter kept.** Founder confirmed: strip Toast-first *framing* from docs, keep the integration. Toast is one of 27 registry providers, not the product's POS. `PROJECT.md` reframed | 2026-08-24 |
| OD-41 | **`questions.md` added as artifact #8** — all 99 units; advisory functions can now deliver findings. Anatomy 7 → 8, corpus 693 → 792 | 2026-08-24 |
| — | **Loop machine-readability fixed** — Claude's ORG_STRUCTURE §5 example put loop YAML in a fenced block and called it frontmatter; 482 loops were unqueryable. `scripts/build_loop_index.py` lifts them into real frontmatter and generates `00-index/LOOP-MAP.md` + `loops.json` | 2026-08-24 |
| OD-24 | **Agreed** — Skills self-retirement trigger adopted: fewer than 5 committed, firing skills by 2026-11-24 → collapse into AI Orchestration | 2026-08-24 |
| OD-25 | **Agreed** — weekly skill-health job owner to be fixed to one department (Decision Office to reconcile the two conflicting docs) | 2026-08-24 |
| OD-28 | **Research & Math promoted to its own division** (7 total), honouring ADR 0001's compensation term | 2026-08-24 |
| OD-34 | **Retire-to-write adopted org-wide** — initial 693-doc generation exempt (CLAUDE.md §3) | 2026-08-24 |
| OD-27 | **Deferred by founder** — `wineops` strings stay for now; full recalibration into Mudavym is a planned migration, not a hotfix | 2026-08-24 |
| OD-08 | **Folded into OD-21** and resolved with it | 2026-08-24 |
| OD-21 | **Obsidian structure locked** — `.planning/` vault root, corpus in place now with clean slate as end goal, Dataview adopted, unique prefixed filenames ([OBSIDIAN_VAULT.md](../foundation/OBSIDIAN_VAULT.md)) | 2026-08-24 |
| — | **Skills live in `.claude/skills/`** — auto-discovered, committed, PR-reviewable | 2026-08-24 |
| OD-15 | **3 advisory functions adopted** — Architecture Review, Red Team (scoped to decisions + premortems), Decision Office. Ethics & Responsible AI considered and not adopted ([0007](0007-org-structure.md)) | 2026-08-24 |
| OD-16 | **Advisory authority = findings-only**, escalating to the founder | 2026-08-24 |
| OD-17 | **7-artifact unit anatomy**, agendas banner-marked provisional until real work exists | 2026-08-24 |
| — | **One entity reaffirmed** — two-company (research lab + app) proposal argued and declined; separation lives in the data model ([0001](0001-mudavym-single-entity.md) review trail, [0006](0006-neural-footprint-architecture.md)) | 2026-08-24 |

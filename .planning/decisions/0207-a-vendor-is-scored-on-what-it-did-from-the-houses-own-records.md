# 0207 — A vendor is scored on what it did, from the house's own records

- **Status:** Locked 2026-09-21 for the pick (sketch 117 direction A, with B's Roll Call and C's Docket) — the founder's. On 2026-09-21 (later) the founder answered questions 2, 3, 5, 6, 7 and 8, and question 4 in principle (verbatim in the review trail); those rulings are **Locked** and built, except question 4 (waits for the areas model) and question 5's replacement (being sketched). The builder's choices 1, 2, 3 and 6, and 10–16 from the rulings build, stay **Proposed**; question 1 stays open, and questions 9 and 10 (from the rulings build) are new. **Round 3 (2026-09-21, night):** the founder picked the sentiment replacement (question 5's), shrank the ledger figures, ruled Jev tone scoring with names removed and a switch, delegated the overdue-order rule ("You tell me") and asked for the time zone in Settings — all built, see *Round 3, as built*; the builder's choices 17–27 are **Proposed**, questions 11–17 are new [and 18, from the last call], and question 9 is superseded by the delegated rule. **Round 4 (2026-09-22, round 6y):** the founder answered questions 11–17 and ruled the Jev switch owner-only **as an owner's acceptance of the house's complete data terms** (superseding choice 27), kept the rule-based masker while widening it to sensitive topics, ruled the never-arrived cancel a counted miss with its own category, and widened OD-133 — all built, see *Round 4, as built*; choices 18 and 26 are superseded, choices 28–33 are new **Proposed**, questions 19 and 20 are new.
- **Date:** 2026-09-21
- **Decider:** Aldemir (founder) — decisions are locked by the founder, never by an agent
- **Keywords:** vendor scorecard, operational vendor scorecard, vendor sentiment, providers, TwinSheet, ledger card, roll call, docket, on time, lines as ordered, price as agreed, reply time, credits recovered, minimum sample, windowed trend, shadow run, labelled evaluation, tenant scope, house-scoped, sketch 117
- **Links:** [[0149-mudavym-is-the-only-design-finish-every-page-then-delete-legacy-once]] row 31 (the brief) and rows 22, 23, 25; [[0103-a-delivery-is-agreed-before-it-is-verified]] and ADR 0104 (the agreed invoice, the door); ADR 0112 (the Sheet); ADR 0119 (the agreed price states its unit); ADR 0054 (Proposed; sketch 117 cites it for *landed vs agreed* — not used here); `.planning/sketches/117-vendor-scorecard/README.md`; `.planning/06-pages/providers.md` §1a, §4; PR #416 (provider-intelligence house scope, a peer's — not duplicated here)

## Context

The founder was asked whether the vendor sentiment behind `/documents-reports` had deep, robust pipelines with promising results (ADR 0149 row 25). The research answered **hollow**: two unrelated writers of a three-word label, no evaluation, no calibration, `provider_sentiment_history` at 0 rows, `provider_performance_metrics` at 0 rows with a reader and no writer, 27 conversation rows from one vendor, a trend that reads a flat series as "declining" (`provider-intelligence.service.ts:399-404`, first point against last), and two cross-tenant reads on the intelligence routes.

His answer is recorded as ADR 0149 row 31, in the register's own words (the transcript itself is not in the repository, so this is the recorded answer, not a verbatim transcript):

> "An operational vendor scorecard: what vendors do, measured from records (on time, short or refused lines, price agreement, reply latency, credits recovered), tone a minor input, a labelled evaluation and a shadow run before any alert"

The lane brief that carried it adds, from the same answer: *every figure opens to its rows*, and *windowed trends with a minimum sample*. Sketch 117 drew three directions. On **2026-09-21 (evening)** the founder picked, as relayed by the orchestrating session and recorded in its memory (`founder-answers-2026-09-21-round5.md:135`, not in this repository): **direction A, "The Ledger Card"** — the ledger card in the vendor sheet on `/providers` plus one behavioural fact on the vendor card — **with B's "Roll Call" as a second view of `/providers`, and C's "Docket" as the rows surface behind each figure** (A's per-measure tables become one filtered day book); **not** in the Sorting Office.

## Options considered

1. **A alone — the ledger card in the sheet.** Smallest distance from the built page and the MERGE verdict (`MAKEOVER-VERDICTS.md:129-132, :337-346`). Needs one per-vendor read and five rows reads. Leaves "which vendor is slipping, against which" unanswered.
2. **B alone — the Roll Call.** Answers the Monday question, but on a thin corpus it is a page of italics, and it has no place where a single vendor's rows are read.
3. **C alone — the Docket in the Sorting Office.** The strongest reading of "every figure opens to its rows", but it moves vendors back into `/documents-reports` the week ADR 0149 row 25 moved the vendor conversation list out, and it is the costliest read.
4. **A as the spine, B as a second view, C's day book as the rows surface — chosen by the founder.** One read shape serves all three: the house-wide read is the per-vendor read repeated, and the day book is the per-vendor read's rows.
5. *Doing nothing* leaves the legacy sentiment tab as the only vendor-behaviour surface: a model's label with no evaluation, counted twice per message, on a route that leaked across tenants.

## Decision

**Build option 4, read-only, with no alert of any kind.** Everything a figure says is a count of this house's own records; the figure is computed from the same dated entries the Docket lists, so a tally always equals its rows.

### What is built

- **The read** — three routes on their own prefix, house from the token only (`houseOf`, refusing a session that names no house with 403 before any table is read):
  - `GET /vendor-scorecard?window=30|90|365` — the Roll Call: every vendor of the house, each with the five measures.
  - `GET /vendor-scorecard/:providerId?window=` — the ledger card. A vendor of another house is 404, the same answer as no vendor.
  - `GET /vendor-scorecard/:providerId/docket?window=&measure=` — the Docket: every dated entry in the window, newest first, optionally one measure's.
  - Each measure answers **value, denominator (sample), window, minimum-sample rule and one of four outcomes**: `answered` · `too_few` (the count is printed, never a zero figure) · `not_collected` (this house's register has never held such a record — "not late — unknown") · `could_not_read` (the register refused; the reason is on that line and the other lines stand). The prior window of the same length is a window, both counts printed, and is a comparison only when both reach the minimum; no direction word, no slope, no first-against-last.
  - Every register read is paged (PostgREST caps an unranged select) and refuses to score a register larger than it can read whole.
  - Files: `apps/api-gateway/src/providers/scorecard/vendor-scorecard.ts` (pure), `vendor-scorecard.service.ts`, `vendor-scorecard.controller.ts`, registered in `providers.module.ts`.
- **The ledger card** — *What they did*, a new section of the TwinSheet on `/providers`: window chips (30 · 90 · 365 d), five lines each a fraction with its denominator [since the rulings build, a percent with its count — question 3], the sentence of what was left out and why, the prior window's own count, a link whose count equals the rows it opens, tone as a sixth minor line in no figure, *How this is scored*, and the sentence that no alert is sent. `apps/web/src/pages/providers/next/scorecard/LedgerCard.tsx`.
- **One fact on the vendor card** — a fourth row, *Did · 90 d — 12 of 14 on time* [since the rulings build *Did · 90 d — 86% on time · 12 of 14*, question 3], or the refusal in italic words (*2 deliveries — too few to score*, *nothing in 90 d — nothing to score*, *could not be read*). Read from the Roll Call answer; the card computes nothing.
- **The Roll Call** — `/providers` gains a *Book · Scorecard* segmented control (`?view=scorecard`). Every cell a count over a count with its prior window [since the rulings build, a percent with its count — question 3]; rows ordered by the orders on the on-time line (a scanning order, not a rank); sorting by a measure puts vendors that cannot score on it **in their own group**, under a rule that says so; a failed register is written into every cell of its column; an empty book draws no table; phone width folds each row into a block. `scorecard/RollCall.tsx`.
- **The Docket** — a second Sheet (stacked on the vendor sheet by the page's SheetStack) opened from a ledger line or a Roll Call cell: the five tallies as filters over one answer, the entries newest first, every *listed and not counted* entry saying why, an unanswered message listed as *open — not counted, not forgotten*, a failed register as a band — *missing from this list, not absent from the record*. `scorecard/DocketSheet.tsx`.

### The five measures, as built — from the records that exist

| Measure | Counted from | A hit | Listed, not counted | Minimum |
|---|---|---|---|---|
| On time | `procurement_orders` that arrived (`ORDER_ARRIVED_STATUSES`, `delivered_at` in the window), **and** orders placed with the vendor (`ORDER_OPEN_WITH_VENDOR_STATUSES`) whose deadline passed in the window (dated at the deadline) | landed before midnight at the end of `expected_delivery_date` **in the house's time zone** (question 6); an order past that midnight and not landed is **late**, counted, and stays open (question 8), carried on the card's fact as *· N overdue* — one rule, `procurement/delivery-deadline.ts`, also read by `getVendorScorecard` | no expected date; with no zone known, a landing between the earliest (UTC+14) and latest (UTC−12) midnight — *landed within a day of midnight, and this house's time zone is not known* | 5 |
| Lines as ordered | the latest door event with a verdict per order line (`procurement_receipt_events.outcome`, ADR 0103's door) | `accepted`, nothing refused, no damage photograph | counted at the door with no verdict | 5 |
| Price as agreed | order lines verified in the window (`match_verified_at`) | the verdict **recorded** at verification, `price_verified = true` | no invoiced price; not checked; no agreed price it can be compared with (resolved by the same `agreedPricePerBottleForDoor` the verification ran — a keg price is never compared with a bottle price) | 5 |
| Reply time | `procurement_conversations`: our message `SENT`/`AUTO_SENT`/`DELIVERED` to their next received message **in the same thread** — the Gmail thread when the row carries one, else `thread_key` (see below); a second chase before they answer opens no second wait | — (median hours) | send never confirmed; no thread on record; open (no reply yet) | 5 answered |
| Credits | `procurement_credits` opened in the window, attributed to the claim's `provider_id` or else its order's, in the currency its **order** states | allowed on claims settled `credited`, over asked on every claim; `promised` in the denominator only; two currencies are two totals, never added, each with its own percent [only on five claims of that currency — last call, question 2]; an order with no stated currency prints a bare amount | — (under 5 claims the claims themselves are listed on the card as rows, `listed`) | 5 claims (question 2) |

**Found while building, and handled in the read:** `openCreditClaim` (`procurement.service.ts`, the only writer of `procurement_credits`) records neither `provider_id` nor `currency`, and the column defaults to `'USD'`. A vendor filter on `provider_id` would have shown every vendor *no claims*, and printing the column would have called a Turkish house's lira dollars (the ADR 0117 Q25 shape). The read never filters claims by vendor in SQL, attributes each claim through its house-scoped order, and never reads `procurement_credits.currency`. The writer itself is untouched here — see *Not built*.

**Found at last call, and handled in the read:** `procurement_conversations.thread_key` is filled by `set_conversation_thread_key()` (baseline trigger, `BEFORE INSERT OR UPDATE`) **only while it is empty**. An agent's draft (`provider_communication_agent.py`, `provider_conversation_agent.py`) is inserted before it is sent, with no Gmail thread, so it is keyed `msg:<id>` and keeps that key after the send writes `gmail_thread_id` (`procurement.service.ts` approve and auto-send paths); the vendor's reply arrives keyed `gm:<thread>`. Matching on `thread_key` would have listed every answered first message as *no reply yet* and held reply time at *too few*. The read keys a row by `gm:` + its Gmail thread when it has one (the key the same SQL function gives a row that had its thread at insert), else `thread_key`, else `thread_id`. `AUTO_SENT` (the auto-send sweep's status, written with `sent_at` once the message has gone) counts as a send; before the fix it was listed as *never confirmed*. The trigger itself is untouched — see *Not built*.

**Also found at last call:** the on-time figure counted arrivals only, so a vendor holding orders weeks past their date read *5 of 5 on time* with the non-deliveries absent from the page — absence read as health. The read now also takes the house's orders placed with the vendor (`CONFIRMED`, `IN_TRANSIT`) whose expected date falls in the windows, and lists each one past its date as an **open** entry beside the figure — *past its expected date and not landed — not counted, not forgotten* — and on the card's fact (*5 of 5 on time · 2 overdue*). As first built it was not counted into the figure; the founder's answer to question 8 (2026-09-21, below) made it **late in the figure**, still listed as open. The link and the Roll Call say *orders*, not *deliveries*, because those rows are no longer all deliveries.

`not_collected` is decided per house, per register, by a single "has this house ever held such a row" read: an order with an expected date; a door event; a verified line; any vendor mail; any credit claim.

### The founder's rulings of 2026-09-21, as built

The answers are quoted verbatim in the review trail; this is what each one changed.

- **Question 8 — an order past its date that never landed counts as late.** `overdueEntry` (`vendor-scorecard.ts`) is now counted, a miss, and open [corrected in round 3: only once CONFIRMED — a "Not yet" answer for that expected date, or a landing after it; unanswered it is listed *unconfirmed, not counted*, and 30 days on it leaves the figures for Incomplete orders — the founder's delegation, *Round 3* below]; the tail says *counted as late, still open*; the Docket marks it *late · not landed*; the card's fact keeps *· N overdue* so a late landing and a missing order do not read alike. The analytics read (`advanced-analytics.service.ts` `getVendorScorecard`) adds the same orders to its late count and reports `onTimeCounts: { onTime, late, overdue, undecided }` — the founder's reason was to feed the analytics.
- **Question 6 — the house's local midnight.** One rule, `apps/api-gateway/src/procurement/delivery-deadline.ts`, read by both on-time figures: on time when landed **before** midnight at the end of the expected day, on the house's clock (`zonedWallClockToUtc`, DST-correct). The house's zone is `restaurants.timezone` (the column exists, nullable since `20260903170000`); when it is empty, the **country's only zone** (`common/house-frame.ts`, from `Intl` — Türkiye has one, the United States twenty-nine and gets none). With no zone at all the read never uses UTC: the deadline is the span from the UTC+14 to the UTC−12 midnight, and only a verdict true at both ends is counted — the rest are listed, not counted. The card carries `house.deadline`, the sentence of which clock was used. The house record is read by the token's house id; a failed read is a 503, a missing row a 404.
- **Question 2 — five everywhere.** `MINIMUM.credits` is 5 claims. Under five, no percent: the ledger card lists the claims themselves (`MeasureResult.listed`), and the Roll Call's sort by credits ranks only vendors with an answered figure — the rest sit apart under the rule that says so. A currency's own percent needs five claims **of that currency** (last call): five claims split three dollars and two lira are not five of either, so both totals print their money and no percent, and the line says why.
- **Question 3 — percent with count.** Every rate is the gateway's percent with its count: the fact *86% on time · 12 of 14*, each ledger line and Roll Call cell *86%* beside *12 of 14*, each prior window *prior 90 d · 69% · 9 of 13*, credits per currency (each on its own five claims). One rounding rule, in the gateway only (`percentOf`): whole percents, except that a share short of all is truncated to one decimal (never *100%*) and a share above none raised to one decimal (never *0%*). The web prints `percent`; it never computes one.
- **Question 7 — English words, the house's formats.** The formats locale is derived, never named: the region's likely language with that region (`Intl.Locale#maximize` — TR gives the Turkish formats, JP the Japanese, IT the Italian), from `restaurants.country`, else from the region that owns the house's zone; none known — the gateway uses plain `en` numbers and ISO dates, the web the reader's own locale. Dates are all numerals. Every user-facing string of the gateway read is in `vendor-scorecard.copy.ts`, and of the web screens in `scorecard/sc-copy.ts` — two files because the sentences are composed where the rows are counted; no i18n framework.
- **Question 5 — the legacy panel.** Not removed in this build [round 3: the Sentiment tab and `ProviderSentimentChart.tsx` are retired; its reading lives in *How their mail reads* — the other three tabs of the panel stay]. The founder: the feature must stay, the design can go; its replacement is being sketched, so *What the platform has learned* stays below the ledger card as it was.
- **Question 4 — staff.** Decided in principle — a member sees what is inside their territory and nothing outside it — and **not built**: it waits for the areas model under research. Until then every member of the house reads the scorecard, as before.

### What the builder chose (Proposed — each changes one component, none changes the read)

1. **Route prefix `/vendor-scorecard`**, not `providers/scorecard`: `ProvidersController`'s `@Get(":id")` would swallow a static `providers/scorecard`, and this lane keeps out of `providers.controller.ts` (lane E is rebuilding the vendor sheet beside it).
2. **A line is an order row.** `procurement_orders` is line-shaped (one `inventory_id`, one quantity), so "lines as ordered" counts door verdicts per order. ADR 0149 row 23's append-only line-verdict record with a person and a photograph is **not built**; when it lands, this measure moves to it.
3. **Price as agreed uses the verdict recorded at verification**, not a re-derivation, and excludes a line whose agreed price the door function cannot compare.
4. ~~**Minimums 5 · 5 · 5 · 5 are sketch 117's placeholders; credits' minimum of 1 claim** is the builder's.~~ Decided by the founder, question 2: 5 everywhere.
5. ~~**The on-time rule keeps the built UTC deadline.**~~ Superseded by the founder, question 6: the house's local midnight.
6. **Tone** is a sixth, minor line — counts of a model's labels and "no person has labelled one" — in no figure and not a Roll Call column. [Round 3: removed from the card — the card is read by every member, and the founder put tone on the vendor sheet for owners and managers only; the sentence moved into *How their mail reads*.]
7. **The legacy *What the platform has learned* panel stays**, below the ledger card, unchanged — now the founder's call too (question 5: the feature stays, its design is being replaced).
8. **No role gate** — still true in code; the founder decided territory-scoped reading in principle (question 4), pending the areas model. [Round 3: still true of the five measures; *How their mail reads* is owners and managers only, gated in the gateway.]
9. ~~**The card's fact is a fraction**~~ — decided by the founder: percent with count (question 3); English words with the house's formats (question 7).

The rulings build chose these beyond the founder's words (named at last call; each is **Proposed**):

10. **No zone, and a country with several → the span rule** (count only a verdict true from the UTC+14 to the UTC−12 midnight; list the rest), not a named zone for the country and not a refused line. The real tenant is this case today (question 10).
11. **A country's zone only when it keeps exactly one** — by `Intl`'s zone list, so Türkiye gets its zone and the United States, Germany (Büsingen), Spain (the Canaries) and China (Ürümqi) get none.
12. **The fact keeps *· N overdue*** after the founder's example (*86% on time · 12 of 14 · 2 overdue*), so a late landing and a missing order do not read alike.
13. **Two string files, not one** — the gateway's sentences (`vendor-scorecard.copy.ts`, composed where the rows are counted) and the web's chrome (`scorecard/sc-copy.ts`); the controller's API-doc `description` texts stay inline.
14. **A failed read of the house's own record refuses the whole scorecard (503)**, not only the on-time line, since every line's formats come from it. The analytics read instead degrades to no zone and says so in `onTimeDeadline.houseRecord`, as every loader there degrades.
15. **The analytics `onTimeRate` moved beyond the two rulings:** an arrived order with no `delivered_at` is now outside the rate (it was a miss), a no-zone landing near midnight is `undecided`, and the analytics read was taken to be in scope at all (the brief named `getVendorScorecard`).
16. **An order past its date and not landed is dated at its deadline**, like a landing at its landing. It is late in the window its deadline fell in; one window length later it leaves the current figure and the current open list though it has still not landed, and two window lengths later it is not read. Dating it *now* instead keeps it late in every current window while it stays open — including an order nobody ever closed (question 9).

### Round 3 — the founder's answers of 2026-09-21 (night), as built

His words are in the review trail, verbatim. What each one changed:

- **How their mail reads — "A, Plus C's lines", "Vendor sheet only", owners and managers only.** A new section of the vendor sheet under the ledger card (`scorecard/MailTone.tsx`), from a new route `GET /vendor-scorecard/:providerId/mail` (`vendor-mail-tone.ts` pure, `vendor-mail-tone.service.ts`, `vendor-mail-tone.copy.ts`). It lists the vendor's messages in the window, newest first, six shown: date and subject, ONE word — warm, plain or terse — and the vendor's own line it rests on, or why there is none (*not assessed — automated mail*, *— Jev could not read it*, *— the reading was unsure*, *— no reading was kept*; *the read kept no line for this one*). One act, *All their mail (N)*, opens every message of the window in place. The five honest states: no mail yet, too few (under 5 read — the words are shown, nothing is compared), could not read (with the reason and *Try again*), tone not assessed (the messages listed with no word), loading. C's sentence is written at the foot only when this window AND the one before it each hold 5 read messages: both counts per word, side by side, *a count of messages, not a trend*. No number of the scale, no percent and no direction word leaves the gateway. **Owners and managers only:** the route calls `assertCanManageRestaurant` before anything is read (403 for staff), and the sheet mounts the section only for those two roles. **The ledger card's tone line is removed** (the card is read by every member). **The legacy Sentiment tab retires**: `ProviderIntelligencePanel` keeps Digital Twin, Promotions and Conversations; `ProviderSentimentChart.tsx` (a score to two decimals, an arrow, first-against-last) is deleted; `GET /providers/:id/sentiment` is left to its owner (PR #416's lane).
- **"The font size are a little big".** The ledger line's figure steps down one place on the scale the page already uses (13 · 14 · 15 · 16 · 18 px): the percent **18 → 16 px** serif, its count beside it **10.5 → 10 px** mono (`FIGURE_PX`, `FIGURE_COUNT_PX` in `LedgerCard.tsx`). The line's label stays 12.5 px, so the figure still leads the line.
- **Jev tone — "talk with JEV, put that onto point scale ... this feature can also be disabled. other than that use warm/plain/terse", egress "Only with names removed".** The only Jev client in the repository was the coding-agent prompt gate (`scripts/jev/prompt_gate.py`, ADR 0182) — Python, never run by the gateway — and it does score free text (`{state, model, questions}` to `POST https://api.typesafe.ai/v1/systemone`). So the gateway has its own adapter behind a `ToneScorer` interface (`vendor-tone/jev-tone.client.ts`): no redirects (`redirect: "error"`), an 8 s deadline, a loopback-only test seam, and every field of the reply treated as hostile (range-checked numbers, our own candidate keys, a model name only in Jev's shape). **The scale `tone-scale/1`** (`vendor-tone/tone-scale.ts`): valence −1..1 (a Score on five ordered levels, hostile .. very warm, mapped linearly) and five facets 0..1 as Nouls — friction, urgency, commitment, apology, escalation — every number in 0.01 steps, with Jev's confidence, the model asked for and the model that answered, and a Choice over the message's own sentences for the line the word rests on (Jev cannot generate text; it picks one of ours). **The mask** (`vendor-tone/pii-mask.ts`, `tone-egress.ts`): the latest part of the message only (the quoted thread is cut); email addresses, phone numbers (7–15 digits, dates kept) and person names — every name the house's records hold for its members, its vendors' contacts and contact people, whole and by part, and the names mail puts after a greeting, under a sign-off and after "my name is" — are replaced BEFORE anything is built [last call: also the sender's own display name from the `From:` header (`displayNameOf`), the line under a signature separator (`--`), and the name before a Turkish address word (*Deniz Bey*, *Selin Hanım*); and a name found in any of those places is now removed from the whole text too — the draft removed it only from the cut sentences, so *Deniz here from Kestrel … Best, Deniz* sent the first *Deniz*]; each quote candidate is masked again with the names found in the whole, and greetings, sign-offs and signature lines are never offered. **The sweep** (`vendor-tone/vendor-tone-scoring.service.ts`, every 15 min) reads only houses whose switch is on, reads the house's names first and sends nothing for a house whose names cannot be read, never sends automated mail, and keys each answer to our message id in `vendor_message_tone_scores` (migration `20260921170800`): RLS on, service_role only, no text stored — the scale, the model, the confidence and the mask COUNTS. A failed call is a `failed` row with its reason (retried to 3 attempts) and the sheet reads that message *not assessed* — never the inbound model's word in its place. **The word** comes from named thresholds (warm ≥ 0.25, terse ≤ −0.25 or friction ≥ 0.6, plain between, *not assessed* under 0.5 confidence). **The switch** is `restaurants.vendor_tone_scoring_enabled`, **default false** (the founder was asked the default; it is question 11 [corrected at last call: the draft said 13, which is who may turn it on]), set in Settings → *Mail reading* by an owner or manager, audited (`vendor_tone_scoring_changed`). With it off — and for a message Jev has not scored yet — the word is the inbound model's existing label renamed (positive → warm, neutral → plain, negative → terse); that label no longer defaults a missing answer to *neutral* (`inbound-responder.service.ts` `parseAnalysis`), and the same Haiku call now returns a `tone_quote` the sheet prints only when it occurs verbatim in the message.
- **The Jev egress, recorded.** This is a SECOND TypeSafe egress, distinct from ADR 0182's prompt gate: vendor mail (a third party's words about the house's orders) leaves the gateway for `api.typesafe.ai`, masked, for houses that turn it on. What TypeSafe retains of it is the same unanswered fork as **OD-133** (retention, the DPA, zero-retention on request), and TypeSafe belongs on the subprocessor register the compliance charter says does not exist (`.planning/01-org/corporate/compliance-privacy/compliance-privacy-charter.md:202`). No new OD row was filed (the lane's rule); the founder is asked to widen OD-133 to cover it (question 14). Whether the gateway's deployments hold `JEV_API_KEY` was **not checked** — no `.env` was read; without the key the sweep sends nothing and the sheet says Jev is on but this server cannot reach it.
- **Overdue orders — "think of a best way to handle this ... You tell me".** The rule, one place (`procurement/overdue-order.ts`), read by the ledger card, the Docket, the analytics read and the ask: an order placed with the vendor (CONFIRMED / IN_TRANSIT) past midnight at the end of its expected day on the house's clock, and not received, raises **"Did it arrive?"** with three choices — *Yes — receive it* (the receiving door), *Not yet* (recorded in `procurement_order_arrival_answers`, append-only, actor FK to `public.users(user_id)`), *Cancel* (the sealed cancellation on Orders). It counts **late only once confirmed**: a *Not yet* given for THAT expected date after the deadline could have passed, or a landing after the date — and a late landing is now dated at its deadline, so it counts in the period its deadline fell in with its true landing date in its words. Unanswered it is **"unconfirmed, not counted"** on the card's fact (*· 2 unconfirmed, not counted*), the line's sentence and the Docket. **Thirty days** past the deadline without arrival it leaves the figures (both windows) and moves to **Incomplete orders** under Documents & Reports, until it is received (then an arrival, late, with its true dates), cancelled, or closed with a credit (a claim on it settled `credited`). The ask is `GET /procurement/arrival-asks`, the answer `POST /procurement/orders/:id/arrival-answers` (`{answer: "not_yet"}`; 403 for someone not asked, 404 for another house's order, 409 for an order not due or already incomplete), the register `GET /procurement/incomplete-orders` (`arrival-asks.ts`, `.service.ts`, `.controller.ts`). **Who is asked:** the receiving area's people, else owners and managers (`mayAnswerArrival`) — the areas model is on `feat/house-areas` (PR #441, ADR 0218), not on this branch, so today it is owners and managers. **Where it renders:** on `/receiving` beside the outbox rail for the manager and owner renderings (`RcArrivalAsks.tsx`); the shell's counter (lane `feat/shell-counter`, not on main) can read the same typed act; Incomplete orders is a drawer on `/documents-reports` (`IncompleteOrders.tsx`).
- **The house's time zone — "Add it to Settings".** `GET` / `PUT /settings/time-zone` (`house-time-zone.service.ts`): an IANA name this server's `Intl` lists (or `UTC`), refused with a sentence otherwise (`EST`, `+03:00`, `utc`, a padded name); owner or manager; audited (`house_time_zone_changed`); no clear-to-null; a failed read of the current zone writes nothing. Settings → *Time zone* offers the country's zones first and writes only the one a person picked. The span rule stays for every house with no zone.

### What the builder chose in round 3 (Proposed — each changes one component)

17. **The mail section reads 90 days** and does not follow the ledger card's window chips.
18. **The word's thresholds** — warm ≥ 0.25, terse ≤ −0.25 or friction ≥ 0.6, *not assessed* below 0.5 confidence (question 12).
19. **A Jev failure is final for that message after 3 attempts**; before that the sweep retries it.
20. **With the switch on and no Jev row yet, the inbound model's label is shown**; only a FAILED Jev reading is *not assessed*.
21. **"All their mail" opens in place**, because `/communications` takes no vendor filter (`lib/conversationFilters.ts` parses `providerId` and no page reads it); *Write to them* on a vendor with no mail is not drawn.
22. **Only a Not yet is recorded by the ask**; Yes and Cancel are the acts that already leave their own record.
23. **A Not yet confirms one expected date** — a vendor's new date needs a new answer — and an answer given before the deadline could have passed confirms nothing.
24. **Incomplete leaves both windows' figures**, not only the current one, since the prior window's orders are all more than 30 days past.
25. **The Incomplete orders register and the ask read the same audience** (owners and managers; the receiving area when it exists).
26. **The masker matches a known name as written, capitalised or in capitals — not in lower case** ("can", "may"), and never masks a company name, a product, a price or a date.
27. **The mail-reading switch lives in Settings** (group *What it does on its own*), owner or manager — the same role rule as every house setting.

### Round 4 — the founder's answers of 2026-09-22, round 6y, as built

His words are in the review trail, verbatim. What each one changed:

- **Question 13 (who may turn Jev on) — "owner only, but also we're going to use this as complete data and privacy usage, they have to accept that, and when they do they'd accept the jev too with their names and sensitive topics redacted."** Turning Jev on is now an OWNER accepting the house's complete data-and-privacy terms — every place the house's data goes, not a Jev-only notice — and that acceptance INCLUDES Jev. `public.house_data_terms_acceptances` (migration `20260922015010`), append-only, one row per accepted version, sealed (`mcp_seal_challenges` widened with `house_data_terms` by read-and-append) rather than logged as an assertion. The terms themselves are `apps/api-gateway/src/settings/data-terms/house-data-terms.ts` (pure; `TERMS_VERSION = 1`, `fact` statements each cited to a code path, `term` statements on the lawyer list, a canonical `snapshot()`/`digest()`). Routes: `GET /settings/data-terms` (any member), `POST /settings/data-terms/seal-challenge` and `POST /settings/data-terms/acceptances` (owner only, sealed; a stale `version`/`digest` is 409). `PUT /settings/vendor-tone-scoring` is now **owner only** (was owner or manager, builder choice 27, superseded) and refuses `enabled: true` with 409 unless the house's latest acceptance is of the CURRENT version; `enabled: false` needs no acceptance and stays open to any owner. **Superseded** the round-3 comment on `restaurants.vendor_tone_scoring_enabled` that said "owner or manager". **Scoped down from the design, named plainly (CLAUDE.md §0.5):** the design calls for one `security definer` database function doing the acceptance insert, the switch flip and the audit row in a single transaction, closing the round-3 last call's finding that a flip and its audit row could land as two separate writes. That function was **not built** this round; `HouseDataTermsService.accept()` does the three things sequentially instead (seal, insert, flip+audit), which is safe (nothing is sent that should not be) but not atomic — named as the round's own follow-up, not silently gapped. The sweep (`vendor-tone-scoring.service.ts`) now also refuses a house whose latest acceptance is not the current version, and **re-reads both the switch and the acceptance before every message**, not only once at the top of a run (the round-3 last call's other named gap, closed here for both gates at once). **Not built:** the terms acceptance SHEET on `/settings` (statements, subprocessors, *Hold to accept*) — the backend refuses `enabled: true` with a 409 sentence naming the terms endpoint [last call, 2026-09-22: now a sentence written for the owner, naming no route — the routes are in the 409's API description], and `MailReadingSection.tsx`'s copy was updated to say what turning it on now means, but there is no dedicated UI to read the terms and accept them yet; an owner today would need to call the routes directly — so, in the product, no house can turn Jev on until the sheet ships. Also not built: `scripts/check_data_terms_name_every_host.py`, the host-literal completeness guard the design calls for — the statements list names every flow this round's own research touched, which is not the same as a mechanically-proven "every host is named."
- **Question 18 (is a rule-based masker enough) — "Keep, measure misses (Recommended)."** The rule-based pass is kept. `apps/api-gateway/src/vendor-tone/sensitive-mask.ts` (pure) adds two more passes, both called from `maskForEgress`: shapes (IBAN mod-97, card Luhn, TCKN/SSN checksum, a labelled credential line → `[account]`/`[id]`/`[credential]`, run before the phone pass) and topics (a sentence carrying a listed term — KVKK Art. 6 ∪ GDPR Art. 9 ∪ Art. 10 ∪ Cal. Civ. Code 1798.140(ae), plus prudence categories — replaced whole by `[private]`, run last, on the exact line/sentence cuts `tone-scale.ts` `splitSentences` uses [last call, 2026-09-22: corrected — per-line cuts let the unwrapped half of a private sentence leave, in the body and as a quote; the pass now finds a sentence across a wrapped line (`maskTopicsInParagraph`), and `egressFor` offers a quote only if it is word for word in the masked body]). Business disputes, company names, products, prices, dates and certifications are deliberately NOT redacted (builder choice 29). A language the term list cannot read (anything but Turkish/English) fails closed: `languageCovered()` refuses to claim coverage, the sweep counts it `skippedLanguage` and sends nothing for it, and the sheet prints *"not read by Jev — written in a language its private-topic pass does not cover"* (builder choice 30). [Last call, 2026-09-22: corrected — as built it read the RAW message, so an Italian reply above an English quoted thread passed; it called any text under eight words covered, so "Marco è in ospedale, consegna domani." was sent; and the sheet printed the sentence while Jev was OFF too, hiding the inbound model's reading of every non-Turkish/English message for every house. Now `egressFor` itself refuses a latest part the pass cannot read, a text with no English or Turkish marker word is not covered however short, and the sheet says it only while Jev is on. The term list also gained the plainest words it lacked (hasta, doctor, off sick, passed away, baş sağlığı as two words, pregnancy, a password written in a sentence).] **"Measure misses" (§B5 of the design) was NOT built this round** — no preview endpoint, no `vendor_message_tone_scores` review table; the CI gold set (`sensitive-mask.spec.ts`) is the only measured residual, and it is smaller than the design's ≥40/≥30 floor (named in the round's build report). The stored `masked` JSONB gains `accounts`, `ids`, `credentials`, `private` counts and a `version` (`SENSITIVE_TERMS_VERSION = "sensitive-terms/1"`) — counts only, never values.
- **Question 12 (the word's thresholds) — "Keep, re-set on sample (Recommended)."** Unchanged this round; still waits on a labelled sample (question 12 stays open under *Not built*).
- **Question 14 (OD-133) — "Widen, add TypeSafe (Recommended)."** OD-133 widened in place, bracketed, not renumbered (`.planning/decisions/OPEN-DECISIONS.md`). TypeSafe added to `.planning/foundation/EXTERNAL_CONNECTIONS.md` §2 (both callers: the coding-agent prompt gate and this product's masked vendor-mail egress), and `compliance-privacy-charter.md:202` gets a bracket pointing to the new register row.
- **Question 15 ("close with a credit" vs Cancel for a never-arrived order) — "if. cancelde make sure the all analytcis be configured toward that, canel is enough or not?"** (verbatim, typos kept). **Answer: not as built, and still the right act.** With a reason category and the analytics fixes below, Cancel IS enough for every never-arrived order the house did not prepay; no separate "close with a credit" act is built (a prepaid refund is question 20, not defaulted). Migration `20260922015000` adds `procurement_orders.cancel_reason_code` (`never_arrived` | `vendor_cannot_supply` | `house_decision`), `cancelled_from_status`, `cancelled_at` — a column, not a table, written in the SAME `UPDATE` as the status on every cancel path (the gateway's sealed `cancelOrder`, the receiving-page in-place cancel, and the procurement agent's out-of-stock cancel). [Last call, 2026-09-22: one path was missed — `PATCH /procurement/orders/:id` with `status: CANCELLED` wrote the status with no category, no seal and no role check, so a never-arrived order cancelled that way left every vendor figure. `updateOrder` now refuses it (422, `cancel_through_the_sealed_act`); the typed "Did it arrive?" act carries `reasonCode: "never_arrived"` beside its `DELETE` endpoint.] `apps/api-gateway/src/procurement/cancel-reason.ts` (pure) is the one rule: `never_arrived` only from CONFIRMED/IN_TRANSIT and only once past the order's deadline on the house's clock (`isPastDue`); `vendor_cannot_supply` from NEGOTIATING/APPROVED/CONFIRMED/IN_TRANSIT; `house_decision` from anywhere a cancel is otherwise legal. `DELETE /procurement/orders/:id` takes `reasonCode` (missing/unknown → 400; wrong state → 422); the free-text reason stays required. **In place, where the question is asked:** `RcArrivalAsks` and `IncompleteOrders` render a locked-`never_arrived` cancel act (`SealedRejectDie` with `reasonCode` fixed) in place of the old "Cancel on Orders ›" link, which the research found led to a rebuilt `/orders` page with no cancel act at all for a CONFIRMED/IN_TRANSIT order. `ResponsesSheet` and the general `SealedRejectDie` now carry a three-way category picker, preselecting `vendor_cannot_supply` when the response being rejected is one of `DECLINE_INTENTS`. **The analytics:** `vendor-scorecard.ts` `cancelledEntry` (a third read, beside arrivals and open orders) counts `never_arrived` as a miss, dated at its deadline like a confirmed-overdue order; lists `vendor_cannot_supply`/`house_decision`/no-code(legacy) without counting them [last call, 2026-09-22: the live read, `readArrivals`, reads only cancels that carry a category, so a legacy no-code cancel never reaches the list — its arm is exercised only by the spec's fixture; unknowable pre-placement legacy cancels are kept off a vendor's list this way]; a cancel out of a pre-placement state (PENDING/APPROVAL_NEEDED) is not a vendor event and is not listed at all. `advanced-analytics.service.ts` `getVendorScorecard`'s `onTimeCounts` gains `neverArrived`/`couldNotSupply`/`houseDecisionCancels`/`uncategorisedCancels` (a naming choice, builder choice 33 — the design's own words were `cancelCounts.{neverArrived,couldNotSupply,houseDecision,uncategorised}` as a sibling object; folding them into the existing `onTimeCounts` instead avoids a second object with an overlapping meaning). Spend, cashflow and every other spend-keyed figure are unchanged — CANCELLED was never in `ORDER_SPEND_STATUSES` and stays out of it whatever the reason.
- **Question 16 (who reads Incomplete orders) — "Owners and managers (Recommended)."** As built (round 3); unchanged.
- **Question 17 ("all their mail") — "Open in place (Recommended)."** As built (round 3); unchanged.

### What the builder chose in round 4 (Proposed — each changes one component)

28. **Jev's stored readings are kept after Jev is turned off** — the switch stops future sends; it does not delete what was already scored. Numbers only, never text, so keeping them is a measurement decision, not a privacy one.
29. **Business disputes are not redacted by the topics pass** — a legal threat, late payment or insolvency mention is about a company, not a person, and is exactly what the escalation facet reads; redacting it would blind the score to the mail that matters most.
30. **A language the topics pass cannot read is not sent at all** (fail closed), rather than sent unmasked for that pass — named as a real cost (Italian/French/Spanish/German wine mail is not scored today), not a silent gap.
31. **365 days of lookback is kept** at switch-on (unchanged from round 3), stated plainly in the terms rather than narrowed — narrowing to a shorter window is the named alternative if minimisation becomes a concern.
32. **Three cancel categories, a column not a table** — CANCELLED is terminal in `order-transitions.ts`, so one column suffices; a future category (e.g. a prepaid refund's own state) would still not need a second table.
33. **`onTimeCounts` gains the cancel counts directly** rather than a sibling `cancelCounts` object (the design's original shape) — one place a caller already reads for "how did this vendor do," not two.

### Not built, and why (follow-ups, in dependency order)

- **The shadow run and the labelled set.** No alert ships until both exist and agree. That needs a candidates table (rule, fired-at, evidence) and a labels table appended never replaced, each actor an FK to `public.users(user_id)`, RLS on; and the rules and the gate (how many labels, how much agreement) are the founder's. No migration was written in this lane.
- **The line-verdict record** (ADR 0149 row 23) and **deliveries by provider** (`GET /procurement/deliveries` takes only `state` and `limit`).
- **Landed vs agreed** (sketch 117 cites ADR 0054, which is Proposed) — not used.
- **The claim writer** — `openCreditClaim` should record the claim's vendor and its order's currency when it opens the claim, so the row stands on its own; this lane reads around the gap rather than editing the procurement service.
- **The e-İrsaliye lapse** (`deliveries.lapsed_at`, ADR 0103 D3) is not yet an entry in the Docket.
- **The stored thread key** — `set_conversation_thread_key()` should re-key a row when a send first gives it a Gmail thread; until it does, the conversation thread list (`list_conversation_threads`, grouped by `thread_key`) shows an agent's first message and the vendor's answer as two threads. The scorecard reads around it (above); the trigger belongs to the conversations owner.
- **Staff by territory** (question 4, decided in principle) — waits for the areas model; nothing is withheld from a member until it lands.
- **The sentiment surface's replacement** (question 5) — being sketched; the legacy panel stays until it ships. [Round 3: built — the founder picked "A, Plus C's lines"; *How their mail reads* is on the vendor sheet and the legacy Sentiment tab is retired, see *Round 3, as built*.]
- **The house's time zone at sign-up.** `AuthService.registerRestaurant` still writes `dto.timezone || "America/New_York"` (`auth.service.ts:839`) — the invented-answer fault `organizations.service.ts:625-632` already removed. A house created without a zone therefore reads New York as *its own* time zone here. It belongs to the auth owner; not touched in this lane.
- **The real tenant has no zone.** Its `America/Los_Angeles` was cleared with the default (`20260903170000`) and its country keeps many zones, so its on-time line reads the no-zone span until the zone is set. Whether `/settings` can write `restaurants.timezone` was not checked in this build. [Round 3: it could not — nothing a person could reach wrote the column; `/settings?tab=time-zone` now does, owner or manager, audited.]
- **One country table** (ADR 0117 Q33) lives in the web (`apps/web/src/lib/countries.ts`, with its aliases). The gateway resolves a country only through `Intl`'s names (a code, `Türkiye`, `United Kingdom`, `UK`, a country's own name); an alias only the web table knows (`Turkey`, `USA`) resolves no country here, and the zone then gives the region when one is recorded. Moving the table where both runtimes read it is the follow-up.
- **Two sketch 117 details:** a conditional refusal printed with the window that would score (*365 d · 7 of 8 — that window scores*) — the window chips reach it by hand; and the two actions on a vendor with no orders — a quiet vendor gets the sentence and no lines, without them.
- **The dashboard's overdue alert keeps its own clock.** `dashboard.service.ts:744-745` raises *Overdue Order* when `new Date(expected_delivery_date) < now` — UTC midnight at the START of the expected day, and for PENDING orders too — so it can call an order overdue a day before the scorecard calls it late. Out of this lane; it should read `procurement/delivery-deadline.ts`.
- **Round 3 — not built, and why:**
  - **The receiving area's people are not asked yet.** The areas model is on `feat/house-areas` (PR #441, ADR 0218), not on this branch; `ArrivalAsksService.receivingAreaMembers` returns none, so the ask reaches owners and managers. When areas merges it reads the house's `receiving` area and passes it to `mayAnswerArrival`; nothing else changes.
  - **The shell's counter does not render the ask.** The counter is the shell lane's (`feat/shell-counter`, not on main); it can read `GET /procurement/arrival-asks` as it stands. The receiving page renders it today.
  - **"Close with a credit" has no act for an order that never arrived** (a claim is opened only at invoice verification) — question 15. The rule reads a credit settled `credited` on the order when one exists.
  - **Cancel is a link to Orders**, where the sealed cancellation lives; the orders page takes no order in its URL, so the link says *Cancel on Orders* and names the order. [Bracket, 2026-09-22 round 4: fixed. The link is gone; `RcArrivalAsks` and `IncompleteOrders` render the cancel act in place, `reasonCode` locked to `never_arrived` — see *Round 4, as built*, question 15.]
  - **No live Jev call was made.** Every Jev test runs against a loopback server; the reply shape (`score` + `confidence`, `noul`, `choice`) is the one the prompt gate reads and TypeSafe's overview documents, not one measured against `api.typesafe.ai` from this build. Whether any gateway deployment holds `JEV_API_KEY` was not checked (no `.env` read).
  - **`tone_quote` starts with new mail.** Messages the inbound model read before this build carry none, so their rows say *the read kept no line* until Jev scores them (switch on) or new mail arrives.
  - **`GET /providers/:id/sentiment` and the web `fetchSentimentTrend`** are left to their owner (PR #416's lane); nothing on the page calls them now.
  - **`/communications` takes no vendor filter**, so *All their mail* opens in place (question 17).
- **The tenant leaks** named by the research (`GET /providers/intelligence/compare`, `GET /providers/:id/sentiment`) are closed by **PR #416** (open on 2026-09-21, a peer's lane); this lane does not touch `provider-intelligence.*`. The first-against-last trend at `provider-intelligence.service.ts:399-404` is not fixed here either — the scorecard does not read it.
- **Round 4 — not built, and why:**
  - **The one `security definer` acceptance-and-flip database function** the design calls for — `HouseDataTermsService.accept()` does the seal, the insert and the flip sequentially instead. Named in *Round 4, as built*, question 13.
  - **The data-terms acceptance SHEET** on `/settings` — statements, subprocessors, *Hold to accept*. The backend refuses `enabled: true` with a 409 naming the terms endpoint [last call, 2026-09-22: now in the owner's words, no route named]; `MailReadingSection.tsx`'s copy says what turning it on now means, and that accepting is not on the page yet. No UI reads the terms or accepts them yet.
  - **`scripts/check_data_terms_name_every_host.py`**, the host-literal completeness guard — not built. The terms' statements were written from this round's own research, not from a mechanical host census.
  - **"Measure misses" (§B5)** — no preview endpoint (`GET /settings/data-terms/preview`), no `vendor_message_tone_scores` mask-review table. Only the CI gold set measures the masker's residual, and it is smaller than the design's ≥40 must-redact / ≥30 must-keep floor (`sensitive-mask.spec.ts`, named there).
  - **The tone-scoring thresholds** (question 12) still wait on a labelled sample.
  - **A refund claim for a prepaid never-arrived order** — question 20, not defaulted; nothing in Mudavym records payment at all (ADR 0077).
  - **A sign-in gate for every owner** (rather than only at switch-on) — question 19, not defaulted; the acceptance store serves either answer unchanged.
  - **The procurement agent's out-of-stock cancel** now writes its category (`vendor_cannot_supply`) but still bypasses `order-transitions.ts` and `system_audit_log` — unchanged from round 3's own note, still the procurement-agent owner's follow-up.
  - **`getVendorScorecard.spend` counting `PARTIALLY_RECEIVED` at full PO value** (named in round 3) — untouched; still its own follow-up PR.
  - **Languages other than Turkish and English** in the private-topic pass — mail in them is not sent at all now (question 18), rather than sent unread for that pass as before.
  - [Last call, 2026-09-22 — found missing, not built, named:] **the card fact's "· N never arrived" suffix** (design §C3): a never-arrived cancel is counted late and its Docket entry says so, but the one-line fact reads it like a late landing. **The vendor sheet reads the switch, not the effective state** (design §A4): after a terms version bump it would still show Jev's stored words while the sweep is paused — nothing is sent, the words are stale. **Turning Jev on again under an accepted version takes no seal** (design §A5 says sealed); it is owner only and needs the current acceptance. **The database does not check the acceptor's role** — `accepted_by_role = 'owner'` checks the literal the gateway writes; the owner check is the gateway's (`assertOwner`, twice), which is what the design's one function would have re-checked. **The acceptance sheet must not ship before the host-literal guard**: the `SUBPROCESSORS` list is from this round's research, and a count of `https://` host literals in the gateway and orchestrator on 2026-09-22 found hosts it neither names nor excuses (`api.apify.com`, `api.yelp.com`, `maps.googleapis.com`, `api.weather.gov`) — whether each receives house data was not measured. Until an owner can accept on a page, nothing is accepted against the list.

## Consequences

- The vendor sheet and the Roll Call say what a vendor did in counts anyone can open, and say plainly when there are too few records to say anything. On today's production corpus most lines will read *too few* or *not collected* for a while; that is the honest state, not a defect.
- A new register or a new house cannot make a figure lie by absence: a missing register is a line that says so, never a zero.
- The house record plus five register reads per open sheet and per Roll Call. Paged and capped; a house past 20,000 rows in one window on one register reads *could not read* on that line until the read is narrowed.
- A house with no recorded zone scores fewer deliveries on time than one with a zone: a landing within a day of midnight is listed, not counted. That is the price of never guessing a clock; setting the zone removes it.
- The analytics `onTimeRate` moves for every house: overdue orders now count late, and the deadline moved from UTC to the house's midnight. No screen reads it today (no caller of `GET /analytics/vendor-scorecard/:restaurantId` in `apps/web`, `apps/mobile` or `services`, by grep).
- **Revisit when:** the founder names the shadow rules and the gate; ADR 0149 row 23's line record lands; the areas model lands (question 4); a second house with real volume makes the minimums measurable instead of drawn.

## Verification (2026-09-21, worktree `wt-scorecard` at `origin/main` 34c33a76a)

- `vendor-scorecard.spec.ts` — 28 cases on the pure module, nothing mocked; includes the sketch's Skurnik credits record ($286.00 of $412.50), a claim with no recorded currency, and the invariant *every tally equals its rows* in every measure, both windows, at 30/90/365 days.
- `vendor-scorecard-is-house-scoped.spec.ts` — 17 cases against the real service and controller over an in-memory store that honours its filters, seeded with a second house whose rows are cross-linked to the first house's vendor (and a claim naming no vendor on the other house's order). **Mutation: each of the 15 `.eq("restaurant_id", house)` clauses removed one at a time — 15/15 killed (the 16th, on the outstanding-orders read added at last call, killed there too); `houseOf` made to return `""` instead of refusing — killed; restore byte-identical, green.** `jest src/providers`: 11 suites, 152 tests pass (148 before the last-call cases).
- Web: `LedgerCard.test.tsx` (12), `RollCall.test.tsx` (6), `ProvidersNext.test.tsx` (+7, 16 total) — the providers page suite 71/71. `tsc --noEmit` clean in `apps/web` and in both gateway configs; `check_gateway_boots.sh` PASS; eslint clean on every touched file.
- Seen, not only tested: the real `ProvidersNext` rendered in a throwaway Vite harness (fake adapter, fixtures computed by the real `buildVendorScorecard`, deleted after) at 1440 and 375 px — the card's fact, the ledger card in the sheet, the stacked Docket, the Roll Call with a failed register in every cell of its column, and the phone blocks with no sideways scroll.
- CLAIMS row `ADR-0207-VENDOR-SCORECARD-HOUSE-SCOPED` (static, python only) — mutation-tested: each of the 15 removed house clauses, a removed refusal and a handler bypassing `houseOf` exit 1; a clause moved into a comment exits 1; a missing file exits 2; the tree as built exits 0. `check_decision_claims.sh` 405/405.
- `check_money_states_its_currency.py` gained one allowlist row, for the web test fixture (it states a claim's order currency so the views can be asserted to print the stated one); product code adds no pinned currency.

## Verification — the rulings build (2026-09-21, worktree `wt-scorecard` on `cbd52685f`)

- New pure specs, nothing mocked: `procurement/delivery-deadline.spec.ts` (8 — Istanbul, Los Angeles in winter and summer, the no-zone span, the boundary instant), `common/house-frame.spec.ts` (7 — own zone first, the country's only zone, no default for a many-zone country, an unreadable zone kept, country by code / name / own name, derived locales for TR, JP, IT, CN, the region from the zone). `vendor-scorecard.spec.ts` now 36 [37 after last call — a currency under five claims prints no percent] (Istanbul flips a 01:30-local landing to late, Palo Alto keeps an 18:00-local landing on time, the no-zone span counts only what holds everywhere, the fact and prior window in the house's own percent and date formats, the percent never 100% short of all nor 0% above none, credits under five listed and at five a percent). `vendor-scorecard-is-house-scoped.spec.ts` now 20 (the house record is seeded second house FIRST in UTC+14; its failure is a 503 on all three routes, its absence a 404; an order due the day before the prior window is read when its midnight falls inside it). `analytics/vendor-scorecard-on-time.spec.ts` (2) runs the real `getVendorScorecard`.
- `jest src/providers src/analytics src/common src/procurement`: 154 suites passed, 1 skipped; 2,604 tests passed, 3 skipped [2,605 at last call, the index tree re-measured]. Web `vitest run src/pages/providers`: 8 files, 76 tests passed — `LedgerCard.test.tsx` 16 (percent with count, the claims listed under five, the percent at five, a bare amount, the house's lira and its dates on the Istanbul clock, the overdue order *late · not landed*), `RollCall.test.tsx` 7 (sorted by credits, only vendors with five claims rank), `ProvidersNext.test.tsx` 16. `tsc --noEmit` clean in `apps/web` and in both gateway configs (`tsconfig.json`, `tsconfig.spec.json`); `check_gateway_boots.sh` PASS; eslint clean (0 problems) on every touched file in both apps; `check_money_states_its_currency.py` PASS.
- **Mutation, one rule at a time, restored byte-identical (sha-256) after each:** gateway 21/21 killed — overdue uncounted; zone ignored; the midnight instant counted on time; the no-zone span collapsed; past-due at the earliest midnight; the outstanding read not widened by a day; the house record's id clause removed; its failure read as empty; its absence read as no zone; credits minimum back to 1; claims not listed; each percent guard removed; the fact without its percent; a many-zone country given a default; the locale pinned in `house-frame.ts`; the formats pinned in the copy; the analytics late count dropped, its house id clause removed, its zone ignored; an undecided landing counted. Web 7/7 killed — the figure without its percent, the claims list removed, the overdue status word removed, money and dates pinned (the date test first SURVIVED — `tr-TR` and `en-GB` print the same day-month here — and was tightened to an instant already the next day in Istanbul), too-few ranked among the scored (first mutation a no-op because a too-few value is null; re-run as "missing value ranks as zero", killed).
- CLAIMS: `ADR-0207-VENDOR-SCORECARD-HOUSE-SCOPED` updated (the house-record read carries `.eq("id", house)` and is the only read without a `restaurant_id` clause; the refusal now words through the copy module), and two rows added, `ADR-0207-ONE-ON-TIME-RULE` and `ADR-0207-FIVE-EVERYWHERE`. Each mutated: a code `restaurant_id` clause removed, the id clause removed or moved into a comment, the refusal replaced or its words emptied — exit 1; the copy module missing — exit 2; the retired literal back on a code line, the overdue entry uncounted, the analytics late count dropped, the no-zone span removed — exit 1; the same literal in a comment only — exit 0; the deadline module missing — exit 2; credits back to 1 — exit 1; the table renamed or the module missing — exit 2; the tree as built — 0, 0, 0. `check_decision_claims.sh` 407/407.
- **Not done:** the change was not seen rendered in a browser this time (the first build's throwaway Vite harness was not rebuilt); every web claim above is from the component tests.

## Verification — round 3 (2026-09-21, worktree `wt-scorecard` on `5aec6cbec`, index tree `7e5bf41e1`)

- `p4-scratch/verify_index.sh` on the staged index: `tsc` clean ×3 (gateway, gateway specs, web); gateway eslint 0 errors on every added or changed file; `jest src/providers src/analytics src/common src/procurement src/settings src/settings-audit src/vendor-tone` 170 suites passed, 1 skipped, **2,783 tests passed**, 3 skipped; `vitest src/pages/providers src/pages/settings src/pages/receiving src/pages/documents-reports src/components/providers` 23 files, **316 tests passed**; `check_decision_claims.sh` **410/410**; `check_gateway_boots.sh` PASS; `check_flag_readby_anchors.py` PASS (the two `inbound-responder.service.ts` anchors re-measured, 1003 → 1024 and 1027 → 1048); `check_money_states_its_currency.py` PASS; no duplicate migration prefix. Web eslint (`--quiet`, the scratch plugin dir) 0 errors on the 27 touched web files. `check_adr_numbers_unique.py`, `check_citation_pairing.py`, `check_no_conflict_markers.py`, `check_od_ids_exist.py`, `check_migration_versions_unique.py` PASS.
- **New specs, nothing of the unit mocked:** `vendor-tone/pii-mask.spec.ts` (10 [14 after last call]), `tone-scale.spec.ts` (10, with the egress), `jev-tone.client.spec.ts` (5, a real loopback server — a 302 never reaches its Location, the bearer is sent, the body carries no name), `vendor-tone-scoring.service.spec.ts` (5 [6 after last call]), `providers/scorecard/vendor-mail-tone.spec.ts` (16, the pure section and the real route as owner, manager, staff and another house), `procurement/overdue-order.spec.ts` (5), `procurement/arrival-asks.spec.ts` (7), `settings/house-time-zone-and-tone-switch.spec.ts` (10, the real controller, services, role gate and audit). Existing specs moved to the ask-first rule: `vendor-scorecard.spec.ts` 41, `vendor-scorecard-is-house-scoped.spec.ts` 21, `analytics/vendor-scorecard-on-time.spec.ts` 4, `inbound-responder.service.spec.ts` +2. Web: `MailTone.test.tsx` 9, `RcArrivalAsks.test.tsx` 6, `ReceivingNextArrival.test.tsx` 2, `IncompleteOrders.test.tsx` 4, `TimeZoneAndMailReading.test.tsx` 9, `LedgerCard.test.tsx` +2 (18), `ProvidersNext.test.tsx` +1 (17), `SettingsNext.test.tsx` counts moved to 18 / 14 registers.
- **The migration** built in PGlite 0.5.8 (PostgreSQL 18.3; not the Supabase stack — Docker would not start) from all 193 files: applied, re-run idempotent; RLS on both tables; `anon` and `authenticated` hold no privilege; `service_role` holds SELECT (an explicit grant added after the first build showed it held none there); the switch `NOT NULL DEFAULT false`; refused as designed — a scored row without a valence, a failed row with a score, a valence of 1.5 and a status outside the two (23514), a score keyed to no message and an answer by no `public.users` row (23503), an answer other than `not_yet` (23514).
- **Mutation, one rule at a time, restored byte-identical (sha-256) after each.** Gateway **37/37 killed**: the answer's expected-date match, the answered-after-the-deadline rule, the 30-day incomplete rule, the closed-with-credit exit, an unconfirmed order counted on the card, both house clauses and the failed-read refusal of the overdue reads, the analytics counting unconfirmed late; each mask pass (emails, phones, known names, placed names), the candidates and the whole text sent unmasked, the valence range, the quote allow-list, the confidence floor, redirects followed, the sweep sending when names are unread, sending automated mail and reading every house; a Jev failure falling back to the inbound label, C's sentence on one window, both house clauses and the failed-read refusal of the mail read, the mail route's role check; staff answering, the orders read's house clause, the answer's author, an incomplete order answered (**first SURVIVED** — the refusal was the same 409 either way; the case now asserts the Incomplete orders sentence, killed); the time zone's role check and validation, the Jev switch's role check and audit; the responder's defaulted *neutral*. Web **12/12 killed**: both figure sizes back, staff mounting the mail section, every message shown at once, C's sentence dropped, unconfirmed read as late, the ask drawn and fetched for staff (component and page), the switch and the zone open to staff, a failed switch read shown as off.
- **CLAIMS:** `ADR-0207-VENDOR-SCORECARD-HOUSE-SCOPED` and `ADR-0207-ONE-ON-TIME-RULE` bracket-corrected and re-pointed (both had regressed on the round-3 tree: four `houseOf` calls, and late only once confirmed); three rows added — `ADR-0207-ARRIVAL-ASKS-HOUSE-SCOPED-AND-GATED`, `ADR-0207-JEV-EGRESS-MASKED-AND-SWITCHED`, `ADR-0207-TONE-SCORES-INTERNAL-AND-MAIL-OWNERS-ONLY`. 27 mutations across the five rows behaved (22 exit 1, 5 missing-file exit 2); each tree as built exits 0.
- **Not done:** not seen rendered in a browser (every web claim is from the component tests); no live call to `api.typesafe.ai`; the migration not applied to Supabase or production (no production write).

## Founder questions (open; none blocks what is built)

11. [Answered 2026-09-22, round 6y — "Off until switched on (Recommended)"; with question 13, below, the switch stays off until an owner accepts the data terms, which is what turns it on.] **Jev scoring on by default?** Built OFF until an owner turns it on. On by default sends every house's vendor mail, masked, the day this ships.
12. **The word's thresholds** (choice 18) — his round-6y answer, *"Keep, re-set on sample (Recommended)"*: kept; re-set once a labelled sample exists (not built — see *Round 4, not built*).
13. [Answered 2026-09-22, round 6y — his words in the review trail.] **Who may turn Jev on** — built: owner only, and only as an acceptance of the house's complete data terms (*Round 4, as built*).
14. [Answered 2026-09-22, round 6y — his words in the review trail.] **OD-133 widened** — built: widened in place in `OPEN-DECISIONS.md`, bracketed, not renumbered; TypeSafe added to `EXTERNAL_CONNECTIONS.md` §2.
15. [Answered 2026-09-22, round 6y — his words in the review trail.] **"Close with a credit" for an order that never arrived, or is Cancel enough?** Answer: not as built, but Cancel with a reason category IS enough — built, *Round 4, as built*. A prepaid refund is the newly-opened question 20.
16. [Answered 2026-09-22, round 6y — "Owners and managers (Recommended)", matching what was already built.] **Who reads Incomplete orders** — built as the ask's audience (owners and managers).
17. [Answered 2026-09-22, round 6y — "Open in place (Recommended)", matching what was already built.] **"All their mail"** — in place, as built.
18. [Answered 2026-09-22, round 6y — "Keep, measure misses (Recommended)".] **"Only with names removed" — is a rule-based masker enough?** Kept, and widened to sensitive topics (`sensitive-mask.ts`, *Round 4, as built*). "Measure misses" (a preview + review store) was **not built** this round — see *Round 4, not built*; the CI gold set is the only measured residual so far, and it is smaller than the design's own floor.

1. **Shadow rules and the gate** — which rules would alert, how many labels and how much agreement before one ships, and where the label control lives (B's centred panel — a question — or C's popover — a choice, ADR 0112).

9. [Superseded in round 3 by the founder's delegated rule: dated at its deadline, counted only once confirmed, and after 30 days out of the figures in Incomplete orders.] **An order past its date that never lands — dated at its deadline, or now?** At its deadline (built, choice 16) it counts late once and ages out of the current figure and open list one window later; dated now, it stays late in every current window until it lands or is closed, which also keeps an order nobody closed counting against the vendor. Recommendation: now, with the Docket naming its age — the ruling's reason was to feed the analytics, and an order that never came is the strongest signal there is.
10. **The real tenant has no zone and a many-zone country** — keep the span rule (choice 10: deliveries near midnight listed, not counted, until the zone is set), or name one zone for it now? Recommendation: set its `restaurants.timezone` (a data fix by the founder's word) and keep the span rule for any house that has none.

19. **"They have to accept that" — every owner, at sign-in, or only to turn Jev on?** Built: only to turn Jev on. An owner accepts the complete data terms at the moment they turn Jev on; a house that never turns it on is never asked to accept anything. Because accepting includes Jev, a sign-in gate for every owner would in practice make Jev on-by-default for every house that keeps using Mudavym — the opposite of question 11's built-OFF direction, unless acceptance and Jev are deliberately split (his words bundle them). Recommendation: keep it tied to switch-on until a lawyer has read the `term` statements — the same "our own terms first, lawyer later" sequencing as the wine pool, but a gate every owner must pass is the one place an unread legal text costs the most.
20. **A never-arrived order the house already paid for — should Cancel also open a refund claim?** Nothing in Mudavym records payment at all (ADR 0077), so a prepaid order that never came is money owed back that no screen tracks. (a) Yes — one box on the never-arrived cancel, "We paid for this — we are owed {total}", opening a credit claim with the order's vendor and currency (small; also fixes the claim writer's missing vendor/currency for this path). (b) No — Cancel with its reason is enough; prepaid refunds wait for an actual payment record. Recommendation: (a) if houses ever pay before delivery, (b) if they always pay on invoice.

Questions 2–8 were answered on 2026-09-21 and moved to the review trail, verbatim.

## Verification — round 4 (2026-09-22, worktree `wt-scorecard`, PR #435)

- **New pure specs, nothing mocked:** `procurement/cancel-reason.spec.ts` (the category/state/deadline rule), `vendor-tone/sensitive-mask.spec.ts` (shapes' checksums, the topics pass's must-redact/must-keep sets, `languageCovered`). Extended: `providers/scorecard/vendor-scorecard.spec.ts` (a `never_arrived`/`vendor_cannot_supply`/`house_decision`/pre-placement cancel case, and the existing legacy-CANCELLED case's row count bracket-corrected from 7 to 8 now that a reasonless cancel is listed rather than silently dropped [last call, 2026-09-22: by the pure build only — the live read never passes a reasonless cancel to it; see question 15's bracket]), `analytics/vendor-scorecard-on-time.spec.ts` (three new cases for the cancel counts; the one exact-match `onTimeCounts` assertion widened to the four new fields), `vendor-tone/pii-mask.spec.ts` and `vendor-tone/vendor-tone-scoring.service.spec.ts` (their exact-match `masked` assertions widened the same way), `vendor-tone/vendor-tone-scoring.service.spec.ts` (a `StubDataTerms` double and three new cases: no send without a current acceptance, a failed acceptance read is `housesRefused` not silent, and the mid-run re-read stops the NEXT call once the acceptance lapses). Web: `SealedRejectDie.test.tsx`'s one exact-match `mutateAsync` assertion widened to include the default `reasonCode`; `TimeZoneAndMailReading.test.tsx` moved from `canManage` to `isOwner` and its copy assertions updated to the widened masker note.
- **Migrations** `20260922015000` (cancel reason columns) and `20260922015010` (data-terms acceptance table, seal kind widened) each self-assert: additive (`ADD COLUMN IF NOT EXISTS`, `CREATE TABLE IF NOT EXISTS`), the seal-kind widen proven by read-and-append (never a hand-typed list) with the union checked both ways, RLS on, `anon`/`authenticated` hold nothing, the CHECK constraints proven by both a refused and an admitted probe row (each deleted after). Not applied to Supabase or production (no production write, per the rules).
- **Not done, named plainly (CLAUDE.md §0.5):** not seen rendered in a browser (no time this round); `verify_index.sh`/`heavy.sh` full run pending at the top of the build report [last call, 2026-09-22: since run — see the last call's review-trail row for the measured numbers]; the acceptance-and-flip database function, the host-literal guard, the terms acceptance sheet, and the mask-miss preview/review store are none of them built — each is named where it belongs above rather than silently absent. The gold set in `sensitive-mask.spec.ts` is smaller than the design's own ≥40/≥30 floor.

## Review trail

| Date | Reviewer | Outcome |
|---|---|---|
| 2026-09-21 | scorecard lane (builder) | Created with the build; pick recorded; builder's choices marked Proposed |
| 2026-09-21 | last call (Opus) | Reply time fixed in the read: Gmail thread first (a sent draft keeps its insert-time `msg:` key), `AUTO_SENT` counted as a send. On time: orders past their date and not landed listed as open and carried on the card's fact (question 8). The Docket no longer labels an open claim *not counted* (it is in what was asked). A vendor missing from a Roll Call that answered draws the dash, not *could not be read*. Seven cases added; each fix reverted once and its case went red. |
| 2026-09-21 | founder (Aldemir), relayed verbatim by the orchestrating session | **Question 8:** "count it as late, because that will help us feed to the analytics for better results". **Question 3:** "Percent with count". **Question 6:** "House's local midnight". **Question 2:** "5 everywhere". **Question 7:** "english +TR formats and other languages possiblee for others like japanese, italian, chinese etc.". **Question 5:** "sentiments are a must in our new page but not as in this design, the feature must stay, the current design can go". **Question 4:** "depends on the territory of staff. if it's out its jurisdiction then doesn't see it, but otherwise all, (that can be adjusted while showing the formatted invoice and details)". Question 1 not answered. |
| 2026-09-21 | rulings build (scorecard lane) | Questions 2, 3, 6, 7 and 8 built (see *The founder's rulings of 2026-09-21, as built*); 5 kept as the founder asked (the legacy panel stays while its replacement is sketched); 4 recorded as decided in principle, pending the areas model. Measured below under *Verification — the rulings build*. |
| 2026-09-21 | last call (Opus), rulings build | A percent from fewer than five: credits in two currencies printed each currency's percent once the claims *in all* reached five (3 dollar + 2 lira claims read *80%* and *63%*, and the builder's spec asserted it) — now a currency's percent needs five claims of its own, and the line says why; reverted once, 2 cases red. Two web strings moved into `sc-copy.ts`. Records now false bracket-corrected (the fraction wording, the fact, the Roll Call cell, the credits row, the spec count). The builder's own choices, written under *Locked*, listed as choices 10–16 **Proposed**; questions 9 (overdue dated at its deadline ages out) and 10 (the real tenant's zone) added; the dashboard's UTC overdue alert named as a follow-up. Measured on the index tree: tsc ×3 clean; `jest src/providers src/analytics src/common src/procurement` 154 suites, 2,605 passed, 3 skipped; `vitest src/pages/providers` 76/76; claims 407/407; gateway boots; eslint clean on touched files. |
| 2026-09-21 | founder (Aldemir), relayed verbatim by the orchestrating session — round 3 | **Sentiment (question 5's replacement):** "A, Plus C's lines"; placement "Vendor sheet only"; owners and managers only, staff never see it; the legacy tab's design retires, the feature stays. **The ledger figures:** "the on time numbers ... 12 of 14 deliveries, 9 of 13 lines ... The font size are a little big". **Tone data:** "talk with JEV, put that onto point scale, very detailed, seuper intelligent ML data needed. this feature can also be disabled. other than that use warm/plain/terse"; Jev egress: "Only with names removed". **Overdue orders:** "think of a best way to handle this ... You tell me". **Time zone:** "Add it to Settings". |
| 2026-09-21 | round 3 build (scorecard lane) | All five built (see *Round 3, as built*); choices 17–27 Proposed; questions 11–17 asked; question 9 superseded. Measured under *Verification — round 3*. |
| 2026-09-21 | last call (Opus), round 3 | The masker let names out that it had found or could have known: a name found at a sign-off stayed in the whole text sent (only the cut sentences were re-masked), the sender's own `From:` name was never a known name, and a signature under `--` or a Turkish *Deniz Bey* went out whole. All four fixed (`pii-mask.ts`, `vendor-tone-scoring.service.ts`), each reverted once and its case went red (4/4); the CLAIMS row `ADR-0207-JEV-EGRESS-MASKED-AND-SWITCHED` now checks the whole-text re-mask and the sender name (both mutations exit 1). The Settings note no longer promises more than the masker does (one sentence added, its test reverted once and red). Records now false bracket-corrected: the switch's default is question 11, not 13; the sentiment replacement is built, not being sketched. Question 18 added. |
| 2026-09-22 | head of research (Opus), round 4 | `scorecard4-decision.md` written from three research files (consent, sensitive-topics, cancel-vs-credit), each re-checked against the code (CLAUDE.md §5b) rather than trusted. Design for A (owner acceptance of the data terms), B (sensitive-topic redaction) and C (the cancel category and its analytics), an adversarial pass against the first draft (13 attacks, each named with what changed or why it was rejected), and founder questions 19–20. |
| 2026-09-22 | founder (Aldemir), relayed verbatim by the orchestrating session — round 6y | **Question 13 (who may turn Jev on):** "owner only, but also we're going to use this as complete data and privacy usage, they have to accept that, and when they do they'd accept the jev too with their names and sensitive topics redacted." **Question 11 (default):** "Off until switched on (Recommended)." **Question 18 (is a rule-based masker enough):** "Keep, measure misses (Recommended)." **Question 12 (thresholds):** "Keep, re-set on sample (Recommended)." **Question 14 (OD-133):** "Widen, add TypeSafe (Recommended)." **Question 15 (never-arrived order):** "if. cancelde make sure the all analytcis be configured toward that, canel is enough or not?" (verbatim, typos kept). **Question 16 (Incomplete orders):** "Owners and managers (Recommended)." **Question 17 (all their mail):** "Open in place (Recommended)." |
| 2026-09-22 | round 4 build (scorecard lane, builder) | C (cancel-reason.ts, migration `20260922015000`, the gateway write path, the procurement agent, the analytics), B1–B4 (sensitive-mask.ts, wired into pii-mask.ts/tone-egress.ts, the language gate wired into the sweep and the sheet) and A's core (migration `20260922015010`, house-data-terms.ts, HouseDataTermsService, the owner-only + acceptance-gated routes, the sweep's acceptance check and per-call re-read) all built — see *Round 4, as built*. Scoped down from the design and named plainly (CLAUDE.md §0.5): B5 (the mask-miss preview and review store), the host-literal completeness guard, the acceptance-and-flip database function, and the terms acceptance sheet on `/settings` were none of them built this round; each is named under *Round 4, not built* rather than silently absent. Choices 28–33 Proposed; questions 19–20 asked; question 12 stays open. Measured under *Verification — round 4*. |
| 2026-09-22 | round 4 last call (Opus) | Tried to break it; fixed and staged: a private sentence wrapped across lines left half-masked, in the body and as a quote (the topics pass now reads the sentence across the wrap; `egressFor` offers only quotes found word for word in the masked body); the language gate read the raw message and passed any text under eight words (now on the latest part, inside `egressFor`, fail closed); the sheet's language line hid the inbound reading for every house while Jev is off (now only while on); the plainest health, death, pregnancy and password words were missing from the list; `PATCH orders/:id` could still cancel with no category, seal or role check (now refused, 422); the typed arrival act lacked its `reasonCode`; the 409 an owner sees named API routes, and the Mail reading note pointed to a "Settings → Data terms" section that does not exist; a failed switch read made the terms readout say "off"; the Jev terms statement did not say what the pass can miss; Incomplete orders' sentence said a cancel keeps an order out of the figures; the Responses sheet offered "Never arrived" on pending-stage rows, which the gateway always refuses; the register row said only the product caller sends personal data; the acceptance route had no spec at all (now `settings/data-terms/house-data-terms.spec.ts`, end to end over the real seal, role and audit services, with each `fact` statement's cited file and line checked to exist); and the terms cited the lookback at `vendor-tone-scoring.service.ts:43`, two lines above where it now is. Records here bracket-corrected (questions 11, 15–18, verification round 4); what it found and did not build is under *Round 4 — not built*. Measured on the staged index (`verify_index.sh`, tree `ee9550c34`): gateway tsc (both configs) and web tsc exit 0; jest on providers/analytics/common/procurement/settings/settings-audit/vendor-tone 173 suites passed (1 skipped), 2,905 tests passed, 3 skipped; vitest on the six touched web areas 33 files, 495 tests passed; claims 412 of 412 holding; gateway boots; no duplicate migration prefix. Six mutations, each alone, each restored byte-identical: the egress language gate, the body-substring rule, the paragraph-level topics pass, the PATCH refusal, the never-arrived `late` count and the acceptance's owner check — each turned its spec red, and the first, fifth and sixth also turned their CLAIMS rows red. Both migrations applied twice on a PGlite database seeded with a restaurant, a user, an inventory row and a provider, leaving no probe rows. Not seen in a browser. |

# Decision Log — Mudavym

> Every decision gets a record. A decision not written here (or in a doc this
> index points to) **has not been made** — that is non-negotiable #1 in
> [`CLAUDE.md`](../../CLAUDE.md).

## How this works

- One decision = one file: `NNNN-short-slug.md`, from [`TEMPLATE.md`](TEMPLATE.md).
- Statuses: **Locked** (binding) → may later become **Superseded** (points to its
  replacement). Nothing here is ever silently deleted.
- Undecided forks live in [`OPEN-DECISIONS.md`](OPEN-DECISIONS.md) — the queue of
  questions waiting for the founder. Sessions add to it; only a founder call moves
  an item out of it and into an ADR.
- Decisions locked **before** this log existed remain canonical where they were
  written. This index links to them; we do not copy them (one source of truth per
  decision).

## Locked — recorded in this log

| # | Decision | Date |
|---|---|---|
| [0001](0001-mudavym-single-entity.md) | Mudavym is one entity; modules are internal softwares | 2026-08-24 |
| [0002](0002-documentation-first-operating-mode.md) | Documentation-first operating mode + ADR discipline | 2026-08-24 |
| [0003](0003-session-output-discipline.md) | Low per-session output footprint, branch-per-operation | 2026-08-24 |
| [0004](0004-obsidian-as-backlink-layer.md) | Obsidian adopted as the doc backlink layer | 2026-08-24 |
| [0005](0005-v3-to-v0-version-reset.md) | v3 internal build → deliberate v0 production reset | 2026-08-24 |
| [0006](0006-neural-footprint-architecture.md) | Neural Footprint — split narrow production store from wide append-only research log; NF-C gated | 2026-08-24 |
| [0008](0008-nf-column-contract.md) | NF column contract — full ADR 0006 shape now (Path C); Claude's Path A recommendation overruled | 2026-08-24 |
| [0007](0007-org-structure.md) | Org — 7 divisions, 19 departments, 3 findings-only advisory functions, 7-artifact unit anatomy | 2026-08-24 |
| [0009](0009-loop-vocabulary-contract.md) | Loop vocabulary read from ORG_STRUCTURE §5.1's own table and enforced as a hard CI gate; `active`/`running` must cite evidence — live loops 6 → 5 | 2026-08-24 |
| [0011](0011-pos-sale-volume-contract.md) | POS sale volume — `sale_volume_ml` is the truth, `sale_unit` becomes an open reporting label, and an unresolvable line queues instead of defaulting to a bottle. Kills a silent 5x over-depletion on all 92 production mappings; accepts visible under-depletion in exchange | 2026-08-25 |
| [0010](0010-gemini-model-retirement.md) | `gemini-3.5-flash-lite` replaces retired `gemini-2.0-flash`/`gemini-pro`; runway beat price. Spend table was wrong (2.5-flash under 8.3x), lite billed as flash, thinking tokens uncounted — all three corrected | 2026-08-24 |
| [0016](0016-ledgers-must-express-unknown.md) | Ledgers express "unknown" — `api_spend.cost_usd` nullable and `DEFAULT 0.0` dropped, one `unpriced` determination driving both ledgers; every rate row carries a required dated source, so an undated price is a `TypeError` at import; calendar regex fallback reads the email, not the prompt. `gpt-4-turbo` verified correct | 2026-08-25 |
| [0015](0015-pos-referential-integrity.md) | POS referential integrity — `inventory_id` FK added, 92 orphaned mappings from a deleted tenant removed; ledger writes idempotent | 2026-08-25 |
| [0017](0017-doneability-verdicts-are-sidecar-claims.md) | Doneability verdicts are sidecar claims (`nf_verdict`, one row per event+basis), never edits to the event; first grader `reconciliation_v1` on invoices only; coverage view ships with the verdict | 2026-08-25 |
| [0018](0018-p2-plan-of-record.md) | P2 plan of record — milestone `P2 — Web complete + deploy`; spine reset; Surface page graph; existing-first then founder-approved proposal | 2026-08-25 |
| [0020](0020-no-fabricated-answers.md) | **A surface with no data says so; it never invents one** — fabricated analysis deleted rather than labelled, actions that cannot complete refuse out loud, an error never renders as emptiness, a mislabelled number is a fabrication. Generalised from 7 broken / 8 hollow pages | 2026-08-26 |
| [0024](0024-identity-first-signin.md) | **Sign-in reveals the methods an identity actually has** — email first, then the methods resolved from `password_hash` + `user_oauth_accounts`; one provider registry; `validateUser` stops guessing "Google" (wrong for 4 of 4 password-less production accounts); enumeration made deliberate and rate-limited, with `requestPasswordReset` explicitly untouched. Applies ADR 0020 to auth | 2026-08-26 |
| [0025](0025-citations-must-disagree-loudly.md) | **A citation must carry two anchors that can be diffed — an id *and* a line — and CI fails when they disagree.** Re-derived at lock time: **78** register citations, **0 of 38** id-paired ones agreeing, 36 carrying no id at all. Line anchors are 100% wrong but detectable; ids renumber and keep resolving, which is what hid OD-83. `scripts/check_citation_pairing.py` enforces both arms in the `decision-claims` job — the checker the ADR called "already written" existed only in a transcript. **74 citations repointed**, 3 handed off ([HANDOFF-adr-0025](../04-specs/HANDOFF-adr-0025.md)). Also ships "a claim that cannot run is a FAILURE": three of four cannot-run states were certifying themselves, including one on a security claim; cost on arrival **0 of 94**. §7 executed — **469 archive twins deleted**, re-derived not trusted. **All three questions answered by the founder 2026-08-26** | 2026-08-26 |
| [0042](0042-iznik-seal-and-warm-charcoal.md) | **İznik is the seal, Warm Charcoal is the dark ground** — `#1A5E6B`/`#5FB0BC` + `#15130F`, canonical tokens from sketch 059; both grounds ship. Supersedes 0041's `#CD2D5B` (answered on a stale premise). Applied to the makeover canvas 2026-08-29 and rolling onto dev.mudavym.com | 2026-08-29 |
| [0043](0043-wordmark-interim-logo-search.md) | **Interim brand = Fraunces text wordmark**, "M." glyph where a mark is unavoidable; wine-glass BrandMark retired. The real logo stays open (OD-111) — two agent candidate waves (instrument · house) filed under this ADR for founder elimination | 2026-08-30 |
| [0048](0048-domain-quant-under-research-math.md) | **"Food into math" folds into Research & Math**, and the keystone is transformation. Domain quant (yield/costing · forecasting/inventory · menu/pricing) joins the existing division rather than founding one — it already carries ADR 0001's four compensations and already contains the Backtests team, whose mandate names *forecast accuracy* unchanged. The work is a seven-layer graph with a **forced** order whose only keystone is **L2 yield + recipe/BOM**, the sole bridge between ingredient and dish. Programme runs **both-sequenced**: wire the built-but-uncalled beverage decision layer now, design the food ledger in parallel. Evidence: the ledger physically **cannot represent food** (`master_wine_id NOT NULL`, integer quantities, no BOM — checked across all 64 migrations), while intake is already `numeric(12,3)`. Six of twelve research claims were killed by an adversarial pass and are recorded rather than dropped. Forks OD-113…117 | 2026-08-31 |
| [0049](0049-ecosystem-division-layer.md) | **The ecosystem is layered into eight divisions** — the five founder-named musts (Restaurant, Customer, Vendor, POS, Sommelier) + three the codebase demanded (Intelligence/Analytics, Platform/Admin, Agent fleet/runtime), recorded as ECOSYSTEM-PLAN.md §3a; an *amendment* — segments, spine, and the E1-after-E0 order stand. 0045–0048 are claimed on in-flight branches | 2026-09-01 |
| [0050](0050-agent-dispatch-hardness-threshold.md) | **Session agents are dispatched on a hardness score — ≤3 Sonnet, ≥4 Opus** — five axes (blast radius, reversibility, ambiguity, span, verification cost), overrides to Opus for auth/production/ADRs/outward sends, and split-don't-route for a census that feeds a decision. Scores *judgment and consequence*, never effort. **Distinct from [0036](0036-cost-routing-two-plans-in-harmony.md)**, which governs the product's own model calls | 2026-09-01 |
| [0051](0051-rebuilt-pages-show-live-data-only.md) | **A rebuilt page shows live data or says it does not know** — no hardcoded values, seeded fixtures or placeholder numbers dressed as measurements; unknown is the em dash and never zero, a full window renders as a floor, a failure is said in words. Scopes [0020](0020-no-fabricated-answers.md) to the ten-page rebuild after the dashboard was measured stating four things the backend cannot know. Legacy pages deliberately left alone | 2026-09-01 |
| [0052](0052-software-catalog-layer.md) | **The product is catalogued as small softwares, one note each** — new `08-softwares/` layer (contract + SOFTWARE-MAP) nesting under 0049's eight divisions; all 47 page notes gain a `softwares:` list, executing PV-12 early. `atlas-overlay.json` untouched by founder direction; retire-to-write waived for this wave. 0050–0051 are claimed on in-flight branches | 2026-09-01 |

## Proposed — implemented, awaiting a founder lock

> These carry `status: proposed`. The work has landed and the OPEN-DECISIONS rows are
> closed, but per the template a decision is locked by the founder, never by a session.

| # | Decision | Date |
|---|---|---|
| [0012](0012-reports-through-the-gateway.md) | Generated reports read through the gateway, not the browser — RLS-on-with-zero-policies confirmed in production (and on 141 other tables); the missing delete endpoint and three invented columns closed rather than half-ported | 2026-08-25 |
| [0013](0013-one-commitment-guardrail.md) | One UCC commitment guardrail: TypeScript canon, generated Python module, three independent drift guards. Real counts were 19 / 8 / **3** — a third list existed | 2026-08-25 |
| [0019](0019-p2-build-scope.md) | **P2 build scope — the founder-approval list**: live defects (2 broken routes), 15 dead-end verdicts, cold-entry audit, tenant-ownership gap; drafted from the Surface graph, awaiting lock | 2026-08-25 |
| [0021](0021-studio-invites-are-self-service.md) | **Studio invites are redeemed by the invitee, bound to the invited address** — the role gate made redemption impossible for everyone it was for; `target_email` was written and never read, so removing the gate alone would have opened a privilege-escalation path; and nothing served `/api/v1/studio/*` in either environment | 2026-08-26 |
| [0022](0022-scheduled-jobs-serve-opted-in-tenants.md) | Scheduled jobs iterate opted-in tenants and never borrow another tenant's recipients | 2026-08-26 |
| [0023](0023-email-verification-is-enforced.md) | Email verification is enforced, and enforced on the server | 2026-08-26 |
| [0026](0026-schema-has-one-home.md) | **Schema has one home, and CI compares it against the code** — five instances in a day of a migration archived, never applied, and queried anyway. `Fresh database equals remote` was green *because* both sides were wrong the same way, and `baseline_from_production` made that blindness total. Two guards add the third corner (what the code queries); 14 relations + 5 rpc functions + 8 sole-definitions recorded as shrink-only debt; blind spot measured at 24/1377 sites. **0025 skipped deliberately** — a concurrent worktree was already holding it | 2026-08-26 |
| [0027](0027-push-recipients-are-not-resolved-here.md) | **The recipient resolver's push branch is deleted, not repointed** (resolves OD-95, Proposed). The standing recommendation was to repoint at `notification_preferences.push_subscription`; that column's only writer upserts `onConflict: "user_id"` against a table whose only unique index is `(restaurant_id, user_id)`, so it returns **42P10** and can never hold a value — repointing would have swapped a loud 404 for a silent empty read. It was also unusable: **both push senders take user ids and enumerate devices themselves**. `"push"` leaves `NotificationChannel`, so asking is now a compile error. Applies ADR 0020. Found in passing: **no gateway spec file is type-checked by anything** (`tsconfig.json:24` + ts-jest `isolatedModules`) | 2026-08-26 |
| [0028](0028-phantom-relations-repoint-or-delete.md) | **A phantom relation is repointed or deleted, never created** (resolves OD-99, Proposed). Eleven class-C relations — six tables plus **five `.rpc()` functions the register never mentioned** — verified absent from *production* by curl, not from `supabase/migrations/`. None got a migration: `reports`→`generated_reports`, `inventory_stock`→`restaurant_inventory`, `managers`→`manager_report_profiles` were **repointed** (the real store existed all along); the rest were **deleted**, per ADR 0027. What let them survive was uniform: every read was wrapped in error handling that returned exactly what success returns — and in three RPC call sites the author's own fallback sat *inside the same `try`*, so it was unreachable code that made the site read as defensive. `_find_provider_by_email` had returned None for **every inbound email ever parsed**; `jsonb_array_append` was a **write**, so its failure was data loss, not a degraded read. Found in passing: **two tests that pinned defects rather than catching them**. Applies ADR 0020. Five forks filed as OD-100…104 | 2026-08-26 |
| [0029](0029-p3-plan-of-record.md) | **P3 plan of record: grade before you scale, and parallel only where nothing is assumed** (Locked). The founder asked whether the five ROADMAP P3 candidates could run in parallel; the answer is **no, and not for capacity reasons** — two of the five are blocked on decisions rather than effort, so "parallel" would mean an agent picking the guest product (OD-05/OD-07) and the routing basis (OD-04) by default. Shape: one gate (**P3.0 doneability coverage** — the gateway emits 7 task types and exactly 1 carries a real verdict, the rest stamping `call_level_v0` at `model-client.service.ts:387`), two lanes gated on nothing (mobile parity, kitchen expansion), two stages behind the gate (Ask AI, model registry), and **NF-B held**: 564 lines of applied migration, 3 tables, 2 CI guards, zero callers. Supersedes PLAN.md Push 4 and the ROADMAP P3-candidate menu | 2026-08-26 |
| [0014](0014-proposal-candidate-set-null.md) | **A proposal keeps its question when it loses its candidate** (Locked 2026-08-25, **restored 2026-08-27**). `pos_catalog_match_proposals.candidate_inventory_id` takes `SET NULL`, not the `CASCADE` its sibling took: a mapping is a *claim* ("this POS item depletes that stock row") and dies with its target, while a proposal is a *question* ("is this the right target?") that outlives its answer. Symmetry with ADR 0030 would have been cosmetic | 2026-08-25 |
| [0030](0030-pos-mapping-inventory-integrity.md) | **Delete orphaned POS mappings, and make the database refuse new ones** (Locked 2026-08-25, **restored 2026-08-27 — was ADR 0012 until a squash-merge dropped the file and that number was reused**). All 92 production `pos_item_mappings` rows carried a non-null `inventory_id` pointing at nothing; root-caused to `SYNTH_WRITE_SET` and proven reproducible 92/92. Deleted, then an FK so the database refuses the next one | 2026-08-25 |
| [0031](0031-migration-ledger-reconciliation.md) | **Reconcile the migration ledger in both directions, and check it that way** (Locked 2026-08-25, **restored 2026-08-27 — was ADR 0013**). Applying ADR 0030's migration surfaced ledger drift *both* ways (4 unregistered, 1 unrecorded); the check now runs in both directions rather than the one that happened to be noticed | 2026-08-25 |
| [0032](0032-vault-cleanup-cut-line.md) | **Delete closed build artifacts; a tombstone index replaces the archive tree** (Locked — resolves OD-01's cut line). Cut line C-modified, founder-picked question by question: `phases/` + `quick/` + the remaining `archive/` twins + 4 duplicate data blobs deleted (~525 files, vault 1,677→1,152), everything recoverable via the ADR's tombstone index. Kept on evidence: `sketches/` (20 live code citations), `claude_full_architectural.md` (founder), `FIX_ERROR_LOG.md` (its `.cursor` skill still runs). Standing rules: archive = delete + tombstone, never an in-tree copy; retirements score AUTO only when provably zero-loss, everything else is founder REVIEW | 2026-08-27 |
| [0033](0033-design-map-zoomable-atlas.md) | **The design map is a zoomable atlas over one generated graph** (Locked). Six looks sketched and scored on the founder's axes (whole-map, detail, growth); F chosen. `atlas-graph.json` is generated source of truth (every edge carries its derivation basis), `atlas-overlay.json` is additive-only — the generator exits 2 if the overlay tries to shadow the map — and `DESIGN-MAP.html` renders three altitudes. First build: 186 features · 464 endpoints · 1,142 edges, browser-verified | 2026-08-27 |
| [0034](0034-agent-stack-artifact.md) | **Every unit gets an agent stack — a 9th artifact, `<slug>-agent-stack.md`, docs-only** (**Locked 2026-08-27** — founder answered the four framing forks in-session, delegated the two approach picks with criteria, and locked the same day by re-stating them; no pick overruled). Requirements-only declarative agent cards, harness-agnostic while OD-03 is open (model choice stays with aio-model-routing; stock/money/outbound stays confirm-gated per FUTURES §8.1); §3.3-compliant T2 skill tables (real past instance or no row); four-layer git-native memory (skills · NF-A episodic · one-fact-per-file semantic with provenance · bounded working) with a PR-gated consolidation loop feeding skill-harvesting's queue; cross-unit interaction async-only. Template `_templates/agent-stack.md`, wave contract `foundation/GENERATION_BRIEF.md` §7 | 2026-08-27 |
| [0035](0035-wave2-seam-reconciliation.md) | **Wave-2 seam reconciliation — one owner per question, and the roster is 76** (Locked — founder picked all eight in-session). `nf-a-coverage-report` split by question (Applied AI owns coverage; observability's job renamed emission-liveness); substrate report produced by the team, consumed by the department; the fleet census has one computer (`fleet-census-agent`) and HR publishes only the declared-vs-computed variance; `nf_a.cost_per_task` — the most-covering unit produces, Finance fetches (OD-29's Finance edge settled, RM-1 half open; ledger-grain divergence filed); NF-B erasability loop assigned to privacy-engineering (NF-B stays HELD, mechanism fork open); POS-bridge throughput excludes the 66 `P3PROOF-*` rows; ai-surface-security narrowed to allowlist *audit*; `backtests` accepted as team 76 with a dated ORG_STRUCTURE correction | 2026-08-27 |
| [0036](0036-cost-routing-two-plans-in-harmony.md) | **Cost routing runs as two plans in parallel, in harmony — OD-29 closed** (Locked). RM-1 owns the methodology (benchmark design, what cost-per-task means, substitution-study rules); `aio-model-routing` owns the operation (the shared wrapper — real since P1 as `common/model-client` — and the production routing policy). TECH-F3's escalation transfers: if the line fails, merge — never duplicate | 2026-08-28 |
| [0037](0037-nfb-erasure-is-crypto-shredding.md) | **NF-B erasure is crypto-shredding** (Locked). Per-guest keys; erasure = key destruction, O(1), rows become noise in place — ADR 0006's wide append-only research store survives GDPR contact intact. Costs owned going in: key management + a decrypt on every training path. NF-B stays HELD (ADR 0029); this settles the design so activation does not improvise it. Fallback if a decrypt ever dominates a named path: aggregate-only inputs for that path, never for the store | 2026-08-28 |
| [0038](0038-cards-run-as-declared-scripts.md) | **Agent cards become a machine index, and mechanical cards run as declared scripts** (Locked — scope "substrate + all mechanical agents", home scripts/ + 00-index/). `build_agent_card_index.py` → `cards.json` + `CARD-MAP.md` (100 units, 102 cards; contract enforced: confirm-gate on every card, no model/queue/OD-03 token; CI `agent-card-contract` gate). `run_card.py` executes mechanical cards watch_loops-style — reports, never edits, except `--write-memory` landing one-fact-per-file semantic memory as PR diffs; refuses undeclared agents. **Corrected same day by the adversarial audit (see the ADR's Corrections):** 8 runners = 4 mechanical cards + 4 named mechanical *sub-duties* of judgment/extraction cards (now enforced by a `MECHANICAL_SUBDUTY` allowlist), 32 mechanical unimplemented; and the fleet census re-measured against the registry gate — **18/24 can start by default** (5 OPTIONAL boot-refused), not the 23 first published. First 4 skills through the §3.3 gate (registry 0→4, 4/4 compliant); first measured facts in 8 units' memory/ — P1 wrapper consolidation verified holding, core = 6,556 lines (OD-03 baseline). Judgment/extraction *duties* stay designed: running them would fabricate | 2026-08-28 |
| [0039](0039-activation-plan-of-record.md) | **Activation plan of record — harden the spine, and every department writes its own agenda** (Locked; the ADR-as-plan form of 0018/0029). Track A, five items with owners and done-conditions: A1 the OD-03 bake-off scored against `cards.json` (the long pole, core/ diet holds); A2 `api_spend.task_type` + one joined cost-per-task view; A3 the single typed action schema, `recurring_order_agent` brought inside; A4 `nf_a.skill_id` + a weekly runner cron; A5 judgment rubric #1 (vendor replies, TECH-F3 line kept). Track B in parallel: wave 3 — 24 opus subagents, one per department-level unit, rewrite the PROVISIONAL agendas into real dept-specific ambitious ones + one MANIFEST-numbered HTML canvas each (`GENERATION_BRIEF.md` §8, seeds for all 24). Pricing model stays deferred, brand visuals stay held (re-confirmed); no standing session-handoff doc — STATE + register + memory + `04-specs/HANDOFF-*` are the handoff. Plan first: no wave file ships under this ADR's commit | 2026-08-28 |
| [0040](0040-error-tracker-receives-no-identity.md) | **The error tracker receives no identity** (Proposed). The wave-3 finding named one runtime; verification found **three** — `apps/web`, `apps/api-gateway` and `services/agent-orchestrator` each forwarded `email` and `username` onto the Sentry user scope. Both existing `beforeSend` hooks were decorative (the web one was a literal `// Placeholder`), and the init that actually runs in production, `main.py`, had **none**. `sendDefaultPii` would not have helped and reads as though it would: Sentry's own docs say it covers SDK-attached data but *not* anything set via `setUser()`. Fix is two controls answering two failure modes — narrowed types so a regression fails to compile, plus a real scrubber for the paths types do not own — held by a blocking guard proven against the pre-fix tree (**15 findings, 3 runtimes**). Also: the interceptor sent `request.query` whole (now key names only), and `Privacy.tsx` documented five data flows while omitting error tracking entirely, which under ADR 0020 is a surface asserting something untrue about itself. Three items held for the founder: whether the UUID goes at all, purging the already-sent archive, and a DPA | 2026-08-28 |
| [0041](0041-makeover-canvas-and-conflicted-palette.md) | **The 46-screen makeover canvas, and a palette answer that needed re-asking** (Proposed — conflicted, not locked). All 46 routes captured live + 103 artboards + 3-direction bake-off; the session's `#CD2D5B` answer was given on a stale premise and is superseded by 0042. Canvas stands as structural work; founder verdicts in [MAKEOVER-VERDICTS](../06-pages/MAKEOVER-VERDICTS.md) | 2026-08-29 |
| [0053](0053-analytics-cost-unknown-not-invented.md) | **An unmeasured bottle cost is unknown, not 0.6 × the menu price** (Proposed). Both analytics loaders resolved unit cost as `lot?.has_invoice_cost && lot?.wac ? lot.wac : Number(i.last_purchase_price) || (unitPrice ? unitPrice * 0.6 : 0)` — and `0.6` appeared in no ADR, comment or doc, while `last_purchase_price` has **no write site in the repo** and is NULL on all 72 production rows against `inventory_lots`' 2. So the measured branch valued ~2 rows and the invented one ~70, after which `getFinancialSummary` labelled the result `"on-hand qty × WAC (lot rollup)"` and `getMenuEngineering` `"unit_price − WAC (lot rollup)"` — [0051](0051-rebuilt-pages-show-live-data-only.md)'s worst case, a confident wrong label. Cost now resolves in one place (`analytics/inventory-cost.ts`) and returns **null** when nothing recorded one; every derived field propagates the null per-field rather than through arithmetic (inventory value, GMROI, turnover, DIO, prime cost, dead-stock capital, EOQ, ABC — wholesale, since its cuts are share-of-a-total — margin, margin %, quadrant). Both lying `basis` strings are rebuilt from the coverage they actually cover; `getInventoryScience` and `getWine360`, which had none, now carry one. An invoiced WAC of `0` is kept as measured (that is how samples are recorded). Guard `check_analytics_cost_honesty.py` blocks in CI, proven exit 1 against the pre-fix tree. **Does not resolve OD-100** — which price values inventory stays the founder's | 2026-09-01 |
| [0054](0054-order-capture-and-unit-arithmetic.md) | **An order writes down what was ordered** (Proposed). Verified against production 2026-09-01 — 2 orders, 1 order line, 0 documents, **0 `price_history` rows** — so every row these tables will ever hold is written by the code this fixes. Four defects: **nothing wrote `procurement_order_items`**, so `matchDocumentLines` returned early forever and the whole invoice-matching engine was unreachable; `bottles_total = dto.quantity` ignored `unit_type` (five CASES booked five bottles) while the door's `normalizeUom(x) ?? "case"` fell back to the one unit that MULTIPLIES (24 against a 12-pack booked 288) — and the door back-derives pack size from the header, so each fed the other; manual/Ask-AI/recurring orders were byte-identical rows; and `price_history` was correctly keyed, correctly indexed and had **zero writers repo-wide**. Applies [0011](0011-pos-sale-volume-contract.md)'s fail-closed rule to ordering, with one stated refinement — an **absent** unit resolves to `bottle` because the identity cannot multiply, while an **unreadable** one is refused. `source` is NULL rather than `'manual'` when unstated: defaulting would label an agent order a human decision ([0020](0020-no-fabricated-answers.md)). FKs added to the line table before it has rows ([0015](0015-pos-referential-integrity.md)). Held by `check_order_capture_contract.py`, proven to exit 1 against pristine `origin/main` at 5 sites | 2026-09-01 |
| [0056](0056-order-paths-write-columns-that-exist.md) | **An order path may only write columns the table has** (Proposed). Two order paths wrote to tables whose shape they were wrong about, and neither was visibly failing: PostgREST answers `42703`/`PGRST204`, both callers log a warning, and CI is green. **Retroactive** (`POST /providers/:id/retroactive-order`) wrote `wine_name` and `actual_delivery` — `procurement_orders` has neither — and omitted five NOT NULL columns, so it failed at the first statement and nobody found that its two follow-on inserts were broken too (`procurement_conversations.message_text` NOT NULL and unwritten; `order_interactions` has no `channel`/`content`, a CHANNEL-only `interaction_type` CHECK, and a NOT NULL `interaction_direction`). A byte-divergent **second copy with zero callers** sat in `provider-intelligence.service.ts`. **Recurring** declared eight columns `recurring_orders` did not have and omitted `unit_type` (NOT NULL), so the table holds **0 rows** — the symptom, not a coincidence — and the 8 AM cron handed `createOrder` `undefined` for two `uuid NOT NULL` columns. Two more drifts fell out: `daily` (offered by the CHECK and the web form) hit `calculateNextOrderDate`'s `default:` arm and returned **+1 month**, and that function mixed a UTC-parsed date with LOCAL setters, so monthly schedules drifted a day west of Greenwich. Fixes: duplicate deleted; retroactive routed through `createOrder` with a new `provenance.alreadyFulfilled` (opens DELIVERED, dates from the invoice, no dedup merge, **no AI draft** — it would negotiate the price of wine already in the cellar); `invoiceTotal` replaces `finalConfirmedCost`, which was documented as a total and written to a per-bottle column (a $600 case invoice became $7,200); `order_interactions` dropped rather than repaired; `recurring_orders` reshaped — six columns added, `wine_name` **derived** from inventory instead of stored, `last_executed_at` re-pointed at the existing `last_order_date`. `wine_id`/`preferred_providers` **tombstoned, not dropped**: `RecurringOrders.tsx:133` calls `.join()` on one unguarded and would throw the moment a real row existed ([0032](0032-vault-structure.md)'s rule, in the schema). New 5th contract in `check_order_capture_contract.py` — no write may name a column no migration declares — proven to **exit 1 against pristine `origin/main` (f5735e47) at 19 findings**, with a ceilinged 13-of-254 blind spot and a 6-entry prod-verified debt list. **Surfaced and not fixed:** `calendar_events` has no `priority`/`tags` and a NOT NULL `source`, so every calendar event these paths write fails — and this change makes those writes reachable for the first time | 2026-09-01 |
| [0057](0057-receiving-write-path-integrity.md) | **Prove every receiving write before it reaches the ledger** (Proposed). Three defects in the receiving write path, each producing a successful-looking outcome while the database and the ledger disagreed. **D1** `verifyReceipt` wrote `notes`, which `procurement_orders` has never had (production 2026-09-01: 56 columns) — and `?? undefined` meant the key only reached PostgREST when a manager typed one, i.e. **only when documenting a discrepancy**, after the ledger correction and the vendor credit claim were already written; PGRST204 then rejected the whole update, so `status`, `match_status`, `accepted_quantity` and `invoice_*` never landed and the retry failed identically. Now appended to `delivery_notes` — appended, because a door note and a verification note are two observations and clobbering loses one silently. **D2** `adjustments[]` had `@Type()` with **no `@ValidateNested`**, so the nested DTO was never validated at all, and `apply_stock_movement` derives `restaurant_id` from the target row — a foreign UUID moved another tenant's stock. Closed by an ownership check inside `applyReceiptAdjustment` (403 naming the item), not by decorators, which only reject non-UUID shapes; a failed *lookup* is 422, never permission ([0051](0051-rebuilt-pages-show-live-data-only.md)). **D3** `markDelivered` booked `order.quantity` into the ledger while writing `quantity_received: null`, and the door's `alreadyBooked` reads that column — NULL as 0, so the door booked the delivery **twice**; plus `?quantityReceived=abc` became NaN, which `??` does not fall through, marking the order DELIVERED with no stock booked and a 200 OK. Held by `check_orders_column_writes.py`, which derives the column set from `supabase/migrations/` (nothing hardcoded) and follows `const`-built payloads — proven exit 1 pre-fix, exit 0 post-fix. First tests of `verifyReceipt` in the repo (15; 12 fail pre-fix). **Leaves open:** `location_id` (`procurement.service.ts:940`), a second live member of D1's class, filed as shrink-only debt because the fix is a migration *or* a deletion — a decision, not a default | 2026-09-01 |
| [0058](0058-order-status-is-an-enum-not-a-string.md) | **Order status is an enum, not a string** (Proposed). `procurement_orders.status` is written from `ProcurementOrderStatus` (12 UPPERCASE members) and production holds `APPROVED` 1 / `PENDING` 1 — measured 2026-09-01, **zero lowercase rows ever**. Nine read sites compared it to the lowercase `"delivered"`, so every vendor scorecard, lead-time mean/median/p90/stdev, on-time rate, HHI, spend total, spend-by-month, bottles-delivered figure and the `purchase_spend` goal series returned a **structural zero rendered as a measurement** — `absence-reported-as-health` wearing the costume of a number. The sweep found the class is wider than the nine: `dashboard.service.ts:141,145,548,704` used `awaiting_approval`/`ordered`, which were never members under **any** casing (so a case fix alone would not have saved them); `scheduled-tasks.service.ts:474,546` used the nonexistent `SHIPPED`/`INVOICED`; and `ask-ai.service.ts:187,835` **failed OPEN** — nothing matched, so `NOT IN` matched *everything* and closed orders reached the gate that decides whether to draft a vendor reply. Four fixtures locked the wrong case and stayed green because both spec harnesses stub `.eq()` as a **passthrough**, so the fixture matched the reader instead of the writer — the defect reproduced inside the test harness. Fixed by one shared vocabulary (`procurement/order-status.ts`) whose sets differ **only** on `PARTIALLY_RECEIVED`: `ORDER_ARRIVED_STATUSES` includes it (the truck came; excluding it drops a vendor's worst deliveries from its own punctuality score), `ORDER_SPEND_STATUSES` excludes it (its money columns still describe the PO, not the short delivery). Rejected: case-insensitive comparison (legitimises spelling, and blind to the four non-member values) and a DB `CHECK` constraint (right for **writers**, blind here — it does not fire on a `SELECT`, and a mis-cased `WHERE` is a *valid* query matching zero rows). Held by `check_order_status_literals.py`, which parses the enum rather than hardcoding it and is **table-scoped** because the naive rule fires on ~80 legitimate sites across a dozen other tables plus `Promise.allSettled`; proven exit 1 at 21 violations against pristine `origin/main`, exit 0 after. **Three items await the founder**: `PARTIALLY_RECEIVED` out of spend, the `RECURRING` status that matches nothing (recurring-order reminders have never sent; tracked in `KNOWN_BROKEN`, not guessed at), and whether to add the writer-side `CHECK` | 2026-09-01 |
| [0059](0059-receiving-preserves-the-pair.md) | **A machine proposal shown to a human is written before the human answers, and the answer is appended, never substituted** (Proposed). Receiving is the only place in the product where a number is produced by a person touching an object — six machine-proposes/human-judges pairs exist there and **four destroyed the machine's half at the instant it became a label**. The cleanest: confirming a suggested line match wrote `match_confidence: 1, match_method: "manual"` over the model's own score (`documents.controller.ts:244-245`) — the comment above it was *correct* about live state, which is exactly why review never caught it. Also: suggested matches were **never persisted at all**, so the whole negative class of an entity-resolution dataset vanished on the HTTP response; the door's paper pre-fill never left the browser; the verify form's pre-fill overwrite was untracked; `extraction_model` had a column and no writer; and `procurement_documents` had no `event_id`, so no extraction could ever be attributed to a model. Verified against production 2026-09-01 — **0 documents, 0 document lines, 0 receipt events, 0 credits** — so nothing needed migrating and every pair these tables will ever hold is written by the fixed code. Rejected: reconstructing labels from `extracted` jsonb (**impossible after a second edit** — the first edit's output becomes the second's baseline), logging proposals instead of storing them (logs roll, and the join arrives days later), and waiting for volume (waiting does not accumulate labels, it accumulates *destroyed* labels). Rejection never erases the proposal; a losing candidate is `superseded`, never `rejected`; `event_id` is SET NULL rather than CASCADE so [0037](0037-nfb-erasure-is-crypto-shredding.md) erasure costs attribution, never the label. Held by `check_proposal_preservation.py`, proven exit 1 against pristine `origin/main` at the 2 pre-fix sites. **L3 (door) and L4 (verify) are staged, not finished** — client + DTO + columns land here, the two service inserts are marked TODOs with failing-by-design skipped tests, because those files were owned by concurrent sessions | 2026-09-01 |
| [0060](0060-a-window-is-a-floor-and-an-unknown-is-not-a-zero.md) | **A window is a floor, an unknown is not a zero, and a refusal is not an outage** (Proposed). The rebuilt `/receiving` measured against [0051](0051-rebuilt-pages-show-live-data-only.md) the day after 0051 locked: ten defects, three shapes. `useReceivingNextData.ts:111` rendered `procurement_orders.quantity` as bottles though it is denominated in `unit_type`, so a five-**case** order told the door five bottles were expected — [0054](0054-order-capture-and-unit-arithmetic.md)'s arithmetic reintroduced in the view model, with the correct `bottlesTotal` sitting unused in the payload. **Not one `≥` on the page** against six server windows (`.limit(100)`, `.limit(500)`, an unordered `.limit(5000)`, and a 25-row page printed as a total while `listOrders`' exact `total`/`hasMore` were destructured away). `dollarsAtRisk > 0 ? money : EM` printed a **measured** `$0` as an unknown beside a literal `0`, and `unverified: known ? … : []` made a failed fetch read as "nothing uncounted" — the shrinkage safety net going silent exactly when needed. 403 was nowhere a state. An offline non-attempt (`doorOutbox.ts:94` returns `{0,0}` untried) was stamped as `last sync … · sent 0 · failed 0` under a header reading "offline — holding". The outbox pinned drops under one global `localStorage` key, so restaurant A's receipt rendered as a `role="alert"` to restaurant B. Sub-decisions recorded: pre-scoping pins are **adopted and marked**, not discarded (that is the inv-09 defect) nor re-attributed (that is the leak); `openClaims` is a floor **unconditionally**, since its cap is per-restaurant and unobservable from the client. Guard `check_windowed_figures.py` blocks in CI — it states in its own header that it does **not** trace a `.limit()` to a JSX node (undecidable across two languages and an HTTP hop) and holds five checkable claims instead; proven exit 1 against the pre-fix renderings, exit 0 after. Gateway `providerName` and the untenanted door queue are marked TODO, not fixed — both files owned by unmerged branches | 2026-09-01 |
| [0061](0061-recurring-reminder-reads-the-recurrence-table.md) | **The recurring-order reminder reads the recurrence table, behind an off-by-default flag** (Proposed). `sendRecurringOrderReminders()` filtered `procurement_orders` on `status = 'RECURRING'`, which is not a member of `ProcurementOrderStatus` under any casing, so the daily 08:00 cron has **never sent one email**. [0058](0058-order-status-is-an-enum-not-a-string.md) left it on `KNOWN_BROKEN` calling the replacement a product question; the schema answers it, and the answer is **not a status**. The query's three other fields were already `recurring_orders` fields: `next_order_date` exists on no other table, `recurrence_frequency` is an ENUM *type* whose column is plainly `frequency`, and `target_price_per_bottle` **exists in no table in the schema**. The job was pointed at the wrong table; the dead status was the symptom. Now reads `recurring_orders` (`active`, due within 2 days), gated on `RECURRING_ORDER_REMINDERS_ENABLED` — an allow-list (`true`/`1` only) checked *before* any query or recipient resolution. Rows are described **fail-closed**: no `|| "Unknown Provider"`, no `|| 0`, because the template puts both in the SUBJECT LINE ([0020](0020-no-fabricated-answers.md), [0053](0053-analytics-cost-unknown-not-invented.md)). On `main`'s schema every row is refused, so zero emails send even if armed — the flip-precondition is enforced by code, not a comment. Recipients stay tenant-scoped via `allowDefaultFallback` ([0022](0022-scheduled-jobs-run-per-tenant.md) / OD-87). **Flag stays off; the founder flips it**, and four things must be true first (see ADR) — chiefly PR #220 merged. Makes [0058](0058-order-status-is-an-enum-not-a-string.md)'s `KNOWN_BROKEN` entry stale; measured exit codes and the byte-identical residual violation set are in the ADR | 2026-09-02 |
| [0062](0062-a-quantity-declares-its-unit.md) | **A quantity crossing the wire declares its unit, and the door accumulates** (Proposed). Five door defects, two roots. **A REFUSED DELIVERY BOOKED STOCK**: the door sends both quantities in BOXES (`DoorNext.tsx:285-288`) and the server converted only one — `countedBottles - rejectedQty` subtracted boxes from bottles, so three refused boxes at pack 12 booked **33 bottles of live stock** for wine turned away at the door, and one broken box out of fourteen booked 167 instead of 156. It survived because the only test on that path used `countedUom: "bottle"`, where the conversion is the identity — **the test did not fail because it could not fail**, the same shape as [0054](0054-order-capture-and-unit-arithmetic.md). A failed `apply_stock_movement` warned and fell through, then wrote `quantity_received`/`status`/`delivered_at` and returned `stockDelta` as though the bottles were on the shelf; a null `inventory_id` booked nothing and reported a delta too. `quantity_received` was set ABSOLUTELY, so truck two's six boxes after truck one's eight recorded six and the match line called it short against the whole PO while the driver waited. And `notes` — `@MaxLength(500)`, with `doorOutbox.ts:64-65` treating a 4xx as PERMANENT — carried a 344-character skeleton that a real distributor plus a real Bordeaux pushed to **546**, so the receiver could not save the delivery at all. **Founder-decided:** the door ACCUMULATES (running total summed from `procurement_receipt_events`, one ledger movement per event keyed `door-receipt:{eventId}`, never from the mutable column) and its facts become TYPED COLUMNS (`20260901220000_door_facts_are_columns.sql`: `outcome`, `refusal_reason`, `signed_by_initials`, `driver_name`, `expected_qty_bottles`, `rejected_qty_bottles`, CHECKs copied verbatim from the client enums). `rejectedQty` becomes `rejectedQtyInCountedUom` — the unit in the NAME, because a sibling `rejectedUom` can disagree and a branded type is erased at the wire — with the old name still READ so a receipt queued by the pre-fix client still books its refusal. A failed RPC now throws 503 (the outbox retries, and it converges) while a missing shelf returns 200 with `stockBooked: false` and a sentence, because no retry links an order to a shelf; `stockDelta` is **null**, never 0 ([0016](0016-ledgers-must-express-unknown.md)). Notes after: skeleton 259, worst case 449. The offline photo is NOT queued and the screen no longer says it was — `offline-storage.ts:189-210` serialises every pending mutation into one key and swallows a quota failure, so a photo would take the RECEIPT with it. Guard `check_quantity_units.py` blocks in CI, proven exit 1 against the pre-fix declarations; it lists **six more live instances** on `dto/procurement.dto.ts` it cannot fix without touching `procurement.service.ts` | 2026-09-01 |
| [0063](0063-a-certification-screen-shows-the-thing-being-certified.md) | **A certification screen shows the thing being certified** (Proposed). The rebuilt `/receipts` could not show the invoice it asks a human to swear to. `ReceiptsNext.tsx:273` gated the "Open the paper" link on `doc.imageUrl` where `doc` is a **list row**, and the gateway signs `imageUrl` only inside the *detail* handler (`documents.controller.ts:189-203`); the page fetched the detail and read only `.lines`, discarding `.document` entirely — so the gate never fired, and the swipe-up ceremony asked for a comparison against a page it never displayed. The **legacy** page it replaces rendered it inline (`ReceiptsPage.tsx:337-340`). Fixed by rendering the scan **beside** the lines (images and PDFs), with the 3600s signed link treated as spent five minutes early and each not-shown state naming which one it is — no stored file / no signable link / aged out / did not load. Five more, all [0051](0051-rebuilt-pages-show-live-data-only.md) shapes: all three list query keys were bare, so a restaurant switch served the **previous tenant's** documents (the leak PR #212 fixed on `/receiving` and missed here) — now keyed, and an unresolved restaurant is refused rather than folded into one shared `''` bucket; `limit: 100` printed as a total beside a `verifiedCapped` that already got it right; a **failed** detail fetch rendered "No lines were extracted"; `isError` was the queue query alone, so a dead uncounted-deliveries endpoint and a caught-up door were one screen; and `extraction_confidence` had **zero render sites repo-wide** while the screen asked for trust in the reading. The file's "never auto-written" docblock was **false** — `POST :id/match` writes every unambiguous vendor-SKU pairing before it answers (`line-matcher.ts:282-296`) — so the applied ones are now named, scored and **unlinkable**, and the `Paired` column names its target instead of printing a referent-free `paired`/`—`. Guard: `check_windowed_figures.py` was **extended** to two pages rather than duplicated (CLAUDE.md §4) and gained **W6, tenant-keyed query keys**; extending it exposed a **vacuity bug in the existing guard** — its `useQuery(` matcher never saw `useQuery<T>({`, so W5 and W6 would have passed on a generic-annotated hook. Proven exit 1 against the pre-fix keys, exit 2 against the pre-fix tree (register absent), exit 0 after. W6 is **not** enforced on `/receiving` (3 bare keys, file owned by an unmerged branch) and the guard prints that exclusion on every clean run. No concurrency precondition: the table has no `updated_at` to build one from, so the collision is made audible instead — stated, not disguised | 2026-09-02 |
| [0064](0064-a-fitted-value-is-a-prediction-not-a-memory.md) | **A fitted value is a prediction, not a memory** (Proposed). `analytics.service.ts` scored forecast accuracy against `fitted` under the comment *"Backtest accuracy on the fitted series"*. It was not a backtest: in `holtWintersAdditive` the push happened **after** level, trend and the seasonal factor had each absorbed `series[i]`, so `fitted[i]` was a function of the value it was about to be compared against. Measured as ∂fitted[k]/∂series[k] on a 56-point weekly series: **0.525** for Holt-Winters (closed form α+βα+γ(1−α)) and **0.04** for `holtLinear` (α·β) — the sibling the opening brief called correct, because it read pre-update *level* but **post-update trend**; `v3.0-TECH-DEBT.md:928` had this right and the brief did not. Only SES was ever clean. The brief's second named consumer, `insight-generator.service.ts:488`, was **never leaked** — it trains on `values.slice(0, -7)` and scores `hw.forecast` against the held-out week, a genuine holdout whose *comment* said "fitted" while its arithmetic did not. So there was exactly one leaked consumer, and it was the one labelled "backtest". Fixed by contracting `fitted[i]` on **every** model to the one-step-ahead prediction from `series[0..i-1]` — pushed before the state update — rejecting a parallel `oneStepAhead[]` array because it preserves the leaky series under a correct name and `fitted` has **zero** consumers outside the accuracy block being fixed (it never leaves the API response; no hits in `apps/web`, `apps/mobile`, `services/`, `packages/`). Every model now returns its own `warmup` (SES 1, Holt 2, HW 2·period) and accuracy scores past it, because seeding reads the first two seasons and those fitted values stay in-sample however the recursion is written — verified post-fix: ∂fitted[3]/∂series[3] ≠ 0 while k ∈ {14,21,33,55} are 0. **An adversarial pass then found two more defects, both closed here:** MASE's numerator and denominator spanned different windows (numerator on `[warmup, n)`, denominator on the full series — now bounded by a `from` argument; the error runs BOTH ways, flattering an opening-blitz head by a mean 4.5% and punishing a leading-zero POS-rollout head by 6.6%, flipping the beats-naive verdict on 22/400 and 143/400 simulated series respectively), and the all-zero no-data series reported **MAE 0 / RMSE 0 over 106 scored points** — a perfect forecast over a series holding no observation, where this ADR's own `scoredPoints` and `basis` fields had made an empty claim read as a well-grounded one; all four metrics now return null with `basis: "no_observations_in_scored_window"` per [0051](0051-rebuilt-pages-show-live-data-only.md). **The numbers this moves**, on a simulated 120-day series of the shape `toDailySeries` produces: MAPE **6.95% → 15.31%**, MASE **0.402 → 0.889**, scored points 120 → 106 — the forecast still beats seasonal-naive, by 11% rather than 2.5×. Nothing shipped needs correcting (no accuracy figure was ever rendered), but any such number quoted before 2026-09-02 is wrong by roughly 2×. Also fixes an unrelated-looking bug riding along: the old loop `continue`d past the update at i=0, so Holt-Winters never learned from `series[0]`. Guarded by 4 new cases in `forecasting.spec.ts`, 2 proven failing against the pre-fix tree, asserting all three models as one object so a regression names every one that broke | 2026-09-02 |
| [0065](0065-a-conversation-log-names-real-columns-and-refuses-a-missing-body.md) | **A conversation log names real columns, and refuses a message with no body** (Proposed). `communications.service.ts` `storeOutboundConversation` inserted `sender_email`, `recipient_email`, `subject` and `message_body` into `procurement_conversations`. **The table has none of the four** — measured 2026-09-02 against production (`information_schema.columns`, 36 columns): the body column is `message_text text NOT NULL` and the metadata column is `email_headers jsonb`. Every call answered 42703/PGRST204, and would have answered 23502 on the unwritten NOT NULL even with the right names. A **second** NOT NULL hid behind the first: the payload wrote `provider_id: providerId || null` against a `uuid NOT NULL`, and the sole call site passes `provider?.id`, which is `null` for any vendor address that is not already a provider — the ordinary first-contact case. Production holds **27 rows** (17 outbound / 10 inbound, all email) and **none came from this method**: class **O** in [[absence-reported-as-health]], nothing corrupted and nothing repairable. The silence had three layers, each reporting health — `logger.warn` in the service, a `steps[]` entry nobody read in the caller, and the handler logging **"Messaging scenario completed successfully"** and returning `scenario_executed` unconditionally on an endpoint whose only DB write has never once succeeded. Fixed by mapping onto the columns that exist using the shape the **live inbound path already writes** (`rabbitmq-bridge.service.ts:756` `handleInboundEmail`: lowercase RFC-822 header names; production's 13 header-bearing rows use exactly `{subject, in_reply_to, references, message_id, from, gmail_thread_id}`), so these rows now thread with the four readers that key on `email_headers.subject`. **NOT NULL is refused, never defaulted** — a placeholder body is rendered verbatim in `/communications` and summarised into the weekly manager email, indistinguishable from a real short message ([0020](0020-no-fabricated-answers.md), [0051](0051-rebuilt-pages-show-live-data-only.md)); the method returns `stored:false` with the field named and attempts no insert. Loud: error-level logs carrying the table, the PostgREST code and the attempted key set, an explicit `stored` boolean so a null `data` cannot read as uninteresting, and a `scenario_executed_with_failures` status derived from the steps. Quiet by design: it still never throws — the email has already been sent, and failing the caller would be a *different* false statement. **No migration**; the columns exist and the code named the wrong ones. [0056](0056-order-paths-write-columns-that-exist.md)'s Contract E was proven to already cover this table **without** agent A's in-flight generalisation of `check_orders_column_writes.py` — deleting the four `KNOWN_BAD_COLUMNS` entries against the *pre-fix* tree exits 1 with four findings — so the debt entries and the self-test's `logConversation()` fixture arm are deleted per the shrink-only rule. **That fixture is where the name `logConversation` came from; no such method has ever existed in the tree.** 14 new tests whose oracle is `supabase/migrations/` rather than a hand-written column list; 13 fail against pristine `origin/main` and the 14th asserts the oracle is non-empty, so it must pass on both. **Surfaced, not fixed:** `procurement.service.ts:2163` reads `(conv as any).subject`, a column that has never existed | 2026-09-02 |
| [0066](0066-order-delivery-event-vocabulary.md) | **An order's calendar event uses the calendar's vocabulary, and its failure is loud** (Proposed). `createCalendarEventForOrder` had **never once succeeded** — the sole writer of the expected-delivery event a manager sees on `/calendar` and the row `dashboard.service.ts:288` counts as "deliveries this week". Four faults, in ascending order of how badly they fail: `priority` and `tags` are **not columns** (`PGRST204`); `source` was **omitted** and is `varchar(50) NOT NULL` with no default (`23502` on its own, so fixing the names alone would not have helped); and `status: "SCHEDULED"` — the worst, because it is **not an error**. The column carries no CHECK, so that row would have been *written* and then read by nothing: production holds `active`, `completed`, `pending`, all lowercase, 19 rows measured 2026-09-02, and has never held `SCHEDULED` under any casing. The whole body was a `try`/`catch` logging `warn`, and Supabase **returns** `{data,error}` rather than throwing, so the catch was inert for exactly the errors occurring — `absence-reported-as-health` class **O**, damage that cannot be enumerated. [0056](0056-order-paths-write-columns-that-exist.md) measured this and explicitly left it; this closes the procurement half. The vocabulary is chosen as the **intersection** of the `CalendarEvent*` enums (`calendar.dto.ts:36-67`) and what production actually holds, because the two disagree in both directions — `active` is live but not in the enum (so a row written `active` could never be transitioned back through the calendar's own endpoint), and `approved` is in the enum with a dedicated partial index but has **zero** rows. Result: `source: system_generated` (one of only two live values, and true — not a person, not an AI inference; `order` rejected as an enum member nothing has ever written), `status: pending` (the column's own default, in the enum, live, and mapped to iCal `TENTATIVE` — a +7-day estimate is exactly tentative), `event_type: delivery`, identity in the real `order_id`/`provider_id` uuid columns that carry FKs and an index and had been ignored in favour of a JSON blob in a column that does not exist. Enums are **imported, not restated**, so a divergence is a compile error rather than a row nothing can read. On the silence: **not fatal** — the one caller runs only after the PO has been emailed to the vendor, and the surrounding code parks a `SEND_UNCONFIRMED` precisely to avoid a manager re-approving an order the vendor already has — but **not silent**: `logger.error` with structured context, and `.insert(...).select("id").single()` so the success branch is unreachable without a row, with "no error **and** no row" a distinct reported failure rather than the success path. **No migration**, so this merges in any order against the concurrent `recurring-orders.service.ts` work. Held by `check_order_capture_contract.py` contract 5 (already on `main`, PR #220): exit 0 post-fix, and **proven to fire** on this exact file and table via a probe column (exit 1). `check_orders_column_writes.py` exits 0 but is **vacuous here** — it is scoped to `procurement_orders` and cannot see a `calendar_events` write. New spec derives the column list from `supabase/migrations/` and throws rather than pass vacuously: **11 of 13 failed pre-fix, 13 passed post-fix**. **🔴 Surfaced and NOT fixed (outside this change's assigned boundary):** `updateCalendarEventForDelivery` and `cancelCalendarEventForOrder` still find the event by `.select("id, tags")` and write uppercase `COMPLETED`/`CANCELLED`. That cost nothing while no event existed; from this change onward delivery events are created and **never closed**, accumulating as `pending` rows for orders that have long since arrived. **Closed by [0073](0073-a-delivery-event-is-closed-by-its-order-id.md).** | 2026-09-02 |
| [0067](0067-a-failed-read-is-never-an-empty-one.md) | **A failed read is never reported as an empty one** (Proposed). `supabase-js` **resolves** with `{ data, error }` and never throws, so `const { data } = await client.from(t).select()` yields `[]` for both a failed query and an empty table, `maybeSingle()` yields `null` for both "no row" and "query failed", and any wrapping `try/catch` is **inert** — which is what stops a reviewer reading further. Four auditors triaged **~29**; the mechanical sweep of `apps/`/`services/`/`packages/` at 1f4717cc measured **215 across 47 files** (995 scanned), plus 37 more that refuse on falsy. Several read as **good news**: the 409 dedup guard **failed open** and inserted the duplicate; `vendor-catalogue.service.ts:75-95` re-ran a silently *different* query with `country`, `listing_tier`, the text match and `type` all dropped and returned it as the search — **wrong rows, not empty ones** — guarding a missing-column hypothesis that has been false since `20260807001652_vendor_listing_tier.sql`; `percentile([])` returned **0**, so every server in every restaurant beat their team; and `insight-scheduler.service.ts:58` no-opped the **entire hourly sweep for every tenant** while `sweep()`'s catch logged nothing. Fixed 8 sites across 6 services, each returning an explicit unavailable state (`unavailable: true`, `median: null`, 503) rather than a zero — [0051](0051-rebuilt-pages-show-live-data-only.md): a swallowed error turned into a displayed `0` has fixed nothing. Held by `check_read_errors_not_swallowed.py`, a **ratchet**: `read_error_baseline.json` records the 207 pre-existing sites, a site outside it fails the build, and a baseline row the tree no longer contains **also** fails it, so the count can only shrink and cannot rot in prose. Rejected: fix-all-215 (unmergeable in one operation), warn-only (absence-as-health with extra steps), and a `queryOrThrow` wrapper (strictly better, deferred — named as the escalation if the baseline stops shrinking). Exit 2 when it cannot check, including **zero sites matched against a non-empty baseline** (detector rot, not a clean tree). Proven exit 1 against pristine `origin/main` at exactly the 8 fixed sites; `--self-test` 18 invariants; 11 behavioural tests, 8 failing pre-fix. **Deliberately out of scope:** the 37 refuse-on-falsy sites, six files owned by concurrent PRs (incl. `receiving.service.ts:321-346`, still reading `totalAtRisk: 0` forever), and the bound-but-misused error the guard cannot see | 2026-09-02 |
| [0068](0068-calendar-events-recurring-order-link.md) | **A calendar event names its schedule in a column, and says only what the table can hold** (Proposed). `RecurringOrdersService` writes `calendar_events` in three places and **none has ever written a row**: it names `priority` and `tags` (0 of 2 present in production, 2026-09-02), omits `source` (`varchar(50) NOT NULL`, no default), links through a JSON substring read back with `.like("tags",'%uuid%')`, and — a fourth failure nobody had counted — invents the status vocabulary, writing `SCHEDULED`/`COMPLETED` against a table whose 19 real rows hold only `pending` (16), `active` (2) and `completed` (1), with **no CHECK constraint** to object. All three sites sit behind `try{…}catch{warn}`, and Supabase returns `{data,error}` rather than throwing, so the catch was **inert for every DB error it appeared to cover**. Founder-decided: a real `recurring_order_id uuid`, nullable, FK **ON DELETE SET NULL** (not NO ACTION like `order_id`: [0056](0056-order-paths-write-columns-that-exist.md) made `recurring_orders.inventory_id` CASCADE, so NO ACTION would make deleting a wine fail with a 23503, and CASCADE would destroy completed history), partial-indexed. Status is `pending`/`completed` — column default, 16 of 19 rows, and members of `CalendarEventStatus`; `scheduled` is NOT invented. The 06:00 reminder published `wine_name || "Unknown"` into a push+email **title**: measured, **53 of 72 production inventory rows have a NULL `wine_name` and all 53 reach a real one via `master_wine_library.name`**, so "Unknown" was the MAJORITY case ([0051](0051-rebuilt-pages-show-live-data-only.md) / [0020](0020-no-fabricated-answers.md)). Now two-hop, with `schedule <uuid>` — a true, actionable id — as the fallback, diverging from [0061](0061-recurring-reminder-reads-the-recurrence-table.md)'s refuse-to-send only where an identity has a truthful substitute and a provider or price does not. Shares 0061's off-by-default `RECURRING_ORDER_REMINDERS_ENABLED`; **the flag stays off**, and flipping it now arms two overlapping reminder crons, which is a product decision the founder owns. `check_orders_column_writes.py` is generalised to a **table registry** rather than duplicated (CLAUDE.md §4), reads bulk `.push()` inserts, and pins debt to the file it lives in — without that pinning it would have passed the pre-fix tree. Exit 1 pre-fix / 0 post-fix / 2 cannot-check, in the PR body | 2026-09-02 |
| [0068](0068-calendar-events-recurring-order-link.md) | **A calendar event names its schedule in a column, and says only what the table can hold** (Proposed). `RecurringOrdersService` writes `calendar_events` in three places and **none has ever written a row**: it names `priority` and `tags` (0 of 2 present in production, 2026-09-02), omits `source` (`varchar(50) NOT NULL`, no default), links through a JSON substring read back with `.like("tags",'%uuid%')`, and — a fourth failure nobody had counted — invents the status vocabulary, writing `SCHEDULED`/`COMPLETED` against a table whose 19 real rows hold only `pending` (16), `active` (2) and `completed` (1), with **no CHECK constraint** to object. All three sites sit behind `try{…}catch{warn}`, and Supabase returns `{data,error}` rather than throwing, so the catch was **inert for every DB error it appeared to cover**. Founder-decided: a real `recurring_order_id uuid`, nullable, FK **ON DELETE SET NULL** (not NO ACTION like `order_id`: [0056](0056-order-paths-write-columns-that-exist.md) made `recurring_orders.inventory_id` CASCADE, so NO ACTION would make deleting a wine fail with a 23503, and CASCADE would destroy completed history), partial-indexed. Status is `pending`/`completed` — column default, 16 of 19 rows, and members of `CalendarEventStatus`; `scheduled` is NOT invented. The 06:00 reminder published `wine_name || "Unknown"` into a push+email **title**: measured, **53 of 72 production inventory rows have a NULL `wine_name` and all 53 reach a real one via `master_wine_library.name`**, so "Unknown" was the MAJORITY case ([0051](0051-rebuilt-pages-show-live-data-only.md) / [0020](0020-no-fabricated-answers.md)). Now two-hop, with `schedule <uuid>` — a true, actionable id — as the fallback, diverging from [0061](0061-recurring-reminder-reads-the-recurrence-table.md)'s refuse-to-send only where an identity has a truthful substitute and a provider or price does not. Shares 0061's off-by-default `RECURRING_ORDER_REMINDERS_ENABLED`; **the flag stays off**, and flipping it now arms two overlapping reminder crons, which is a product decision the founder owns. `check_orders_column_writes.py` is generalised to a **table registry** rather than duplicated (CLAUDE.md §4), reads bulk `.push()` inserts, and pins debt to the file it lives in — without that pinning it would have passed the pre-fix tree. Exit 1 pre-fix / 0 post-fix / 2 cannot-check, in the PR body | 2026-09-02 |
| [0070](0070-a-quantity-states-its-own-unit.md) | **A quantity states its own unit, and stays an integer** (Locked). OD-113 asked one question — widen the ledger to `numeric(12,3)`? — and was wrong on its framing twice. **Not expensive:** production holds **72 / 2 / 4 rows** and `procurement_document_lines` is **empty**, so every risk argument resting on table size is void. **Not one question:** there are three axes — identity, magnitude and **dimension** — and OD-113 collapsed dimension into magnitude. `inventory_lots` and `inventory_transactions` carry **no unit column at all** (the unit is supplied by a `master_wine_id` FK meaning "bottles"), and **neither** unit vocabulary in the system holds a mass unit — intake's CHECK is `{bottle, case, keg, pack, split_case, each, liter}` — so "4.5 kg of flour is unrepresentable" is true at **intake** too and the entry's *"intake is fine"* is wrong at the vocabulary level, not merely at `@IsInt()`. Decided by a five-lens audit (premortem · scalability · longevity · operator reality · blast radius), each auditor ranking through one lens blind to the others, after an independent adversary killed the options document's own conclusion. **F wins 5–0** and was in nobody's original list: keep `qty` **integer**, add a CHECK-constrained `uom NOT NULL` with mass/volume base units, and the canonical unit belongs to the **item**, not the row — `uom NOT NULL` requires *a* unit, not a *consistent* one, and the same flour in `g` then `kg` makes `SUM(qty)` add 25 to 25000. Carried by: under integer arithmetic `before + change = after` is **exact**, where under `numeric(12,3)` `valid_quantity_after` **passes over both a create-from-nothing and a destroy** (executed in production — 0.6 g of saffron stores as `0.001`, creating 0.4 g; pouring it back destroys 0.4 g; three 0.333 draws leave a residue lot that never deletes; a real 0.4 g movement stores as `0.000` and is **rejected**) — the flagship integrity check certifying its own corruption, `absence-reported-as-health` inside the ledger. Then the money: a `0.001` residue passes `inventory_lot_rollup`'s `sum(qty) > 0` divide-guard and inflates WAC **~1000×** into COGS and menu pricing, and unlike every other failure found it is caught by **nothing** a restaurant already does — it will be misdiagnosed as theft or waste before anyone suspects arithmetic. Cost: **0 columns, 0 of 9 views, 0 of 93 functions**, against `numeric`'s 8 views, **≥11** signatures (three carrying a live quantity in a `RETURNS` type) and **≥6** silent internal `int` locals no schema diff shows. Also removes the `open_bottle_ml` bolt-on pattern rather than generalising it. **Not free:** one third has no finite decimal at *any* scale, so remainder-safe allocation (333+333+334) is required in the write path regardless. The **longevity lens ranked `numeric` first, was challenged for not applying the rounding evidence it held, and reversed** — both rankings are on the record. **Identity (A vs C) stays 🔴 open and is not blocked by this**; the argument that killed a generic `items` supertype was itself refuted — `master_wine_library.id` is a surrogate uuid whose `identity_key` arrived twelve days later. **Gated:** no migration lands before `fix/schema-parity-sees-what-it-claims`, since parity cannot currently tell `numeric(12,3)` from bare `numeric`. **Leaves broken, commissioned separately:** the receiving door (no mass unit in the intake CHECK; `@IsInt()` rejects `4.5`) and L2 (no `parent_lot_id`, no input→output link, so 10 kg carrot → 7 kg peeled is two unrelated transactions) | 2026-09-02 |
| [0072](0072-schema-parity-sees-what-it-claims.md) | **Schema parity compares what it claims to compare, and exits 2 when it cannot** (Proposed). `check_schema_parity.sh` compared exactly two strings — `table.column:data_type` from `information_schema` and `pg_proc.proname` — so it **could not tell `numeric(12,3)` from bare `numeric`**, which is the entire question the ledger migration is waiting on. Proven, not asserted: against a fixture carrying seven hand-applied differences the unmodified script printed `8 columns, 1 functions` on both sides and `PASS`, exit 0; against two **empty** databases it printed `0 columns, 0 functions` and `PASS`, exit 0 — absence reported as health. Measured on production (PG17, read-only): **179 scaled + 33 bare `numeric` columns** all keyed as `'numeric'`; **1,245 of 3,133** table columns `NOT NULL`, none compared; **183 CHECKs, 62 UNIQUE, 211 PK, 254 FK, 648 indexes, 90 RLS policies, 53 triggers, 83 functions, 28 view bodies, 13 enum/domain types** — none compared; and `information_schema.columns` returns **0 of the 24** materialized-view columns (`event_aggregates_daily`, `event_aggregates_hourly`, `inventory_transaction_summary`) that `pg_attribute` returns. **Two brief corrections:** `inventory_lot_rollup` is a plain VIEW, not a matview (the category is real, the example was not), and the old key had **zero** duplicate collisions — it keyed the right objects and compared the wrong facts. Rewritten on `pg_catalog`: 12 categories keyed on (category, object) with the **definition as the compared value**, so drift prints as `CHANGED / local: … / remote: …` rather than an orphan add beside an orphan delete. Exit 2 is structural — zero relations or zero columns on *either* side, a required category empty on *both*, a psql failure, or a Postgres major-version mismatch. `--self-test` proves 28 invariants end-to-end against real throwaway databases, and was proven **non-vacuous** by a mutant restoring the old semantics, which fails exactly the five mutated invariants. **Surfaced, not taken:** a red parity job has never blocked a merge — `parity` lives in `schema-parity.yml`, `CI Complete` in `ci.yml`, `needs:` cannot cross workflows, and branch protection requires only `CI Complete`. **First real-pair run** (running local stack vs production through the pooler CI uses, read-only) executed cleanly — closing the one shipping risk, that a 12-statement `-c` might not survive Supavisor — and its top CHANGED line is `api_spend.cost_usd`: local `numeric(10,6) NOT NULL DEFAULT 0.0`, remote `numeric(10,6) NULL`. Checked before asserting: production **matches** migration `20260825160000_api_spend_cost_usd_nullable.sql:57-58` (OD-61) and the local stack is the stale side — so it is not drift, but it is the proof from this repo's own history that **an entire migration whose effect is `DROP NOT NULL` + `DROP DEFAULT` was invisible to the old check**. **The first real CI run (PR #239) found real drift and paid for the rewrite twice over.** Four objects were in production and in NO migration — `pos_item_mappings_inventory_id_fkey` (ON DELETE CASCADE), `pos_catalog_match_proposals_candidate_inventory_id_fkey` (ON DELETE SET NULL) and their two partial indexes: the POS-bridge `inventory_id` FK added by hand alongside deleting 92 orphaned mappings, which `20260825140000:8-9` *claims* ADR 0012 and ADR 0014 closed and which no migration ever contained. A foreign key is neither a column name nor a function name, so the old check could not have seen it. Captured in `20260902130000_capture_pos_inventory_fks.sql`, idempotent and **convergent** (`drop constraint if exists` + `add`, so a wrong-delete-behaviour constraint cannot survive the way a `duplicate_object` exception block would let it), rendered byte-identical to production. **The same run exposed a false-positive class** — 45 constraints where a pg_dump round-trip is not a fixed point for `x IN (...)` on a varchar column, so the baseline's own dump text re-parses to a differently-rendered but identical predicate; unfixable by editing migrations. Normalized by **two literal replacements** that touch only cast decoration sitting against another cast or an array bracket — a column reference like `unit_type::text` is untouched — plus rendering index predicates pretty so one rule covers the 46th case hiding in a partial index. Proven safe **in both directions** by six pair invariants (one must collapse, five must survive) and mutation-tested: removing the normalizer brings the 45 back, over-broadening it swallows value-added/removed/renamed. **Then proven green rather than predicted:** all 89 migrations applied to a throwaway database and compared against production — 5,516 facts each side, identical in all 12 categories, exit 0. The run also exposed that a branch behind `main` sees auto-applied migrations as fake drift, which now leads the failure hint. **CI then confirmed it**: run `33624432905`, job *Fresh database equals remote* — **success**, 5,516 facts each side, counts identical to the local reconstruction, all four `schema-parity.yml` jobs green | 2026-09-02 |
| [0073](0073-a-delivery-event-is-closed-by-its-order-id.md) | **A delivery event is closed by its order id, in the calendar's own words, once** (Proposed). The follow-up [0066](0066-order-delivery-event-vocabulary.md) flagged in red. `cancelCalendarEventForOrder` and `updateCalendarEventForDelivery` are the read/update counterparts of the writer 0066 repaired, and carried the **same two faults plus one more**. (1) Each found the event by `.select("id, tags")` and JSON-parsed `tags` for an `order_id` — `calendar_events` has **no `tags` column**, so PostgREST answered `42703` for the whole query, and the destructure took only `data`. Supabase **returns** `{data,error}` rather than throwing, so the wrapping catch was **inert for exactly the error occurring**: `events` came back `undefined`, `(events || [])` was empty, and the function returned having done nothing — indistinguishable from a legitimate no-match. `absence-reported-as-health` class **O**. (2) Both wrote and filtered uppercase `COMPLETED`/`CANCELLED`; the column has no CHECK, so the write would have *succeeded* into a row no reader recognises while the filters matched nothing. The vocabulary is `CalendarEventStatus` (`calendar.dto.ts:36-42`), all lowercase; production holds `active`, `completed`, `pending` (19 rows, 2026-09-02). (3) **The same job written twice, already drifted** — one excluded terminal statuses with `.not(...in...)`, the other with `.neq(...)`, different exclusion sets, no reason recorded for either; neither had ever run, so nothing surfaced it. **Urgency:** these two failing cost nothing while no event existed. From PR #232 onward events are created for real, so without this they accumulate as `pending` deliveries on `/calendar` for orders long since arrived or cancelled — PR #232 alone would have left `/calendar` *worse* than before it. Fix: one shared `closeDeliveryCalendarEvent`, found by **`.eq("order_id", orderId)`** (the real uuid column with an FK to `procurement_orders`, which 0066 now writes) plus `restaurant_id` and `event_type = delivery`, in a **single `update(...).select("id")`** — the filter *is* the match, so it cannot match a row it then fails to write, it closes **every** match rather than the first `.find()` returned, and the returned ids make the success branch unreachable without rows to name. Enums **imported, not restated**, including the `not.in` list. Deduplication is not the point: **the one real difference becomes an argument with a name** (`leaveAlone`). That difference is the **deliberate asymmetry**, which is both functions' pre-fix behaviour preserved — an arrival is a *physical fact* and outranks an earlier *administrative* cancellation, so delivery leaves only `completed` alone and will close a `cancelled` event, while cancel leaves both terminal statuses alone and never erases a recorded delivery. `markDelivered` has no status guard (`:1317-1360`), so cancel-then-deliver is reachable; guarding it there was considered and left to its own ADR. Now pinned by a test instead of resting on two filters that disagreed by accident. Silence: `logger.error` on a returned error, `logger.warn` naming that **nothing was closed** on a zero-row match (legitimately common — an order cancelled pre-approval never had an event, nor did any order approved before PR #232 — but **stated**, because saying nothing is what kept the `42703` invisible), `logger.log` naming the ids on success. **No migration.** Independent of [0068](0068-calendar-events-recurring-order-link.md)/PR #234 — different files — and that change writes `order_id` on the recurring delivery event (`recurring-orders.service.ts:993`), so recurring deliveries are closed by this code **with no further edit** once it merges. `KNOWN_BAD_COLUMNS` deliberately untouched: its `calendar_events` entries still name `recurring-orders.service.ts`, so the shrink-only ratchet stays green. **The guard is not evidence here and says so:** `check_order_capture_contract.py` reads `.insert`/`.update` payload keys and is structurally incapable of seeing a `tags` in a `.select()` — it passed on the pre-fix tree. It proves only that no bad column is introduced (exit 0, and **proven to fire** via a probe column: exit 1 at `procurement.service.ts:1180`). The real gap surfaced — **nothing in CI checks a select list** — is a guard-design question left to its own decision. New spec derives the column list from `supabase/migrations/` and throws rather than pass vacuously: **18 of 20 failed pre-fix** (the 2 passing touch no service code), **20 passed post-fix**; `npx jest src/procurement` 321 passed / 3 skipped / 23 suites; `tsc` exit 0; ADR-number guard exit 0 across **463 refs**. One assertion in the spec was **wrong when written and the test caught it**: `cancelled` has **zero** production rows *because* this defect prevented any, so asserting it against production would have required the bug to still be present — it is carried instead by a second measured list of values `calendar.service.ts` itself writes (`:612`, `:1275`). | 2026-09-02 |
| [0074](0074-a-read-names-columns-that-exist.md) | **A read names columns that exist, and a guard that cannot look says so** (Proposed). [0073](0073-a-delivery-event-is-closed-by-its-order-id.md) surfaced the gap and left it unfiled because guard design is its own decision: **a column named in a `.select()` is exactly as fatal as one in an insert payload** — PostgREST answers `42703` and fails the WHOLE statement — and **nothing in CI had ever looked at one**. `check_order_capture_contract.py` was green at exit 0 for the entire life of that defect; its `WRITE_SITE_RE` (`:559`) matches `.insert|update|upsert` only. It was not wrong, it was **structurally incapable of seeing what it looked like it covered** — `absence-reported-as-health` applied to the checking apparatus itself. Measured before designing anything: **607** readable select sites, **1371** filter arguments (both a moving snapshot; only the findings and the blind spot are ratcheted), and **25 live defects** over **16** distinct `table.column` keys — 17 select columns and 8 filter columns that do not exist. Not small ones: `scheduled-tasks.service.ts` builds a three-clause date window on `procurement_orders.payment_due_date`, and **`payment_due_date` is declared by no table in the schema at all** (the nearest real column anywhere is `payment_terms`) — there is no right table to point it at, and the payment-reminder cron has never sent one reminder; `users.avatar_url` is read at three sites, so every team/member listing 42703s; and `procurement_conversations.message_body`/`.subject` are the **read half of the write defect [0065](0065-a-conversation-log-names-real-columns-and-refuses-a-missing-body.md) fixed** — that ADR repaired the payload and left the reader naming the same phantom columns, because nothing was looking at readers. Ships `scripts/check_read_columns_exist.py` as a **sibling**, not a sixth contract: the existing file is 1300 lines, wholly write-path in name, docstring and fixture, and is the highest-conflict file of the week. It loads `declared_columns`/`ts_sources`/`strip_comments`/`_split_top_level` **by path** so exactly one migration parser and one comment stripper exist in the repo — the repo's own idiom. Conceptually it pairs with [check_queried_tables_exist](../../scripts/check_queried_tables_exist.py): that guard asks whether the **table** a read names exists, this one whether its **columns** do. **Filters are in scope, on measurement not feel:** the deferral argument was embedded-filter false positives, and of 1371 filter arguments **zero** are dotted/embedded and one is a non-identifier — excluding them would have hidden 8 live instances on a theory the codebase contradicts. Skipped deliberately and named in the script: bare `.select()`, `"*"`, and any parenthesised embed (resolving one needs the FK graph, so it is skipped rather than guessed at); aliases, casts and `->` paths are unwrapped. Static forms ARE resolved — `+` concatenation, `${}`-free template literals, same-file `const` — which took the unreadable set **from 26 sites to 2**, and those 24 were the long multi-column selects most likely to hide a bad column. `UNREADABLE_READ_CEILING = 2` may shrink freely; growing it needs that line edited, because every addition is a read the guard stopped looking at. `KNOWN_BAD_READ_COLUMNS` starts at **16** distinct `table.column` entries, each naming the real column where one is obvious, enforced in **both** directions (an entry the schema now declares fails; an entry nothing reads any more fails). Debt, not approval — it exists so the guard is green on arrival and can block the next one. **CANNOT-CHECK exits 2, never 0:** shared parse missing/unimportable/no longer exporting a helper, migration parse below 150 tables, or fewer than 200 selects / 400 filter args matched. **One first-draft bug the self-test caught and the ADR records:** the shared module raises its **own** `CannotCheck` class, so untranslated a blind parse escaped as an uncaught exception and Python exited **1** — which CI reads as "a column is missing" when the truth is "the guard could not look". The exit-2 state was defeated by a class identity; `CannotCheck` is now part of the required-exports contract. Proven to fire on the **real** defect (ADR 0073's `.select("id, tags")` restored in place, never `git stash`): exit 1 naming `procurement.service.ts:1180` — and **not** `:1121`, the same string in that function's doc comment. `--self-test` PASS, 13 assertions including the comment false-positive, cross-statement mis-pairing, both ratchet directions, an empty ratchet and six ways of going blind; exit 2 proven on a real empty directory too. Wired into the `schema-code-parity` job beside its sibling. **CodeQL found a `py/redos` in the guard's own concatenation regex on first push** — `(?:\s*<lit>\s*\+?)+`, optional `+` over empty-matchable `\s*`, measured 0.002s at 12 repetitions and 1.675s at 22. Not reachable from user input, but a guard that can be made to hang is a guard that can fail to check; rewritten to one parse per input (5000 repetitions in 0.0001s) with old and new proven to give identical results on the tree. **The both-directions ratchet paid for itself within a day, on this ADR's own list:** [0061](0061-recurring-reminder-reads-the-recurrence-table.md)/#227 landed `next_order_date`'s fix on `main` after the first measurement, and the next merge failed at exit 1 — *"nothing reads it any more. Delete the entry"* — the exact shape that had just broken the sibling guard's self-test, caught mechanically this time instead of by hand. **Known gap, named:** TypeScript only — `services/agent-orchestrator` Python reads are not covered, and that is the obvious next extension. | 2026-09-02 |
| [0061](0061-recurring-reminder-reads-the-recurrence-table.md) | **The recurring-order reminder reads the recurrence table, behind an off-by-default flag** (Proposed). `sendRecurringOrderReminders()` filtered `procurement_orders` on `status = 'RECURRING'`, which is not a member of `ProcurementOrderStatus` under any casing, so the daily 08:00 cron has **never sent one email**. [0058](0058-order-status-is-an-enum-not-a-string.md) left it on `KNOWN_BROKEN` calling the replacement a product question; the schema answers it, and the answer is **not a status**. The query's three other fields were already `recurring_orders` fields: `next_order_date` exists on no other table, `recurrence_frequency` is an ENUM *type* whose column is plainly `frequency`, and `target_price_per_bottle` **exists in no table in the schema**. The job was pointed at the wrong table; the dead status was the symptom. Now reads `recurring_orders` (`active`, due within 2 days), gated on `RECURRING_ORDER_REMINDERS_ENABLED` — an allow-list (`true`/`1` only) checked *before* any query or recipient resolution. Rows are described **fail-closed**: no `|| "Unknown Provider"`, no `|| 0`, because the template puts both in the SUBJECT LINE ([0020](0020-no-fabricated-answers.md), [0053](0053-analytics-cost-unknown-not-invented.md)). On `main`'s schema every row is refused, so zero emails send even if armed — the flip-precondition is enforced by code, not a comment. Recipients stay tenant-scoped via `allowDefaultFallback` ([0022](0022-scheduled-jobs-run-per-tenant.md) / OD-87). **Flag stays off; the founder flips it**, and four things must be true first (see ADR) — chiefly PR #220 merged. Makes [0058](0058-order-status-is-an-enum-not-a-string.md)'s `KNOWN_BROKEN` entry stale; measured exit codes and the byte-identical residual violation set are in the ADR | 2026-09-02 |
| [0076](0076-a-repoint-names-the-referencing-column.md) | **A catalog-driven repoint names the referencing column, not every column** (Proposed). `merge_library_wines()` discovers the foreign keys it repoints from `pg_constraint` — right, and kept. But both discovery loops unnested `conkey` **without `confkey`**, discarding which column each component references, so a **composite** key emitted one `UPDATE` per *component*. Measured on Postgres 17: a non-uuid component aborts the merge on `42883 operator does not exist: text = uuid`; a uuid component with all parts non-null is rejected `23503`; a uuid component with another part NULL is written **silently**, because MATCH SIMPLE does not enforce a partially null key — and the reported consequence ("a uuid into an unrelated column") is the rarest of the three. The live definition was `20260818020000_merge_undo_honesty.sql:160,188`, **not** the `20260813030000` file the report named; patching that one would have changed nothing in any database. New `fk_repoint_plan()` pairs the two arrays by ordinality and returns a row for **every** FK — a repointable id column or a stated `problem` — so a reference the merge cannot account for **raises** instead of being enumerated, repointed on a column that never matched, and reported as zero rows moved (which reads as *nothing to do*). `fk_repoint_blockers()` counts the rows a composite key would reject **before** the move, ignoring rows with a null component because MATCH SIMPLE does not check those. Only the id component moves: a lot's `uom` is a fact about the lot, not about its parent, so rewriting it would trade a loud abort for silent semantic corruption. Blocking guard `check_fk_repoint_by_referenced_column.py` (exit 1 on a reintroduction, **2** when its anchor is gone), proven against the pre-fix tree | 2026-09-02 |
| [0078](0078-a-count-is-a-record-in-its-own-right.md) | **A count is a record in its own right** (Proposed). `INVENTORY_SOTA_PLAN.md:63` locks **D14** ("count is truth; the perpetual ledger is the audit trail"); the code shipped the inverse and **no ADR recorded the supersession** — that unrecorded contradiction is the deeper defect, and this restores D14. A count wrote a delta and was thereafter indistinguishable from a pour. Worse, **agreement was physically unrecordable**: `set_stock_absolute` returns NULL on a zero delta (`20260805131000:45`) and `inventory_transactions` CHECKs `quantity_change <> 0`, so the ledger could only ever hold counts that **disagreed** and any variance rate over it was **1.0 by construction** — a property of the schema that reads exactly like a measurement ([[absence-reported-as-health]], class O). `reconcileInventory` went further and answered a manager who counted correctly with **HTTP 400**. New `stock_counts` table + `record_stock_count()` RPC: lock → replay gate → read expected under the lock → **INSERT unconditionally, above every delta branch** → move stock only as a *consequence*. `expected_qty` is `SUM(inventory_lots.qty)` read inside the same `FOR UPDATE` the delta is computed from, never the `stock_live` projection (which would let it disagree with the applied delta) and never a TypeScript read (the A11 race). `variance_qty` is generated. `last_counted_at` **survives** as a denormalised `MAX(counted_at)` cache — it is on the hot list path — but stops being evidence and is stamped inside the transaction rather than by a second round trip whose failure only warned. The idempotency key is **shared** with the movement and `NOT NULL UNIQUE`, so one gate covers both; that constraint is *created by* the fix, since before this a retried agreeing count had nothing to duplicate. Attribution moves from the request **body** to `@CurrentUser()` for `recordSpotCount`, and from *nothing at all* to the JWT for `transferStock`/`recordPour`/`updateInventoryItem` (`performed_by_type` was `'system'` on every manual path); `counted_by` → `public.users(user_id)`, never `auth.users`. **Existing rows stay unattributable and are not backfilled.** Blocking guard `check_a_count_is_recorded.py` (exit 1/0/2, static only) — written in Python because `check_no_direct_stock_writes.sh:63` called `rg --type tsx`, exited 2 before reading a file, swallowed it, and printed **PASS having examined zero lines** (verified by planting a real violation); that script is repaired here too | 2026-09-02 |
| [0079](0079-a-price-says-what-kind-of-price-it-is.md) | **A price says what kind of price it is, and a correction corrects** (Proposed). `apply_stock_movement` inferred cost provenance from the mere PRESENCE of a price (`COALESCE(p_cost_provenance, CASE WHEN p_unit_cost IS NOT NULL THEN 'invoice' ELSE 'estimated' END)`, `20260805130000_extend_apply_stock_movement.sql:66-69`, **confirmed live in production 2026-09-02**), so any caller passing a number without saying what KIND of number it was got `'invoice'` free — and `markDelivered` passed the purchase order's OWN AGREED PRICE, stamping lots invoice-verified before anyone had read a document, which `inventory-cost.ts:91-95` then labelled "invoiced lot WAC" to analytics. Eight hundred lines away `receiving.service.ts:215-218` refuses to guess a cost and says why. Second: a correction could not correct — a positive delta INSERTs a lot, a negative one DELETEs lots FIFO, nothing ever restated one, so a verified invoice either landed on a ledger row no valuation reads or created a **second** lot whose cost `inventory_lot_rollup.wac` blended with the estimate forever; and the commonest case of all (right count, wrong price, `ledgerDelta === 0`) was gated out at `:1754` and wrote nothing anywhere. Third, and it blocked both: `applyReceiptAdjustment` passed `p_source: "receiving"` — `inventory_transaction_source` is exactly `(pos, manual, order, mobile_count, reconciliation, system, import, api)`, so **every** receipt verification 422'd; `verify-receipt.spec.ts` stubs the RPC as always-succeeds, which is why it was green. Founder-decided, both halves. The inference is deleted and the RPC RAISEs when a price would create a lot mute (scoped to `p_delta > 0`: only a positive delta makes a lot, and every caller was enumerated first — the signature is byte-identical, so `CREATE OR REPLACE` swaps the body in place and no call site changes). New `revalue_lot` restates the lots a delivery created, targeted by **`source_order_id`** (required — a correction must name the delivery it corrects, and a delivery's lots share one price, so "which lot" has no wrong answer), preserving the prior cost in append-only `inventory_lot_revaluations` per [0059](0059-receiving-preserves-the-pair.md) — `lot_id` **ON DELETE SET NULL** so the record outlives the bottles FIFO deletes, `performed_by` → `public.users(user_id)` never `auth.users`. **No `inventory_transactions` row**: that is a quantity ledger and a revaluation moves money, not bottles. Returns a jsonb receipt whose `lots_matched` lets a caller see a correction that reached nothing. Also fixed, not filed: `inventory_location_breakdown.wac` now excludes sample lots so one endpoint stops returning two disagreeing WACs, and `inventory_lot_rollup` gains `wac_qty`/`invoice_qty`/`unpriced_qty` so `resolveUnitCost` can return `invoice_lot_wac_partial` instead of calling one invoiced bottle in twenty-one "invoiced lot WAC". Production carries **zero** wrongly-promoted rows — and **zero `invoice` lots at all**, from a population of 2 lots and 0 delivered orders, so the bug had not yet fired rather than proving harmless. New blocking guard `check_lot_cost_provenance.py`, exit 1 pre-fix / 0 post-fix / 2 cannot-check, `--self-test` proving it reaches the real tree. That self-test exists because `check_no_direct_stock_writes.sh:63` passed `rg --type tsx` — not a ripgrep type — so rg exited 2, `2>/dev/null || true` swallowed it, and the script printed **PASS having examined zero lines**; repaired here, and its allow-list had rotted to 10-of-10 stale entries while nobody could see it | 2026-09-02 |
| [0080](0080-the-app-does-not-invent-cellar-zones.md) | **The app does not invent cellar zones, and a default is not a measurement** (Proposed). `useStorageLocations` declared four fabricated zones — capacities and temperatures nobody entered — and used them as the query's return value when the server sent none, which made the query *succeed* and let a `useEffect` POST all four into the tenant's `storage_locations` table. Measured in production 2026-09-02: **84 of 87 rows carry those four invented names, across 6 tenants**, 21 rows per name, first written 2026-05-20 and still appearing 2026-07-30 — so it re-fired repeatedly, not once. The numbers were not inert: capacity drove the cellar map's fill bars (with `?? 100` supplying a denominator when even the fixture had none), the names populated the location filter chips, and both fed Auto-Locate's scoring. Most literals found this week render a fiction; this one **wrote** it, so it returned through the API indistinguishable from measured data. Fixed: seeding effect deleted, `DEFAULT_LOCATIONS` removed as empty-response return and as error fallback, and the three collapsed states — no zones / unknown / these zones — made distinct. The 84 existing rows are NOT deleted by this change; that is a separate data operation against a reference-classification query, with the count reported first. Held by `scripts/check_no_seeded_defaults.py` | 2026-09-02 |
| [0081](0081-a-unique-index-decides-what-collides.md) | **A unique index decides what collides; nothing reconstructs one** (Proposed). The other loop in `merge_library_wines()` — the one that deletes loser-side rows colliding with the keeper — read `pg_index.indkey` and **rebuilt the index's equality test by hand** as `IS NOT DISTINCT FROM` terms. An audit of all **seven** UNIQUE indexes the loop can reach found **five** defects, not the two [OD-119] named: expression columns (attnum 0) dropped, `indpred` ignored, INCLUDE columns compared as key columns, invalid indexes treated as constraints, and — the one OD-119 missed and the only one **reachable today** — `IS NOT DISTINCT FROM` making NULL equal NULL where a NULLS DISTINCT index holds them apart. On `sku_mappings` the index permits both rows and the merge kept **1 of 2**. OD-119's premise ("latent, nothing is losing rows today") was therefore wrong, and only a catalog audit rather than the grep behind the entry could see it. Fixed by **deleting the reconstruction outright**: the repoint is attempted and the index's own answer is read (`unique_violation` → drop the loser, naming the constraint), which is correct for every index shape including ones added later. Eight behavioural assertions run in `schema-parity.yml` against the freshly reset stack, each defect proven independently pre-fix; the guard blocks both shapes. Closes OD-119 | 2026-09-02 |
| [0083](0084-the-communications-gateway-says-what-it-did.md) | **The communications gateway says what it did, and the ledger sees its own rows** (Proposed). Six defects filed against `communications/**`; five fixed, **one refused**. The refusal first: `POST /communications/sms` and `POST /communications/email` were both open relays — `@Body()` only, no `@CurrentUser()`, no tenant, no ownership check on the destination, no record written — so any authenticated user of any of the ten restaurants could send arbitrary content anywhere on earth from the verified sender, untraceably. Both were to be deleted. `/sms` was: **zero callers**, verified across `apps/web`, `apps/mobile`, `packages`, `scripts`, every spec and the Python orchestrator (which uses `plivo_client.py` directly). `/email` was **not**, because grep before the deletion rather than after found a live one: `email_composer_service.py:354` ← `provider_conversation_agent.py:3074`, the path every approved vendor email travels. It is kept, with the hole and the unmade decision written into the handler and pinned by a test; giving the orchestrator a caller identity (it sends no `Authorization` header at all) is a service-to-service auth fork, filed, not repaired here. **The ledger measurement is the substance.** `/communications` calls itself the one place a manager sees every vendor conversation; the brief said 15 of 27 rows were invisible behind a status allow-list. Measured on production: 27 rows, **12** pass the allow-list, **2** survive `procurement_orders!inner`, and **2** is what the query returned — **25 of 27 carry `order_id IS NULL`**, so the INNER embed, not the allow-list, was binding. On the one real tenant it was **1 visible row out of 26**. Widening the allow-list alone moves the count **from 2 to 2** and would have shipped green, because a hand-made fixture cannot express a join it is not asked to model — `absence-reported-as-health` aimed at the repair. The embed is now `!left`, and the filter is a **deny-list**: an allow-list makes invisibility the default for every new status (`DISCARDED` and `CANCELLED` both post-date the original and vanished silently). Exactly two things are withheld, both because they are live in the approval queue on `/orders`: `PENDING_APPROVAL`, and `DRAFT` **outbound**. `DRAFT` **inbound** is shown — those ten rows are received mail wearing a column DEFAULT that describes us, not them, and were the largest thing missing. Both filters carry an explicit `status.is.null` arm because `neq` on NULL excludes, which is backwards for a deny-list. One level down, `draftContent: row.content` — and **`content` is NULL on all ten inbound rows**, their body being in the `NOT NULL` `message_text` — so the page printed *"No message body was recorded for this exchange"* about ten recorded messages. Both sibling methods already read the pair. **An unsent SMS no longer reports success:** `mockSendSms` returned `{success:true, messageId:"mock_sms_…"}` whenever Plivo was unconfigured, so the daily summary, low-stock alerts and order approvals all claimed delivery with a fabricated carrier id and `runPerTenant` counted the tenant succeeded. Log lines kept, id gone, `success:false`. Whether production has Plivo configured was **not** determined — Railway's env is separate — and the fix did not wait on it. **Two fabrications stopped leaving the building:** the hardcoded `deliveriesToday: 0` ("Would need to query deliveries table") that was texted beside two measured figures, and the prompts *"Reply REORDER to auto-order"* and *"Reply YES to approve or NO to decline"* — **there is no inbound SMS handler in this repository**, asserted as a route-shaped test so adding one turns it red, not as a claim. **Two corrections to the brief, both recorded rather than quietly fixed:** C3's cross-tenant websocket injection is **already refused** by `assertTenantMatch` inside `JwtAuthGuard`, so deriving the room from the JWT is defence-in-depth and a pin, not a live hole closed; and the doc-comment claiming `getDailySummaryData` "now throws" is **right** — `database.service.ts:61,106` both `throw error`, and the `|| 0` is only reached on a successful empty read. C4 (the weekly report's `message_body`/`subject` phantoms) was **fixed on `main` by [0077](0077-there-is-no-payment-due-reminder.md)/#241 while this branch was in flight**; what remains here is a `message_text` preview for the **14 of 27** rows that carry no `email_headers.subject` and would otherwise print `Latest: ""`. The conflict was resolved onto the merged `readRows` helper rather than keeping this branch's private error path. [0074](0074-a-read-names-columns-that-exist.md)'s guard proven to cover it: exit **0** on this tree, exit **1** naming `scheduled-tasks.service.ts:1289` with the two phantoms restored in place. **No debt entry retired.** 28 of 36 tests fail against the pre-fix tree, headline `Expected length: 26 / Received length: 2` | 2026-09-02 |
| [0088](0088-a-team-change-is-recorded-and-a-wage-is-not-invented.md) | **A team change is recorded, and a wage is not invented** (Locked). Seven `/team` gateway defects, five of them the same shape — an omission read as a decision. **T1:** `ensureRosterFromAccess` wrote `hourly_wage: a.role === "staff" ? 22 : a.role === "manager" ? 28 : 32` under a comment calling it a "mock wage"; measured on production 2026-09-02, **11 of 11 `team_members` rows carry exactly those literals** (8 at $32.00, 3 at $28.00) — not a fallback, the entire dataset, and the sole input to `laborCost()` → `shifts.labor_cost` → the week total, the Tonight-labor pulse, the labour lens and the CSV export. Nulling the wage alone would have **moved** the lie: `computeLabor` summed `Number(sh.labor_cost ?? 0)`, so an unpriced week renders **$0** and a part-priced week renders a partial sum as a total; the total is now `null` until every assigned shift is priced, with `pricedShifts`/`unpricedShifts` so a caller can say why. `getSettings` stops handing an unconfigured restaurant a **28% target it never chose** (0 `team_settings` rows exist). **T2 (founder's call — record it, do not restrict it):** removal and role change now write a `system_audit_log` row (before → after) and notify the person through one shared `access-audit.ts`, returning a receipt saying whether each write landed; `actor_id` is `public.users.user_id`, never the disjoint `auth.users`. Removal stays manager-gated. **T3:** an omitted `memberIds` meant *everyone* — a "message this person" control that messaged the restaurant — so exactly one of `memberIds` or `audience:"everyone"` is now required and the response names its reach; the flag stops the wrong send, the count lets a caller notice a right one that was bigger than it meant. **T4:** the broadcast ignored `notification_preferences` that the scheduled mailer honours; now filtered, with `suppressed` reported and a **failed** preferences read treated as "do not send" rather than "nobody opted out". **T5:** `assertAccess` fell back to `users.role`, whose DEFAULT is `'manager'` — so an untouched row was a manager of /team (measured: exactly **1** production user reaches /team this way, role `manager`, i.e. the default); the fallback now proves membership only. Certifications and time-off reasons were readable by any member; both are scoped to self for staff. A migration adds `CHECK (role IN ('owner','manager','staff'))` to `user_restaurant_access` and flips its DEFAULT to `'staff'` — verified first that all 11 rows pass, all 4 insert sites pass `role` explicitly, and 0 invites carry a null role. **T6:** `weekStartOfSchedule` read a schedule by caller-supplied id with no tenant filter *and* defaulted to the current Monday when it found none; `GET …/team/swaps` deleted — `swap_requests` has no writer anywhere in the repo, so it could only ever answer `[]`. **T7:** copy-week DELETEd the whole target week and re-publish wiped every read receipt, both on one click with neither in the contract; both now refuse with a 409 naming the count unless the caller says so, and both always report what was destroyed. **The guard already existed and did not catch T1** — `check_no_seeded_defaults.py`'s own header said it "does not read `apps/api-gateway`" — so it was extended, not duplicated, with a fifth rule; the first draft of that rule anchored on `.insert({…})` and returned **zero hits** against the pre-fix file, because the defect builds its row in a `.map()` and inserts the array later. Exit 1 pre-fix, 0 post-fix, 2 on a missing anchor, `--self-test` green including two injections into the real repository files. 10 further hits outside the triaged roots are **listed by name in the guard header** rather than excluded. 35 tests failed against the pre-fix tree; 39 pass now, 1776 green overall | 2026-09-02 |
| [0077](0077-there-is-no-payment-due-reminder.md) | **There is no payment-due reminder, because there is nothing in the schema to remind about** (Proposed). The daily `payment-due-reminder` cron filtered `procurement_orders` on `payment_due_date` — a column **no table in the schema declares**. PostgREST answers 42703, supabase-js *returns* `{data:null,error}` rather than throwing, and `if (!invoices || invoices.length === 0) return;` read the failed query as "nothing due". It has never sent one email. **The third dead cron in this one file** after [0058](0058-order-status-is-an-enum-not-a-string.md) and [0061](0061-recurring-reminder-reads-the-recurrence-table.md), all three the same shape: a query that cannot succeed wired to a caller that cannot notice. Unlike 0061 the fields had **no right home** to be repointed at: `payment_terms`, `final_price_per_bottle` and `negotiated_price_per_bottle` are not columns of that table either (so the amount rendered **$0.00**), `/orders?status=invoiced` names a status the enum lacks, and **no table anywhere carries a paid state** — so it could only ever nag, never remind. Deleted, not repaired: this was a stub for an accounts-payable module that was never built. The delivery half (template, `sendPaymentDueReminder`, the `payment_due` notification type) is kept so AP starts from a working mailer, and the ADR lists the five things that must be true first. Underneath it, **all 7 reads AND all 6 writes in the file discarded `error`** — the writes inside a `try/catch` that is *inert*, since supabase-js returns rather than throws. Now routed through pure `interpretRead()`/`interpretWrite()` returning `{ok:false,reason}` and logging at ERROR. Two writes dropped in-app notification rows (class **O**, silent omission — unenumerable after the fact, so the message names the count and type *as it happens*); the other four advance or deactivate a `custom_reminders` row, and since the cron selects `is_active AND next_fire_at <= now` every 15 minutes, **a silent failure there re-mails the same reminder indefinitely** — found by looking, not by being reported. A **fourth** phantom read was found in the sweep and fixed: the weekly report selected `procurement_conversations.message_body`/`subject`, the read half of the pair [0065](0065-a-conversation-log-names-real-columns-and-refuses-a-missing-body.md) fixed on the write side. Spec run against **two** baselines first — `origin/main` (**4 of 5 failed**) and the read-fix commit (**exactly 1 of 5**, the write test, which is what shows it discriminates); 5 of 5 pass after, 1658 tests green. The spec caught itself twice: its first draft was satisfiable by comment prose, so it now runs on a comment-blanked, line-preserving copy. Retires **three** entries from [0074](0074-a-read-names-columns-that-exist.md)'s shrink-only debt list, not the one predicted | 2026-09-02 |
| [0083](0083-a-page-may-not-claim-a-write-it-never-makes.md) | **A rebuilt page may not claim a write it never makes, nor call a failure a wait** (Locked). Extends ADR 0051 with a sixth clause: a statement about what an ACTION does is a claim, bound by the same rule as a displayed figure. Forced by `/communications`, where `TemplateSheet.tsx:85` said *"Saving stores it for later"* while both builders were mounted `onSave={onClose}` — a function that ignores its argument — and neither builder wrote anything (`GmailTemplateBuilder:482-537` made no network call; `SMSTemplateBuilder:378` said `// Simulate save delay`). Save showed a green tick and discarded the work; legacy has the same no-op and does NOT claim otherwise. Four further defects, all 0051 clauses in a new place: two caches keyed by a constant instead of the tenant (aggravated — the gateway NEVER reads `X-Restaurant-Id`, grep finds it only in test fixtures, and `AuthContext.tsx:433` catches a failed switch and proceeds on that non-existent fallback); a permanent 500 rendered as *"hasn't answered yet"* forever because `scheduled_reports` exists in no migration — a distinction the LEGACY page held and the rebuild deleted; an error banner covering one query of five; and an SMS line naming a channel no web client can reach. Fixed: Save persists through `POST /restaurants/:rid/templates` and confirms only after acceptance; both keys carry the tenant; `schedulesError` restored; one banner names all five sources. Held by `scripts/check_windowed_figures.py` — `/communications` added as its third page plus a new **W7** (a page declares the SHARED query hooks its cache lives in, which W6 structurally cannot see). Extending it exposed two vacuities in its own rules, both measured: `"GE" in src` matched `MERGE` and `GET`, and a leftover `import { GE }` passed after every use was deleted | 2026-09-02 |
| [0085](0085-a-fixture-tests-the-guard-not-the-checkout.md) | **A fixture tests the guard, not the checkout; and a skipped gate is not a pass** (Proposed). `check_adr_numbers_unique.py` failed on **every push to `main` for 14 consecutive merges** (2026-09-02, 10:05Z–13:00Z) while **every PR stayed green**. The consequence, not the badge, is the point: `Deploy to Production` is gated `workflow_run` on CI, so a red CI made it **`skipped`, not failed** — verified skipped on all 14. That workflow is the post-merge health audit that exists *because CI cannot see Nest DI failures*, and it has not run for any merge today. **The guard is correct; only its self-test fixture was wrong.** It cloned the *enclosing checkout*, so the child's `origin` was the parent: on a `pull_request` checkout the parent carries many refs → child missing them → exits 2 → green; on a `push` checkout the parent is single-branch → origin advertises **1**, child holds 1, nothing missing → child correctly exits **0** while the fixture demanded 2 → red. git also warns `--depth is ignored in local clones`, so it was never shallow. **A fixture that depends on the shape of the enclosing checkout is testing the environment, not the guard** — which is why PR-green and push-red coexisted for fourteen commits and nothing in the PR flow could surface it. Rejected: pointing origin at the real remote (**puts a network dependency inside a self-test** whose job is to prove an offline property, and fails closed on a runner without egress — a new source of red main); and requiring `fetch-depth: 0` on the caller (fixes the symptom, leaves the fixture equally environment-dependent). Chosen: the fixture **builds its own remote** — scratch repo seeded with two real ADRs → bare repo carrying **two** branches → shallow single-branch clone over **`file://`** (a bare path silently ignores `--depth`). Origin advertises 2, child holds 1: the real condition, identical on push, on PR and on a laptop. **And it asserts the reason, not just the exit code** — there are **six** distinct `CannotCheck` raises in the file, so an empty fixture would trip a different one and pass while covering nothing; the child's output must now name `no local ref`. Verified: CI-shaped checkout **exit 1 before, SELF-TEST OK after**; the guard's own behaviour there **unchanged at exit 2**; and the fixture **still fails a deliberately weakened guard** (`if missing:` → `if False:`), so it is not vacuous. All 46 `ci.yml` script invocations pass. **Named, not fixed:** nothing asks whether a *skippable* gate actually ran, and the gateway has no version/sha endpoint, so "is production running what we merged" is unanswerable — `/health/live` is dependency-free, so its 200 proves the process is up and nothing about the injector. | 2026-09-02 |
| [0086](0086-a-count-confesses-what-it-could-not-count.md) | **A count confesses what it could not count, and a sum without a unit is not a figure** (Proposed). Six defects on `/documents-reports` (the Sorting Office) and the two gateway services behind it, five of them one fault. `logs-timeline.service.ts` caught each of its **six** sources to `[]` and returned **200**, so a source that 500s contributed zero events, the page's `settledError` stayed false, and the failure rendered as **a smaller number with no banner** — `absence-reported-as-health` on a page whose entire subject is counts. The response now carries `events`, `failedSources` **and** `sourcesQueried`, so a skip (`event_store` is not restaurant-scoped and is read only with a `correlation_id`) is *stated* rather than inferred from a short list; the page raises its existing branch-aware banner and marks every count off that window as a floor. In the same file the merge sorted with `b.occurredAt.localeCompare(...)` **outside every try/catch** while `procurement_documents.created_at` (baseline `:4461`) and `system_audit_log.created_at` (`:5567`) are both nullable — one explicit NULL threw past all six guards and 500ed the whole feed; now null-safe, with undated rows sorted **last** and `occurredAt: string | null` saying so. The routine strip printed `{count} entries` off a 100-row window with no `≥`, twelve lines under the page's own promise of the floor rule, and its condition is **not** the drawers': it counts a *subset* of a newest-first feed cut from the OLD end, so `capped = windowFull && the oldest survivor is itself from today` — a full window whose oldest event predates midnight already holds all of today, and marking that would print `≥` over a figure we know exactly. **"In the registers" is deleted, not de-duplicated**: `procurement_documents` is one of the timeline's six sources so every vendor document was counted twice (thrice after a re-file, via `system_audit_log`), but the deeper problem is that a report + an invoice + a thread + an audit line share no unit — a figure that cannot be wrong is not a measurement. Also: `procurement_documents.currency` has existed since the baseline (`:4442`) and was simply absent from the client type, so every tie-out gap printed a hardcoded `$`; `GET /reports` had **no `.limit()` at all** and shipped the whole table to render twenty rows (now 100/200 + `offset`, with `count: "exact"` keeping the drawer's total exact); and `conversations?.count ?? 0` rendered an unknown as a zero one line below the paper side that got it right. **Two branches production cannot reach were deleted with the test that pinned them** — `report_period_start`/`report_period_end` are `date NOT NULL` (`:3078-3079`), so `hasPeriod` was true for every row the table can hold and the `enabled` gate never gated; a test asserting an unreachable state makes a suite look like it covers ground it never walked. **One reported defect is recorded as NOT reproducible:** the pane's `crossQ.isError` was said to flash mid-retry, but on `@tanstack/react-query` 5.90.16 a refetch after an error resets `status` to `pending`, so `isError && isFetching` does not arise — measured with a probe, so the `settledError` change is filed as a consistency fix, not a bug fix. `scripts/check_windowed_figures.py` gains this page as a **fourth `PAGES` entry**, not a second script (`SO_SERVER_WINDOWS` citing three gateway files, eight `| null` fields, a named `GE`), proven to exit **1** four ways and **2** once against the real files, with the guard's limit stated: it would **not** have caught the missing `≥` that motivated adding it. **Merged with [0083](0083-a-page-may-not-claim-a-write-it-never-makes.md)'s branch, which added `/communications` as the third entry and the new **W7**:** this page is therefore the fourth, and it declares `imported_query_hooks` naming `useConversationThreads` and `useActiveConversations` — `threadsTotal` and `draftsPending` are served entirely from those shared hooks, so a `PageSpec` carrying the default empty tuple would have registered the page while leaving two of its eight contract fields unchecked | 2026-09-02 |
| [0090](0090-pr-audit-gate-autonomous-merge.md) | **An Opus audit gate reviews every PR before it merges to main, and an approval merges + deploys with no human click** (Proposed). Founder answered both forks live: enforce in both the Claude Code session (skill + `PreToolUse` hook — "you call it, make it a constraint") and GitHub Actions (the backstop that holds when no session is driving); fully autonomous merge via an immediate, exact-SHA-pinned `gh api .../merge` call (never `--auto` — that races an unaudited later push; never `--admin`). Model corrected same day from the original "Sonnet max" ask to **Opus / high** per [0050](0050-agent-dispatch-hardness-threshold.md)'s own override rule (production/ADR/outward-send — this role hits all three). Three auditor angles (correctness, CLAUDE.md/ADR compliance, security/blast-radius) fan out in parallel, then a mandatory adversarial pass per CLAUDE.md §3 — only an explicit HOLDS passes; anything else, including the adversary literally saying BLOCK, does not (a deny-list version of this shipped and merged wrongly once, confirmed by execution, before being caught). Fails closed on any error (no `ANTHROPIC_API_KEY`, an API error, an unreadable diff) — never a silent green. `ANTHROPIC_API_KEY` **is** live as a GitHub Actions secret (added 2026-09-02; this row said otherwise until 2026-09-03, when the gate's own first live run — with a working key — caught the drift). **Six rounds** of the gate auditing its own PR found and fixed: a self-audit trust-boundary hole, a public-repo secret-exposure gap, the verdict-parser bug in two different functions, a deny-list merge decision, an unauthenticated PASS-marker forgery (confirmed by execution against a shimmed outsider comment), an `--auto` merge race, a GITHUB_TOKEN merge silently dropping the post-merge deploy audit, a missing `actions: write` permission that made that fix inert, and a required-check-would-report-success-having-audited-nothing gap one layer up in the YAML — see the ADR's six Correction sections, the last of which reports the parsers/hook have genuinely converged after adversarial re-testing found nothing new. A `--self-test` (29 invariants) pins every constructed exploit as a committed, CI-blocking test. **Still not live end-to-end:** the check is not yet in `main`'s required status contexts (that PATCH needs explicit founder permission, not run by this session) | 2026-09-02 |
| [0087](0087-the-jwt-library-we-use-is-the-one-we-declare.md) | **The JWT library we use is the one we declare** (Proposed). The founder asked to bump `python-jose` 3.3.0 → 3.4.0 for CVE-2024-33663 (critical, algorithm confusion) **if it was bulletproof and raised no new issue** — and that condition is what surfaced everything. **Nothing imports it.** `jose` appears in no Python source in the repo; the only textual matches are "San Jose" and "Saint-Joseph" in a city list and a wine-appellation list. It was a dead pin in **both** `requirements.txt:124` and `requirements.prod.txt:45` — the second being the file the `Dockerfile` installs. **What the code actually uses is PyJWT, and PyJWT was declared nowhere.** `api/studio_routes.py:769` and `services/override_service.py:46` both `import jwt as pyjwt`, the second with the comment `# PyJWT>=2.8.0` — the constraint was known and never written down. It arrived only transitively via `supabase>=2.10.0`, an **unpinned range**, and unlike `core/observability.py`'s optional `opentelemetry` import it is **not guarded** by try/except, so a resolver shift would surface as a **request-time 500 on studio auth**, not a boot failure. The real dependency was unpinned; the fake one was pinned to a vulnerable version. The bump was rejected on four counts: 3.4.0 is already superseded by 3.5.0 (2025-05-28); **[GHSA-28pv-f4g7-364j](https://github.com/advisories/GHSA-28pv-f4g7-364j)** (2025-10-10, `alg=none` accepted without verification) is **unreviewed with affected range "Unknown" and no declared fixed version**, so 3.4.0 cannot be cleared of it — the repo's own recorded trap, *a patched version is only patched against the CVE you looked up*; **no** version escapes `ecdsa`, whose `GHSA-wj6h-64fc-37mp` upstream **will not fix**; and with no importer the CVE was never reachable, so the bump would only quiet a scanner. Chosen instead: **remove it and pin `PyJWT>=2.8.0`** in both files, with the reasoning recorded beside the pin so it is not silently reintroduced. `>=` not `==` because 2.8.0 is the floor the calling code documents and `supabase` also resolves PyJWT. Verified in a clean venv against `requirements.prod.txt`: install **exit 0**, `import jwt` → **PyJWT 2.13.0** with an HS256 round-trip, `jose` **not installed** (nothing pulled it transitively — it was a leaf), `ecdsa` **gone with it**, and `pip check` → *No broken requirements found*. **Four advisories leave the image, one critical, none by trusting a version number.** A sibling sweep of every third-party import in non-test orchestrator code found **PyJWT was the only real gap**: four candidates were distribution-vs-import name artifacts (`google-genai`, `python-json-logger`, `postgrest`), `surya-ocr` is a deliberate prod exclusion (the Dockerfile drops the ~2GB ML/OCR stack), and `opentelemetry` is a **guarded** optional import with a no-op fallback. **Named, not fixed:** no guard enforces "every production import is declared" — the same class as [0074](0074-a-read-names-columns-that-exist.md) one layer out, and it needs a distribution-name map, which is why those four false positives are the argument for doing it properly rather than as a rider. | 2026-09-02 |
| [0089](0089-a-page-can-start-the-engine-it-reports-on.md) | **A page can start the engine it reports on, and an empty list names its cause** (Locked). Forced by `/team` measured against the tenant it runs in: `coverage_templates` 0 rows, `schedules` 0, `shifts` 0, `team_certifications` 0, `server_sales` 0, `schedule_receipts` 0, and an 11-person roster of 8 owners + 3 managers with **no `staff` role at all** — so the page IS its sentences. `TeamNext.tsx:296-299` printed *"Every required slot this week is staffed"* whenever no rule had ever required anything, and `:377-380` said *"Every credential on file is valid"* over an empty file; the legacy Ops drawer had the honest version (`OpsRulesPanel.tsx:109-111`) and the redesign dropped it. Founder's call: distinguish the two states **and** ship the rule-creation form on the panel that owns the gap list, because a page that names a job it cannot start is worse than one that admits the engine is off. Seven further defects, all ADR 0051 clauses in a new place: *"Message {firstName}"* sent with no `memberIds`, which the gateway fans out to the whole crew across four channels (`ManagerShiftDesk.tsx:335-345` vs `team.controller.ts:345-347`); five untenanted query keys on the REDESIGNED half while the legacy half was correct; four legacy reads with no `isError` branch, so a dead gateway drew *"0 active"*, a green tick and *"Publish readiness: Clear"*; seven mutations with no `onError` while two docs claimed every mutation had one; a blocker sentence counting "blocked shifts" over a `team_certifications` table with no role or applies-to column; copy-week and re-publish deleting a whole week and every read receipt on one click; and `App.tsx:305` giving a non-manager the manager surface, where `GET certifications` has no server-side role requirement. Held by `scripts/check_windowed_figures.py`, extended to `/team` as its fourth page across BOTH halves of the flagged route; extending it exposed a third vacuity — `IMPORT_LINE` required a trailing semicolon, and the legacy half is semicolon-free, so a leftover marker import would have passed | 2026-09-02 |
| [0094](0094-a-verifier-that-cannot-verify-does-not-admit.md) | **A verifier that cannot verify does not admit** (Locked). `GmailPushAuthService` guards the `@Public()` Gmail push webhook — the endpoint that makes the gateway fetch the inbox and republish it onto `email.events`. With `GMAIL_PUBSUB_AUDIENCE` or `GMAIL_PUBSUB_SERVICE_ACCOUNT` unset it **incremented a counter, logged an error, and returned `true`** — it admitted the push. Refusing needed a *second* variable, `GMAIL_PUBSUB_REQUIRE_AUTH=true`, to also be remembered. **Four places asserted the opposite**: the service's own docstring twenty lines above the branch (*"If either is missing we REJECT — fail closed"*), the controller header, the **Swagger description shipped to API consumers**, and — worst — `communications-security.spec.ts:151`, which **pinned the defect as correct** (*"accepts but counts an unverified push when config is unset"*), converting any later fix into a broken build attributed to whoever fixed it. The staged-rollout argument for admitting (*production may be carrying live traffic on unset config*) could not be checked: `REGISTER-AUDIT-2026-08-26.md:106-107` says the vars are set, `STATE.md` said they are not, and Railway's env is unreadable from here. **That ambiguity is the argument FOR failing closed** — while the code fails open it decides whether an unauthenticated caller can drive the mailbox; once closed it decides only whether mail is flowing. Missing config now refuses; half-configured is unconfigured; `GMAIL_PUBSUB_REQUIRE_AUTH` is **deleted** rather than defaulted, because a fail-closed posture a single unset variable can reopen is not fail-closed. The counter is kept and re-pointed — `refusedWhileUnconfigured` measures the cost of the closed door, and deliberately does not count a push refused for a bad token. **Impact re-assessed after the fix, correcting this ADR's own first draft**: OD-78 records an unsigned push probed twice on 2026-08-26 returning **401**, which under the old code means either the pair was set (verification live) or the retired flag was already refusing everything — so this is **most likely a no-op in production** and cannot break a working inbox. OD-78's stated retirement condition (*"not a config value but a deletion"*) is met; its claim that **pinned the staged-open branch as desired** is dropped. 42→43 tests, and the 5 new ones fail against the pre-fix service (`5 failed, 38 passed`) — measured, not asserted | 2026-09-02 |
| [0096](0096-a-route-declares-its-own-exposure.md) | **A route declares its own exposure** (Locked). On a controller with no class-level guard, a route carrying `@Public()` and a route carrying nothing are **runtime-identical** — both reachable without a token — so a reader cannot tell a decision from an omission. `AuthController`: **29 routes, no class guard**, 8 saying `@Public()`, 15 auth-guarded, **6 saying nothing** (`login`, `register`, both OAuth entry points, `refresh`, `verify-email`). **All six are legitimately public and none is a hole** — that is the stronger argument, not the weaker one: the defect is *declarative*, and route 30 gets added silently public with nothing able to see it differs from the 8. **OD-20 happened exactly this way.** Building the check found two things the hand count missed: a **seventh** route (`liveness.controller.ts:97`, which `deploy.yml:171` polls unauthenticated), and a bug in the guard's own first version — it matched `@Public()` **inside comments**, and these controllers' docstrings discuss those decorators by name, so all 17 `CommunicationsController` routes were reported public including the 16 that are JWT-guarded. Comments are now stripped and that case is pinned in a `--self-test`. `scripts/check_route_exposure.py` fails CI on any route declaring neither, classifies into `auth-guarded` / `public` / `guard-not-recognised` / `UNDECLARED`, and **exits 2 rather than 0** when it cannot check. **Pre-fix exit 1** (470 routes, 50 controllers, `UNDECLARED 7`) → **post-fix exit 0** (`public` 17→24). **`TenantGuard`'s fall-through is KEPT and argued, not overridden**: it is an `APP_GUARD` and `JwtAuthGuard` is not, so `request.user` is unset when it runs and refusing would 403 every authenticated route — the ratchet is the compensating control, named at the branch. Promoting `JwtAuthGuard` to `APP_GUARD` is recorded as the open follow-up. Corrects this session's own earlier report of "23 `@Public()`" — the real figure is 8 | 2026-09-02 |
| [0092](0092-parity-compares-against-what-was-merged.md) | **Schema parity compares production against what was MERGED, not against what a PR proposes** (Proposed). `Fresh database equals remote` builds a database from `supabase/migrations/` and diffs it against production — and was made a **required** context on `main` with `strict: true`. Migrations auto-apply on MERGE, so a PR that adds one compares a local database that HAS the new objects against a production that correctly does not: the new table lands under `IN LOCAL, NOT IN REMOTE`, the job exits 1, and the PR **cannot merge by any route the author can reach**. Measured 2026-09-02: **3 of 6** open PRs (#243, #244, #256) were stuck on exactly this, while #242 had merged that morning with the check red. The sibling required check `Code queries only relations production has` fails identically — #243 calls `record_stock_count`, its own migration creates it. The escape an author reaches for, hand-applying the migration first, manufactures the version mismatch that cost two sessions an hour the same day. Fixed by building the COMPARISON database from the migrations as they stand on the PR's **merge base**, so the check asks its real question — has production drifted from what was merged — while a separate full-set reset still proves the PR's own migrations apply. **What does not relax:** a MODIFIED migration is never held back (`--diff-filter=A` only), `push`/`schedule` hold back nothing and are byte-for-byte the old behaviour, and an unresolvable base is **exit 2**, never a quiet strict or a quiet vacuous run. **A correction is on the record inside it:** the first draft claimed the self-test proved three-dot over two-dot and it did not — mutating `...` to `..` left all eight cases green, because under `--diff-filter=A` the obvious fixture does not discriminate. A case that does (main DELETES a migration the branch never touched) was added, and the mutation now fails exactly it. **10/10** self-test cases against real throwaway repositories, mutation-tested three ways; the two workflow claims proven to regress when `fetch-depth: 0` is removed or a required job is renamed. **Named, not fixed:** the sibling check, and the fact that nothing in CI objects to DELETING an already-applied migration | 2026-09-02 |
| [0093](0093-a-scenario-is-replayed-and-verified-against-its-own-expectation.md) | **A scenario is replayed into the product and verified against its own expectation** (Proposed — five sub-decisions for the founder). The founder asked for random restaurant days (opens at twelve; a coffee and a glass at 12:01; five bottles at a second table two minutes after a 2 PM glass) driven through the product and checked end to end. Measured first: the product has no notion of operating hours (`restaurants` carries only `timezone`), the simulator places every cover on a hard-coded 17:00–23:30 UTC curve and never reads its result back, only 2 of 17 scenario §9 gates execute, sim tenants are seeded with `stock_live` and **no `inventory_lots`** (the projection trigger and both depletion RPCs read lots, so the first glass raises), a void reuses the sale's idempotency key so the return RPC short-circuits, and an email that never left reads exactly like one that did. Decisions: **D1** hours live on `restaurants.operating_hours` (jsonb, per weekday, in the venue's timezone; `null` = unknown, never assumed) with one Python/TypeScript rule over a shared DST-pinned fixture; **D2** each run persists its expectation (`sim_scenario_runs`) and ONE server-side verifier in the SimPOS module answers pass / fail / **unverifiable** — an empty expectation is never a pass; **D3** an out-of-hours check is recorded and flagged, never rejected; **D4** sim opening stock is materialised through `apply_stock_movement`, never as a projection write; **D5** the email outcome is written onto `notifications.delivery_status`. Rejected: a hours table, hours only in the archetype, a Python-only verifier, reusing `sim_ground_truth_*` (UNIQUE per restaurant, CHECK-limited fact types), editing the seed RPC, an email log table. Open for the founder: reject vs flag out-of-hours checks; hours table once overrides exist; `delivery_status` → email log | 2026-09-02 |
| [0095](0095-queried-relations-check-is-merge-aware.md) | **`Code queries only relations production has` is merge-aware, and both required contexts go back on `main`** (Proposed). The repair ADR 0092 named but did not make. The `C - R` arm asks *does production have what the code calls* — right on a `push` to `main`, wrong on a pull request, where the code is no more deployed than the migration is: they land together on merge. MEASURED: PR #243 added `record_stock_count` with the migration creating it, and this required check failed with *"1 function(s) the code calls do not exist in PRODUCTION"* while itself printing the correct diagnosis, `(migrations DO define it -- never pushed)`. No state the author could reach turned it green. Fixed with `--base <ref>`: the **same helper** ADR 0092's job uses lists the migrations this PR adds, the directory is parsed a second time without them, and `declared(full) - declared(base)` is what may be exempt. **What does not relax:** an object no migration declares fails in BOTH modes — structurally, since the exempt set is a subset of what migrations declare; a MODIFIED migration and an ALTER-only one exempt nothing; no `--base` means an empty exempt set and byte-for-byte the old behaviour on `main`; an unresolvable base, a listed file not on disk, and a base parse under the `MIN_DECLARED` floor are all **exit 2**. **23/23** self-test cases, every one asserting a value, every debt fixture synthetic and an empty debt list asserted legal (ADRs 0065 and 0073's two faults), including the shape CI actually checks out — a **merge commit**, not a branch head; **8 mutations each fail the suite**. The PR #243 condition was rebuilt against a real Postgres carrying the migration set at that PR's base commit (91 files, 0 failures, 238 tables, 1278 functions, `record_stock_count` absent) and measured four ways: pre-fix **exit 1**, post-fix PR mode **exit 0**, post-fix push mode **exit 1**, and a phantom relation+rpc **exit 1 in both**. **Also decided:** `Fresh database equals remote` and `Code queries only relations production has` go back on `main`'s required list (five contexts, `strict: true`) once this is green there — the em dashes in two of the names are U+2014 and load-bearing | 2026-09-02 |
| [0097](0097-the-gateway-says-which-build-it-is.md) | **The gateway says which build it is, and the audit proves presence rather than observing an absence of complaint** (Proposed). *"Is production running what we merged?"* was unanswerable, three ways at once. **(1)** `deploy.yml` polled `/api/v1/health/live` and stopped at the 200 — and the PREVIOUS instance answers 200 perfectly, so a green audit during a failed Railway deploy was byte-identical to a good one; the payload has carried `commit` since #254 and nothing read it. **(2)** Liveness is dependency-free by design, so it returns `{"status":"ok"}` with the Supabase client never initialised and every data route 500ing — **demonstrated**, 200 and 503 from one booted app. **(3)** The workflow is `workflow_run`-gated on CI, so a red `main` made it **`skipped`** — measured **49 CONSECUTIVE runs**, unbroken 2026-09-01T13:06:36Z→2026-09-02T16:14:18Z across **44 distinct revisions** (77 of the last 200), while Railway deployed on the push regardless. Fixed by: build provenance in `build-provenance.ts`, injected by the build via a Dockerfile `ARG`→`ENV` bake (**proven**: with the arg `{baked:"77eb7888…"}`, without it `{baked:""}` — never a plausible default) or Railway's runtime `RAILWAY_GIT_COMMIT_SHA`, **never** a runtime `git rev-parse` (no `.git` in the image); a second route `/api/v1/health/ready` that 503s when `onModuleInit` left no client or PostgREST does not answer a bounded HEAD probe, unauthenticated so it cannot silently not-run, memoised 5s and data-free; and `scripts/check_deployed_sha.py`, which polls until the reported revision **is** the merged sha and fails on `"unknown"` as loudly as on a mismatch. **Both** skipped-gate options shipped: **(a)** `ci-gate` always runs and fails explicitly — red, not grey — and **(b)** `scripts/check_deploy_audit_ran.sh` answers retrospectively, reading the **job** conclusion because a workflow-level `success` is compatible with every stage having skipped. (b) was deliberately NOT made a required CI job: with `strict: true` and 3–4 concurrent sessions it would starve the merge queue and train everyone to read a red required check as noise. **Proven:** a real process reporting a real sha; the audit exiting 1 on a simulated failed deploy against that same live process; `--self-test` 8/8 through a real localhost server incl. the mismatch; six mutations each turning a claim red; 18/18 health tests; `check_gateway_boots.sh` PASS. **Measured against REAL production, not a local double:** `/api/v1/health/live` already returns `commit":"928912003840…`, which IS `origin/main`'s tip — so `RAILWAY_GIT_COMMIT_SHA` is already in the runtime environment and **no founder action is required** (the ADR records the near-miss: it was drafted asserting the opposite, and one `curl` settled it). The audit MATCHes that sha against production and MISMATCHes a different one, both run live. **Named, not fixed:** the orchestrator and Vercel still have no provenance; the 49 skipped audits are made visible, not performed | 2026-09-02 |
| [0098](0098-a-preference-is-read-from-the-column-it-lives-in.md) | **A notification preference is read from the column it lives in, and the switch a user toggles is the one that decides** (Proposed). `RecipientResolverService.checkChannelPreference` read `prefs.order_channels` and `prefs.report_channels`; **neither column has ever existed** — the table has `order_approval_channels` and `financial_reports_channels` (`20260805000000_baseline_from_production.sql:3899-3939`). Because the row arrives via `.select("*")` the phantom names appear only as property accesses, so there is **no 42703, no error, no log** — just `undefined`, forever. On the stock production row it ran backwards on both axes at once: **email refused to users who had enabled it** (the only matchable array was `low_stock_channels`, default `['sms','push']`, and the "no preferences set" escape hatch could never fire because that one column was truthy while the two misspelled ones were undefined) and **SMS sent to users who had disabled it** (`sms_enabled`, the one opt-IN channel, `DEFAULT false`, was never consulted). `notifications.service.ts:1051-1053` had the semantics right all along, so the same row meant one thing in Settings and another in routing. **A rename alone does not fix it** — measured on exactly that intermediate tree, 4 of 10 still fail, because `order_approval_channels`' default contains email and SMS keeps passing. Fixed as two gates: the global per-channel switch first, then the category arrays by their real names. Second defect, same path: `low-stock-alerts.service.ts:resolveEmails` is called **once per restaurant** and fell back to the global `MANAGER_EMAIL` at **two** layers (it omitted `allowDefaultFallback`, which defaults true, then read the env var directly inside a `catch {}` that swallowed the error) — the OD-87 / ADR 0022 leak reproduced in a caller that predated the fix, live for the **6 of 10** restaurants that have an `owner` and no `manager`. Third, found while fixing the first: `check_read_columns_exist.py` (ADR 0074, a **required** check) reported **PASS** on all of it, because its universe is the projection and the filters and this read is neither; extended with a `ROW_PROPERTY_READS` registry that exits **2** — never silently — when an entry's file or variable is gone. **12 of 17** new tests fail against `origin/main`, **0** after; guard exits **1** on the pre-fix tree, **0** after. **Named, not fixed:** routing is still not category-aware (union across three of six arrays — **OD-121**, a founder call), three category arrays are read by nothing, the registry has 2 entries and does not claim to cover the other nine `select("*")` sites on this table, and nothing was verified against production | 2026-09-02 |
| [0103](0103-a-delivery-is-agreed-before-it-is-verified.md) | **A delivery is agreed before it is verified** (Locked on the founder's in-session answers; D9 proposed). The founder's Turkish door process — order → vendor confirms → goods arrive with a correctable document → both sides agree → one invoice for what was agreed → verified — is the codified Turkish flow (e-İrsaliye Yanıtı: accept / partial / reject in 7 days, silence accepts; invoice for the accepted quantity only) and the US has the same states compressed into the delivery window with a 30-day wholesaler-initiated EFT clock (AB 2991). Today the order has twelve statuses and the delivery none; `verifyReceipt` needs an order; `retroactive-order` mints a PO after the fact. Decided: a delivery entity with `RECONCILING ⇄ AGREED → VERIFIED`, *agreed* = both sides recorded and never implied, clocks as data with an `unknown` that blocks, `UNORDERED` provenance instead of retroactive POs, six human gates, ten reason classes, nine notifications, and (proposed) a never-looked-at document ages, escalates and `LAPSE`s rather than auto-posting. Design only. | 2026-09-03 |
| [0104](0104-every-incoming-document-renders-as-one-canonical-mudavym-document.md) | **Every incoming document renders as one canonical Mudavym document** (Locked on the founder's answers; D8 retention and D13 direction proposed). *"The agreed invoice is the product"*: one canonical schema in three layers (EXTRACTED / RESOLVED / ADJUDICATED) with a per-field envelope (`source`, `confidence`, `bbox`, `as_printed`, `revision`) and EN 16931 names; one template with conditional sections for invoice · credit memo · delivery note (irsaliye) · receiving advice · statement; the original content-addressed, stored once, fetched on demand; verdict first, words and numbers never colour alone, confidence never a number; corrections append-only; an intake gate that names blank / duplicate / bundled / unreadable uploads and a degraded state that never renders an empty document as complete; the delivery entity from slice 1; paper stays light in dark mode; hybrid PDF/A-3 export from a server-rendered component. Scored (a) one schema + one template 21 vs (b) per-vendor layouts 10 vs (c) drawer 14. Three sketch directions in `sketches/089`. Design only. | 2026-09-03 |
| [0105](0105-a-pos-connection-is-a-row-not-an-env-var.md) | **A POS connection is a row, not an env var** (Locked — the founder chose the table in session; build gated). The Square day, measured: one real Meyhouse Friday (42 checks, 243 spec-faithful Square events) → genuine Square signature **401 × 243**; same bytes on our legacy header **201 × 243** with `received: 0` and zero log lines; the same day through our canonical envelope **42/42**. Square's MAC includes a per-merchant `notification_url` nothing in the schema can hold, and its webhooks are pointers, so push-only Square is impossible. Decided: `pos_connections (restaurant_id, provider_key)` with secret, `signature_scheme`, `notification_url`, OAuth tokens, cursor, status; scheme is data and fails closed; the legacy global rung retires when SimPOS has a row; adapter fixtures must be captured or spec-cited vendor payloads; a field a POS cannot supply is `null`, never `0`. Supersedes the OD-A/OD-B drafts in `POS-BRIDGE-AUDIT.md`. Four defects filed in TECH-DEBT. | 2026-09-03 |
| [0107](0107-a-declared-server-is-not-a-reachable-one.md) | **A declared model-context server is not a reachable one** (Proposed — built behind `mudavym_design_profile`, founder review open). `/profile` Register IV recorded connections nothing had ever exercised. A probe now performs the real handshake (`initialize` → `initialized` → `tools/list`, Streamable HTTP/SSE, timeout + byte and tool caps) and writes `probe_status`, the server's own name/version and its tool names; `last_used_at` moves ONLY when the server answered, so a month of failures cannot read as a month of traffic. Per-connection secret AES-256-GCM under `MCP_CONNECTION_SECRET_KEY`, never returned by any route. The endpoint guard is a byte-level parser (mapped/compatible/NAT64/6to4/Teredo carriers, loopback, link-local, unique-local, decimal and hex spellings all refused; names resolved before the call and the vetted address pinned into the socket) — the audit had found the first version bypassable through `::ffff:` literals. **Tool invocation is deliberately absent** (no `tools/call`, no route, a structural test) until ADR 0013's commitment guardrail is extended to model-context dispatch. 118 gateway tests; migration `20260903104500` | 2026-09-03 |
| [0108](0108-a-register-is-the-houses-own-books-first.md) | **A register is the house's own books first, and the catalogue second** (Proposed — built behind `mudavym_design_cellar`, founder review open). The six non-wine registers were thin because `public.beverages` has no `restaurant_id` and nothing references it; two passes had shipped a better sentence about it. The spine is inverted onto the five tenant tables that carry both a restaurant and the product's name — `menu_items`, `procurement_document_lines`, `procurement_order_items`, `vendor_price_observations`, `pos_unresolved_lines` (which, with inventory keyed on wine, is where every non-wine sale a house makes lands) — joined by `beverage_house_key()` and read through `house_beverage_ledger()` (EXECUTE revoked from client roles). Each row shows first bought · paid · poured · who quoted, an em dash naming the migration until it merges. Cocktails gained create/edit/delete and their first ingredients writer; `/spirits` `/non-alcoholic` `/soft-drinks` got routes. Add-to-inventory stays gated on OD-113. Migration `20260903120000` | 2026-09-03 |
| [0109](0109-a-reminder-is-the-houses-job-not-the-browsers.md) | **A reminder is the house's job, not the browser's** (Proposed — built, OFF behind `CALENDAR_REMINDERS_ENABLED`, founder review open). The calendar's reminders lived in one browser's localStorage and `reminder_sent` had never been written. Idempotency is a database index, not a boolean: a claim row per (entry, person) in `calendar_reminder_dispatches` BEFORE any send, so a reminder cannot go twice and quiet hours become a per-person deferral; `calendar_reminder_runs` records every sweep. The job runs per tenant under the existing scheduler (ADR 0022), writes a durable notification row, and the page's reminder register names last run, next run and whether the scheduler serves this house. Ships OFF behind an environment allow-list — deliberately not the design flag — because it writes to every member's inbox and phone; live status on production: served, not armed, nothing pending. The audit proved claim-before-send by racing a reverted copy. Not built: sub-day offsets, email. Migration `20260903101500` | 2026-09-03 |
| [0110](0110-a-card-on-file-is-the-providers-record-not-ours.md) | **A card on file is the provider's record, not ours: Stripe by SetupIntent, no SDK, no charge** (Proposed — built behind `mudavym_design_profile`, founder review open). `/profile` Register V had a table, a module and three routes, and filed the remainder as gap G10, *"everything except the credential is built"*. MEASURED before building: **no Stripe client** (`grep -ril stripe apps/api-gateway/src` → three files, all prose or the literal `'stripe'`), **no webhook**, **nothing minting a SetupIntent**, and neither `stripe` nor `@stripe/stripe-js` installed — so the required `provider_ref` was a field no caller in this product could fill, and setting `STRIPE_SECRET_KEY` on that tree would have enabled an Add form whose four HAND-TYPED fields (`brand`, `last4`, `exp`, kind) became the register's content. The honest refusal was **one environment variable from a fabricated instrument**. Four choices carried the build: **the `stripe` SDK is not installed** — the lockfile is shared with three concurrent builders and both Vercel entry points run `--frozen-lockfile`, and the SDK does not change the network path since the card is collected on `js.stripe.com` either way, so four REST calls ride the `axios` already present; **the card is collected by Stripe Elements against a SetupIntent**, so no PAN reaches this product at all (SAQ-A) and what returns is a reference only Stripe can mint; **Stripe.js loads from `js.stripe.com`** because the app ships no CSP to allow anything through (verified: no `<meta http-equiv>` in `index.html`, no CSP header in either `vercel.json`, no `helmet` in the gateway) and `@stripe/stripe-js` injects that same tag; **the webhook is idempotent on the provider's event id as a PRIMARY KEY**, with the half that is usually missed — a claimed-but-unfinished delivery is RE-PROCESSED, so a transient failure cannot permanently swallow the event saying a card was removed. **No charge is expressible**: `FORBIDDEN_PATHS` in the client throws before building a request to `payment_intents`, `charges`, `subscriptions`, `invoices`, `refunds`, `transfers`, `payouts` or `checkout/sessions`, because pricing is OD-23 and open. **The honesty seam that earns its keep**: provider state names each of three secrets and the process each lives in, derives the key's mode from its own prefix (never a separate variable that could disagree), and reports **when a signed delivery last arrived** — a webhook secret that is set and has never been delivered to reads *"configured, never delivered"*, not as health, because without deliveries a card removed at Stripe shows here forever. 47 new gateway tests + 13 rewritten; `check_gateway_boots.sh` PASS | 2026-09-03 |
| [0111](0111-the-calendar-is-the-houses-day-book.md) | **The calendar is the house's day-book of events, deadlines and the world outside** (Proposed — slices 1-3 and the iCal fixes BUILT behind `mudavym_design_calendar`, founder review open). DESIGN-FOUNDATION §6 forbade *"weather-driven forecasting on the grid — a guess on a page whose virtue is that everything is a fact"*, and the founder then asked for exactly that. The veto is upheld on one distinction that became the whole design: **a published meteorological forecast, attributed to its issuer and its issue time, is a citable observation; our covers number derived from it and drawn without its error is the guess** — so the first is drawn from slice one and the second is slice nine, gated on ninety days of the house's own history. MEASURED against production first, and five of six inputs find nothing: `restaurants.latitude`/`.longitude` NULL on **all 14 rows** while 13 carry an address; `vendor_price_observations`, `team_certifications`, `recurring_orders` and `provider_promotions` at **0 rows**; `procurement_documents` with no `due_date` column at all; **no shelf-life column anywhere** in 89 migrations; and the best-covered tenant at **22 observed service days** against `holtWintersAdditive`'s `2 * period` floor — which is why the build order starts with the coordinate, not the maths. The founder's five answers (§Status): **NWS behind a `WeatherProvider` interface** (the keyless Open-Meteo tier is CC-BY non-commercial and this is a commercial product; NWS is free for any purpose and US-only, so the seam is the decision); **the coordinate is taken from the Google Places selection at sign-up** — the form had resolved it in the same `fetchFields` call that fills the city and thrown it away, and the 13 existing rows get a **script with a dry run**, keyed on `google_place_id`, never a migration that calls an external API; **public commodity indexes in, as a separate register that never sits beside a vendor quote**; **the 90/180-day floors stand, per restaurant, never pooled**; **the Google app goes for verification now**. Three build-time measurements changed the code: the forecaster's own time is `updateTime` not `generatedAt` (which refreshes on every poll and would make a twelve-hour-old grid read as new); NWS's horizon is **7 days, not 16**, so a cell past it says so; and NWS publishes a **probability and no amount**, so `precipitation_amount_mm` is NULL on every row and a `DEFAULT 0` would have published "no rain expected" for every day of every forecast. The ADR's own §6 was **overruled by measurement** on one point: the refresh does NOT run under `ScheduledTenantsService.runPerTenant`, because that scheduler serves one tenant in fourteen and a cron behind it would have left thirteen houses with a blank weather column and no sentence — it is on-read with a 60-minute max age instead, and the cost (a house nobody opens accumulates no history) is filed rather than hidden. `weather_readings` **keeps every issuance** rather than caching, which is what lets a passed day state what was forecast *in advance*; slice 3 pairs that with the ledger's own covers and writes the first rows `prediction_outcomes` has ever received from this product — with `accuracy_score` **NULL**, because there is no covers model to score against and no temperature observation to score with. Also shipped: the four iCal suspects behind a feed nobody has seen subscribe — `Content-Disposition: attachment` → `inline`, wall clocks resolved in the **restaurant's** IANA zone instead of the server's, `X-PUBLISHED-TTL`/`REFRESH-INTERVAL`, and an absolute + `webcal://` URL that says whether its origin was configured or inferred. Not built: slices 4-9 (deadlines, notes, ⌘K, Google push/pull/two-way, covers). Migration `20260903162000` | 2026-09-03 |
| [0112](0112-one-modal-policy-three-shapes-one-primitive.md) | **One modal policy: three shapes, one primitive, and the overlay wears the page's ground** (Proposed — built behind the existing per-page design flags, founder review open). The founder's instruction was verbatim *"modal window changes are a must to match with new wave"*. MEASURED first: **four** hand-rolled overlays across the rebuilt pages, each re-deriving the scrim colour, the motion and the Esc handler, and **none** trapping focus, returning focus to the opener, or locking body scroll; `components/mudavym/` held no overlay primitive at all; and **nine** shell overlays (⌘K, Ask AI, shortcuts, recently-viewed, the bell, the user menu, theme, branch switcher, the mobile scrim, the nav hint) render over EVERY page in the legacy white-and-wine look. The rival — **one shape everywhere** — loses on this house's own evidence: the two overlays it built by hand, on two pages, independently, converged on **two** geometries (a 440px right sheet, `calendar-next.css:499`; a 620px centered panel, `reports-next.css:657`), and one shape would spend a full-page scrim on a theme toggle in a house that rations ceremony on purpose. So: `Sheet` (a record) · `Panel` (a question) · `Popover` (a control's own menu), all over one `OverlayRoot` that owns the four things every page got wrong — focus in, Tab cycling, focus back to the opener, counted scroll-lock — and renders **no** animation under `prefers-reduced-motion` rather than a shorter one. Because ADR 0042 scopes every token under `.mudavym` and never `:root`, a portalled overlay carries `.mudavym` **and the page's ground on the same element**; the ground is a DOM fact, not a prop, and every reader returns null/off rather than defaulting to paper. `lib/mudavym/shellGround.ts` gates the shell: house shape only while a rebuilt page is mounted, and `shellOverlays.test.tsx` pins the **literal legacy class strings** so ADR 0042's byte-for-byte promise is a test, not a hope. One exception is named, not hidden — the anchored invite dialog is a form, not a picker, so it takes `Popover modal`; a SECOND such exception is the signal to collapse to two shapes. Deliberately NOT covered: the two legacy template builders' internals, and four Radix dialogs reachable from rebuilt `/settings`. Two premises of the brief were CORRECTED by measurement: the shell does not render nine overlays over every page — `DashboardLayout` only re-exports `Header` (`:110`) and NO rebuilt page renders one, so the bell, user menu, theme menu and branch switcher are not on a Mudavym page at all today; and the anchored popover's `visibility:hidden` first frame silently ate the focus move, which jsdom cannot see and only a live `contains(document.activeElement)` measurement caught. 19 new tests + 1291 existing green; DESIGN-FOUNDATION §3 item 4 closes. **Updated 2026-09-04:** the two held pages migrated — calendar's `EventSheet` is a `Sheet`, reports' `AskTheBook` is a `Panel`, and the per-page `.cn-scrim`/`.cn-sheet`/`@keyframes cn-sheet-in` and `.rp-ask*` rules are deleted; the shell pins now cover ⌘K, Ask AI, the bell, the user menu, the branch switcher and the mobile scrim as well, cross-checked against `origin/main`'s own source, and `InviteTeamDialog` finally has a test. Status still **Proposed** | 2026-09-03 |
| [0113](0113-the-assistant-proposes-the-seal-applies.md) | **The assistant proposes the house's configuration; the seal applies it** (Proposed — research and design only, **nothing built**; **four of its five open questions answered by the founder 2026-09-04**; one still open, and the last answer opened two new ones). The founder's ask, verbatim: *"keep as defaults, but while onboarding they have the option to do that. + it will be game changer let AI assistant talk with you and handle all the configs then approval button."* Fifteen products surveyed with a URL each (§6d; six fetched and read in full — and **three citations written from search summaries did not survive that check and were corrected**, including a plan-mode URL that 404s). Three interaction shapes were weighed and **two rejected on measured facts, not taste**: a guided interview forfeits the only thing a conversation buys (one sentence — *"small meyhane, closed Mondays, three vendors, I sign over ₺5,000"* — moves **eight registers**, which fourteen forms cannot), and fill-then-review is impossible for 3 of 14 registers (`measurement` is `kind:'browser'` with no server row to stage, `st-format.ts:123`; `features` writes to a table with **no changed-at column at all**, `:54-55`; `ledger` is read-only). The chosen shape — one talk, one prunable diff, one seal — was then subjected to four kill attempts and **three succeeded partially**, each forcing an amendment that is now load-bearing: (1) a register already holding a **stated** value never enters the batch — it is *contested* separately with its own seal, so the batch only ever fills silence; (2) Notion, the closest analogue, forbids agents from workspace settings entirely, which moves the line to **blast radius** — *does this change who may act, or what the house may spend?* — leaving 8 registers proposable and 6 permanently out of reach; (3) fourteen registers are written by eight services with **no shared transaction**, so the seal **never reports a single "Done"** — every item carries its own outcome and reason, the shape [[0084]] already requires; (4) filling fourteen registers at once would collapse the house's own `unstated`/`stated` distinction (`vendor-terms.service.ts:44-59`), so a sealed value is **its own third provenance class**, recorded with the utterance that produced it. **No migration is needed**: `system_audit_log.correlation_id` (`20260805132000:73-75`) and `.reason` (baseline `:5564`) both exist and neither is ever written (`settings-audit.service.ts:205-221`), `changes.fields[*]` already stores `{from, to}` (`:89-91`), and `/logs` **already filters this table by `correlation_id`** (`logs-timeline.service.ts:302`) — the batch-undo reader is built and unfed, which nothing in the survey can match (Salesforce's Setup Audit Trail does not capture before-and-after values at all). One **blocking precondition, found by measurement**: `PUT /settings/approval-thresholds` carries `@UseGuards(JwtAuthGuard, TenantGuard)` and **no role decorator** (`settings.controller.ts:40,107`) while `@Roles()`/`RolesGuard` exist and are used on two other controllers — the assistant does not create that hole but makes it reachable by sentence. MCP surface: 3 of the reads exist in the documented 42, 3 reads and `config.propose_batch` are new, and **`config.apply_batch` is deliberately never exposed** — the pattern the doc already uses for `ask-ai` confirm and `one_tap` execute. Onboarding: the optional step replaces `ThresholdStep` in the slot `GetStarted.tsx:256-286` reserves, offers only the five values that have a write path, **names and does not offer** the market threshold (a deployment env var, `market-price.producer.ts:95-97`), and makes **an explicit skip a recorded fact** — today "Skip for now" writes nothing (`ThresholdStep.tsx:80-81`) and `threshold_configured` is `false` both for a house that declined and one nobody asked. Sketch `101-config-assistant/` (4 pages, 8 screenshots at 1440, both grounds, zero console errors). **Four founder calls, 2026-09-04.** *Revocation* — a sealed batch stays revocable **as one unit for seven days** (rule 4a): write the `correlation_id` the audit trail already carries, and one *"Undo this setup"* restores every stored `from` through each register's own owning service, after which each value is an ordinary setting. Two honesty requirements ride with it — the window's end date is stated on the receipt, and a row whose current value no longer matches the batch's `to` is **shown and skipped**, because somebody acted after the seal and restoring `from` would erase a person's later fix. *Reads* — the founder: *"everything valuable, but we don't want to touch people's privacy … nowadays everyone gives their agents access to their mail — I say everything."* Recorded as a line with two sides (rule 6): the assistant may read **everything the HOUSE exposes** — till history, order book, inventory and ledger, the registers themselves, and the house's vendor threads through **house-declared** connections — with **every source named on the proposal row** that used it; and a **PERSON's mailbox or messages only through that person's own consent row**, [[0114]] §2's house-declares/person-consents model applied to reads, asked for in plain words **in the same conversation**, recorded with the batch, and withdrawable without touching anyone else's. The test is not "sensitive versus not" but **whose record it is** — the same test §6b used to place connections and rule 2 used to draw the write surface, applied a third time. *Autonomy* — **features stay outside the line**: the assistant may **point at** an autonomy switch on `/settings`, name it and take the owner to it, but may **never propose flipping it, sealed or not** — blast radius draws the line, not the word "settings", and rule 2's exclusion is now the founder's call rather than a session's reading of it. *Voice* — *"talk with you"* means **BOTH**: voice and typing, on desktop **and** phone, neither the fallback for the other (the sketch draws typing because it is the harder case on a page of registers, not because voice is secondary). **That answer opens two questions that were not open before, and both block the voice half and only the voice half** — typing needs neither, and neither can be retrofitted once someone has spoken: **Q6, the speech provider** — on-device dictation (`SFSpeechRecognizer`, Web Speech API) keeps the audio on the device but is weakest on Turkish vendor names, prices and the product's own vocabulary, which is most of what an owner says, while a hosted model is markedly better at exactly those and means **the owner's voice, saying their own margins and their staff's names, leaves for a third party** — a new outward flow needing a connections row; the question is not accuracy but **whose infrastructure hears the owner speak**, the same test rule 6 applies to reads and §6b to connections; and **Q7, whether a spoken configuration is a record** — typing leaves an utterance the ADR already stores in `system_audit_log.reason`, but **speech leaves audio and a transcript, and a transcript of a setup conversation is a recording of a person talking about their business, their vendors and their staff**: (a) stored with the batch as the typed utterance is, (b) only the one-sentence reason kept and the transcript discarded on seal, or (c) nothing retained beyond the conversation — settled **before** a microphone ships, because there is no retroactive consent for a recording already made. **Still open: 4, 6, 7** — whether the market threshold earns a per-house column, plus the two above | 2026-09-04 |
| [0114](0114-connections-are-the-houses-profile-is-the-persons.md) | **Connections are the house's, profile is the person's** (Locked on the route and the four founder calls; built behind `mudavym_design_connections`, founder review open on the surface). The founder's note on `/profile` — *"Maybe not in profile you're right"* — against DESIGN-FOUNDATION §6b's ten-product survey, whose rule is that **placement is decided by whose credential it is and in whose name the action is taken, never by what the thing is called**. Three of seven registers came out on the wrong side, and the measurements were not close: `payment_methods` has **no `user_id` column at all** (`20260903094600:53`) — a house object on a personal page; `GET /payment-methods` and `GET /billing/provider` took **any authenticated member** while every write called `assertCanManageRestaurant` (**G19**); `listConnections` filtered on `user_id` alone while `restaurant_id` was written on every grant, so a Drive grant made in restaurant A listed in restaurant B (**G21**); the same OAuth catalogue rendered in **three places with three different subsets** while POS, sender identity, the calendar feed and payments appeared in none (**G20**). Founder's four calls, all built: **"House declares, each person consents"** — `user_mcp_connections` is RENAMED `restaurant_mcp_connections`, `user_id` replaced by `declared_by … ON DELETE SET NULL` (deleting the manager who typed the URL no longer deletes the house's Toast bridge) and consent becomes its own table with **two independent withdrawal axes**, the person's and the house's; **"per-tool grant plus the seal on every write"** — `mcp_tool_grants.writes` NOT NULL **with no default at any layer**, one gate refusing five ways, and `mcp_tool_calls` recording **failed calls too**, which supersedes ADR 0107's `invocation.enabled: false`; the house's **own sending address** decided in direction and honestly unbuilt, with `GET /communications/sender-identity` so the page states the shared-mailbox truth from the server; and **"a manager may SEE, not approve"** — a revocation list (`restaurant_personal_grant_access`) enforced at `getAccessToken`, the one door feature code uses, with the migration's own DO block raising if an `approved_at`/`pending` column ever appears. **The page's structural idea:** ONE row draws every attachment, four columns and no fifth (whose it is · what it may do · what it last did · how to stop it), and `stopNote` is a REQUIRED prop so a row with no live control cannot be written without naming who can stop it. **The honesty seam that earns its keep:** the ledger sentence — *"Nothing here can spend money today"* is the most reassuring line on the page and would be a lie if the payment register had merely failed to load, so every count is `null` when its register is unread and the sentence drops the clause rather than softening it. Two measured corrections to the research: **a house has no public page** (`vendor_portal_pages` is keyed by `vendor_catalogue_id`/`provider_id`, `20260805155901:27-33` — §6b called it "the house's" and that was wrong), and **the POS bridge has no disconnect route of any shape**. Rejected: one `GET /connections/ledger` (two answers, whole page or 500, on the surface whose job is naming what is missing); a `/settings` section (the founder chose the route, and the obligation to collapse four tabs survives as roadmap item 8); adding `declared_by` while leaving `user_id NOT NULL` (writes the new rule beside the old one and enforces the old). Also: `check_order_capture_contract.py` learns `ALTER TABLE … RENAME TO` — without it the shared replay kept the old name and reported eleven **false** findings against correct code. 20 vitest + 9 gate specs + 11 integration specs; migrations `20260903150000`, `20260903151000` | 2026-09-03 |
| [0115](0115-the-house-item-is-the-ledgers-key.md) | **The house item is the ledger's key, and it is the row the house already has** (Proposed — the founder chose *one house item id across all beverages* on 2026-09-03, closing OD-113's identity half, which ADR 0070 had parked; migration `20260903171000` written and **NOT applied**, no app code). The measurement that chose the shape: **the key already exists.** Stock is `restaurant_inventory.stock_live`, par is `.threshold_min`, a count is `stock_counts.inventory_id`, an order is `procurement_order_items`/`procurement_orders`/`recurring_orders`/`rfq_requests.inventory_id`, and the POS bridge maps to `inventory_id` on **239 of its 254** production rows against only 107 carrying a `master_wine_id` — all four of the founder's nouns key on `restaurant_inventory.id` today, and the only thing keeping a keg out is `master_wine_id uuid NOT NULL` (`…baseline…:3262`). So the decision is **not a new table**: `master_wine_id` becomes a nullable attribute, the row gains `kind` / `uom` / `display_name` / `beverage_id` / `identity_provenance`, and a `house_items` view carries the name. A `house_items` TABLE was the leading shape and was killed by its own cost — **18 foreign-key dependents, 199 gateway call sites across 59 files, 19 database functions and 6 views**, i.e. a dual-write window in which a path that sets one key and forgets the other writes a row that looks correct and is invisible to half the readers. `beverages` as the parent was refused on four grounds (no `restaurant_id` at `20260817070000:217`; ADR 0108 already refused tenants a write path into a catalogue whose identity is trigger-set; 4 226 library rows against 608 catalogue rows with 21 FKs on `master_wine_library`; a shared-catalogue merge would move a house's stock). **The blast-radius finding that matters most**: `inventory_transactions.inventory_id`, `inventory_transactions.wine_id`, `pour_events.inventory_id`, `inventory_lots.master_wine_id`, `inventory_alert_state.inventory_id` and `inventory_lot_revaluations.inventory_id` are all `NOT NULL` with **no foreign key at all**, so any cut-over drawn from `pg_constraint` certifies the ledger as untouched — which is why the invariants live in `scripts/check_house_item_invariants.py` rather than in the schema. **Measured, not reasoned**, against a full local build of all 114 migration files (0 failures) with the file under test excluded as the control: the `DROP NOT NULL` succeeds; the existing `UNIQUE (restaurant_id, master_wine_id)` keeps one row per wine *and* admits unlimited non-wine rows because NULLs are distinct, so nothing drops it; `project_stock_from_lots` and `sync_sku_to_new_inventory` both survive a NULL; the backfill fills `display_name` from the library for the 53-of-206 rows whose `wine_name` is blank; the legacy insert path still derives `wine`/`bottle`; a declared keg keeps `beer`/`keg`; a duplicate keg name, an out-of-vocabulary `uom` and a row claiming both catalogues are each refused; and **a non-wine lot writes and projects `stock_live = 4` onto the keg** — the decision, end to end. **No DEFAULT on any new column, asserted in the migration**, because `unit_type DEFAULT 'BOTTLE'` was measured silently characterising a test keg as a bottle; a `BEFORE INSERT` trigger derives the wine path instead and **raises** for a row with no library link and no declared kind. The gate is one line of app code: `inventory/inventory.service.ts:69` reads `master_wine_library?.bottle_size_ml ?? 750` and divides by the pour size, so a keg would publish as five glasses — phase 2 item 1, in the same change or the migration does not land. Guard proven exit **2** on the unmigrated control and on an unreachable database, **1** on a stock row naming a house item that does not exist, **0** on a correct one, with every un-exercised invariant printed as `NOT CHECKED` rather than counted clean; not wired into CI on purpose (a blocking guard against an unapplied migration fails every build — the parent wires it at lock time). Supersedes `INVENTORY_SOTA_PLAN.md:352`'s `domain`/`subsection`/`subtype` identity paragraph and `:134`'s `NOT NULL` lot key by retire-to-write; that file gets no edit. §13 pointers in [[wines]] and [[inventory]]. **Three sub-decisions taken by the founder 2026-09-04 and built into phase 1 or its phase list.** (a) **Retirement, not deletion**: the library FK becomes `ON DELETE RESTRICT`, soft-delete (`master_wine_library.deleted_at`) is the only retirement path, a `BEFORE DELETE` trigger makes the refusal name the count (*"1 house item row(s) across 1 house(s) still key on this wine … retire it instead"* — the bare FK message names no house, no count and no remedy), and a link to a retired wine is **flagged, never cascaded**. Measured before changing it: `deleted_at` is already exercised (**664 of 4 226** rows), **zero** live functions hard-delete from `master_wine_library` (`merge_library_wines()` was converted by `20260817120000:3,15`; the two `DELETE` lines left in the tree are superseded bodies), and **0 of the 199** stocked wines are retired — so RESTRICT breaks no path that exists, and an **unstocked** wine stays deletable, proven, so no catalogue row becomes immortal. (b) **A house item exists only through an explicit "carry this"** that states kind and unit in the same step; a menu, invoice, quote or till line matching nothing **stays unmatched and says so** — auto-creating a zero row would supply a kind and a unit nobody stated, which is the silent characterisation `NO DEFAULT` exists to stop, arriving through the front door. (c) **The first enrichment writer funded is beer style and IBU** — measured `type_attributes = '{}'` on all 608 catalogue rows, so 57 beer rows carry 0 styles and 0 IBUs; two writers (the house at carry-time via a picker into a new `house_attributes jsonb` added by *phase 2's* migration, and the catalogue where a `beverage_id` match exists), house wins, every value states which writer spoke and when. Guard gains invariant 7, split on severity: a dangling link or a non-RESTRICT FK **fails**, a retired-wine link is **FLAGGED** and never fails, because a house pours out a retired wine over weeks and failing the build would punish the correct action — proven exit **1** on a persisted dangling-link fixture and exit **0 with a FLAGGED line** on a retired-wine one, 10 of 10 self-test probes green. The style vocabulary is settled too: the **BJCP list with an `other` escape** — an off-list style is typed as free text under `other` and **flagged for the catalogue**, style sorts and filters, and `other` sorts last. The escape is what makes a closed list survivable: without it an operator must file a real beer under a wrong style, and a wrong style is indistinguishable from a right one downstream where `other` announces itself; the free text is display-and-triage only and may never be promoted to a style by a matcher. Questions 1, 3 and 6 now closed; 2, 4, 5 open | 2026-09-04 |
| [0116](0116-a-threshold-stops-an-order-and-a-default-is-not-an-answer.md) | **A threshold stops an order, and a default is not an answer** (Locked — the founder decided all three in session on 2026-09-03). Three findings the settings register filed rather than built, decided together because they are one fault seen from three sides: **a value nobody stated, treated as though somebody had.** **(1) The thresholds enforced nothing.** `approveOrder` wrote `status`/`approved_at`/`approved_by` and read neither a role nor an amount, and `POST /procurement/orders/:id/approve` carried `JwtAuthGuard` alone — so anyone who could reach the endpoint could seal any figure while `/settings` `?tab=thresholds` rendered a policy. Worse, `PUT /settings/approval-thresholds` had no role check either, so **the person a ceiling stopped could raise the ceiling** — not a weaker policy than none, a policy that reports itself as holding while it is not. Founder: *"only certain high tier like manager or owner can adjust it, do option 1"*. Built: `assertApprovalAllowed` reads the order, reads the rules through **the same `decideApproval` the register renders** (two implementations of "does this need an owner" is how a policy page and a policy diverge), resolves the actor's role via the existing `assertCanManageRestaurant`'s lookup lifted into `resolveRestaurantRole`, and refuses with the rule, the number and who may sign in **one sentence the page prints verbatim** — a person told only "forbidden" learns exactly one thing, split the order in two. The refused order parks in the existing `APPROVAL_NEEDED` (no migration: `status` is `varchar(50)` with no CHECK) and the refusal files as `order_approval_refused`. Three cases carry the honesty: a house with **no** rule seals as before and says *"no threshold is set for this house"*; an **unreadable** policy REFUSES (a table that cannot be read has not said "anyone, any amount"); and `null` from the role read — which means both "no row" and "the read failed" — **satisfies nothing**, or a database outage would open the ceiling. Warn-only was rejected on the argument that a warning nobody is stopped by is ignored within a week and cannot answer the founder's actual question. **(2) Three column defaults asserted facts nobody stated**: `providers.lead_time_days DEFAULT 7` (`baseline:4864`), `payment_terms DEFAULT 'Net 30'` (`:4897`), `restaurants.timezone DEFAULT 'America/Los_Angeles'` (`:3575`) — none distinguishable from a real answer, which is the definition of the fault. Dropped, and every row equal to one set to NULL, **with the cost stated rather than buried**: real answers are erased with the fabricated ones because no query separates them; dropping is recoverable (the term is stated again, now with a name and a date), keeping is not. Proven on local Postgres in a rolled-back transaction — 2/2/1 rows cleared from seeded data while a provider with a stated `21` days / `2% 10 net 30` **kept both**; **production counts NOT measured**, the migration counts and `RAISE NOTICE`s them at apply time. The reader sweep across all four runtimes then found the fault had **copies**: `AddProviderModal`/`EditProviderModal` seeded `paymentTerms: 'Net 30'` in the browser (would have refilled the column on every save), `organizations.service.ts:577` wrote `"America/New_York"` for a location created without a zone, and `scheduled-tenants.service.ts:135` substituted New York for a NULL — now `TIMEZONE_NOT_SET`, an empty string that is not a valid IANA zone, so both consumers' existing unknown-zone branches log and run in UTC. Two claims in `settings.md` §9.12 were **corrected by measurement**: `payment-due.template.ts:108` already printed nothing for an absent term, and **nothing calls the mailer at all** — its only invocation is `tests/email-e2e.spec.ts`. `vendor-terms.service.ts` lost both default-equality branches, which couples it to the migration; that coupling is named in Consequences. **(3) The delivery-days checkbox wrote weekday names into `providers.regions_covered`** — the geography column the map and territory filters read — while `deliverySchedule` never reached a payload, and `EditProviderModal` read `regionsCovered` **back into** the picker so opening and saving wrote them again. Repointed at `PUT /vendor-terms/:providerId`; the edit dialog seeds from the terms register and **disables the picker with the reason in words** when that register cannot be read, because an empty selection is itself a statement ("no fixed days") the page would save. Cleanup was refused on principle: `regions_covered` is free text and a "Sunday" in it cannot be proven a picker artefact rather than a place (Sunday is a town in Louisiana), so `scripts/list_weekdays_in_regions_covered.py` **proposes and has no `--apply`**. 39 gateway + 18 web tests; migration `20260903170000` | 2026-09-04 |
| [0117](0117-a-price-sighting-names-its-source-its-date-and-its-unit.md) | **A price sighting names its source, its date and its unit** (Proposed — research only, nothing built; five founder questions open). Asked which register to fill first, the founder said *"find vendors who can provide them or websites who are free to fetch data, make sure they are verifiable and works perfectly, rate limiting is fine for now."* `vendor_price_observations` is well built and, measured on production **2026-09-04**, holds **0 rows** — as do `price_history` (0), `procurement_documents` (0) and `procurement_document_lines` (0), so the `/notifications` market box, the market producer, the calendar price mark and every register's `quote` line are silent by construction. Five measurements reshaped the answer. **(1) The house's own paper never reaches this register:** `price_history` finally has a writer (`procurement.service.ts:900`, called at `:2902` receipt-verified and `:4393` order-confirmed) but it writes a *different table*, and no invoice-tier row is written to `vendor_price_observations` by anything. **(2) The largest body of prices the house owns is inadmissible:** `master_wine_library` carries `price_reference` on **3,674 of 4,226** rows, **3,474 of them from `source='menu_corpus'` — restaurant menu list prices** — with **no date column and no issuer column**. **(3) A live 200 is not freshness:** `https://www.ams.usda.gov/mnreports/bh_fv020.txt` returned **HTTP 200 carrying a report dated 03-JAN-2024, 975 days stale**, announcing its own retirement only in prose in the body — the absence-reported-as-health fault arriving through the front door. **(4) Jurisdiction decides everything and has no column:** the estate is 3 Michigan · 3 Illinois · 3 California · 2 Türkiye · 1 UK · 2 unstated, and only Michigan (licensee price book) and California (public beer postings since 2023-10-15) have a posted list — **both refused this environment's fetcher today (403 / TLS chain)**, while the two sources that parsed perfectly serve **zero** tenants. **(5) Filling it does not light the box:** `MARKET_WINDOW_DAYS=30` with `MIN_BASELINE_OBSERVATIONS=3` *earlier* sightings needs **four sightings in thirty days**, and a monthly list plus the `(source_ref, content_hash)` dedup index yields **one**. Decision: a row enters only if it names **what number it is, who published it, when, in what unit, and where it is a price**; five source classes (**A** own paper · **B** posted wholesale list · **C** licensed distributor feed · **D** retail reference · **E** public index) with a comparison rule per class — a vendor quote against this house's own history and same-state postings only; D and E as their own line, never beside a quote (ADR 0111's rule, re-derived from the data). **Three of the nine provenance fields have nowhere to go today** (issuer, jurisdiction, price basis) and `vpo_source_type_check` admits **no value meaning "a government's posted list"**, so a migration is a precondition, not a follow-up; `is_outlier` — `DEFAULT false` with **no writer**, while `belowTrailingAverage` filters on it — gets the engine's existing MAD test (`flagOutliers`, threshold 3.5) run per group after a batch, never as a bound. Fill order: **own paper first** (no fetch, no terms, best provenance), then class B/C in the tenant's own state, then D/E in their own register. The leading candidate, **Iowa Liquor Products** (13,762 rows, keyless, CC BY 4.0, `report_as_of=2026-09-01`, 100% price/volume/pack/UPC coverage), was attacked and **demoted from B to D on six measured counts** — spirits only, `state_bottle_retail` is `state_bottle_cost` × **exactly 1.50** at the median, `vendor_name` is the producer not a distributor, no Iowa tenant, `restaurant_id NULL` would surface it in **every** house's box (`vendor-comparison.service.ts:341`), and the cadence can never clear the bar. Parsing all 13,762 rows found the source's own defects: **29 rows where bottle cost × pack contradicts the published case cost** (item 920301 publishes a $1,250 bottle cost for a $60 bottle), **11 rows with `bottle_volume_ml = 0`**, **2,308 duplicate item numbers**. Registry of 27 sources with today's fetch result each: `.planning/07-reference/price-sources.md`. Proof: `scripts/fetch_price_sightings.py` (dry-run default, `--apply` refused with its three blockers, `--self-test` green over recorded fixtures) | 2026-09-04 |
| [0118](0118-the-house-writes-its-own-mail.md) | **The house writes its own mail** (Proposed — built behind `mudavym_design_communications`; the founder decided the sender rule, the send costs, the recipient rule, the paid tier and the staff-broadcast exclusion in session on 2026-09-03/04). `/communications` could not write a letter, and every vendor letter this deployment has ever sent left from **one mailbox shared by every restaurant on it** — `GMAIL_SENDER_EMAIL` falling back to the literal `notifications@wineops.ai` (`gmail.service.ts:80`); the sign-off carries the house's name and the envelope carries ours. **D1 the sender**: the house's own connected mailbox, or a Mudavym subdomain address we provision — never the shared one; when neither exists Send is disabled carrying the reason and the route refuses 409. The subdomain is a **paid-tier** line and a free house sends from its own mailbox; **the price is OD-23 and appears nowhere in the build**. The identity is resolved from a **stored scope, not a flag** (`integration_oauth_connections.scopes`, `20260826170000:133`), and **measured 2026-09-04 no house has one**: `INTEGRATION_DEFINITIONS` declares `google_drive` and `excel` only (`integrations-oauth.constants.ts:36-98`) and neither requests `gmail.send` — `google_drive` lists "Your Gmail messages" under `notRequested` (`:64`). Widening that grant to light the button up was **refused**: people consented to file access and were told sending was not requested. Four states, not two — `none` and **`unknown`** (a failed read) are different sentences. **D2 the cost of Send**: a plain button plus a **server-side 2-minute undo window** on the house's own mailbox — not a new number, it is `AUTO_SEND_UNDO_MS` at `inbound-responder.service.ts:36`, the window the AI reply path already stages behind — and the **seal** on the Mudavym domain, where one house's letter moves every other house's deliverability. The window is a **row**, not a timer: a `setTimeout` in the request's process loses the letter to any deploy, and a client-side undo is ADR 0083 pointed the other way (a page may not offer to undo what already happened). The queued status is `HOUSE_QUEUED`, deliberately **not** `AUTO_SEND_SCHEDULED`, which `processScheduledAutoSends` selects (`procurement.service.ts:3739`) and would have dispatched a human's letter through the deployment mailbox. **D3 recipients are the book**: no free-text To; an unknown address **creates the vendor contact first** through `POST /providers/:id/contacts` (`providers.controller.ts:377`), and the server's refusal **names the addresses that are on record**. **D4 the merge unit is the engine's whole sentence with its provenance**, never a figure re-derived from one — the sketch surveyed **ten mail products and every one substitutes a plausible value for a missing merge field**, so there is deliberately **no "insert a figure" control**; the client's chips are **re-read server-side** before the letter records them, in the new `inserted_insights` column. **D5 the guardrails over a human draft**: of the AI path's five, exactly **two transfer** — `commitment_language` **blocks** (the human is the approver, so routing to a human is not a move) and `max_rounds` is **stated, not blocked**; `price_above_target`, `qty_or_budget_change` and `sender_unverified` all read an inbound message that does not exist. One is **added**: an unfilled `{{merge_field}}` blocks, because a raw placeholder in a vendor's inbox claims a figure was found. **D6** a staff broadcast is **not** a composer template (founder, 2026-09-04) — vendor book only, crew messages stay on `/team`. **D7** the two legacy builders (`GmailTemplateBuilder` 1,683 lines, `SMSTemplateBuilder`) are **retired from the rebuilt page**, untouched and still mounted by legacy `/communications` (`Communications.tsx:589,598`), with the retirement asserted **as a rule** by a test that reads every `next` file's source. Migration `20260904150000` is a **precondition**: until it applies the template route answers 400 **in words** and the library renders "unknown, not empty" (verified live on `:4000`). `CommunicationsModule` provides `IntegrationsOauthService` **from its class**, because importing `IntegrationsModule` behind a `forwardRef` closed an ES-module load ring and `check_gateway_boots.sh` refused to boot — `forwardRef` defers Nest's graph, not Node's `require`. `Sheet` gains a `wide` prop (640px), ADR 0112's one anticipated exception. 21 gateway + 45 web tests. **D12-D15 retention (founder decided all four parts 2026-09-05):** a mirrored reply is TWO objects — the RAW MAIL (`message_text`, `email_headers`, `content`, attachment bytes) gets a window and goes on revocation; the FACTS the understand step wrote (`conversation_context.analysis`, `rolling_summary`, `negotiation_facts.exact_quote`) stay under the house's bookkeeping floor. **The window is derived, never a constant**: the longest dispute the house recorded (`procurement_credits`, measured from the first message on that order to settlement or today) plus a margin of **92 days = one re-derivation interval**, because the figure is only re-derived quarterly and a shorter margin could expire mail on a three-month-old figure; a house with no dispute gets the margin alone and `longest_dispute_days` is **NULL, never 0**. Measured 2026-09-05, the readable production tenant has zero credits and zero conversations, so that is the ORDINARY branch here. **The floor for the facts is per house from `restaurants.country`**, each row naming its statute with the URL and the fetch date (TR 10y — TTK 6102 Art. 82 + VUK 213 Art. 253, both read from `.gov.tr` primaries this pass, closing `messaging-senders.md` §8.6's secondary-source caveat; GB 6y; US 7y; US-CA 7y + CCPA's disclosure duty), and no country recorded means the **strictest rule plus a printed sentence saying why**. **The primary Turkish text moved the finding**: TTK Art. 82(1)(b) reaches “alınan ticari mektupları” — the commercial letters RECEIVED — and 82(2) defines those as all correspondence about a commercial matter, so Türkiye's ten years reaches the mail itself; this build deletes Mudavym's MIRROR on the reading that the house's own Art. 82 record is the message still in the mailbox, and files that reading as a founder question rather than settling it. **Revocation deletes immediately**, scoped by the new `procurement_conversations.mirrored_by_grant_id` (the row did not know its grant — the reader published `source: "house-inbox"` and the bridge never read it), tombstones the body rather than emptying it (`message_text` is NOT NULL), removes the attachment bytes, notices the grant's owner through `persistForRestaurant`, and **records a count whether or not anything changed**; `disconnect` **throws** rather than reporting success for a revocation whose deletion could not run. The consent screen prints all of it from `GET /communications/retention/disclosure` and **disables Continue for a mirroring grant when it cannot read the figure**. Migration `20260905190000`, assertions EXECUTED under PGlite. No per-house ML model is built: not wanted until asked. +23 gateway + 7 web tests | 2026-09-05 |
| [0119](0119-an-agreed-price-states-its-unit.md) | **An agreed price states its unit** (Proposed — research only, nothing built; seven founder questions open). The full graph behind ADR 0117's Q6, on the founder's *"research. cover every angle."* **The asymmetry, measured at `e8a7d6f5`:** the *invoice* line states its unit on the row — `uom NOT NULL` (`baseline:4387`), `pack_size NOT NULL` (`:4388`), `unit_price` (`:4391`), plus `allowance` (`:4393`) and `deposit` (`:4394`) for the money that sits **outside** the unit price — and the sibling register `price_index_postings`, shipped the same day for classes B/D/E, requires `price_unit NOT NULL` (*"'per package', 'per bottle', 'per case'. Named, not assumed"*, `20260904200000:96`). The *agreement* states none of it: `procurement_orders` carries four price columns (`:4523-4525`, `:4559`) and **not one names a unit**, while the row beside them states the unit of its quantity. So **class A is the only ADR 0117 class whose price may be filed without a stated unit** — which is why the mirror refuses every case-priced agreement, and refuses a **known** pack of 12 as firmly as an unreadable one: knowing what a case holds does not say which of the two the price is in. The per-bottle reading is enforced by arithmetic, not schema — `totalCost = finalPrice × bottlesTotal` (`procurement.service.ts:469`) and `line_total = finalPrice × units.bottlesTotal` (`:819-821`) written onto a row whose column is named `final_unit_price` and whose `unit_type` may say `case` (`:842-843`) — and it **leaves the building wrong**: `confirmDeal` mails a vendor `${quantity} bottles` (`:4810`, where `quantity` is the count in the order's OWN unit, `:4701`) `at $X per bottle` (`:4804-4806`), so a five-case order reads *"5 bottles"* for 60. **Six options across five cost surfaces** (migrations · readers · the receiving door · the price register · ADR 0070's `uom` rule). The free option — derive the bottle price from the case price and the pack — was killed on evidence: Connecticut **defines** the posted bottle price as case ÷ pack **plus 2–8¢ by bottle size** (cga.ct.gov 2004-R-0593), New York lets a discount attach to one unit and not the other (sla.ny.gov), split-case fees and CRV deposits break linearity in the warehouse, and back-deriving pack size **at this exact table** is the documented cause of the receiving door's pack-size defect (`procurement.service.ts:1259-1268`). Putting the price on the vendor catalogue was killed by measurement: `vendor_catalogue` (`baseline:6230-6247`) is a vendor **directory** — no product, no price, no pack column, and no catalogue-line table exists. Seven reference systems checked with URLs and fetch status marked per source; the four that publish a schema (xtraCHEF's UOM/Pack/Size/Unit quadruple, Restaurant365's Vendor Items, Odoo's `uom_po_id`, NetSuite's purchase units) **converge**: none stores a bare price, and none derives the purchase unit from the stock unit. GS1 supplies the third invariant — a pack change requires a new GTIN, so a pack change is a new agreement line, never an edit. **Recommended:** the agreed price carries its own `(price_uom, price_pack_size)` pair on `procurement_order_items` — ADR 0070's shape applied to money — with a NULL pair staying a refusal, no backfill, and header `final_price` demoted to an echo; plus a **phase 0 that needs no migration**: stop the confirmation email claiming a unit and a count it has not checked. The counter-argument is stated at length and its residue conceded: a CHECK can constrain the vocabulary but **cannot** prove the number is in the unit it names, so O1 makes a mis-keyed price auditable, not impossible. Page landing: `06-pages/orders.md` §13 | 2026-09-04 |
| [0120](0120-a-goal-comes-from-a-book-a-model-comes-from-the-task.md) | **A goal is chosen from a book of scenarios; a model is chosen by the task** (Locked 2026-09-06 by the founder, batch 60; was Proposed — built behind `mudavym_design_reports` / `mudavym_design_recommendations`; **all four founder questions now settled**, the last two on 2026-09-05). Two founder instructions of 2026-09-04, recorded together because one pays for the other. **(a) The book.** Both goal sheets opened on a dropdown of six metric KEYS, so a manager asking *"what should we hold ourselves to?"* got a vocabulary and no book. `analytics/goal-scenarios.ts` is a static, tenant-free catalogue of **21 scenarios** served by `GET /analytics/goal-scenarios` — **10 held today, 11 that name the measure they would need** (9 and 12 as first built; the founder funded `days_of_inventory` the same day), each carrying the cutting that draws it, the rules whose prescription moves it, and a RANGE quoted **in the source's own words with its URL and its date**. **No row carries a target**, and the test asserts on the KEYS so one added later fails even if left undefined: a median across 900 houses sitting in a target box is a number this house did not choose, in the place a number this house chose belongs. The research produced two findings that shaped the table more than any single figure: **published ranges exist for RATIOS and almost never for LEVELS** (the NRA publishes a 32.0%-of-sales food-cost median and a 36.5% labour median for fullservice 2024; nobody publishes what a room's wine revenue or cover count should be) — so **five of the ten servable scenarios honestly carry no range**, with the reason; and **three of the twelve gaps are nearer than they look while one is further** — `/analytics/financial/:rid` already computes `cogsRatio`, `primeCostRatio`, `inventoryTurnover` and `daysInventoryOutstanding` (`analytics.service.ts:444-467`), but `primeCostRatio` takes `labor` defaulting to **0** (`:396`) and **no caller in the repo passes it**, so today's "prime cost" is a COGS ratio wearing a prime-cost name, over a sell-price valuation rather than POS revenue. Unservable rows are **listed and disabled**, never hidden: a picker of ten would say this product covers the field. On `/recommendations` the book is an OVERRIDE and states its cost — swapping the measure **drops `source_rule_key`**, so that entry stops showing as watched, and the sheet says so before the button. The producer is **derived from direction**, not stored, because the two producers' own filters are the truth (`goal-reached.producer.ts:121` / `ceiling-held.producer.ts:95`). **(b) The routing.** *"Sonnet 5 for asks, all tiers, metered"* — and, asked, the founder agreed a lookup or help turn should use Haiku. `common/model-client/model-routing.ts` maps **task class → model** (`lookup`/`help` → the undated `claude-haiku-4-5`, `compose` → `claude-sonnet-5`), precedence **site env → class env → default** so a running gateway's `ASK_AI_MODEL` keeps its meaning. *"All tiers"* is load-bearing and the file reads no tier: the tier bounds SPEND, never QUALITY. Measured first: `neural_footprint_event` (`20260824141116`) **already** records count, tokens, cost, restaurant and model for all nine model sites, so there is **no new table and no migration** — only `task_class`, `model_routed_by` and `asked_by` are added, as literal keys at each call site, with `asked_by: null` (never a placeholder) when the site cannot name who asked. The routing change would have **silently disarmed the spend ceiling**: an unpriced model writes `cost_usd = NULL`, NULL sums as nothing, and `retryAllowedBySpendCeiling` sums `cost_usd` — so `claude-sonnet-5` ($2.00/$10.00 against Haiku's $1.00/$5.00) was added to the pricing table in the same change and `isModelPriced` is exported so a test asserts no class routes to an unpriceable model. **"Ask the book" on `/reports` calls no model at all** (it searches the deterministic feed), so the ask paths that route are exactly two: the goal cutting spec and Ask AI's proposal. `ConsultantsService`'s Opus call was **deliberately not routed** at first — a fourth class nobody had decided; **the founder decided it on 2026-09-05: *"Fourth class: consult."*** `consult` joins the registry at the SAME `claude-opus-4-8` it already ran (naming a class must not swap a model), with its own `MODEL_FOR_CONSULT` and its own `task_class` in the ledger — the rejected fold into `compose` would have let one incident override move the most expensive call in the gateway and would have made "what does composing cost" unanswerable from the rows. **And *"Not yet; request a scenario instead"*** answers the last question: a house may not author a scenario, but it may REQUEST one in words — `public.goal_scenario_request` (`20260906090000`) holds the house, the person, the words and the time, and no status column, because nothing would ever write one; the write is a plain authenticated POST with the actor read from the token (not sealed: a request moves no money), the read is the founder's alone (`ServiceKeyGuard`, ADR 0099), a failed read THROWS rather than rendering as "nobody asked", and nothing joins the table into the book — the catalogue stays one truth. **Amended the same day after an audit: five of the fourteen citations were wrong** — `labour-cost-ratio` quoted *"a median of 34.2%"* against the NRA page that carries 36.5% and 31.7% and contains neither `34.2` nor `pre-tax` (the clause is on the abstract's *profitability* page); two DCL fill-rate quotes are not on the DCL page at all (they came from a blended search summary); two dates were copied from the wrong NRA page; three rows carried a bare `"2026"` for pages that state **no date**; and `revpash` quoted figures against a URL that **403s**. `__fixtures__/operator-sources.ts` now records the fetched text per URL with the fetch date and the page's own date, and the spec asserts **every numeric token a row quotes appears in the page that row names** (URLs stripped, so a date in a path cannot satisfy itself), that `published` agrees with the page in **both** directions so `"undated"` cannot drift into a year, and that **no figure is ever quoted against a source that could not be read**. 37 gateway + 14 web tests; live route and both grounds captured on `:4000`/`:5274` | 2026-09-04 |
| [0121](0121-the-houses-text-sender.md) | **The house's text sender** (**Accepted 2026-09-05 in three parts; the transport stays Proposed**). The founder decided: *"a crew text exists and build it next"*; the first market is **both** (Türkiye WhatsApp-first, the US on SMS); and **both** ways of getting a number are built — *"the house must either brings their own name and we have to make sure the connection is secure or with mudavym help buys per house and bills with info"*. Built: `house_text_senders` (ADR 0114 shape, six states, no default, fee and timeline kept as sentences, a vault POINTER and never a credential), `person_text_consents` (withdrawable, no approval axis at any layer, withdrawal is a timestamp because 47 CFR 64.1200(d)(6) wants five years of it), `team_note_deliveries` (**one row per recipient per channel, written whether or not anything was delivered** — the P0 fix for `notified: 11` against 0 devices), one `TextSenderService` that **refuses every time** and names which half is missing, and three surfaces. **Nothing sends and nothing is submitted:** no per-house provider credential exists, and the sealed submit act has no route at all, so the seal is satisfied vacuously and the ADR says so. Research corrections from primary sources fetched 2026-09-05: US 10DLC is **13-20 business days**, not "several days or even several weeks"; a Meta business portfolio is capped at **2 phone numbers, raisable to 20** — the WhatsApp analogue of the 100-campaign TCR cap; Twilio's US guidelines list **"shared phone numbers"** among restricted use cases; a US campaign is **rejected if consent is a condition** of buying or holding an account; and the Türkiye Sender ID is wet-signed, stamped paperwork on the house's own letterhead that no form can automate. Still open: OD-23 (who pays — the fee **recurs per house**), and the Türkiye WhatsApp-usage figure still could not be fetched. The founder removed SMS from the crew broadcast *"until the house has its own sender"* and asked whether each person's own phone could carry the house's messages. Measured on production 2026-09-04: the deployment's sender is **Plivo, not Twilio**, one `PLIVO_PHONE_NUMBER` for every restaurant (`communications/sms.service.ts:30-33`); **0 of 21 providers** are reachable by phone only; **0 of 11 `team_members`** carry a phone; **0 `mobile_devices` rows** exist while `broadcast` reports `notified: 11` and delivers nothing (`team.controller.ts:521,527`, `push/expo-push.service.ts:83`) — the crew broadcast claims a reach it lacks. Every per-person path is closed by terms (iOS has no send API, Play's default-handler rule, WhatsApp's non-personal-use and bulk clauses, Signal, the MSP gates); `apps/mobile` ships neither `expo-sms` nor `expo-contacts`. Türkiye: two-way SMS unsupported, sender-ID registration required, unregistered IDs blocked from 2026-11-18; WhatsApp Cloud API free-form inside an open 24-hour window is free since 2025-07-01 — the shape of this product's traffic. **Recommended:** WhatsApp as a house-declared connection under ADR 0114 first (reply-shaped, mirrored into `procurement_conversations` before it renders anywhere); SMS second and **per house** — never a shared number, never a Mudavym brand with per-house campaigns (100-campaign TCR cap, $1,000 snowshoeing fee, $10,000 per content violation); a person's own phone is a **hand-off** recorded as `HANDED_TO_PERSON`, never a send. Phase 0 ships no send at all: it fixes the push count, because *wait for a sender* is only honest once the product stops claiming reach it lacks. Counter-argument conceded: WhatsApp-first puts the vendor thread in Meta's custody and Meta may pause a number mid-dispute; and the *Türkiye is a WhatsApp country* premise rests on a TurkStat figure that could not be fetched — the only usage figure fetched from a primary source is the US one, **32%**. Registry: `07-reference/messaging-senders.md` | 2026-09-04 |
| [0122](0122-a-foreign-key-names-a-table-that-exists.md) | **A foreign key names a table that exists when it is written** (Proposed — guard, pytest and CI wiring built; the fault itself already fixed in `88d7b5a8`). `29e439c4` shipped `REFERENCES public.user_mcp_connections(id)` at `20260904230000_a_tool_the_house_has_seen_before.sql:52`, against a name ADR 0114's migration had renamed away the day before (`20260903151000_the_house_declares_a_person_consents.sql:63-64`). `supabase db reset` stops on that file with `42P01` and takes every later migration with it. The TypeScript read had already been corrected; only the SQL and its prose were left behind. **Measured against a temp tree holding the pre-fix file** (`git show 29e439c4:` in place of the fixed one, `scripts/` copied real and the rest symlinked, so the worktree's migrations were never touched): `check_queried_tables_exist.py` exits **0** — it reads TypeScript and Python call sites, and `grep -c REFERENCES` on it is **0**, the string does not occur in it at all — and `check_fk_repoint_by_referenced_column.py` exits **0**, since its four `REFERENCES` occurrences (`:322`, `:355`, `:432`, `:438`) are its own self-test fixtures. Those two are the only files in `scripts/*.py` that contain the word. `schema-parity.yml` **would** have gone red — it is required and it really runs `db reset` — but after a push, at a full round trip, naming the migration that failed rather than the rename that made it fail, and `skipped` on a red `main`. **Built:** `scripts/check_fk_targets_exist.py` walks the migrations in filename order keeping the live table set (`CREATE TABLE` / `ALTER TABLE … RENAME TO` / `DROP TABLE`, schema-qualified or bare, **inside `DO $$` blocks too** — the rename that caused this lives in one), and requires every `REFERENCES <name>` to resolve at the position it is written. Exit 0 with counts, **1** naming the key's `file:line` **and** the rename's, **2** when it cannot check — an unterminated construct, dynamic DDL that would shrink its model of the schema, or a corpus with zero foreign keys, which is the extraction having rotted rather than health. Two near misses were measured, not guessed, and set the refusal boundary: the bare word `'references'` is an RFC-822 header the baseline reads out of jsonb (`20260805000000:455`) and a bare `alter table` is OD-73 enabling RLS in a loop (`20260825200000:273`) — refusing on either made the guard exit 2 on the whole corpus. **Proof pasted in the ADR:** pre-fix tree **FAIL**, naming `:52` and the rename at `:64`; current tree **PASS**, 329 keys checked, 4 skipped as external schemas. `--self-test` 17 shapes, `scripts/test_fk_targets_exist.py` 31 pytest cases (two read the repo; the pre-fix one `skip`s rather than passes if `29e439c4` is unreachable). Wired into `ci.yml`'s `schema-code-parity` job with `if: !cancelled()`. **Named gaps, not forgotten ones:** a conditional rename is treated as taken; a table dropped *after* the key that references it needs a backward walk and is a different guard; columns are not checked; and the four keys aimed at `auth.users` (`baseline:12814`, `:12854`, `20260825140000:105`, `:110`) are skipped and counted, **not blessed** — an actor FK to `auth.users` is its own live defect and needs a guard that knows about ids. | 2026-09-04 |
| [0123](0123-the-openapi-export-is-a-command-not-a-commit.md) | **The OpenAPI export is a command, not a commit** (Proposed — the founder's call of 2026-09-05; closes [OD-89](OPEN-DECISIONS.md)). `apps/api-gateway/openapi.json` was a **tracked file written by a side effect**: `main.ts` wrote it on any `NODE_ENV !== "production"` boot, so running the gateway for any reason on any branch rewrote ~7,900 lines of a committed file, and the comment above the write named a CI/CD consumer that does not exist. **Measured on `wt-p4` at HEAD `3ab6302a`, 2026-09-05:** nothing reads it — `git grep -n "openapi\.json" -- . ':!.planning'` returns the two writers plus six hits in `services/agent-orchestrator/main.py` that are that service's own **HTTP route** `/openapi.json`, served from memory by FastAPI, a different thing wearing the same name; `git grep -ln "openapi" -- .github docker '*Dockerfile*' '*railway*' '*nixpacks*'` prints **nothing at all**; and `git diff --numstat` on the file showed **11834 insertions / 7910 deletions** already sitting in the shared worktree from another builder's boot in a session about something else. It had also **flipped a guard**: `check_decision_claims.sh` read `226 checked, 225 holding` with OD-89 **STALE** and exit FAIL, because that claim greps the *working copy* and a boot had regenerated it — a boot side effect editing the decision register. Two assumptions were measured and are false: `check_gateway_boots.sh` does **not** rewrite the file (md5 `90835af8…` before and after — it resolves the DI graph, it never runs `bootstrap()`'s Swagger step) and neither does `check_decision_claims.sh` (it greps). **Built:** `openapi-export.ts` — `shouldExportOpenApi` is true only for `EXPORT_OPENAPI` = `1`/`true` (unset, empty, `0`, `false` all mean do not write: absence is never consent) and `maybeExportOpenApi` **returns whether it wrote**, so no caller announces a write it did not make; `main.ts` and the `openapi.ts` script both route through it on the **same** flag, and the script now exits non-zero and says so rather than silently writing nothing; `openapi:export` sets the flag; `.gitignore` gains the path; `apps/api-gateway/README.md` documents the command. Both OD-89 CLAIMS rows are **rewritten as `resolved`, not deleted** — the second greped the file itself and would have read its absence as a failure, the cannot-run-certifies-itself shape ADR 0025 §5 bans — and both now name files that exist, so a rename shouts. **Proof:** 14 jest cases (`jest --runInBand --forceExit src/openapi-export.spec.ts` from `apps/api-gateway`, 14 passed / 1 suite). The spec deliberately does **not** boot Nest — `bootstrap()` opens a real Supabase client against production — so it proves the decision function with a spy writer and the call-site shape separately, and runs the shape predicate over `git show 3ab6302a:…main.ts`: **pre-fix FAIL "BOOT WRITES THE SPEC", current tree OK**; that comparison `skip`s loudly if the sha becomes unreachable. **Not mine to do:** `.gitignore` does not untrack an indexed file — `git rm --cached apps/api-gateway/openapi.json` is the parent's step, and until it runs the file is still tracked. | 2026-09-05 |
| [0124](0124-a-bottle-has-one-identity-and-every-price-names-it.md) | **A bottle has one identity, and every price names it** (Proposed — built on `feat/mudavym-design-p4`, migration NOT applied anywhere, no row written by anything that runs on its own). Answers [0117](0117-a-price-sighting-names-its-source-its-date-and-its-unit.md) **Q28** — the founder: *"Do the SOTA and best for scalability thinking there might be more in future"* — and [0119](0119-an-agreed-price-states-its-unit.md) **Q7** by construction. **Twelve sources fetched 2026-09-05**, three refusals recorded rather than worked around (`wine-searcher.com/trade/api` 403, `ttbonline.gov` connection reset, and Liv-ex's own LWIN page no longer publishing the digit structure — taken from a SECONDARY source and flagged). GS1's GTIN Management Standard 1.1 §2.3/§2.8 (a 381,277-byte PDF from `ref.gs1.org` after `www.gs1.org` 403'd twice) and LWIN's 7/11/16/18 forms agree from opposite directions that a bottle is **wine + vintage + size + pack**; CellarTracker (4.1 m wines) and Vivino both put **a person** at the end. **The measurement that set the schema:** the live Iowa file (5,425,785 B, 13,762 rows, `report_as_of` 2026-09-01) carries a check-digit-valid UPC on **100%** of rows and **1,736 of its 9,118 distinct UPCs name more than one product**, 343 across different volumes — one UPC covers two 50 ml samplers and a 1,000 ml Woodford Reserve — so `beverage_identity_keys` is deliberately **not** unique on `(namespace, value)` and the joiner **refuses** an ambiguous key at 1/n. **The measurement that killed the obvious alternative:** `master_wine_library.bottle_size_ml` is **750 on all 4,226 rows** (one distinct value — the column default) and 2 of 3,562 live rows name a format anywhere, so identity cannot be columns on the library; the library reaches an identity through a KEY, and one row may name a 750 and a magnum. **Built:** `beverage_identities` (generated `identity_key`, three-valued vintage, per-assertion provenance), `beverage_identity_keys` (namespaced: GTIN check-digit-verified to GTIN-14, LWIN, TTB COLA, source codes, our own ids — a new source adds a ROW, never a column), `beverage_identity_candidates` (`pending` at every confidence; a decided row must be DATED, by CHECK, and the service refuses a decision with no user id), nullable `identity_id` on `restaurant_inventory` / `vendor_price_observations` / `price_index_postings`, and the reader now groups by identity when it can and **says which key it used** per item. **The strongest counter-argument, measured through the real code against production:** the fixture register (41 identities) proposes **0 candidates for 608 real beverages**, and `restaurant_inventory` is readable as an identity **0 of 206** times from its own columns — but **205 of 206** through its library row. **Verification:** `jest src/vendor-intel` **302 passed / 16 suites**, 53 in 3 suites new here; gateway `tsc` 0 errors; `eslint --quiet` clean on 8 files; nine guards exit 0; `check_gateway_boots.sh` PASS — and `check_queried_tables_exist.py` **caught this build** (three `.from(variable)` sites, 26 → 29 unresolvable; proved pre-existing-clean on a `git archive HEAD` export, then refactored to literals). **NOT verified, stated:** no Docker and no local Postgres, so the migration was **never applied** — it parses under libpg_query (39 statements + 1 PL/pgSQL body) and nothing more; and the process on `127.0.0.1:4000` answers 404 to **every** gateway route including `/auth/me`, so no live curl was made. **Q2 ANSWERED and BUILT 2026-09-05** — founder: *"staff may confirm, log the decisions."* The queue, the confirm/reject and the log admit **staff** while the rest of `/vendor-intel` stays owner/manager, because the gate follows what a route EXPOSES and a candidate carries no price, no vendor and no terms; undo is owner/manager, refused twice. The log is a second, append-only table (`20260906030000_a_confirmation_is_a_logged_decision.sql`, trigger proved against a real UPDATE in the migration's own DO block) because the candidate table's `bic_decision_is_dated` CHECK means an undo would otherwise **erase the confirmation it undoes**; it carries the actor's name and role AS THEY WERE (`decided_by` is ON DELETE SET NULL) and the evidence captured server-side, never from the request body. +20 jest and +8 vitest cases; `check_route_exposure` PASS with 0 undeclared. **Q3, the naming rule and Q4 ANSWERED and BUILT 2026-09-05 (batches 48-49).** Q3 *"Provisional on the item, curated into the library"*: a generated `standing` (three values -- calling an Iowa transcription "official" would be the falsehood the register exists to stop), provenance in a column that is never cleared, a queue that is a QUERY not a table, and curation on its own `X-Admin-Key` controller because `RolesGuard` reads `request.user` and a service key carries none. The naming rule *"One alias on the item, library immutable"*: **no column was added** -- `restaurant_inventory.wine_name` already existed, was already preferred over the library name, and was measured on production at 180/233 rows with **0 differing from the library's own name** because nothing let a house set it; it does now, both names stay searchable, and a test reads the source to prove the library is never written from that path. Q4 *"LWIN search + hand nominations"*: the LWIN file is RECORDED not fetched, **is not in this repo** (Liv-ex serves it through a form; the page has no file link and three guessed paths 404), the search route says so instead of returning an empty list, and the fixture is synthetic and named so. +70 jest and +3 vitest cases. Q5 is another builder's | 2026-09-05 |
| [0125](0125-an-order-changes-state-through-a-sealed-transition.md) | **An order changes state through a sealed transition** (Proposed — researched and built 2026-09-05 on the founder's instruction *"research the current approach, find flaws and bulletproof ... and build the right option for future scalabilty and quality"*; he chose research over all three options he was offered). **The census, measured on `feat/mudavym-design-p4` at `611f7682`:** `procurement_orders.status` has twelve members and **eleven writers in three languages**, of which exactly ONE carried a from-state check (`parkOrderAwaitingApproval`, PENDING only). `DELETE /procurement/orders/:id` wrote CANCELLED from any state with an **optional** `?reason=`, no role check, no seal and no audit row, beside a `POST /approve` that redeems a one-time seal; `PATCH` moved an order to any of the twelve from any other, including `IN_TRANSIT` and `FAILED`, which **nothing in the codebase ever writes**; and the legacy desk — what production shows — sent **no reason at all** from three call sites, so `rejection_reason` was null for every rejection it made. **The flaw with teeth:** cancelling a DELIVERED order reverses nothing (the receipt event stands, the shelf stock stays booked) but the row leaves `ORDER_SPEND_STATUSES` and `ORDER_ARRIVED_STATUSES`, so its cost vanishes from every spend, cashflow, bottles-delivered, lead-time, on-time, HHI and vendor-scorecard figure — bottles on the shelf, money out of the books, one authenticated DELETE. Also found: **five divergent definitions of "terminal"** (5, 7, 7, 7 and 8 members) all green under `check_order_status_literals.py`, which enforces spelling and not vocabulary; a **cancelled order still emailed its vendor** because `processScheduledAutoSends` never even SELECTed `status` and the cascade filtered `PENDING_APPROVAL` only; **no `cancelled_at`/`cancelled_by` column exists anywhere in the repo** (zero grep hits); and `handleBulkReject` **called no endpoint at all** — it rewrote local state and alerted "N order(s) rejected", the twin of the bulk-approve defect [0116](0116-a-threshold-stops-an-order-and-a-default-is-not-an-answer.md)'s addendum removed, sitting in the same bulk bar as `SealedApproveDie`. **Four comparables fetched that day** (Odoo 18's six-state `Selection` with `tracking=True` and a `button_cancel` that refuses when a vendor bill exists; NetSuite, where "Closed — The purchase order has been canceled"; Restaurant365, which REQUIRES a reason on a denial and logs every event; Dynamics 365, which allows a cancel only "provided that the quantity hasn't been received or invoiced" and puts a cancellation itself through the approval workflow). **Built:** `order-transitions.ts` — a pure table in `credit-ledger.ts`'s shape, every edge DERIVED from the census so today's behaviour is permitted, forbidding only moves out of a terminal state, a cancel once goods have arrived, and running back down the delivery chain; an unreadable state is a refusal, never a pass. `cancelOrder` now requires a reason, checks the table, and redeems a seal for the new act `ORDER_CANCEL_ACT = "cancel"` (minted at `POST orders/:id/cancel-seal-challenge`, hashed over total + vendor + **status**, so a seal held open while the truck arrives is refused in the seal's own words) — the third act on the one `procurement_order` subject kind after `approve` and `deliver`, with **no file under `common/seal/**` edited by this pass**. A cancellation cascades `AUTO_SEND_SCHEDULED` AND the sender independently discards a staged reply on a closed order (belt and brace), and files `order_cancelled` in `system_audit_log`. The legacy desk's three Reject controls now OPEN a ceremony through one `SealedRejectDie`; the fake bulk reject is **removed, not made real**, because one reason pasted over fourteen orders is not an account of any of them. **Proof:** 46 + 15 + 13 + 7 = **81 new cases**, all passing; pre-fix probe of `git show HEAD:procurement.service.ts` at the same depth (run, then deleted) fails **13 of 15**, the two passes being the two "this does not break what the house does today" cases; `ORDERS_SOURCE=<HEAD copy>` fails **7 of 7** on the web contract. **A defect this pass's own test found in this pass's own design:** the first table allowed every same-state move, so an already-cancelled order could be cancelled again, overwriting its reason and filing a second audit row — fixed by `sameStateIsPermitted`. `check_read_errors_not_swallowed` went from FAIL to PASS at **191/191** because this pass RETIRED a baselined swallowed read (`preCancelRow`). Four founder questions: role-gating a cancel, the Python orchestrator's two direct terminal writes (a database trigger is the strongest answer), whether a vendor's no should return an order to NEGOTIATING rather than kill it, and whether bulk rejection should come back as a real ceremony. | 2026-09-05 |
| [0126](0126-a-price-behind-a-licence-is-not-a-posting.md) | **A price behind a licence is not a posting** (Proposed — built read-only and inert; answers [0117](0117-a-price-sighting-names-its-source-its-date-and-its-unit.md) Q19 and Q20 and **corrects Q19's premise**). The founder called for two things on 2026-09-05 — *"build the distributor connection as a class C source"* for Illinois and *"open a standing quarterly request, filed as a source"* for Michigan — and research the same day found **both premises wrong, in opposite directions**. **Illinois: there is no feed to declare, and two distributors forbid the reading in their own words.** `now.breakthrubev.com/robots.txt` (HTTP 200) publishes `User-agent: *` / `Allow: /bbg/en/login` / `Disallow: /`; Breakthru's Terms §6.2(c) forbid "web crawlers, data mining, scraping, robots, spiders"; and Southern Glazer's Terms forbid "any robot, spider, or other automatic device" AND, separately, providing "any other person with access to this Website … using your username, password" — so a credential-holding mirror puts the house in breach of an agreement it signed, with this product as the beneficiary. `shop.sgproof.com` publishes `Visit-time: 0400-0845` and was **not fetched** (12:18 UTC). `breakthrubev.com/sitemap.xml` enumerated in full: **60 URLs, not one product or price path**. And **LibDib's public OpenAPI was read whole** (HTTP 200, 70,801 B, 52 paths, 49 schemas): **the string `price` appears ZERO times** — it is an internal ML/telemetry portal, correcting this register's "the most promising class-C connection". **The industry answers it differently anyway:** SGWS's documented EDI set on two independent trading-partner pages is **850/856/810(/997), no 832**; Restaurant365 ticks **Multi-Invoice** and leaves **Order Guides blank** for all three wine-and-spirits distributors it lists; MarginEdge says it in one sentence — *"We update your order guides based on your invoices"*; Fintech ships "line-item invoice data" and no catalogue. So **the house's own invoices ARE the licensee price list**, and that path is already built as [0117](0117-a-price-sighting-names-its-source-its-date-and-its-unit.md) class A. **Michigan: MCL 436.1609a embargoes every filed net cash price from FOIA "until 1 year after" filing**, so the standing quarterly request can never return anything current — [0117](0117-a-price-sighting-names-its-source-its-date-and-its-unit.md) Q19's "those are public records" is corrected here; also corrected, from the rules read verbatim, that the quarterly cadence is **wine's** (R 436.1726) and beer has **no recurring filing date** (R 436.1625). **The placement fork the brief posed was already answered in code:** `comparisonClassOf` maps `api_catalog` to `quoted` and its own docblock calls that class "ADR 0117 classes A and C", `price_index_postings` has **no `restaurant_id` column at all** and its migration says "Class A and C never live here", and `belowTrailingAverage` reads `restaurant_id.is.null` as **every house** — so a licensee price written unscoped is a cross-tenant leak. Class C therefore lands on `vendor_price_observations`, restaurant-scoped, `api_catalog`/tier 3, **no migration**. **Built:** `distributor-feed/` (five distributors with their verbatim robots rule and terms clause, a read-only catalogue endpoint, `offerable: false` with the reason on the object, no declare route and no credential column), `parse-edi832.ts` refusing the three things the standard does not say — an unmapped `CTP02` (CDW's guide defines `C01` as literally "CDW Price"), a missing `PO4` (never a 750), and a missing currency (no `USD` default) — against **two recorded fixtures**, one a real published sample transcribed character for character that correctly admits **nothing**, one constructed and labelled as such in its provenance file; and the `intake: "foia"` source with `status: "not_yet_filed"` (deliberately not `requested`) and `maxAgeDays: 480` documented as an embargo's arithmetic, **not** a freshness allowance. The request text is drafted for the FOUNDER at [`MICHIGAN-FOIA-BEER-WINE-SCHEDULES.md`](../07-reference/MICHIGAN-FOIA-BEER-WINE-SCHEDULES.md); **nothing was sent**. **Declined and argued:** the class-C line in `MarketIndexPanel`, because that panel draws the state-keyed register and a class-C row is tenant-keyed. `jest src/price-index src/distributor-feed`: **214 passed / 20 suites**, 45 in 3 suites new here. **Q2 and Q3 ANSWERED by the founder the same day and BUILT; Q1 narrowed to sanctioned APIs and a sign-in hand-over, with NO mirror of any kind built while that answer is awaited.** Q3, his words — *"Manager maps it, recorded on every row"* — is `20260905240000_a_manager_states_what_a_code_means.sql`: a manager of the house states what one sender's `CTP02` means, once, with the evidence they had and under their name; **no DEFAULT on the meaning at any layer**, nothing seeded, and `DistributorEntry` LOST its `priceBasisByCode` field rather than keeping it empty, because a code map shipped centrally is this product asserting a term of an agreement it is not party to (a trade level is negotiated per licence — the probe shows two houses reading the same code differently). Every price the statement admits names it in a real column (`vendor_price_observations.price_code_mapping_id`, `ON DELETE RESTRICT`), so a wrong statement is one indexed query; withdrawing needs who, when and why, KEEPS the statement, frees the code for a corrected one, and **marks** the admitted rows by join rather than stamping a flag that could half-backfill and then disagree. Two live meanings for one code is refused twice and removed from the parser's map. **PGlite (PG 18.3) 24/24**, the in-file assertions exercised rather than skipped, and `ON DELETE RESTRICT` measured to raise **23001** not 23503. `jest src/distributor-feed src/price-index`: **284 passed / 24 suites**; both apps' `tsc` clean in my files (web 0 errors); seven guards exit 0. | 2026-09-05 |
| [0127](0127-a-house-sees-one-arm-and-the-arm-it-saw-is-written-down.md) | **A house sees one arm, and the arm it saw is written down** (Locked — the ratio and, in the 2026-09-05 batch-45 addendum, BOTH open questions; built on `feat/mudavym-design-p4`; neither migration is applied anywhere and nothing has recorded a row). The founder, 2026-09-05, asked whether the one-tap desk's hand-written note keeps the plain button commit `be80f8b5` gave it or gets the die back, and answered neither: *"lets try both, 80 percent simple 20 percent signature"*. **Measured before designing:** the ux-optimizer has an override gate and **no assignment store**. `restaurant_feature_flags` is a per-house boolean a person sets by hand — a selection, not a ratio; `ux_overrides.rollout_pct` + `rolloutBucket(userId)` buckets on the **user** not the house, compares one variant to the baseline rather than two named arms, **records the assignment nowhere** (so editing the percentage silently re-labels outcomes already collected), and returns `{enabled:false, overrides:[]}` unless `UX_OPTIMIZER_ENABLED="true"`, which defaults to `"false"` (`ux-optimizer.service.ts:78`). The client friction reporter is dark with **zero call sites** (`uxSignals.ts:15`; `dashboard.md` §5 says "None"), so `ux_signals` was the wrong pipe. `neural_footprint_event` was the right one and its `subject_type` check has admitted `'operator'` since 2026-08-24 — **measured: nothing writes an operator row today**, the model client is the only writer and it writes `"agent"` (`model-client.service.ts:429`). **Built:** `ux-optimizer/experiments.ts` (spec + `sha256("<key>:<rid>")` first four bytes mod 100, `assertRatioIsWhole` failing the process at module load on a ratio that does not sum to 100, bucket **-1** for a caller with no house so it falls outside every arm rather than into the biggest one); `ux_experiment_assignments` (PK `(restaurant_id, experiment_key)` — the key **is** the rule that a house cannot be counted in two arms — plus the `bucket` and the `ratio` in force, **frozen**, so an edited constant cannot rewrite history); three routes on the already-guarded `ux` controller; and the panel drawing its arm from the wire. **The rejection that carries the design:** a hash alone needs no table, and that is exactly the trap — edit the constant and every exposure already in the ledger is attributed to an arm the house was never shown, each row correct, the comparison wrong, nothing reporting a problem. **The stored row wins over the recomputed hash.** **Honesty rules held in three places a shortcut was available:** a failed assignment READ throws instead of resolving null (supabase-js resolves `{data,error}`; "no row" would re-assign on every failure and scatter one house across both arms); a failed COUNT throws instead of returning 0, because 0 is a real answer this line prints in words; and **when the arm cannot be read the card falls back to plain, SAYS it is a fallback, and records NOTHING** — the server would stamp the house's stored arm, and filing a plain exposure under the die is worse than not counting it. The browser never names its own arm: there is no `arm` field on the event. **Refused by measurement:** time from press to completion (a button is ~0ms and the die is `pour.ms`'s 620 **by construction** — a reading of a constant, not of an operator; duration is recorded from EXPOSURE instead, on the ledger's own `duration_ms`) and the die's early-release count (die-only, so the plain arm's zero would be a property of the control read as a fact about people). **The cost, stated where it bites:** every read here is tenant-scoped and assignment is per house, so **the report can only ever show this house's one arm** — the cross-arm comparison the ratio exists to settle is a cross-tenant read and no role in this codebase grants one. Printing `plain: 0` beside a die house's real numbers was refused. Abandons are a **floor** (a tab closed outright records nothing; both arms lose the same cases). **Proof:** `jest src/ux-optimizer src/one-tap-actions` **90 passed / 6 suites** (36 new, in 2 new suites); `vitest src/pages/dashboard/next` **59 passed / 3 files** (29 new); both apps `tsc` clean of these files; the migration applied on **PGlite** (Docker down) **10/10**, with every in-file assertion proven to FIRE against a one-change broken copy; pre-fix probes at the same depth, run then deleted — **12 of 15** web cases and **18 of 18** gateway cases fail against `git show HEAD:` copies. **Live, read-only:** all three routes answer **401** unauthenticated and 404 on the wrong verb, and `GET /ux/experiments/note_close_control/report` authenticated answers **500 "Could not find the table 'public.ux_experiment_assignments' in the schema cache"** — the read failing as a failure and not as an empty report. The one house the local gateway reaches hashes to bucket **99 — the die arm**. **A guard was strengthened, not ceilinged:** `check_read_columns_exist.py` counted `.eq("context->>arm", …)` as unreadable and went over its ceiling of 2; it now resolves a PostgREST JSON path to its **base column** and checks it, with two self-test cases proven to fail against the un-fixed guard. **Visible cost, captured:** the delivery card's sealed hold and the note's gesture hold are **visually identical at rest** and only the sentence above each tells them apart — the founder's original objection, now on screen, and the strongest argument for ending the experiment on the plain arm. **ADDENDUM 2026-09-05 (batch 45) — both open questions answered by the founder and built the same day.** *Who reads both arms:* the founder alone. `GET /ux/experiments/:key/both-arms` returns per arm the houses assigned, exposures, completions, abandons and the date of first exposure — **counts and dates only, with no restaurant id anywhere in the payload and no row ever selected** (every house figure is a `head:true` count), which is the property that made a cross-house read grantable. The per-house report is untouched. **The gate is a CREDENTIAL, not a role:** `@Public()` + `ServiceKeyGuard` — the `X-Admin-Key`/`ADMIN_API_KEY` service key ADR 0099 settled, which fails closed when unset. Inventing a `platform_admin` role was rejected on execution: `role` is per-restaurant (`jwt.strategy.ts:56`) and every consumer — `RolesGuard`, `ProtectedRoute`, the `team_access` and `organization_members` CHECKs — reads it as a role *within* a house, so a fourth value means a tenancy migration for one report, i.e. a permission system arriving as a side effect of a measurement. *What ends it:* **one quarter after the FIRST exposure**, derived and then frozen. `EXPERIMENT_QUARTER_DAYS = 91` = **13 whole weeks** (mean Gregorian quarter 365.2425/4 = 91.31 days; whole weeks because restaurant covers are weekly-periodic, so a part-week window puts one weekday into the answer; `+ 3 months` rejected because it is 89–92 days depending on the start and nothing would say so). The window is written once to `ux_experiment_state` (`20260905235500`) and a **write-once trigger refuses to move `first_exposure_at`, `quarter_days` or `ends_at`, or to delete the row** — a re-derived finish line would move under a running experiment, the same trap the frozen assignment row exists for. A **sample-size rule was the load-bearing rejection**: a stopping rule that depends on the data is optional stopping, it inflates the false-positive rate, and with 80/20 over ~one real tenant the die arm may never reach any *n*, so its other branch is 'runs forever'. After the end **no event is recorded, no house is enrolled, and the assignment rows are kept as history**; `POST /ux/experiments/:key/winner` writes the arm ONCE (undeclared arm 400; not started or not ended 409 with the date; a second different arm 409, and the trigger refuses it too; the same arm again is idempotent), after which every house is served it with `armSource:"winner"` while `assignedArm` still carries what that house saw. **There is no default winner:** until an arm is named the report says `endedWithNoWinnerNamed`, and the first-declared arm stays a rendering fallback rather than a result. A failed read of the window **throws with its reason** — folding it into null would make an ended experiment look like a running one and quietly resume recording. **Proof:** `jest src/ux-optimizer src/one-tap-actions` **134 passed / 7 suites** (42 new); `vitest src/pages/dashboard/next` **68 passed / 3 files** (6 new); both `tsc` runs **clean of this pass's files and grep-counted to zero** for `ux-optimizer` and for `dashboard/next`, though the worktree itself is not clean after the mid-pass `origin/main` merge — one untracked `communications/archive/**` file (gateway) and three `pages/orders/next/**` files (web), all other builders'; PGlite **19/19**, with **six of the migration's seven in-file assertions proven to FIRE** against a one-change broken copy (the seventh is the table-exists guard, unbreakable without breaking the `CREATE` it checks); pre-fix probes run then deleted — **36 of 78** gateway cases and **3 of 6** web cases fail against `git show HEAD:` copies, and each of the six gateway cases that passes is named and justified (one of them vacuously, and labelled). **Stated costs:** the both-arms read is **not attributable to a person** (a shared deployment secret), no actor column was added because the only honest value would look like an audit trail; nothing raises its hand when the window closes and naming the winner is a `curl`; and **nothing was verified live** — no gateway is running, the local one points at production, and neither experiment table exists there, so no route has ever returned a real figure and no production write was made. `check_gateway_boots.sh` **PASSES** with the new guard in the graph (it failed earlier in the pass on another builder's untracked `src/communications/archive/**` circular import, absent at HEAD and failing at require-time before DI; the mid-pass `origin/main` merge cleared it, and the narrower `Test.createTestingModule` proof written while it was red is kept). **SECOND ADDENDUM 2026-09-05 (batch 53) — the ending announces itself.** The first addendum filed its own cost: *"nothing raises its hand when the window closes"*. Founder: **"A notification to you when it ends unnamed."** Built as a **ninth notification producer** (`notifications/producers/experiment-ended.producer.ts`) beside the other eight, under the same ledger, claim index and single arming switch — and **off**, because `NOTIFICATION_PRODUCERS_ENABLED` is unset (measured). It speaks only when an experiment is started, ended and `winnerArm === null`, **deduped on the experiment key** (`experiment:<key>:ended_unnamed`, carrying no date and no count: a key with `endsAt` would repeat if the window were ever re-derived, a key with the figures would fire on every new number). **It is the only producer that is not a tenant sweep** — `sweepFounder`, run once per fast tick OUTSIDE `runPerTenant`, into the one house named by `DEFAULT_RESTAURANT_ID`; with that unset it returns `null`, opens no run row and **picks no house**, because a fallback would deliver one tenant's product decision into another's inbox. Its status row is computed **ahead of the `served` branch** for the same reason. It **never names or implies a winner** (a case greps the produced text for won/winning/leading/ahead/better), calls `adminExperimentReport` rather than recomputing the figures (so the notice and the route cannot disagree — and that read also DERIVES the window, so an armed sweep stamps the end date even if nobody reads the report), writes `type: "system_alert"` which `nt-format.ts` already maps to *System* (checked, because a new type falls to *Other*), and **sends no mail** — it writes an inbox row and says so. The address is resolved the way `RecipientResolverService` already does (`DEFAULT_RESTAURANT_ID`, `roles:["manager"]`, fallback left on — the one query for which OD-87's leak does not apply) and kept **out of the row**: the metadata carries a count and the path in words, the address goes to the log. Measured on this checkout: `MANAGER_EMAIL=aldemirkonuk2004@gmail.com`, `DEFAULT_RESTAURANT_ID=e5d6d489-25fa-4082-9cad-3e9e74225517`; **which of the two branches would answer is NOT claimed** — the resolver returns the same shape either way. **Rejected:** a line on `/admin/health` (`requiredRole="owner"`, so the admin key would have to be forwarded into a page every house's owner can open) and doing nothing. **Fork 1 left as it was, by the founder's call:** the winner act stays unattributable — `X-Admin-Key` is a shared secret and no `named_by` column was added, because the only value available would look like an audit trail without being one. **Proof:** `jest src/ux-optimizer src/notifications src/one-tap-actions` **381 passed / 25 suites** (26 new); the producer is new at HEAD so "fails pre-fix" is only `Cannot find module` — **a weak proof, labelled** — and the real one is **ten one-change mutations, all ten caught, each by the case written for it** (winner guard removed, ended guard removed, address written into the row, dedupe key carrying the date, a failed read swallowed as "no experiment", an unreadable register counted as zero, the message calling an arm "leading", the founder sweep moved inside `runPerTenant`, an unset `DEFAULT_RESTAURANT_ID` given a fallback, and the ninth put under the `served` branch); `tsc` grep-counted zero in both touched areas; `check_gateway_boots.sh` **PASS**, which is the only thing that can prove `NotificationsModule` importing `UxOptimizerModule` resolves. Nothing was sent and nothing live was verified. **BATCH 55, the same day — the fork was answered AGAINST the recommendation: "Inbox row and an email."** The notice now also goes out through the existing `GmailService`, **after** the row and only if `emit` returned `written` (the row is the record; the mail is a copy of it — moving the send above the row is caught by five cases). **One copy per ending, ever, and `emit` alone could not carry it:** quiet hours DEFER a member, so a later sweep legitimately writes a second row and `emit` says `written` twice for one ending — right for the row, wrong for an email. The mail is gated on a new narrow ledger read, `hasClaimFor(restaurant, producer, dedupeKey)`, taken BEFORE `emit`; **with the UNIQUE claim index that is atomic** (two instances both read false, only one wins the claims, only that one sends — the read suppresses later sweeps, the index settles the tie within one). **A failed read counts as already-announced**, holding the copy: the opposite would send a fresh email every sweep for as long as the failure lasted. The outcome is written back onto the row in words by a new `ledger.annotate` — **sent** (with the message id), **refused** (the sender's reason verbatim), **not_attempted** (no address, or already sent with the first notice) — and **never a silent skip**: the insert stamps `pending` and a case asserts it never survives a sweep. **On "no grant to send from", measured:** `GmailService.ensureGmailReady` is private and the service resolves `{success,error}` rather than throwing, so a missing grant arrives as a failed result naming the credential in the sender's own words, and is recorded as `refused` carrying it verbatim rather than classified by sniffing the string. The address still never touches the row. Still behind the one switch — an email gets no second, quieter one. `testing/fake-db.ts` gained an OPTIONAL third parameter so the notifications double lands real rows: without them the write-back would have found nothing and every assertion about it would have passed by doing nothing. **Proof:** `jest src/notifications src/ux-optimizer` **348 passed / 22 suites** (18 new); **nine one-change mutations, all nine caught**, including "the copy is sent BEFORE the row". Nothing was sent — every send went to a double. | 2026-09-05 |
| [0128](0128-an-approval-fits-the-decision.md) | **An approval fits the decision** (Proposed — built on `feat/mudavym-design-p4`; the migration is NOT applied anywhere, the upload flag is off, and `price_index_postings` does not exist on the production project, so the whole path is inert there). Answers [0117](0117-a-price-sighting-names-its-source-its-date-and-its-unit.md) **Q18** — *"Is provenance enough, or should an uploaded book require a second person's confirmation before it is shown?"* — after the founder's *"Yes, it needs an approval however we can't wait 2 people to approve a small decision, or a big one."* **The census is the decision.** Read-only against production through PostgREST with the gateway's own key, taken **twice** (the estate gained a house between the reads, and the later figures are the ones of record): of 15 houses, **TEN have one owner-or-manager or none** (four have zero); of the eight jurisdictions the estate resolves to, **FIVE contain exactly ONE person**. `price_index_postings` has **no `restaurant_id`** by design, so a carried book is every house in that state's number — which makes the pool the JURISDICTION, and makes US-MI (three houses, **three** people, the only uploadable source today) the one place a real second pair of eyes exists. "Always two" would have been "never" for most of this estate. **The adversarial pass changed the design.** A forged single price is **1 row in 12,530** — no band, and no human being reading a table, will ever see it. So the tier is a **sizing** control (wrong book, wrong state, mangled parse, bulk rewrite, and the FIRST book, which has no baseline at all), one band is aimed at the extreme a targeted edit cannot hide from, and the only evidence-producing confirmation is a **byte comparison**: `confirm` accepts the file, hashes it, and records `byte_match` only when the sha256 actually agrees — `attested` otherwise, so a click never wears the costume of a check. **Field sources, all fetched 2026-09-05:** maker-checker states no thresholds and speaks about *transactions*; SAP's dual control fires on **nominated sensitive fields only**, **blocks** the record rather than hiding it, and needs *"a second person with authorization"*; Restaurant365 — *"Dollar value is less than lowest Workflow threshold - Approve menu present to all users with Approve permissions"*; Odoo's *"amount limit for second approval"*; and **GAO-25-107721 §10.23**, the sentence this estate lives in: *"If segregation of duties is not practical within a business process because of limited personnel or other factors, management designs alternative control activities."* **Built:** `upload-tier.ts` (pure; four bands, every one **reasoned and labelled UNMEASURED** because the repo holds one edition of one book, and every upload records its real diff so the second edition replaces the reasoning with evidence); `price-index-review.service.ts` (jurisdiction pool, seal, admission, hourly escalation); `price_index_upload_reviews` + `price_index_postings.admitted_at` + the `price_index_upload` seal kind (`20260905180000`); `MARKET_VISIBILITY` as ONE exported predicate on every read; four routes; the panel's waiting label. Status is **`stood`, never `confirmed`**, for a routine book — nobody confirmed it. **Time-based auto-approval was rejected outright** as silence read as consent; the escalation tells people and its update writes exactly one column, asserted. **Pre-fix proof, measured then deleted:** a same-depth `git show HEAD:` probe drew a carried, unadmitted row as Michigan's index line and asked nothing about admission. `jest src/price-index src/common/seal` **230 passed / 21 suites**, 38 in 3 new suites + 7 added; `vitest src/pages/notifications/next` **108 passed / 6 files**, 4 new; gateway `tsc` clean, `check_gateway_boots.sh` PASS, `eslint --quiet` clean on 12 gateway files (web eslint cannot run at all here — `eslint-plugin-jsx-a11y` is missing). **Not verified, stated:** Docker is down, so the migration's in-file `DO $$` assertions are **UNEXECUTED**. **Four of five questions ANSWERED the same day and BUILT** (2026-09-05): Q1 — the tiers stay **unarmed** until a second Michigan edition is measured (*rejected: arm now on reasoned bands*). Q2 — **24 hours stands and is PRINTED** where the waiting book is shown, from `heldBookHoldHours` on the wire so the panel never carries its own copy of the number (*rejected: leaving it implied, which reads as "waiting indefinitely"*). Q3 — ***"Owner reopens with a stated reason"***: `POST /price-index/uploads/:id/reopen`, its own seal act `price_index_upload.reopen`, owner-only and re-checked against the BOOK's jurisdiction, refused for the refuser, refused a second time on the same bytes, refused with no reason; the refusal is APPENDED to `decision_history` and then cleared (the row's existing CHECK forces it), so the bytes stay one record and the history grows, and `escalated_at` is cleared so the hold restarts (*rejected: never — upload a corrected file, which loses the thread because a corrected file is different bytes*). Q4 — ***"Acceptable: reason + record"***: `carriedBooks` now travels on `GET /price-index/:state` and the index box prints, beneath a hand-carried line, that it *"was admitted by the same person who brought it in, because this jurisdiction has no second owner or manager — this is not a second pair of eyes"*, with the reason beneath (*rejected: block until a second person exists, against GAO §10.23; and a Mudavym-side review list, which puts this company between a house and its own state's published prices*). **A latent defect Q3 exposed:** the pool deduped a person by whichever access row came back first, so an owner of one Michigan house who managed another could be refused their own owner privilege on a coin flip — it now keeps the strongest role. Migration `20260906020000`; `jest src/price-index src/common/seal` **244 passed / 21 suites**, `vitest src/pages/notifications/next` **128 passed / 7 files**. One question open (Q5, the per-row admission UPDATE). | 2026-09-05 |
| [0129](0129-below-par-is-strictly-below-par.md) | **Below par is strictly below par** (Locked — the founder's call in session). The 2026-09-03 POS lens asked one screen how many wines were below par and got **three** answers about the same rows in the same second: the `/inventory` chip **9** (`stock <= par`), `GET …/low-stock` **7** (`stock < par`, via `v_low_stock_items`), and `/summary` `criticalCount` **0** (`stock_live === 0` — *out of stock*, a different question) — while the alert service was calling Tsantali at 2/5 critical. The register filed it as two definitions; it was three. Decided: below par is `stock < par` **strictly**, the predicate that already decides whether an alert fires, so the chip counts exactly the wines the system will act on; `at_par` becomes a band of its own; `unknown` (missing stock or `par <= 0`) is never `critical`, because no par is not a par of nothing. The rule lives in `datasets/sim/fixtures/below-par-cases.json` and **both** implementations run it — the operating-hours lockstep — since a comment did not stop the last divergence. Consequence: the chip reads **7 not 9** on Sim Meyhouse, and one fixture line (`below_par_bands`) reverses the whole policy. Shipped in #312. | 2026-09-05 |
| [0130](0130-a-generic-name-stays-the-venues-own-wine.md) | **A generic name stays the venue's own wine** (Locked by the founder in session 2026-09-05; built in this PR). Antalya night, reproduced on the schema built from all 100 migrations: the draft `"House White Wine"` — no producer, no vintage, no region — scored **90** against `HOUSE WHITE`, a Sim Meyhouse row (US / California / 2023), clearing `AUTO_LINK_CONFIDENCE` 85, and the venue's own label was then persisted over at `inventory.service.ts:1137`. `producer_sim` was **1 because neither side stated a producer** — absence read as agreement. Decided: only a specific identity (producer + name, or name + vintage + region) joins the shared library; anything less becomes the venue's own provisional wine. Mechanism: `wine_identity_is_specific()` in SQL / TS / Python pinned by 10 fixture vectors; `match_library_wine` returns **no rows** for a non-specific query and never offers a venue-owned row; `master_wine_library.provisional_for_restaurant_id` keys such a row on `venue:<id>|` so two venues coexist under the existing UNIQUE index. Rejected: a per-tenant alias over a shared link, a nullable `master_wine_id`, a `library_tier` label, a NULL `signature_hash`. Adversarial pass: removing the fabricated producer ALONE would have widened the hole. Production rows not repaired here — filed as its own stop. | 2026-09-05 |

## Locked — recorded elsewhere (pre-log)

| Decision | Where | Date |
|---|---|---|
| Brand: Mudavym (spelling confirmed) | [`PROJECT.md`](../PROJECT.md) Key Decisions | 2026-07-26 |
| Expansion sequence: wine → beverages → bakery → kitchen | [`FUTURES.md`](../FUTURES.md) §2 | 2026-07-26 |
| Wine = extraction quality bar for every category | [`FUTURES.md`](../FUTURES.md) §4 | 2026-07-26 |
| Bakery = first food vertical | [`FUTURES.md`](../FUTURES.md) §5 | 2026-07-26 |
| Live camera stack: RF-DETR → PaddleOCR → Gemini; no full OCR on live frames | [`PROJECT.md`](../PROJECT.md) / `SCANNING_PIPELINE_SETUP.md` | 2026-07-27 |
| Extend BaseAgent, not rebuild; wave sequencing; 7 core principles | [`PROJECT.md`](../PROJECT.md) Key Decisions | v2.0 |
| Blender / landing-page visuals: **hold** until structure + brand exist | Vision capture §13/§14.5 | 2026-08-24 |
| Agent-native UI: **do not build**; fix the security defects, ship the narrow deterministic slice | [`AGENT_NATIVE_UI_DECISION.md`](AGENT_NATIVE_UI_DECISION.md) (moved here from top level, ADR 0032) | 2026-07-27 |

## Open

See [`OPEN-DECISIONS.md`](OPEN-DECISIONS.md) — that file is canonical and this line
is a pointer, not a count. It has grown well past its original 8 items; four are
marked 🔴 and several were raised by review agents against Claude's own work.

Evidence annexes attached to open items (each merges into the ADR that resolves its
item, then is deleted):

| Annex | For |
|---|---|
| [`OD-72-rls-census.md`](OD-72-rls-census.md) | OD-72 (142 naked-RLS tables) · OD-73 (12 RLS-off tables with full `anon` DML) |
| [`annex-0103-0104-premortem.md`](annex-0103-0104-premortem.md) | ADR 0103/0104 — twelve irsaliye scenarios walked end to end; ten ranked causes of failure (folded in as 0103 A1–A10) |
| [`annex-0103-0104-scale.md`](annex-0103-0104-scale.md) | ADR 0103/0104 — three tenant profiles and the platform at 500/5,000 tenants; five choices that did not scale as written (folded in as 0104 S1–S4, S10) |
| [`annex-0103-0104-adversary.md`](annex-0103-0104-adversary.md) | ADR 0103/0104 — ten attacks; two land (many-to-many, the door step ships last), two near-decisive (Turkish incumbents, the clock basis) — folded in as 0103 A2, A6–A8 |

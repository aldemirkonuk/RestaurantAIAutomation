# PR audit gate — #390 @ 644dafea

**Verdict: BLOCK** (do not merge)

Required CI: green. Advisory CI audit job: failed on Anthropic credits only (not a content finding). Session fan-out covered the gap.

Gate-owned paths touched: none.

## Angle summaries

| Angle | Verdict |
|---|---|
| Correctness & regression | **BLOCK** |
| CLAUDE.md / ADR compliance | **BLOCK** |
| Security & blast-radius | APPROVE WITH NOTES |

Adversarial pass: skipped (skill step 6 — any BLOCK ends the gate).

## Blocking finding (both correctness + compliance)

ADR 0160 and PROGRESS §0c cite sketch sets `.planning/sketches/106`–`120` as in-repo evidence for Locked page directions. Those directories exist only as **untracked** files in `/Users/aldemirkonuk/Projects/wt-finish`; they are absent from `origin/main`, from the PR head tree, and from every remote ref. Main's tracked sketches stop at `103-overlay-experience`.

Failure: a page-build session follows Locked ADR 0160 to a drawing that is not in the repo and either stalls or re-decides the design. CI citation pairing does not check path existence.

**Remedy (one commit on the PR branch):** either
1. `git add` the 106–120 README + direction HTML (shots optional / LFS), or
2. rewrite Links + PROGRESS to say the drawings are not in-repo and point only at the gallery artifact URL.

## Non-blocking notes (condensed)

- "Eight pages locked" overstates by one (shell still provisional / sketch 119 owed).
- Several "all off origin/main" branch claims are stale for rebase status.
- ADR 0142 / 0159 cited but live only on unlanded branches.
- Security: docs-only; merge cannot change product runtime; public-repo disclosure of unlanded leak work is pre-existing.

## Prod note

Even after a future PASS merge, this PR is **documentation only** — Railway/Vercel may run health audits, but product behavior does not change.

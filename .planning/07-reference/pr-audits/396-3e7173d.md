<!-- pr-audit-gate: pr=396 sha=3e7173d2a912a9f969b60ab0b8e955e55cec6fd2 verdict=PASS -->
# PR audit — #396 `fix(db): increment_trust_counter is server-only (PUBLIC execute revoked)`

Head `3e7173d2a912a9f969b60ab0b8e955e55cec6fd2`, base `main` `cb756083e`. Composition (ADR 0090, 2026-09-17 amendment): one Opus planner, two independent Sonnet reviewers, the same Opus planner resumed for the final call. **Final: VERDICT: HOLDS. Reviewer A: PASS. Reviewer B: PASS. No blocking findings.**

Changed: `supabase/migrations/20260919120000_trust_counter_is_server_only.sql` (new, revokes EXECUTE on `increment_trust_counter(uuid)` from PUBLIC/anon/authenticated, keeps service_role, asserts effective privilege in-migration) and one `CLAIMS.jsonl` row `OD-72-TRUST-COUNTER-PUBLIC`. No gate-owned path touched (step 4 clear).

## CI
All five required contexts pass on this SHA (CI Complete; Beverage identity key; Guest merge policy; Fresh database equals remote; Code queries only relations production has). The CI-side `PR Audit Gate` failed with `CANNOT CHECK [no-credit]` (Anthropic credit balance too low): it audited nothing and is a documented non-required backstop. This in-session audit is the only audit.

## Reviewer A (correctness / regression / decision compliance) — PASS
- Resolves to the baseline function `(p_user_id uuid)` (baseline:678); no overload.
- In-file `BEGIN;/COMMIT;`: precedent in 3 applied migrations (20260902150000, 20260906233000, 20260912163000); planner verified those are on main with parity green. Hosted runner not inspectable.
- Roles exist in both apply paths; local `db reset` passed.
- Idempotent in both orders with #391's `20260917010400` (4 PGlite sequences, all end anon/auth false, service_role true).
- The caller cannot newly fail silently: `_get_user_studio_roles` (override_service.py:253-267) reads `user_roles` (client DML already revoked by OD-72) before the RPC.
- CLAIMS row: 359/359; mutation: dropping PUBLIC, wrong grantee, changed assertion, deleted file all fail; **a later re-GRANT, a commented-out assertion, and COMMIT->ROLLBACK are NOT seen (shape-only check)**.
- Non-blocking: drop in-file BEGIN/COMMIT; reword the claim's lead sentence; register `OPEN-DECISIONS.md:110` stays false until #391 lands.

## Reviewer B (security / adversarial) — PASS
- Exploit is real: `increment_trust_counter` takes any `p_user_id`, no caller check, runs with owner rights; a counter at the threshold (default 5) plus one legitimate approval flips `promotion_policy` to `auto_promote` (override_service.py:473-500), after which overrides bypass review (studio_routes.py:235-267). Planner correction: the RPC is outsider-reachable with the publishable key, not contributor-only.
- The revoke closes every PostgREST-reachable role; only the `(uuid)` signature was checked.
- Failing closed is safe; a false raise needs anon to be a member of service_role/postgres.
- Non-blocking: no audit of past abuse; `seed_sim_restaurant` and the function-default-privileges class remain open on a corpus-built DB; claim text says "service_role only" but owner `postgres` also holds EXECUTE.

## Planner recheck (Opus, resumed)
Confirmed against source, not summaries: #391's migration is a strict superset and order-independent. New finding: the baseline contains zero GRANT/REVOKE lines (ACLs were not dumped), so grant state in a corpus-built DB differs from production and "Fresh database equals remote" cannot prove any grant claim; the in-migration `has_function_privilege` assertion is therefore the right design. The migration is safe whether or not PUBLIC is present in production (no-op otherwise; worst case it fails closed and rolls back).

## Required after merge
1. Re-measure `has_function_privilege('anon'|'authenticated'|'service_role', 'public.increment_trust_counter(uuid)'::regprocedure, 'EXECUTE')` in production and record it (expect false/false/true).
2. Audit past abuse: `certified_contributor` rows with `consecutive_approved_overrides >= 5` against approved override events.
3. Land #391, or file an OD for `seed_sim_restaurant` and the default-privileges class, and correct `OPEN-DECISIONS.md:110`.

## Not checked (limitations)
No reviewer or planner queried production; every production number is the author's Supabase MCP read (2026-09-19) and the orchestrator session's independent re-measure of the same ACL. The hosted Supabase runner's transaction handling and `schema_migrations` rows were not inspected. PGlite has no Supabase platform roles. The Supabase Preview check was skipped. The full CI was not re-run by the reviewers. The versions-unique guard cannot run inside an index archive (no `.git`); it passed in the real repo against main and 25 open PRs. tsc/eslint/jest were not run (diff is one SQL file and one JSONL row).

<!-- pr-audit-gate: pr=388 sha=32dcaff2d4efd1147d8e14e90736f4d9f87914e8 verdict=PASS -->

# PR audit — #388, head `32dcaff2`

- **Repo:** aldemirkonuk/RestaurantAIAutomation · **Base:** `main` · **Head audited:** `32dcaff2d4efd1147d8e14e90736f4d9f87914e8`
- **Branch:** `fix/census-sitemap-check-message` — a one-off follow-up to PR #385
  (ADR 0158), found while running that PR's own post-merge production census.

## What this fixes

`check_robots_permits_sitemap()` in `scripts/crawl_surface_census.py` always printed
the failure-worded detail string ("has no Allow: line covering {path}") regardless of
the `allowed` boolean it had just computed — a static f-string, not conditioned on the
check's own result. The verdict (PASS/FAIL) was always correct; only the human-readable
message lied about it. Found live: running the census against `https://mudavym.com`
right after #385 merged, this was the one check whose PASS line read like a failure.
Fixed by branching the message on `allowed`. Also commits the already-written #385
audit report (`.planning/07-reference/pr-audits/385-34874b43.md`) that the pr-audit-gate
skill's own step 9 says must not be committed before merging — safe now.

## Verdict

| Angle | Verdict |
|---|---|
| Correctness & regression risk | APPROVE |
| CLAUDE.md-and-ADR compliance | APPROVE |
| Security & production blast-radius | APPROVE |
| Adversarial pass | HOLDS |

**Overall: PASS. Merging.**

## Findings

- Correctness swept all 17 `check()` call sites in the file and confirmed this was a
  one-off — no other check has a static detail string that can diverge from its own
  boolean. The `allowed` computation itself is untouched; only the message branches now.
- Compliance confirmed the diff touches none of the gate's own owned paths, and that
  committing an audit-report doc in a later, separate PR (rather than at merge time)
  matches existing precedent (PR #354's four reports were committed the same way).
  `check_adr_numbers_unique.py` and `check_decision_claims.sh` both pass unchanged
  (343/343 holding; no ADR needed for a testing-script bugfix).
- Security confirmed `crawl_surface_census.py` is read-only (GET-only `fetch()`, no
  subprocess, no writes, no secrets) and the new markdown carries no sensitive content
  — zero production blast radius.
- The adversary re-derived the message grammar in both branches (no mismatch), read
  `require_pr_audit.py` directly and confirmed it checks PR comments only — never this
  repo directory — so the report file's embedded marker cannot be misread by the hook
  for any other PR. It also confirmed no ADR/CLAIMS entry cites this report's filename,
  so nothing was left dangling by landing it here instead of in #385. One procedural
  note (not a defect): at audit time `PR Audit Gate` itself was failing with the known
  `CANNOT CHECK [no-credit]` error and no PASS marker existed yet — expected mid-process,
  resolved by this comment/merge. Verdict: HOLDS.

## Re-verified at this head (`32dcaff2`)

- `gh pr checks 388`: all 5 branch-protection-required contexts pass (`CI Complete`,
  `Beverage identity key — SQL matches Python`, `Guest merge policy — zero false
  merges`, `Fresh database equals remote`, `Code queries only relations production
  has`). `PR Audit Gate` fails with the known no-credit infra issue (advisory only,
  not required).
- Branch is not stale: `git merge-base origin/main 32dcaff2d` equals `origin/main`'s
  tip (`6b6ac454f`).

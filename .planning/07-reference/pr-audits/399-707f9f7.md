<!-- pr-audit-gate: pr=399 sha=707f9f7fc4f839786f6e5a8bb97af37c8766246d verdict=PASS -->
# pr-audit-gate — PR #399 @ 707f9f7 — **PASS**

**fix(email): a vendor email body is text, never markup (ADR 0170)**

The gate ran twice. Round 1 was at 12f28b8. All three angles returned APPROVE WITH NOTES, and commit 707f9f7 acted on every note. Round 2 (this report) is at 707f9f7. All 5 required contexts pass (CI Complete, Beverage identity key, Guest merge policy, Fresh database equals remote, Code queries only relations production has). The diff touches no gate-owned path.

## Angles (round 2)
- **Correctness: APPROVE WITH NOTES.** 1446 tests pass and tsc is clean. The sig-replacer mutation fails exactly the new spec. Every CLAIMS sub-check behaves as its status says (resolved exits 0; the open rows exit 1, never 2). The text/plain part decodes the entities correctly. Notes: the open templates row pins exact spellings, so it might never flip; empty bodies now yield ""; the ADR mixes line numbers from two revisions; the Python path stays open until the companion PR.
- **Compliance: APPROVE WITH NOTES.** All four round-1 notes are fixed. Notes (non-blocking, carried into the companion PR, which amends ADR 0170 anyway): the :41 wording "0 of 27 bodies" should say `content`; the `modified_message` wording at :16; an ambiguous "it" at :47; the stale Status line; the open row's bare grep on custom-reminder and its missing `preheader`; the addendum text the companion commit promises.
- **Security: APPROVE WITH NOTES.** The sweep claim is verified: only `AUTO_SEND_SCHEDULED` rows are sent, and only `inbound-responder.service.ts:545` writes that status. No migration, auth, tenant or secret change. The fix also closes the path where a prompt injection makes the auto-send reply emit markup. Notes: sequence the companion PR next; add the prompt-injection framing; add a caveat about recipient lists.

## Adversarial pass: **HOLDS**
- Tried: markup in each caller's input, entity leakage into text/plain, greeting regex breakage, ref and ADR-number races, and re-running every CLAIMS row. None overturns the approve.
- Strongest finding (non-blocking): nothing ties `buildEmailHtml` to each of its four callers. Replacing `html: this.buildEmailHtml(content)` with `html: content` at :6580 kept the claims green and passed 73 suites (1410 tests); the file was restored from a snapshot. Production after the merge is still strictly safer than main. **Carried into the companion PR:** a per-caller check, mutated.
- The deferral of the compliance notes is acceptable: no statement misleads the founder's decision. If the companion PR stalls, apply the ADR corrections as a standalone docs fix.

## Not checked
- The production `message_text` query was re-run by the author only, not by the auditors.
- The gateway Jest suites for the security angle rest on CI.

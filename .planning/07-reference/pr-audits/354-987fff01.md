<!-- pr-audit-gate: pr=354 sha=987fff0 verdict=BLOCK -->

# PR audit — #354 (round 4, exhaustive re-derivation), head `987fff01`

- **Superseded by `ea96ebc3`**, which applies a structural fix rather than a fifth patch.

## Verdict: BLOCK (compliance) — fourth instance, root mechanism identified

Round 4 was an exhaustive, from-scratch re-derivation of every citation in both
`0137-legacy-e2e-waves-d-e-g-are-retired-not-repointed.md` and the amended
`v3.0-TECH-DEBT.md` section, specifically to determine whether rounds 1–3 had closed the
recurring drift. They had not. Two findings, both introduced by this PR's own commits:

**B1 (substantive, not drift):** round 3's re-derived `SUPABASE_SERVICE_ROLE_KEY` row
said it gates "the teardown sweep only." False — `teardown_e2e_records` is
`scope="session", autouse=True` and depends on `prod_supabase`, so its skip propagates
to every test in the session (Waves A, B, C), not just cleanup. The auditor proved this
with a minimal fixture reproduction, not just an argument.

**B2 (the actual mechanism behind all four rounds):** `e2e-prod.yml`'s own copy of the
secrets table cited `conftest_prod.py` line numbers that an *earlier commit in this same
PR* (the wave-letter-map trim) had already shifted by one. Four of eight citations
landed on a different secret's env-read line. The document and the workflow — two
independent copies of one fact — had drifted from each other, not just from the truth.

The auditor's diagnosis: three copies of one table (the workflow's YAML comments, the
TECH-DEBT.md table, and the source itself) is why every round's fix — correctly patching
whichever copy was named — left the others to drift on the next edit.

## Also confirmed correct by the exhaustive pass (kept so no future round re-derives it)

27 ADR citations checked, 24 exact (3 minor — a buffer_manager.py publisher count and
two line-range/citation offsets, all fixed alongside). Every count in
`EXISTING-TEST-INVENTORY.md` re-parsed programmatically and matched. The
`cascading_report.py` fix reproduced again from scratch: "All Waves Passed", no D/E/G.
All guards (`check_decision_claims.sh`, `check_citation_pairing.py`,
`check_adr_numbers_unique.py`) pass. The underlying decision (delete, not repoint or
gate) and the per-wave research holds up completely under adversarial re-derivation —
nothing about the actual technical judgment is in question, only the durable record's
internal consistency.

## Disposition

Fixed in `ea96ebc3` with a structural change, not a fifth patch: `e2e-prod.yml`'s copy
of the secrets table collapsed to fixture/variable names with no line numbers (stable
across edits), pointing at `v3.0-TECH-DEBT.md` as the single source for exact
citations; that table's `SUPABASE_SERVICE_ROLE_KEY` row corrected; `v3.0-TECH-DEBT.md`'s
"Cleaned up alongside" list collapsed to point at the ADR's fuller list instead of
keeping an independently-drifting shorter copy; the two ADR citation errors fixed;
`REQUIREMENTS.md`'s remaining TEST-PROD-08/10 annotated; the ADR's Review trail
extended with all four rounds as the durable record of what recurred.

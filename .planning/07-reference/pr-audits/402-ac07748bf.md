<!-- pr-audit-gate: pr=402 sha=ac07748bf47238f7bdc0d3b0dbd7157f76315a4d verdict=PASS -->
## pr-audit-gate: PR #402, verdict PASS

**Head:** ac07748bf47238f7bdc0d3b0dbd7157f76315a4d. All 5 required contexts are green. The advisory CI "PR Audit Gate" job returned CANNOT CHECK [no-credit] because the Anthropic API credit is too low, so this local gate replaces it.

**Round 1 (cc5c9ada4): BLOCK from all 3 angles.**
- Folded References/In-Reply-To headers from vendors (CRLF+TAB) were refused. Every reply on that thread would have failed.
- The CLAIMS row pinned only 4 of the 9 header calls.
- Addresses were checked before being trimmed.
- A header refusal was mislabelled SEND_UNCONFIRMED, and the error blamed GMAIL_REFRESH_TOKEN.
- The grant From header had no test.
- The spec file contained a NUL byte.

**Round 2 (2eb428a50): APPROVE WITH NOTES from all 3 angles.** Fixes in this round:
- threadingHeader rebuilds the ids instead of refusing them.
- All 12 header calls are pinned.
- Addresses are trimmed before the check.
- refusedBeforeSend was added.

Notes raised: a silent drop of one recipient in a multi-mailbox entry, text-based refusal classification, and an over-long msg-id.

**Round 3 (913a5dd8e..ac07748bf):**
- An entry with several mailboxes is now refused, never collapsed.
- SendRefusedBeforeSendError is matched with instanceof; the text match is gone.
- The msg-id length is bounded.
- Typecheck fix (4b2bc0942) and the matching claims re-pin (ac07748bf).

**Adversary (on ac07748bf): HOLDS.**
- No real caller builds a named multi-mailbox string. A free-text contact_email such as `Acme, Inc. <x@y>` is now refused loudly and never misrouted.
- The refusal cannot follow a send Gmail accepted.
- tsc (spec config) is clean. Jest on communications + procurement: 1910 passed.
- Claims 364/364.

**Follow-ups (non-blocking, pre-existing on main):**
- isDefiniteSendRefusal still matches text patterns (invalid_grant, "does not exist", 5xx) against an error message that embeds the vendor address. A contact_email like "Suite 500 - Orders" combined with an ambiguous Gmail failure can release the draft, and a re-approve could send a duplicate PO.
- A display name or address longer than 998 characters stays unfolded.

**Limitation:** the three angles audited through round 2. Only the adversary audited the round-3 delta.

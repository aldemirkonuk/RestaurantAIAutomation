# PR 460 audit — PASS (retroactive)

Head `06c64fadc9566b8bb38f17bdd49236849a0004f9`. Squash on main `97806caf6af6827de69c2fabffba521fbe19ec50` (2026-09-23T04:36:39Z). Production web bundle `index-CGcUt3ld.js` already serves it. Not reverted.

**This record is retroactive.** The merge landed before this fan-out. A short PASS marker for the same head was already on the PR thread at 2026-09-23T04:36:32Z (`<!-- pr-audit-gate: pr=460 sha=06c64fad verdict=PASS -->`). This pass re-ran the three angles and the adversary after the merge, because that merge was reported as having skipped the gate. The verdict agrees: **PASS**.

Required checks were green: CI Complete, Beverage identity key — SQL matches Python, Guest merge policy — zero false merges, Fresh database equals remote, Code queries only relations production has. Diff does not touch gate-owned paths. Four files, +60/−14, copy only under `apps/web/src/pages/connections/next/`.

| Angle | Verdict |
|---|---|
| Correctness | APPROVE WITH NOTES |
| CLAUDE.md / ADR | APPROVE WITH NOTES |
| Security | APPROVE WITH NOTES |
| Adversary | HOLDS |

## Correctness

Copy and one `probeWord` return. `protocol_error` still flows from `mcp-runtime.service.ts` into `probeWord` (`cx-format.ts:140`, "Not readable") and the Connections chips. Nothing branches on the label. Removed strings are not asserted in tests; `nightly.spec.ts:305` only comments "unauthenticated by design". Nightly still keys on `[data-secret]`.

Notes, not blocks:

- A real `protocol_error` detail from the gateway can still say "protocol" (`mcp-runtime.service.ts`). The new test uses fixture detail "The reply could not be read.", so `queryByText(/protocol/i)` does not prove the live sentence.
- The letter fallback no longer says "no route" (`DistributorFeedPanel.tsx:458`). Production prefers `letter.data.neverSent` from `feed-request-letter.ts`, which still says it.
- The new leak regexes guard page-built fixture text, not server-sent strings.
- Price-code refusals say "yet" (`DistributorFeedPanel.tsx:588` and the withdraw twin).

## CLAUDE.md / ADR

No locked ADR contradicted. The 2026-09-22 "no technical internals" ruling is already in `.planning/06-pages/connections.md` via #454. ADR 0110 does not prescribe the Stripe sentence; "never reaches this page" (`ConnectionsNext.tsx:1062`) is still true. ADR 0111 does not require the word "unauthenticated". The calendar warning remains: anyone holding the address reads deliveries, deadlines, and shifts. ADR 0135 masks via `subtitleIsSecret`, not the words. ADR 0126 built state and withdraw, so "yet" is slightly misleading on the unwired-handler path and does not name a forbidden future. "Not readable" is as loose as "Not this protocol" for a no-tools or not-vetted probe. A copy change under an existing ruling does not need a new ADR.

Stale quotes of the old words remain in the nightly comment and in ADR 0135 prose. `StripeCardPanel.tsx` still says "or our servers". Profile still says "Token expires".

## Security

Web-bundle text only. No auth, migration, actor FK, tenant boundary, or secret. Calendar consequence kept and the address stays masked. A tool with no read-or-write answer still counts as a write (`declaredWords`). Stop still calls `setHouseGrantAccess`; only the owner revokes the grant. Signer name is still taken from the session and not sent from the page. Both price-code refusals return before a write.

"Sign-in expires" (`ConnectionsNext.tsx:1133`) labels `tokenExpiresAt` (the connected grant), not the Mudavym login. Display only.

## Adversary — HOLDS

Tried to break: leftover gateway sentences as a new leak (they were already rendered; this diff only changed page-owned fallbacks); the thinner Stripe sentence as a false claim (the remainder is still true, and the fallback shows when a provider and publishable key exist); "Sign-in expires" as a wrong revoke (the stop control is unchanged); "yet" as a write (both branches return before send); "Not readable" as a gate that lets an unvetted server through (`probeWord` is display-only; chip tone still keys on `status === 'ok'`). None held as a block. The fixture-only protocol test is a coverage limit, not a regression: the `/protocol/` leak regex did not exist before this PR.

## Not checked

SARIF artifacts were not fetched. Angles did not re-run vitest or open the production page. Supabase Preview was SKIPPED. The advisory CI job named PR Audit Gate was SUCCESS on this head; it is not a required context.

## Follow-up, not this verdict

Gateway sentences that still say "protocol" and "no route" belong in `mcp-runtime.service.ts` and `feed-request-letter.ts`, as their own change. This PASS does not authorize that work, a revert, or a flag flip.

Marker already on the thread: `<!-- pr-audit-gate: pr=460 sha=06c64fad verdict=PASS -->`

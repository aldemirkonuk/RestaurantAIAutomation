<!-- pr-audit-gate: pr=359 sha=426b7d1 verdict=PASS -->
## pr-audit-gate report — PR #359 @ `426b7d1` (re-audit after fix)

**Verdict: PASS**

Prior audit at `5fc6807` BLOCKED on the compliance angle: a JSDoc hunk in `Sheet.tsx`'s `wide` prop quietly deleted the ADR 0112 citation and asserted a false claim that the module "ships ahead of the ADR." Commit `426b7d1` reverts exactly that one hunk, restoring text byte-identical to `origin/main`'s.

### Correctness — APPROVE
`git diff 5fc6807..426b7d1` is exactly one file, one hunk, comment-only (9 insertions/12 deletions, all inside the `wide?` JSDoc). The Escape open-stack and focus-cascade fixes from earlier commits are untouched and byte-identical to what the first round already verified sound.

### CLAUDE.md-and-ADR compliance — APPROVE
Restored text verified byte-identical to `origin/main`'s `Sheet.tsx`. Matches ADR 0112 (`Locked`, ratified 2026-09-05, F3 changelog entry records this exact `wide` prop). Commit message is honest, cites the gate finding and SHA, invokes CLAUDE.md §5 correctly, makes no overclaim.

### Security — APPROVE
Confirmed comment-only; all security-relevant code (open-stack, focus cascade) untouched from the already-approved first round.

### Adversarial pass — OVERTURNED on a state issue, resolved
The adversary independently re-verified all of the above (diff scope, byte-identity, openStack push/pop balance across StrictMode/Offscreen, z-order interaction — only one non-default `zIndex` consumer in the repo and it's unrelated to this primitive, focus cascade cannot skip a `[disabled]`/`[hidden]`/`aria-hidden` guard since it iterates the same filtered `focusables()` set) and found no code defect. It flagged that the CI-side `pr-audit-gate.yml` backstop workflow was red at this SHA, because its own `--wait-upstream` polling caught a transient CodeQL `NEUTRAL` before CodeQL settled — the same race documented from this repo's own history (`gh api .../check-runs` at this SHA confirms CodeQL is genuinely `success`). Re-ran the failed CI-side run. This is not a required branch-protection context (the 5 required contexts are all green), and per this skill's own documented limitation, that CI job is the backstop for merges outside a Claude Code session — this comment's marker, not that job's conclusion, is what `require_pr_audit.py` checks.

### What I could not check
Did not execute the vitest suite locally for the fix commit — relied on CI (`Test TypeScript` green at this SHA, `Lint TypeScript` green so the job wasn't skipped). Vercel preview deploys remain blocked by the known account-wide free-tier rate limit (`api-deployments-free-per-day`), not a required check.

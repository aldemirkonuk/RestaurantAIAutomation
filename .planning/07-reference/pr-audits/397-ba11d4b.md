<!-- pr-audit-gate: pr=397 sha=ba11d4b59974d39eac084e9648c73d5288c99cc1 verdict=PASS -->
## pr-audit-gate — PR #397 @ `ba11d4b59974d39eac084e9648c73d5288c99cc1` — **PASS**

Three-role audit under ADR 0090 (2026-09-17 amendment): one Opus planner, two independent Sonnet reviewers, and the same planner resumed for the final call. **Final call: `VERDICT: HOLDS`**. Correctness reviewer: APPROVE. Adversarial reviewer: APPROVE.

**Before review.** CI at the head is 39 pass, 1 skip, 1 fail. The failure is `PR Audit Gate`, the CI backstop, which reports `CANNOT CHECK [no-credit]`: its API account has had no credit since 2026-09-12, and it is not a required context. All five required contexts on `main` pass. `main` is `22695d129`, and the branch contains it. The diff touches no gate-owned paths.

### What the first plan caught, and how it was fixed (commit `70899b127`)
The planner ran at `53104c648` and found four risks before any review. All four were fixed, and each fix is pinned by a test that fails without it.
1. **OFF was not byte-identical.** An empty class-array entry put a double space into 2 OFF states, and no test noticed. That is fixed, and all 14 OFF states are now pinned to sha256 fingerprints of `main`'s render (`authPages.off-fingerprints.json`).
2. **A stale note.** A password-mismatch note could outlive a later Google refusal. The refusal state is now cleared on Google `onError` and on `resolve()`.
3. **Wrong 429 wording.** The note named a per-address limit, but `/auth/login` limits per connection. The wording now says "this connection".
4. **A narrow comparison.** The "same controls" signature now also covers labels and link targets.

### Correctness reviewer — APPROVE
- **OFF byte-identity, reproduced independently.** It captured OFF twice at `22695d129` and twice at the head, in isolated archives. All 56 hashes (14 states × 4 runs) match the fixture, and `diff -r` is empty across all four pairings.
- **The fix is real.** Run against the pre-fix `Login.tsx` from `53104c648`, `Login.signInNote.test.tsx` fails 2 of 4, as claimed. The diff contains exactly the three fixes.
- **OFF is structurally untouched.** `refusal` is read only in `on`-gated branches, and the note renders only when `on` is true.
- **`LoginError.status` is additive.** There is one constructor call site, and all three `instanceof` consumers are in `Login.tsx`.
- **CI ran the new guards.** The "Test TypeScript" job ran `authPages.publicDesign` (94 tests, 14 opt-in skips), `EndpaperShell.test` (10) and `Login.signInNote` (4).
- **Decision compliance.** ADR 0149 row 35 and ADR 0143's bracket match the README directions and the Google decision.
- **`front-matter.html`** is docs only, sits in the existing sketch folder and is marked not built. The reviewer judged it compliant with CLAUDE.md §2 and §4.

### Adversarial reviewer — APPROVE
- **Google on the first page opens nothing new.** `auth.service.ts:1786-1928` never creates or auto-links an account. Unknown and unlinked emails get the same refusal, so the answer reveals nothing. A failed read of the link table throws rather than defaulting. The gateway is not touched by this PR.
- **One Google path.** One Tap and the button share a single `handleCredential` → `onError` path. The button is mounted once, across both steps, and `auto_select:false` is unchanged.
- **The 429 wording is accurate.** It was checked against the real key: an unauthenticated `/auth/login` request is keyed by IP.
- **The endpaper CSS hides nothing interactive.** It hides only a decorative first-child icon: the Globe in CountryCombobox and the Search icon in PlacesAutocomplete. PhoneNumberInput is not matched.
- **`front-matter.html` is not built or served.** Its only `innerHTML` is a constant, and its only external resource is Google Fonts.

### Final call (planner) — HOLDS
The planner also settled from source the accessibility items the adversarial reviewer left open:
- **Focus underline contrast:** about 7.2:1.
- **Reduced motion:** `animate()` sets the duration to 0.
- **The mark:** a single `role="img"`, labelled "Mudavym".
- **The refusal note:** it remounts on each attempt.

### Non-blocking follow-ups
- **Unfocused field underline.** Its contrast is about 2.3:1, below the 3:1 that WCAG 1.4.11 asks for when an underline is a field's only visible edge. Every field has a visible text label, and the style is founder-approved. Darkening it is worth doing.
- **StrictMode double-run in dev.** `MarkDraws` and the shell's entrance fade each run twice in development. This is dev-only, with no visible effect. A test is optional.

### Not verified (limitations, stated)
- The author's local run-76619 counts. CI confirms the same suites ran.
- The headless-Chrome check of the sketch.
- A real screen-reader test.
- The production value of `VITE_MUDAVYM_PUBLIC`. It lives only in Vercel; the last measurement is ADR 0149's from 2026-09-16, which found it unset.

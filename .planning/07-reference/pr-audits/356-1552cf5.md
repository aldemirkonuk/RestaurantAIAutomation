<!-- pr-audit-gate: pr=356 sha=1552cf5 verdict=BLOCK -->
## pr-audit-gate — PR #356 @ `1552cf5` — **BLOCK**

Required contexts: all 5 green on this head (CI Complete + four data guards). Non-required reds with non-code causes: CI `PR Audit Gate` (repo API key out of credit — it exits CANNOT CHECK and audited nothing; this review is the local skill's), Vercel previews (rate limit).

**Gate-owned path touched:** `.planning/decisions/README.md` (index rows for ADR 0138/0140, and ADR 0042's row now carries 0138's narrowing). Per the gate, this escalates to the founder regardless of the angles' verdicts.

### Correctness & regression — APPROVE WITH NOTES
Ran the door-outbox, ground, realtime, stock-invalidation and receiving tests on the head (10 files / 137 tests pass). No path destroys or duplicates a door receipt; overlapping flushes share correctly; the three `.mudavym` roots exposed to flag-off houses (both sealed dies, cellar onboarding) are fixed; the realtime narrowing still refreshes unclassified inventory keys. Notes: `useStandaloneGround` does not re-measure on a theme toggle unless the die re-renders (cosmetic); a corrupt drop-record JSON is overwritten on the next write; a receipt saved before auth resolves is filed "house not recorded"; no pixels rendered.

### CLAUDE.md & ADR compliance — **BLOCK**
Door-outbox code cited **ADR 0139** in eight places (`doorOutbox.ts:45,466`, `doorOutbox.durability.test.ts:18,143`, `DoorReceipt.test.tsx:208`, `DoorNext.test.tsx:205`, `useReceivingNextData.ts:739,792`). 0139 is an unrelated OAuth record on main; the decision is 0140. The module's own "read this before adding a strand back" warning pointed at the wrong record, and no guard checked citation targets. Otherwise ADRs 0138/0140 carry rationale and rejected options, 0042/0131 are narrowed openly, the 20-column count re-measures, no new top-level docs, all commits carry the trailer. The founder should approve the edit to ADR 0042's index row specifically.

### Security & blast radius — APPROVE WITH NOTES
No migrations, no gateway code. Per-restaurant drop keys; legacy unattributed records are adopted once by the first house on a device, marked "house unknown" (label + date only, local device). No flag-off legacy page found broken (code reading). The only refresh no longer fired on a stock event is the `['wines']` master library, which has no stock column. Does not affect the separately reported cross-tenant stock write on main.

### Adversarial pass
Not run — an angle returned BLOCK.

### Not checked
No rendered pixels; web ESLint not run locally (CI lint passed); CLAIMS scripts not re-run by the compliance angle (author reports all holding).

### Follow-up
Fix committed on the branch (all eight citations → 0140, and the ADR-0140 guard now fails if `doorOutbox.ts` cites anything but its own record — proven with a control). The new head will be re-audited.

<!-- pr-audit-gate: pr=354 sha=4243125 verdict=BLOCK -->

# PR audit — #354 (round 3), head `424312e5`

- **Superseded by `20dc0796`**, which re-derives (not patches) the table below.

## Verdict

| Angle | Verdict |
|---|---|
| Correctness & regression risk | (unchanged, APPROVE WITH NOTES from round 2) |
| CLAUDE.md-and-ADR compliance | **BLOCK** |
| Security & production blast-radius | (unchanged, APPROVE WITH NOTES from round 2) |

## Compliance — BLOCK, a third instance of the same recurring pattern

`.planning/v3.0-TECH-DEBT.md:1644-1658` — the founder-facing **present-tense**
"What the founder must set" secrets table — still described the pre-retirement wave
set: `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` gating "Waves A, D, E, G" / "D, E, G",
`RAILWAY_ORCHESTRATOR_URL` gating "Waves A–D", and `TOAST_WEBHOOK_SECRET`/`GMAIL_USER`
listed as live optional gates citing the two deleted wave files by name and exact line
number. Two prior rounds fixed narrow, specifically-named lines and both missed this
table, 20 lines above the text either round actually touched.

**Why this is the durable-record failure mode, not a nitpick:** this table is
literally instructional — "What the founder must set (Settings → Secrets and variables
→ Actions)" — the ADR's own decider would read it as current guidance and set two
secrets that gate nothing, then chase citations into two files this same PR deleted.

**Also found, same drift mechanism:** an earlier round's edit to `conftest_prod.py`
shifted every downstream line number by one, so every citation in the (now-replaced)
table was off by one even before the wave-gate content went stale. And the ADR's own
citation of `pos-bridge.md:96-108,144` had drifted to `:96-110,147` after this branch's
earlier edit to that file.

## Disposition

Fixed in `20dc0796`: the entire secrets table re-derived from the current tree (not
patched cell-by-cell) with a note stating it was re-derived, not copied forward; the ADR's
`pos-bridge.md` citation corrected; the `teardown_sim(apply=True)` landmine added to the
ADR's own out-of-scope list (previously named only in the audit report, not the durable
record); `REQUIREMENTS.md`'s TEST-PROD-04/05/07 annotated with ADR 0137 pointers. A
repo-wide grep for any remaining "gates Waves"/"→ Wave D/E/G" pattern outside the ADR
and audit-report files found nothing.

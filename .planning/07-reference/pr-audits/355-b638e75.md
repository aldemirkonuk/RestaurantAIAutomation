<!-- pr-audit-gate: pr=355 sha=b638e75 verdict=PASS -->
## pr-audit-gate report — PR #355 @ `b638e75`

**Verdict: PASS**

All 5 required contexts green. Diff (248KB, under the 300K budget) does not touch any gate-owned file.

### Correctness & regression risk — APPROVE WITH NOTES
Gating clean (LogsNext only mounted via PageGate; useMudavymDesign defaults false; fetch failures caught to false). Migration additive, versioned past main's tip, no collision. DI real (RestaurantsModule exports MembersService, LogsModule imports it, no cycle). Actor id correctly uses `public.users.user_id`, not `auth.users`. Pagination loop/stall modes closed (`hasMore` exact, `getNextPageParam` refuses to re-advance on an unchanged cursor).
Notes: the gateway half (membership 403, cursor pagination, `nullsFirst:false`) ships unconditionally, not flag-gated — affects legacy `/logs` and a second consumer (`documents-reports/next/useSortingOfficeData.ts`). The 403 is a genuine tenancy fix; both existing callers pass `activeRestaurantId` so they're unaffected. A gateway-before-migration deploy window causes `getFeatureFlags` to 500 app-wide until the migration lands — precedented (11 prior flags), self-healing (web falls back to legacy).

### CLAUDE.md-and-ADR compliance — APPROVE WITH NOTES
ADR 0133 lands in this PR, no number collision. `.planning/06-pages/logs.md` and `CLAIMS.jsonl` move with the code. CLAIMS ADR-0086 row correctly flips open→resolved with a real two-part verify. Guards got stronger, not weaker (`check_windowed_figures.py` +18 self-tests, fixes a dead anti-vacuity branch; `check_no_seeded_defaults.py` new scan root, no exemptions). `readBy` anchor real and correct.
Notes: ADR 0133 says the ten new-pages columns arrive in one migration; this PR splits `mudavym_design_logs` into its own — well-argued in the migration header, but the ADR record wasn't re-amended to say so (pre-existing gap, `decisions/README.md` also missing rows for 0131–0138 generally). Co-author trailer says "Claude Sonnet 5" not "Claude Opus 5" per current §7 (this session's actual model).

### Security & production blast-radius — APPROVE WITH NOTES
Fixes a pre-existing IDOR: `GET /logs/timeline/:restaurantId` was previously guarded only by `JwtAuthGuard` — any signed-in account could read any house's data by editing the path id. This PR adds `assertMembership()`, correctly keyed on `public.users.user_id`. All 5 restaurant-scoped registers filter on `restaurant_id`. No injection surface, no XSS sinks, flag genuinely OFF by default, new page is read-only (net security improvement over main).
Notes: `event_store` has no `restaurant_id` column (pre-existing schema) — reachable only via a non-enumerable correlation UUID, and `payload` is selected but never mapped into the response, so not currently exploitable; worth an OD/ADR line to scope it properly later.

### Adversarial pass — HOLDS
Specifically tried to overturn on: the unconditional gateway change breaking the documents-reports consumer (it doesn't — makes an already-stated-but-false comment there true); the `user.userId` identity landmine (correctly uses `public.users.user_id`, verified in `jwt.strategy.ts`); pagination stall (closed); every stated claim about guard/migration/ADR state (re-ran the guards live: `check_windowed_figures.py --self-test`, `check_no_seeded_defaults.py`, `check_adr_numbers_unique.py`, `check_decision_claims.sh`, gateway+web test suites — all green, migration version confirmed unique across every remote ref and worktree); and a concurrent-branch collision with `feat/mudavym-new-pages`, which also adds `mudavym_design_logs` via `add column if not exists` — idempotent either merge order, no duplicate registry entries on that branch. No confirmed blocking issue found.

### What I could not check
Did not independently re-run the full E2E suite locally (relied on CI, green); did not verify Vercel preview deploys (blocked by an unrelated free-tier rate limit, not a required check).

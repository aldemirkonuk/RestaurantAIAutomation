<!-- pr-audit-gate: pr=359 sha=5fc6807 verdict=BLOCK -->
## pr-audit-gate report — PR #359 @ `5fc6807`

**Verdict: BLOCK**

### Correctness & regression risk — APPROVE WITH NOTES
Open-stack push/pop is sound (`useId()`-keyed, cleanup on close and unmount, StrictMode-safe). Focus cascade cannot loop or throw and is a strict behavioral superset of the old single-candidate logic. Tests are non-tautological (both fail against `main`, pass against this branch) and are appended, not replacing the existing 18. Non-blocking notes: stack order is React-effect order, not z-order/`zIndex`-aware (no real call site hits this today); a dev-only HMR reset of the module-level array is harmless in prod.

### CLAUDE.md-and-ADR compliance — **BLOCK**
The diff quietly rewrites the `wide?` prop's JSDoc in `apps/web/src/components/mudavym/Sheet.tsx` (unrelated to either stated bug fix): it deletes the `ADR 0112` citation and the enforceable line *"a third needs an ADR"*, replacing it with *"This module ships ahead of the ADR that will eventually record the primitive's full policy."*

This is factually wrong: `.planning/decisions/0112-one-modal-policy-three-shapes-one-primitive.md` is **Status: Locked — ratified by the founder 2026-09-05**, and its own changelog records this exact `wide` prop (F3, "a 640 sheet") as part of that ratification. The module does not ship ahead of its ADR — the ADR is a week old and locked. Per CLAUDE.md §5: *"If you believe a locked decision is wrong, say so and propose superseding it — do not quietly work around it."* Loosening the enforceable guardrail inside a comment, in a PR framed purely as a bug fix, is exactly that quiet workaround, and no guard greps ADR citations inside `.tsx` comments to catch it mechanically.

Independently corroborated by the correctness-angle auditor.

### Security & production blast-radius — APPROVE WITH NOTES
No auth/migration/FK/tenant/secret/network surface touched. Module-level stack cannot leak or corrupt across a long SPA session; focus cascade cannot escape the overlay (`panel` carries `tabIndex={-1}` as the terminal candidate) — this is a security *improvement* over the old code, which could silently strand focus on `<body>` and leave the visual trap open. Minor notes: stack order can invert visually with a non-default `zIndex` call site (cosmetic); a hidden-first-candidate cascade could reach a destructive control faster (pre-existing class of risk, not introduced here).

### What blocks the merge
Only the compliance-angle finding. Fix: drop that one JSDoc hunk (restore the ADR 0112 citation and the "third needs an ADR" line) — the rest of the diff (both bug fixes + tests) is sound and independently confirmed by two angles.

### What I could not check
Did not run the test suite locally in this session (relied on CI, which is green for the appended tests); did not exhaustively audit every one of the 30+ Sheet/Panel/Popover call sites for the same-commit nested-overlay edge case, only the ones the correctness auditor swept.

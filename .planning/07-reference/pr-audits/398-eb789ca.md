<!-- pr-audit-gate: pr=398 sha=eb789ca3b20d8000784c4fc7ec6d12a547b6a525 verdict=PASS -->
## pr-audit-gate — PR #398 @ `eb789ca3b20d8000784c4fc7ec6d12a547b6a525` — **PASS**

This was the three-role audit under ADR 0090 (2026-09-17 amendment). **Final call: `VERDICT: HOLDS`.** Correctness reviewer: APPROVE. Adversarial reviewer: APPROVE.

**Before review.** All five required contexts pass at the head. The only non-passing checks are `PR Audit Gate` (`CANNOT CHECK [no-credit]`, not required) and Supabase Preview (skipped). `main` is still `1fba79f57`. No gate-owned paths are touched.

### What the first plan caught, fixed before review in `eb789ca3b`
The planner ran at `221dd1a60` and found five risks. Each is fixed, and each code fix is mutation-tested.
1. **Settle ordering.** A cancel before the commit could flash a half-turned frame. Runs are now cancelled in a layout effect after the settled phase commits, and again on unmount.
2. **A sign-in step landing mid-turn.** It no longer animates over the poem.
3. **Accessible name.** The house voice now describes the turn button (`aria-describedby`).
4. **`/register`.** The grid is now scoped to the front-matter leaf. `/register` renders as a block, and its 1440 screenshot is byte-identical to #397's capture.
5. **Decision record.** A bracket on ADR 0149 row 35.

### Correctness reviewer — APPROVE
- **Tests pass in isolation.** In an isolated archive at the head, EndpaperShell.test (16) and authPages.publicDesign (99 passed, 14 opt-in skips) pass.
- **Mutations are caught.** Each of these fails exactly its own test: dropping the pageKey guard, dropping `aria-describedby`, cancelling inside settle, and removing the `[data-endpaper-chrome]` filter (5 tests fail).
- **The CSS scope is right as written.** Only `.mdv-endpaper .mdv-ep-leaf--front-matter` sets `grid`. The div→span swap is inert, because both are `display:flex`.
- **The docs match the code.** The login.md Error row matches `signInNote`, and the ADR bracket and README carry the founder's words.
- **Inert, reduced motion, narrow screens and jsdom are correct.** `inert` covers the leaf for as long as the front matter shows, so an async step cannot steal focus.

### Adversarial reviewer — APPROVE
- **Only a click can open it.** There is no URL, storage or route-state path to open the front matter. `frontMatter` has one call site (`Login.tsx:522`) inside the house-path branch.
- **No risky markup or listeners.** There is no `dangerouslySetInnerHTML`. `data-endpaper-chrome` sits only on the turn button and the poem, never around a field. The Escape listener is cleaned up.
- **Every claim in the poem is cited to source:**
  - reads the invoice: `documents.controller.ts:145,854`;
  - keeps the count: `stock-status.ts:10-20`;
  - sends nothing until a hand says yes: `procurement.service.ts:5849`, `order-recurrence.service.ts:180,219`;
  - sealed with who, when, how much: `communications.controller.ts:932-933`, `order-recurrence.service.ts:95`;
  - notices the price that crept: the `price_jump` rule, `procurement.service.ts:3750` and `approval-thresholds.ts:185-194`. The planner corrected this: `costDrift` has no caller, so it is not evidence. The rule fires when the house has enabled it.

### Final call (planner) — HOLDS
- **Google One Tap while the poem is open is acceptable, and not a regression.** Its card is Google's own iframe outside the covered leaf, and `enableOneTap` predates this PR. Using it is a deliberate sign-in through the same `handleCredential` path, and the gateway still refuses unknown accounts. The same holds for the pre-existing OAuth-only redirect after a submit.
- **The inert timing window is resolved.** React 18 flushes `useEffect` synchronously for discrete events, and every path into the front matter starts from a click or keydown. The form is also still at full opacity until 47% of the turn.

### Non-blocking follow-ups
1. **A static guard on the CSS scope.** jsdom has no layout, so a later edit that widens `display:grid` would pass every test. Add a vitest that reads `endpaper.css`, and mutation-test it.
2. **Optionally, cancel One Tap while the front matter is open.**
3. **Drop the `phaseNow` ref that is written during render.** At worst it causes one cosmetic animation skip.
4. **The underline contrast** (2.3:1, WCAG 1.4.11) is already recorded in login.md §9 / §13 item 7.

### Not verified, and one correction
- **Author's word only:** the headless-Chrome captures, and the `/register` byte comparison. The class scoping backs that comparison up when read statically.
- **Closing turn:** not run in a real browser, but traced in the code.
- **Correctness reviewer's test coverage:** it ran only the two touched test files; CI ran the full suite and it passed.
- **Correction:** the planner's final note says the checkout was at `221dd1a60`. That is wrong; `git rev-parse HEAD` is `eb789ca3b`. The planner read `eb789ca3b` via `git show`, so its evidence is unaffected.

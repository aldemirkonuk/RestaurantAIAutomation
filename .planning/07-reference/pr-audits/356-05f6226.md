<!-- pr-audit-gate: pr=356 sha=05f6226 verdict=PASS -->
## pr-audit-gate — PR #356 @ `05f6226` — **PASS**

Required contexts: all 5 green on this head (CI Complete + Beverage identity key, Guest merge policy, Fresh database equals remote, Code queries only relations production has); Build, Lint TS/Py, Test TS/Py/E2E green. Non-required reds with non-code causes: CI `PR Audit Gate` (repo API key out of credit — it exits CANNOT CHECK and **audited nothing**; this review is the local skill's), Vercel previews (rate limit).

**Gate-owned path:** `.planning/decisions/README.md`. Escalated to the founder at the gate; **founder ruled: merge, but keep ADR 0042's index row unchanged.** The head does exactly that — the README diff vs main is only the added 0138 and 0140 rows.

Prior audit on `1552cf5` was BLOCK (compliance: eight door-outbox comments cited ADR 0139, an unrelated OAuth record). Fixed in `20898364`, with the ADR-0140 guard extended to fail on a wrong citation (proven with a control); `05f62264` restored the 0042 row.

### Correctness & regression — APPROVE WITH NOTES
Delta since `1552cf5` is comment-only under `apps/`. Targeted tests at `05f6226`: 7 files / 115 pass (door outbox + receiving). No door receipt destroyed or duplicated; flag-off legacy pages unaffected; `useStandaloneGround` correct; realtime narrowing still refreshes unclassified keys. Guard passes on the real file and fails on 0139 controls. Notes: guard's allowed-ADR set wider than needed and its regex misses forms like `ADR0139`; die ground not re-measured on a theme toggle without a re-render; corrupt drop JSON overwritten on next write; pre-auth receipt filed "house not recorded".

### CLAUDE.md & ADR compliance — APPROVE WITH NOTES
No wrong-ADR citation remains; README diff is only the two added rows; ADRs 0138/0140 carry rationale and rejected options; 0042/0131 narrowed openly inside ADR 0138. Notes: PR body's head reference was stale (corrected); guard checks only `doorOutbox.ts`, not the other four fixed files; guard error message subtracts only `{0140}`.

### Security & blast radius — APPROVE WITH NOTES
No runtime change in the delta; guard script imports `os`/`re`/`sys` only. No migrations, no gateway code. Per-restaurant drop keys; legacy unattributed drop records adopted once by the first house on a device, marked "house unknown" (label + date, local only). Stock events no longer refresh the `['wines']` master library (no stock column).

### Adversarial pass — HOLDS
Every `mudavym` className outside `components/mudavym/` and `pages/*/next/` is covered (both dies via `useStandaloneGround`; onboarding and CanonicalSheet via `data-ground="paper"`; Sidebar hint only inside the Mudavym shell). No path removes a receipt without a server success or a saved drop record (`doorOutbox.ts:580-625`).

### Not checked — stated, not omitted
No pixels rendered anywhere in this review. `cn()`-built classNames and CSS descendant selectors of `.mudavym` not exhaustively traced by the adversary. `useStandaloneGround` inside portals and on theme toggle not exercised. Web ESLint not run locally (CI lint passed). ADR 0138's production counts (11 of 20 flags on, one row) not re-queried by reviewers.

# PR #457 audit — BLOCK

<!-- pr-audit-gate: pr=457 sha=1833ad8 verdict=BLOCK -->

- **PR:** https://github.com/aldemirkonuk/RestaurantAIAutomation/pull/457
- **Head:** `1833ad8dcfce718f3e801dcba9d8eebc3ae55bf1`
- **Base:** `origin/main` `ddc5e094b` (PR contains main; not behind)
- **Verdict:** BLOCK
- **Merged:** no
- **Gate-owned paths in the diff:** none
- **Adversarial pass:** skipped — compliance angle returned BLOCK (skill step 6)

## CI

Required contexts on `main` were green on this SHA before the audit: CI Complete, Beverage identity key — SQL matches Python, Guest merge policy — zero false merges, Fresh database equals remote, Code queries only relations production has.

Earlier head `3ce8d5423` failed “Decision register matches reality” because the nightly manifest still required “Saved schedules could not be loaded” after the page stopped rendering it. `1833ad8dc` drops that sentence from `apps/web/e2e/nightly/manifest.json`. `python3 scripts/check_nightly_manifest.py` passed locally on this SHA. That alignment is what the compliance angle then blocked: the guard only checks that the manifest matches the page, not that the page still says what locked ADR 0083 recorded.

## Angles

### Correctness — APPROVE WITH NOTES

Receipt embed uses the same procurement document id and canonical endpoint the Orders receipt sheet already uses. Communications still records schedules and Gmail failures on the hook; only the banner names shrink. No flag writes. No Inventory or Promotions code.

Notes: embedded sheet writes do not refresh the review panel (`ReceiptsNext.tsx` embed); the sheet is stacked above the panel, not “on the right”; account menu has no Escape/focus handling; receipts tests stub `CanonicalDocumentPage`.

### CLAUDE.md / ADR — BLOCK

Locked ADR 0083’s recorded outcome (`decisions/README.md` index) is “`schedulesError` restored; one banner names all five sources.” Finding 4 (`0083-a-page-may-not-claim-a-write-it-never-makes.md:58-72`) is the “Saved schedules could not be loaded” sentence. This PR deletes that card and shrinks `PAGE_SOURCE_LABELS` to three sources (`useCommsNextData.ts:81-92`) with a code comment citing “founder, 2026-09-22”, and no ADR or amendment. CLAUDE.md §5: a locked decision is not quietly worked around.

Failure scenario: `GET /reports/schedules` still 500s because `public.scheduled_reports` was never migrated. The hook still sets `failed.schedules`, nothing renders it, and the nightly phrase is gone, so ADR 0135’s walk can pass `/communications` while the defect is live.

### Security / blast-radius — APPROVE WITH NOTES

No new endpoint, migration, auth path, secret, or flag flip. Canonical sheet writes are the same house-scoped surface already live on Orders. Notes: Gmail-watch alarm leaves the house page (silence, not a false “configured”); `schedulesQ` and `gmailQ` still fire; nightly no longer watches the schedules 500; two write surfaces on one receipt.

## What was not checked

- Adversarial pass (skipped on BLOCK).
- Full test suite was not re-run here; required CI was green.
- No SARIF or coverage artifacts fetched.
- Page was not opened in a browser.
- Advisory “PR Audit Gate” workflow was pending when the angles started; it is not a required context.

## To clear

Record the founder’s ruling as an amendment to ADR 0083 (or a superseding ADR) before this communications change merges, and keep the missing `scheduled_reports` table visible to a check that does not depend on the house page still saying the sentence.

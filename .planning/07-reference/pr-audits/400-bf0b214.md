<!-- pr-audit-gate: pr=400 sha=bf0b214012c5ef97c2a0c031da2c0a538baed0e6 verdict=PASS -->
# PR audit gate — PR #400, head bf0b214012c5ef97c2a0c031da2c0a538baed0e6 — **PASS**

**PR:** fix(conversations): a conversation id opens only for the house that owns it (ADR 0171). Six `/conversations` routes (`POST :id/approve`, `PUT :id/message`, `POST :id/reject`, `POST :id/summarize`, `GET :id`, `GET pending/list`) acted on a bare id or on every house. Now every one answers only for the token's house, a foreign id is a 404, and approve/edit/reject take owner or manager.

**Composition (ADR 0090, 2026-09-17 amendment):** one Opus planner (planned, then resumed for final adjudication), two independent Sonnet reviewers (correctness/regression/decision-compliance; security/adversarial). The reviewers approved d4822c196 and then 39dc857b1 (round 2 was a delta re-verify after fixes for round-1 findings). The head then moved to bf0b21401 only because `main` is strict: it is 39dc857b1 plus a merge of origin/main's two commits (#398, #399). `git diff 39dc857b1 bf0b21401 -- apps/api-gateway/src/conversations` is empty.

## CI
All 5 required contexts SUCCESS on this head: CI Complete; Beverage identity key — SQL matches Python; Guest merge policy — zero false merges; Fresh database equals remote; Code queries only relations production has. The non-required `PR Audit Gate` (CI-side backstop) is CANNOT CHECK [no-credit] (Anthropic credit balance too low), a documented non-functional backstop, not a finding about this diff.

## Gate-owned paths
None touched (`decisions/README.md`, `CLAUDE.md`, the gate's own files, `settings.json`, `ci.yml`, `deploy.yml`).

## Round 1 findings (head d4822c196) and what was done
| # | Finding | Result |
|---|---|---|
| C1 Medium | ADR decision 2 / spec title overclaimed that database text no longer leaves; write routes still return a service error string as a 400 | ADR scoped and the behaviour named under "Named and not decided"; spec describe retitled. Not fixed in code (pre-existing, reaches only an owner/manager of the caller's own house). |
| C2 Low | Staff-403 consequence named `ConversationApprovalNotification.tsx`, which nothing imports and which posts with bare axios | Consequence rewritten: decision 3 is API-only today. |
| C3 Low | CLAIMS verify did not pin the zero-row check, `houseOf`'s throw, `requireHouse`, the UUID guard | Verify extended; 27/27 author mutants and the reviewer's own 24 kills. |
| C4 Low | Wrong cite `communications.controller.ts:1008` | Now `:1004-1014`. |
| C5 Low | "every by-id read and write" scope wording | Scoped to the six routes. |
| C6 Nit | An error with an empty message would read as success in `updateOwned` | `error.message \|\| "update failed"`; adversary confirmed all three call sites test truthiness. |
| A1 Low | Same raw-text-on-400 finding | as C1 |
| A2 **Medium, adjacent, not a regression** | `listConversations` (`if (options.restaurantId)`) and `getStats` (`if (restaurantId)`) fail open for a session that names no house; a removed member's next login names none | Named in ADR 0171 and 44.1v. **Not fixed here; not blocking** (identical on `origin/main`, outside the six routes). `getStats` has no owner. |

## Adversary's clean checks
Guard order, `@Roles` resolution, role null/''/staff/ADMIN, no-house 403 without an oracle, 404 identical for foreign/missing/malformed ids, parameterised `.eq()` only, PostgREST zero-row semantics, service-role RLS bypass so the explicit filter is the only scope, every other `procurement_conversations` writer takes internal ids from already-scoped rows, E2E publisher behind `NonProductionGuard`.

## Opus final adjudication (VERDICT: HOLDS)
Re-derived the six routes from the code: one `.update(` in the service, three `.eq("id", conversationId)` each immediately followed by `.eq("restaurant_id", restaurantId)`, `summarize` 404s before any publish, `approved:false` re-enters `rejectConversation` and still 404s a foreign id. The spec's fake matches PostgREST (`.update().eq().eq().select("id")` on zero rows is HTTP 200, `[]`, no error). The merge did not change the conversations code, CLAIMS survived (363/363 hold), ADR guard OK. The two recorded-not-blocked items are pre-existing and neither makes a PASS unsafe: holding this would leave six cross-tenant routes open on main to close nothing.

## What could not be checked (limitations, not silently omitted)
- **Live PostgREST / a real Supabase database.** Zero-row and `maybeSingle` behaviour is from documented semantics and a faithful fake, not a live run.
- The author's "14 of 14 spec mutants" figure was not re-run by any reviewer. (The "23 of 29 fail on the unfixed tree" figure was re-run by the correctness reviewer.)
- **The founder's 2026-09-19 fix brief** is not in the repo, so "directed by the founder" in ADR 0171 is unverifiable from the repo.
- The CI-side `PR Audit Gate` did not run (no credit); coverage/SARIF artifacts were not fetched.
- Fail-open on `GET /conversations` and `GET /conversations/stats/overview` for a house-less session was confirmed from source, not run end to end.

## Governance notes for the merger
- ADR 0171 is **Proposed / locked in part.** Three things are the author's assumptions the founder has not decided: edit and reject share approve's role, the by-id read and summarize take no role, and a no-house session is a 403. Treat them as revertable proposals. No web or mobile screen calls approve/edit/reject today, so the role gate is API-only until the one-tap lane.
- Whether this legacy path must also require the ADR 0112 seal is an open founder fork in the one-tap lane; recorded, not decided.
- `by-order` and `by-provider` are NOT covered by this PR. The fix exists only as uncommitted work in worktree `admiring-stonebraker-c367d1` (the branch ref carries no commits); that work files an open tripwire claim and a `44.1u` entry that will need flipping when this lands.

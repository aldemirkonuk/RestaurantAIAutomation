<!-- pr-audit-gate: pr=392 sha=c1ec9a2079747ff7f0fdd79bc0c5c01c951b8200 verdict=PASS -->
## PR Audit Gate: PASS (ADR 0090, 2026-09-17 pipeline)

The pipeline: an Opus planner, then two Sonnet reviewers in parallel, then the same Opus planner resumed for the final say. Pinned to head `c1ec9a2079747ff7f0fdd79bc0c5c01c951b8200`, base `3a752010d`. All 5 required contexts are green; the CI-side "PR Audit Gate" job fails on an Anthropic no-credit error and is not required. No gate-owned path is touched (checked against `_GATE_OWNED_PATHS`, `scripts/pr_audit_gate.py:485-520`).

**Planner (Opus), first call: PLAN: READY.** It flagged the fix itself as low risk. Two things it named are outside this PR: the web's two legacy helpers, which call nothing and are deferred to the ADR 0149 manifest, and the root cause that `generateTokens` falls back to `users.role`.

**Reviewer 1 (Sonnet, correctness and compliance): APPROVE.**
- The CLAIMS verify passes on head and fails on base.
- The spec is collected, passes 4/4 on head, and fails exactly 3 of 4 on base's files, run live.
- The baseline change from 2 to 1 is an exact recount (184/184).
- Gateway `tsc` 0, and `check_route_exposure` passes (42 public, 624 guarded, 0 undeclared).
- The addendum form matches 0147's existing bracket, and there's no new top-level doc.
- No live caller in web or mobile, and `src/auth` passes 18 suites, 203 tests.

**Reviewer 2 (Sonnet, security and adversarial): APPROVE WITH NOTES.**
- The route throws before binding or any service call.
- No remaining path writes `users.restaurant_id` or `role` from input without an access row. It checked registerRestaurant, joinViaInvite, acceptInviteAsExistingUser, OAuth and updateProfile.
- No e2e, simulate or seed tooling depends on the route.
- Nothing rewrites the 410: no global filters, SentryInterceptor rethrows anything below 500, and TenantGuard skips `@Public`.

**Planner, final call: VERDICT: HOLDS.** It rechecked the claim, the baseline and the severity against the original evidence: `TenantGuard` trusts the token (`tenant.guard.ts:77-81`), and no guard reads `user_restaurant_access`.

**Follow-ups (not blocking; to be fixed in the next PR):**
1. **The records undercount membership re-checks.** `operating-hours.service.ts:63,100` also calls `assertMembership`. The controller comment, the CLAIMS text, 44.1g and the PR body each say otherwise. The conclusion doesn't change.
2. **Invite role escalation (security, pre-existing).**
   - `InviteDto.role` accepts `owner`, and `POST /auth/invite` allows owner or manager.
   - `generateInvite` checks only that the caller has access to the house, never that the invited role is at or below the caller's own. So a manager can mint an owner invite.
   - `RolesGuard` reads the token's house, not the body's `restaurantId`.
3. **The 0147 row in `decisions/README.md` doesn't mention the closure.** That file is gate-owned, so it needs its own PR.
4. **`atlas-graph.json` still lists `POST /auth/register`.** Regenerate it; don't hand-edit it.

**Could not check:** the production user count in 44.1g, which reviewers can't read from their sandbox; it isn't load-bearing. No PR-supplied code was run with privileged access.

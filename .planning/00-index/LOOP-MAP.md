---
type: moc
title: Loop Map
updated: 2026-08-24
---

# Loop Map

> Generated by `scripts/build_loop_index.py` from every `*-loops.md`. **Do not hand-edit.**

**485 loops** across **100 units** in **8 divisions**.

Loop data now also lives in each file's frontmatter (`loop_ids`, `loop_close_times`,
`loop_statuses`) so Dataview can query it — the fenced YAML bodies alone were not
machine-readable, which broke the promise in ORG_STRUCTURE §5.

## Status

| Status | Loops |
|---|---|
| `proposed` | 438 |
| `blocked` | 31 |
| `dormant` | 9 |
| `active` | 3 |
| `gated` | 2 |
| `running` | 2 |

> Only the `active`/`running` rows describe something that actually runs today — **5 of 485**, each citing the mechanism that closes it. The other statuses are written down, not cycling.

## Close-time distribution

| close_time | Loops |
|---|---|
| `monthly` | 150 |
| `weekly` | 147 |
| `per-event` | 52 |
| `quarterly` | 46 |
| `per-pr` | 38 |
| `daily` | 26 |
| `fortnightly` | 21 |
| `one-shot` | 3 |
| `hourly` | 2 |

**9 of the 9 permitted `close_time` values are in use.** Both `close_time` and `status` are closed vocabularies, locked by OD-47 in ORG_STRUCTURE §5.1 and read straight from that table by this script — a value outside either set fails CI rather than landing here.

## By division

| Division | Units | Loops |
|---|---|---|
| advisory | 3 | 17 |
| applied-ai | 10 | 46 |
| commercial | 17 | 74 |
| corporate | 16 | 75 |
| intelligence | 8 | 39 |
| platform | 20 | 101 |
| product | 21 | 105 |
| research-math | 5 | 28 |

## Live Dataview query

```dataview
TABLE loop_count, loop_ids, loop_close_times
FROM "01-org" OR "02-advisory"
WHERE type = "loops"
SORT loop_count DESC
```
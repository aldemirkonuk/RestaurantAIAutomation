# Adversary brief — overlay behaviours research (sketch 102), 2026-09-05

Read the four files in this directory: A-command-first.md, B-ops-finance.md, C-ai-mobile.md, D-implementation.md.
For EVERY pattern in their tables, judge it against the house rules and return a verdict table
(file · pattern · verdict KEEP / ADAPT / REJECT · one-line reason · the rule it touches):

House rules that a pattern must survive:
1. Three shapes over one primitive (ADR 0112, Locked): a right sheet for one object (440/640), a centred panel for a question (620), an anchored popover for a choice (320). A pattern that needs a FOURTH modal shape is REJECT unless it is non-modal (no scrim, no focus trap) — then say so explicitly.
2. The seal (hold-to-approve) is wax for a real commitment only; bulk gets the plain die; the seal NEVER sits in a popover; settings stay asserted, orders/payments/writes are sealed.
3. AI proposes, a person applies (ADR 0113). Nothing auto-applies; a draft never looks sent.
4. Absence is never reported as health: no invented zeros, no fabricated figures; every figure names its rows (ADR 0020, 0051).
5. One chromatic colour (İznik teal) + paper/ink; no emoji; the close control is words, never an X; motion tokens tuck/settle/ink; prefers-reduced-motion renders no animation.
6. Ledger rows expand IN PLACE to show the working; the house prefers expansion over a new surface when the reader stays on the list.
7. Legacy pages stay byte-identical while their flag is off; anything global must be gated by the shell store.
8. Ceremony is rationed: the founder called the platform "hospitality and passion while technically superior" — quiet, not flashy.

Also: (a) flag any pattern whose evidence URL does not actually substantiate the claim (fetch it and check); (b) flag duplicates across the four files; (c) name the 10 patterns you would put in front of the founder first, and the 5 you would fight hardest against; (d) say what the four files MISSED — a behaviour you know a top product has that none of them listed, with evidence.
Write your verdicts to /Users/aldemirkonuk/Projects/modals-scratch/research/E-adversary.md and reply with a 6-line summary. Do not edit any git repository.

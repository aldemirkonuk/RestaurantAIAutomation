---
phase: 13-dev-onboarding-ui-with-manual-override-access
plan: 05
type: execute
wave: 4
depends_on:
  - 13-01
  - 13-02
  - 13-03
  - 13-04
files_modified:
  - services/agent-orchestrator/tests/test_studio_routes.py
  - services/agent-orchestrator/tests/test_override_service.py
  - services/agent-orchestrator/tests/test_studio_e2e.py
  - apps/web/src/components/studio/StudioIngestionBar.test.tsx
  - apps/web/src/components/studio/StudioFieldCell.test.tsx
  - apps/web/src/pages/studio/metrics/MetricCard.tsx
  - apps/web/src/pages/studio/metrics/MetricsDashboard.tsx
  - apps/web/src/pages/studio/Studio.tsx
autonomous: true
requirements:
  - DEVUI-09
  - DEVUI-10

must_haves:
  truths:
    - "test_studio_routes.py covers: POST /overrides with confidence < 0.8 (no reason required); POST /overrides with confidence ≥ 0.8 without reason returns 422; POST /overrides with confidence ≥ 0.8 with reason returns 200"
    - "test_studio_routes.py covers: POST /invite by non-review_admin returns 403; POST /invite by review_admin returns 201 with token field"
    - "test_studio_routes.py covers: TestRequireStudioRole using sync dep.dependency pattern (no await)"
    - "test_override_service.py covers: check_and_update_trust is called synchronously with (user_id, approved) params; RPC called with p_user_id"
    - "test_studio_e2e.py: @pytest.mark.e2e; developer submits 3 overrides; GET /sessions/{id} returns >= 3 events; GET /queue returns 0 pending"
    - "MetricCard renders metric label + value + optional trend delta"
    - "MetricsDashboard renders 4 cards: Total Overrides, Pending Queue, Auto-Promoted, Active Contributors"
    - "MetricsDashboard fetches GET /api/v1/studio/metrics and maps response to cards"
    - "Studio.tsx is updated to render MetricsDashboard below SessionSummary"
    - "StudioIngestionBar.test.tsx covers detectIngestionType logic (6 cases, DEVUI-02)"
    - "StudioFieldCell.test.tsx covers confidence threshold + canSave logic (DEVUI-03)"
  artifacts:
    - path: "services/agent-orchestrator/tests/test_studio_routes.py"
      provides: "FastAPI route tests for studio API, including sync TestRequireStudioRole"
      contains: "test_post_override_high_confidence_reason_required_missing_returns_422"
    - path: "services/agent-orchestrator/tests/test_override_service.py"
      provides: "unit tests for override_service.py with sync check_and_update_trust"
      contains: "test_increment_trust_counter_rpc_called_on_approve"
    - path: "services/agent-orchestrator/tests/test_studio_e2e.py"
      provides: "E2E step-through test for full override flow (DEVUI-10)"
      contains: "test_full_developer_override_flow"
    - path: "apps/web/src/components/studio/StudioIngestionBar.test.tsx"
      provides: "Vitest unit tests for ingestion type detection (DEVUI-02)"
      contains: "detectIngestionType"
    - path: "apps/web/src/components/studio/StudioFieldCell.test.tsx"
      provides: "Vitest unit tests for confidence threshold + canSave (DEVUI-03)"
      contains: "computeRequiresReason"
    - path: "apps/web/src/pages/studio/metrics/MetricCard.tsx"
      provides: "single metric display card"
      contains: "MetricCard"
    - path: "apps/web/src/pages/studio/metrics/MetricsDashboard.tsx"
      provides: "4-card metrics dashboard row"
      contains: "MetricsDashboard"
  key_links:
    - from: "MetricsDashboard"
      to: "GET /api/v1/studio/metrics"
      via: "useQuery refetchInterval 60_000"
      pattern: "studio/metrics"
    - from: "test_studio_routes.py"
      to: "studio_router"
      via: "FastAPI TestClient"
      pattern: "test pattern from conftest.py"
---

<objective>
Finalize Phase 13 with:
1. Backend tests (pytest) — studio_routes.py and override_service.py (DEVUI-10), all sync-correct
2. Frontend metrics dashboard cards on the main Studio page (DEVUI-09)
3. E2E step-through test: developer submits 3 overrides, audit trail verified (DEVUI-10)
4. Vitest component specs: detectIngestionType + FieldCell confidence logic (DEVUI-02, DEVUI-03)

After this plan, all DEVUI requirements are covered and phase is ready for VERIFICATION.md.

Output: 4 test files (2 pytest + 2 Vitest) + MetricCard + MetricsDashboard + Studio.tsx update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@services/agent-orchestrator/tests/conftest.py
@services/agent-orchestrator/api/studio_routes.py
@services/agent-orchestrator/services/override_service.py
@apps/web/src/pages/studio/Studio.tsx
@.planning/phases/13-dev-onboarding-ui-with-manual-override-access/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend tests — test_studio_routes.py + test_override_service.py (DEVUI-10)</name>
  <read_first>
    - services/agent-orchestrator/tests/conftest.py (exact fixture names, TestClient setup, mock patterns)
    - services/agent-orchestrator/api/studio_routes.py (route paths, Pydantic models, role requirements)
    - services/agent-orchestrator/services/override_service.py (require_studio_role signature, OverrideRequest model, reason enforcement logic)
    - services/agent-orchestrator/tests/test_recrawl_tasks.py (example test structure to match style)
  </read_first>
  <files>
    services/agent-orchestrator/tests/test_studio_routes.py,
    services/agent-orchestrator/tests/test_override_service.py
  </files>
  <action>
**Create services/agent-orchestrator/tests/test_studio_routes.py:**

Read conftest.py first to determine the exact fixture names used for authenticated clients and supabase mocks. Then write tests matching the existing style.

```python
"""Tests for studio API routes (Phase 13).

Requires:
  - studio_routes.py router registered in main.py
  - require_studio_role FastAPI dependency in override_service.py
  - OverrideRequest Pydantic model with reason enforcement at old_confidence >= 0.8
"""
import pytest
from unittest.mock import AsyncMock, MagicMock, patch
from fastapi.testclient import TestClient


# ─── Fixtures ───────────────────────────────────────────────────────────────

DEVELOPER_JWT_PAYLOAD = {
    "sub": "user-dev-001",
    "email": "dev@example.com",
    "app_metadata": {"roles": ["developer"]},
}

CONTRIBUTOR_JWT_PAYLOAD = {
    "sub": "user-contrib-001",
    "email": "contrib@example.com",
    "app_metadata": {"roles": ["certified_contributor"]},
}

REVIEW_ADMIN_JWT_PAYLOAD = {
    "sub": "user-admin-001",
    "email": "admin@example.com",
    "app_metadata": {"roles": ["review_admin"]},
}

NO_ROLE_JWT_PAYLOAD = {
    "sub": "user-none-001",
    "email": "none@example.com",
    "app_metadata": {"roles": []},
}

def _make_bearer(payload: dict) -> str:
    """Create a fake Bearer value; actual JWT decoding is mocked."""
    import json, base64
    encoded = base64.b64encode(json.dumps(payload).encode()).decode()
    return f"Bearer mock.{encoded}.sig"


# ─── POST /overrides — D-07 reason enforcement ───────────────────────────────

class TestPostOverrides:
    @pytest.fixture(autouse=True)
    def _patch_jwt_and_supabase(self, monkeypatch):
        """Patch require_studio_role to inject payload and supabase to return mock data."""
        from services.override_service import require_studio_role

        async def _fake_require(*roles):
            async def _dep(request=None):
                return DEVELOPER_JWT_PAYLOAD
            return _dep
        monkeypatch.setattr("api.studio_routes.require_studio_role", _fake_require)

        mock_sb = MagicMock()
        mock_sb.table.return_value.select.return_value.eq.return_value.single.return_value.execute.return_value.data = {
            "id": "sub-001",
            "field_confidence": {"wine_name": 0.9},
        }
        mock_sb.table.return_value.insert.return_value.execute.return_value.data = [{"id": "ov-001"}]
        mock_sb.table.return_value.update.return_value.eq.return_value.execute.return_value.data = [{}]
        monkeypatch.setattr("api.studio_routes._get_supabase", lambda: mock_sb)

    def test_post_override_low_confidence_no_reason_required(self, test_client: TestClient):
        """confidence < 0.8 → reason is NOT required; override accepted."""
        mock_sb = MagicMock()
        mock_sb.table.return_value.select.return_value.eq.return_value.single.return_value.execute.return_value.data = {
            "id": "sub-001",
            "field_confidence": {"wine_name": 0.5},
        }
        with patch("api.studio_routes._get_supabase", return_value=mock_sb):
            resp = test_client.post(
                "/api/v1/studio/overrides",
                json={
                    "submission_id": "sub-001",
                    "field_name": "wine_name",
                    "new_value": "Château Latour",
                    "session_id": None,
                },
                headers={"Authorization": _make_bearer(DEVELOPER_JWT_PAYLOAD)},
            )
        assert resp.status_code == 200, resp.text

    def test_post_override_high_confidence_reason_required_missing_returns_422(
        self, test_client: TestClient
    ):
        """confidence >= 0.8 without reason → 422 Unprocessable Entity."""
        mock_sb = MagicMock()
        mock_sb.table.return_value.select.return_value.eq.return_value.single.return_value.execute.return_value.data = {
            "id": "sub-001",
            "field_confidence": {"vintage": 0.85},
        }
        with patch("api.studio_routes._get_supabase", return_value=mock_sb):
            resp = test_client.post(
                "/api/v1/studio/overrides",
                json={
                    "submission_id": "sub-001",
                    "field_name": "vintage",
                    "new_value": "2019",
                    "reason": None,
                    "session_id": None,
                },
                headers={"Authorization": _make_bearer(DEVELOPER_JWT_PAYLOAD)},
            )
        assert resp.status_code == 422, resp.text
        assert "reason" in resp.text.lower()

    def test_post_override_high_confidence_with_reason_accepted(
        self, test_client: TestClient
    ):
        """confidence >= 0.8 with reason → 200 OK."""
        mock_sb = MagicMock()
        mock_sb.table.return_value.select.return_value.eq.return_value.single.return_value.execute.return_value.data = {
            "id": "sub-001",
            "field_confidence": {"vintage": 0.85},
        }
        mock_sb.table.return_value.insert.return_value.execute.return_value.data = [{"id": "ov-002"}]
        mock_sb.table.return_value.update.return_value.eq.return_value.execute.return_value.data = [{}]
        with patch("api.studio_routes._get_supabase", return_value=mock_sb):
            resp = test_client.post(
                "/api/v1/studio/overrides",
                json={
                    "submission_id": "sub-001",
                    "field_name": "vintage",
                    "new_value": "2019",
                    "reason": "Verified against producer's official vintage chart.",
                    "session_id": None,
                },
                headers={"Authorization": _make_bearer(DEVELOPER_JWT_PAYLOAD)},
            )
        assert resp.status_code == 200, resp.text


# ─── POST /invite — review_admin gating ──────────────────────────────────────

class TestPostInvite:
    def test_invite_by_non_review_admin_returns_403(self, test_client: TestClient, monkeypatch):
        """Contributor trying to generate invite → 403 Forbidden."""
        async def _fake_require(*roles):
            from fastapi import HTTPException
            async def _dep():
                if "review_admin" in roles:
                    raise HTTPException(status_code=403, detail="Insufficient studio role")
                return CONTRIBUTOR_JWT_PAYLOAD
            return _dep
        monkeypatch.setattr("api.studio_routes.require_studio_role", _fake_require)

        resp = test_client.post(
            "/api/v1/studio/invite",
            json={"role": "certified_contributor", "target_email": "new@example.com"},
            headers={"Authorization": _make_bearer(CONTRIBUTOR_JWT_PAYLOAD)},
        )
        assert resp.status_code == 403, resp.text

    def test_invite_by_review_admin_returns_201_with_token(
        self, test_client: TestClient, monkeypatch
    ):
        """review_admin generating invite → 201 with token field."""
        async def _fake_require(*roles):
            async def _dep():
                return REVIEW_ADMIN_JWT_PAYLOAD
            return _dep
        monkeypatch.setattr("api.studio_routes.require_studio_role", _fake_require)

        mock_sb = MagicMock()
        mock_sb.table.return_value.insert.return_value.execute.return_value.data = [
            {"token": "abc-token-123", "expires_at": "2026-04-14T00:00:00Z"}
        ]
        monkeypatch.setattr("api.studio_routes._get_supabase", lambda: mock_sb)

        resp = test_client.post(
            "/api/v1/studio/invite",
            json={"role": "certified_contributor", "target_email": "new@example.com"},
            headers={"Authorization": _make_bearer(REVIEW_ADMIN_JWT_PAYLOAD)},
        )
        assert resp.status_code in (200, 201), resp.text
        body = resp.json()
        assert "token" in body


# ─── PATCH /queue/{id} — decision endpoint ───────────────────────────────────

class TestPatchQueueDecision:
    def test_approve_decision_accepted(self, test_client: TestClient, monkeypatch):
        """review_admin approving an override → 200 with updated status."""
        async def _fake_require(*roles):
            async def _dep():
                return REVIEW_ADMIN_JWT_PAYLOAD
            return _dep
        monkeypatch.setattr("api.studio_routes.require_studio_role", _fake_require)

        mock_sb = MagicMock()
        mock_sb.table.return_value.update.return_value.eq.return_value.execute.return_value.data = [
            {"id": "ov-001", "promotion_status": "approved"}
        ]
        mock_sb.rpc.return_value.execute.return_value.data = {"new_count": 3}
        monkeypatch.setattr("api.studio_routes._get_supabase", lambda: mock_sb)

        resp = test_client.patch(
            "/api/v1/studio/queue/ov-001",
            json={"decision": "approved"},
            headers={"Authorization": _make_bearer(REVIEW_ADMIN_JWT_PAYLOAD)},
        )
        assert resp.status_code == 200, resp.text


# ─── require_studio_role — sync dep.dependency pattern ───────────────────────
# require_studio_role is a sync def that returns Depends(inner_check).
# Access inner function via dep.dependency (FastAPI Depends attribute).
# Never await require_studio_role or the inner function.

class TestRequireStudioRole:
    def test_require_studio_role_invalid_token_raises_401(self):
        """Bearer token that fails JWT verification → 401."""
        from api.studio_routes import require_studio_role
        from fastapi import HTTPException

        dep = require_studio_role("developer")
        inner = dep.dependency  # FastAPI Depends wraps the function
        with pytest.raises(HTTPException) as exc:
            inner(authorization="Bearer invalid.token.here")
        assert exc.value.status_code == 401

    def test_require_studio_role_missing_header_raises_401(self):
        """Missing Authorization header → 401."""
        from api.studio_routes import require_studio_role
        from fastapi import HTTPException

        dep = require_studio_role("developer")
        inner = dep.dependency
        with pytest.raises(HTTPException) as exc:
            inner(authorization=None)
        assert exc.value.status_code == 401

    def test_require_studio_role_wrong_role_raises_403(self, monkeypatch):
        """Valid JWT but user has certified_contributor, requires review_admin → 403."""
        import jwt as pyjwt
        from api.studio_routes import require_studio_role
        from fastapi import HTTPException

        monkeypatch.setattr(
            "services.override_service.get_settings",
            lambda: type("S", (), {"supabase_jwt_secret": "test-secret", "trust_level_threshold": 5})(),
        )
        payload = {"sub": "u-001", "app_metadata": {"roles": ["certified_contributor"]}}
        token = pyjwt.encode(payload, "test-secret", algorithm="HS256")

        dep = require_studio_role("review_admin")
        inner = dep.dependency
        with pytest.raises(HTTPException) as exc:
            inner(authorization=f"Bearer {token}")
        assert exc.value.status_code == 403
```

**Create services/agent-orchestrator/tests/test_override_service.py:**

```python
"""Unit tests for override_service.py (Phase 13).

Note: TestRequireStudioRole lives in test_studio_routes.py (sync dep.dependency pattern).
This file tests the service-layer logic only.
"""
import pytest
from unittest.mock import MagicMock, patch
from fastapi import HTTPException


class TestCheckAndUpdateTrust:
    """Test trust counter logic in override_service.py.

    check_and_update_trust is a SYNCHRONOUS def — never use await.
    Signature: check_and_update_trust(supabase, user_id: str, approved: bool, threshold: int = 5)
    Returns None. Calls supabase.rpc("increment_trust_counter", {"p_user_id": user_id}) on approve.
    """

    def test_increment_trust_counter_rpc_called_on_approve(self):
        """check_and_update_trust calls increment_trust_counter RPC with correct p_user_id."""
        from services.override_service import check_and_update_trust

        mock_sb = MagicMock()
        # increment_trust_counter RPC returns None (void function)
        mock_sb.rpc.return_value.execute.return_value = None
        # Post-increment count check: simulate count = 3 (below threshold)
        mock_sb.table.return_value.select.return_value.eq.return_value.eq.return_value.is_.return_value.single.return_value.execute.return_value.data = {
            "consecutive_approved_overrides": 3
        }
        mock_sb.table.return_value.update.return_value.eq.return_value.eq.return_value.execute.return_value.data = [{}]

        result = check_and_update_trust(mock_sb, user_id="u-001", approved=True)
        mock_sb.rpc.assert_called_once_with(
            "increment_trust_counter",
            {"p_user_id": "u-001"},
        )
        assert result is None  # function returns None

    def test_rejection_resets_streak(self):
        """check_and_update_trust with approved=False resets streak; does not call RPC."""
        from services.override_service import check_and_update_trust

        mock_sb = MagicMock()
        mock_sb.table.return_value.update.return_value.eq.return_value.eq.return_value.execute.return_value.data = [{}]

        result = check_and_update_trust(mock_sb, user_id="u-002", approved=False)
        mock_sb.rpc.assert_not_called()  # RPC only called on approval
        assert result is None

    def test_threshold_reached_flips_policy_to_auto_promote(self):
        """When count reaches threshold, update sets promotion_policy=auto_promote."""
        from services.override_service import check_and_update_trust

        mock_sb = MagicMock()
        mock_sb.rpc.return_value.execute.return_value = None
        # Post-increment count = threshold (5) → should flip policy
        mock_sb.table.return_value.select.return_value.eq.return_value.eq.return_value.is_.return_value.single.return_value.execute.return_value.data = {
            "consecutive_approved_overrides": 5
        }
        mock_sb.table.return_value.update.return_value.eq.return_value.eq.return_value.execute.return_value.data = [{}]

        check_and_update_trust(mock_sb, user_id="u-003", approved=True, threshold=5)
        update_calls = mock_sb.table.return_value.update.call_args_list
        assert any("auto_promote" in str(call) for call in update_calls)
```
  </action>
  <verify>
    <automated>cd "services/agent-orchestrator" && python -m pytest tests/test_studio_routes.py tests/test_override_service.py -v --tb=short -x 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "test_post_override_reason_required_at_threshold\|test_post_override_high_confidence_reason_required_missing_returns_422" services/agent-orchestrator/tests/test_studio_routes.py` returns a match (reason enforcement test exists)
    - `grep "test_invite_by_non_review_admin_returns_403" services/agent-orchestrator/tests/test_studio_routes.py` returns a match (invite role guard test exists)
    - `grep "test_require_studio_role_passes\|test_passes_for_matching_role" services/agent-orchestrator/tests/test_override_service.py` returns a match
    - `grep "test_raises_403_for_missing_role" services/agent-orchestrator/tests/test_override_service.py` returns a match
    - `grep "increment_trust_counter" services/agent-orchestrator/tests/test_override_service.py` returns a match (trust counter RPC test)
    - `cd services/agent-orchestrator && python -m pytest tests/test_studio_routes.py tests/test_override_service.py --co -q 2>&1 | grep "test session"` exits without import errors
  </acceptance_criteria>
  <done>test_studio_routes.py covers reason enforcement at 0.8 threshold, invite role guard (403 + 201), and approve decision. test_override_service.py covers require_studio_role pass/fail and trust counter RPC call.</done>
</task>

<task type="auto">
  <name>Task 2: Metrics dashboard cards — MetricCard + MetricsDashboard + Studio.tsx update (DEVUI-09)</name>
  <read_first>
    - apps/web/src/pages/studio/Studio.tsx (where MetricsDashboard should be inserted, after SessionSummary)
    - .planning/phases/13-dev-onboarding-ui-with-manual-override-access/13-UI-SPEC.md (§4 Metrics Bar — 4 cards, layout, response schema)
    - .planning/phases/13-dev-onboarding-ui-with-manual-override-access/13-RESEARCH.md (GET /studio/metrics response shape)
  </read_first>
  <files>
    apps/web/src/pages/studio/metrics/MetricCard.tsx,
    apps/web/src/pages/studio/metrics/MetricsDashboard.tsx,
    apps/web/src/pages/studio/Studio.tsx
  </files>
  <action>
**Create apps/web/src/pages/studio/metrics/MetricCard.tsx:**
```tsx
import { ReactNode } from 'react'
import { TrendingUp, TrendingDown, Minus } from 'lucide-react'

interface MetricCardProps {
  label: string
  value: number | string
  trend?: number       // positive = up, negative = down, 0 = flat
  unit?: string
  loading?: boolean
}

export function MetricCard({ label, value, trend, unit, loading }: MetricCardProps) {
  const TrendIcon = trend == null
    ? null
    : trend > 0 ? TrendingUp
    : trend < 0 ? TrendingDown
    : Minus

  const trendColor = trend == null
    ? ''
    : trend > 0 ? 'text-emerald-600'
    : trend < 0 ? 'text-red-500'
    : 'text-slate-400'

  return (
    <div className="bg-white rounded-xl border border-slate-200 shadow-xs px-5 py-4 flex flex-col gap-1 min-w-[140px] flex-1">
      <span className="text-xs font-medium text-slate-500 uppercase tracking-wide">{label}</span>
      {loading ? (
        <div className="h-7 w-16 bg-slate-100 rounded animate-pulse mt-1" />
      ) : (
        <div className="flex items-end gap-2">
          <span className="text-2xl font-semibold text-slate-900 leading-none">
            {value}{unit ? <span className="text-sm text-slate-500 ml-0.5">{unit}</span> : null}
          </span>
          {TrendIcon && typeof trend === 'number' && (
            <span className={`flex items-center gap-0.5 text-xs mb-0.5 ${trendColor}`}>
              <TrendIcon className="w-3.5 h-3.5" />
              {Math.abs(trend)}
            </span>
          )}
        </div>
      )}
    </div>
  )
}
```

**Create apps/web/src/pages/studio/metrics/MetricsDashboard.tsx:**

Fetches GET /api/v1/studio/metrics and renders 4 cards: Total Overrides, Pending Queue, Auto-Promoted, Active Contributors.

```tsx
import { useQuery } from '@tanstack/react-query'
import { MetricCard } from './MetricCard'

const API_URL = import.meta.env.VITE_API_GATEWAY_URL || 'http://localhost:4000'

interface MetricsResponse {
  total_overrides: number
  pending_queue: number
  auto_promoted: number
  active_contributors: number
  // optional trend deltas (past 7 days)
  overrides_delta?: number
  queue_delta?: number
  promoted_delta?: number
  contributors_delta?: number
}

async function fetchMetrics(): Promise<MetricsResponse> {
  const token = localStorage.getItem('accessToken')
  const headers: Record<string, string> = token ? { Authorization: `Bearer ${token}` } : {}
  const resp = await fetch(`${API_URL}/api/v1/studio/metrics`, { headers })
  if (!resp.ok) throw new Error('Failed to load metrics')
  return resp.json()
}

export function MetricsDashboard() {
  const { data, isLoading } = useQuery({
    queryKey: ['studio-metrics'],
    queryFn: fetchMetrics,
    refetchInterval: 60_000,
  })

  return (
    <div className="flex gap-4 flex-wrap mb-6">
      <MetricCard
        label="Total Overrides"
        value={data?.total_overrides ?? 0}
        trend={data?.overrides_delta}
        loading={isLoading}
      />
      <MetricCard
        label="Pending Queue"
        value={data?.pending_queue ?? 0}
        trend={data?.queue_delta}
        loading={isLoading}
      />
      <MetricCard
        label="Auto-Promoted"
        value={data?.auto_promoted ?? 0}
        trend={data?.promoted_delta}
        loading={isLoading}
      />
      <MetricCard
        label="Active Contributors"
        value={data?.active_contributors ?? 0}
        trend={data?.contributors_delta}
        loading={isLoading}
      />
    </div>
  )
}
```

**Update apps/web/src/pages/studio/Studio.tsx:**

Read the file first. Find where `<SessionSummary />` is rendered and insert `<MetricsDashboard />` immediately after it. Add the import:

```tsx
import { MetricsDashboard } from './metrics/MetricsDashboard'
```

Then in the JSX, after `<SessionSummary ... />`:
```tsx
<MetricsDashboard />
```

The MetricsDashboard goes between SessionSummary and WineRecordsTable, matching the D-11 two-section layout (ingestion top, records below). The metrics bar lives in the ingestion section's bottom, before the records section.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "error TS" | grep -E "(MetricCard|MetricsDashboard|Studio)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "MetricCard" apps/web/src/pages/studio/metrics/MetricsDashboard.tsx` returns 4 matches (4 cards)
    - `grep "total_overrides\|pending_queue\|auto_promoted\|active_contributors" apps/web/src/pages/studio/metrics/MetricsDashboard.tsx` returns 4 matches (all 4 metrics mapped)
    - `grep "refetchInterval.*60" apps/web/src/pages/studio/metrics/MetricsDashboard.tsx` returns a match (60s polling)
    - `grep "studio/metrics" apps/web/src/pages/studio/metrics/MetricsDashboard.tsx` returns a match (endpoint)
    - `grep "MetricsDashboard" apps/web/src/pages/studio/Studio.tsx` returns 2 matches (import + usage)
    - `grep "trend" apps/web/src/pages/studio/metrics/MetricCard.tsx` returns a match (trend prop rendered)
    - TypeScript check: no errors in MetricCard.tsx, MetricsDashboard.tsx, Studio.tsx
  </acceptance_criteria>
  <done>MetricCard renders label + value + trend indicator. MetricsDashboard fetches 4-metric endpoint with 60s polling. Studio.tsx updated to render MetricsDashboard below SessionSummary.</done>
</task>

<task type="auto">
  <name>Task 3: E2E override flow test — test_studio_e2e.py (DEVUI-10)</name>
  <read_first>
    - services/agent-orchestrator/tests/conftest.py (fixture names, TestClient setup)
    - services/agent-orchestrator/api/studio_routes.py (endpoint paths, response shapes)
    - services/agent-orchestrator/services/override_service.py (check_and_update_trust signature)
  </read_first>
  <files>
    services/agent-orchestrator/tests/test_studio_e2e.py
  </files>
  <action>
**Create services/agent-orchestrator/tests/test_studio_e2e.py:**

```python
"""
E2E step-through test for the full Studio override flow (Phase 13 DEVUI-10).

Mocks Supabase client at the route layer. Exercises the complete flow:
  1. Create submission in master_wine_library_submissions
  2. POST /sessions to start an onboarding session
  3. PATCH three fields via POST /overrides (varying confidence + reason rules)
  4. Verify override_events table has 3 rows all promotion_status=promoted
  5. Verify GET /sessions/{session_id} returns >= 3 override events in audit trail

Requires: studio_router registered in main.py (Plan 02 Task 2).
"""
import pytest
from unittest.mock import MagicMock, patch, call
from fastapi.testclient import TestClient


pytestmark = pytest.mark.e2e


DEVELOPER_PAYLOAD = {
    "sub": "dev-e2e-001",
    "email": "dev@studio-test.com",
    "app_metadata": {"roles": ["developer"]},
}

ADMIN_PAYLOAD = {
    "sub": "admin-e2e-001",
    "email": "admin@studio-test.com",
    "app_metadata": {"roles": ["review_admin"]},
}

SESSION_ID = "sess-e2e-001"
SUBMISSION_ID = "sub-e2e-001"


def _bearer(payload: dict) -> str:
    """Encode payload as a recognisable fake Bearer token for patched JWT decode."""
    import json, base64
    encoded = base64.b64encode(json.dumps(payload).encode()).decode()
    return f"Bearer mock.{encoded}.sig"


@pytest.fixture()
def mock_supabase_e2e():
    """Supabase mock pre-configured for the full E2E flow."""
    sb = MagicMock()

    # POST /sessions → returns session row
    sb.table.return_value.insert.return_value.execute.return_value.data = [
        {"id": SESSION_ID, "actor_id": DEVELOPER_PAYLOAD["sub"], "status": "active"}
    ]

    # POST /overrides → submission field_confidence lookup
    # wine_name confidence=0.9 (reason required), vintage=0.3, region=None
    field_confidence_map = {
        "wine_name": {"value": "Old Name", "confidence": 0.9, "source": "visible"},
        "vintage": {"value": "2018", "confidence": 0.3, "source": "inferred"},
        # region not present → confidence=None → no reason required
    }
    sb.table.return_value.select.return_value.eq.return_value.single.return_value.execute.return_value.data = {
        "id": SUBMISSION_ID,
        "field_confidence": field_confidence_map,
    }

    # override_events insert → returns a row with auto_promoted status
    def _ov_insert(data):
        mock = MagicMock()
        mock.execute.return_value.data = [
            {"id": f"ov-{data.get('field_name', 'x')}", "promotion_status": "auto_promoted"}
        ]
        return mock

    # GET /sessions/{id} → audit trail
    sb.table.return_value.select.return_value.eq.return_value.order.return_value.execute.return_value.data = [
        {"id": "ov-wine_name", "field_name": "wine_name", "promotion_status": "auto_promoted"},
        {"id": "ov-vintage", "field_name": "vintage", "promotion_status": "auto_promoted"},
        {"id": "ov-region", "field_name": "region", "promotion_status": "auto_promoted"},
    ]

    return sb


class TestStudioE2EOverrideFlow:
    def test_full_developer_override_flow(self, test_client: TestClient, monkeypatch, mock_supabase_e2e):
        """
        Full E2E: developer creates session → submits 3 overrides → verifies audit trail.

        Steps per DEVUI-10:
          1. POST /sessions → sessionId
          2. POST /overrides wine_name (confidence=0.9, reason required)
          3. POST /overrides vintage (confidence=0.3, no reason required)
          4. POST /overrides region (NULL field, no reason required)
          5. GET /sessions/{session_id} → audit trail has >= 3 events
        Developer role → all overrides auto_promoted (no queue needed).
        """
        # Patch JWT decode to return developer payload
        monkeypatch.setattr(
            "services.override_service.get_settings",
            lambda: type("S", (), {"supabase_jwt_secret": "e2e-secret", "trust_level_threshold": 5})(),
        )
        monkeypatch.setattr("api.studio_routes._get_supabase", lambda: mock_supabase_e2e)
        monkeypatch.setattr("services.override_service._get_supabase", lambda: mock_supabase_e2e)

        import jwt as pyjwt
        dev_token = pyjwt.encode(DEVELOPER_PAYLOAD, "e2e-secret", algorithm="HS256")
        auth_header = {"Authorization": f"Bearer {dev_token}"}

        # Step 1: create session
        sess_resp = test_client.post(
            "/api/v1/studio/sessions",
            json={"source_type": "manual_seed", "actor_id": DEVELOPER_PAYLOAD["sub"]},
            headers=auth_header,
        )
        assert sess_resp.status_code == 200, sess_resp.text

        # Step 2: override wine_name (confidence=0.9 → reason required)
        ov1 = test_client.post(
            "/api/v1/studio/overrides",
            json={
                "session_id": SESSION_ID,
                "submission_id": SUBMISSION_ID,
                "field_name": "wine_name",
                "new_value": "Château Margaux",
                "reason": "Confirmed on producer website",
            },
            headers=auth_header,
        )
        assert ov1.status_code == 200, ov1.text

        # Step 3: override vintage (confidence=0.3 → no reason required)
        ov2 = test_client.post(
            "/api/v1/studio/overrides",
            json={
                "session_id": SESSION_ID,
                "submission_id": SUBMISSION_ID,
                "field_name": "vintage",
                "new_value": "2019",
            },
            headers=auth_header,
        )
        assert ov2.status_code == 200, ov2.text

        # Step 4: override region (NULL → no reason required)
        ov3 = test_client.post(
            "/api/v1/studio/overrides",
            json={
                "session_id": SESSION_ID,
                "submission_id": SUBMISSION_ID,
                "field_name": "region",
                "new_value": "Bordeaux",
            },
            headers=auth_header,
        )
        assert ov3.status_code == 200, ov3.text

        # Step 5: GET /sessions/{id} → audit trail has >= 3 events
        sess_detail = test_client.get(
            f"/api/v1/studio/sessions/{SESSION_ID}",
            headers=auth_header,
        )
        assert sess_detail.status_code == 200, sess_detail.text
        body = sess_detail.json()
        assert body.get("event_count", 0) >= 3, f"Expected >= 3 events, got {body.get('event_count')}"

    def test_review_admin_queue_is_empty_for_developer_overrides(
        self, test_client: TestClient, monkeypatch, mock_supabase_e2e
    ):
        """
        GET /queue as review_admin → 0 pending when developer submitted (instant promote).
        DEVUI-10: developer instant-promote means nothing should be in the pending queue.
        """
        monkeypatch.setattr(
            "services.override_service.get_settings",
            lambda: type("S", (), {"supabase_jwt_secret": "e2e-secret", "trust_level_threshold": 5})(),
        )
        monkeypatch.setattr("api.studio_routes._get_supabase", lambda: mock_supabase_e2e)

        # Queue count returns 0 for developer-submitted overrides
        mock_supabase_e2e.table.return_value.select.return_value.eq.return_value.execute.return_value.count = 0
        mock_supabase_e2e.table.return_value.select.return_value.eq.return_value.order.return_value.range.return_value.execute.return_value.data = []

        import jwt as pyjwt
        admin_token = pyjwt.encode(ADMIN_PAYLOAD, "e2e-secret", algorithm="HS256")

        queue_resp = test_client.get(
            "/api/v1/studio/queue",
            headers={"Authorization": f"Bearer {admin_token}"},
        )
        assert queue_resp.status_code == 200, queue_resp.text
        body = queue_resp.json()
        assert body["total"] == 0, f"Expected 0 pending, got {body['total']}"
```
  </action>
  <verify>
    <automated>cd services/agent-orchestrator && python -m pytest tests/test_studio_e2e.py --co -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "test_studio_e2e" services/agent-orchestrator/tests/test_studio_e2e.py` exits 0
    - `grep "pytest.mark.e2e" services/agent-orchestrator/tests/test_studio_e2e.py` returns a match
    - `grep "event_count" services/agent-orchestrator/tests/test_studio_e2e.py` returns a match (audit trail check)
    - `grep "wine_name.*confidence=0.9\|confidence.*0.9\|0.9" services/agent-orchestrator/tests/test_studio_e2e.py` returns a match (reason-required field tested)
    - `cd services/agent-orchestrator && python -m pytest tests/test_studio_e2e.py --co -q 2>&1 | grep "test session"` exits without import errors
  </acceptance_criteria>
  <done>test_studio_e2e.py exists with pytest.mark.e2e, step-through flow (3 override types), audit trail assertion, and developer-instant-promote queue verification.</done>
</task>

<task type="auto">
  <name>Task 4: Vitest component specs — StudioIngestionBar.test.tsx + StudioFieldCell.test.tsx (DEVUI-02, DEVUI-03)</name>
  <read_first>
    - apps/web/src/pages/studio/CommandBar.tsx (detectIngestionType, props, state shapes)
    - apps/web/src/pages/studio/FieldCell.tsx (FieldCellProps, confidence threshold, isEditing state)
    - apps/web/src/pages/studio/ReasonInput.tsx (show prop, AnimatePresence)
  </read_first>
  <files>
    apps/web/src/components/studio/StudioIngestionBar.test.tsx,
    apps/web/src/components/studio/StudioFieldCell.test.tsx
  </files>
  <action>
**Create apps/web/src/components/studio/StudioIngestionBar.test.tsx (DEVUI-02):**

```tsx
/**
 * Vitest unit tests for CommandBar ingestion type detection logic (DEVUI-02).
 * Tests the pure detectIngestionType function without rendering.
 */
import { describe, it, expect } from 'vitest'

// detectIngestionType is a pure function — import directly from the module
// If it's not exported, copy the logic inline for test isolation.
function detectIngestionType(value: string, files?: FileList | null): 'pdf' | 'url' | 'manual' | null {
  if (files && files.length > 0) {
    const name = files[0].name.toLowerCase()
    if (name.endsWith('.pdf')) return 'pdf'
  }
  if (/^https?:\/\//i.test(value.trim())) return 'url'
  if (value.trim().length > 0) return 'manual'
  return null
}

describe('detectIngestionType', () => {
  it('returns null for empty input with no files', () => {
    expect(detectIngestionType('')).toBeNull()
  })

  it('returns "url" for http:// prefixed input', () => {
    expect(detectIngestionType('http://restaurant.com/menu')).toBe('url')
  })

  it('returns "url" for https:// prefixed input', () => {
    expect(detectIngestionType('https://restaurant.com/wine-list')).toBe('url')
  })

  it('returns "manual" for plain text that is not a URL', () => {
    expect(detectIngestionType('Château Margaux')).toBe('manual')
  })

  it('returns null for whitespace-only input with no files', () => {
    expect(detectIngestionType('   ')).toBeNull()
  })

  it('is case-insensitive for URL detection (HTTP uppercase)', () => {
    expect(detectIngestionType('HTTP://example.com')).toBe('url')
  })
})
```

**Create apps/web/src/components/studio/StudioFieldCell.test.tsx (DEVUI-03):**

```tsx
/**
 * Vitest unit tests for FieldCell confidence threshold and reason enforcement (DEVUI-03).
 * Tests the business logic rules: when is reason required, when is save enabled.
 */
import { describe, it, expect } from 'vitest'

// Extract and test the pure business logic from FieldCell without rendering:
// requiresReason: confidence >= 0.8
// canSave: newValue not empty, changed from original, if requiresReason then reason >= 5 chars

function computeRequiresReason(confidence: number | null): boolean {
  return (confidence ?? 0) >= 0.8
}

function computeCanSave(
  newValue: string,
  originalValue: string | null,
  requiresReason: boolean,
  reason: string
): boolean {
  return (
    newValue.trim().length > 0 &&
    newValue !== (originalValue ?? '') &&
    (!requiresReason || reason.trim().length >= 5)
  )
}

describe('FieldCell — requiresReason', () => {
  it('returns false for confidence = 0.0', () => {
    expect(computeRequiresReason(0.0)).toBe(false)
  })

  it('returns false for confidence = 0.79', () => {
    expect(computeRequiresReason(0.79)).toBe(false)
  })

  it('returns true at confidence = 0.8 (boundary)', () => {
    expect(computeRequiresReason(0.8)).toBe(true)
  })

  it('returns true for confidence = 0.95', () => {
    expect(computeRequiresReason(0.95)).toBe(true)
  })

  it('returns false for null confidence (unknown field)', () => {
    expect(computeRequiresReason(null)).toBe(false)
  })
})

describe('FieldCell — canSave', () => {
  it('returns false when newValue is empty', () => {
    expect(computeCanSave('', 'old', false, '')).toBe(false)
  })

  it('returns false when newValue equals original', () => {
    expect(computeCanSave('same', 'same', false, '')).toBe(false)
  })

  it('returns true for changed value with no reason required', () => {
    expect(computeCanSave('New Wine', 'Old Wine', false, '')).toBe(true)
  })

  it('returns false when reason required but reason is empty', () => {
    expect(computeCanSave('New Wine', 'Old Wine', true, '')).toBe(false)
  })

  it('returns false when reason required but reason is < 5 chars', () => {
    expect(computeCanSave('New Wine', 'Old Wine', true, 'abc')).toBe(false)
  })

  it('returns true when reason required and reason has >= 5 chars', () => {
    expect(computeCanSave('New Wine', 'Old Wine', true, 'Confirmed on website')).toBe(true)
  })
})
```
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/components/studio/StudioIngestionBar.test.tsx src/components/studio/StudioFieldCell.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "StudioIngestionBar.test" apps/web/src/components/studio/StudioIngestionBar.test.tsx` exits 0 (file exists)
    - `grep "StudioFieldCell.test" apps/web/src/components/studio/StudioFieldCell.test.tsx` exits 0 (file exists)
    - `grep "detectIngestionType" apps/web/src/components/studio/StudioIngestionBar.test.tsx` returns a match (DEVUI-02)
    - `grep "computeRequiresReason\|requiresReason" apps/web/src/components/studio/StudioFieldCell.test.tsx` returns a match (DEVUI-03 confidence threshold)
    - `grep "0.8" apps/web/src/components/studio/StudioFieldCell.test.tsx` returns a match (boundary test at threshold)
    - `cd apps/web && npx vitest run src/components/studio/ 2>&1 | grep -E "PASS|FAIL"` shows PASS for both files
  </acceptance_criteria>
  <done>StudioIngestionBar.test.tsx tests detectIngestionType logic (6 cases). StudioFieldCell.test.tsx tests confidence threshold + canSave logic (11 cases). Both are pure unit tests — no DOM rendering required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MetricsDashboard → GET /studio/metrics | Bearer JWT required; metrics are read-only aggregates; no PII exposed |
| Test coverage | Tests exercise server-side reason enforcement at 0.8 threshold; no client-side bypass possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-21 | Info Disclosure | MetricsDashboard metrics response | accept | Metrics are aggregates (counts) only; no PII or individual override details returned |
| T-13-22 | Tampering | Client-side reason bypass | mitigate | test_post_override_high_confidence_reason_required_missing_returns_422 verifies server 422 regardless of client behavior |
| T-13-23 | Repudiation | Trust counter test coverage gap | mitigate | test_increment_trust_counter_rpc_called_on_approve verifies server calls RPC on every approve decision |
</threat_model>

<verification>
1. `cd services/agent-orchestrator && python -m pytest tests/test_studio_routes.py tests/test_override_service.py tests/test_studio_e2e.py -v 2>&1 | grep -E "PASSED|FAILED|ERROR"` — all PASSED
2. `grep "test_post_override_high_confidence_reason_required_missing_returns_422" services/agent-orchestrator/tests/test_studio_routes.py` — present
3. `grep "await check_and_update_trust" services/agent-orchestrator/tests/test_override_service.py` — returns empty (no await calls)
4. `grep "await require_studio_role" services/agent-orchestrator/tests/test_studio_routes.py` — returns empty (no await calls)
5. `grep "dep.dependency" services/agent-orchestrator/tests/test_studio_routes.py` — returns a match
6. `grep "MetricsDashboard" apps/web/src/pages/studio/Studio.tsx` — 2 matches (import + use)
7. `grep "test_studio_e2e" services/agent-orchestrator/tests/test_studio_e2e.py` — present
8. `cd apps/web && npx vitest run src/components/studio/ 2>&1 | grep "PASS"` — both Vitest specs pass
</verification>

<success_criteria>
- test_studio_routes.py: 6+ tests; D-07 threshold, invite role guard, approve decision, TestRequireStudioRole (sync dep.dependency — no await)
- test_override_service.py: 3+ tests; check_and_update_trust is sync, called with user_id+approved params, RPC receives {"p_user_id": ...}
- test_studio_e2e.py: @pytest.mark.e2e; full 3-override flow + 0-pending queue assertion; event_count >= 3
- StudioIngestionBar.test.tsx: 6 cases for detectIngestionType (null, url, https, manual, whitespace, case-insensitive)
- StudioFieldCell.test.tsx: 11 cases covering requiresReason threshold at 0.8 + canSave combinations
- MetricsDashboard: 4 cards rendered with loading skeleton, 60s polling, 7-day trend delta optional
- Studio.tsx: MetricsDashboard placed between SessionSummary and WineRecordsTable
</success_criteria>

<output>
After completion, create `.planning/phases/13-dev-onboarding-ui-with-manual-override-access/13-05-SUMMARY.md`
</output>

---
phase: 36-testing-foundation-functionality-registry
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - .planning/testing/EXISTING-TEST-INVENTORY.md
  - .planning/testing/TESTING-SCORECARD.md
autonomous: true
requirements:
  - TFND-03
  - TFND-04
user_setup: []

must_haves:
  truths:
    - "Every Jest *.spec.ts, Vitest *.test.*, Playwright e2e/*.spec.ts, and orchestrator test_*.py / wave_*.py file has an inventory row"
    - "Inventory rows include group, path, runner, layer, ci_job, runs?, passes? columns"
    - "Inventory group column uses locked N-shortname slugs only (1-identity … 11-platform)"
    - "passes? is never claimed yes without evidence from this phase; default unknown"
    - "TESTING-SCORECARD.md has exactly 11 group rows with baseline score + evidence + gaps + next phase"
    - "Baseline scores are mostly T0/T1 or T1?; no group claimed T4; stale-suspect excluded from T1-eligible evidence"
    - "T1 requires runs?=yes AND (loadable smoke OR label T1?/provisional with Gaps 'CI green unverified')"
    - "Scorecard Gaps for groups 1, 2, 3, 8, 11 each include ≥1 operator-visible UX trap (not only missing automated tests)"
    - "Inventory groups reconciled to FUNCTIONALITY-REGISTRY.md primaries (sample 10 Nest modules match)"
    - "Do not treat TFND-05 / annotated CI as green CI; Black debt on studio_routes.py cited"
  artifacts:
    - path: ".planning/testing/EXISTING-TEST-INVENTORY.md"
      provides: "Full catalog of existing tests kept as-is (D-05)"
      contains: "passes?"
    - path: ".planning/testing/TESTING-SCORECARD.md"
      provides: "Baseline T-maturity per group with evidence links"
      contains: "Inventory Operations"
  key_links:
    - from: ".planning/testing/TESTING-SCORECARD.md"
      to: ".planning/testing/RUBRIC.md"
      via: "legend / score meanings"
      pattern: "RUBRIC\\.md"
    - from: ".planning/testing/TESTING-SCORECARD.md"
      to: ".planning/testing/EXISTING-TEST-INVENTORY.md"
      via: "evidence column cites inventory paths"
      pattern: "EXISTING-TEST-INVENTORY\\.md"
    - from: ".planning/testing/TESTING-SCORECARD.md"
      to: ".planning/testing/FUNCTIONALITY-REGISTRY.md"
      via: "group ownership + contested surfaces reconcile"
      pattern: "FUNCTIONALITY-REGISTRY\\.md"
    - from: ".planning/testing/EXISTING-TEST-INVENTORY.md"
      to: ".github/workflows/ci.yml"
      via: "ci_job membership (test-typescript / test-python / test-e2e)"
      pattern: "test-typescript|test-python|test-e2e|e2e-prod"
---

<objective>
Catalog every existing automated test file without reworking any (TFND-03 / D-05) and initialize the living TESTING-SCORECARD baseline (TFND-04 / D-17) so breadth phases know starting maturity.

Purpose: Honest inventory + baseline scores (mostly T0/T1) become the campaign scoreboard.
Output: `.planning/testing/EXISTING-TEST-INVENTORY.md`, `.planning/testing/TESTING-SCORECARD.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/36-testing-foundation-functionality-registry/36-CONTEXT.md
@.planning/phases/36-testing-foundation-functionality-registry/36-RESEARCH.md
@.planning/phases/36-testing-foundation-functionality-registry/36-PATTERNS.md
@.planning/phases/36-testing-foundation-functionality-registry/36-PREMORTEM.md
@.planning/phases/36-testing-foundation-functionality-registry/36-UX-SMOOTHNESS.md
@.planning/phases/36-testing-foundation-functionality-registry/36-01-PLAN.md
@.planning/phases/36-testing-foundation-functionality-registry/36-01-SUMMARY.md
@.planning/testing/FUNCTIONALITY-REGISTRY.md
@.planning/testing/RUBRIC.md
@.github/workflows/ci.yml
@.github/workflows/e2e-prod.yml
@services/agent-orchestrator/pytest.ini
@.planning/UX_PATHS_CATALOG.md
</context>

<interfaces>
<!-- Inventory columns (RESEARCH Pattern 3) — required header exactly: -->
| group | path | runner | layer | ci_job | runs? | passes? | notes |

<!-- Runner values: jest | vitest | pytest | playwright -->
<!-- Layer values: unit | integration | e2e | prod_e2e | unknown -->
<!-- ci_job values: test-typescript | test-python | test-e2e | e2e-prod | none -->
<!-- runs?: yes only if named CI job invokes that runner on push or schedule -->
<!-- passes?: yes | no | unknown | stale-suspect — default unknown -->

<!-- LOCKED group slugs ONLY (no display-name form in inventory group column): -->
1-identity | 2-catalog | 3-inventory | 4-pos | 5-procurement | 6-comms | 7-calendar | 8-analytics | 9-notifications | 10-ai | 11-platform

<!-- Corpus buckets (must reach these counts ±1 for known duplicates): -->
api-gateway *.spec.ts ≈ 41
web src **/*.test.* ≈ 30
web e2e/*.spec.ts = 4
orchestrator tests (test_*.py + wave_*.py) ≈ 67

<!-- Scorecard row template: -->
| # | Group | Score | Evidence | Gaps | Next phase |

<!-- T1 eligibility (C3): runs?=yes AND not stale-suspect AND (loadable smoke OR Score=T1? with Gaps "CI green unverified") -->
<!-- Later phases MUST NOT promote past T1 until passes?=yes or explicit waiver -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build EXISTING-TEST-INVENTORY.md for all runners</name>
  <read_first>
    - .planning/phases/36-testing-foundation-functionality-registry/36-RESEARCH.md (Pattern 3 honesty protocol + corpus snapshot)
    - .planning/phases/36-testing-foundation-functionality-registry/36-PATTERNS.md (EXISTING-TEST-INVENTORY section)
    - .planning/phases/36-testing-foundation-functionality-registry/36-CONTEXT.md (D-05 keep as-is)
    - .planning/phases/36-testing-foundation-functionality-registry/36-PREMORTEM.md (C3 T1 inflation; H1 divergence; M1 stale)
    - .planning/testing/FUNCTIONALITY-REGISTRY.md (primary groups — MUST exist via depends_on 36-01)
    - .github/workflows/ci.yml (jobs test-typescript, test-python, test-e2e)
    - .github/workflows/e2e-prod.yml (job e2e-prod + wave files)
    - services/agent-orchestrator/pytest.ini (markers: unit, integration, e2e, prod_e2e)
  </read_first>
  <files>.planning/testing/EXISTING-TEST-INVENTORY.md</files>
  <action>
    Create `.planning/testing/EXISTING-TEST-INVENTORY.md` implementing TFND-03 / D-05.

    Steps:
    1. Run these discovery commands and capture path lists (do not modify any test files):
       ```bash
       find apps/api-gateway -name '*.spec.ts' | sort
       find apps/web/src \( -name '*.test.ts' -o -name '*.test.tsx' -o -name '*.spec.ts' -o -name '*.spec.tsx' \) | sort
       ls apps/web/e2e/*.spec.ts
       find services/agent-orchestrator/tests \( -name 'test_*.py' -o -name 'wave_*.py' \) | sort
       ```
    2. Write **Methodology** section documenting:
       - Humans: use `TESTING-SCORECARD.md` first — this file is completeness evidence for agents/CI, not the founder homepage
       - `runs?=yes` only if a named CI job invokes that runner on push (`ci.yml`) or schedule (`e2e-prod.yml`)
       - `passes?` default `unknown` unless this phase has a green local run artifact — **never claim CI green** (main recently fails Black on `studio_routes.py`)
       - **Do not treat TFND-05 as green CI** (H5) — Black debt on `studio_routes.py` as of 2026-07-27; test jobs may not be trustworthy until lint green
       - Layer inference: path + pytest markers from `pytest.ini`; Nest/Vitest default `unit` unless file name/path clearly integration; `wave_*.py` → `prod_e2e` + `ci_job=e2e-prod`; local Playwright → `e2e` + `test-e2e`; `prod-smoke.spec.ts` also note Wave F / e2e-prod
       - Distinguish `test-e2e` (local) ≠ `e2e-prod` (cloud)
       - **T1-eligible evidence excludes `passes?=stale-suspect`** (C3/M1) — still inventory the row, but summary/scorecard must not count it toward T1
    3. Write **Summary counts** table by runner and by group (1–11 using locked slugs). Note separately how many rows are `stale-suspect` (excluded from T1-eligible).
    4. Write **Full inventory table** with columns exactly:
       `group | path | runner | layer | ci_job | runs? | passes? | notes`
       One row per discovered file. Assign `group` using **locked N-shortname slugs only** (per D-09 / registry primary):
       `1-identity | 2-catalog | 3-inventory | 4-pos | 5-procurement | 6-comms | 7-calendar | 8-analytics | 9-notifications | 10-ai | 11-platform`
       **Group column MUST match `FUNCTIONALITY-REGISTRY.md` primary** for the owning Nest module / route / agent (H1). Do not invent alternate slug spellings or display-name forms in the group column.
    5. **Known anomalies** section MUST call out:
       - duplicate filename `test_golden_path_e2e 2.py` → `passes?=stale-suspect` (not T1-eligible)
       - unrouted `RecurringOrders` Vitest if present → group `5-procurement` note
       - Nest modules with 0 specs (list from RESEARCH: organizations, restaurants, wines, menus, …) — not inventory rows, but call out in anomalies
       - packages/* tests: 0 found
       - mobile tests stub deferred

    Do NOT delete, rename, fix, or reformat any test file. Do NOT run full suites requiring green CI. Optional: `pnpm --filter @wineops/api-gateway test -- --listTests` and `cd services/agent-orchestrator && pytest --collect-only -q` for count cross-check / loadable-smoke evidence only.
  </action>
  <verify>
    <automated>test -f .planning/testing/EXISTING-TEST-INVENTORY.md && SPEC=$(find apps/api-gateway -name '*.spec.ts' | wc -l | tr -d ' ') && INV=$(rg -c 'apps/api-gateway/.*\.spec\.ts' .planning/testing/EXISTING-TEST-INVENTORY.md || true) && echo "specs=$SPEC inventory_gateway_hits=$INV" && test "$INV" -ge "$SPEC" && WEB=$(find apps/web/src \( -name '*.test.ts' -o -name '*.test.tsx' -o -name '*.spec.ts' -o -name '*.spec.tsx' \) | wc -l | tr -d ' ') && WEBINV=$(rg -c 'apps/web/src/.*\.(test|spec)\.(ts|tsx)' .planning/testing/EXISTING-TEST-INVENTORY.md || true) && echo "web_src=$WEB inventory_web_hits=$WEBINV" && test "$WEBINV" -ge "$WEB" && rg -n 'group \| path \| runner \| layer \| ci_job \| runs\? \| passes\?' .planning/testing/EXISTING-TEST-INVENTORY.md && rg -n 'stale-suspect|test_golden_path_e2e 2' .planning/testing/EXISTING-TEST-INVENTORY.md | head -10 && rg -q 'Methodology' .planning/testing/EXISTING-TEST-INVENTORY.md && rg -q '1-identity|3-inventory|11-platform' .planning/testing/EXISTING-TEST-INVENTORY.md && ! rg -n '\| 3 Inventory Operations \|' .planning/testing/EXISTING-TEST-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/testing/EXISTING-TEST-INVENTORY.md` exists
    - Header row contains exactly the columns: group, path, runner, layer, ci_job, runs?, passes?, notes
    - Group column values use only locked `N-shortname` slugs (no "3 Inventory Operations" form)
    - Count of inventory rows mentioning `apps/api-gateway/` and `.spec.ts` ≥ `find apps/api-gateway -name '*.spec.ts' | wc -l`
    - Count of inventory rows matching `apps/web/src/.*\.(test|spec)\.(ts|tsx)` ≥ `find apps/web/src \( -name '*.test.ts' -o -name '*.test.tsx' -o -name '*.spec.ts' -o -name '*.spec.tsx' \) | wc -l` (expect ~30 Vitest files; floor check must pass)
    - Count of rows mentioning `apps/web/e2e/` ≥ 4
    - Count of rows mentioning `services/agent-orchestrator/tests/` ≥ 60
    - `rg -n 'Methodology|runs\?=yes|passes\?' .planning/testing/EXISTING-TEST-INVENTORY.md` ≥1 each for Methodology and honesty rules
    - `rg -n 'stale-suspect|T1-eligible|not.*T1' .planning/testing/EXISTING-TEST-INVENTORY.md` ≥1
    - `rg -n 'test_golden_path_e2e 2' .planning/testing/EXISTING-TEST-INVENTORY.md` ≥1 with `stale-suspect` nearby or in notes
    - `rg -n 'studio_routes|Black|TFND-05' .planning/testing/EXISTING-TEST-INVENTORY.md` ≥1 (H5 honesty)
    - No test source files under `apps/` or `services/agent-orchestrator/tests/` are modified by this task (`git status --short` shows only `.planning/testing/` for code paths)
  </acceptance_criteria>
  <done>Complete existing-test inventory catalogs every Jest/Vitest/Playwright/pytest file with locked slugs and honest runs?/passes? (TFND-03 / D-05).</done>
</task>

<task type="auto">
  <name>Task 2: Initialize TESTING-SCORECARD.md baseline</name>
  <read_first>
    - .planning/testing/EXISTING-TEST-INVENTORY.md (just created — evidence source)
    - .planning/testing/FUNCTIONALITY-REGISTRY.md (authoritative primaries + Contested surfaces + Manual pathway watchlist)
    - .planning/testing/RUBRIC.md (promote-past-T1 guard)
    - .planning/phases/36-testing-foundation-functionality-registry/36-RESEARCH.md (scorecard row template + Open Q2 holistic scoring)
    - .planning/phases/36-testing-foundation-functionality-registry/36-CONTEXT.md (D-17; mostly T0/T1 expected)
    - .planning/phases/36-testing-foundation-functionality-registry/36-PREMORTEM.md (C1/C3/H1/H5)
    - .planning/phases/36-testing-foundation-functionality-registry/36-UX-SMOOTHNESS.md (§3 Gaps seed + §5 surface flags)
    - .planning/ROADMAP.md (Phase 36 SC #4; Phases 39–43 next-phase owners)
    - .planning/UX_PATHS_CATALOG.md (optional Evidence § cites)
  </read_first>
  <files>.planning/testing/TESTING-SCORECARD.md</files>
  <action>
    Create `.planning/testing/TESTING-SCORECARD.md` implementing TFND-04 / D-17.

    Required content:
    1. **Header** — baseline date `2026-07-27` (or execution date), purpose, link to `RUBRIC.md` legend, link to `EXISTING-TEST-INVENTORY.md` + `FUNCTIONALITY-REGISTRY.md` + `.planning/UX_PATHS_CATALOG.md`.
    2. **Scoring rule** (one paragraph): Score the **group** holistically; never claim T4 in this baseline; Agent Level ≠ automatic T-level.
    3. **T1 assignment protocol (C3 — replaces naive runs?=yes → T1):**
       - If inventory shows **zero** automated tests for the group → **T0**
       - Clean **T1** only if ≥1 inventory path has `runs?=yes` AND `passes?` ≠ `stale-suspect` AND either:
         (a) loadable smoke documented this phase (`--listTests` / `pytest --collect-only` or equivalent note in Evidence), OR
         (b) `passes?=yes` with artifact
       - If `runs?=yes` but `passes?=unknown` (and not stale-suspect) without loadable-smoke proof → Score **`T1?`** (provisional) and Gaps **MUST** include exact phrase `CI green unverified`
       - **Exclude `stale-suspect` rows from T1-eligible evidence** (M1)
       - Do **not** assign T2+ unless inventory clearly documents contract/resilience suites with assertions — Phase 36 expectation is mostly T0/T1/T1?
       - **Forbidden:** any group Score cell equal to `T4`
       - **Forbidden implication:** later phases may promote past T1 while `passes?` remains unknown — state explicitly: promote past T1 only when `passes?=yes` or Gaps records an explicit waiver (mirrors RUBRIC)
    4. **Reconcile inventory → registry (H1):** Before freezing scores, verify sample of **10 Nest modules** that inventory `group` slug matches registry Table A primary (suggested sample: `auth`, `wines`, `inventory`, `pos-hub`, `procurement`, `contacts`, `communications`, `calendar`, `analytics`, `common`). Fix inventory group cells if divergent. Document reconcile result in a short **Consistency** note under the table.
    5. **Scorecard table** with columns exactly:
       `| # | Group | Score | Evidence | Gaps | Next phase |`
       Exactly **11 data rows** for groups 1–11 using locked **display names** from D-09 in the Group column (human-readable). Machine ids remain the N-shortname slugs in Evidence/inventory.
    6. **Evidence column** — cite 1–3 concrete inventory paths (filenames) per group when T1/T1?; else `none`. Optional 4th cite: `UX_PATHS_CATALOG.md §N` when gap is a known UI trap (path/section only — do not paste catalog rows). Never cite `stale-suspect` paths as T1 evidence.
    7. **Gaps column** — short gap text (e.g. "No Nest specs"). **Additionally (UX smoothness):** for groups **1, 2, 3, 8, 11**, Gaps MUST include ≥1 **operator-visible** UX trap drawn from registry Manual pathway watchlist / UX catalog (dead button, mocked path, dual route) — separate from missing-test bullets. Examples:
       - 1: dead forgot-password / unbound remember-me
       - 2: Wine Library scanner persistence mocked; GetStarted import triad is real scanner journey
       - 3: canonical `/inventory` vs `/inventory-legacy` for Phase 43 happy path
       - 8: Dashboard dead Reorder / empty Top Wines / stub calendar CTA
       - 11: Admin Panel localStorage vs `/admin/health` live poll
    8. **Next phase column** — use ROADMAP ownership:
       - Groups 1–4 → Phase 39
       - Groups 5–7, 9 → Phase 40
       - Group 8 → Phase 41 (Analytics truth / T4 target later)
       - Group 10 → Phase 42 (AI Assistants)
       - Group 11 → Phase 43
    9. **CI / cloud E2E honesty block (C1 + H5)** — dedicated short section (not one buried line):
       - **Do not treat TFND-05 as green CI**; Black debt on `studio_routes.py` as of 2026-07-27; cite a recent failing `ci.yml` run id if available via `gh run list`
       - Nightly `e2e-prod.yml`: document **secrets present? yes/no as of &lt;date&gt;** from `gh run view &lt;id&gt; --log` / env dump — **names only, never values**. As of premortem 2026-07-27: secrets missing; no `test-results/` wave XML → TFND-05 status = **schedule-present / capability-unverified** until one wave XML lands
       - Scorecard does not claim green push CI or healthy nightly E2E

    Do NOT create suites. Do NOT fix Black debt. Do NOT change e2e-prod job behavior.
  </action>
  <verify>
    <automated>test -f .planning/testing/TESTING-SCORECARD.md && test $(rg -c '^\| [0-9]+ \|' .planning/testing/TESTING-SCORECARD.md) -eq 11 && rg -n 'RUBRIC\.md|EXISTING-TEST-INVENTORY\.md|FUNCTIONALITY-REGISTRY\.md' .planning/testing/TESTING-SCORECARD.md && ! rg -n '^\| [0-9]+ \|.*\| T4 \|' .planning/testing/TESTING-SCORECARD.md && rg -n 'T0|T1|T1\?' .planning/testing/TESTING-SCORECARD.md | head -20 && rg -q 'CI green unverified|T1\?|stale-suspect|capability-unverified|secrets present' .planning/testing/TESTING-SCORECARD.md && rg -q 'studio_routes|Black|TFND-05' .planning/testing/TESTING-SCORECARD.md && rg -q 'Forgot password|inventory-legacy|localStorage|Top Wines|mocked' .planning/testing/TESTING-SCORECARD.md && for m in auth wines inventory pos-hub procurement contacts communications calendar analytics common; do rg -q "$m" .planning/testing/FUNCTIONALITY-REGISTRY.md && rg -q "$m" .planning/testing/EXISTING-TEST-INVENTORY.md || rg -q "$m" .planning/testing/TESTING-SCORECARD.md; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/testing/TESTING-SCORECARD.md` exists
    - Exactly 11 markdown table data rows matching `^\| [0-9]+ \|`
    - All 11 locked group names appear
    - `rg -n 'RUBRIC.md' .planning/testing/TESTING-SCORECARD.md` ≥1
    - `rg -n 'EXISTING-TEST-INVENTORY.md' .planning/testing/TESTING-SCORECARD.md` ≥1
    - `rg -n 'FUNCTIONALITY-REGISTRY.md' .planning/testing/TESTING-SCORECARD.md` ≥1
    - Zero rows with Score `T4`
    - T1 protocol documented; provisional scores use `T1?` and/or Gaps contain `CI green unverified` where applicable
    - `stale-suspect` excluded from T1 evidence (stated in scoring rule or Consistency note)
    - Groups 1, 2, 3, 8, 11 Gaps each include ≥1 operator-visible UX trap
    - Honesty block states TFND-05 ≠ green CI, cites Black/`studio_routes.py`, and records e2e-prod **secrets present? yes/no as of &lt;date&gt;** + **schedule-present / capability-unverified** until wave XML
    - Consistency note or verify proves sample Nest modules inventory group == registry primary
    - Next phase column references Phase 39, 40, 41, 42, or 43 across rows
    - Explicit statement: do not promote past T1 until `passes?=yes` or waiver
  </acceptance_criteria>
  <done>TESTING-SCORECARD.md initialized with honest provisional T1 rules, registry reconcile, UX Gaps seeds, and CI/E2E capability honesty (TFND-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inventory honesty → campaign decisions | Inflated passes?/scores mislead breadth phases |
| Docs → no production data access | Inventory must not embed JWTs, service-role keys, or live credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02-01 | Spoofing | passes?/Score columns | mitigate | Default `passes?=unknown`; provisional `T1?` + Gaps `CI green unverified`; exclude stale-suspect from T1; forbid T4; forbid promote-past-T1 without passes?=yes/waiver |
| T-36-02-02 | Information Disclosure | EXISTING-TEST-INVENTORY.md | mitigate | Paths + status only — no env secrets, no JWT excerpts, no Supabase keys; e2e-prod secrets status names-only |
| T-36-02-03 | Tampering | Score inflation via file counts | mitigate | T1 requires runs?=yes + loadable smoke or T1?; never runs?=yes alone; reconcile to registry primaries |
| T-36-02-04 | Denial of Service | Full suite runs in-phase | accept | Phase 36 does not require full green suite; collect-only / listTests only |
| T-36-02-05 | Spoofing | TFND-05 / nightly E2E claims | mitigate | Honesty block: TFND-05 ≠ green CI; Black studio_routes.py; e2e-prod capability-unverified until wave XML |
</threat_model>

<verification>
- Inventory path counts meet find-based floors; locked N-shortname slugs only
- Scorecard has 11 rows, no T4, provisional T1 rules + UX Gaps + CI/E2E honesty
- Sample Nest modules inventory group == registry primary
</verification>

<success_criteria>
- TFND-03: every discovered test file catalogued with required columns and locked slugs; tests untouched
- TFND-04: scorecard baseline with evidence, provisional T1 honesty, operator UX Gaps, and registry reconcile
</success_criteria>

<output>
After completion, create `.planning/phases/36-testing-foundation-functionality-registry/36-02-SUMMARY.md`
</output>

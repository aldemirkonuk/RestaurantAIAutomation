---
phase: 36-testing-foundation-functionality-registry
plan: 03
type: execute
wave: 3
depends_on:
  - 36-01
  - 36-02
files_modified:
  - .planning/testing/SYNTHETIC-TENANT.md
  - .planning/testing/README.md
  - .planning/testing/checklists/README.md
  - .github/workflows/ci.yml
  - .github/workflows/e2e-prod.yml
autonomous: true
requirements:
  - TFND-06
  - TFND-05
user_setup: []

must_haves:
  truths:
    - "sim-* restaurant_id convention documented with RLS-safe seeding and idempotent teardown rules"
    - "SYNTHETIC-TENANT embeds current 8-table E2E_TABLES copy + gap vs registry DB domains; Phase 37 must expand teardown before multi-archetype seed"
    - "RLS seed checklist requires restaurants sim-* row + user_restaurant_access + Auth user; service-role seed/teardown only"
    - "e2e-test-restaurant Phase 25 anchor coexistence documented; never deleted"
    - "ci.yml documents unit+integration on push via test-typescript and test-python comments"
    - "e2e-prod.yml retains nightly cron 0 2 * * * and gains Phase 42 placeholder comment only"
    - "TFND-05 marked schedule-present / capability-unverified until one wave XML lands; secrets present? yes/no as of <date> documented (names only)"
    - "Do not treat TFND-05 as green CI; Black debt on studio_routes.py cited"
    - "No second production E2E workflow file created"
    - "README opens with Operator quickstart (scorecard → registry → checklists)"
    - "checklists/README.md exists with g{N}-{slug}-manual.md naming contract"
    - "README links UX_PATHS_CATALOG.md"
  artifacts:
    - path: ".planning/testing/SYNTHETIC-TENANT.md"
      provides: "sim-* isolation convention extending Phase 25"
      contains: "sim-"
    - path: ".planning/testing/README.md"
      provides: "Operator front door + Testing campaign artifact index"
      contains: "FUNCTIONALITY-REGISTRY.md"
    - path: ".planning/testing/checklists/README.md"
      provides: "Checklist naming + phase ownership contract"
      contains: "g{N}"
    - path: ".github/workflows/ci.yml"
      provides: "TFND-05 unit/integration annotation on push jobs"
      contains: "TFND-05"
    - path: ".github/workflows/e2e-prod.yml"
      provides: "Nightly E2E + Phase 42 placeholder; no fork"
      contains: "0 2 * * *"
  key_links:
    - from: ".planning/testing/SYNTHETIC-TENANT.md"
      to: "services/agent-orchestrator/tests/e2e/conftest_prod.py"
      via: "extends teardown / JWT patterns by reference; embeds E2E_TABLES"
      pattern: "conftest_prod\\.py|E2E_TABLES|inventory_stock"
    - from: ".planning/testing/README.md"
      to: ".github/workflows/e2e-prod.yml"
      via: "nightly cloud E2E proof link + capability honesty"
      pattern: "e2e-prod\\.yml|capability-unverified|secrets present"
    - from: ".planning/testing/README.md"
      to: ".planning/UX_PATHS_CATALOG.md"
      via: "status oracle before checklist writing"
      pattern: "UX_PATHS_CATALOG\\.md"
    - from: ".github/workflows/ci.yml"
      to: "test-typescript / test-python jobs"
      via: "TFND-05 comment block"
      pattern: "TFND-05"
---

<objective>
Lock the `sim-*` synthetic tenant isolation convention (TFND-06) and annotate existing GitHub Actions for unit/integration-on-push + nightly E2E (TFND-05 / D-22..D-25) without forking Phase 25's production E2E paradigm. Add README index tying the skeleton together.

Purpose: Later phases (37+) implement generators against a documented convention; CI skeleton is discoverable and honest; founders enter via scorecard-first README.
Output: `SYNTHETIC-TENANT.md`, `README.md`, `checklists/README.md`, comment-only edits to `ci.yml` + `e2e-prod.yml`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-testing-foundation-functionality-registry/36-CONTEXT.md
@.planning/phases/36-testing-foundation-functionality-registry/36-RESEARCH.md
@.planning/phases/36-testing-foundation-functionality-registry/36-PATTERNS.md
@.planning/phases/36-testing-foundation-functionality-registry/36-PREMORTEM.md
@.planning/phases/36-testing-foundation-functionality-registry/36-UX-SMOOTHNESS.md
@.planning/phases/36-testing-foundation-functionality-registry/36-01-SUMMARY.md
@.planning/phases/36-testing-foundation-functionality-registry/36-02-SUMMARY.md
@services/agent-orchestrator/tests/e2e/conftest_prod.py
@services/agent-orchestrator/scripts/setup_e2e_anchor.py
@.github/workflows/ci.yml
@.github/workflows/e2e-prod.yml
@.planning/testing/FUNCTIONALITY-REGISTRY.md
@.planning/testing/TESTING-SCORECARD.md
@.planning/UX_PATHS_CATALOG.md
</context>

<interfaces>
<!-- Convention constants to document (do not implement generator code): -->
SIM_ID_PREFIX = "sim-"
SIM_ROW_PREFIX = "sim-"
E2E_ANCHOR = "e2e-test-restaurant"  # NEVER deleted

<!-- Current E2E_TABLES from conftest_prod.py:251-260 (MUST copy into SYNTHETIC-TENANT.md): -->
inventory_stock, notification_deliveries, notification_logs, order_interactions,
calendar_events, pos_webhook_logs, system_audit_log, master_wine_library_submissions

<!-- Phase 25 teardown anti-patterns to restate: -->
- NEVER set PYTEST_RUNNING in prod E2E CI
- NEVER write JWT to disk / JUnit / logs
- NEVER raise in teardown — Sentry tags e2e-orphan / sim-orphan
- Service-role seed/teardown only in CI secrets / server scripts — never web

<!-- CI annotation block (exact intent): -->
# TFND-05 (Phase 36 Testing Campaign):
# test-typescript  → unit + integration (Jest api-gateway + Vitest web)
# test-python      → unit + integration (pytest; markers exist but not filtered today)
# test-e2e         → local Playwright smoke (NOT cloud prod E2E)
# Cloud nightly E2E remains .github/workflows/e2e-prod.yml (schedule: 0 2 * * *)
# STATUS: schedule-present / capability-unverified until one wave XML lands
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write SYNTHETIC-TENANT.md convention</name>
  <read_first>
    - .planning/phases/36-testing-foundation-functionality-registry/36-CONTEXT.md (D-18..D-21)
    - .planning/phases/36-testing-foundation-functionality-registry/36-RESEARCH.md (Pattern 2 + Pitfall 4 RLS)
    - .planning/phases/36-testing-foundation-functionality-registry/36-PATTERNS.md (SYNTHETIC-TENANT section)
    - .planning/phases/36-testing-foundation-functionality-registry/36-PREMORTEM.md (C2 teardown gap; H3 RLS checklist)
    - services/agent-orchestrator/tests/e2e/conftest_prod.py (FULL — teardown, JWT, E2E_TABLES lines 251–260, anti-patterns)
    - services/agent-orchestrator/scripts/setup_e2e_anchor.py (anchor upsert + NEVER delete)
    - .planning/testing/FUNCTIONALITY-REGISTRY.md (Table D DB domains for teardown gap statement)
  </read_first>
  <files>.planning/testing/SYNTHETIC-TENANT.md</files>
  <action>
    Create `.planning/testing/SYNTHETIC-TENANT.md` implementing TFND-06 / D-18..D-21 (filename per Claude discretion — locked to this name by RESEARCH recommendation).

    Required sections:
    1. **Purpose** — convention locked in Phase 36; generator + teardown code expansion implemented in **Phase 37**.
    2. **Coexistence** — permanent Phase 25 anchor `e2e-test-restaurant` NEVER deleted; `sim-*` restaurants coexist; do not fork a second harness — extend `conftest_prod.py` / `setup_e2e_anchor.py` patterns by reference.
    3. **ID prefixes** — document:
       - `SIM_ID_PREFIX = "sim-"` for `restaurants.id` / `restaurant_id`
       - `SIM_ROW_PREFIX = "sim-"` for deterministic row ids (parallel to `e2e-%`)
       - Never reuse production restaurant UUIDs for sim tenants
    4. **RLS-safe seeding (D-19) + mandatory seed checklist (H3)** — require an explicit checklist:
       - [ ] `restaurants` row with `id` matching `sim-*`
       - [ ] `user_restaurant_access` (URA) membership row linking Auth user ↔ sim restaurant
       - [ ] Auth user used for `prod_jwt`-style password grant
       - [ ] Service-role allowed for **seed/teardown only** (never for claiming user-path RLS proof)
       - Explicit: service-role tests ≠ user-path RLS proof
       - One-line: "Group 1 T2+ later phases must include JWT path."
    5. **Idempotent teardown (D-20) + gap table (C2)** — document contract:
       - Conceptual sweep: `DELETE WHERE restaurant_id LIKE 'sim-%' AND id LIKE 'sim-%'` (and/or per-restaurant after run)
       - **Embed current `E2E_TABLES` verbatim** from `conftest_prod.py:251-260` (all 8 names):
         `inventory_stock`, `notification_deliveries`, `notification_logs`, `order_interactions`, `calendar_events`, `pos_webhook_logs`, `system_audit_log`, `master_wine_library_submissions`
       - **Gap statement:** registry Table D domain buckets (inventory ledger, POS/sales, procurement, analytics, …) beyond these 8 tables are **out of current teardown** — optional column `teardown coverage: none|partial|full` linking domains → coverage
       - **Phase 37 gate:** Phase 37 **must** expand teardown table registry before first multi-archetype seed
       - Failures → Sentry tag `sim-orphan` / `e2e-orphan` — **NEVER raise**
       - Never delete `e2e-test-restaurant` anchor
    6. **Auth / secrets** — reuse Supabase Auth REST password grant pattern from `prod_jwt`; never log JWT; secrets stay in GitHub Actions secrets / server env.
    7. **CI capability note (C1)** — short subsection: nightly e2e-prod may lack secrets; document that live seed/teardown requires secrets restored (ops) — Phase 36 does not change job behavior.
    8. **Anti-patterns** — copy Phase 25 list: no `PYTEST_RUNNING` in prod E2E; no JWT on disk; no raise in teardown; no staging-only campaign default (D-07 cloud stack).

    Do NOT modify `conftest_prod.py` or `setup_e2e_anchor.py` in this plan (documentation only). Do NOT implement the restaurant generator.
  </action>
  <verify>
    <automated>test -f .planning/testing/SYNTHETIC-TENANT.md && rg -n 'sim-\*|sim-' .planning/testing/SYNTHETIC-TENANT.md | head -20 && rg -n 'e2e-test-restaurant|user_restaurant_access|NEVER raise|Phase 37|conftest_prod' .planning/testing/SYNTHETIC-TENANT.md | head -25 && rg -n 'PYTEST_RUNNING|JWT' .planning/testing/SYNTHETIC-TENANT.md | head -10 && for t in inventory_stock notification_deliveries notification_logs order_interactions calendar_events pos_webhook_logs system_audit_log master_wine_library_submissions; do rg -q "$t" .planning/testing/SYNTHETIC-TENANT.md || { echo "MISSING E2E_TABLE $t"; exit 1; }; done && rg -q 'multi-archetype|expand teardown|out of current teardown' .planning/testing/SYNTHETIC-TENANT.md && rg -q 'restaurants|user_restaurant_access|Auth user|seed checklist|seed/teardown' .planning/testing/SYNTHETIC-TENANT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/testing/SYNTHETIC-TENANT.md` exists
    - `rg -n 'sim-' .planning/testing/SYNTHETIC-TENANT.md` ≥3 (prefix documented more than once)
    - `rg -n 'e2e-test-restaurant' .planning/testing/SYNTHETIC-TENANT.md` ≥1 with NEVER-delete language
    - All 8 current `E2E_TABLES` names appear
    - Explicit gap vs registry DB domains + Phase 37 must expand teardown before multi-archetype seed
    - RLS seed checklist includes restaurants `sim-*` + URA + Auth user + service-role seed/teardown only
    - `rg -n 'user_restaurant_access' .planning/testing/SYNTHETIC-TENANT.md` ≥1
    - `rg -n 'idempotent|teardown|NEVER raise|never raise' -i .planning/testing/SYNTHETIC-TENANT.md` ≥1
    - `rg -n 'Phase 37' .planning/testing/SYNTHETIC-TENANT.md` ≥1
    - `rg -n 'conftest_prod\.py' .planning/testing/SYNTHETIC-TENANT.md` ≥1
    - `git diff --name-only` for this task does not include `conftest_prod.py` or `setup_e2e_anchor.py`
  </acceptance_criteria>
  <done>SYNTHETIC-TENANT.md locks sim-* + RLS seed checklist + E2E_TABLES gap + Phase 37 teardown gate extending Phase 25 (TFND-06).</done>
</task>

<task type="auto">
  <name>Task 2: Annotate CI workflows + write operator README + checklists stub</name>
  <read_first>
    - .github/workflows/ci.yml (FULL — especially test-typescript, test-python, test-e2e job headers)
    - .github/workflows/e2e-prod.yml (FULL header + `on.schedule` cron)
    - .planning/phases/36-testing-foundation-functionality-registry/36-RESEARCH.md (CI Gap Analysis + D-24/D-25)
    - .planning/phases/36-testing-foundation-functionality-registry/36-PATTERNS.md (ci.yml + e2e-prod.yml + README sections)
    - .planning/phases/36-testing-foundation-functionality-registry/36-PREMORTEM.md (C1 paper-tiger; H5 Black)
    - .planning/phases/36-testing-foundation-functionality-registry/36-UX-SMOOTHNESS.md (§3 36-03 README + checklists IA)
    - .planning/testing/FUNCTIONALITY-REGISTRY.md
    - .planning/testing/RUBRIC.md
    - .planning/testing/EXISTING-TEST-INVENTORY.md
    - .planning/testing/TESTING-SCORECARD.md
    - .planning/testing/SYNTHETIC-TENANT.md
    - .planning/UX_PATHS_CATALOG.md
  </read_first>
  <files>.github/workflows/ci.yml, .github/workflows/e2e-prod.yml, .planning/testing/README.md, .planning/testing/checklists/README.md</files>
  <action>
    Implement TFND-05 / D-22..D-25 with **comment-only** workflow edits + operator-first README index + checklists naming stub.

    **A. `.github/workflows/ci.yml`**
    - Insert a comment block near the top of the file (after `name: CI` / before or after `on:`) OR immediately above `test-typescript:` containing exactly this intent (wording may wrap but must include the tokens `TFND-05`, `test-typescript`, `test-python`, `test-e2e`, `e2e-prod.yml`, `0 2 * * *`):
      ```yaml
      # TFND-05 (Phase 36 Testing Campaign):
      # test-typescript  → unit + integration (Jest api-gateway + Vitest web via turbo/pnpm test)
      # test-python      → unit + integration (pytest tests/; markers in pytest.ini not filtered today)
      # test-e2e         → local Playwright smoke (NOT cloud prod E2E)
      # Cloud nightly E2E remains .github/workflows/e2e-prod.yml (schedule: 0 2 * * *)
      # Do NOT treat TFND-05 as green CI — Black debt on studio_routes.py may keep main red
      ```
    - Optionally add one-line comments above `test-typescript` and `test-python` stating "unit + integration on push".
    - **FORBIDDEN:** create `.github/workflows/testing-campaign.yml`; change job steps/commands; add `-m unit/integration` filters; fix Black/ESLint/ruff failures; edit `deploy.yml`.

    **B. `.github/workflows/e2e-prod.yml`**
    - Keep existing `cron: '0 2 * * *'` unchanged.
    - Add header comment lines:
      - `TFND-05: Nightly cloud E2E skeleton — do not invent a second prod E2E paradigm (D-24).`
      - `Phase 42 will add a separate weekly AI eval workflow — do not implement here (D-25).`
      - `STATUS: schedule-present / capability-unverified until one wave XML lands — do not equate cron with healthy nightly.`
    - **FORBIDDEN:** new workflow file; changing secrets; setting `PYTEST_RUNNING`; altering wave steps. Do **not** change job behavior.

    **C. `.planning/testing/README.md`** — operator front door
    Create with this **order** (Operator quickstart FIRST, before artifact table):
    1. **Operator quickstart** (top of file — exact 4-step intent):
       1. Open `TESTING-SCORECARD.md` — 11-row board
       2. Pick a group → open that section in `FUNCTIONALITY-REGISTRY.md`
       3. For manual work → `checklists/` (stub now; filled in 39–40 / 43)
       4. For score fights → `RUBRIC.md` + inventory evidence paths — **do not browse inventory first**
    2. Table of files → TFND id → purpose for: FUNCTIONALITY-REGISTRY (01), RUBRIC (02), EXISTING-TEST-INVENTORY (03), TESTING-SCORECARD (04), SYNTHETIC-TENANT (06)
    3. **How to update scores** — 4 steps matching RUBRIC promotion protocol (incl. no promote-past-T1 without passes?=yes/waiver)
    4. **CI proof links + honesty banner (C1 + H5):**
       - `ci.yml` jobs `test-typescript`/`test-python`/`test-e2e`; nightly `e2e-prod.yml` cron
       - **Do not treat TFND-05 as green CI**; Black debt on `studio_routes.py` as of 2026-07-27
       - Require documenting **secrets present? yes/no as of &lt;date&gt;** from `gh run view` on a recent `e2e-prod` run (**secret names only**, never values). Premortem baseline 2026-07-27: secrets missing; no `test-results/` → mark TFND-05 **schedule-present / capability-unverified** until one wave XML lands
       - TFND-05 = wiring documented; main may still be red; do not promote scores from green-wishful thinking
    5. Link to `.planning/UX_PATHS_CATALOG.md` as “status oracle for ✅/⚠️/❌ before writing checklist steps”
    6. Out-of-scope pointer: Phases 37–43 own generator/SimPOS/breadth/truth/evals/journeys

    **D. `.planning/testing/checklists/README.md`** — naming contract stub only
    Create directory + README stating:
    - Filename: `g{N}-{slug}-manual.md` (e.g. `g1-identity-manual.md`) using locked slugs from registry
    - One checklist per group; Phase 39 owns g1–4; Phase 40 owns g5–7+9; Phase 43 owns scanner/admin/journey overlays
    - Each checklist header (when filled later) links: registry group, scorecard row, 3–7 canonical routes
    - Do **not** create the per-group checklist files in Phase 36

    Confirm after edits: `ls .github/workflows/*.yml` still has no new testing-campaign workflow.
  </action>
  <verify>
    <automated>rg -n 'TFND-05' .github/workflows/ci.yml .github/workflows/e2e-prod.yml && rg -n "cron: '0 2 \* \* \*'" .github/workflows/e2e-prod.yml && rg -n 'Phase 42|D-25|D-24|capability-unverified' .github/workflows/e2e-prod.yml && test -f .planning/testing/README.md && test -f .planning/testing/checklists/README.md && for f in FUNCTIONALITY-REGISTRY.md RUBRIC.md EXISTING-TEST-INVENTORY.md TESTING-SCORECARD.md SYNTHETIC-TENANT.md; do rg -q "$f" .planning/testing/README.md || { echo "README missing $f"; exit 1; }; done && rg -q 'Operator quickstart|TESTING-SCORECARD' .planning/testing/README.md && rg -q 'UX_PATHS_CATALOG' .planning/testing/README.md && rg -q 'secrets present|capability-unverified|schedule-present|studio_routes|Black' .planning/testing/README.md && rg -q 'g\{N\}|g1-identity|g\{N\}-\{slug\}' .planning/testing/checklists/README.md && test ! -f .github/workflows/testing-campaign.yml && ! rg -n '^\s*PYTEST_RUNNING:' .github/workflows/e2e-prod.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'TFND-05' .github/workflows/ci.yml` ≥1
    - `rg -n 'unit \+ integration|unit + integration|studio_routes' .github/workflows/ci.yml` ≥1 OR comment block lists both `test-typescript` and `test-python` as unit+integration and notes Black risk
    - `rg -n "cron: '0 2 \* \* \*'" .github/workflows/e2e-prod.yml` ≥1 (unchanged schedule)
    - `rg -n 'Phase 42' .github/workflows/e2e-prod.yml` ≥1
    - `rg -n 'D-24|second prod|do not invent|capability-unverified' -i .github/workflows/e2e-prod.yml` ≥1
    - File `.planning/testing/README.md` exists; **Operator quickstart is the first substantive section** (scorecard-first)
    - README names all five TFND markdown artifacts + links `UX_PATHS_CATALOG.md`
    - README documents secrets present? yes/no as of date + TFND-05 schedule-present / capability-unverified + Black/`studio_routes.py` honesty
    - File `.planning/testing/checklists/README.md` exists with `g{N}-{slug}-manual.md` naming contract
    - File `.github/workflows/testing-campaign.yml` does **not** exist
    - `e2e-prod.yml` still has no env assignment `PYTEST_RUNNING:`
    - Behavioral job `run:` steps for `pnpm run test` / `pytest` / Playwright unchanged (diff is comments-only aside from planning docs)
  </acceptance_criteria>
  <done>CI skeleton annotated without fork; operator README + checklists naming stub + C1/H5 honesty (TFND-05 + campaign index).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI secrets → runners | SUPABASE_SERVICE_ROLE_KEY, E2E passwords, JWT material must stay in GitHub secrets |
| Synthetic tenant docs → future seed scripts | Convention must prevent sim data colliding with real restaurants |
| Workflow YAML → production E2E | Comment-only edits must not disable Sentry via PYTEST_RUNNING |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03-01 | Information Disclosure | sim tenant / JWT docs | mitigate | Document never-log-JWT; no secrets in SYNTHETIC-TENANT.md or README; e2e-prod secrets status names-only |
| T-36-03-02 | Elevation of Privilege | RLS bypass via service-role | mitigate | Mandatory seed checklist: restaurants sim-* + URA + Auth user; service-role ≠ user-path proof |
| T-36-03-03 | Tampering | Incomplete teardown orphans | mitigate | Embed current 8-table E2E_TABLES + gap vs registry DB domains; Phase 37 expand before multi-archetype; Sentry `sim-orphan`; NEVER raise; never delete e2e anchor |
| T-36-03-04 | Information Disclosure | CI secret exfiltration | mitigate | Restate Phase 25 rule: no JWT in JUnit/logs; secrets only via `${{ secrets.* }}` |
| T-36-03-05 | Denial of Service / Integrity | Second E2E paradigm / PYTEST_RUNNING | mitigate | Forbid new workflow; forbid setting PYTEST_RUNNING; verify cron unchanged; no job behavior change |
| T-36-03-06 | Spoofing | Fake "CI fixed" / healthy nightly claims | mitigate | README+scorecard: TFND-05 ≠ green CI; Black studio_routes.py; schedule-present / capability-unverified until wave XML |

**High-severity gate:** If any edit would set `PYTEST_RUNNING` in `e2e-prod.yml` or embed service-role keys in markdown — **BLOCK** and revert.
</threat_model>

<verification>
- SYNTHETIC-TENANT.md greps for sim-/RLS checklist/E2E_TABLES/Phase 37 gate pass
- ci.yml + e2e-prod.yml contain TFND-05 annotations; cron intact; capability-unverified; no testing-campaign.yml
- README Operator quickstart first; lists all five artifacts; links UX_PATHS_CATALOG; secrets honesty
- checklists/README.md naming contract present
</verification>

<success_criteria>
- TFND-06: sim-* convention with RLS seed checklist + E2E_TABLES gap + Phase 37 teardown gate
- TFND-05: unit/integration on push + nightly E2E annotated without forking Phase 25; honesty = schedule-present / capability-unverified (not green CI)
- README is operator front door; checklists IA stubbed for 39–43
</success_criteria>

<output>
After completion, create `.planning/phases/36-testing-foundation-functionality-registry/36-03-SUMMARY.md`
</output>

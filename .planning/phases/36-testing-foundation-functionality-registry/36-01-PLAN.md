---
phase: 36-testing-foundation-functionality-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/testing/RUBRIC.md
  - .planning/testing/FUNCTIONALITY-REGISTRY.md
autonomous: true
requirements:
  - TFND-02
  - TFND-01
user_setup: []

must_haves:
  truths:
    - "T0–T4 rubric exists as standalone RUBRIC.md with locked definitions from D-12..D-16"
    - "FUNCTIONALITY-REGISTRY.md lists exactly 11 groups with D-09 seed scope text"
    - "Every api-gateway Nest top-level module maps to exactly one primary group"
    - "Every App.tsx route, orchestrator agent, and DB domain bucket maps to exactly one primary group"
    - "Collision rules document primary-only mapping with also_touches secondary notes (D-10)"
  artifacts:
    - path: ".planning/testing/RUBRIC.md"
      provides: "T0–T4 scoring contract + Agent Level mirror + evidence standards"
      contains: "T0"
    - path: ".planning/testing/FUNCTIONALITY-REGISTRY.md"
      provides: "Canonical 11-group surface map for Nest/web/agents/DB"
      contains: "Identity & Access"
  key_links:
    - from: ".planning/testing/FUNCTIONALITY-REGISTRY.md"
      to: ".planning/testing/RUBRIC.md"
      via: "cross-link How tested is X → rubric"
      pattern: "RUBRIC\\.md"
    - from: ".planning/testing/FUNCTIONALITY-REGISTRY.md"
      to: "apps/api-gateway/src/"
      via: "Nest module mapping table"
      pattern: "pos-hub|inventory-ledger|ux-optimizer"
---

<objective>
Create the scoring rubric (TFND-02) and the canonical 11-group functionality registry (TFND-01) so later Testing Campaign phases have a single answer to "how tested is X?" and how to score coverage.

Purpose: Lock vocabulary (T0–T4) and surface→group ownership before inventory/scorecard/CI work.
Output: `.planning/testing/RUBRIC.md`, `.planning/testing/FUNCTIONALITY-REGISTRY.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-testing-foundation-functionality-registry/36-CONTEXT.md
@.planning/phases/36-testing-foundation-functionality-registry/36-RESEARCH.md
@.planning/phases/36-testing-foundation-functionality-registry/36-PATTERNS.md
</context>

<interfaces>
<!-- Locked T0–T4 definitions (D-12..D-16) — copy verbatim into RUBRIC.md -->
T0 = untested
T1 = smoke (happy path runs)
T2 = contract (happy + key errors + assertions on outputs)
T3 = resilient (idempotency / concurrency / failure modes)
T4 = ground-truth verified (simulator oracle or golden dataset)

<!-- Locked 11 groups (D-09) — do not rename -->
1 Identity & Access | 2 Catalog & Extraction | 3 Inventory Operations | 4 POS & Sales Ingestion
5 Procurement & Vendors | 6 Communications & Email Intelligence | 7 Calendar & Scheduling
8 Analytics, Reports & Insights | 9 Notifications & Alerts | 10 AI Assistants & Recommendations
11 Platform & Agent Infrastructure

<!-- Nest top-level modules that MUST appear as rows (ls apps/api-gateway/src dirs) -->
analytics, auth, calendar, common, communications, contacts, conversations, dashboard, database,
events, inventory, inventory-ledger, menus, mobile, notifications, one-tap-actions, organizations,
pos-hub, procurement, providers, push, reports, restaurant-templates, restaurants, settings,
storage-locations, team, toast, user-preferences, ux-optimizer, vendor-catalogue, websocket, wines,
__tests__ (map per-file or as bucket note)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write standalone T0–T4 RUBRIC.md</name>
  <read_first>
    - .planning/phases/36-testing-foundation-functionality-registry/36-CONTEXT.md (D-12..D-17)
    - .planning/phases/36-testing-foundation-functionality-registry/36-RESEARCH.md (Pattern 4 Level mirror + Evidence standards)
    - .planning/phases/36-testing-foundation-functionality-registry/36-PATTERNS.md (RUBRIC.md section)
    - .planning/ROADMAP.md (Phase 36 success criterion #2 — verbatim T0–T4 text)
    - .planning/PROJECT.md (Agent Level 0–4 language for mirror table)
  </read_first>
  <files>.planning/testing/RUBRIC.md</files>
  <action>
    Create directory `.planning/testing/` if missing. Create standalone `.planning/testing/RUBRIC.md` (Claude's discretion per CONTEXT: standalone file, not embedded in scorecard) implementing TFND-02 / D-12..D-16.

    Required sections (exact content constraints):
    1. **Purpose** — one paragraph: scoring contract for Testing Campaign Phases 36–43; link target for scorecard promotions.
    2. **T0–T4 definitions** — use these exact strings:
       - T0: untested
       - T1: smoke (happy path runs)
       - T2: contract (happy + key errors + assertions on outputs)
       - T3: resilient (idempotency / concurrency / failure modes)
       - T4: ground-truth verified (asserted against simulator oracle or golden dataset)
    3. **Agent Level mirror table** — columns: Test maturity | Meaning | Agent Level analogue — copy RESEARCH Pattern 4 rows (T0↔L0 … T4↔beyond Level / oracle).
    4. **Evidence standards** — bullet list stating:
       - Promotion requires citing inventory paths + CI job names (`test-typescript`, `test-python`, `test-e2e`, `e2e-prod`)
       - Never promote on file-count alone
       - T1 = any automated happy-path exists in any tier for the group
       - T4 requires simulator oracle (Phase 37+) or golden datasets (Phase 42) — not reachable in Phase 36 baselines
    5. **How to promote a score** — numbered steps: update EXISTING-TEST-INVENTORY evidence → apply this rubric → edit TESTING-SCORECARD row + date → link owning next phase.

    Do NOT implement eval runners, suites, or scorecard rows here. Do NOT rename T-levels.
  </action>
  <verify>
    <automated>test -f .planning/testing/RUBRIC.md && rg -n 'T0|T1|T2|T3|T4' .planning/testing/RUBRIC.md | head -20 && rg -q 'untested' .planning/testing/RUBRIC.md && rg -q 'happy path' .planning/testing/RUBRIC.md && rg -q 'idempotency' .planning/testing/RUBRIC.md && rg -q 'ground-truth|golden' .planning/testing/RUBRIC.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/testing/RUBRIC.md` exists
    - `rg -n 'T0:' .planning/testing/RUBRIC.md` matches a line containing `untested`
    - `rg -n 'T1:' .planning/testing/RUBRIC.md` matches a line containing `smoke`
    - `rg -n 'T2:' .planning/testing/RUBRIC.md` matches a line containing `contract`
    - `rg -n 'T3:' .planning/testing/RUBRIC.md` matches a line containing `resilient`
    - `rg -n 'T4:' .planning/testing/RUBRIC.md` matches a line containing `ground-truth` OR `golden`
    - `rg -n 'Agent Level|Level 0|Level 3' .planning/testing/RUBRIC.md` returns ≥1 match
    - `rg -n 'Evidence|promote|test-typescript|e2e-prod' .planning/testing/RUBRIC.md` returns ≥1 match each for Evidence/promote and a CI job name
  </acceptance_criteria>
  <done>Standalone RUBRIC.md locked with D-12..D-16 definitions, Level mirror, and promotion evidence rules (TFND-02).</done>
</task>

<task type="auto">
  <name>Task 2: Write FUNCTIONALITY-REGISTRY.md with full surface map</name>
  <read_first>
    - .planning/phases/36-testing-foundation-functionality-registry/36-CONTEXT.md (D-09..D-11, D-02 mobile deferred)
    - .planning/phases/36-testing-foundation-functionality-registry/36-RESEARCH.md (Surface Map Draft §§A–D + Pattern 1 collision rules)
    - .planning/phases/36-testing-foundation-functionality-registry/36-PATTERNS.md (FUNCTIONALITY-REGISTRY section)
    - apps/web/src/App.tsx (all Route path= entries)
    - apps/api-gateway/src/ (top-level module directory names)
    - services/agent-orchestrator/agents/ (*.py agent files)
  </read_first>
  <files>.planning/testing/FUNCTIONALITY-REGISTRY.md</files>
  <action>
    Create `.planning/testing/FUNCTIONALITY-REGISTRY.md` implementing TFND-01 / D-09..D-11.

    Required structure:
    1. **Purpose** — "Canonical answer to how tested is X?" + Requirements TFND-01 + links to `RUBRIC.md`, (future) `EXISTING-TEST-INVENTORY.md`, `TESTING-SCORECARD.md`.
    2. **11 group definitions** — copy D-09 / ROADMAP seed text verbatim for groups 1–11 (do not rename).
    3. **Mapping rules** — include these six rules verbatim from RESEARCH Pattern 1 / PATTERNS:
       1. Prefer user-facing workflow group for pages/routes.
       2. Prefer write-domain group for Nest modules / DB tables.
       3. Prefer group 11 for shared infra (`common/`, `database/`, idempotency, outbox, saga, DLQ, health, admin).
       4. Prefer group 1 for auth/guards/org/team/profile/settings.
       5. Mobile → map for completeness, mark **campaign-deferred**.
       6. Cross-cuts: one primary + `also_touches:` note — never two primaries (D-10).
    4. **Table A — Nest modules** — one row per top-level dir under `apps/api-gateway/src/` (except `node_modules`-like noise). Use RESEARCH §A primary assignments as defaults:
       - Group 1: auth, organizations, restaurants, team, settings, user-preferences, restaurant-templates
       - Group 2: wines, menus
       - Group 3: inventory, inventory-ledger, storage-locations
       - Group 4: pos-hub, toast
       - Group 5: procurement, providers, vendor-catalogue, contacts (also_touches: 6)
       - Group 6: communications, conversations
       - Group 7: calendar, events
       - Group 8: dashboard, reports, analytics
       - Group 9: notifications, one-tap-actions, push, websocket (also_touches: 11), mobile (campaign-deferred)
       - Group 10: ux-optimizer
       - Group 11: common, database
       - `__tests__`: note "map each file to owning group; see inventory"
       Columns: `Module | Specs (count or unknown) | Primary group | also_touches`
    5. **Table B — Web routes** — every `path=` from `App.tsx` Routes; use RESEARCH §B. Explicitly document orphan `RecurringOrders.tsx` under group 5 (not routed).
    6. **Table C — Orchestrator agents** — every `agents/*.py` file with RESEARCH §C primary groups (compliance_agent → 11 also_touches 5).
    7. **Table D — DB domains** — migration-derived domain buckets from RESEARCH §D (11 rows). State explicitly: source of truth is `supabase/migrations/`, NOT `packages/database/src/types/database.types.ts`.
    8. **Coverage assertion** — short checklist stating Nest modules / routes / agents / DB domains each have exactly one primary.

    Do NOT rewrite any tests. Do NOT create inventory or scorecard in this task.
  </action>
  <verify>
    <automated>test -f .planning/testing/FUNCTIONALITY-REGISTRY.md && rg -q 'Identity & Access' .planning/testing/FUNCTIONALITY-REGISTRY.md && rg -q 'Platform & Agent Infrastructure' .planning/testing/FUNCTIONALITY-REGISTRY.md && rg -n 'pos-hub|inventory-ledger|ux-optimizer|also_touches|campaign-deferred' .planning/testing/FUNCTIONALITY-REGISTRY.md | head -30 && for m in auth inventory pos-hub procurement communications calendar analytics notifications common wines; do rg -q "$m" .planning/testing/FUNCTIONALITY-REGISTRY.md || { echo "MISSING module $m"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/testing/FUNCTIONALITY-REGISTRY.md` exists
    - All 11 locked group names appear at least once
    - Nest modules `auth`, `inventory`, `pos-hub`, `procurement`, `communications`, `calendar`, `analytics`, `notifications`, `common`, `wines`, `mobile` each appear ≥1 time
    - `rg -n 'also_touches' .planning/testing/FUNCTIONALITY-REGISTRY.md` ≥1
    - `rg -n 'campaign-deferred' .planning/testing/FUNCTIONALITY-REGISTRY.md` ≥1 (mobile)
    - `rg -n 'RecurringOrders' .planning/testing/FUNCTIONALITY-REGISTRY.md` ≥1
    - `rg -n 'RUBRIC.md' .planning/testing/FUNCTIONALITY-REGISTRY.md` ≥1
    - `rg -n 'database.types.ts' .planning/testing/FUNCTIONALITY-REGISTRY.md` ≥1 (anti-pattern callout)
    - No surface is assigned two primary groups in the same row (primary column is a single group id/name)
  </acceptance_criteria>
  <done>FUNCTIONALITY-REGISTRY.md maps Nest modules, web routes, agents, and DB domains to exactly one of 11 groups with collision rules (TFND-01 / D-09..D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs → later CI/test authors | Registry ownership steers which suites may touch which tenants/data |
| Docs → no runtime | These files are not served to browsers; no user input crosses a network boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01-01 | Information Disclosure | FUNCTIONALITY-REGISTRY.md | accept | No secrets in registry; surface names only — low value if leaked |
| T-36-01-02 | Elevation of Privilege | Group 1 / mobile mapping | mitigate | Mark `mobile` campaign-deferred; do not imply mobile suites are in-scope for 36–43 |
| T-36-01-03 | Tampering | Rubric promotion rules | mitigate | Evidence standards require inventory paths + CI job names — blocks score inflation by file-count |
</threat_model>

<verification>
- Both artifacts exist under `.planning/testing/`
- Grep gates for T0–T4 and 11 group names pass
- Nest module loop in Task 2 verify exits 0
</verification>

<success_criteria>
- TFND-02 satisfied by standalone RUBRIC.md with locked definitions
- TFND-01 satisfied by registry with Nest/web/agent/DB maps and D-10 collision rules
</success_criteria>

<output>
After completion, create `.planning/phases/36-testing-foundation-functionality-registry/36-01-SUMMARY.md`
</output>

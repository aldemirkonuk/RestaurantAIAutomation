---
phase: 24-provider-communication-pipeline-email-intelligence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/agent-orchestrator/tests/test_email_intel_agent.py
  - services/agent-orchestrator/tests/test_flag_extractor.py
  - services/agent-orchestrator/tests/test_negotiation_facts.py
  - services/agent-orchestrator/tests/test_model_tiers.py
  - services/agent-orchestrator/tests/test_conversation_to_order.py
  - services/agent-orchestrator/tests/test_sensitivity_detection.py
  - services/agent-orchestrator/tests/test_conversation_embeddings.py
autonomous: true
requirements:
  - FLAG-01
  - FLAG-02
  - FLAG-03
  - FLAG-04
  - FLAG-05
  - FACT-01
  - FACT-02
  - FACT-03
  - FACT-04
  - CORD-01
  - CORD-02
  - CORD-03
  - CORD-04
  - CORD-05
  - CORD-06
  - CORD-07
  - ETHC-01
  - ETHC-02
  - ETHC-03
  - ETHC-04
  - ETHC-05
  - ETHC-06
  - ETHC-07
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - SRCH-05
  - MODL-01
  - MODL-02
  - MODL-03
  - MODL-04
  - MODL-05
  - INTEL-01
  - INTEL-02
  - INTEL-03
  - COMMS-01
must_haves:
  truths:
    - "pytest collects all 7 test files with 0 errors (tests skip, not fail)"
    - "Each test file contains correctly named test functions matching VALIDATION.md per-task verification map"
    - "Test files import with try/except so collection succeeds even before implementation"
  artifacts:
    - path: services/agent-orchestrator/tests/test_email_intel_agent.py
      provides: "Stubs for INTEL-01..06 — classify, promo extraction, dedup"
      min_lines: 50
    - path: services/agent-orchestrator/tests/test_flag_extractor.py
      provides: "Stubs for FLAG-01..05 — categories A-I extraction, rescore, carry-forward"
      min_lines: 60
    - path: services/agent-orchestrator/tests/test_negotiation_facts.py
      provides: "Stubs for FACT-01..04 — append-only, commitment types, integrity check"
      min_lines: 50
    - path: services/agent-orchestrator/tests/test_model_tiers.py
      provides: "Stubs for MODL-01..05 — all three LLM tiers mocked"
      min_lines: 40
    - path: services/agent-orchestrator/tests/test_conversation_to_order.py
      provides: "Stubs for CORD-01..07 — AGREEMENT → order, audit trail"
      min_lines: 50
    - path: services/agent-orchestrator/tests/test_sensitivity_detection.py
      provides: "Stubs for ETHC-01..07 — per-segment tagging"
      min_lines: 40
    - path: services/agent-orchestrator/tests/test_conversation_embeddings.py
      provides: "Stubs for SRCH-01..05 — signal-only embedding, sensitive exclusion, search"
      min_lines: 40
  key_links:
    - from: "VALIDATION.md per-task verification map"
      to: "test function names in stub files"
      via: "exact function names must match — e.g. test_classify_operational, test_promo_extraction"
      pattern: "def test_classify_operational|def test_promo_extraction|def test_deduplication"
---

<objective>
Create 7 test stub files that define the expected behavior contract for Phase 24. All stubs use `pytest.mark.skip` so they COLLECT without error and RUN without failing — they become real tests as each implementation plan completes.

Purpose: Establish the feedback sampling infrastructure from day 1; validate test function names match VALIDATION.md.
Output: 7 test files, all collection-clean, all tests skipped until implementation plans complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-provider-communication-pipeline-email-intelligence/24-VALIDATION.md
@.planning/phases/24-provider-communication-pipeline-email-intelligence/24-RESEARCH.md
@services/agent-orchestrator/tests/test_base_agent_infra.py
@services/agent-orchestrator/tests/test_pos_integration_hardening.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs — email intel, flag extractor, negotiation facts, model tiers</name>
  <read_first>
    - services/agent-orchestrator/tests/test_pos_integration_hardening.py (pytest-asyncio patterns used in this codebase)
    - services/agent-orchestrator/tests/conftest.py (shared fixtures)
    - .planning/phases/24-provider-communication-pipeline-email-intelligence/24-VALIDATION.md (exact test function names required)
  </read_first>
  <files>
    services/agent-orchestrator/tests/test_email_intel_agent.py,
    services/agent-orchestrator/tests/test_flag_extractor.py,
    services/agent-orchestrator/tests/test_negotiation_facts.py,
    services/agent-orchestrator/tests/test_model_tiers.py
  </files>
  <action>
Create 4 test stub files. Each uses `pytest.mark.skip("Wave 0 stub — implement in 24-XX-PLAN.md")` at the function level so tests skip (not fail) until implementation is complete.

**tests/test_email_intel_agent.py** — stubs for INTEL-01..06, COMMS-01:
```python
"""Phase 24 test stubs for EmailIntelAgent — INTEL-01..06, COMMS-01.
Remove skip markers as 24-04-PLAN.md completes."""
import pytest

try:
    from agents.email_intel_agent import EmailIntelAgent
except ImportError:
    EmailIntelAgent = None

try:
    from services.menu_fit_analyzer import MenuFitAnalyzer
except ImportError:
    MenuFitAnalyzer = None


@pytest.mark.asyncio
async def test_classify_operational():
    """INTEL-01: Classifies operational email (order status, invoice) as OPERATIONAL."""
    pytest.skip("Wave 0 stub — implement after 24-04-PLAN.md completes")


@pytest.mark.asyncio
async def test_classify_promo():
    """INTEL-01: Classifies promotional email (discount, clearance) as PROMO."""
    pytest.skip("Wave 0 stub — implement after 24-04-PLAN.md completes")


@pytest.mark.asyncio
async def test_classify_noise():
    """INTEL-01: Classifies spam/noise email as NOISE."""
    pytest.skip("Wave 0 stub — implement after 24-04-PLAN.md completes")


@pytest.mark.asyncio
async def test_operational_routes_to_email_parsing_agent():
    """INTEL-02: OPERATIONAL emails are re-published to email.inbound.received."""
    pytest.skip("Wave 0 stub — implement after 24-04-PLAN.md completes")


@pytest.mark.asyncio
async def test_promo_extraction():
    """INTEL-03: PROMO email → extracted product, discount_pct, valid_until, vendor → vendor_promotions row."""
    pytest.skip("Wave 0 stub — implement after 24-04-PLAN.md completes")


@pytest.mark.asyncio
async def test_deduplication():
    """INTEL-06: Same deal from same vendor within 7 days → suppressed (dedup_hash match)."""
    pytest.skip("Wave 0 stub — implement after 24-04-PLAN.md completes")


@pytest.mark.asyncio
async def test_menu_fit_strong():
    """D-02: MenuFitAnalyzer returns STRONG_FIT when promo product matches current menu."""
    pytest.skip("Wave 0 stub — implement after 24-04-PLAN.md completes")


@pytest.mark.asyncio
async def test_promo_nudges_procurement_agent():
    """INTEL-04: STRONG_FIT/PARTIAL_FIT promo → PROCUREMENT_NUDGE flag created."""
    pytest.skip("Wave 0 stub — implement after 24-04-PLAN.md completes")


@pytest.mark.asyncio
async def test_daily_digest_accumulates():
    """INTEL-05: Daily digest Redis key accumulates promos; cleared after digest send."""
    pytest.skip("Wave 0 stub — implement after 24-04-PLAN.md completes")
```

**tests/test_flag_extractor.py** — stubs for FLAG-01..05:
```python
"""Phase 24 test stubs for FlagExtractor — FLAG-01..05."""
import pytest

try:
    from services.flag_extractor import FlagExtractor
except ImportError:
    FlagExtractor = None


@pytest.mark.asyncio
async def test_category_a_flags():
    """FLAG-01: Provider unavailability message → PROVIDER_UNAVAILABLE flag extracted."""
    pytest.skip("Wave 0 stub — implement after 24-06-PLAN.md completes")


@pytest.mark.asyncio
async def test_category_b_personal_milestone():
    """FLAG-01: 'My son's birthday is Friday' → PERSONAL_MILESTONE flag, INTEL urgency."""
    pytest.skip("Wave 0 stub — implement after 24-06-PLAN.md completes")


@pytest.mark.asyncio
async def test_critical_urgency_recall():
    """FLAG-01: Safety recall message → SAFETY_RECALL flag, CRITICAL urgency."""
    pytest.skip("Wave 0 stub — implement after 24-06-PLAN.md completes")


@pytest.mark.asyncio
async def test_rescore_after_session():
    """FLAG-02: _rescore_flags() updates relevance_score after session close."""
    pytest.skip("Wave 0 stub — implement after 24-06-PLAN.md completes")


@pytest.mark.asyncio
async def test_flag_carry_forward():
    """FLAG-03, D-09: Unactioned flag carries forward once; after 2 no-actions → archived."""
    pytest.skip("Wave 0 stub — implement after 24-06-PLAN.md completes")


@pytest.mark.asyncio
async def test_low_relevance_auto_archive():
    """FLAG-04: Flag with relevance_score < 0.3 → status='archived' automatically."""
    pytest.skip("Wave 0 stub — implement after 24-06-PLAN.md completes")


@pytest.mark.asyncio
async def test_intel_tier_no_push():
    """FLAG-04: INTEL-tier flag never pushed to notifications, visible on provider card only."""
    pytest.skip("Wave 0 stub — implement after 24-06-PLAN.md completes")


@pytest.mark.asyncio
async def test_archived_flags_queryable():
    """FLAG-05: Archived flags returned by archived query endpoint."""
    pytest.skip("Wave 0 stub — implement after 24-06-PLAN.md completes")
```

**tests/test_negotiation_facts.py** — stubs for FACT-01..04:
```python
"""Phase 24 test stubs for NegotiationFactService — FACT-01..04."""
import pytest

try:
    from services.negotiation_fact_service import NegotiationFactService
except ImportError:
    NegotiationFactService = None


@pytest.mark.asyncio
async def test_fact_insertion_append_only():
    """FACT-01: New numerical claim → INSERT with commitment_type; no UPDATE on provenance."""
    pytest.skip("Wave 0 stub — implement after 24-06-PLAN.md completes")


@pytest.mark.asyncio
async def test_commitment_type_indicative():
    """FACT-04: Vague language ('roughly $50') → commitment_type=INDICATIVE."""
    pytest.skip("Wave 0 stub — implement after 24-06-PLAN.md completes")


@pytest.mark.asyncio
async def test_commitment_type_agreement():
    """FACT-04: 'OK that works, agreed' → commitment_type=AGREEMENT."""
    pytest.skip("Wave 0 stub — implement after 24-06-PLAN.md completes")


@pytest.mark.asyncio
async def test_integrity_check():
    """FACT-02, D-08: Provider claims value not in OFFER/AGREEMENT chain → NEGOTIATION_INTEGRITY_FLAG raised."""
    pytest.skip("Wave 0 stub — implement after 24-06-PLAN.md completes")


@pytest.mark.asyncio
async def test_supersede_fact():
    """FACT-01: Updated price → new row INSERT with supersedes_id pointing to old row; old row status='superseded'."""
    pytest.skip("Wave 0 stub — implement after 24-06-PLAN.md completes")


@pytest.mark.asyncio
async def test_agreement_triggers_order_event():
    """FACT-03: AGREEMENT with quantity+price+product → order creation event published."""
    pytest.skip("Wave 0 stub — implement after 24-10-PLAN.md completes")
```

**tests/test_model_tiers.py** — stubs for MODL-01..05:
```python
"""Phase 24 test stubs for model tier architecture — MODL-01..05."""
import pytest
from unittest.mock import AsyncMock, patch


@pytest.mark.asyncio
async def test_tier1_gemini_flash_triage():
    """MODL-01: Gemini 2.0 Flash called with new google-genai SDK for every message triage."""
    pytest.skip("Wave 0 stub — implement after 24-03-PLAN.md / 24-04-PLAN.md completes")


@pytest.mark.asyncio
async def test_tier2_haiku_flag_confirmation():
    """MODL-02: Claude Haiku 4.5 called for ambiguous flags (confidence < 0.7)."""
    pytest.skip("Wave 0 stub — implement after 24-06-PLAN.md completes")


@pytest.mark.asyncio
async def test_tier3_haiku_summarization():
    """MODL-03: Claude Haiku 4.5 called on session close with max_tokens=1024."""
    pytest.skip("Wave 0 stub — implement after 24-07-PLAN.md completes")


@pytest.mark.asyncio
async def test_tier4_embeddings_async():
    """MODL-04: text-embedding-004 embedding wrapped in asyncio.create_task (non-blocking)."""
    pytest.skip("Wave 0 stub — implement after 24-08-PLAN.md completes")


@pytest.mark.asyncio
async def test_gemini_json_mode_enforced():
    """MODL-01: response_mime_type='application/json' enforced; output parsed via json.loads, not eval."""
    pytest.skip("Wave 0 stub — implement after 24-04-PLAN.md completes")
```
  </action>
  <acceptance_criteria>
    - `pytest tests/test_email_intel_agent.py --collect-only 2>&1 | grep "test session starts"` — no collection errors
    - `pytest tests/test_flag_extractor.py --collect-only 2>&1 | grep "test session starts"` — no collection errors
    - `pytest tests/test_negotiation_facts.py --collect-only 2>&1 | grep "test session starts"` — no collection errors
    - `pytest tests/test_model_tiers.py --collect-only 2>&1 | grep "test session starts"` — no collection errors
    - `grep -c "pytest.skip" services/agent-orchestrator/tests/test_email_intel_agent.py` returns 9
    - `grep -c "def test_" services/agent-orchestrator/tests/test_flag_extractor.py` returns 8
    - All 4 files import with try/except so ImportError on missing classes does not fail collection
  </acceptance_criteria>
  <verify>
    <automated>cd services/agent-orchestrator && pytest tests/test_email_intel_agent.py tests/test_flag_extractor.py tests/test_negotiation_facts.py tests/test_model_tiers.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>4 test stub files exist; pytest collects all with 0 errors, all tests show as SKIPPED when run</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs — conversation-to-order, sensitivity detection, embeddings</name>
  <read_first>
    - services/agent-orchestrator/tests/test_email_intel_agent.py (just created — use same import pattern)
    - .planning/phases/24-provider-communication-pipeline-email-intelligence/24-VALIDATION.md (exact test function names: test_agreement_creates_order, test_no_embed_sensitive, test_health_topic_pauses)
  </read_first>
  <files>
    services/agent-orchestrator/tests/test_conversation_to_order.py,
    services/agent-orchestrator/tests/test_sensitivity_detection.py,
    services/agent-orchestrator/tests/test_conversation_embeddings.py
  </files>
  <action>
Create 3 more test stub files using the same pattern.

**tests/test_conversation_to_order.py** — stubs for CORD-01..07:
```python
"""Phase 24 test stubs for conversation-to-order flow — CORD-01..07."""
import pytest

try:
    from agents.provider_conversation_agent import ProviderConversationAgent
except ImportError:
    ProviderConversationAgent = None


@pytest.mark.asyncio
async def test_agreement_creates_order():
    """CORD-01: AGREEMENT with qty+price+product → procurement_orders row with order_source='conversation'."""
    pytest.skip("Wave 0 stub — implement after 24-10-PLAN.md completes")


@pytest.mark.asyncio
async def test_order_appears_on_orders_page():
    """CORD-02: Auto-created order status='PENDING', source badge='conversation' visible in orders list."""
    pytest.skip("Wave 0 stub — implement after 24-10-PLAN.md completes")


@pytest.mark.asyncio
async def test_manager_edit_logs_audit():
    """CORD-03: Manager edit of conversation order → audit row action='edited' with previous_state snapshot."""
    pytest.skip("Wave 0 stub — implement after 24-10-PLAN.md completes")


@pytest.mark.asyncio
async def test_manager_delete_requires_reason():
    """CORD-04: Manager delete without reason annotation → rejected; with reason → audit row action='cancelled'."""
    pytest.skip("Wave 0 stub — implement after 24-10-PLAN.md completes")


@pytest.mark.asyncio
async def test_delete_triggers_draft_message():
    """CORD-05: After cancellation, agent optionally drafts provider message for manager approval."""
    pytest.skip("Wave 0 stub — implement after 24-10-PLAN.md completes")


@pytest.mark.asyncio
async def test_manager_agreement_same_flow():
    """CORD-06: Manager-typed AGREEMENT detected identically to agent-detected — same ticket creation flow."""
    pytest.skip("Wave 0 stub — implement after 24-10-PLAN.md completes")


@pytest.mark.asyncio
async def test_audit_table_append_only():
    """CORD-07: Every state change creates new audit row; no UPDATE on existing audit rows."""
    pytest.skip("Wave 0 stub — implement after 24-10-PLAN.md completes")
```

**tests/test_sensitivity_detection.py** — stubs for ETHC-01..07:
```python
"""Phase 24 test stubs for per-segment sensitivity detection — ETHC-01..07."""
import pytest

try:
    from agents.provider_conversation_agent import ProviderConversationAgent
except ImportError:
    ProviderConversationAgent = None


@pytest.mark.asyncio
async def test_health_topic_pauses():
    """ETHC-01: Message with health disclosure → extracted=false, sensitive=true tag applied."""
    pytest.skip("Wave 0 stub — implement after 24-05-PLAN.md completes")


@pytest.mark.asyncio
async def test_extraction_resumes_after_topic_shift():
    """ETHC-01: After sensitive segment, business message → extraction=true resumes automatically."""
    pytest.skip("Wave 0 stub — implement after 24-05-PLAN.md completes")


@pytest.mark.asyncio
async def test_sensitive_count_in_summary():
    """ETHC-02: Post-session summary includes 'N messages not analyzed — sensitive content'."""
    pytest.skip("Wave 0 stub — implement after 24-05-PLAN.md completes")


@pytest.mark.asyncio
async def test_close_relationship_softer_tone():
    """ETHC-04: providers.close_relationship=true → softer negotiation posture in draft messages."""
    pytest.skip("Wave 0 stub — implement after 24-05-PLAN.md completes")


@pytest.mark.asyncio
async def test_manager_instruction_passthrough():
    """ETHC-05: conversation_context.manager_instructions carried into conversation at appropriate moment."""
    pytest.skip("Wave 0 stub — implement after 24-05-PLAN.md completes")


@pytest.mark.asyncio
async def test_ai_attribution_logged():
    """ETHC-06: Sent message logs disclosure_choice per message; attribution always visible in-app."""
    pytest.skip("Wave 0 stub — implement after 24-05-PLAN.md completes")
```

**tests/test_conversation_embeddings.py** — stubs for SRCH-01..05:
```python
"""Phase 24 test stubs for embedding pipeline and vector search — SRCH-01..05."""
import pytest

try:
    from services.embedding_service import EmbeddingService
except ImportError:
    EmbeddingService = None


@pytest.mark.asyncio
async def test_signal_message_embedded():
    """SRCH-01: Message with has_signal=true → embedding row inserted to conversation_embeddings."""
    pytest.skip("Wave 0 stub — implement after 24-08-PLAN.md completes")


@pytest.mark.asyncio
async def test_noise_message_not_embedded():
    """SRCH-01: Message with has_signal=false → no embedding row created."""
    pytest.skip("Wave 0 stub — implement after 24-08-PLAN.md completes")


@pytest.mark.asyncio
async def test_no_embed_sensitive():
    """SRCH-01, ETHC-01, T-PII-leakage: Message with sensitive=true → no embedding even if has_signal=true."""
    pytest.skip("Wave 0 stub — implement after 24-08-PLAN.md completes")


@pytest.mark.asyncio
async def test_search_returns_ranked_results():
    """SRCH-04: NL query → match_conversation_embeddings RPC → results ranked by similarity score."""
    pytest.skip("Wave 0 stub — implement after 24-08-PLAN.md completes")


@pytest.mark.asyncio
async def test_agent_search_separate_from_manager_search():
    """SRCH-05: Agent uses internal route; manager uses /api/v1/conversations/search endpoint."""
    pytest.skip("Wave 0 stub — implement after 24-08-PLAN.md completes")


@pytest.mark.asyncio
async def test_embedding_is_nonblocking():
    """MODL-04: Embedding call wrapped in asyncio.create_task — does not block message processing."""
    pytest.skip("Wave 0 stub — implement after 24-08-PLAN.md completes")
```
  </action>
  <acceptance_criteria>
    - `pytest tests/test_conversation_to_order.py --collect-only -q 2>&1 | grep "error"` — no errors (empty output expected)
    - `pytest tests/test_sensitivity_detection.py --collect-only -q 2>&1 | grep "error"` — no errors
    - `pytest tests/test_conversation_embeddings.py --collect-only -q 2>&1 | grep "error"` — no errors
    - `grep -c "def test_" services/agent-orchestrator/tests/test_conversation_to_order.py` returns 7
    - `grep -c "def test_" services/agent-orchestrator/tests/test_sensitivity_detection.py` returns 6
    - `grep -c "def test_no_embed_sensitive" services/agent-orchestrator/tests/test_conversation_embeddings.py` returns 1 (VALIDATION.md exact name match)
    - `grep -c "def test_health_topic_pauses" services/agent-orchestrator/tests/test_sensitivity_detection.py` returns 1 (VALIDATION.md exact name match)
    - `grep -c "def test_agreement_creates_order" services/agent-orchestrator/tests/test_conversation_to_order.py` returns 1 (VALIDATION.md exact name match)
  </acceptance_criteria>
  <verify>
    <automated>cd services/agent-orchestrator && pytest tests/test_conversation_to_order.py tests/test_sensitivity_detection.py tests/test_conversation_embeddings.py -v --collect-only 2>&1 | tail -10</automated>
  </verify>
  <done>All 7 test stub files exist; full collection of all 7 files exits 0; all tests show SKIPPED when run; exact function names from VALIDATION.md are present</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test stubs → production imports | try/except import pattern prevents test collection failures from crashing CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | Tampering | test_no_embed_sensitive | mitigate | Test explicitly verifies sensitive=true messages are NOT stored in conversation_embeddings; enforces ASVS V4.1 data minimization |
| T-24-02-02 | Information Disclosure | test stub imports | accept | Stubs use try/except ImportError — class not found doesn't expose internal state, just skips |
</threat_model>

<verification>
1. `cd services/agent-orchestrator && pytest tests/test_email_intel_agent.py tests/test_flag_extractor.py tests/test_negotiation_facts.py tests/test_model_tiers.py tests/test_conversation_to_order.py tests/test_sensitivity_detection.py tests/test_conversation_embeddings.py -v 2>&1 | tail -5` — all SKIPPED, 0 failed, 0 errors
2. All 7 files exist under `services/agent-orchestrator/tests/`
</verification>

<success_criteria>
- All 7 test stub files created under `services/agent-orchestrator/tests/`
- `pytest tests/ --collect-only -q` collects all 7 new files with 0 errors
- All tests show as SKIPPED (not FAILED) when run
- Exact test function names from VALIDATION.md per-task verification map are present
- `test_no_embed_sensitive`, `test_health_topic_pauses`, `test_agreement_creates_order` all exist with correct names
</success_criteria>

<output>
After completion, create `.planning/phases/24-provider-communication-pipeline-email-intelligence/24-02-SUMMARY.md`
</output>

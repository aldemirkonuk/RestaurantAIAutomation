---
phase: 14-comprehensive-e2e-testing-error-resilience
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/e2e/studio-flow.spec.ts
  - apps/web/e2e/navigation.spec.ts
  - apps/web/e2e/auth.setup.ts
  - apps/web/playwright.config.ts
autonomous: false
requirements: [E2E-08]
must_haves:
  truths:
    - "npx playwright test runs all frontend E2E tests"
    - "Studio flow test navigates from login through /studio page load"
    - "Navigation test confirms all protected routes redirect unauthenticated users to /login"
    - "Error state test confirms 401/403 scenarios show appropriate UI feedback"
  artifacts:
    - path: "apps/web/e2e/studio-flow.spec.ts"
      provides: "Studio page Playwright test"
      min_lines: 40
    - path: "apps/web/e2e/navigation.spec.ts"
      provides: "Route navigation + auth guard tests"
      min_lines: 50
    - path: "apps/web/e2e/auth.setup.ts"
      provides: "Auth helper for Playwright tests"
      min_lines: 15
    - path: "apps/web/playwright.config.ts"
      provides: "Updated Playwright config with projects and auth setup"
      min_lines: 20
  key_links:
    - from: "e2e/studio-flow.spec.ts"
      to: "/studio"
      via: "page.goto"
      pattern: "page.goto.*studio"
    - from: "e2e/navigation.spec.ts"
      to: "/login"
      via: "page.goto + expect redirect"
      pattern: "waitForURL.*login"
---

<objective>
Create frontend Playwright E2E tests covering Studio flow, navigation guards, and error states.

Purpose: Validate that the React frontend (Studio, Login, navigation, protected routes) works end-to-end in a browser context. Catches routing regressions, auth guard failures, and component rendering issues.

Output: 3 Playwright test files + updated config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-comprehensive-e2e-testing-error-resilience/14-CONTEXT.md

@apps/web/src/App.tsx
@apps/web/e2e/smoke.spec.ts
@apps/web/playwright.config.ts
@apps/web/src/pages/Login.tsx
@apps/web/src/pages/studio/Studio.tsx
@apps/web/src/components/ProtectedRoute.tsx

<interfaces>
From apps/web/src/App.tsx:
  Public routes: /login, /register, /onboarding
  Studio routes: /studio (dev/cc/admin), /studio/queue (dev/admin), /studio/certify (dev/admin)
  Protected routes (DashboardLayout): /, /inventory, /orders, /wines, /reports,
    /providers, /calendar, /communications, /notifications, /settings, /sommelier, /admin
  Catch-all: /* → redirect to /

From apps/web/e2e/smoke.spec.ts:
  Existing pattern: page.goto('/login'), expect heading 'WineOps AI', expect label 'Email Address'

From apps/web/playwright.config.ts:
  testDir: './e2e'
  baseURL: process.env.E2E_BASE_URL || 'http://127.0.0.1:5173'
  webServer: { command: 'pnpm dev --host 127.0.0.1 --port 5173' }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright config update + auth setup + Studio flow test</name>
  <files>
    apps/web/playwright.config.ts
    apps/web/e2e/auth.setup.ts
    apps/web/e2e/studio-flow.spec.ts
  </files>
  <action>
Update `apps/web/playwright.config.ts`:
- Keep existing config but add `projects` array for organized test execution:
  ```
  projects: [
    { name: 'smoke', testMatch: 'smoke.spec.ts' },
    { name: 'e2e', testMatch: /^(?!smoke).*\.spec\.ts$/ },
  ]
  ```
- Add `outputDir: 'test-results'` for artifact storage.
- Keep the existing webServer block unchanged.

Create `apps/web/e2e/auth.setup.ts`:
- Export a helper function `mockAuthState()` that returns a storageState-compatible object with:
  - localStorage entry for Supabase auth token (key: `sb-{project-ref}-auth-token`, value: JSON with fake JWT `access_token`, `refresh_token`, `user` object with email + id + app_metadata containing roles)
- This allows Playwright tests to bypass login by injecting auth state directly.
- Export `MOCK_USER` constant with test user info.
- Note: since the app uses Supabase auth and the backend may not be running, these tests focus on frontend rendering and routing behavior, not live API integration.

Create `apps/web/e2e/studio-flow.spec.ts`:

**Test 1: `test('login page renders correctly')`**
- Navigate to /login
- Assert: heading "WineOps AI" visible (matches existing smoke test pattern)
- Assert: email input, password input, and submit button visible

**Test 2: `test('unauthenticated user redirected from /studio to /login')`**
- Navigate to /studio (no auth state)
- Assert: redirected to /login (page.waitForURL includes 'login'))

**Test 3: `test('/studio page renders with auth state')`**
- Use `page.context().addInitScript()` or `page.evaluate()` to inject mock auth state (localStorage with Supabase token containing developer role in app_metadata)
- Navigate to /studio
- Note: This test validates the ProtectedRoute guard doesn't block authenticated users with correct roles. The actual Studio component may show a loading state or error if backend is unreachable — assert either the Studio layout renders (nav tabs visible) OR a network error state appears (both are valid behaviors when backend is down).
- Assert: page does NOT redirect to /login

**Test 4: `test('/studio/queue requires elevated role')`**
- Inject auth state with certified_contributor role only (not developer/admin)
- Navigate to /studio/queue
- Assert: either redirected away or shows access denied (ProtectedRoute gate)

Per D-06: mock auth via localStorage injection, no live Supabase needed.
  </action>
  <verify>
    <automated>cd apps/web && npx playwright test studio-flow.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>4 Playwright tests for Studio flow pass. Login renders, auth redirects work, Studio loads with auth, queue blocks non-admin roles.</done>
</task>

<task type="auto">
  <name>Task 2: Navigation + error state Playwright tests</name>
  <files>apps/web/e2e/navigation.spec.ts</files>
  <action>
Create `apps/web/e2e/navigation.spec.ts`:

**Test 1: `test('all protected routes redirect unauthenticated to /login')`**
- For each protected route (/, /inventory, /orders, /wines, /reports, /settings):
  - Navigate to route (no auth)
  - Assert: redirected to /login
  - This catches any route that accidentally loses its ProtectedRoute wrapper

**Test 2: `test('public routes accessible without auth')`**
- Navigate to /login → renders (no redirect)
- Navigate to /register → renders (no redirect)

**Test 3: `test('unknown route redirects to /')`**
- Navigate to /this-does-not-exist
- Assert: redirected to / (App.tsx catch-all: `<Navigate to="/" replace />`)
- Since / is protected, ultimately should land on /login for unauthenticated user

**Test 4: `test('studio nav tabs reachable when authenticated')`**
- Inject auth state with developer role
- Navigate to /studio
- If the StudioLayout renders (check for nav element or specific text), check that links to /studio/queue and /studio/certify exist in the DOM
- This validates the Studio page provides navigation to its sub-pages

**Test 5: `test('register page has form fields')`**
- Navigate to /register
- Assert: heading or form fields visible (email, password inputs exist)
- This ensures the register route is working independently of auth
  </action>
  <verify>
    <automated>cd apps/web && npx playwright test navigation.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>5 navigation tests pass. All protected routes redirect. Public routes accessible. Unknown routes caught. Studio nav tabs present for authenticated users.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Playwright E2E tests in browser</name>
  <what-built>9 Playwright frontend E2E tests covering login, Studio flow, navigation guards, auth redirects, and error states. Tests run against the Vite dev server.</what-built>
  <action>Human verifies Playwright test results in browser.</action>
  <how-to-verify>
    1. Run `cd apps/web && npx playwright test --reporter=html`
    2. Open the HTML report: `npx playwright show-report`
    3. Verify all 9 tests pass (4 studio-flow + 5 navigation)
    4. Check that the existing smoke.spec.ts still passes
    5. Confirm: unauthenticated /studio redirects to /login
    6. Confirm: authenticated /studio shows Studio layout (not login redirect)
  </how-to-verify>
  <verify>Human confirms all tests pass in Playwright HTML report</verify>
  <done>All 9 Playwright tests passing, HTML report reviewed and approved</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → dev server | Playwright tests run against local Vite dev server, no production traffic |
| mock auth tokens | Injected via localStorage, never sent to real Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Spoofing | auth.setup.ts | accept | Mock tokens are for local testing only; no production Supabase project ref used |
| T-14-06 | Information Disclosure | test-results/ | mitigate | .gitignore already excludes test-results/ — verify; Playwright traces contain only local dev screenshots |
</threat_model>

<verification>
1. `cd apps/web && npx playwright test` — all 9+ tests pass (existing smoke + new tests)
2. HTML report generated at apps/web/playwright-report/
3. Auth guard tests confirm redirect behavior for unauthenticated users
4. Studio flow test confirms authenticated access works
</verification>

<success_criteria>
- 9 new Playwright tests pass
- Existing smoke.spec.ts still passes (no regression)
- Studio flow: login → /studio renders → nav tabs visible
- Protected routes: all redirect unauthenticated to /login
- Tests run without live backend (mock auth, frontend-only validation)
</success_criteria>

<output>
After completion, create `.planning/phases/14-comprehensive-e2e-testing-error-resilience/14-03-SUMMARY.md`
</output>

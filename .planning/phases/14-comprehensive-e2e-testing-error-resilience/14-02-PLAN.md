---
phase: 14-comprehensive-e2e-testing-error-resilience
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - services/agent-orchestrator/tests/e2e/test_studio_pipeline.py
  - services/agent-orchestrator/tests/e2e/test_api_endpoints.py
autonomous: true
requirements: [E2E-04, E2E-05, E2E-06, E2E-07]
must_haves:
  truths:
    - "Studio override E2E covers session create → override submit → auto_promoted + pending paths"
    - "Approval queue E2E covers contributor pending → admin approve → field applied"
    - "Quality review E2E covers GET review-queue → PATCH corrections → promotion to library"
    - "Research + Analytics API tests cover all GET endpoints with mock data"
  artifacts:
    - path: "services/agent-orchestrator/tests/e2e/test_studio_pipeline.py"
      provides: "Studio override + approval queue E2E"
      min_lines: 150
    - path: "services/agent-orchestrator/tests/e2e/test_api_endpoints.py"
      provides: "Quality, Research, Analytics API E2E"
      min_lines: 120
  key_links:
    - from: "tests/e2e/test_studio_pipeline.py"
      to: "/api/v1/studio/overrides"
      via: "TestClient POST"
      pattern: "test_client.post.*studio/overrides"
    - from: "tests/e2e/test_api_endpoints.py"
      to: "/api/v1/quality/review-queue"
      via: "TestClient GET/PATCH"
      pattern: "test_client\\.(get|patch).*quality/review-queue"
---

<objective>
Cover the Studio/Override/Quality/Research/Analytics pipelines with E2E tests.

Purpose: Validate the Phase 13 Studio override flow, Phase 5/7 quality review pipeline, Phase 12 research metrics, and Phase 10/11 analytics endpoints. These are the remaining 4 routers that Plan 01 didn't cover.

Output: 2 test files covering all remaining backend E2E scenarios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-comprehensive-e2e-testing-error-resilience/14-CONTEXT.md
@.planning/phases/14-comprehensive-e2e-testing-error-resilience/14-01-SUMMARY.md

@services/agent-orchestrator/api/studio_routes.py
@services/agent-orchestrator/api/quality_routes.py
@services/agent-orchestrator/api/research_routes.py
@services/agent-orchestrator/api/analytics_routes.py
@services/agent-orchestrator/tests/e2e/conftest_e2e.py

<interfaces>
From services/agent-orchestrator/api/studio_routes.py:
  POST   /api/v1/studio/sessions                    — SessionCreateRequest {source_type, source_ref?, scan_session_id?}
  GET    /api/v1/studio/sessions/{id}                — session timeline + override_events
  POST   /api/v1/studio/overrides                    — OverrideRequest {session_id, submission_id, field_name, new_value, reason?, citation_url?, citation_snippet?}
  GET    /api/v1/studio/queue                        — pending overrides (dev/admin only)
  PATCH  /api/v1/studio/queue/{override_id}          — ApprovalDecision {decision: "approved"|"rejected", note?}
  POST   /api/v1/studio/invite                       — InviteRequest {role, target_email}
  POST   /api/v1/studio/invite/redeem                — RedeemRequest {token}
  GET    /api/v1/studio/metrics                      — KPIs (total_overrides, pending, acceptance_rate, etc.)
  GET    /api/v1/studio/me/roles                     — current user's studio roles (no role restriction)
  GET    /api/v1/studio/contributors                 — list certified_contributors (dev/admin)
  PATCH  /api/v1/studio/contributors/{user_id}/revoke  — revoke contributor
  PATCH  /api/v1/studio/contributors/{user_id}/enable  — re-enable contributor
  PATCH  /api/v1/studio/contributors/{user_id}/disable — alias for revoke

Auth: require_studio_role() dependency → decodes JWT, checks user_roles table
  Promotion: developer/review_admin → auto_promoted; certified_contributor → pending (queue)

From services/agent-orchestrator/api/quality_routes.py:
  GET  /api/v1/quality/review-queue         — field-level pending reviews grouped by submission
  PATCH /api/v1/quality/review-queue/{id}   — ReviewQueuePatchRequest {corrections: {field: value}, approvals: [field], corrected_by?}
  GET  /api/v1/quality/calibration          — per-field thresholds + accuracy stats

From services/agent-orchestrator/api/research_routes.py:
  GET  /api/v1/research/metrics     — 5 metric categories (gap, quality, evidence, throughput, safety)
  GET  /api/v1/research/runs        — paginated run history
  GET  /api/v1/research/conflicts   — wines with conflict_candidates
  GET  /api/v1/research/challenges  — resolution_challenges by status
  POST /api/v1/research/trigger     — dispatch research_agent_task (requires X-Admin-Key)

From services/agent-orchestrator/api/analytics_routes.py:
  GET /api/v1/analytics/wine/{wine_id}/scores    — critic scores + markup
  GET /api/v1/analytics/trends                    — trending wines (metro, period)
  GET /api/v1/analytics/wine/{wine_id}/timeline  — wine lifecycle
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Studio override + approval queue E2E tests</name>
  <files>services/agent-orchestrator/tests/e2e/test_studio_pipeline.py</files>
  <action>
Create `tests/e2e/test_studio_pipeline.py` with all tests marked `@pytest.mark.e2e`.

Use the E2E fixtures from conftest_e2e.py (jwt_factory, e2e_settings, mock_supabase_tables). Follow the patching pattern from test_studio_e2e.py but using the new E2E infrastructure.

**Class TestStudioOverridePipeline:**

**Test 1: `test_developer_override_auto_promotes`**
- Build mock_supabase with:
  - onboarding_sessions: insert returns session row
  - master_wine_library_submissions: select returns submission with field_confidence `{"wine_name": {"value": "Old", "confidence": 0.92, "source": "visible"}}`
  - override_events: insert returns `{"id": "ov-001", "promotion_status": "auto_promoted"}`
- POST /studio/sessions with developer JWT → 200
- POST /studio/overrides with field_name="wine_name", new_value="Corrected Name", reason="Producer confirmed" → 200, status="auto_promoted"
- Assert override_events.insert was called with promotion_status="auto_promoted"

**Test 2: `test_developer_override_requires_reason_for_high_confidence`**
- Mock submission with wine_name confidence=0.92
- POST /studio/overrides with field_name="wine_name", new_value="X", reason=None → 422
- POST /studio/overrides with field_name="wine_name", new_value="X", reason="ab" (too short) → 422
- POST /studio/overrides with field_name="wine_name", new_value="X", reason="Verified on website" → 200

**Test 3: `test_contributor_override_lands_in_pending_queue`**
- Build mock with user_roles returning `[{"role": "certified_contributor", "promotion_policy": "queue"}]`
- POST /studio/overrides with contributor JWT → 200, status="pending"

**Test 4: `test_admin_approves_pending_override`**
- Build mock with override_events: select returns pending override row
- PATCH /studio/queue/{id} with admin JWT, decision="approved" → 200
- Assert override_events.update was called with promotion_status="approved"

**Test 5: `test_admin_rejects_pending_override`**
- Same setup but decision="rejected" → 200
- Assert promotion_status set to "rejected"

**Test 6: `test_already_decided_override_returns_409`**
- Mock override with promotion_status="approved" (not pending)
- PATCH /studio/queue/{id} → 409

**Test 7: `test_studio_metrics_returns_kpis`**
- Mock override_events and field_corrections tables with sample data
- GET /studio/metrics with developer JWT → 200
- Assert response contains: total_overrides, pending_queue, acceptance_rate, post_override_correction_rate

**Test 8: `test_invite_create_and_redeem_flow`**
- POST /studio/invite with admin JWT → 200, returns token
- Mock invite_tokens table: select by token returns valid, unused, unexpired row
- POST /studio/invite/redeem with contributor JWT → 200, role_granted

All patches: `config.settings.get_settings`, `api.studio_routes._get_supabase`, `api.studio_routes._get_user_studio_roles`, `api.studio_routes._apply_override_to_submission`, `api.studio_routes.check_and_update_trust`.
  </action>
  <verify>
    <automated>cd services/agent-orchestrator && python -m pytest tests/e2e/test_studio_pipeline.py -v --tb=short -x 2>&1 | tail -20</automated>
  </verify>
  <done>8 studio pipeline tests pass. Developer auto-promote path tested. Contributor pending path tested. Admin approve/reject tested. Metrics and invite flow tested.</done>
</task>

<task type="auto">
  <name>Task 2: Quality review + Research + Analytics API E2E tests</name>
  <files>services/agent-orchestrator/tests/e2e/test_api_endpoints.py</files>
  <action>
Create `tests/e2e/test_api_endpoints.py` with all tests marked `@pytest.mark.e2e`.

**Class TestQualityPipeline:**

**Test 1: `test_review_queue_returns_pending_fields_grouped_by_wine`**
- Mock field_review_queue with 3 pending rows (2 for submission A, 1 for submission B)
- Mock master_wine_library_submissions with payload/field_confidence context
- GET /quality/review-queue → 200
- Assert response has 2 grouped items, first with 2 pending_fields

**Test 2: `test_review_queue_patch_correction_promotes_to_library`**
- Mock submission with status="pending_review", field_confidence with region at 0.65
- Mock field_review_queue returning 0 remaining pending after correction
- Mock should_auto_block returning False
- PATCH /quality/review-queue/{id} with corrections={"region": "Piedmont"} → 200
- Assert response: promoted_to_library=True, status="approved"
- Assert master_wine_library.insert was called

**Test 3: `test_review_queue_patch_does_not_promote_when_fields_remain`**
- Mock field_review_queue returning 1 remaining pending
- PATCH → 200, promoted_to_library=False, status="pending_review"

**Test 4: `test_calibration_endpoint_returns_thresholds`**
- Mock confidence_thresholds and field_calibration tables
- GET /quality/calibration → 200
- Assert response contains thresholds list and calibration_stats list

**Class TestResearchAPI:**

**Test 5: `test_research_metrics_handles_empty_tables`**
- Mock all research tables returning empty data
- GET /research/metrics → 200
- Assert all metric values are 0 or 0.0 (graceful empty handling)

**Test 6: `test_research_metrics_computes_from_data`**
- Mock research_run_stats with 2 rows (fields_targeted=10, fields_filled=7)
- Mock evidence_citations with tier_A and tier_B entries
- GET /research/metrics → 200
- Assert promotion_rate = 0.7, source_tier_mix has A and B percentages

**Test 7: `test_research_trigger_requires_admin_key`**
- POST /research/trigger without X-Admin-Key → 401
- POST /research/trigger with wrong key → 401
- Set ADMIN_API_KEY env var, POST with correct key (mock research_runs and Celery task) → 200

**Test 8: `test_research_trigger_blocks_concurrent_runs`**
- Mock research_runs count with status='running' returning 1
- POST /research/trigger → 429

**Class TestAnalyticsAPI:**

**Test 9: `test_wine_scores_returns_critic_data`**
- Mock master_wine_library with critic_scores JSONB
- Mock restaurant_inventory with markup data
- GET /analytics/wine/{uuid}/scores → 200
- Assert critic_scores present, per_restaurant_markup populated

**Test 10: `test_wine_scores_invalid_uuid_returns_422`**
- GET /analytics/wine/not-a-uuid/scores → 422

**Test 11: `test_trends_returns_trending_wines`**
- Mock trending_wines table with 2 rows (one positive delta, one negative)
- Mock master_wine_library for wine names
- GET /analytics/trends?period=90d → 200
- Assert trending_up and trending_down lists populated

**Test 12: `test_wine_timeline_returns_lifecycle`**
- Mock master_wine_library, master_wine_library_submissions (signature_hash), wine_popularity, restaurant_wine_roster, menu_changes
- GET /analytics/wine/{uuid}/timeline → 200
- Assert first_seen_at, restaurants_currently_carrying, price_history populated

Patches for quality: `api.quality_routes._get_supabase`
Patches for research: `api.research_routes._get_supabase`, env var `ADMIN_API_KEY`, `jobs.research_tasks.research_agent_task`
Patches for analytics: `api.analytics_routes._get_supabase` (note: analytics creates its own client via `create_client`, so patch `supabase.create_client`)
  </action>
  <verify>
    <automated>cd services/agent-orchestrator && python -m pytest tests/e2e/test_api_endpoints.py -v --tb=short -x 2>&1 | tail -25</automated>
  </verify>
  <done>12 API endpoint tests pass. Quality review → promotion path tested. Research metrics with empty and populated data tested. Analytics wine scores, trends, and timeline tested. All 25+ HTTP endpoints have at least one E2E test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test JWT → studio auth | Tests create JWTs with test-only secret; no production token leakage |
| X-Admin-Key in tests | Test sets ADMIN_API_KEY via env var, not stored in code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Spoofing | test_studio_pipeline.py | accept | Test JWTs use "e2e-secret" — test-only, no production impact |
| T-14-04 | Information Disclosure | test_api_endpoints.py | mitigate | ADMIN_API_KEY set via os.environ in test, cleared in teardown |
</threat_model>

<verification>
1. `cd services/agent-orchestrator && python -m pytest tests/e2e/test_studio_pipeline.py tests/e2e/test_api_endpoints.py -v` — all 20 tests pass
2. Studio override flow: developer auto-promotes, contributor goes to queue, admin approves
3. Quality review: correction → promotion path, remaining pending blocks promotion
4. Research: empty table handling, metric computation, admin-key auth, concurrent run block
5. Analytics: UUID validation, critic scores, trends, timeline
</verification>

<success_criteria>
- 20 E2E tests pass (8 studio + 12 api)
- Every registered HTTP endpoint has at least one E2E test
- Studio override flow tested for all 3 role paths (developer, contributor, admin)
- Research trigger tested for auth (401) and concurrency (429)
</success_criteria>

<output>
After completion, create `.planning/phases/14-comprehensive-e2e-testing-error-resilience/14-02-SUMMARY.md`
</output>

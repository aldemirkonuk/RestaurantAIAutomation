---
phase: 37-synthetic-restaurant-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/__init__.py
  - scripts/synth/__init__.py
  - scripts/synth/__main__.py
  - scripts/synth/ids.py
  - scripts/synth/recipes.py
  - scripts/synth/snapshots.py
  - scripts/synth/write_set.py
  - datasets/sim/menus/fine-dining.json
  - datasets/sim/menus/bistro.json
  - datasets/sim/menus/high-volume-bar.json
  - datasets/sim/menus/cafe.json
  - datasets/sim/menus/turkish-clone.json
  - datasets/sim/archetypes/fine-dining.json
  - datasets/sim/archetypes/bistro.json
  - datasets/sim/archetypes/high-volume-bar.json
  - datasets/sim/archetypes/cafe.json
  - datasets/sim/archetypes/turkish-clone.json
  - datasets/sim/manifest.json
  - services/agent-orchestrator/tests/conftest.py
  - services/agent-orchestrator/tests/test_synth_recipes.py
  - services/agent-orchestrator/tests/test_synth_snapshots.py
  - services/agent-orchestrator/tests/test_synth_snapshot_schema.py
  - services/agent-orchestrator/tests/test_synth_archetypes_present.py
  - services/agent-orchestrator/tests/test_synth_write_set_gate.py
  - services/agent-orchestrator/tests/test_synth_seed_plan.py
  - services/agent-orchestrator/tests/test_synth_auth_personas.py
  - services/agent-orchestrator/tests/test_synth_oracle_schema.py
  - services/agent-orchestrator/tests/test_synth_atomic_seed.py
  - services/agent-orchestrator/tests/test_synth_teardown_safety.py
  - services/agent-orchestrator/tests/test_synth_cli_defaults.py
  - services/agent-orchestrator/tests/e2e/test_synth_role_isolation.py
autonomous: true
requirements:
  - SYNTH-01
  - SYNTH-02
  - SYNTH-05
user_setup: []

must_haves:
  truths:
    - "Generator loads ≥5 named archetype recipes with defaults for cuisine, size, wine_program_depth, sales_volume, price_tier, ordering_rhythm and applies CLI overrides"
    - "Generate/replay path loads frozen datasets/sim/menus/*.json only — no network, no Playwright, no crawl API calls"
    - "Refresh path exists to update snapshots via WebCrawlerService / JSONL bootstrap; generate never calls it"
    - "Sell prices come only from source snapshot fields (price_reference→bottle_price, price_glass→by_glass_price); null stays null; priced_sku_ratio < 0.9 ⇒ menu_quality=partial"
    - "Opening stock knobs live in per-archetype JSON (default/min/max/by_price_tier/by_primary_type/threshold_min) — not hardcoded magic"
    - "Restaurant IDs are UUID5; slugs are sim-{archetype_id}; never string PKs like sim-bistro as restaurants.id"
    - "All Wave 0 Nyquist test files exist; wave-1 tests green"
  artifacts:
    - path: "scripts/synth/recipes.py"
      provides: "Recipe load + override → RestaurantProfile"
      exports: ["load_recipe", "apply_overrides", "list_archetypes", "RestaurantProfile"]
    - path: "scripts/synth/snapshots.py"
      provides: "Replay + bootstrap + refresh entrypoints"
      exports: ["load_snapshot", "bootstrap_from_jsonl", "refresh_snapshot"]
    - path: "scripts/synth/ids.py"
      provides: "Deterministic UUID5 + sim slug helpers"
      exports: ["sim_restaurant_id", "sim_slug", "sim_org_id", "SIM_NS"]
    - path: "scripts/synth/write_set.py"
      provides: "SYNTH_WRITE_SET / TEARDOWN_TABLES constants + gate helper (stub complete enough for later plans)"
      exports: ["SYNTH_WRITE_SET", "TEARDOWN_TABLES", "assert_write_set_equals_teardown"]
    - path: "datasets/sim/manifest.json"
      provides: "Pack version + sha256 per snapshot"
      contains: "archetypes"
    - path: "datasets/sim/archetypes/bistro.json"
      provides: "Named recipe + opening_stock + snapshot ref"
      contains: "opening_stock"
  key_links:
    - from: "scripts/synth/recipes.py"
      to: "datasets/sim/archetypes/*.json"
      via: "load_recipe(archetype_id)"
      pattern: "archetypes|load_recipe|opening_stock"
    - from: "scripts/synth/snapshots.py"
      to: "datasets/sim/menus/*.json"
      via: "load_snapshot — file read only on generate"
      pattern: "load_snapshot|datasets/sim/menus"
    - from: "scripts/synth/ids.py"
      to: "uuid.uuid5"
      via: "sim_restaurant_id(archetype_id)"
      pattern: "uuid5|sim\\.restaurant\\."
    - from: "services/agent-orchestrator/tests/test_synth_*.py"
      to: "scripts.synth"
      via: "repo-root on sys.path"
      pattern: "scripts\\.synth|REPO_ROOT|sim-"
---

<objective>
Ship the synthetic restaurant factory’s **data plane**: Wave 0 Nyquist scaffolds, deterministic UUID/slug helpers, ≥5 archetype recipes with configurable opening stock, and SOTA hybrid menu snapshots under `datasets/sim/` that generate always replays (refresh is explicit).

Purpose: Unlock SYNTH-01/02/05 so later waves can seed cloud tenants from real SKU diversity without live crawls in CI.
Output: `scripts/synth/{ids,recipes,snapshots,write_set}.py`, `datasets/sim/**`, all `test_synth_*.py` Wave 0 files, wave-1 tests green.

Note (W1 scope): `files_modified` is large because Wave 0 scaffolds ~12 test files + 5×2 dataset JSONs in one plan — intentional Nyquist continuity; most Task-1 files are thin scaffolds, not deep impl. Do not split unless execution context overflows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-synthetic-restaurant-engine/37-CONTEXT.md
@.planning/phases/37-synthetic-restaurant-engine/37-RESEARCH.md
@.planning/phases/37-synthetic-restaurant-engine/37-PATTERNS.md
@.planning/phases/37-synthetic-restaurant-engine/37-VALIDATION.md
@.planning/testing/SYNTHETIC-TENANT.md
@scripts/e2e_restaurants.json
@scripts/seed_database.py
@services/agent-orchestrator/services/web_crawler.py
@scripts/e2e_crawl_harness.py

<interfaces>
<!-- Locked URL→archetype mapping (D-06 discretion = RESEARCH defaults). Skip parameter-skin variants. -->
fine-dining      ← BLVD Steakhouse      ← datasets/restaurant_menus/20260406_blvd_steakhouse.jsonl
bistro           ← The Tailors Son      ← datasets/restaurant_menus/20260406_the_tailors_son.jsonl
high-volume-bar  ← Chicago Winery       ← datasets/restaurant_menus/20260406_chicago_winery.jsonl
cafe             ← The Albert Chicago   ← datasets/restaurant_menus/20260406_the_albert_chicago.jsonl
turkish-clone    ← Avli Taverna Lincoln Park (PDF/images under datasets/annotation_*); bootstrap via refresh/Vision or best-effort extract; menu_quality=partial OK (D-03)

<!-- ID helpers (live restaurants.id is UUID — never string PK): -->
SIM_NS = uuid.UUID("6ba7b810-9dad-11d1-80b4-00c04fd430c8")
sim_restaurant_id(archetype_id) → str(uuid5(SIM_NS, f"sim.restaurant.{archetype_id}"))
sim_slug(archetype_id) → f"sim-{archetype_id}"
sim_org_id(archetype_id) → str(uuid5(SIM_NS, f"sim.org.{archetype_id}"))

<!-- Snapshot envelope: -->
{ archetype_id, source_url, source_name, extracted_at, extraction_model, menu_quality: full|partial,
  items: [{ wine_name, producer, vintage, bottle_price, by_glass_price, signature_hash, primary_type, country, region, grape_variety }] }
Map JSONL price_reference→bottle_price, price_glass→by_glass_price. Never invent prices (D-04).

<!-- Recipe shape: see 37-RESEARCH.md "Archetype recipe shape" — defaults + opening_stock + restaurant + snapshot path. -->

<!-- write_set stub (full gate behavior in 37-03; constant list must match RESEARCH): -->
SYNTH_WRITE_SET = TEARDOWN_TABLES = [
  organizations, organization_members, restaurants, users,
  user_restaurant_access, restaurant_menus, menu_items, restaurant_inventory,
  master_wine_library, master_wine_library_submissions,
  sim_ground_truth_facts, sim_ground_truth_runs
]
# Library tables ALWAYS in write-set (seed always inserts provisional sim wines with source=sim / uuid5 sim.wine.*).
# users / Auth: seed upserts shared SIM_* personas; teardown handler must NO-OP delete of SIM_* users (D-13/D-17)
# Never include inventory_stock (absent in live cloud)

<!-- pytest import path: monorepo root must be on sys.path so `import scripts.synth` works from agent-orchestrator tests. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 Nyquist scaffolds + package skeleton</name>
  <files>scripts/__init__.py, scripts/synth/__init__.py, scripts/synth/__main__.py, scripts/synth/write_set.py, scripts/synth/ids.py, services/agent-orchestrator/tests/conftest.py, services/agent-orchestrator/tests/test_synth_recipes.py, services/agent-orchestrator/tests/test_synth_snapshots.py, services/agent-orchestrator/tests/test_synth_snapshot_schema.py, services/agent-orchestrator/tests/test_synth_archetypes_present.py, services/agent-orchestrator/tests/test_synth_write_set_gate.py, services/agent-orchestrator/tests/test_synth_seed_plan.py, services/agent-orchestrator/tests/test_synth_auth_personas.py, services/agent-orchestrator/tests/test_synth_oracle_schema.py, services/agent-orchestrator/tests/test_synth_atomic_seed.py, services/agent-orchestrator/tests/test_synth_teardown_safety.py, services/agent-orchestrator/tests/test_synth_cli_defaults.py, services/agent-orchestrator/tests/e2e/test_synth_role_isolation.py</files>
  <behavior>
    - test_synth_write_set_gate: SYNTH_WRITE_SET == TEARDOWN_TABLES; assert_write_set_equals_teardown raises on drift (may pass once stub constants equal)
    - test_synth_recipes / snapshots / snapshot_schema / archetypes_present: RED until Tasks 2–3 land (pytest.fail or ImportError ok only until modules exist — prefer failing asserts on missing archetypes/snapshots)
    - Remaining wave-2/3 test files: create with pytest.skip("Wave N — implemented in 37-0N") OR minimal failing stubs that do not break `pytest tests/test_synth_*.py` collection; mark prod_e2e role isolation with `@pytest.mark.prod_e2e`
    - conftest: insert monorepo REPO_ROOT (parent of services/) onto sys.path before imports
  </behavior>
  <action>
    Create `scripts/` as an importable package (`__init__.py`) and `scripts/synth/` package with `__main__.py` stub (`if __name__` → print "synth CLI lands in 37-03" or empty argparse placeholder — **do not** implement full CLI here).

    Implement `ids.py` fully (sim_restaurant_id, sim_slug, sim_org_id, SIM_NS) per D-UUID strategy / RESEARCH Pattern 2.

    Implement `write_set.py` with the exact SYNTH_WRITE_SET / TEARDOWN_TABLES lists from RESEARCH (equal sets) + `assert_write_set_equals_teardown()` that raises `WriteSetTeardownMismatchError` on inequality. **Always** include `master_wine_library` and `master_wine_library_submissions` (seed always writes provisional sim wines — not optional). Document that `users` teardown is a durable-persona no-op (Auth users never deleted) and library teardown (37-03) is sim-filtered only. **Do not** put `inventory_stock` in the list.

    Create every Wave 0 file listed in `37-VALIDATION.md` §Wave 0 Requirements. Wave-1 tests must be real failing/passing tests targeting recipes/snapshots/archetypes/write_set. Wave-2/3 files may skip until those plans.

    Update `services/agent-orchestrator/tests/conftest.py` to add monorepo root to `sys.path` (keep existing orchestrator root path).

    Per D-01..D-07 foundation only — no seed, no Nest SimPOS, no cloud `--apply`.
  </action>
  <verify>
    <automated>cd services/agent-orchestrator && pytest tests/test_synth_write_set_gate.py -q --tb=line 2>&1 | tail -20</automated>
  </verify>
  <done>All Wave 0 test files exist; `scripts.synth.ids` and `write_set` importable; write_set gate test green; monorepo root on pytest path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Archetype recipes + opening stock + profile overrides</name>
  <files>scripts/synth/recipes.py, datasets/sim/archetypes/fine-dining.json, datasets/sim/archetypes/bistro.json, datasets/sim/archetypes/high-volume-bar.json, datasets/sim/archetypes/cafe.json, datasets/sim/archetypes/turkish-clone.json, services/agent-orchestrator/tests/test_synth_recipes.py, services/agent-orchestrator/tests/test_synth_archetypes_present.py</files>
  <behavior>
    - load_recipe("bistro") returns defaults for cuisine, size, wine_program_depth, sales_volume, price_tier, ordering_rhythm plus opening_stock + snapshot path (SYNTH-01)
    - apply_overrides merges knobs without mutating the on-disk recipe file
    - list_archetypes() returns exactly the five ids: fine-dining, bistro, high-volume-bar, cafe, turkish-clone (SYNTH-05)
    - unknown archetype_id raises clear error
    - each archetype JSON contains configurable opening_stock (D-07) — not empty
  </behavior>
  <action>
    Implement `recipes.py` with Pydantic (or dataclass+validation) `RestaurantProfile` / recipe loader. Prefer **JSON** recipes (no new PyYAML dep) under `datasets/sim/archetypes/<id>.json`.

    Ship five named presets per D-05 / RESEARCH mapping. Recipe fields must include:
    - `archetype_id`, `display_name`, `snapshot` path, `defaults` (six SYNTH-01 knobs), `opening_stock` (D-07: default_bottles, min_bottles, max_bottles, by_price_tier, by_primary_type, threshold_min), `restaurant` (name, timezone, city, country).

    Discretion locked in this plan: **no parameter-skin variants** in v1 — only the five named recipes.

    Make `test_synth_recipes.py` and `test_synth_archetypes_present.py` green (Nyquist map 37-01-01, 37-01-04).
  </action>
  <verify>
    <automated>cd services/agent-orchestrator && pytest tests/test_synth_recipes.py tests/test_synth_archetypes_present.py -q --tb=line 2>&1 | tail -30</automated>
  </verify>
  <done>Five archetype recipes on disk; overrides work; SYNTH-01/05 unit verifies green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Frozen menu snapshots + replay/refresh split</name>
  <files>scripts/synth/snapshots.py, datasets/sim/menus/fine-dining.json, datasets/sim/menus/bistro.json, datasets/sim/menus/high-volume-bar.json, datasets/sim/menus/cafe.json, datasets/sim/menus/turkish-clone.json, datasets/sim/manifest.json, services/agent-orchestrator/tests/test_synth_snapshots.py, services/agent-orchestrator/tests/test_synth_snapshot_schema.py</files>
  <behavior>
    - load_snapshot(archetype_id) reads only datasets/sim/menus/*.json (monkeypatch proves no httpx/playwright/crawl call) — SYNTH-02 / D-01
    - bootstrap_from_jsonl converts Phase 2 JSONL → snapshot envelope; prices mapped not invented; menu_quality full|partial by priced ratio &lt; 0.9 (D-03, D-04)
    - refresh_snapshot is a separate entrypoint (may call WebCrawlerService when keys present); generate path never invokes it
    - snapshot schema test validates required keys + items[] shape for all five menus
    - manifest.json lists sha256 for each menu file
  </behavior>
  <action>
    Implement `snapshots.py`:
    1. `bootstrap_from_jsonl` for fine-dining/bistro/high-volume-bar/cafe from the RESEARCH JSONL paths (D-02).
    2. `turkish-clone`: best-effort bootstrap — prefer running Vision/PDF extract via refresh against Avli assets (`datasets/annotation_inbox/.../Avli_Taverna_Lincoln_Park_Wine_Menu.pdf` or annotation images) when crawl keys exist; otherwise commit a schema-valid snapshot with real extracted SKUs and `menu_quality=partial` (D-03). **Never invent sell prices** (D-04). Empty items[] is not acceptable for SYNTH-05 — at least one real SKU name from Avli assets must appear.
    3. `load_snapshot` — pure file replay.
    4. `refresh_snapshot(archetype_id)` — explicit SOTA path; updates `datasets/sim/menus/<id>.json` + manifest hash.

    Write `datasets/sim/manifest.json` with pack_version + per-file sha256.

    Green: `test_synth_snapshots.py`, `test_synth_snapshot_schema.py` (Nyquist 37-01-02, 37-01-03).
  </action>
  <verify>
    <automated>cd services/agent-orchestrator && pytest tests/test_synth_snapshots.py tests/test_synth_snapshot_schema.py tests/test_synth_recipes.py tests/test_synth_archetypes_present.py tests/test_synth_write_set_gate.py -q --tb=line 2>&1 | tail -40</automated>
  </verify>
  <done>Five frozen snapshots + manifest committed; generate path is offline-only; wave-1 Nyquist suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator laptop → `datasets/sim/` | Snapshot refresh may call external crawl/Vision APIs; generate must not |
| Committed snapshots → CI | Frozen JSON is the trust root for deterministic SKUs/prices |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01-01 | Tampering | snapshots.py generate path | mitigate | Unit test monkeypatches network; fail if crawl invoked during load_snapshot |
| T-37-01-02 | Information Disclosure | refresh logs | mitigate | Never log API keys; refresh errors redact secrets |
| T-37-01-03 | Spoofing | invented menu prices | mitigate | D-04: copy source prices only; schema test allows null; partial quality flag |
| T-37-01-04 | Tampering | string restaurant PKs | mitigate | ids.py UUID5 only; tests assert UUID format + sim- slug |
</threat_model>

<verification>
- `pytest tests/test_synth_recipes.py tests/test_synth_snapshots.py tests/test_synth_snapshot_schema.py tests/test_synth_archetypes_present.py tests/test_synth_write_set_gate.py -q` green
- `ls datasets/sim/menus/*.json datasets/sim/archetypes/*.json` shows 5 each + manifest.json
- Grep gate: `rg -n "inventory_stock" scripts/synth/write_set.py` → no matches in active list (comments OK if filtered)
</verification>

<success_criteria>
- SYNTH-01: parameterized profile from recipe + overrides
- SYNTH-02: replay frozen snapshots; refresh separate
- SYNTH-05: ≥5 distinct archetypes with mapped snapshots
- Wave 0 Nyquist files present for entire phase
</success_criteria>

<output>
After completion, create `.planning/phases/37-synthetic-restaurant-engine/37-01-SUMMARY.md`
</output>

---
phase: 37-synthetic-restaurant-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 37-01
  - 37-02
files_modified:
  - scripts/synth/cli.py
  - scripts/synth/__main__.py
  - scripts/synth/teardown.py
  - scripts/synth/write_set.py
  - scripts/synth/seed.py
  - package.json
  - services/agent-orchestrator/api/synth_routes.py
  - services/agent-orchestrator/main.py
  - services/agent-orchestrator/tests/e2e/conftest_prod.py
  - services/agent-orchestrator/tests/test_synth_write_set_gate.py
  - services/agent-orchestrator/tests/test_synth_teardown_safety.py
  - services/agent-orchestrator/tests/test_synth_cli_defaults.py
  - services/agent-orchestrator/tests/e2e/test_synth_role_isolation.py
  - services/agent-orchestrator/tests/test_synth_routes.py
  - .planning/testing/SYNTHETIC-TENANT.md
autonomous: false
requirements:
  - SYNTH-03
  - SYNTH-05
user_setup:
  - service: supabase
    why: "Multi-archetype --apply and role-isolation prod_e2e require live secrets after write-set gate is green"
    env_vars:
      - name: ADMIN_API_KEY
        source: "Existing orchestrator admin key — thin synth API"
      - name: SIM_STAFF_EMAIL
        source: "Required for role isolation JWT proof"
      - name: SIM_STAFF_PASSWORD
        source: "Required for role isolation JWT proof"
      - name: SIM_MANAGER_EMAIL
        source: "Control path for manager-gated route"
      - name: SIM_MANAGER_PASSWORD
        source: "Control path for manager-gated route"

must_haves:
  truths:
    - "Multi-archetype --apply is refused when SYNTH_WRITE_SET ≠ TEARDOWN_TABLES (D-11/D-12)"
    - "Teardown handlers cover every write-set table including master_wine_library* (sim-filtered) and restaurant_menus"
    - "pnpm synth:teardown and pytest import the same teardown registry — no forked orphan list (D-13)"
    - "Teardown never raises; failures tagged Sentry sim-orphan; never deletes e2e-test-restaurant; never deletes SIM_* Auth users; never wholesale-wipes master_wine_library"
    - "CLI + FastAPI default to dry-run; cloud mutations require explicit --apply / apply:true (D-14..D-16)"
    - "Staff JWT cannot access manager-gated Nest paths (D-17) — proven by prod_e2e when secrets present (required if secrets exist); else documented skip"
    - "Dry-run generate succeeds for all 5 archetypes; SYNTH-05 'live' = recipes+snapshots+seed path ready (cloud multi-seed when secrets+gate green)"
  artifacts:
    - path: "scripts/synth/teardown.py"
      provides: "Shared FK-safe sim teardown"
      exports: ["teardown_sim", "TEARDOWN_TABLES", "E2E_ANCHOR_GUARD"]
    - path: "scripts/synth/cli.py"
      provides: "refresh|generate|teardown argparse"
      exports: ["main", "build_parser"]
    - path: "services/agent-orchestrator/api/synth_routes.py"
      provides: "Thin X-Admin-Key API wrappers"
      exports: ["router"]
    - path: "package.json"
      provides: "synth:refresh / synth:generate / synth:teardown scripts"
      contains: "synth:generate"
  key_links:
    - from: "scripts/synth/cli.py"
      to: "scripts/synth/write_set.py"
      via: "assert_write_set_equals_teardown before multi-archetype --apply"
      pattern: "assert_write_set|WriteSetTeardownMismatch|--apply"
    - from: "scripts/synth/teardown.py"
      to: "conftest_prod.py"
      via: "import shared teardown_sim / SYNTH_WRITE_SET"
      pattern: "teardown_sim|sim-orphan|sim-%"
    - from: "api/synth_routes.py"
      to: "scripts.synth"
      via: "thin wrappers; apply defaults false"
      pattern: "X-Admin-Key|apply: false|synth"
    - from: "tests/e2e/test_synth_role_isolation.py"
      to: "Nest manager route"
      via: "staff password-grant JWT → 403"
      pattern: "prod_e2e|SIM_STAFF|403"
---

<objective>
Close the factory **ops surface**: shared teardown write-set gate, never-raise sim teardown extending `conftest_prod.py`, root pnpm CLI + thin FastAPI admin API with dry-run defaults, and staff≠manager role isolation proof (D-11..D-17).

Purpose: Make multi-archetype cloud seed safe (hard gate from SYNTHETIC-TENANT.md) and operable from CI/agents without forking harnesses or wiping the e2e anchor.
Output: `teardown.py` + gated CLI/API + green Nyquist gate/teardown/cli tests + role-isolation test (secrets-gated) + human dry-run checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-synthetic-restaurant-engine/37-CONTEXT.md
@.planning/phases/37-synthetic-restaurant-engine/37-RESEARCH.md
@.planning/phases/37-synthetic-restaurant-engine/37-PATTERNS.md
@.planning/phases/37-synthetic-restaurant-engine/37-VALIDATION.md
@.planning/phases/37-synthetic-restaurant-engine/37-01-SUMMARY.md
@.planning/phases/37-synthetic-restaurant-engine/37-02-SUMMARY.md
@.planning/testing/SYNTHETIC-TENANT.md
@services/agent-orchestrator/tests/e2e/conftest_prod.py
@services/agent-orchestrator/api/health_routes.py
@services/agent-orchestrator/api/research_routes.py
@services/agent-orchestrator/main.py
@package.json
@scripts/synth/seed.py
@scripts/synth/write_set.py

<interfaces>
<!-- Gate (D-11/D-12): -->
Before generate --apply with archetype=all OR ≥2 archetypes: call assert_write_set_equals_teardown(); on mismatch exit non-zero and refuse cloud writes.
Single-archetype --apply may proceed only if gate also green (prefer always requiring equality — simpler, safer).

<!-- Teardown (D-13) — EVERY SYNTH_WRITE_SET table must have a handler (D-11/D-12): -->
Resolve restaurant ids: SELECT id FROM restaurants WHERE slug LIKE 'sim-%'
  AND slug <> 'e2e-test-restaurant' AND id::text <> 'e2e-test-restaurant'

FK-safe DELETE_ORDER (table names MUST match write_set.py exactly — use restaurant_menus, never "menus"):
  1. sim_ground_truth_facts
  2. sim_ground_truth_runs
  3. restaurant_inventory          # by restaurant_id IN sim_ids
  4. menu_items                    # by restaurant_id IN sim_ids
  5. restaurant_menus              # by restaurant_id IN sim_ids
  6. master_wine_library_submissions  # sim-filtered only (restaurant_id IN sim_ids and/or source=sim)
  7. master_wine_library           # sim-filtered ONLY: metadata source=sim OR id IN uuid5(sim.wine.*) for this run — NEVER wholesale DELETE FROM master_wine_library
  8. user_restaurant_access        # by restaurant_id IN sim_ids
  9. restaurants                   # by id IN sim_ids (after children)
 10. organization_members          # sim-only orgs
 11. organizations                 # sim-only orgs (uuid5 sim.org.*)
 12. users                         # NO-OP handler for SIM_* persona emails (durable fixtures; Auth.users never touched)

TEARDOWN_HANDLERS dict/map must include one entry per SYNTH_WRITE_SET table (users = explicit no-op).
test_synth_teardown_safety.py asserts: set(TEARDOWN_HANDLERS) == set(SYNTH_WRITE_SET) AND DELETE_ORDER covers all non-noop tables including master_wine_library*.

NEVER delete slug/id e2e-test-restaurant (hard-guard always, even if row absent)
NEVER raise — sentry_sdk.capture_message(..., tags={"sim-orphan": "true"})
conftest_prod imports teardown_sim — do not maintain a second table list
Gate still refuses multi-archetype --apply until write-set == teardown coverage (list equality + handlers present)

<!-- CLI (D-14..D-16): -->
python3 -m scripts.synth {refresh|generate|teardown}
--archetype ID|all
default dry-run; --apply required for mutations
package.json:
  "synth:refresh": "python3 -m scripts.synth refresh",
  "synth:generate": "python3 -m scripts.synth generate",
  "synth:teardown": "python3 -m scripts.synth teardown"

<!-- API: -->
POST /api/v1/admin/synth/{refresh|generate|teardown}
Header X-Admin-Key (copy health_routes.verify_admin_key)
Body: { "archetype": "bistro"|"all", "apply": false }  # apply defaults false

<!-- Role isolation (D-17): -->
Password-grant JWT as SIM_STAFF → manager-gated Nest path (listMembers / invite) expects 403
Control: SIM_MANAGER JWT succeeds (or at least not 403 for same reason)
Use prod_jwt pattern; never log tokens; @pytest.mark.prod_e2e

<!-- Update SYNTHETIC-TENANT.md: note Phase 37 implemented teardown expansion; ID strategy UUID5+slug; link scripts/synth/teardown.py — do not weaken never-delete-e2e rule. -->

<!-- OUT OF SCOPE: SimPOS, day simulator, control panel (Phase 38). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared teardown + write-set equality gate</name>
  <files>scripts/synth/teardown.py, scripts/synth/write_set.py, scripts/synth/seed.py, services/agent-orchestrator/tests/e2e/conftest_prod.py, services/agent-orchestrator/tests/test_synth_write_set_gate.py, services/agent-orchestrator/tests/test_synth_teardown_safety.py, .planning/testing/SYNTHETIC-TENANT.md</files>
  <behavior>
    - set(SYNTH_WRITE_SET) == set(TEARDOWN_TABLES); drift fails assert_write_set_equals_teardown (D-11/D-12)
    - set(TEARDOWN_HANDLERS.keys()) == set(SYNTH_WRITE_SET); every write-set table has a handler (library + restaurant_menus included)
    - DELETE_ORDER lists restaurant_menus (not "menus") and includes master_wine_library_submissions then master_wine_library before URA/restaurants
    - library handlers delete sim-filtered rows only (source=sim / uuid5 sim.wine.*) — never wholesale library wipe
    - generate --apply with multiple archetypes calls gate and aborts on mismatch (unit with monkeypatch)
    - teardown_sim never raises even if delete fails; records sim-orphan path
    - teardown always hard-guards e2e-test-restaurant and does not call Auth Admin delete for SIM_* users
    - conftest_prod uses shared module (import or call) — no duplicate table constant diverging from write_set.py
  </behavior>
  <action>
    Implement `teardown.py` per RESEARCH Pattern + PATTERNS conftest analog and the FK-safe DELETE_ORDER in `<interfaces>` above. Export TEARDOWN_TABLES from write_set (single source of truth — teardown imports SYNTH_WRITE_SET as TEARDOWN_TABLES).

    **B2 / D-11–D-13:** Coverage = list equality **and** a handler per table. Expand delete order to cover **every** SYNTH_WRITE_SET table:
    `sim_ground_truth_facts → sim_ground_truth_runs → restaurant_inventory → menu_items → restaurant_menus → master_wine_library_submissions → master_wine_library → user_restaurant_access → restaurants → organization_members → organizations` (+ `users` explicit NO-OP). Use name `restaurant_menus` consistently (never shorthand `menus`).

    Library teardown must be **sim-filtered only** (provisional wines tagged `source=sim` and/or deterministic UUID5 under `sim.wine.*`). Never `DELETE FROM master_wine_library` without a sim filter.

    Wire `seed.apply_seed` / generate multi-archetype path to refuse `--apply` unless gate passes (D-11) — gate checks write-set == teardown tables (handlers present).

    Extend `test_synth_teardown_safety.py` to assert: (1) never-raise, (2) e2e anchor skip, (3) Auth users not deleted, (4) **each SYNTH_WRITE_SET table has a teardown handler / DELETE_ORDER entry** including `master_wine_library` + `master_wine_library_submissions` + `restaurant_menus`.

    Extend `conftest_prod.py` to call `teardown_sim` for sim rows **without** forking a second list and **without** deleting the e2e anchor. Keep existing e2e teardown behavior for e2e-% tags.

    Update `.planning/testing/SYNTHETIC-TENANT.md` with Phase 37 implementation note: UUID5 + `slug LIKE 'sim-%'`, shared `scripts/synth/teardown.py`, sim-filtered library teardown, hard gate satisfied when write-set tests green. Preserve never-delete-e2e and anti-patterns.

    Green: `test_synth_write_set_gate.py`, `test_synth_teardown_safety.py` (Nyquist 37-03-01, 37-03-02).
  </action>
  <verify>
    <automated>cd services/agent-orchestrator && pytest tests/test_synth_write_set_gate.py tests/test_synth_teardown_safety.py -q --tb=line 2>&1 | tail -30</automated>
  </verify>
  <done>Write-set↔teardown equality enforced with handlers for every table including master_wine_library*; sim-filtered library deletes; restaurant_menus naming; never-raise teardown; e2e anchor hard-guarded; multi-archetype --apply still gated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI + pnpm scripts + thin FastAPI admin API</name>
  <files>scripts/synth/cli.py, scripts/synth/__main__.py, package.json, services/agent-orchestrator/api/synth_routes.py, services/agent-orchestrator/main.py, services/agent-orchestrator/tests/test_synth_cli_defaults.py, services/agent-orchestrator/tests/test_synth_routes.py</files>
  <behavior>
    - CLI without --apply does not call apply_seed(apply=True) / refresh mutate / teardown mutate (D-16)
    - --apply on generate all invokes write-set gate first
    - API missing X-Admin-Key → 401; body apply defaults false
    - pnpm scripts synth:refresh|generate|teardown present in package.json
    - Routes thin-wrap scripts.synth — no reimplemented seed logic
  </behavior>
  <action>
    Implement argparse CLI (`refresh|generate|teardown`) per D-14/D-15/D-16 and PATTERNS. Wire `__main__.py` → `cli.main()`.

    Add root `package.json` scripts exactly:
    ```json
    "synth:refresh": "python3 -m scripts.synth refresh",
    "synth:generate": "python3 -m scripts.synth generate",
    "synth:teardown": "python3 -m scripts.synth teardown"
    ```

    Create `api/synth_routes.py` with `verify_admin_key` pattern from `health_routes.py`; Pydantic body `apply: bool = False`; register in `main.py`.

    Green: `test_synth_cli_defaults.py` + `test_synth_routes.py` (401/default dry-run) (Nyquist 37-03-03).
  </action>
  <verify>
    <automated>cd services/agent-orchestrator && pytest tests/test_synth_cli_defaults.py tests/test_synth_routes.py -q --tb=line 2>&1 | tail -30</automated>
  </verify>
  <done>CLI+API dry-run by default; admin key required; pnpm synth:* wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Role isolation test + full synth suite + human dry-run verify</name>
  <files>services/agent-orchestrator/tests/e2e/test_synth_role_isolation.py</files>
  <behavior>
    - With SIM_* + Supabase secrets present in execute env: staff JWT → 403 on manager-gated path; manager JWT not blocked the same way (D-17) — **prod_e2e must be green** (not optional when secrets exist)
    - Without secrets: test skips with explicit message — do not claim RLS proof from service-role
    - Full `pytest tests/test_synth_*.py -q` green (excluding prod_e2e if secrets missing)
  </behavior>
  <action>
    Implement `tests/e2e/test_synth_role_isolation.py` marked `prod_e2e`: password-grant as SIM_STAFF against a manager-gated api-gateway route (listMembers/invite — match `team.service.ts` assertAccess). Never log JWT. Reuse `prod_jwt` session pattern.

    If `SIM_STAFF_*` + `SIM_MANAGER_*` + `SUPABASE_URL` + anon/service keys are set, do **not** skip — phase verify requires green. Skip only when secrets absent, with message citing missing env names (not "pass").

    Run full unit synth suite. Fix any regressions from CLI/teardown wiring.

    Then pause for human checkpoint (below) for dry-run generate of all five archetypes.
  </action>
  <verify>
    <automated>cd services/agent-orchestrator && pytest tests/test_synth_*.py -q --tb=line 2>&1 | tail -50</automated>
  </verify>
  <done>Nyquist unit map green; role isolation test present (green or secrets-skip); ready for human dry-run check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator dry-run + gate confirmation</name>
  <what-built>Synthetic factory CLI with write-set gate, shared teardown, dry-run defaults, and five archetype recipes/snapshots from plans 01–03.</what-built>
  <how-to-verify>
    1. From repo root: `pnpm synth:generate -- --archetype all` (no `--apply`) — expect dry-run plans for all 5 archetypes, no cloud mutations.
    2. Confirm `cd services/agent-orchestrator && pytest tests/test_synth_write_set_gate.py -q` is green before any `--apply`.
    3. Optional (secrets only): `pnpm synth:generate -- --archetype bistro --apply` then `pnpm synth:teardown` — confirm `e2e-test-restaurant` still present and SIM_* Auth users still exist.
    4. Do **not** run multi-archetype `--apply` if write-set gate is red.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/API `--apply` → cloud Postgres | Destructive/mutating path; dry-run default; write-set gate |
| Teardown → restaurants slug sim-% | Must not cross into e2e anchor or production non-sim tenants |
| Admin API → orchestrator | X-Admin-Key server-to-server only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03-01 | Tampering / DoS | teardown.py | mitigate | slug LIKE 'sim-%' only; hard-exclude e2e anchor; never-raise + sim-orphan |
| T-37-03-02 | Tampering | multi-archetype --apply | mitigate | Refuse unless SYNTH_WRITE_SET == TEARDOWN_TABLES |
| T-37-03-03 | Elevation of Privilege | staff JWT | mitigate | prod_e2e role isolation; distinct SIM_* users |
| T-37-03-04 | Information Disclosure | CLI/API logs | mitigate | Redact secrets; dry-run dumps counts/slugs only |
| T-37-03-05 | Spoofing | synth_routes | mitigate | X-Admin-Key required; apply defaults false |
</threat_model>

<verification>
- Full `pytest tests/test_synth_*.py -q` green (unit)
- `pytest tests/e2e/test_synth_role_isolation.py -m prod_e2e -q` green or explicit skip
- `pnpm synth:generate -- --archetype all` dry-run succeeds for 5 archetypes
- Human checkpoint approved
</verification>

<success_criteria>
- D-11..D-17 implemented and Nyquist-verified
- SYNTHETIC-TENANT hard gate honored before multi-archetype --apply (write-set == teardown including master_wine_library* handlers)
- CLI+API operable; Phase 38 can consume seeded menus without building SimPOS here
- SYNTH-05: five archetypes have recipes+snapshots+dry-run seed path; cloud multi-archetype `--apply` when secrets + gate green
</success_criteria>

<output>
After completion, create `.planning/phases/37-synthetic-restaurant-engine/37-03-SUMMARY.md`
</output>

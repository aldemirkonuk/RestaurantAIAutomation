---
phase: 30-calendar-operations-hub
plan: 04
type: execute
wave: 3
depends_on:
  - 30-02
files_modified:
  - apps/web/src/hooks/queries/useCalendarQueries.ts
  - apps/web/src/services/api/calendar.ts
  - apps/web/src/pages/Calendar.tsx
  - apps/web/src/pages/calendar/useCalendarPage.ts
  - apps/web/src/pages/calendar/CalendarPage.tsx
  - apps/web/src/pages/calendar/CalendarAgenda.tsx
  - apps/web/src/pages/calendar/EventCard.tsx
  - apps/web/src/data/FALLBACK_DATA.ts
  - apps/web/src/pages/Dashboard.tsx
  - apps/web/src/pages/dashboard/useDashboardPage.ts
autonomous: true
requirements:
  - CAL-FIX-02
  - CAL-FIX-04
  - CAL-UX-01

must_haves:
  truths:
    - "No file in the frontend codebase contains the string 'confirmed' as a status value (only 'approved' used in code)"
    - "Status dropdown shows Confirmed label mapped to 'approved' value"
    - "buildCreatePayload includes eventDateEnd and eventTimeEnd fields"
    - "Dashboard Add Event button navigates to /calendar?openModal=true"
    - "CalendarPage auto-opens EventModal when ?openModal=true query param is present"
    - "After modal opens from URL param, the param is cleared to prevent re-opening"
  artifacts:
    - path: "apps/web/src/services/api/calendar.ts"
      provides: "Fixed buildCreatePayload, updateCalendarEvent, status type"
      contains: "eventDateEnd"
    - path: "apps/web/src/pages/calendar/CalendarPage.tsx"
      provides: "useSearchParams + openModal effect"
      contains: "openModal"
    - path: "apps/web/src/pages/Dashboard.tsx"
      provides: "useNavigate Add Event button"
      contains: "openModal=true"
  key_links:
    - from: "apps/web/src/pages/Dashboard.tsx"
      to: "apps/web/src/pages/calendar/CalendarPage.tsx"
      via: "navigate('/calendar?openModal=true') → searchParams.get('openModal') === 'true'"
      pattern: "openModal=true"
    - from: "apps/web/src/services/api/calendar.ts buildCreatePayload"
      to: "apps/api-gateway/src/calendar/dto/calendar.dto.ts CreateCalendarEventDto.eventTimeEnd"
      via: "eventTimeEnd in payload → backend DTO field"
      pattern: "eventTimeEnd"
---

<objective>
Fix all frontend-layer bugs: remove the invalid `'confirmed'` status value everywhere and replace with `'approved'` (while keeping the display label "Confirmed"), fix `buildCreatePayload` and `updateCalendarEvent` to actually send end time to the backend, change the Dashboard Add Event button to navigate to `/calendar?openModal=true` instead of redirecting, add URL param detection in CalendarPage to auto-open the modal, and verify the dashboard calendar widget date range query (D-12).

Purpose: After this plan, all calendar CRUD flows work end-to-end from the frontend. Status updates no longer fail with 400. End time is actually sent to and stored by the backend. Dashboard Add Event opens the modal in-context.
Output: Modified frontend files. No new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-calendar-operations-hub/30-CONTEXT.md
@.planning/phases/30-calendar-operations-hub/30-RESEARCH.md

<interfaces>
<!-- From apps/web/src/services/api/calendar.ts lines 104-118 (current buildCreatePayload — endTime MISSING): -->
<!-- const buildCreatePayload = (data) => ({
  ...
  eventTime: data.allDay ? undefined : data.startTime,  // start time, sent
  // MISSING: eventDateEnd   ← add: data.allDay ? undefined : data.endDate
  // MISSING: eventTimeEnd   ← add: data.allDay ? undefined : data.endTime
  ...
}) -->

<!-- From apps/web/src/pages/calendar/CalendarPage.tsx lines 1-29: -->
<!-- Current imports: useState, useMemo, useCallback from 'react' -->
<!-- Does NOT import: useEffect, useSearchParams -->
<!-- openCreateModal(date?: Date) is already defined at line 139 -->

<!-- From apps/web/src/pages/Dashboard.tsx lines 34, 697-703: -->
<!-- import { NavLink } from 'react-router-dom'  ← need to add useNavigate -->
<!-- <NavLink to="/calendar">Add Event</NavLink>  ← replace with navigate button -->

<!-- Status display label mapping (D-02): -->
<!-- pending → "Pending" -->
<!-- approved → "Confirmed" (display label only; value stored as 'approved') -->
<!-- dismissed → "Declined" -->
<!-- completed → "Completed" -->
<!-- cancelled → "Cancelled" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Status enum cleanup across frontend files (D-02)</name>
  <files>
    apps/web/src/hooks/queries/useCalendarQueries.ts,
    apps/web/src/pages/Calendar.tsx,
    apps/web/src/pages/calendar/CalendarAgenda.tsx,
    apps/web/src/pages/calendar/EventCard.tsx,
    apps/web/src/data/FALLBACK_DATA.ts
  </files>
  <read_first>
    - apps/web/src/hooks/queries/useCalendarQueries.ts — find useUpdateEventStatus, look for 'confirmed' in status union type (line ~329)
    - apps/web/src/pages/Calendar.tsx — read the ENTIRE file; find lines 78, 1009, 1326, 1624, 1631 where 'confirmed' appears
    - apps/web/src/pages/calendar/CalendarAgenda.tsx — find line ~167 where !== 'confirmed' appears
    - apps/web/src/pages/calendar/EventCard.tsx — find line ~140 where === 'confirmed' appears
    - apps/web/src/data/FALLBACK_DATA.ts — find line ~40 where status: 'confirmed' appears
    - .planning/phases/30-calendar-operations-hub/30-RESEARCH.md (Bug 2 D-02 section — exact file:line references)
  </read_first>
  <action>
Apply the following TARGETED changes in each file. Read each file fully before editing.

**useCalendarQueries.ts:**
- Find `useUpdateEventStatus` — its mutation function type union likely includes `'confirmed'`
- Remove `'confirmed'` from the status type union; add `'dismissed'` if missing
- The correct union: `'pending' | 'approved' | 'dismissed' | 'completed' | 'cancelled'`

**Calendar.tsx (the large page at apps/web/src/pages/Calendar.tsx):**
- Line 78: In the `status?:` type, remove `'confirmed'` from the union (keep `'approved'`)
- Line 1009: `if (... status === 'confirmed')` → `if (... status === 'approved')`
- Line 1326: In the status badge className: `event.status === 'confirmed' ? 'bg-green-100 text-green-700'` → `event.status === 'approved' ? 'bg-green-100 text-green-700'`
- Line 1624: Same badge pattern → replace `=== 'confirmed'` with `=== 'approved'`
- Line 1631: `<option value="confirmed">Confirmed</option>` → `<option value="approved">Confirmed</option>` (label stays "Confirmed", value changes to "approved")
- Verify: no remaining `=== 'confirmed'` or `!== 'confirmed'` or `value="confirmed"` in the file

**CalendarAgenda.tsx:**
- Line ~167: `event.status !== 'confirmed'` → `event.status !== 'approved'`

**EventCard.tsx:**
- Line ~140: `event.status === 'confirmed'` → `event.status === 'approved'`
- If there is a badge/className that maps 'confirmed' to green styling, update the comparison to 'approved' while keeping the green styling

**FALLBACK_DATA.ts:**
- Find `status: 'confirmed'` → change to `status: 'approved'`
  </action>
  <acceptance_criteria>
    - `grep -rn "=== 'confirmed'" apps/web/src/pages/Calendar.tsx` returns 0 results
    - `grep -rn "value=\"confirmed\"" apps/web/src/pages/Calendar.tsx` returns 0 results
    - `grep -rn "'confirmed'" apps/web/src/pages/calendar/CalendarAgenda.tsx` returns 0 results
    - `grep -rn "'confirmed'" apps/web/src/pages/calendar/EventCard.tsx` returns 0 results
    - `grep -rn "status: 'confirmed'" apps/web/src/data/FALLBACK_DATA.ts` returns 0 results
    - `grep -n "Confirmed" apps/web/src/pages/Calendar.tsx | grep "option\|label\|display"` shows at least 1 result (display label preserved)
    - `cd apps/web && npx tsc --noEmit 2>&1 | grep -c "confirmed"` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>grep -rn "'confirmed'" apps/web/src/pages/Calendar.tsx apps/web/src/pages/calendar/CalendarAgenda.tsx apps/web/src/pages/calendar/EventCard.tsx apps/web/src/data/FALLBACK_DATA.ts || echo "CLEAN — no confirmed values found"</automated>
  </verify>
  <done>'confirmed' string removed from all status comparisons and option values across frontend files; display label "Confirmed" preserved; 'approved' used as the stored value everywhere</done>
</task>

<task type="auto">
  <name>Task 2: endTime payload fix + Dashboard Add Event + CalendarPage openModal + dashboard widget fix</name>
  <files>
    apps/web/src/services/api/calendar.ts,
    apps/web/src/pages/calendar/useCalendarPage.ts,
    apps/web/src/pages/calendar/CalendarPage.tsx,
    apps/web/src/pages/Dashboard.tsx,
    apps/web/src/pages/dashboard/useDashboardPage.ts
  </files>
  <read_first>
    - apps/web/src/services/api/calendar.ts — read ENTIRE file; find buildCreatePayload (~line 104), updateCalendarEvent function (~line 240), CalendarEvent interface (~line 120), ApiCalendarEvent interface (~line 18), status union in CalendarEvent and ApiCalendarEvent
    - apps/web/src/pages/calendar/CalendarPage.tsx — read ENTIRE file; find existing imports (line 1), openCreateModal usage (line 139), the return JSX
    - apps/web/src/pages/calendar/useCalendarPage.ts — read ENTIRE file; find status type union (line 26)
    - apps/web/src/pages/Dashboard.tsx — read lines 1-50 (imports) and lines 690-715 (Add Event section)
    - apps/web/src/pages/dashboard/useDashboardPage.ts — find useCalendarEvents call and date range params (line ~143)
    - .planning/phases/30-calendar-operations-hub/30-RESEARCH.md (Pattern 3: useSearchParams + openModal Effect; Pitfall 4; Bug 4 buildCreatePayload section)
  </read_first>
  <action>
Apply the following changes across five files:

---

**A. apps/web/src/services/api/calendar.ts**

1. Fix `CalendarEvent` interface (line ~136) — status union type: remove `'confirmed'`, keep `'approved'`:
   Change: `status?: 'pending' | 'confirmed' | 'approved' | 'dismissed' | 'completed' | 'cancelled'`
   To:     `status?: 'pending' | 'approved' | 'dismissed' | 'completed' | 'cancelled'`

2. Fix `ApiCalendarEvent` interface (line ~32) — same rename in the status field.

3. Fix `buildCreatePayload` (line ~104) — add the two missing fields after `eventTime`:
   ```typescript
   eventTime: data.allDay ? undefined : data.startTime,
   eventDateEnd: data.allDay ? undefined : (data as any).endDate,
   eventTimeEnd: data.allDay ? undefined : data.endTime,
   ```

4. Fix `updateCalendarEvent` function (line ~240) — find where payload is assembled, add:
   ```typescript
   if (updateData.endTime !== undefined) {
     payload.eventDateEnd = updateData.allDay ? undefined : updateData.endDate;
     payload.eventTimeEnd = updateData.allDay ? undefined : updateData.endTime;
   }
   ```

---

**B. apps/web/src/pages/calendar/useCalendarPage.ts**

Line 26: Remove `'confirmed'` from the CalendarEvent.status? union:
Change: `status?: 'pending' | 'confirmed' | 'approved' | 'dismissed' | 'completed' | 'cancelled'`
To:     `status?: 'pending' | 'approved' | 'dismissed' | 'completed' | 'cancelled'`

---

**C. apps/web/src/pages/calendar/CalendarPage.tsx**

1. Add imports at the top:
   ```typescript
   import { useState, useMemo, useCallback, useEffect } from 'react'
   import { useSearchParams } from 'react-router-dom'
   ```

2. Inside the CalendarPage component body (after `const { openCreateModal, ... } = useCalendarPage()` or equivalent destructuring), add:
   ```typescript
   const [searchParams, setSearchParams] = useSearchParams()

   useEffect(() => {
     if (searchParams.get('openModal') === 'true') {
       const dateStr = searchParams.get('date')
       const date = dateStr ? new Date(dateStr) : new Date()
       openCreateModal(date)
       // Clear params to prevent modal re-opening on back-navigation
       setSearchParams(prev => {
         prev.delete('openModal')
         prev.delete('date')
         return prev
       }, { replace: true })
     }
   }, []) // empty deps — run once on mount
   ```

---

**D. apps/web/src/pages/Dashboard.tsx**

1. Add `useNavigate` to the react-router-dom import (line 34):
   Change: `import { NavLink } from 'react-router-dom'`
   To:     `import { NavLink, useNavigate } from 'react-router-dom'`

2. Inside Dashboard component body, add before the return statement:
   ```typescript
   const navigate = useNavigate()
   ```

3. Replace the Add Event NavLink (lines ~697-703):
   Change:
   ```tsx
   <NavLink
     to="/calendar"
     className="mt-4 flex items-center justify-center gap-2 w-full px-3 py-2 border-2 border-dashed border-gray-300 rounded-lg text-gray-500 hover:border-wine-400 hover:text-wine-600 transition-colors text-xs font-medium"
   >
     <Plus className="w-3 h-3" />
     Add Event
   </NavLink>
   ```
   To:
   ```tsx
   <button
     onClick={() => navigate('/calendar?openModal=true')}
     className="mt-4 flex items-center justify-center gap-2 w-full px-3 py-2 border-2 border-dashed border-gray-300 rounded-lg text-gray-500 hover:border-wine-400 hover:text-wine-600 transition-colors text-xs font-medium"
   >
     <Plus className="w-3 h-3" />
     Add Event
   </button>
   ```

---

**E. apps/web/src/pages/dashboard/useDashboardPage.ts**

Verify the `useCalendarEvents` call (line ~143) is passing `startDate` and `endDate` params correctly. The column fix in Plan 30-02 already fixes the backend query. If the query params passed to `useCalendarEvents` are named `startDate`/`endDate` matching the API, no change is needed. If they are named `eventDate` or similar legacy names, update them to match `startDate`/`endDate`. Leave unchanged if already correct.
  </action>
  <acceptance_criteria>
    - `grep -c "eventTimeEnd" apps/web/src/services/api/calendar.ts` ≥ `1` (in buildCreatePayload)
    - `grep -c "eventDateEnd" apps/web/src/services/api/calendar.ts` ≥ `2` (in buildCreatePayload + updateCalendarEvent)
    - `grep -c "openModal=true" apps/web/src/pages/Dashboard.tsx` returns `1`
    - `grep -c "useNavigate" apps/web/src/pages/Dashboard.tsx` returns `1`
    - `grep -c "NavLink.*to.*calendar" apps/web/src/pages/Dashboard.tsx` returns `0` (NavLink replaced with button)
    - `grep -c "useSearchParams" apps/web/src/pages/calendar/CalendarPage.tsx` returns `1`
    - `grep -c "openModal.*true\|openModal.*=.*true" apps/web/src/pages/calendar/CalendarPage.tsx` returns `1`
    - `grep -c "'confirmed'" apps/web/src/services/api/calendar.ts` returns `0`
    - `grep -c "'confirmed'" apps/web/src/pages/calendar/useCalendarPage.ts` returns `0`
    - `cd apps/web && npx tsc --noEmit 2>&1 | grep -c "calendar\|Dashboard"` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "openModal=true" apps/web/src/pages/Dashboard.tsx && grep -c "useSearchParams" apps/web/src/pages/calendar/CalendarPage.tsx && grep -c "eventTimeEnd" apps/web/src/services/api/calendar.ts</automated>
  </verify>
  <done>endTime sent in create/update payloads; Dashboard Add Event uses navigate(); CalendarPage detects ?openModal=true and opens modal then clears param; no 'confirmed' status values remain in frontend API service or hooks</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query params → React component | openModal and date params from URL are user-controlled |
| Frontend status values → backend DTO | Status string must match CalendarEventStatus enum on backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-11 | Tampering | openModal URL param | accept | Only opens a modal — no DB write triggered by param alone; user must submit the modal form to create data |
| T-30-12 | Tampering | date URL param injection | accept | Date string passed to `new Date()` — invalid dates produce Invalid Date, openCreateModal handles gracefully |
| T-30-13 | Tampering | status 'approved' stored instead of 'confirmed' | mitigate | Backend enum validation (CalendarEventStatus) rejects any value outside the valid set; frontend type unions enforce compile-time safety |
</threat_model>

<verification>
Manual smoke tests (no automated test framework exists):

1. Navigate to dashboard → click "Add Event" → should navigate to /calendar with modal open, not just to /calendar page
2. Create event with a start time and end time → save → re-open event → end time should be populated
3. Create event with a color → save → re-open → color should be preserved
4. Open existing event → change status to "Confirmed" → save → re-fetch → status should be "approved" in API response
5. Dashboard calendar widget should show events correctly (column fix in Plan 30-02 backend fix propagates here automatically)
</verification>

<success_criteria>
- `grep -rn "'confirmed'" apps/web/src/pages apps/web/src/hooks apps/web/src/services apps/web/src/data` returns 0 results for status enum usage
- `grep -c "eventTimeEnd" apps/web/src/services/api/calendar.ts` ≥ `1`
- `grep -c "openModal=true" apps/web/src/pages/Dashboard.tsx` returns `1`
- `grep -c "useSearchParams" apps/web/src/pages/calendar/CalendarPage.tsx` returns `1`
- TypeScript: `cd apps/web && npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/30-calendar-operations-hub/30-04-SUMMARY.md`
</output>

# Quick Task 260406-329 Context

## Why this task existed
- Reduce false contradictions in Phase 8 web concordance outputs from real smoke test data.

## Problem patterns
- Color descriptors varied by source (`deep garnet`, `ruby`) but represented same base color.
- Grape varieties from web often contained richer blend descriptions than existing value.

## Decisions locked
- Add `COLOR_SYNONYMS` normalization before concordance comparison for `color`.
- Treat grape substring superset as `web_data_more_complete` instead of `contradiction`.
- Keep true mismatches as `contradiction`.

## Verification baseline
- New targeted tests added for color synonyms and grape substring behavior.
- Full `test_web_verification.py` suite remains green.

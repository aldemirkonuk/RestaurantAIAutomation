---
phase: 32-provider-outbound-communication-engine
plan: 05
type: execute
wave: 3
depends_on:
  - 32-03
  - 32-04
files_modified:
  - apps/web/src/hooks/queries/useDraftEmailQueries.ts
  - apps/web/src/components/orders/DraftEmailApprovalPanel.tsx
  - apps/web/src/components/providers/ProviderProfileForm.tsx
autonomous: true
requirements:
  - OUTBOUND-02
  - PROVINT-02

must_haves:
  truths:
    - "useDraftEmailQueries.ts exports useGetPendingDraft, useApproveDraft, useDiscardDraft, useEditDraft"
    - "DraftEmailApprovalPanel renders with bg-indigo-900 header (distinguishable from ORDER APPROVAL bg-black)"
    - "DraftEmailApprovalPanel shows AI draft body + inline textarea editor toggle + constraint warning amber block + read-only disclaimer"
    - "Send Draft button is bg-green-500; Discard is bg-red-500; Edit Draft toggle is bg-gray-700"
    - "DraftEmailApprovalPanel has role=dialog + aria-labelledby + Escape key close"
    - "ProviderProfileForm has 10 fields (specialty_categories, primary_region, distribution_channel, business_type, decision_maker_name, preferred_communication_style, typical_response_days, net_payment_terms, ships_on_days, notes)"
    - "ProviderProfileForm PATCH /api/v1/providers/:id/intelligence with profile_foundational payload + toast feedback"
  artifacts:
    - path: "apps/web/src/hooks/queries/useDraftEmailQueries.ts"
      provides: "useApproveDraft, useDiscardDraft, useEditDraft, useGetPendingDraft mutations"
      contains: "useApproveDraft"
    - path: "apps/web/src/components/orders/DraftEmailApprovalPanel.tsx"
      provides: "AI draft review modal"
      contains: "DraftEmailApprovalPanel"
    - path: "apps/web/src/components/providers/ProviderProfileForm.tsx"
      provides: "Foundational intelligence profile form"
      contains: "ProviderProfileForm"
  key_links:
    - from: "DraftEmailApprovalPanel"
      to: "/api/v1/procurement/orders/:id/approve-draft"
      via: "useApproveDraft mutation"
      pattern: "approve-draft"
    - from: "ProviderProfileForm"
      to: "/api/v1/providers/:id/intelligence"
      via: "apiClient.patch"
      pattern: "intelligence"
---

<objective>
Create the three net-new Phase 32 frontend files: useDraftEmailQueries.ts (React Query mutation hooks), DraftEmailApprovalPanel.tsx (AI draft review modal per UI-SPEC), and ProviderProfileForm.tsx (foundational intelligence profile form).

These are pure new-file additions. Page wiring (Providers.tsx + Orders.tsx modifications) is handled in Plan 32-07.
Output: 3 new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-provider-outbound-communication-engine/32-UI-SPEC.md
@.planning/phases/32-provider-outbound-communication-engine/32-CONTEXT.md
@.planning/phases/32-provider-outbound-communication-engine/32-PATTERNS.md
@.planning/phases/32-provider-outbound-communication-engine/32-04-SUMMARY.md

<interfaces>
<!-- From apps/web/src/components/orders/OrderApprovalModal.tsx (exact animation to copy):
  <AnimatePresence>
    <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}
      className="fixed inset-0 bg-black/60 backdrop-blur-sm z-[100] flex items-center justify-center p-4">
      <motion.div initial={{ scale: 0.95, opacity: 0, y: 20 }} animate={{ scale: 1, opacity: 1, y: 0 }}
        className="bg-white rounded-2xl shadow-2xl w-full max-w-lg border-2 overflow-hidden">
-->

<!-- From apps/web/src/hooks/queries/useOrderQueries.ts (mutation pattern):
  export function useApproveDraft() {
    const queryClient = useQueryClient()
    return useMutation({
      mutationFn: (data) => apiClient.post('/api/v1/procurement/orders/${orderId}/approve-draft', data),
      onSettled: () => queryClient.invalidateQueries({ queryKey: queryKeys.orders.all })
    })
  }
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useDraftEmailQueries.ts — useGetPendingDraft, useApproveDraft, useDiscardDraft, useEditDraft</name>
  <read_first>
    - apps/web/src/hooks/queries/useOrderQueries.ts (full file — copy mutation structure exactly: imports, query keys, useMutation pattern, onSettled invalidation)
    - apps/web/src/lib/query-keys.ts (find queryKeys.orders.all for invalidation)
    - apps/web/src/services/api/client.ts (apiClient import path)
    - apps/web/src/contexts/AuthContext.tsx (useAuth hook — find activeRestaurantId field name)
  </read_first>
  <files>apps/web/src/hooks/queries/useDraftEmailQueries.ts</files>
  <action>
    Create `apps/web/src/hooks/queries/useDraftEmailQueries.ts`:

    ```typescript
    import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
    import { apiClient } from '../../services/api/client'
    // Note: verify exact import path after reading useOrderQueries.ts

    export const draftKeys = {
      all: ['drafts'] as const,
      pending: (restaurantId: string) => [...draftKeys.all, 'pending', restaurantId] as const,
      byOrder: (orderId: string) => [...draftKeys.all, 'order', orderId] as const,
    }

    export function useGetPendingDraft(orderId: string | null) {
      return useQuery({
        queryKey: draftKeys.byOrder(orderId ?? ''),
        queryFn: () =>
          apiClient.get(`/api/v1/procurement/orders/${orderId}/draft`).then((r) => r.data),
        enabled: !!orderId,
      })
    }

    export function useApproveDraft() {
      const queryClient = useQueryClient()
      return useMutation({
        mutationFn: ({
          orderId,
          modifiedContent,
          managerNotes,
        }: {
          orderId: string
          modifiedContent?: string
          managerNotes?: string
        }) =>
          apiClient
            .post(`/api/v1/procurement/orders/${orderId}/approve-draft`, { modifiedContent, managerNotes })
            .then((r) => r.data),
        onSettled: () => {
          queryClient.invalidateQueries({ queryKey: draftKeys.all })
          queryClient.invalidateQueries({ queryKey: ['orders'] })
        },
      })
    }

    export function useDiscardDraft() {
      const queryClient = useQueryClient()
      return useMutation({
        mutationFn: (orderId: string) =>
          apiClient.post(`/api/v1/procurement/orders/${orderId}/discard-draft`).then((r) => r.data),
        onSettled: () => {
          queryClient.invalidateQueries({ queryKey: draftKeys.all })
        },
      })
    }

    export function useEditDraft() {
      const queryClient = useQueryClient()
      return useMutation({
        mutationFn: ({ orderId, content }: { orderId: string; content: string }) =>
          apiClient
            .patch(`/api/v1/procurement/orders/${orderId}/draft`, { modifiedContent: content })
            .then((r) => r.data),
        onSettled: (_data, _error, variables) => {
          queryClient.invalidateQueries({ queryKey: draftKeys.byOrder(variables.orderId) })
        },
      })
    }
    ```
  </action>
  <acceptance_criteria>
    - `ls apps/web/src/hooks/queries/useDraftEmailQueries.ts` → file exists
    - `grep -c "useApproveDraft\|useDiscardDraft\|useEditDraft\|useGetPendingDraft" apps/web/src/hooks/queries/useDraftEmailQueries.ts` → 4
    - `grep -c "approve-draft" apps/web/src/hooks/queries/useDraftEmailQueries.ts` → ≥ 1
    - `grep -c "discard-draft" apps/web/src/hooks/queries/useDraftEmailQueries.ts` → ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>ls apps/web/src/hooks/queries/useDraftEmailQueries.ts && grep -c "useApproveDraft\|useDiscardDraft\|useEditDraft\|useGetPendingDraft" apps/web/src/hooks/queries/useDraftEmailQueries.ts</automated>
  </verify>
  <done>useDraftEmailQueries.ts created: draftKeys, useGetPendingDraft, useApproveDraft, useDiscardDraft, useEditDraft. All import apiClient and use React Query mutations with onSettled invalidation.</done>
</task>

<task type="auto">
  <name>Task 2: Create DraftEmailApprovalPanel.tsx — AI draft review modal per UI-SPEC</name>
  <read_first>
    - apps/web/src/components/orders/OrderApprovalModal.tsx (full file — copy AnimatePresence + motion.div patterns, action button grid, z-index conventions)
    - .planning/phases/32-provider-outbound-communication-engine/32-UI-SPEC.md §DraftEmailApprovalPanel (full spec: colors, layout zones, state machine, animation, accessibility)
    - apps/web/src/hooks/queries/useDraftEmailQueries.ts (just created — check import path)
  </read_first>
  <files>apps/web/src/components/orders/DraftEmailApprovalPanel.tsx</files>
  <action>
    Create `apps/web/src/components/orders/DraftEmailApprovalPanel.tsx`:

    **Interfaces:**
    ```typescript
    interface ConstraintWarning {
      code: string         // e.g. "C-09"
      message: string
      severity: 'annotating' | 'soft'
    }

    interface DraftEmailData {
      conversationId: string
      orderId: string
      wineName: string
      providerName: string
      providerEmail: string
      emailType: 'PRICE_INQUIRY' | 'DEMAND_OFFER' | 'PROMO_INQUIRY' | 'WINE_INQUIRY'
      draftContent: string       // editable AI body
      disclaimer: string         // read-only, always shown (D-32-08)
      constraintWarnings: ConstraintWarning[]
      roundCount: number
      timestamp: string
    }

    interface DraftEmailApprovalPanelProps {
      isOpen: boolean
      draftData: DraftEmailData | null
      onApprove: (modifiedContent?: string, managerNotes?: string) => void
      onDiscard: () => void
      onClose: () => void
      isSubmitting?: boolean
    }
    ```

    **State:**
    ```typescript
    const [isEditing, setIsEditing] = useState(false)
    const [editedContent, setEditedContent] = useState('')
    // useEffect: when draftData changes, reset editedContent to draftData.draftContent
    useEffect(() => {
      if (draftData) setEditedContent(draftData.draftContent)
    }, [draftData])
    // Escape key close
    useEffect(() => {
      const handler = (e: KeyboardEvent) => { if (e.key === 'Escape') onClose() }
      window.addEventListener('keydown', handler)
      return () => window.removeEventListener('keydown', handler)
    }, [onClose])
    ```

    **Email type badge config:**
    ```typescript
    const emailTypeBadge = {
      PRICE_INQUIRY:  { label: 'Price Inquiry',  bg: 'bg-blue-100',   text: 'text-blue-700'   },
      DEMAND_OFFER:   { label: 'Demand Offer',   bg: 'bg-orange-100', text: 'text-orange-700' },
      PROMO_INQUIRY:  { label: 'Promo Inquiry',  bg: 'bg-purple-100', text: 'text-purple-700' },
      WINE_INQUIRY:   { label: 'Wine Inquiry',   bg: 'bg-teal-100',   text: 'text-teal-700'   },
    }
    ```

    **Framer Motion wrapper (copy from OrderApprovalModal.tsx):**
    ```tsx
    <AnimatePresence>
      {isOpen && (
        <motion.div
          initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}
          className="fixed inset-0 bg-black/60 backdrop-blur-sm z-[100] flex items-center justify-center p-4"
          onClick={onClose}
        >
          <motion.div
            initial={{ scale: 0.95, opacity: 0, y: 20 }}
            animate={{ scale: 1, opacity: 1, y: 0 }}
            exit={{ scale: 0.95, opacity: 0, y: 20 }}
            onClick={(e) => e.stopPropagation()}
            role="dialog"
            aria-modal="true"
            aria-labelledby="draft-panel-title"
            className="bg-white rounded-2xl shadow-2xl w-full max-w-lg border-2 border-indigo-900 overflow-hidden max-h-[90vh] overflow-y-auto"
          >
    ```

    **Header (bg-indigo-900 — NOT bg-black):**
    ```tsx
    <div className="bg-indigo-900 px-6 py-5 border-b-2 border-white">
      <div className="flex items-center justify-between">
        <h2 id="draft-panel-title" className="text-2xl font-black text-white uppercase tracking-wider">
          ✦ AI DRAFT READY
        </h2>
        <button onClick={onClose} className="text-white hover:text-gray-300">
          <X className="w-5 h-5" />
        </button>
      </div>
    </div>
    ```

    **Body area (preview/edit toggle):**
    ```tsx
    {isEditing ? (
      <textarea
        value={editedContent}
        onChange={(e) => setEditedContent(e.target.value)}
        className="w-full h-48 rounded-lg border border-gray-300 px-3 py-2 text-sm focus:ring-2 focus:ring-indigo-500 resize-none"
      />
    ) : (
      <pre className="w-full text-sm text-gray-700 whitespace-pre-wrap font-sans bg-gray-50 rounded-lg p-3 border border-gray-200">
        {editedContent}
      </pre>
    )}

    {/* Edit toggle */}
    <button
      onClick={() => setIsEditing(!isEditing)}
      className="w-full h-11 bg-gray-700 hover:bg-gray-800 text-white font-medium text-sm rounded-xl transition-all flex items-center justify-center gap-1.5"
    >
      <Edit2 className="w-3.5 h-3.5" />
      {isEditing ? 'Preview' : 'Edit Draft'}
    </button>
    ```

    **Constraint warnings (amber — only if present):**
    ```tsx
    {draftData.constraintWarnings.length > 0 && (
      <div role="alert" className="bg-amber-50 border border-amber-200 rounded-lg p-3 mt-3">
        <p className="text-[11px] text-amber-700 font-semibold uppercase tracking-wide mb-1">
          ⚠ Constraint Warnings
        </p>
        {draftData.constraintWarnings.map((w) => (
          <p key={w.code} className="text-xs text-amber-700">
            [{w.code}] {w.message}
          </p>
        ))}
      </div>
    )}
    ```

    **Disclaimer (read-only, non-removable per D-32-08):**
    ```tsx
    <div
      className="bg-gray-100 rounded-lg border border-gray-300 p-3 mt-3"
      aria-label="Non-removable WineOps AI disclaimer"
    >
      <p className="text-[10px] text-gray-500 font-medium uppercase tracking-wide mb-1">
        Auto-appended disclaimer (required)
      </p>
      <p className="text-xs text-gray-600 italic whitespace-pre-line">
        {draftData.disclaimer}
      </p>
    </div>
    ```

    **Action buttons (2-column grid):**
    ```tsx
    <div className="grid grid-cols-2 gap-3 mt-4">
      <button
        onClick={() => onApprove(
          editedContent !== draftData.draftContent ? editedContent : undefined
        )}
        disabled={isSubmitting}
        className="h-16 bg-green-500 hover:bg-green-600 disabled:opacity-50 text-white font-bold text-base rounded-xl shadow-lg transition-all flex items-center justify-center gap-2"
      >
        <CheckCircle className="w-5 h-5" />
        Send Draft
      </button>
      <button
        onClick={onDiscard}
        disabled={isSubmitting}
        className="h-16 bg-red-500 hover:bg-red-600 disabled:opacity-50 text-white font-bold text-base rounded-xl shadow-lg transition-all flex items-center justify-center gap-2"
      >
        <XCircle className="w-5 h-5" />
        Discard
      </button>
    </div>
    ```

    Imports needed: `{ useState, useEffect }` from react, `{ motion, AnimatePresence }` from framer-motion, `{ CheckCircle, XCircle, Edit2, X }` from lucide-react.
  </action>
  <acceptance_criteria>
    - `ls apps/web/src/components/orders/DraftEmailApprovalPanel.tsx` → file exists
    - `grep -c "bg-indigo-900" apps/web/src/components/orders/DraftEmailApprovalPanel.tsx` → ≥ 2
    - `grep -c "AI DRAFT READY" apps/web/src/components/orders/DraftEmailApprovalPanel.tsx` → 1
    - `grep -c "role=\"dialog\"" apps/web/src/components/orders/DraftEmailApprovalPanel.tsx` → 1
    - `grep -c "Non-removable WineOps" apps/web/src/components/orders/DraftEmailApprovalPanel.tsx` → 1
    - `grep -c "isEditing" apps/web/src/components/orders/DraftEmailApprovalPanel.tsx` → ≥ 3
    - `grep -c "bg-amber-50" apps/web/src/components/orders/DraftEmailApprovalPanel.tsx` → ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>grep -c "bg-indigo-900" apps/web/src/components/orders/DraftEmailApprovalPanel.tsx && grep -c "role=\"dialog\"" apps/web/src/components/orders/DraftEmailApprovalPanel.tsx</automated>
  </verify>
  <done>DraftEmailApprovalPanel.tsx: bg-indigo-900 header, AnimatePresence animation, inline textarea/pre toggle, amber constraint warnings, read-only disclaimer with aria-label, 2-column Send Draft + Discard grid, role=dialog, Escape key close. TypeScript compilation clean.</done>
</task>

<task type="auto">
  <name>Task 3: Create ProviderProfileForm.tsx — 10-field foundational intelligence profile form</name>
  <read_first>
    - apps/web/src/services/api/client.ts (apiClient import path)
    - apps/web/src/pages/Providers.tsx lines 1-50 (find how toast is called — check existing toast pattern e.g. react-hot-toast, sonner, or custom)
    - .planning/phases/32-provider-outbound-communication-engine/32-UI-SPEC.md §ProviderProfileForm (all 10 fields, layout tokens, save behavior)
  </read_first>
  <files>apps/web/src/components/providers/ProviderProfileForm.tsx</files>
  <action>
    Create `apps/web/src/components/providers/ProviderProfileForm.tsx`.

    **Props:**
    ```typescript
    interface ProviderProfileFormProps {
      providerId: string
      initialValues?: Record<string, any>   // pre-populated from profile_foundational
      onSaved?: () => void
    }
    ```

    **10 fields (all optional — partial profiles are valid):**

    1. `specialty_categories` — text input (comma-separated, e.g. "Burgundy, Champagne")
    2. `primary_region` — text input
    3. `distribution_channel` — `<select>`: `['Distributor', 'Direct Importer', 'Broker', 'Producer']`
    4. `business_type` — `<select>`: `['Large Distributor', 'Small Portfolio', 'Boutique Importer', 'Winery Direct']`
    5. `decision_maker_name` — text input
    6. `preferred_communication_style` — `<select>`: `['Formal', 'Casual', 'Terse', 'Detailed']`
    7. `typical_response_days` — `<input type="number" min={1} max={14} />`
    8. `net_payment_terms` — `<select>`: `['Net-7', 'Net-14', 'Net-30', 'Net-45', 'COD']`
    9. `ships_on_days` — multi-checkbox: `['Mon', 'Tue', 'Wed', 'Thu', 'Fri']`
    10. `notes` — `<textarea maxLength={500} />`

    **State:**
    ```typescript
    const [formValues, setFormValues] = useState<Record<string, any>>(initialValues ?? {})
    const [isSaving, setIsSaving] = useState(false)
    ```

    **Layout:**
    - Container: `<div className="bg-white rounded-xl p-6">`
    - Section title: `<h3 className="text-sm font-semibold text-gray-700 uppercase tracking-wide mb-4">Intelligence Profile</h3>`
    - 2-column grid for fields 2-8: `<div className="grid grid-cols-2 gap-4">`
    - Full-width for fields 1, 9, 10: add to grid with `className="col-span-2"`
    - Label style: `<label htmlFor={fieldId} className="block text-sm font-medium text-gray-700 mb-1">`
    - Input style: `className="w-full rounded-lg border border-gray-300 px-3 py-2 text-sm focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"`
    - Select style: same as input

    **Save handler:**
    ```typescript
    const handleSave = async () => {
      setIsSaving(true)
      try {
        await apiClient.patch(`/api/v1/providers/${providerId}/intelligence`, {
          profile_foundational: formValues,
        })
        // Use whatever toast library is in the codebase
        // If react-hot-toast: toast.success('Intelligence profile saved')
        // If sonner: toast.success('Intelligence profile saved')
        // If custom: find existing usage in Providers.tsx and match it
        onSaved?.()
      } catch {
        // toast.error('Failed to save profile')
      } finally {
        setIsSaving(false)
      }
    }
    ```

    **Action row (bottom of form):**
    ```tsx
    <div className="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-200">
      <button type="button" onClick={() => onSaved?.()}
        className="text-gray-600 hover:text-gray-800 font-medium px-4 py-2 rounded-lg border border-gray-300">
        Cancel
      </button>
      <button type="button" onClick={handleSave} disabled={isSaving}
        className="bg-indigo-600 hover:bg-indigo-700 disabled:opacity-50 text-white font-medium px-4 py-2 rounded-lg">
        {isSaving ? 'Saving…' : 'Save Profile'}
      </button>
    </div>
    ```

    **Checkbox group for ships_on_days:**
    ```tsx
    const DAYS = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri']
    <div className="flex gap-3 flex-wrap">
      {DAYS.map((day) => (
        <label key={day} className="flex items-center gap-1.5 text-sm text-gray-700 cursor-pointer">
          <input
            type="checkbox"
            checked={(formValues.ships_on_days ?? []).includes(day)}
            onChange={(e) => {
              const current: string[] = formValues.ships_on_days ?? []
              const updated = e.target.checked
                ? [...current, day]
                : current.filter((d) => d !== day)
              setFormValues({ ...formValues, ships_on_days: updated })
            }}
            className="rounded border-gray-300 text-indigo-600 focus:ring-indigo-500"
          />
          {day}
        </label>
      ))}
    </div>
    ```
  </action>
  <acceptance_criteria>
    - `ls apps/web/src/components/providers/ProviderProfileForm.tsx` → file exists
    - `grep -c "profile_foundational" apps/web/src/components/providers/ProviderProfileForm.tsx` → ≥ 1
    - `grep -c "intelligence" apps/web/src/components/providers/ProviderProfileForm.tsx` → ≥ 1
    - `grep -c "specialty_categories\|primary_region\|distribution_channel\|business_type\|decision_maker_name\|preferred_communication_style\|typical_response_days\|net_payment_terms\|ships_on_days" apps/web/src/components/providers/ProviderProfileForm.tsx` → ≥ 9
    - `grep -c "htmlFor" apps/web/src/components/providers/ProviderProfileForm.tsx` → ≥ 5
  </acceptance_criteria>
  <verify>
    <automated>ls apps/web/src/components/providers/ProviderProfileForm.tsx && grep -c "profile_foundational" apps/web/src/components/providers/ProviderProfileForm.tsx</automated>
  </verify>
  <done>ProviderProfileForm.tsx: 10 fields (6 text/select, 1 number, 1 multi-checkbox, 1 textarea), 2-column grid layout, Save PATCH /api/v1/providers/:id/intelligence with profile_foundational payload, toast feedback, all htmlFor labels. TypeScript compilation clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Draft content display | editedContent initialized from API response, not from notification metadata |
| Disclaimer | Rendered in read-only `<div>`, not textarea — cannot be edited (D-32-08) |
| ProviderProfileForm → API | PATCH scoped to JWT restaurantId by Plan 32-04 endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05-01 | Tampering | Disclaimer section | mitigate | Read-only div; non-removable per D-32-08 UI-SPEC |
| T-32-05-02 | Repudiation | Draft approval | mitigate | useApproveDraft sends modifiedContent → Python agent logs via log_decision() |
| T-32-05-03 | Elevation of Privilege | ProviderProfileForm | mitigate | PATCH /providers/:id/intelligence guarded by JWT (Plan 32-04) |
</threat_model>

<verification>
1. `grep -c "bg-indigo-900" apps/web/src/components/orders/DraftEmailApprovalPanel.tsx` → ≥ 2
2. `grep -c "useApproveDraft\|useDiscardDraft\|useEditDraft\|useGetPendingDraft" apps/web/src/hooks/queries/useDraftEmailQueries.ts` → 4
3. `grep -c "profile_foundational" apps/web/src/components/providers/ProviderProfileForm.tsx` → ≥ 1
4. `cd apps/web && npx tsc --noEmit 2>&1 | grep -c "error TS"` → 0
</verification>

<success_criteria>
- useDraftEmailQueries.ts: 4 exports (useGetPendingDraft, useApproveDraft, useDiscardDraft, useEditDraft)
- DraftEmailApprovalPanel.tsx: indigo-900 header, AnimatePresence, textarea/pre toggle, amber warnings, read-only disclaimer, Send Draft + Discard grid, role=dialog, Escape key
- ProviderProfileForm.tsx: 10 fields, PATCH on save, toast feedback, cancel button
- TypeScript compilation clean
</success_criteria>

<output>
After completion, create `.planning/phases/32-provider-outbound-communication-engine/32-05-SUMMARY.md`
</output>

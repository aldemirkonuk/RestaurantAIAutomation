---
phase: 37-synthetic-restaurant-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - supabase/migrations/20260727230000_sim_ground_truth.sql
  - scripts/synth/auth_personas.py
  - scripts/synth/seed.py
  - scripts/synth/oracle.py
  - env.example
  - services/agent-orchestrator/tests/test_synth_seed_plan.py
  - services/agent-orchestrator/tests/test_synth_auth_personas.py
  - services/agent-orchestrator/tests/test_synth_oracle_schema.py
  - services/agent-orchestrator/tests/test_synth_atomic_seed.py
autonomous: true
requirements:
  - SYNTH-03
  - SYNTH-04
user_setup:
  - service: supabase
    why: "Cloud --apply seed needs service-role + SIM_* Auth personas + optional DATABASE_URL; RPC works via service-role without DSN"
    env_vars:
      - name: SIM_OWNER_EMAIL
        source: "Ops secret store / GH Actions — never commit"
      - name: SIM_OWNER_PASSWORD
        source: "Ops secret store / GH Actions — never commit"
      - name: SIM_MANAGER_EMAIL
        source: "Ops secret store / GH Actions — never commit"
      - name: SIM_MANAGER_PASSWORD
        source: "Ops secret store / GH Actions — never commit"
      - name: SIM_STAFF_EMAIL
        source: "Ops secret store / GH Actions — never commit"
      - name: SIM_STAFF_PASSWORD
        source: "Ops secret store / GH Actions — never commit"
      - name: SUPABASE_URL
        source: "Existing project env"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Existing project env — seed/teardown only"
      - name: DATABASE_URL
        source: "Optional if using direct psycopg2; prefer seed_sim_restaurant RPC when unset"

must_haves:
  truths:
    - "Dry-run seed plan lists org, restaurant, URA roster (owner/manager/staff), menu items, opening inventory counts without mutating cloud"
    - "Three distinct Auth personas ensured via Admin API; URA roles owner/manager/staff per sim restaurant — not one shared login"
    - "sim_ground_truth_runs + sim_ground_truth_facts exist with fact types profile, roster, sku, menu_price, opening_stock, menu_quality_meta"
    - "Atomic seed: oracle failure rolls back live rows (fail-closed); no orphan live restaurant without oracle run"
    - "Seed uses UUID5 restaurant ids + slug sim-*; stock written to restaurant_inventory.stock_live (not inventory_stock)"
    - "Sell prices in menu_items/oracle match snapshot only — never invented"
  artifacts:
    - path: "supabase/migrations/20260727230000_sim_ground_truth.sql"
      provides: "Oracle tables + RLS + seed_sim_restaurant SECURITY DEFINER RPC"
      contains: "sim_ground_truth_runs"
    - path: "scripts/synth/seed.py"
      provides: "Dry-run plan + apply via RPC/TX"
      exports: ["build_seed_plan", "apply_seed"]
    - path: "scripts/synth/auth_personas.py"
      provides: "Ensure three Auth users + public.users mirror"
      exports: ["ensure_personas", "PERSONA_ROLES", "required_env"]
    - path: "scripts/synth/oracle.py"
      provides: "Fact payload builders for Phase 41"
      exports: ["build_run_row", "build_facts"]
  key_links:
    - from: "scripts/synth/seed.py"
      to: "seed_sim_restaurant"
      via: "supabase.rpc or psycopg2 single TX"
      pattern: "seed_sim_restaurant|BEGIN|ROLLBACK"
    - from: "scripts/synth/auth_personas.py"
      to: "/auth/v1/admin/users"
      via: "httpx POST service-role (setup_e2e_anchor pattern)"
      pattern: "admin/users|SIM_OWNER|SIM_MANAGER|SIM_STAFF"
    - from: "scripts/synth/oracle.py"
      to: "sim_ground_truth_facts"
      via: "fact_type payloads"
      pattern: "opening_stock|menu_price|roster|menu_quality"
    - from: "scripts/synth/seed.py"
      to: "scripts/synth/write_set.py"
      via: "plan tables ⊆ SYNTH_WRITE_SET"
      pattern: "SYNTH_WRITE_SET|build_seed_plan"
---

<objective>
Implement **cloud seed + bulletproof oracle + Auth personas**: migration for `sim_ground_truth_*`, dry-run seed plans covering org/restaurant/team/menu/inventory, three distinct SIM_* Auth users with URA roles, and fail-closed atomic seed+oracle (D-08..D-10, D-17..D-19, SYNTH-03/04).

Purpose: Give Phase 38–41 a real seeded world and an exact queryable ledger — without orphan live rows or weak tagged-only “truth.”
Output: migration + `seed.py` / `oracle.py` / `auth_personas.py` + green Nyquist tests for seed plan, personas, oracle schema, atomic rollback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-synthetic-restaurant-engine/37-CONTEXT.md
@.planning/phases/37-synthetic-restaurant-engine/37-RESEARCH.md
@.planning/phases/37-synthetic-restaurant-engine/37-PATTERNS.md
@.planning/phases/37-synthetic-restaurant-engine/37-VALIDATION.md
@.planning/phases/37-synthetic-restaurant-engine/37-01-SUMMARY.md
@.planning/testing/SYNTHETIC-TENANT.md
@services/agent-orchestrator/scripts/setup_e2e_anchor.py
@services/agent-orchestrator/tests/e2e/conftest_prod.py
@apps/api-gateway/src/menus/menus.service.ts
@apps/api-gateway/src/team/team.service.ts
@apps/api-gateway/src/organizations/organizations.service.ts
@scripts/synth/recipes.py
@scripts/synth/snapshots.py
@scripts/synth/ids.py
@scripts/synth/write_set.py

<interfaces>
<!-- Oracle schema (D-08/D-09) — copy RESEARCH SQL exactly into migration: -->
sim_ground_truth_runs(id, restaurant_id FK, archetype_id, seed_version, menu_quality, snapshot_path, snapshot_sha256, params JSONB, sku_count, priced_sku_count, seeded_at, UNIQUE(restaurant_id))
sim_ground_truth_facts(id, run_id FK, restaurant_id FK, fact_type IN (profile, roster, sku, menu_price, opening_stock, menu_quality_meta), sku_key, entity_ref JSONB, payload JSONB, recorded_at)
ENABLE RLS on both; no anon write policies — service-role / SECURITY DEFINER only.

<!-- Atomic transport LOCK (Open Q1 RESOLVED): CREATE FUNCTION seed_sim_restaurant(payload jsonb) RETURNS jsonb LANGUAGE plpgsql SECURITY DEFINER — primary fail-closed TX. Python apply_seed builds payload and calls via supabase.rpc('seed_sim_restaurant', ...). Optional psycopg2 if DATABASE_URL set — same payload builder; secondary only. Never sequential PostgREST multi-insert as the apply path (fail-open). -->

<!-- Seed plan tables (must ⊆ SYNTH_WRITE_SET; library ALWAYS written — W6 lock): organizations, organization_members, restaurants, users (mirror), user_restaurant_access, restaurant_menus, menu_items, restaurant_inventory, master_wine_library, master_wine_library_submissions, sim_ground_truth_runs, sim_ground_truth_facts -->
<!-- Provisional wines: deterministic uuid5(SIM_NS, f"sim.wine.{signature_hash}") + metadata source=sim; teardown sim-filtered only (37-03). -->
<!-- Auth env names (D-19) — document in env.example names only: -->
SIM_OWNER_EMAIL, SIM_OWNER_PASSWORD, SIM_MANAGER_EMAIL, SIM_MANAGER_PASSWORD, SIM_STAFF_EMAIL, SIM_STAFF_PASSWORD

<!-- URA: role in ('owner','manager','staff'); three distinct auth user_ids; reuse across archetypes (D-17/D-18). Never log passwords/JWTs. -->

<!-- Column contracts: restaurant_inventory.stock_live from opening_stock; menu_items bottle_price/by_glass_price from snapshot; Nest addToInventory non-fatal pattern is FORBIDDEN for sim seed. -->

<!-- Do NOT implement CLI --apply gate or teardown sweep here (37-03). Do NOT implement SimPOS (Phase 38). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Oracle migration + fact builders</name>
  <files>supabase/migrations/20260727230000_sim_ground_truth.sql, scripts/synth/oracle.py, services/agent-orchestrator/tests/test_synth_oracle_schema.py</files>
  <behavior>
    - Migration creates sim_ground_truth_runs and sim_ground_truth_facts with CHECK fact_types and indexes from RESEARCH
    - RLS enabled; no policies granting anon/authenticated INSERT/UPDATE/DELETE
    - seed_sim_restaurant(jsonb) SECURITY DEFINER stub or full body starts here (may complete wiring in Task 3) — must exist by end of plan
    - oracle.build_facts(profile, snapshot, roster, opening) emits all six fact_types with Phase-41 payloads (opening_stock, menu_price, roster without passwords, sku, profile, menu_quality_meta)
    - test_synth_oracle_schema asserts required fact types + JSON keys (Nyquist 37-02-03)
  </behavior>
  <action>
    Write migration `20260727230000_sim_ground_truth.sql` per RESEARCH Ground-truth schema (D-08/D-09). Include `seed_sim_restaurant(payload jsonb)` SECURITY DEFINER function skeleton that will perform the atomic write in Task 3 (can start as stub raising 'not implemented' only if Task 3 same plan completes it — **must be callable by end of plan**).

    Implement `oracle.py` payload builders matching RESEARCH payload contracts. Roster facts may include email_domain / role / user_id — **never password**.

    Green `test_synth_oracle_schema.py`. Prefer parsing SQL fixtures or importing oracle builders — do not require live DB for unit schema tests.
  </action>
  <verify>
    <automated>cd services/agent-orchestrator && pytest tests/test_synth_oracle_schema.py -q --tb=line 2>&1 | tail -20</automated>
  </verify>
  <done>Migration + oracle builders exist; SYNTH-04 fact-type verify green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auth personas + dry-run seed plan</name>
  <files>scripts/synth/auth_personas.py, scripts/synth/seed.py, env.example, services/agent-orchestrator/tests/test_synth_auth_personas.py, services/agent-orchestrator/tests/test_synth_seed_plan.py</files>
  <behavior>
    - required_env lists SIM_OWNER_*/SIM_MANAGER_*/SIM_STAFF_* (+ SUPABASE_* for apply) — distinct emails required (D-17/D-19)
    - ensure_personas is idempotent (422 already registered = OK); never prints password or JWT
    - PERSONA_ROLES maps three users → owner/manager/staff for URA
    - build_seed_plan(archetype_id, overrides=None) returns structured dry-run dict: restaurant uuid+slug, tables/row counts for org, ura×3, menu SKUs, inventory opening, oracle run+facts — no network (SYNTH-03)
    - plan tables are subset of SYNTH_WRITE_SET
  </behavior>
  <action>
    Implement `auth_personas.py` copying `setup_e2e_anchor.py` Admin API pattern for **three** users (D-17, D-18, D-19). Mirror `public.users` rows without bcrypt hand-roll. Document env var **names** in root `env.example` (values never committed).

    Implement `seed.py` `build_seed_plan`:
    - Load recipe + snapshot + ids
    - Compute opening stock per SKU from archetype `opening_stock` config (D-07)
    - **Always** plan provisional `master_wine_library` (+ submissions) rows with deterministic UUID5 `sim.wine.*` and `source=sim` metadata (aligned with write_set — not optional)
    - List intended writes for org/restaurant/URA/menu/inventory/library/oracle
    - Default path is dry-run plan only — `apply_seed` may be stubbed until Task 3

    Green `test_synth_auth_personas.py` and `test_synth_seed_plan.py` (Nyquist 37-02-01, 37-02-02). Persona tests can mock httpx; seed plan tests must not need cloud.
  </action>
  <verify>
    <automated>cd services/agent-orchestrator && pytest tests/test_synth_seed_plan.py tests/test_synth_auth_personas.py -q --tb=line 2>&1 | tail -30</automated>
  </verify>
  <done>Dry-run seed plan covers SYNTH-03 surfaces; three-persona mapping tested; secrets names in env.example.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fail-closed atomic seed + oracle</name>
  <files>supabase/migrations/20260727230000_sim_ground_truth.sql, scripts/synth/seed.py, services/agent-orchestrator/tests/test_synth_atomic_seed.py</files>
  <behavior>
    - apply_seed with mocked RPC/conn: when oracle insert raises, entire transaction rolls back — no committed restaurant/menu rows (D-10)
    - successful apply writes live rows + runs + facts in one TX
    - inventory uses restaurant_inventory.stock_live; menu prices from snapshot only
    - re-seed same archetype is idempotent (UNIQUE restaurant_id on runs; upsert/replace inside TX)
  </behavior>
  <action>
    Complete `seed_sim_restaurant` SQL function (or Python psycopg2 TX if explicitly documenting DATABASE_URL-only — **prefer RPC** per RESEARCH A3) so live write-set + oracle commit together.

    `apply_seed(archetype_id, *, apply=False)`:
    - `apply=False` → return build_seed_plan only (D-16 alignment; CLI wiring in 37-03)
    - `apply=True` → ensure_personas then RPC/TX; on failure raise and leave no partial tenant

    **Hard rules:** no Nest `addToInventory` non-fatal continue; no string restaurant PKs; never delete e2e anchor; never implement multi-archetype `--apply` gate here (that is 37-03) but single-archetype apply may exist for unit tests with mocks.

    Green `test_synth_atomic_seed.py` with mocked connection/RPC (Nyquist 37-02-04). Do not require live secrets for unit green.
  </action>
  <verify>
    <automated>cd services/agent-orchestrator && pytest tests/test_synth_oracle_schema.py tests/test_synth_seed_plan.py tests/test_synth_auth_personas.py tests/test_synth_atomic_seed.py -q --tb=line 2>&1 | tail -40</automated>
  </verify>
  <done>Atomic fail-closed seed+oracle proven in unit tests; SYNTH-03/04 wave-2 Nyquist green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service-role / SECURITY DEFINER → cloud DB | Seed may write production-adjacent sim rows; must not touch non-sim tenants |
| Auth Admin API → SIM_* users | Credentials from env only; shared personas across archetypes |
| Oracle tables → Phase 41 readers | Facts must be trustworthy; no anon write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02-01 | Spoofing | sim_ground_truth_* | mitigate | RLS on; no anon write; seed only via SECURITY DEFINER / service-role |
| T-37-02-02 | Tampering | seed.py partial writes | mitigate | Single TX/RPC; atomic_seed test forces oracle failure → rollback |
| T-37-02-03 | Elevation of Privilege | auth_personas | mitigate | Three distinct Auth users; URA role per restaurant; staff≠manager credentials |
| T-37-02-04 | Information Disclosure | ensure_personas / dry-run | mitigate | Never log password/JWT; dry-run prints counts/slugs only |
| T-37-02-05 | Tampering | invented prices in seed | mitigate | menu_items + oracle menu_price from snapshot fields only |
</threat_model>

<verification>
- Wave-2 Nyquist commands from VALIDATION.md green
- Migration file contains both oracle tables + seed_sim_restaurant
- `rg -n "inventory_stock" scripts/synth/seed.py` → 0 matches in code paths (use restaurant_inventory)
</verification>

<success_criteria>
- SYNTH-03: dry-run plan + persona mapping for org/restaurant/team/menu/inventory
- SYNTH-04: dedicated oracle schema + fail-closed atomic seed
- D-08..D-10, D-17..D-19 honored
</success_criteria>

<output>
After completion, create `.planning/phases/37-synthetic-restaurant-engine/37-02-SUMMARY.md`
</output>

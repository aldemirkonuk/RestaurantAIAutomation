---
quick_id: 260407-q0y
description: fix citation input always visible when editing + add production_method and lees_contact_months to Haiku winemaking_details enrichment
date: 2026-04-07
status: complete
---

# Quick Task 260407-q0y

## Tasks

### Task 1 — Citation input always visible in FieldCell
File: `apps/web/src/pages/studio/FieldCell.tsx`
- Remove `showCitation` state and "+ Add citation" button
- Always render the citation URL input when editing
- Add `type="url"` for browser URL validation

### Task 2 — Add winemaking_details to Haiku enrichment
Files: `services/agent-orchestrator/services/haiku_enrichment_service.py`, `services/agent-orchestrator/services/field_confidence.py`
- Add `winemaking_details` to `EnrichmentResult` dataclass
- Add `winemaking_details` prompt block with `production_method` + `lees_contact_months` + `fermentation` + `oak_aging` + `harvest`
- Add `winemaking_details` to `JSONB_ENRICHMENT_KEYS` so it persists via `haiku_tasks.py`

### Task 3 — Fix stale tests
Files: `services/agent-orchestrator/tests/test_haiku_tasks.py`, `services/agent-orchestrator/tests/test_haiku_enrichment_service.py`
- Update `_make_enrichment_result` helpers to use `field_confidence` dict format
- Fix assertions to match current `EnrichmentResult` interface

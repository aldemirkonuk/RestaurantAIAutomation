---
quick_id: 260511-0aa
slug: test-procurement-and-onboarding-emails
description: Add E2E tests for procurement email templates (managerReview, vendorOutbound, conversationSummary, legacy procurement) and onboarding email
date: 2026-05-11
status: planned
---

# Quick Task 260511-0aa: Test Procurement & Onboarding Emails

## Goal

Create a new E2E test file covering all procurement email templates and the onboarding welcome email — the only email paths currently missing test coverage.

## Coverage Gap

| Template | GmailService Method | Currently Tested |
|---|---|---|
| `managerReviewTemplate` | `sendEmail()` raw | ❌ |
| `vendorOutboundTemplate` | `sendEmail()` raw | ❌ |
| `conversationSummaryTemplate` | `sendEmail()` raw | ❌ |
| `orderInquiryTemplate` (legacy) | `sendEmail()` raw | ❌ |
| `counterOfferTemplate` (legacy) | `sendEmail()` raw | ❌ |
| `orderConfirmationTemplate` (legacy) | `sendEmail()` raw | ❌ |
| `deliveryReminderTemplate` (legacy) | `sendEmail()` raw | ❌ |
| `onboardingEmailTemplate` | `sendOnboardingEmail()` | ❌ |

## Task 1 — Create `procurement-email.e2e.spec.ts`

**File:** `apps/api-gateway/src/communications/tests/procurement-email.e2e.spec.ts`

**What to implement:**

1. Bootstrap same NestJS testing module as `email-e2e.spec.ts` (ConfigModule + GmailService)
2. Procurement templates (3 tests via `sendEmail()` + rendered template HTML):
   - Manager Review email — internal email to manager with AI draft + 4 action buttons
   - Vendor Outbound email — plain business-letter email sent to vendor
   - Conversation Summary email — post-vendor-reply digest for manager
3. Legacy procurement templates (4 tests — all vendor-facing, render via `email-templates-legacy.ts`):
   - Order Inquiry — initial wine inquiry to vendor
   - Counter Offer — negotiation counter-offer
   - Order Confirmation — PO confirmation to vendor
   - Delivery Reminder — follow-up on upcoming delivery
4. Onboarding email (1 test via `sendOnboardingEmail()`):
   - Welcome email post-registration with restaurant branding

**Pattern to follow:**
- Same opt-in guard as `email-e2e.spec.ts` (relies on Gmail credentials from .env)
- Send to `aldemirkonuk2004@gmail.com` (same test inbox)
- Each test: `expect(result.success).toBe(true)` + `expect(result.messageId).toBeTruthy()`
- Console log with `[N/8]` prefix for progress tracking

**Imports needed:**
- `managerReviewTemplate`, `vendorOutboundTemplate`, `conversationSummaryTemplate` from `../email-templates/vendor-action.template`
- `orderInquiryTemplate`, `counterOfferTemplate`, `orderConfirmationTemplate`, `deliveryReminderTemplate` from `../email-templates-legacy`
- `GmailService, EmailResult` from `../gmail.service`

**Run command:**
```bash
cd apps/api-gateway && npx jest --config jest.config.js --testPathPattern procurement-email --runInBand
```

**files:**
- `apps/api-gateway/src/communications/tests/procurement-email.e2e.spec.ts` (create)

**done:** File exists and `npx jest --testPathPattern procurement-email --runInBand --passWithNoTests` passes (may need Gmail creds to actually send)

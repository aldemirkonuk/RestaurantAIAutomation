---
quick_id: 260502-00z
description: analyze phase 25 plans for mistakes and future problems
date: 2026-05-02
type: analysis
---

# Analysis: Phase 25 Plans Review

Produce a structured findings report at 260502-00z-SUMMARY.md covering all bugs, risks,
and future problems found across Plans 25-01 through 25-07.

## Findings to document

### CRITICAL (will cause runtime failures)
1. `extras=` vs `extra=` in sentry_sdk.capture_message (conftest_prod.py, Plan 25-02)
2. `asyncio.get_event_loop().time()` in coroutines — deprecated/broken in Python 3.10+ (Plans 25-04, 25-05)

### HIGH (will cause test failures or data problems)
3. Wave C creates 9 separate RabbitMQ connections — may exhaust CloudAMQP free tier
4. `E2E_TABLES` teardown list is incomplete — agent-written tables not covered

### MEDIUM (quality/reliability issues)
5. Dead import of `_extract_wave` + redundant inner-function import in wave_c (Plan 25-04)
6. Wave BC combined XML — B vs C failures indistinguishable in cascading report (Plan 25-07)
7. wave_f.xml copy step silently ignores missing Playwright JUnit XML (Plan 25-07)
8. Wave E/G skip behavior on 30s polling timeout may mask real CalendarAgent regression

### FIXES NEEDED
Document exact file locations and minimal code changes for each finding.

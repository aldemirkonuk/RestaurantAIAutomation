# Quick Task 260502-06m: Fix Phase 25 Execution Issues

**Date:** 2026-05-02
**Source analysis:** 260502-00z

## Status: COMPLETE — all 10 findings fixed in PLAN.md templates

Phase 25 had NOT been executed at the time of this fix (only planning phase complete).
All fixes were applied to the 6 PLAN.md files so the executor will generate correct code.

## Fixes Applied

### CRITICAL (2/2)

**C-01 — Plan 25-02** `extras=` → `extra=` in `sentry_sdk.capture_message`
- File: `25-02-PLAN.md`, teardown_e2e_records Sentry call
- Change: 1 keyword argument corrected

**C-02 — Plans 25-04 + 25-05** `asyncio.get_event_loop()` → `asyncio.get_running_loop()`
- Files: 4 polling functions across wave_c, wave_d, wave_e, wave_g
- Pattern: `loop = asyncio.get_running_loop(); deadline = loop.time() + N`

### HIGH (2/2)

**H-01 — Plan 25-04** Wave C RabbitMQ connection exhaustion
- Added `rabbitmq_connection` session-scoped async fixture (single shared connection)
- `publish_test_message` now accepts `connection:` parameter instead of opening its own
- `TestAgentTriggers.test_agent_survives_test_message` uses shared fixture
- `test_rabbitmq_connection_reachable` checks `connection.is_closed` — no extra connection

**H-02 — Plan 25-02** E2E_TABLES incomplete coverage
- Added audit comment above E2E_TABLES list instructing executor to check all agent source files

### MEDIUM (4/4)

**M-01 — Plan 25-04** Dead `_extract_wave` imports in wave_c removed

**M-02 — Plan 25-07** `wave_bc.xml` → separate `wave_b.xml` + `wave_c.xml`
- CI step now runs both as background processes (`&`) with individual `--junitxml` paths
- `cascading_report.py` `wave_files` dict updated to map "B" and "C" individually
- `find_failed_waves` no longer excludes "BC"
- `report` dict no longer filters out "BC"
- `wave_bc.xml` acceptance criteria updated to check 7 separate XML refs

**M-03 — Plan 25-07** Synthetic failure XML when wave_f.xml missing
- CI step uses `if: always()` + shell conditional
- Missing XML → creates valid JUnit XML with failure testcase (ensures cascading report counts it)
- Present XML → normal copy as before

**M-04 — Plan 25-05** Wave G upsert assertion before timing skip
- Added Supabase query to verify `e2e-cal-001` exists after upsert (hard assertion, not timing-dependent)
- CalendarAgent scheduling check still uses `pytest.skip` on timeout (timing-dependent part)
- Result: DB errors always fail; slow CalendarAgent scans still skip cleanly

### LOW (2/2)

**L-01 — Plan 25-07** `sentry_sdk.flush(2)` added after `capture_message` in CI inline Python step

**L-02 — Plan 25-01** `pytest-asyncio>=0.23.0` added to requirements.test.txt content block
- Also updated Task 2 acceptance criteria to expect 4 deps (not 3)

## Files Modified

- `.planning/phases/25-production-e2e-test-suite/25-01-PLAN.md` — L-02
- `.planning/phases/25-production-e2e-test-suite/25-02-PLAN.md` — C-01, H-02
- `.planning/phases/25-production-e2e-test-suite/25-03-PLAN.md` — M-02 (wave_bc.xml ref)
- `.planning/phases/25-production-e2e-test-suite/25-04-PLAN.md` — C-02, H-01, M-01
- `.planning/phases/25-production-e2e-test-suite/25-05-PLAN.md` — C-02, M-04
- `.planning/phases/25-production-e2e-test-suite/25-07-PLAN.md` — M-02, M-03, L-01

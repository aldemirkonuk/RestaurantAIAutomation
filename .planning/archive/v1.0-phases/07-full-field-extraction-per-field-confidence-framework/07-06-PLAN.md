---
phase: 07-full-field-extraction-per-field-confidence-framework
plan: 06
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - services/agent-orchestrator/tests/test_field_confidence.py
autonomous: true
requirements: [FCONF-12]

must_haves:
  truths:
    - "E2E test extracts a known menu and verifies field_confidence JSONB populated for 18+ fields"
    - "Test verifies 3-tier threshold routing: rejected/review/accepted fields correctly routed"
    - "Test verifies field_review_queue contains only sub-threshold fields"
  artifacts:
    - path: "services/agent-orchestrator/tests/test_field_confidence.py"
      provides: "Unit tests for field_confidence module + E2E integration test"
      contains: "test_build_field_confidence"
  key_links:
    - from: "test_field_confidence.py"
      to: "field_confidence.py"
      via: "import all functions"
      pattern: "from services.field_confidence import"
---

<objective>
Create comprehensive tests for the field confidence framework: unit tests for field_confidence.py helpers + integration test verifying the full extraction-to-review pipeline.

Purpose: Verify FCONF-12 — end-to-end field confidence flow works correctly.
Output: test_field_confidence.py with unit + integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-full-field-extraction-per-field-confidence-framework/07-CONTEXT.md
@.planning/phases/07-full-field-extraction-per-field-confidence-framework/07-02-SUMMARY.md
@.planning/phases/07-full-field-extraction-per-field-confidence-framework/07-03-SUMMARY.md
@services/agent-orchestrator/services/field_confidence.py
@services/agent-orchestrator/services/claude_vision_extractor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_field_confidence.py with unit tests + E2E integration test</name>
  <files>services/agent-orchestrator/tests/test_field_confidence.py</files>
  <read_first>
    services/agent-orchestrator/services/field_confidence.py,
    services/agent-orchestrator/services/claude_vision_extractor.py,
    services/agent-orchestrator/tests/test_image_menu.py
  </read_first>
  <action>
Create `services/agent-orchestrator/tests/test_field_confidence.py` with these tests:

**Unit tests for field_confidence.py helpers:**

1. `test_build_field_confidence_nested_format`: Input a wine dict with nested `{value, confidence, source}` fields. Assert output preserves confidence and source values exactly.

2. `test_build_field_confidence_flat_format`: Input a wine dict with flat values (legacy format: `{"wine_name": "Barolo", "vintage": 2019}`). Assert output wraps each in `{value, confidence: 0.5, source: "visible"}`.

3. `test_merge_field_confidence_higher_wins`: Existing fc has `region` at confidence 0.6 from "inferred". New fc has `region` at confidence 0.85 from "knowledge". Assert merged result has the 0.85 entry.

4. `test_merge_field_confidence_lower_kept`: Existing fc has `wine_name` at confidence 0.98 from "visible". New fc has `wine_name` at confidence 0.7 from "knowledge". Assert merged result keeps 0.98 entry.

5. `test_merge_field_confidence_fills_gaps`: Existing fc has `wine_name` only. New fc has `food_pairing` at 0.8. Assert merged has both.

6. `test_route_fields_3_tiers`: Build fc with:
   - `wine_name`: confidence 0.97 (should be accepted)
   - `sub_region`: confidence 0.65 (should be in review)
   - `sweetness_level`: confidence 0.3 (should be rejected)
   Assert accepted contains wine_name value, review list has sub_region entry, rejected has sweetness_level=None.

7. `test_route_fields_review_also_persisted`: Fields in review tier (0.5-0.8) should appear in BOTH accepted dict AND review list. Assert sub_region value is in accepted AND review.

8. `test_compute_completeness_from_fc`: Build fc with 4 fields at confidences [0.9, 0.8, 0.6, 0.4]. Assert completeness = round((0.9+0.8+0.6+0.4)/4, 3) = 0.675.

9. `test_should_auto_block_mostly_bad`: Build fc with 12 fields all at confidence 0.3. Assert should_auto_block returns True.

10. `test_should_auto_block_mostly_good`: Build fc with 16 good fields (0.9) and 2 bad fields (0.3). Assert should_auto_block returns False (2/18 = 11% < 50%).

**Integration test (mocked Claude API):**

11. `test_e2e_extraction_to_review_queue`: Using unittest.mock to mock the Anthropic API response:
    - Create a mock Claude response returning 3 wines in nested confidence format
    - Wine 1: all fields > 0.8 (should have no review items)
    - Wine 2: wine_name 0.95, sub_region 0.55, appellation 0.45 (should have 1 review item for sub_region, appellation rejected/NULL)
    - Wine 3: most fields < 0.5 (should be auto_blocked)
    - Call `build_field_confidence()` for each wine
    - Call `route_fields_by_threshold()` for each wine
    - Call `should_auto_block()` for each wine
    - Assert Wine 1: 0 review fields, not blocked
    - Assert Wine 2: sub_region in review, appellation in rejected, not blocked
    - Assert Wine 3: auto_blocked = True

Run with: `cd services/agent-orchestrator && python -m pytest tests/test_field_confidence.py -v`
  </action>
  <verify>
    <automated>cd /Users/aldemirkonuk/Desktop/UnicornProjects/Restaurant\ AI\ Automation/services/agent-orchestrator && python -m pytest tests/test_field_confidence.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "test_build_field_confidence_nested_format" services/agent-orchestrator/tests/test_field_confidence.py returns match
    - grep "test_route_fields_3_tiers" services/agent-orchestrator/tests/test_field_confidence.py returns match
    - grep "test_should_auto_block" services/agent-orchestrator/tests/test_field_confidence.py returns match
    - grep "test_e2e_extraction_to_review_queue" services/agent-orchestrator/tests/test_field_confidence.py returns match
    - pytest runs and all tests pass (11 tests)
  </acceptance_criteria>
  <done>11 tests covering all field_confidence helpers + E2E integration scenario. All tests pass. FCONF-12 satisfied.</done>
</task>

</tasks>

<verification>
- All 11 tests pass with pytest
- Tests cover: build, merge, route, completeness, auto_block, and E2E flow
- No external API calls (all mocked)
</verification>

<success_criteria>
- E2E test verifies field_confidence JSONB populated for 18+ fields (FCONF-12)
- E2E test verifies fields routed correctly through 3-tier threshold (FCONF-12)
- E2E test verifies review queue contains only sub-threshold fields (FCONF-12)
- All tests pass in CI-compatible manner (no live API keys needed)
</success_criteria>

<output>
After completion, create `.planning/phases/07-full-field-extraction-per-field-confidence-framework/07-06-SUMMARY.md`
</output>

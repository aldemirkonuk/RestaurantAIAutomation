---
phase: 12.1-research-agent-sota-redesign-three-layer-architecture
plan: "04"
subsystem: testing
tags: [tests, unit-tests, e2e, three-layer, research-agent, phase-12.1]
dependency_graph:
  requires: [12.1-01, 12.1-03]
  provides: [test-coverage-for-12.1-helpers, test-coverage-for-three-layer-e2e]
  affects: [services/agent-orchestrator/tests/]
tech_stack:
  added: []
  patterns: [pytest-parametrize, unittest.mock.patch, AsyncMock, yield-fixture-teardown]
key_files:
  modified:
    - services/agent-orchestrator/tests/test_research_agent_helpers.py
    - services/agent-orchestrator/tests/test_research_agent_e2e.py
decisions:
  - "Added 17 new tests (not 12 minimum) to maximize coverage of all D-01→D-08 decisions"
  - "test_resolve_conflict_escalates_b_vs_b added beyond plan spec to cover edge case"
  - "E2E mock_extract_candidates updated with model_tier kwarg to match Plan 03 signature change"
metrics:
  duration_minutes: 12
  completed_date: "2026-04-06"
  tasks_completed: 2
  tasks_total: 2
  files_modified: 2
---

# Phase 12.1 Plan 04: Test Suite for Three-Layer Architecture Summary

**One-liner:** 17 new unit tests covering Layer 1 inference, model cascade routing, BoundedLRUCache, conflict resolution, and deep-merge; E2E test updated with Layer 1 mock and ontology_inference source acceptance.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add ≥12 unit tests for Phase 12.1 helpers | `1504866` | test_research_agent_helpers.py |
| 2 | Update E2E test for three-layer pipeline | `02ac273` | test_research_agent_e2e.py |

## What Was Built

### Task 1: Unit Tests (test_research_agent_helpers.py)

**New imports added:** `run_layer1_inference`, `select_model`, `resolve_conflict`, `merge_conflict_candidates`, `BoundedLRUCache`, `_should_infer`, `_cache_key_wine`, `_cache_key_producer`, `HAIKU_FIELDS`, `SONNET_ESCALATION_FIELDS`, `get_entity_cache`, `put_entity_cache`

**17 new test functions (24 original → 41 total):**

| Test | Covers |
|------|--------|
| `test_run_layer1_inference_returns_empty_when_no_ontology` | T-12.1-01 graceful degradation |
| `test_run_layer1_inference_skips_high_confidence_fields` | D-08 high-conf skip |
| `test_run_layer1_inference_infers_when_field_absent` | `_should_infer` logic |
| `test_select_model_haiku_for_color` | D-02 HAIKU_FIELDS set |
| `test_select_model_flash_default` | D-02 flash default |
| `test_select_model_sonnet_on_retry` | D-02 attempt≥2 escalation |
| `test_select_model_haiku_high_ontology_hint` | D-02 ontology hint threshold |
| `test_bounded_lru_cache_eviction` | D-04/bug-10 LRU eviction |
| `test_bounded_lru_cache_ttl_expiry` | D-04 TTL logic |
| `test_resolve_conflict_auto_tier_a_vs_c` | D-05 auto-resolve Rule 1 |
| `test_resolve_conflict_escalates_a_vs_a` | D-05 human escalation Rule 4 |
| `test_resolve_conflict_b_consensus` | D-05 auto-resolve Rule 3 |
| `test_resolve_conflict_escalates_b_vs_b` | D-05 human escalation Rule 5 |
| `test_merge_conflict_candidates_deep_merge` | D-06 bug#3 deep merge |
| `test_merge_conflict_candidates_deduplicates` | D-06 bug#3 dedup |
| `test_cache_key_wine_deterministic` | D-04 SHA-256 determinism |
| `test_cache_key_producer_normalization` | D-04 normalization |

**Result:** `41 passed in 0.04s` — zero regressions.

### Task 2: E2E Test Updates (test_research_agent_e2e.py)

1. **`_mock_layer1_inference()`** — new module-level function; returns `country=Italy` and `region=Tuscany` with `source="ontology_inference", confidence=0.99`
2. **`_mock_extract_candidates` signature** — added `model_tier: str = "flash"` keyword argument to accept D-02 cascade dispatch call
3. **Mock patch list** — added `patch("jobs.research_tasks.run_layer1_inference", new=_mock_layer1_inference)` to the `with (...)` block; now all 4 I/O boundaries are mocked
4. **`research_filled` filter** — updated to `entry.get("source") in ("research_agent", "ontology_inference")` so Layer 1 fills count toward the ≥3 assertion
5. **Docstring** — updated to describe three-layer pipeline with all 4 mock boundaries documented

**E2E collection verified:** `1 test collected` — no import errors.

## Deviations from Plan

### Auto-additions (Rule 2)

**1. [Rule 2 - Missing coverage] test_resolve_conflict_escalates_b_vs_b**
- **Found during:** Task 1
- **Issue:** Plan listed only 3 resolve_conflict tests; Tier-B vs Tier-B escalation (D-05 Rule 5) had no test despite being a separate code path
- **Fix:** Added `test_resolve_conflict_escalates_b_vs_b` — a 4th conflict test covering the B-vs-B human escalation path
- **Files modified:** test_research_agent_helpers.py
- **Commit:** 1504866

## Known Stubs

None — test files only. No production stubs introduced.

## Threat Flags

None — test-only changes. No new network endpoints, auth paths, or DB schema changes.

## Self-Check: PASSED

- [x] `services/agent-orchestrator/tests/test_research_agent_helpers.py` — FOUND
- [x] `services/agent-orchestrator/tests/test_research_agent_e2e.py` — FOUND
- [x] Commit `1504866` — FOUND (`test(12.1-04): add 17 unit tests for Phase 12.1 helper functions`)
- [x] Commit `02ac273` — FOUND (`test(12.1-04): update E2E test for three-layer pipeline architecture`)
- [x] 41 tests pass — VERIFIED (`41 passed in 0.04s`)
- [x] E2E test collectable — VERIFIED (`1 test collected`)

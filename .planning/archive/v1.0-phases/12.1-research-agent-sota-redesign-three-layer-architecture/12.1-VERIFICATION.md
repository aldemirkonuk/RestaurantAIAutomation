---
phase: 12.1-research-agent-sota-redesign-three-layer-architecture
verified: 2026-04-06T22:30:00Z
status: human_needed
score: 16/16 must-haves verified
re_verification: false
human_verification:
  - test: "Start the full Celery worker stack and submit a real wine submission with 5+ NULL fields to research_agent_task(). Inspect research_run_stats after completion."
    expected: "null_rate_after < null_rate_before, time_to_fill_hours is non-zero, regression_blocked_count is 0 or a small integer. Layer 1 fills ≥1 field via ontology_inference source."
    why_human: "Requires live Supabase, Redis, Serper API key, and Anthropic/Gemini keys. Cannot mock the three-layer interaction in a static code check."
  - test: "POST /api/v1/research/trigger without X-Admin-Key header."
    expected: "HTTP 401 Unauthorized with {'detail': 'Invalid or missing X-Admin-Key'}."
    why_human: "Requires a running FastAPI server. Runtime auth enforcement is not statically verifiable."
  - test: "GET /api/v1/research/metrics after running at least one research task."
    expected: "Response contains all 5 metric categories (gap_closure, quality, evidence_hygiene, throughput_cost, safety). evidence_hygiene.fetch_verify_pass_rate_by_tier has keys A, B, C with float values. safety.regression_rate is a computed float, not 0.0 literal."
    why_human: "Requires live Supabase with populated research_run_stats, evidence_citations, and research_runs tables."
  - test: "GET /api/v1/research/challenges?status=open after a tier-A contradiction is triggered in _process_record."
    expected: "Response includes the challenge row from resolution_challenges table with all fields populated."
    why_human: "Requires live Supabase and a wine submission where tier-A fetch-verified evidence contradicts an existing human_resolved field."
---

# Phase 12.1: Research Agent SOTA Redesign — Verification Report

**Phase Goal:** Redesign the research agent with a three-layer SOTA architecture — Layer 1 deterministic ontology inference, Layer 2 cascade LLM enrichment (haiku/flash/sonnet routing), Layer 3 Reflexion-based deep research with adaptive retry + plateau detection — plus fix all 10 critical bugs, implement hierarchical entity caching, authority-weighted conflict resolution, API authentication, and comprehensive tests.

**Verified:** 2026-04-06T22:30:00Z  
**Status:** HUMAN_NEEDED  
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Layer 1 ontology inference fills appellation-derived fields at confidence 0.99 | ✓ VERIFIED | `run_layer1_inference()` in helpers.py:558–680; fills grape_variety, country, region, sub_region, color with `confidence=0.99, source="ontology_inference"` |
| 2 | Layer 1 checks vintage plausibility and flags implausible vintages | ✓ VERIFIED | helpers.py:656–678; validates year 1800–current+1 and checks `appellation_rules.first_vintage` |
| 3 | `select_model()` routes fields to haiku/flash/sonnet by field difficulty and attempt | ✓ VERIFIED | helpers.py:691–712; `select_model("color")=="haiku"`, `select_model("region")=="flash"`, `select_model("region", attempt=2)=="sonnet"` — confirmed by test + runtime check |
| 4 | Entity cache supports Redis (if REDIS_URL set) with fallback to bounded in-memory LRU | ✓ VERIFIED | helpers.py:510–541; `get_entity_cache` checks Redis first, falls back to `_memory_cache` OrderedDict; `put_entity_cache` writes to both |
| 5 | Ontology lookups cached in `_memory_cache` using `_cache_key_region` keys (D-04 Tier 3) | ✓ VERIFIED | helpers.py:581–602; checks `_memory_cache.get(cache_key)` before ontology DB call; stores result under `_cache_key_region(appellation)` |
| 6 | `resolve_conflict()` auto-resolves tier-A vs tier-C/B conflicts; escalates ambiguous ones | ✓ VERIFIED | helpers.py:719–787; Rules 1-3 auto-resolve, Rules 4-7 escalate to human; confirmed by 4 unit tests |
| 7 | `merge_conflict_candidates()` deep-merges instead of replacing (bug #3 fixed) | ✓ VERIFIED | helpers.py:794–813; preserves existing field keys and deduplicates by source_url |
| 8 | In-process URL cache is bounded (max 1000 entries, LRU eviction) — bug #10 fixed | ✓ VERIFIED | research_tasks.py:130; `_url_cache = BoundedLRUCache(max_size=1000)` |
| 9 | POST /trigger requires X-Admin-Key header matching ADMIN_API_KEY — bug #7 fixed | ✓ VERIFIED | research_routes.py:34,501; `verify_admin_token()` with `Depends(verify_admin_token)` on trigger endpoint |
| 10 | Metrics endpoint uses bounded queries (.limit(10000)) — bug #9 fixed | ✓ VERIFIED | research_routes.py:131,144,157,171; all 4 queries (stats, citations, runs, corrections) have `.limit(10000)` |
| 11 | `fetch_verify_pass_rate_by_tier` A/B/C breakdown in metrics response (D-07) | ✓ VERIFIED | research_routes.py:242–279; tier_verified/tier_total dicts computed, included in evidence_hygiene response |
| 12 | GET /challenges returns open resolution_challenges with submission context (D-07) | ✓ VERIFIED | research_routes.py:452–499; queries `resolution_challenges` table, paginated by status/limit/offset |
| 13 | Celery beat has staleness_reverify_task scheduled weekly at 2 AM Sunday (D-07) | ✓ VERIFIED | celery_app.py:111–114; `crontab(day_of_week=0, hour=2, minute=0)`, task name matches research_tasks.py:1355 |
| 14 | regression_rate computed from actual data, not hardcoded 0.0 — bug #6 partial metrics side | ✓ VERIFIED | research_routes.py:309; `sum(regression_blocked_count) / sum(fields_targeted)` via `_safe_div` |
| 15 | Three-layer `_process_record` executes Layer 1 → Layer 2 → Layer 3 in sequence | ✓ VERIFIED | research_tasks.py:588 (Layer 1: `run_layer1_inference`), 644 (Layer 2: `select_model`), 875 (Layer 3 Reflexion loop); each layer processes remaining gaps |
| 16 | All tests pass: 41 unit tests (24 original + 17 new) + E2E collectable | ✓ VERIFIED | `41 passed in 0.05s`; E2E: `1 test collected in 0.00s` — confirmed live |

**Score: 16/16 truths verified**

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `services/agent-orchestrator/config/settings.py` | Cascade routing + cache + Reflexion settings | ✓ VERIFIED | 10 new attributes: `research_cascade_haiku_model`, `research_cascade_flash_model`, `research_cascade_sonnet_model`, `redis_url`, `research_cache_wine_ttl_days`, `research_cache_producer_ttl_days`, `research_cache_max_memory_entries`, `research_max_reflexion_retries` |
| `services/agent-orchestrator/services/research_agent_helpers.py` | Layer 1 inference, select_model, entity cache, resolve_conflict, merge_conflict_candidates | ✓ VERIFIED | +365 lines; all 7 new exports present and substantive |
| `services/agent-orchestrator/api/research_routes.py` | Auth-protected trigger, aggregate metrics, per-tier pass rate, challenges endpoint | ✓ VERIFIED | `verify_admin_token`, `.limit(10000)`, `fetch_verify_pass_rate_by_tier`, `get_research_challenges` all present |
| `services/agent-orchestrator/jobs/celery_app.py` | staleness_reverify_task beat schedule entry | ✓ VERIFIED | `research-staleness-reverify-weekly` with correct crontab and task name |
| `services/agent-orchestrator/jobs/research_tasks.py` | Three-layer research pipeline, staleness task, all D-06 bug fixes | ✓ VERIFIED | `_run_layer1` pattern present; 16/16 acceptance checks passed per 03-SUMMARY self-check |
| `services/agent-orchestrator/tests/test_research_agent_helpers.py` | Unit tests for Plan 01 new functions | ✓ VERIFIED | `test_run_layer1_inference_returns_empty_when_no_ontology` + 16 more; 41 total tests pass |
| `services/agent-orchestrator/tests/test_research_agent_e2e.py` | Updated E2E test for three-layer pipeline | ✓ VERIFIED | `_mock_layer1_inference`, patch for `run_layer1_inference`, `ontology_inference` in source filter, "three-layer" in docstring |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `research_agent_helpers.py` | `ontology_normalization.py` | `run_layer1_inference` imports inside try/except | ✓ WIRED | helpers.py:588–597; deferred import avoids test-env crashes |
| `research_agent_helpers.py` | `config/settings.py` | `select_model` reads cascade model names via settings | ✓ WIRED | helpers.py:691–712 uses `HAIKU_FIELDS`; settings has the model strings (used in research_tasks.py) |
| `research_routes.py` | `resolution_challenges` table | GET /challenges queries table | ✓ WIRED | research_routes.py:473,481; `supabase.table("resolution_challenges")` |
| `celery_app.py` | `research_tasks.py` | beat schedule references `research.staleness_reverify` | ✓ WIRED | celery_app.py:112 → research_tasks.py:1355; task name matches exactly |
| `research_tasks.py` | `research_agent_helpers.py` | imports `run_layer1_inference, select_model, resolve_conflict, BoundedLRUCache` | ✓ WIRED | research_tasks.py:69–87; import block with all required functions |
| `research_tasks.py` | `resolution_challenges` table | Layer 3 writes challenges when tier-A contradicts human_resolved | ✓ WIRED | research_tasks.py:768; `supabase.table("resolution_challenges").insert(...)` |
| `test_research_agent_helpers.py` | `research_agent_helpers.py` | imports and tests all new functions | ✓ WIRED | test file:33–60; imports all 12 new exports; 17 tests pass |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `_process_record` (research_tasks.py) | `layer1_fills` | `run_layer1_inference(existing_fc, ...)` calling ontology DB | ✓ Real data (or empty dict on DB error) | ✓ FLOWING |
| `_process_record` (research_tasks.py) | `entity_cache` result | `get_entity_cache(key, redis_client)` → Redis or `_memory_cache` | ✓ Real cached data or None | ✓ FLOWING |
| `get_research_metrics` (research_routes.py) | `fetch_verify_pass_rate_by_tier` | Single-pass loop over `cit_rows` from `evidence_citations` table | ✓ DB-computed per tier | ✓ FLOWING |
| `get_research_metrics` (research_routes.py) | `regression_rate` | `sum(regression_blocked_count)` from `research_run_stats` rows | ✓ DB-aggregated (0.0 via `_safe_div` when no rows) | ✓ FLOWING |
| `staleness_reverify_task` (research_tasks.py) | `stale_citations` | `evidence_citations` table query with `retrieved_at < cutoff` | ✓ DB query with 180-day cutoff | ✓ FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `select_model("color")` returns "haiku" | `python3 -c "from services.research_agent_helpers import select_model; print(select_model('color'))"` | `haiku` | ✓ PASS |
| `select_model("region", attempt=2)` returns "sonnet" | inline python3 | `sonnet` | ✓ PASS |
| BoundedLRUCache(3) evicts on 4th insert | inline python3 | `len==3, a==None, d==4` | ✓ PASS |
| All 41 unit tests pass | `pytest tests/test_research_agent_helpers.py` | `41 passed in 0.05s` | ✓ PASS |
| E2E test is collectable | `pytest tests/test_research_agent_e2e.py --collect-only` | `1 test collected` | ✓ PASS |
| Settings importable with all new attributes | inline python3 | `Settings OK` | ✓ PASS |
| Routes importable with /challenges present | inline python3 | `Routes OK: [..., '/api/v1/research/challenges', ...]` | ✓ PASS |
| Celery beat entry present with correct task name | inline python3 | `Celery beat OK` | ✓ PASS |
| staleness_reverify_task.name matches beat schedule | inline python3 | `Tasks OK` | ✓ PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| D-01 | Plans 01, 03 | Three-layer architecture: Layer 1 deterministic, Layer 2 cascade, Layer 3 Reflexion | ✓ SATISFIED | `run_layer1_inference` in helpers; three sequential layers in `_process_record` |
| D-02 | Plans 01, 03 | Model cascade: Haiku/Flash/Sonnet routing by field difficulty and attempt | ✓ SATISFIED | `select_model()` in helpers; `model_tier` param in `_extract_field_candidates`; settings has model names |
| D-03 | Plans 01, 03 | Adaptive Reflexion retry with plateau detection; `research_exhausted` marking | ✓ SATISFIED | research_tasks.py:875–991; plateau detection at:931; `research_exhausted` at:991 |
| D-04 | Plans 01, 03 | Hierarchical entity caching: Redis + in-memory LRU for wine/producer/region | ✓ SATISFIED | `get_entity_cache`/`put_entity_cache` in helpers; wired in `_research_async` + `_process_record` |
| D-05 | Plans 01, 03 | Authority-weighted conflict resolution: auto-resolve tier-A vs tier-C; escalate tier-A vs tier-A | ✓ SATISFIED | `resolve_conflict()` in helpers; called in `_process_record` Layer 2 |
| D-06 (bugs #1–#10) | Plans 01, 02, 03 | All 10 critical bug fixes | ✓ SATISFIED | Bug #1: tier-correct promotion; #2: all-provider budget; #3: deep-merge; #4: Redis budget flag; #5: time_to_fill_hours; #6: regression_blocked_count; #7: X-Admin-Key auth; #8: DNS resolution; #9: .limit(10000); #10: BoundedLRUCache |
| D-07 | Plans 02, 03 | Staleness re-verification, resolution challenges, per-tier metrics, GET /challenges | ✓ SATISFIED | `staleness_reverify_task`; `resolution_challenges` insert; `get_research_challenges` endpoint; `fetch_verify_pass_rate_by_tier` |
| D-08 | Plan 01, 03 | Sub_region from appellation inference; high-confidence fields not overwritten | ✓ SATISFIED | helpers.py:636–640 (sub_region fill); `_should_infer()` prevents overwriting fields with conf ≥ 0.8 |

**Note on requirement ID namespace:** D-01 through D-08 are internal design decisions defined in `12.1-CONTEXT.md`, not rows in `REQUIREMENTS.md`. `REQUIREMENTS.md` uses RSCH-01..11 for Phase 12. Phase 12.1 is a redesign sub-phase and inherits Phase 12's RSCH requirements. All RSCH-01..11 remain open in REQUIREMENTS.md traceability table (Phase 12.1 improves the architecture but the RSCH completion tracking is under Phase 12's parent milestone work).

**RSCH requirements advanced by this phase:**
- RSCH-01 (gap closure): three-layer pipeline improves null field fill rate
- RSCH-02 (citation requirement): Layer 1 fills get `source_url="ontology://phase9"` citation records
- RSCH-03 (corroboration rate): conflict auto-resolution + tier-A authority improves quality
- RSCH-05 (conflict detection): `resolve_conflict()` and `resolution_challenges` table now wired
- RSCH-07 (budget cap): bug #2 fixed (all-provider sum), Redis budget flag (bug #4)
- RSCH-08 (attempts bounded): three-layer reduces wasted Serper calls via Layer 1 + cache
- RSCH-09 (regression rate): `check_regression_guard` + `regression_blocked_count` tracking (bugs #5, #6)
- RSCH-10 (metrics endpoint): `fetch_verify_pass_rate_by_tier`, computed `regression_rate` (bugs #6, #9)
- RSCH-11 (E2E test): updated for three-layer with `_mock_layer1_inference` and `ontology_inference` source

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `research_agent_helpers.py:583` | `_memory_cache.get(cache_key)` returns tuple `(value, ts)` — the TTL check done by `get_entity_cache` is **NOT applied** in the Tier 3 cache path | ℹ️ Info | Tier 3 ontology entries never expire from `_memory_cache` (only evicted by LRU overflow). This is intentional per 01-SUMMARY decision note ("no TTL filtering at read time for appellation rules"), but means stale ontology data could persist until memory pressure forces eviction. Acceptable for static ontology data. |
| `research_routes.py` | `GET /challenges` has no auth protection (`verify_admin_token` not applied) | ⚠️ Warning | The challenges endpoint returns wine research data. While low-sensitivity, this is inconsistent with the auth applied to POST /trigger. Not a blocker since the endpoint is admin-facing and wine data is not PII. |

---

### Human Verification Required

#### 1. Three-Layer Pipeline Integration Test

**Test:** Start the full stack (`docker-compose up` or `start-all.sh`). Submit a wine record with NULL fields for region, country, grape_variety, color, and sub_region, but with a valid appellation set. Call `research_agent_task(submission_id)` directly.

**Expected:** Layer 1 fills 3–5 fields (region, country, grape_variety, color, sub_region) from ontology at `confidence=0.99` with `source="ontology_inference"`. Layer 2/3 process the remaining NULL fields. `research_run_stats` row shows `time_to_fill_hours > 0.0` and `regression_blocked_count >= 0`.

**Why human:** Requires live Supabase, ontology tables populated from Phase 9, Redis (optional), Serper API key, and Anthropic/Gemini keys. The three-layer flow requires all infrastructure to be running simultaneously.

#### 2. POST /trigger Authentication

**Test:** Using curl or Postman, call `POST /api/v1/research/trigger` (a) without `X-Admin-Key` header, (b) with wrong key, (c) with correct `ADMIN_API_KEY` env value.

**Expected:** (a) HTTP 422 (header required, no default) or 401 if empty env. (b) HTTP 401 with `"Invalid or missing X-Admin-Key"`. (c) HTTP 200 task dispatched.

**Why human:** Requires running FastAPI server with `ADMIN_API_KEY` env var set.

#### 3. Metrics Endpoint Full Validation

**Test:** Run at least one `research_agent_task` against a real wine submission. Then call `GET /api/v1/research/metrics`.

**Expected:** Response JSON contains all 5 top-level keys: `gap_closure`, `quality`, `evidence_hygiene`, `throughput_cost`, `safety`. `evidence_hygiene.fetch_verify_pass_rate_by_tier` has `{"A": float, "B": float, "C": float}`. `safety.regression_rate` is a float (may be 0.0 if no regressions occurred, but computed from DB not hardcoded).

**Why human:** Requires live Supabase with research data populated across `research_run_stats`, `evidence_citations`, `research_runs`, `field_corrections` tables.

#### 4. GET /challenges Not Auth-Protected (Warning Validation)

**Test:** Call `GET /api/v1/research/challenges` without `X-Admin-Key` header.

**Expected (current):** HTTP 200 with challenges list (no auth). Confirm this is acceptable per the project's security policy for admin-facing endpoints.

**Why human:** Requires running FastAPI server. Also requires policy decision on whether the challenges endpoint should require auth.

---

### Gaps Summary

No automated verification gaps found. All 16 observable truths are verified in the codebase. The 4 human verification items above require live infrastructure — they are not code defects but behavioral integration tests that cannot be confirmed statically.

The only notable observation is that `GET /challenges` lacks `Depends(verify_admin_token)` while `POST /trigger` has it. This inconsistency (⚠️ Warning) is the closest item to a gap but is non-blocking given the admin-facing nature and low sensitivity of the data.

---

_Verified: 2026-04-06T22:30:00Z_  
_Verifier: Claude (gsd-verifier)_
